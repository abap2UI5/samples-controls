" @keywords overview catalogue index all samples search sort filter start ports
" @summary Every ported demo kit sample in one searchable, sortable table - control, sample, class and rating - linking to the OpenUI5 original and starting the port in the system.
"! Generated overview app - lists every abap2UI5 api sample app in a table.
"! The search field filters the table on the client (binding_call Contains, no
"! round-trip); its query is two-way bound (search_query), so it survives a
"! round-trip or an app state restore (draft) and view_display re-applies the
"! filter via follow_up_action.
"! The title carries the ported-app count in parentheses. The sortable Since
"! column (next to Control) shows the UI5 release the CONTROL appeared in (from
"! ui5/universe.json; blank when older than tracking / since forever). It is
"! coloured orange (ObjectStatus Warning) when newer than UI5 1.71; a deprecated
"! control's name is struck through (FormattedText htmlText, so the strikethrough
"! can vary per row). There is no per-SAMPLE Since column - whether a sample needs
"! a release newer than 1.71 is carried by the Hide-newer-than-1.71 filter and
"! spelled out in the Open column's info popover.
"! The page has no header of its own: render_header( ) puts a Bar into its
"! customHeader, with the back button and the title on the left and the SHARED
"! OVERVIEW HEADER of the abap2UI5 family on the right - one core:Icon per
"! sample repository, then, set apart by a wider margin (NOT a separator
"! control - a block-level child breaks the row on UI5 1.71, see
"! render_header( )), the documentation and this
"! repository, each explaining itself in its tooltip. Every repository is
"! installed on its own, so each icon decides for itself - class_installed( )
"! instantiates the target class, an installed overview app is entered with
"! nav_app_call( ) (the back button returns), and a missing one opens a popover
"! saying it has to be installed first, with the link to its repository. There
"! are two states and no more: active, and the ONE inactive icon - this
"! repository's own entry, greyed out with no press, so the row reads the same
"! in all three overviews. Keep it in sync with the copies in abap2UI5/samples
"! and abap2UI5/samples-stack.
"! Three header checkboxes (default all on) filter the table
"! entirely on the client via each row's visible expression: Hide non-OpenUI5,
"! Hide newer than 1.71 (2020), Hide deprecated (the ui5_only flag behind the
"! first one has no column of its own - the badge column was dropped 2026-07-29).
"! A Shell switch toggles the
"! sap.m.Shell letterboxing (appWidthLimited), client-side. Navigation lives in
"! the trailing Open column, which carries three buttons, each anchored to its
"! own runtime id. The chain-link one opens the LINKS popover: four full-width
"! Transparent Buttons - Control API Reference, Sample Link, Sample Source Code,
"! abap2UI5 Source Code, each opening its target in a new tab through the
"! URLHELPER REDIRECT frontend action (a Button carries no href, and open_new_tab
"! is same-origin only). The second starts
"! this abap2UI5 app directly in a new tab (open_new_tab; the start URL is
"! same-origin, so it passes isValidRedirectURL) - the overview stays open in its
"! own tab. The trailing information one opens the INFO popover with the
"! port's generation notes - live-check status, the members that need a release
"! newer than 1.71, and the deviation list as a bullet list; it renders only on a
"! row that carries at least one of the three.
"! The Rating column is a 1-5 "by feel" score of
"! how much attention a port deserves (not coloured): app complexity, how heavily
"! it was reworked/corrected (IMPROVISED/DROPPED_171/SUBSET_DATA/NOTE), whether it
"! was reviewed/discussed (it carries a checked block), and how important a live
"! re-test is (pending LIVE_TESTs, roundtrip-free wiring, popups, needs-newer-UI5);
"! 1 = simple faithful 1:1, 5 = complex/reworked/worth a close look. Sort it
"! descending to surface the samples worth a closer manual look.
"! Both popovers are backend round-trips, but the row's press carries only its
"! CLASS: the generation notes, the live-check text and the four reference URLs
"! are looked up from the catalog in on_event, so they never enter the bound
"! model. Only bound columns are public state, which keeps the persisted draft
"! (and the model JSON of every render) small - a transpiled runtime such as
"! the playground re-parses that draft on every round-trip.
"! The search field above the table filters all rows by a
"! substring over the text columns (module, control, since, sample,
"! class) only, and each sortable column header carries ascending/
"! descending sort icons - both run entirely on the frontend
"! (cs_event-binding_call via follow_up_action, no server round-trip). Do not edit
"! by hand - regenerate with scripts/generate-overview.mjs
CLASS z2ui5_cl_smpc_app_000 DEFINITION PUBLIC.

  PUBLIC SECTION.
    INTERFACES z2ui5_if_app.

    " the BOUND row - only what the table renders, sorts or filters on. Every
    " public attribute is part of the app state the framework persists as a
    " draft and re-parses on the next round-trip, so the heavy per-port text
    " (generation notes, live-check note, the four reference URLs) is
    " deliberately NOT here: it is looked up server-side from the catalog when
    " a popover asks for it (see on_event). Keeping it in the model made the
    " draft ~578 kB and every round-trip of a transpiled runtime took
    " ~30 s in the XML parse (which is quadratic there); the split brings the
    " draft to ~199 kB and the round-trip to ~3-4 s on the same machine.
    TYPES:
      BEGIN OF ty_s_row,
        module        TYPE string,
        ctrl_name     TYPE string,
        name          TYPE string,
        class         TYPE string,
        start_url     TYPE string,
        has_check     TYPE abap_bool,
        has_notes     TYPE abap_bool,
        has_p171      TYPE abap_bool,
        since         TYPE string,
        since_post171 TYPE abap_bool,
        ui5_only      TYPE abap_bool,
        is_post171    TYPE abap_bool,
        is_deprecated TYPE abap_bool,
        dep_text      TYPE string,
        ctrl_html     TYPE string,
        score         TYPE i,
        score_tip     TYPE string,
        filter        TYPE string,
      END OF ty_s_row.
    TYPES ty_t_row TYPE STANDARD TABLE OF ty_s_row WITH EMPTY KEY.

    DATA t_app TYPE ty_t_row.
    " the search field's text (two-way, so it survives a round-trip and the
    " draft): the filter itself runs on the client, but only a value that is
    " part of the MODEL comes back when the app is restored. view_display
    " re-applies the filter for a non-initial query via follow_up_action.
    DATA search_query TYPE string.
    " sap.m.Shell letterboxing toggle (two-way, drives Shell appWidthLimited)
    DATA shell_on  TYPE abap_bool.
    " header filter checkboxes (two-way; each row's visible expression binding
    " hides it when the matching flag is set and the row carries that trait)
    DATA hide_non_ui5   TYPE abap_bool.
    DATA hide_post171   TYPE abap_bool.
    DATA hide_deprecated TYPE abap_bool.

  PROTECTED SECTION.
    " the full catalog row - the generated facts plus everything derived from
    " them. It lives only in local variables (get_catalog is a METHOD, never an
    " attribute), so none of it reaches the persisted app state.
    TYPES:
      BEGIN OF ty_s_app,
        module        TYPE string,
        control       TYPE string,
        ctrl_name     TYPE string,
        name          TYPE string,
        class         TYPE string,
        path          TYPE string,
        api_url       TYPE string,
        js_url        TYPE string,
        ui5_url       TYPE string,
        abap_url      TYPE string,
        start_url     TYPE string,
        checked       TYPE string,
        has_check     TYPE abap_bool,
        notes         TYPE string,
        has_notes     TYPE abap_bool,
        post171       TYPE string,
        has_p171      TYPE abap_bool,
        since         TYPE string,
        since_post171 TYPE abap_bool,
        ui5_only      TYPE abap_bool,
        is_post171    TYPE abap_bool,
        is_deprecated TYPE abap_bool,
        dep_text      TYPE string,
        ctrl_html     TYPE string,
        score         TYPE i,
        score_tip     TYPE string,
        filter        TYPE string,
      END OF ty_s_app.
    TYPES ty_t_app TYPE STANDARD TABLE OF ty_s_app WITH EMPTY KEY.

    DATA client TYPE REF TO z2ui5_if_client.

    " the overview apps of the abap2UI5 family, in the order the shared header
    " renders them - each repository is installed on its own, so the header
    " asks per entry whether its overview app is on THIS system
    " sap.ui.core.IconColor knows no blue - Positive, Critical, Negative and
    " Neutral are the semantic four - so the interactive icons of the header
    " carry the accent of the sap_horizon theme as a plain CSS colour, and the
    " one that leads nowhere keeps the semantic grey
    CONSTANTS:
      BEGIN OF cs_color,
        active   TYPE string VALUE `#0064D9`,
        inactive TYPE string VALUE `Neutral`,
      END OF cs_color.

    CONSTANTS:
      BEGIN OF cs_overview,
        samples      TYPE string VALUE `z2ui5_cl_smp_app_000`,
        samples_old  TYPE string VALUE `z2ui5_cl_demo_app_g00`,
        controls     TYPE string VALUE `z2ui5_cl_smpc_app_000`,
        " this overview app before its 2026-08 rename to the three-digit
        " number scheme - an installation that predates it still answers to
        " this name (the dmo-era name is older still and no longer tried)
        controls_old TYPE string VALUE `z2ui5_cl_smpc_app_overview`,
        stack        TYPE string VALUE `z2ui5_cl_smps_app_000`,
        " the overview app of samples-stack before its 2026-08 rename to
        " three-digit app numbers - an installation that predates it still
        " answers to this name
        stack_old    TYPE string VALUE `z2ui5_cl_smps_app_00`,
      END OF cs_overview.

    CONSTANTS:
      BEGIN OF cs_url,
        docs     TYPE string VALUE `https://abap2UI5.org`,
        samples  TYPE string VALUE `https://github.com/abap2UI5/samples`,
        controls TYPE string VALUE `https://github.com/abap2UI5/samples-controls`,
        stack    TYPE string VALUE `https://github.com/abap2UI5/samples-stack`,
      END OF cs_url.

    METHODS on_event.
    METHODS row_of
      IMPORTING
        val           TYPE string
      RETURNING
        VALUE(result) TYPE ty_s_app.
    METHODS view_display.
    METHODS derive
      CHANGING
        app TYPE ty_s_app.
    METHODS get_catalog
      RETURNING
        VALUE(result) TYPE ty_t_app.
    " The header every abap2UI5 overview app shares - see the class
    " documentation. Keep it in sync with the copies in abap2UI5/samples and
    " abap2UI5/samples-stack.
    METHODS render_header
      IMPORTING
        page  TYPE REF TO z2ui5_cl_ui5_view_builder
        title TYPE string.
    " A repository that is not on this system stays clickable and says what is
    " missing - a popover on the icon that was pressed, with the GitHub link to
    " install it from.
    METHODS install_display
      IMPORTING
        anchor TYPE string
        href   TYPE string
        name   TYPE string.
    METHODS header_button
      IMPORTING
        toolbar     TYPE REF TO z2ui5_cl_ui5_view_builder
        icon        TYPE string
        " the entry's name - the tooltip opens with it and the popover of an
        " uninstalled repository is titled after it
        name        TYPE string
        descr       TYPE string
        href        TYPE string
        class       TYPE string OPTIONAL
        " the overview app's PREVIOUS name, tried when CLASS is not on the
        " system: a repository that renamed its overview app is installed under
        " both names in the wild for a while
        class_old   TYPE string OPTIONAL
        here        TYPE abap_bool DEFAULT abap_false
        " this entry opens a new group of the header row, so it carries the
        " wider margin that sets the groups apart - see render_header( )
        group_start TYPE abap_bool DEFAULT abap_false.
    METHODS class_installed
      IMPORTING
        val           TYPE string
      RETURNING
        VALUE(result) TYPE abap_bool.
    METHODS link_press
      IMPORTING
        url           TYPE string
      RETURNING
        VALUE(result) TYPE string.

  PRIVATE SECTION.
ENDCLASS.


CLASS z2ui5_cl_smpc_app_000 IMPLEMENTATION.

  METHOD z2ui5_if_app~main.

    me->client = client.
    IF client->check_on_init( ).
      " default filtering (all on) + Shell on, set once so later round-trips keep
      " whatever the user toggled (the flags are two-way bound)
      shell_on        = abap_true.
      hide_non_ui5    = abap_true.
      hide_post171    = abap_true.
      hide_deprecated = abap_true.
      view_display( ).
    ELSEIF client->check_on_navigated( ).
      view_display( ).
    ELSEIF client->check_on_event( ).
      on_event( ).
    ENDIF.

  ENDMETHOD.


  METHOD on_event.

    " the app the shared header navigates to (WHEN `NAV_APP` below) - declared
    " here because abaplint wants every definition at the top of the routine
    DATA li_app TYPE REF TO z2ui5_if_app.

    CASE client->get_event( ).

      WHEN `LINKS`.
        " the four link buttons for the pressed row, opened in a popover
        " anchored to the button (arg 2). Only the row KEY travels through the
        " client (arg 1, `${CLASS}`) - the URLs are rebuilt here from the
        " catalog, so they never sit in the bound model and never bloat the draft.
        DATA(link) = row_of( client->get_event_arg( ) ).
        DATA(api)  = link-api_url.
        DATA(js)   = link-js_url.
        DATA(ui5)  = link-ui5_url.
        DATA(abap) = link-abap_url.

        DATA(links) = z2ui5_cl_ui5_view_builder=>factory( ).
        DATA(box) = links->ele( n = `FragmentDefinition` ns = `core`
            )->a( n = `xmlns`      v = `sap.m`
            )->a( n = `xmlns:core` v = `sap.ui.core`

            )->ele( `Popover`
                )->a( n = `title`        v = `Links`
                )->a( n = `placement`    v = `Auto`
                )->a( n = `contentWidth` v = `26rem`

                )->ele( `VBox`
                    )->a( n = `class` v = `sapUiContentPadding` ).

        " One full-width Transparent Button per target, stacked in the VBox. A
        " Button cannot carry an href, and cs_event-open_new_tab is same-origin
        " only (isValidRedirectURL), so the press goes through the URLHELPER
        " REDIRECT frontend action with { URL, NEW_WINDOW: true } - the same
        " new-tab behaviour a Link target="_blank" had, client-side and with no
        " round-trip. The URL is also the tooltip, so it stays readable/copyable.
        " The three OpenUI5 targets are empty for a ui5_only row (the control is
        " not in the OpenUI5 checkout), so each renders only when it resolves.
        IF api IS NOT INITIAL.
          box->tag( `Button`
              )->a( n = `text`    v = `Control API Reference`
              )->a( n = `icon`    v = `sap-icon://document-text`
              )->a( n = `type`    v = `Transparent`
              )->a( n = `width`   v = `100%`
              )->a( n = `tooltip` t = api
              )->a( n = `class`   v = `sapUiTinyMarginBottom`
              )->a( n = `press`   v = link_press( api ) ).
        ENDIF.
        IF ui5 IS NOT INITIAL.
          box->tag( `Button`
              )->a( n = `text`    v = `Sample Link`
              )->a( n = `icon`    v = `sap-icon://sys-monitor`
              )->a( n = `type`    v = `Transparent`
              )->a( n = `width`   v = `100%`
              )->a( n = `tooltip` t = ui5
              )->a( n = `class`   v = `sapUiTinyMarginBottom`
              )->a( n = `press`   v = link_press( ui5 ) ).
        ENDIF.
        IF js IS NOT INITIAL.
          box->tag( `Button`
              )->a( n = `text`    v = `Sample Source Code`
              )->a( n = `icon`    v = `sap-icon://source-code`
              )->a( n = `type`    v = `Transparent`
              )->a( n = `width`   v = `100%`
              )->a( n = `tooltip` t = js
              )->a( n = `class`   v = `sapUiTinyMarginBottom`
              )->a( n = `press`   v = link_press( js ) ).
        ENDIF.
        box->tag( `Button`
            )->a( n = `text`    v = `abap2UI5 Source Code`
            )->a( n = `icon`    v = `sap-icon://syntax`
            )->a( n = `type`    v = `Transparent`
            )->a( n = `width`   v = `100%`
            )->a( n = `tooltip` t = abap
            )->a( n = `press`   v = link_press( abap ) ).

        " say why the reference links are missing rather than leaving a gap
        IF api IS INITIAL.
          box->tag( `MessageStrip`
              )->a( n = `text`      v = `This control is in no OpenUI5 checkout, so this sample has no Control API Reference, Sample Link or Sample Source Code.`
              )->a( n = `type`      v = `Information`
              )->a( n = `showIcon`  v = `true`
              )->a( n = `class`     v = `sapUiSmallMarginTop` ).
        ENDIF.

        client->popover_display( xml = links->stringify( ) by_id = client->get_event_arg( 2 ) ).

      WHEN `INFO`.
        " everything the generator knows ABOUT the port (as opposed to where it
        " points): the live-check status, the members that need a UI5 release
        " newer than 1.71, and the deviation notes. Own popover behind the info
        " button, anchored to it (arg 2); the button only renders on a row that
        " carries at least one of the three. Like LINKS, only the row key
        " travels (arg 1) - the texts are read from the catalog here.
        DATA(row)     = row_of( client->get_event_arg( ) ).
        DATA(checked) = row-checked.
        DATA(post171) = row-post171.
        DATA(notes)   = row-notes.

        DATA(info) = z2ui5_cl_ui5_view_builder=>factory( ).
        DATA(ibox) = info->ele( n = `FragmentDefinition` ns = `core`
            )->a( n = `xmlns`      v = `sap.m`
            )->a( n = `xmlns:core` v = `sap.ui.core`

            )->ele( `Popover`
                )->a( n = `title`        v = `Generation notes`
                )->a( n = `placement`    v = `Auto`
                )->a( n = `contentWidth` v = `30rem`

                )->ele( `VBox`
                    )->a( n = `class` v = `sapUiContentPadding` ).

        IF checked IS NOT INITIAL.
          ibox->tag( `ObjectStatus`
              )->a( n = `text`  t = checked
              )->a( n = `state` v = `Success` ).
        ENDIF.

        IF post171 IS NOT INITIAL.
          ibox->tag( `ObjectStatus`
              )->a( n = `text`  t = |Needs a UI5 release newer than 1.71: { post171 }|
              )->a( n = `state` v = `Warning`
              )->a( n = `class` v = `sapUiTinyMarginTop` ).
        ENDIF.

        IF notes IS NOT INITIAL.
          " render the notes as an HTML bullet list (FormattedText): each
          " ` // `-separated bullet becomes one <li> with its leading LABEL
          " (NOTE / IMPROVISED / POST-1.71 / ...) in bold. The note text is
          " HTML-escaped first (it can contain <, >, & - e.g. id="x", a<b, or a
          " literal <strong> mention); the builder's xml_escape escapes it a
          " second time and UI5 un-escapes once, so FormattedText shows it verbatim.
          SPLIT notes AT ` // ` INTO TABLE DATA(t_line).
          DATA(html) = `<ul>`.
          LOOP AT t_line INTO DATA(line).
            DATA(esc) = line.
            REPLACE ALL OCCURRENCES OF `&` IN esc WITH `&amp;`.
            REPLACE ALL OCCURRENCES OF `<` IN esc WITH `&lt;`.
            REPLACE ALL OCCURRENCES OF `>` IN esc WITH `&gt;`.
            DATA(col) = find( val = esc sub = `:` ).
            IF col > 0.
              html = |{ html }<li><strong>{ substring( val = esc len = col + 1 ) }</strong>{ substring( val = esc off = col + 1 ) }</li>|.
            ELSE.
              html = |{ html }<li>{ esc }</li>|.
            ENDIF.
          ENDLOOP.
          html = |{ html }</ul>|.
          ibox->tag( `FormattedText`
              )->a( n = `htmlText` t = html ).
        ENDIF.

        client->popover_display( xml = info->stringify( ) by_id = client->get_event_arg( 2 ) ).

      WHEN `INSTALL`.
        " a header icon whose repository is not on this system - anchor class,
        " GitHub URL and repository name travel as the event arguments
        install_display( anchor = client->get_event_arg( )
                         href   = client->get_event_arg( 2 )
                         name   = client->get_event_arg( 3 ) ).

      WHEN `NAV_APP`.
        " a button of the shared header whose target overview app is on this
        " system - the class travels as the event argument and is resolved
        " here, so a repository that is NOT installed cannot break this one
        DATA(nav) = to_upper( client->get_event_arg( ) ).
        TRY.
            CREATE OBJECT li_app TYPE (nav).
            client->nav_app_call( li_app ).

          CATCH cx_root INTO DATA(lx_nav) ##CATCH_ALL.
            " a press that does nothing at all is the worst answer this header
            " can give, and it is what the silent catch here used to produce.
            " Only the running system knows why the overview app of the other
            " repository did not start, so let it say so.
            client->message_box_display( text = |{ nav }: { lx_nav->get_text( ) }| type = `error` ).
        ENDTRY.

    ENDCASE.

  ENDMETHOD.


  METHOD row_of.

    " the catalog row behind a pressed table row, by its class name (the only
    " thing the press event carries). READ TABLE, not a table expression: the
    " 702 downport turns `tab[ … ]` into a raise the transpiled runtime maps to
    " an uncatchable ASSERT.
    DATA(catalog) = get_catalog( ).
    READ TABLE catalog INTO result WITH KEY class = val.
    IF sy-subrc <> 0.
      CLEAR result.
      RETURN.
    ENDIF.

    derive( CHANGING app = result ).

  ENDMETHOD.


  METHOD view_display.

    DATA(catalog) = get_catalog( ).
    CLEAR t_app.
    LOOP AT catalog ASSIGNING FIELD-SYMBOL(<app>).

      derive( CHANGING app = <app> ).

      " only the columns the table renders, sorts or filters on go into the
      " bound model - the notes, the live-check text and the four URLs stay in
      " the catalog and are fetched per row in on_event (see ty_s_row)
      APPEND VALUE #( module        = <app>-module
                      ctrl_name     = <app>-ctrl_name
                      name          = <app>-name
                      class         = <app>-class
                      start_url     = <app>-start_url
                      has_check     = <app>-has_check
                      has_notes     = <app>-has_notes
                      has_p171      = <app>-has_p171
                      since         = <app>-since
                      since_post171 = <app>-since_post171
                      ui5_only      = <app>-ui5_only
                      is_post171    = <app>-is_post171
                      is_deprecated = <app>-is_deprecated
                      dep_text      = <app>-dep_text
                      ctrl_html     = <app>-ctrl_html
                      score         = <app>-score
                      score_tip     = <app>-score_tip
                      filter        = <app>-filter ) TO t_app.

    ENDLOOP.

    DATA(view) = z2ui5_cl_ui5_view_builder=>factory( ).

    DATA(page) = view->ele( n = `View` ns = `mvc`
        )->a( n = `xmlns`      v = `sap.m`
        )->a( n = `xmlns:mvc`  v = `sap.ui.core.mvc`
        )->a( n = `xmlns:core` v = `sap.ui.core`

        )->ele( `Shell`
            " Shell on/off = letterboxing (limited app width); two-way bound so the
            " header Switch toggles it live on the client
            )->a( n = `appWidthLimited` v = |\{= !!${ client->_bind( shell_on ) } \}|
            )->ele( `Page` ).

    " title and back button come with the custom header (render_header), not
    " with the page - a Page renders either its own header or a custom one
    render_header( page = page title = |abap2UI5 Demo Kit ({ lines( t_app ) })| ).

    page->ele( `subHeader`
        )->ele( `OverflowToolbar`
            " client-side filter over the table: liveChange/search run
            " a binding_call Contains filter via follow_up_action (no round-trip)
            )->tag( `SearchField`
                )->a( n = `placeholder` v = `Search the table - module, control, since, sample, class`
                )->a( n = `width`       v = `24rem`
                " two-way bound so the typed query is part of the model and
                " comes back with the app state (round-trip, draft restore);
                " the filtering itself stays client-side (below)
                )->a( n = `value`       v = client->_bind( search_query )
                )->a( n = `liveChange`  v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `filter` ) ( `FILTER` ) ( `Contains` ) ( `${$parameters>/newValue}` ) ) )
                )->a( n = `search`      v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `filter` ) ( `FILTER` ) ( `Contains` ) ( `${$parameters>/query}` ) ) )
            " default-on filter checkboxes; each is two-way bound and the row
            " visible expression reacts live (no round-trip)
            )->tag( `CheckBox`
                )->a( n = `text`     v = `Hide non-OpenUI5`
                )->a( n = `selected` v = client->_bind( hide_non_ui5 )
                )->a( n = `tooltip`  v = `Hide samples whose control is not part of OpenUI5`
            )->tag( `CheckBox`
                )->a( n = `text`     v = `Hide newer than 1.71 (2020)`
                )->a( n = `selected` v = client->_bind( hide_post171 )
                )->a( n = `tooltip`  v = `Hide samples that need a UI5 release newer than 1.71`
            )->tag( `CheckBox`
                )->a( n = `text`     v = `Hide deprecated`
                )->a( n = `selected` v = client->_bind( hide_deprecated )
                )->a( n = `tooltip`  v = `Hide samples whose control is deprecated`
            )->tag( `ToolbarSpacer`
            )->tag( `Label`
                )->a( n = `text` v = `Shell`
            " Shell on/off = sap.m.Shell letterboxing (two-way, drives appWidthLimited)
            )->tag( `Switch`
                )->a( n = `state`   v = client->_bind( shell_on )
                )->a( n = `tooltip` v = `Toggle the Shell letterboxing (limited app width)`

        )->end(
    )->end(

        )->ele( `Table`
            )->a( n = `id`      v = `idOverviewTable`
            )->a( n = `sticky`  v = `ColumnHeaders`
            )->a( n = `items`   v = client->_bind( t_app )

            )->ele( `columns`
                )->ele( `Column`
                    )->ele( `HBox`
                        )->a( n = `alignItems` v = `Center`

                        )->tag( `Text`
                            )->a( n = `text` v = `Module`
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-ascending`
                            )->a( n = `tooltip` v = `Sort by Module ascending`
                            )->a( n = `class`   v = `sapUiTinyMarginBegin`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `MODULE` ) ) )
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-descending`
                            )->a( n = `tooltip` v = `Sort by Module descending`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `MODULE` ) ( `X` ) ) )

                    )->end(
                )->end(
                )->ele( `Column`
                    )->ele( `HBox`
                        )->a( n = `alignItems` v = `Center`

                        )->tag( `Text`
                            )->a( n = `text` v = `Control`
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-ascending`
                            )->a( n = `tooltip` v = `Sort by Control ascending`
                            )->a( n = `class`   v = `sapUiTinyMarginBegin`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `CTRL_NAME` ) ) )
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-descending`
                            )->a( n = `tooltip` v = `Sort by Control descending`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `CTRL_NAME` ) ( `X` ) ) )

                    )->end(
                )->end(
                )->ele( `Column`
                    )->ele( `HBox`
                        )->a( n = `alignItems` v = `Center`

                        )->tag( `Text`
                            )->a( n = `text` v = `Since`
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-ascending`
                            )->a( n = `tooltip` v = `Sort by Since ascending`
                            )->a( n = `class`   v = `sapUiTinyMarginBegin`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `SINCE` ) ) )
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-descending`
                            )->a( n = `tooltip` v = `Sort by Since descending`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `SINCE` ) ( `X` ) ) )

                    )->end(
                )->end(
                )->ele( `Column`
                    )->ele( `HBox`
                        )->a( n = `alignItems` v = `Center`

                        )->tag( `Text`
                            )->a( n = `text` v = `Sample`
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-ascending`
                            )->a( n = `tooltip` v = `Sort by Sample ascending`
                            )->a( n = `class`   v = `sapUiTinyMarginBegin`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `NAME` ) ) )
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-descending`
                            )->a( n = `tooltip` v = `Sort by Sample descending`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `NAME` ) ( `X` ) ) )

                    )->end(
                )->end(
                )->ele( `Column`
                    )->ele( `HBox`
                        )->a( n = `alignItems` v = `Center`

                        )->tag( `Text`
                            )->a( n = `text` v = `abap2UI5`
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-ascending`
                            )->a( n = `tooltip` v = `Sort by abap2UI5 ascending`
                            )->a( n = `class`   v = `sapUiTinyMarginBegin`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `CLASS` ) ) )
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-descending`
                            )->a( n = `tooltip` v = `Sort by abap2UI5 descending`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `CLASS` ) ( `X` ) ) )

                    )->end(
                )->end(
                )->ele( `Column`
                    )->ele( `HBox`
                        )->a( n = `alignItems` v = `Center`

                        )->tag( `Text`
                            )->a( n = `text` v = `Rating`
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-ascending`
                            )->a( n = `tooltip` v = `Sort by Rating ascending`
                            )->a( n = `class`   v = `sapUiTinyMarginBegin`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `SCORE` ) ) )
                        )->tag( `core:Icon`
                            )->a( n = `src`     v = `sap-icon://sort-descending`
                            )->a( n = `tooltip` v = `Sort by Rating descending`
                            )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-binding_call t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `sort` ) ( `SCORE` ) ( `X` ) ) )

                    )->end(
                )->end(
                )->ele( `Column`
                    )->a( n = `width` v = `9rem`
                    )->a( n = `hAlign` v = `Center`

                    )->tag( `Text`
                        )->a( n = `text` v = `Open`

                )->end(
            )->end(

            )->ele( `items`
                )->ele( `ColumnListItem`
                    " header checkboxes filter the table entirely on the client: a
                    " row is hidden when a hide-flag (two-way bound model-root) is set
                    " AND the row carries that trait (UI5_ONLY / IS_POST171 /
                    " IS_DEPRECATED). Expression binding, re-evaluated live on toggle,
                    " no round-trip - like the Shell Switch.
                    )->a( n = `visible` v = |\{= !(${ client->_bind( hide_non_ui5 ) } && $\{UI5_ONLY\}) && !(${ client->_bind( hide_post171 ) } && $\{IS_POST171\}) && !(${ client->_bind( hide_deprecated ) } && $\{IS_DEPRECATED\}) \}|
                    )->ele( `cells`
                        )->tag( `Text`
                            )->a( n = `text` v = `{MODULE}`
                        " control name, struck through when deprecated (never
                        " coloured); FormattedText so the strikethrough can vary per row
                        )->tag( `FormattedText`
                            )->a( n = `htmlText` v = `{CTRL_HTML}`
                            )->a( n = `tooltip`  v = `{DEP_TEXT}`
                        " Since: the release the control appeared in; coloured orange
                        " (Warning) when it is newer than UI5 1.71
                        )->tag( `ObjectStatus`
                            )->a( n = `text`    v = `{SINCE}`
                            )->a( n = `state`   v = |\{= $\{SINCE_POST171\} ? 'Warning' : 'None' \}|
                            )->a( n = `tooltip` v = `{DEP_TEXT}`
                        )->tag( `Text`
                            )->a( n = `text` v = `{NAME}`
                        )->tag( `Text`
                            )->a( n = `text` v = `{CLASS}`
                        " rating 1-5 (by feel): how much attention the port
                        " deserves - complexity, rework, review, test-priority
                        " (not coloured); tooltip lists the drivers
                        )->tag( `Text`
                            )->a( n = `text`    v = `{SCORE} / 5`
                            )->a( n = `tooltip` v = `{SCORE_TIP}`

                        " Open column: three buttons, each anchored to its own runtime
                        " id ($event.oSource.sId). First opens the links popover (the
                        " four reference targets); second launches the abap2UI5 app
                        " directly in a new tab (open_new_tab - the start URL is
                        " same-origin, so it passes isValidRedirectURL), leaving the
                        " overview open in its own tab; third opens the
                        " generation-notes popover - shown only on a row that HAS
                        " something to say (checked / post-1.71 / notes)
                        )->ele( `HBox`
                            )->tag( `Button`
                                )->a( n = `icon`    v = `sap-icon://chain-link`
                                )->a( n = `type`    v = `Transparent`
                                )->a( n = `tooltip` v = `Links: Control API Reference, Sample Link, Sample Source Code, abap2UI5 Source Code`
                                " only the row KEY travels (the four URLs are rebuilt
                                " server-side in on_event, so they stay out of the model
                                " and out of the persisted draft), plus the button's own
                                " runtime id as the popover anchor
                                )->a( n = `press`   v = client->_event( val = `LINKS` t_arg = VALUE #(
                                    ( `${CLASS}` ) ( `$event.oSource.sId` ) ) )
                            )->tag( `Button`
                                )->a( n = `icon`    v = `sap-icon://action`
                                )->a( n = `type`    v = `Transparent`
                                )->a( n = `tooltip` v = `Start this abap2UI5 app in a new tab`
                                )->a( n = `press`   v = client->follow_up_action( val = client->cs_event-open_new_tab t_arg = VALUE #( ( `${START_URL}` ) ) )
                            )->tag( `Button`
                                )->a( n = `icon`    v = `sap-icon://message-information`
                                )->a( n = `type`    v = `Transparent`
                                )->a( n = `tooltip` v = `Generation notes: how this port was built - live-check status, post-1.71 members, deviations`
                                " a backtick literal, not a |…| template: ABAP ends a string
                                " template at the next |, so the expression binding's || would
                                " close it mid-way. Nothing here needs interpolation anyway.
                                )->a( n = `visible` v = `{= ${HAS_CHECK} || ${HAS_P171} || ${HAS_NOTES} }`
                                )->a( n = `press`   v = client->_event( val = `INFO` t_arg = VALUE #(
                                    ( `${CLASS}` ) ( `$event.oSource.sId` ) ) )

                        )->end(
                    )->end(
                )->end(
            )->end(
        )->end( ).

    client->view_display( view->stringify( ) ).

    " Re-apply the client-side table filter for a restored query. The filter is
    " a frontend-only binding operation (the model keeps all rows), so a rebuilt
    " view starts unfiltered - while the SearchField, being two-way bound, does
    " show the query again. follow_up_action runs the very same binding_call
    " after the view was rendered, so both are back in sync.
    IF search_query IS NOT INITIAL.
      client->follow_up_action( val   = client->cs_event-binding_call
                                t_arg = VALUE #( ( `idOverviewTable` ) ( `items` ) ( `filter` ) ( `FILTER` ) ( `Contains` ) ( search_query ) ) ).
    ENDIF.

  ENDMETHOD.


  METHOD derive.

    " everything a catalog row does not carry as a generated fact: the bare
    " control name, the four reference links, the start URL, the three
    " has-something flags, the control's display markup and the search blob.
    " Called per row when the table is built, and for the single row a popover
    " asks about (row_of) - so the links exist without living in the model.
    DATA(libpath) = replace( val = app-module
                             sub = `.`
                             with = `/`
                             occ = 0 ).

    " display only the bare control, without its namespace (sap.f.GridList -> GridList)
    DATA(dot) = find( val = app-control sub = `.` occ = -1 ).
    app-ctrl_name = COND #( WHEN dot >= 0 THEN substring( val = app-control off = dot + 1 ) ELSE app-control ).

    " the three reference links point into OpenUI5 - API reference, sample
    " source, live runner - so they only exist for a library OpenUI5 ships.
    " A ui5_only row (control not in the OpenUI5 checkout) has none of the
    " three there: leaving them built would hand out four links of which three
    " 404, and the commercial host is not an option (pattern-lint
    " commercial-ui5-host).
    " They stay empty and the popover renders only what resolves - the ABAP
    " class link is repository-local and always correct
    IF app-ui5_only = abap_false.
      app-api_url = |https://sdk.openui5.org/api/{ app-control }|.
      app-js_url  = |https://github.com/SAP/openui5/tree/master/src/{ app-module }| &&
                    |/test/{ libpath }/demokit/sample/{ app-name }|.
      app-ui5_url = |https://sdk.openui5.org/resources/sap/ui/documentation/sdk/index.html| &&
                    |?sap-ui-xx-sample-id={ app-module }.sample.{ app-name }| &&
                    |&sap-ui-xx-sample-lib={ app-module }|.
    ENDIF.
    app-abap_url  = |https://github.com/abap2UI5/samples-controls/blob/main/{ app-path }|.
    app-start_url = |{ client->get( )-s_config-origin }{ client->get( )-s_config-pathname }| &&
                    |?app_start={ to_upper( app-class ) }|.
    app-has_check = xsdbool( app-checked IS NOT INITIAL ).
    app-has_notes = xsdbool( app-notes IS NOT INITIAL ).
    app-has_p171  = xsdbool( app-post171 IS NOT INITIAL ).

    " control name: struck through when the control is deprecated, otherwise
    " plain - never coloured (carried as FormattedText htmlText so the
    " strikethrough can vary per row); a plain control is rendered as-is
    app-ctrl_html = COND string(
        WHEN app-dep_text IS NOT INITIAL
        THEN |<span style="text-decoration:line-through">{ app-ctrl_name }</span>|
        ELSE app-ctrl_name ).

    " one blob per row, bound as the FILTER column that the table search's
    " client-side Contains filter (binding_call) matches against. Only the
    " VISIBLE text columns feed it - Module, Control (bare name), Since,
    " Sample, abap2UI5 (class) - so a query like "Date" no longer
    " matches hidden text buried in the notes/checked/post-1.71 fields
    app-filter = app-module && ` ` && app-ctrl_name && ` ` &&
                 app-since  && ` ` && app-name      && ` ` &&
                 app-class.

  ENDMETHOD.


  METHOD get_catalog.

    " A single VALUE #( ) holding every catalog row exceeds the maximum permitted
    " ABAP statement length, so the generator emits the catalog in size-bounded
    " chunks (a few rows each); every chunk after the first appends to the
    " previous ones via VALUE #( BASE result ). Texts too long to fit into their
    " own row are assigned to textN just ahead of it - such a row is always
    " alone in its statement, because the next hoisting row reuses the variable.
    "
    " abap2ui5lint-disable -- this method is DATA, not code: every literal
    " below quotes a sidecar deviation verbatim, and a linter rule that reads
    " the SOURCE for a call shape - popover_display( by_id = `` ), a {/path}
    " written as text, a sap-icon:// name - reads that prose as this class's
    " own code. Measured 2026-09-12: 24 findings on this class, 23 of them in
    " this method and every one of them a quote (the real one was the INFO
    " button's icon in view_display). The block ends at the -enable below, so
    " the overview's actual view code stays fully judged.
    DATA text1 TYPE string.
    DATA text2 TYPE string.

    result = VALUE #(
      ( module = `sap.f`              control = `sap.f.Card`                            name = `Card`                                          class = `z2ui5_cl_smpc_app_117` path = `src/01/04/z2ui5_cl_smpc_app_117.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.64`
        notes = `NOTE: **Rebuilt 1:1 on 2026-08-05** - it was a breadth probe that showed one simplified card. Both cards of the sample are there now: the booking card with its two sorted city ComboBoxes, the` &&
                 ` DatePicker and the Book button (whose fixed explanation toast is composed on the client), and the whole second card - the revenue List with its CustomListItem template, Title/Text and the` &&
                 ` ObjectStatus state. The two named models fold into the one default model (cities> -> T_CITIES, products> -> T_PRODUCTS, last path segment identical) and the ComboBox sorter rides along as a raw` &&
                 ` binding-info string. The port has NO structural difference from the original left, so its structural_diff skip is gone - the gate reported it stale, which is what that ratchet is for.` ) ).

    text1 = `POST-1.71: one post-1.71 member is kept for the 1:1 port: sap.ui.table.Table.rowMode="Auto" (the rowMode aggregation replaced visibleRowCountMode in UI5 1.119); it is what makes the table fill the` &&
            ` DynamicPage's fitContent area. // NOTE: Card.fragment.xml is rebuilt as a core:FragmentDefinition and shown with client->popover_display( by_id ), anchored to the pressed GenericTag exactly as` &&
            ` onGenericTagPress's openBy(oSourceControl) does (app 229 idiom) - so the Popover, the f:Card, its card:NumericHeader and the two card:NumericSideIndicators are extra against the archived` &&
            ` DynamicPageAnalyticalTable.view.xml. The original caches the fragment and re-opens the same instance; the port rebuilds it per press, which is equivalent because the card is entirely static. // NOTE:` &&
            ` the two frontend formatters are computed in the ABAP backend (thin-frontend rule, app 115 does the same for the same table): formatAvailableToObjectState becomes the AVAILABLESTATE field bound to` &&
            ` ObjectStatus.state, and formatAvailableToIcon becomes AVAILABLEICON bound to c:Icon.src. Both are derived from Status = 'Available', exactly as initSampleDataModel derives Available. // NOTE:`.
    text1 = text1 && ` initSampleDataModel is reproduced in model_init: the full mock /ProductCollection (123 rows) plus the Suppliers and Categories arrays it derives from the distinct SupplierName / Category values, the` &&
            ` Heavy flag (WeightMeasure > 1000 as the string 'true'/'false', which is why the CheckBox keeps its sap.ui.model.type.String binding) and headerExpanded, which the DynamicPage binds. // NOTE:` &&
            ` DeliveryDate is Date.now() - (i % 10 * 4 days) in the original - a moving value, so it is anchored on a FIXED base date here (the corpus rule for now/random values, apps 164/181/289) and carried as` &&
            ` an ISO date STRING. The DatePicker keeps its sap.ui.model.type.Date binding but takes an ISO source pattern instead of the original's 'timestamp': the model carries a date string, not a JS epoch` &&
            ` number (CAPABILITIES date row). DeliveryDateStr, which the controller computes and no control binds, has no counterpart. // NOTE: handleDetailsPress composes its MessageToast from the pressed row's` &&
            ` ProductId; the row field resolves on the client, so it stays there as a roundtrip-free control_global MESSAGE_TOAST show with the {0} placeholder - the same wire app 115 uses for the same button. //`.
    text1 = text1 && ` NOTE: the icon-only share Button in the title's actions is text-less and tooltip-less in the sample. The port gives it tooltip="Share" so it is reachable with a screen reader - the one accessibility` &&
            ` addition in this port. // NOTE: not yet verified in a running system: the DynamicPage with a sap.ui.table.Table in fitContent mode and the card popover anchored on the GenericTag. **e2e-verified` &&
            ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_559.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...),` &&
            ` which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the` &&
            ` rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.DynamicPage`                     name = `DynamicPageAnalyticalTable`                    class = `z2ui5_cl_smpc_app_559` path = `src/02/04/z2ui5_cl_smpc_app_559.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.42`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 member is kept for the 1:1 port: sap.ui.table.Table.rowMode="Auto" (the rowMode aggregation replaced visibleRowCountMode in UI5 1.119); it is what makes the table fill the DynamicPage's` &&
                 ` fitContent area.` ) ).

    text1 = `NOTE: The controller's Card popover (onPressOpenPopover, wired to the GenericTag press and the 'Button with layoutData' press) is reproduced 1:1 since 2026-07-30: both presses fire OPEN_POPOVER with` &&
            ` $event.oSource.sId and the backend rebuilds view/Card.fragment.xml verbatim (Popover placement Bottom / showHeader false / contentWidth 300px > f:Card > card:NumericHeader 'Sales Revenue' with the` &&
            ` two card:NumericSideIndicator Target/Deviation rows) shown via popover_display( by_id = pressed control ) - the Fragment.load + openBy equivalent. The fragment's Popover, Card, NumericHeader and` &&
            ` NumericSideIndicator controls are counted by structural-diff against the union that includes the original's own Card.fragment.xml, so the counts match; both press attributes are restored 1:1. //` &&
            ` NOTE: The Edit button's press='.toggleAreaPriority' is reproduced since 2026-07-30: DynamicPageTitle.areaShrinkRatio is two-way bound ({/AREASHRINKRATIO}, an added attribute vs the original view -` &&
            ` declared here) seeded with the metadata default 1:1.6:1.6, and the TOGGLE_AREA_PRIORITY round-trip flips it to 1.6:1:1.6 and back (the controller read the default from`.
    text1 = text1 && ` getMetadata().getProperty('areaShrinkRatio').getDefaultValue(); the port seeds the same literal - re-verify should UI5 ever change the default). // NOTE: showFooter is two-way bound to a model flag` &&
            ` ({/SHOWFOOTER}, default false) and the 'Toggle Footer' button flips it via a TOGGLE_FOOTER round-trip + re-render; the original toggles it imperatively (setShowFooter). headerExpanded and` &&
            ` toggleHeaderOnTitleClick are likewise bound to model flags (both default true) - the original binds {/headerExpanded}/{/titleClickable} against a model that never sets them, so they fall back to the` &&
            ` control defaults, reproduced here explicitly. // NOTE: The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined with the columns the table binds (Name, ProductId,` &&
            ` SupplierName, Width, Depth, Height, DimUnit, Price, CurrencyCode). The Price/CurrencyCode Currency composite type binding and the items sorter (path 'Name') are kept 1:1 as raw binding-info strings.` &&
            ` // NOTE: Width/Depth/Height are TYPE string (not i): the mock carries decimal dimensions (40.8, 3.1) and they bind display-only into a text template ({WIDTH} x {DEPTH} x {HEIGHT} {DIMUNIT}), so a`.
    text1 = text1 && ` string keeps the exact value; TYPE i would truncate (an earlier version did, via json-to-abap's first-row inference — tool since fixed). Price stays packed (DECIMALS 2) for the Currency type binding,` &&
            ` decimals preserved.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.DynamicPage`                     name = `DynamicPageFreeStyle`                          class = `z2ui5_cl_smpc_app_170` path = `src/01/04/z2ui5_cl_smpc_app_170.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.Avatar (control @since 1.73, incl. its src/class/press members) is used 1:1 in snappedContent and the DynamicPageHeader; needs UI5 >= 1.73. DynamicPage is the in-scope headline` &&
            ` control (@1.42); Avatar is a secondary control kept 1:1 per the post-1.71 member policy. // POST-1.71: sap.f.DynamicPage.breakpointChange (@since 1.147, incl. its currentRange/currentWidth` &&
            ` parameters) is the whole point of this sample and is wired 1:1 since 2026-07-30 as a view attribute (an added attr - the original attaches it imperatively in onInit via attachBreakpointChange):` &&
            ` BREAKPOINT_CHANGE transports ${$parameters>/currentRange} and ${$parameters>/currentWidth}, the backend maps Phone->M / Tablet->L / else XL into the two-way bound Avatar displaySize ({/AVATAR_SIZE},` &&
            ` an added attribute on both Avatars, seeded XL) and toasts Media Range: <range> (<width>px) exactly like onBreakpointChange. @since verified fork-openui5/src/sap.f/src/sap/f/DynamicPage.js:315.` &&
            ` Requires a UI5 release >= 1.147; on older releases the event never fires and the Avatars keep the seeded XL (the property gate is blind to sap.f event params). // NOTE: showFooter is two-way bound`.
    text1 = text1 && ` ({/SHOW_FOOTER}, default true) and the Toggle Footer button flips it on a round-trip - the faithful abap2UI5 form of the controller's setShowFooter(!getShowFooter()); the scalar literal` &&
            ` showFooter="true" -> binding is not a structural diff. The Home/Examples/Avatar presses raise client-composed MessageToasts via follow_up_action control_global MESSAGE_TOAST (roundtrip-free, apps` &&
            ` 005/060). Both the footer round-trip and the toast wiring are unverified in a running system. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_244.mjs).` &&
            ` (Scope corrected 2026-08-23: the module sets a 500px viewport and asserts the media-range toast. It never presses Toggle Footer and never presses Home/Examples/the Avatar, so neither item this` &&
            ` deviation calls unverified is covered - what the module does prove belongs to the breakpointChange deviation.) // NOTE: The two Avatar src values point at the sdk.openui5.org host` &&
            ` (https://sdk.openui5.org/test-resources/sap/uxap/images/imageID_275314.png) per the offline asset-URL rule; the original uses the relative ./test-resources path. Literal src values are not compared` &&
            ` by structural-diff.`.
    text2 = `sap.m.Avatar (control @since 1.73, incl. its src/class/press members) is used 1:1 in snappedContent and the DynamicPageHeader; needs UI5 >= 1.73. DynamicPage is the in-scope headline control (@1.42);` &&
            ` Avatar is a secondary control kept 1:1 per the post-1.71 member policy. // sap.f.DynamicPage.breakpointChange (@since 1.147, incl. its currentRange/currentWidth parameters) is the whole point of this` &&
            ` sample and is wired 1:1 since 2026-07-30 as a view attribute (an added attr - the original attaches it imperatively in onInit via attachBreakpointChange): BREAKPOINT_CHANGE transports` &&
            ` ${$parameters>/currentRange} and ${$parameters>/currentWidth}, the backend maps Phone->M / Tablet->L / else XL into the two-way bound Avatar displaySize ({/AVATAR_SIZE}, an added attribute on both` &&
            ` Avatars, seeded XL) and toasts Media Range: <range> (<width>px) exactly like onBreakpointChange. @since verified fork-openui5/src/sap.f/src/sap/f/DynamicPage.js:315. Requires a UI5 release >= 1.147;` &&
            ` on older releases the event never fires and the Avatars keep the seeded XL (the property gate is blind to sap.f event params).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.DynamicPage`                     name = `DynamicPageResponsiveAvatar`                   class = `z2ui5_cl_smpc_app_244` path = `src/02/04/z2ui5_cl_smpc_app_244.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: showFooter is two-way bound to a model flag ({/SHOWFOOTER}, default false) and the 'Toggle Footer' button flips it via a TOGGLE_FOOTER round-trip + re-render; the original controller toggles it` &&
            ` imperatively (onToggleFooter -> setShowFooter). The original view has no showFooter attribute (it defaults false), so adding the bound attribute is the faithful thin-frontend form of the imperative` &&
            ` toggle and structural-diff does not flag it (it only flags missing attributes). toggleHeaderOnTitleClick keeps the original {/titleClickable} binding (default true - the original binds it against a` &&
            ` model that never sets it, so it falls back to the control default). // NOTE: onGenericTagPress lazily Fragment.loads Card.fragment.xml (a Popover > sap.f.Card > sap.f.cards.NumericHeader with two` &&
            ` sap.f.cards.NumericSideIndicator) and openBy(the pressed GenericTag). The port builds that same popover server-side on the GENERIC_TAG_PRESS round-trip and shows it anchored to the pressed GenericTag` &&
            ` via client->popover_display( xml = ..., by_id = $event.oSource.sId ) - the popover XML parameter is named ``xml`` (not ``val`` like popup_display). Faithful (lazy either way), matches app 229; the`.
    text1 = text1 && ` GenericTag.press attribute is kept (rewired to the abap2UI5 event). All Card/NumericHeader/NumericSideIndicator controls of the fragment are present in the port (built in popover_card), so the` &&
            ` fragment union in structural-diff is satisfied. // NOTE: The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined with the columns the Products table binds (Name,` &&
            ` ProductId, SupplierName, Width, Depth, Height, DimUnit, Price, CurrencyCode). The items sorter (path 'Name') and the Price/CurrencyCode Currency composite type binding are kept 1:1 as raw` &&
            ` binding-info strings (path upper-cased to the ABAP field names). Width/Depth/Height are TYPE string because the mock carries decimal values (40.8, 3.1) bound display-only into a text template` &&
            ` ({WIDTH} x {DEPTH} x {HEIGHT} {DIMUNIT}); Price stays packed (DECIMALS 2) for the Currency type binding. // NOTE: stickySubheaderProvider='iconTabBar' is kept 1:1 as the association to the content` &&
            ` IconTabBar (id='iconTabBar'); it is a control-id association resolved by XMLView.create within the same view. stickySubheaderProvider is @since 1.65 (<= 1.71, base) - no POST_171 needed. All`.
    text1 = text1 && ` Card/NumericHeader/GenericTag members used are <= 1.71. // NOTE: unverified in a running system: that the Card popover opens anchored to the pressed GenericTag (popover_display by_id =` &&
            ` $event.oSource.sId), and that the Toggle Footer round-trip flips showFooter and reveals/hides the DynamicPage footer. **e2e-verified 2026-07-30** (transpiled-framework interaction,` &&
            ` scripts/e2e-smoke.mjs): the GenericTag press opens the Card popover with the NumericHeader rendering ('Sales Revenue'; asserted on text - the popover box measures empty headless); the` &&
            ` layoutData-button variant is the identical wire. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_238.mjs). // NOTE: TOGGLE_FOOTER writes the bound showFooter` &&
            ` field and nothing else since 2026-08-23. It re-rendered the whole view until then, which the original's onToggleFooter never does - a view_display( ) destroys the MAIN slot and builds a new XMLView,` &&
            ` so every press snapped the IconTabBar back to the Products tab (neither side declares a selectedKey) and reset the pinnable header and the scroll position. The bound property needs no re-render: the` &&
            ` model diff is pushed automatically.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.DynamicPage`                     name = `DynamicPageWithStickySubheader`                class = `z2ui5_cl_smpc_app_238` path = `src/01/04/z2ui5_cl_smpc_app_238.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `NOTE: this sample is sap.m.sample.WizardBranching (app 535) with its wizard page replaced by an f:DynamicPage, and the port follows that: the same eight-step branching wizard and the same review page,` &&
            ` wrapped in a DynamicPage (id dynamicPage, class sapUiNoContentPadding, showFooter, stickySubheaderProvider=ShoppingCartWizard) whose f:title carries the 'Header Title' Title and whose f:footer` &&
            ` carries the Accept/Reject OverflowToolbar. The other differences from the sibling are kept 1:1 as well: the wizard gains height="auto", its four step forms use ColumnLayout instead of` &&
            ` ResponsiveGridLayout, the review page's product List drops mode/enableBusyIndicator, and the wizard page's footer buttons are the handler-less Accept/Reject pair (Submit/Cancel live on the review` &&
            ` page, as in the sibling). // NOTE: toggleHeaderOnTitleClick="{/titleClickable}" is kept as a bound field. The controller never writes /titleClickable, so the binding resolves to undefined in the` &&
            ` original and the property falls back to its default; the port binds an abap_bool that stays false, which is what undefined evaluates to. // NOTE: The original keeps the form data in nested paths on`.
    text1 = text1 && ` one JSON model (/CreditCard/Name, /CashOnDelivery/FirstName, /BillingAddress/Address ...). abap2UI5 keeps one default model, so the nested paths are folded to flat fields whose last segment is` &&
            ` identical - the same fold app 166 makes. /CashOnDelivery/Phone Number, whose last segment carries a space, becomes phonenumber. // IMPROVISED: goToPaymentStep and billingAddressComplete branch the` &&
            ` wizard from the COMPLETE handler, and that cannot be reproduced 1:1. WizardStep._complete fires complete and then calls Wizard._handleNextButtonPress in the SAME tick (sap/m/WizardStep.js), so a` &&
            ` nextStep that only arrives with the abap2UI5 round trip is one press too late. Measured against real OpenUI5 in headless Chromium with the port's eight step declarations: with the branch left to the` &&
            ` complete round trip the FIRST press on PaymentTypeStep threw 'The wizard is in branching mode, and no next step is defined for the current step, please set one' out of Wizard._getNextStep, the` &&
            ` progress stayed on PaymentTypeStep, and only the SECOND press advanced; with the branch already set the same press advanced straight to CreditCardStep. Unlike app 534 the button really is reachable -`.
    text1 = text1 && ` PaymentTypeStep declares no validated, so it defaults to true and WizardStep.setButtonVisibility displays its Next button from the first render (measured buttonDisplayed=true). The port therefore` &&
            ` sends the branch BEFORE the press instead of on it: PaymentTypeStep carries nextStep=CreditCardStep - the payment default model_init seeds - as a declared association the original does not have, and` &&
            ` branch_payment re-sends the current choice from the SegmentedButton's selectionChange and from the YES branch of the discard confirmation. Both branch methods are ALSO called unconditionally at the` &&
            ` end of view_display( ), which is what makes the branch survive a rebuild - without it PaymentTypeStep falls back to its static nextStep whatever the user chose, and BillingStep comes back with no` &&
            ` branch at all, which is the "wizard is in branching mode and no next step is defined" throw. Deleting those two calls as unaccounted-for restores it. BillingStep needs no declared association: its` &&
            ` Next button is gated behind validated, only a CHECK_BILLING answer can raise that flag, and CHECK_BILLING is also the step's activate wire - so the branch_delivery call it carries always lands before`.
    text1 = text1 && ` the button can appear (measured: at the activate event validated=false and the button is hidden; the deferred answer sets nextStep, and only then does validated flip). The complete wires stay in the` &&
            ` view and re-assert the same value, as goToPaymentStep and billingAddressComplete do. Cost of the declared association: the progress navigator reads 4 (varying) at load where the original reads 2` &&
            ` (varying) - the port shows from the start what the original shows once the payment branch is chosen. BillingStep's own reveal is unchanged, and declaring its nextStep too would have cost the varying` &&
            ` indication altogether (measured 5 (fixed)), which is why it is not declared. // NOTE: checkCreditCardStep / checkCashOnDeliveryStep / checkBillingStep call validateStep and invalidateStep` &&
            ` imperatively. Each step's validated property is bound two-way here and the same liveChange wires recompute it in ABAP with the same rule (at least three characters), so the Next button gates exactly` &&
            ` as in the original. // IMPROVISED: setDiscardableProperty only asks for confirmation while the wizard's PROGRESS STEP is past the step being discarded (getProgressStep() !== discardStep). A backend`.
    text1 = text1 && ` cannot read getProgressStep(), so the port keeps two flags that stand in for the two comparisons: each is raised by the activate wire of the step that follows the compared one, and cleared again when` &&
            ` the progress is discarded. This costs two added activate attributes (BankAccountStep, DeliveryAddressStep and DeliveryTypeStep, which the sample leaves unwired) and gets the question asked in the` &&
            ` same situations. The YES branch discards the progress through control_by_id and remembers the new value; the NO branch restores the remembered one, both as in the original. // NOTE: handleDelete` &&
            ` removes the row whose title the delete event carries and never removes the last one; the port sends the title with the event and does the same, then recomputes the total the way calcTotal does.` &&
            ` attachRequestCompleted keeps the first FIVE rows of the mock product collection and seeds the payment and delivery defaults - the port seeds exactly those five rows. // NOTE: completedHandler,` &&
            ` _navBackToStep and _handleMessageBoxOpen navigate the NavContainer and the wizard imperatively. All three go through control_by_id here ('to', 'goToStep', 'discardProgress' - all whitelisted), the`.
    text1 = text1 && ` same idiom app 101 uses for the sibling sample. _navBackToStep's backToPage/attachAfterNavigate pair collapses to one 'to' on the DynamicPage followed by 'goToStep' - abap2UI5 schedules the two` &&
            ` frontend actions in order, so the after-navigate callback the original needs has no counterpart. // NOTE: The five product images are the demo kit's own test-resources files, re-hosted on` &&
            ` sdk.openui5.org. // NOTE: The **e2e-verified 2026-08-25** marker on this port overstated what the nightly covered: the interaction module of that day asserted the cart rows, the calcTotal sum, the` &&
            ` eight steps and the row delete and never pressed Next, so no branch was ever exercised and the broken first press went unseen. meta/interactions/z2ui5_cl_smpc_app_560.mjs now walks ContentsStep ->` &&
            ` PaymentTypeStep -> CreditCardStep and fails if one press does not reach the branch, and it pins PaymentTypeStep's declared nextStep so a rebuild that loses the association is caught. Still unverified` &&
            ` in a running system: the Bank Transfer and Cash on Delivery branches, the delivery-address branch, the two discard confirmations, the DynamicPage's sticky subheader over the wizard and the review`.
    text1 = text1 && ` page's six Edit links.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.DynamicPage`                     name = `DynamicPageWithWizard`                         class = `z2ui5_cl_smpc_app_560` path = `src/01/04/z2ui5_cl_smpc_app_560.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `POST-1.71: two post-1.71 members are kept for the 1:1 port: sap.f.FlexibleColumnLayout.autoFocus (since UI5 1.76) and restoreFocusOnBackNavigation (since 1.77), both set by the sample's own FCL view.` &&
            ` // NOTE: the sample is a Component with a ROUTER and three views (FlexibleColumnLayout, List, Detail). abap2UI5 serves one view, so the three become one: the FCL declares its begin and mid column` &&
            ` pages inline and the routing collapses to the FCL's own layout property - toDetail opens the mid column (MidColumnFullScreen - see the maxColumnsCount note below) and handleClose closes it` &&
            ` (OneColumn), which is what navTo(layout) does. Since 2026-08-31 the URL the router writes IS reproduced, via the framework's app-owned hash routing (cs_event-hash_attach_changed + hash_set, needs` &&
            ` abap2UI5 newer than 1.144.0 - abap2UI5#2693, the app-012/584 recipe): toDetail pushes '#/detail/{layout}', handleClose returns the URL to the bare start - the empty ':section:' pattern, pushed as '/'` &&
            ` which the Router normalizes to the empty app hash - the ':section:' list route ('#/3') carries the anchor-bar selection (see the section deviation), a hash change the app did not write (browser`.
    text1 = text1 && ` Back/Forward, a manual edit) round-trips as HASH_CHANGED, and a deep link / reload re-derives the state in view_display( ) (hash_apply is idempotent). The sample's own actionButtonsInfo helper state` &&
            ` still has no counterpart - the close button is the only navigation action and it is always the right one to show while the mid column is open. Same fold as app 575. // NOTE: the three archived views` &&
            ` declare different default namespaces - FlexibleColumnLayout.view.xml has xmlns="sap.f", List.view.xml xmlns="sap.uxap" with m: for sap.m, and Detail.view.xml xmlns="sap.m" with f: for sap.f. One` &&
            ` abap2UI5 view can only have one default namespace (sap.m here), so every control of the list page carries a different PREFIX than in its original file even though it is the same control:` &&
            ` ObjectPageLayout, ObjectPageDynamicHeaderTitle, ObjectPageSection and ObjectPageSubSection become uxap:-prefixed; Title, Button, Label, Text and form:SimpleForm lose their m: prefix; and the` &&
            ` FlexibleColumnLayout gains an f: one. The whole missing/extra pairing of this port's structural diff is that shift - no control is actually added or dropped, and the doubled Title and Button counts`.
    text1 = text1 && ` are the two pages' own, counted together in one view. // NOTE: the sample exists to show that a column RESIZE keeps the object page's scroll position: List.controller attaches columnResize on the FCL` &&
            ` and calls setSelectedSection( sections[iSectionIndex] ) when the begin column goes back to one column, with the index kept in the URL by _updateUrlOnNavigate. Reproduced 1:1 since 2026-08-31 - both` &&
            ` halves this deviation used to declare unreachable now exist. The URL half: the ':section:' route is the app-owned hash (see the routing deviation), written by the ObjectPage's navigate event, whose` &&
            ` event argument computes indexOfSection where the control lives (${$parameters>/section}.getParent().indexOfSection(${$parameters>/section}) - the 575 thin-frontend idiom). The instance half:` &&
            ` setSelectedSection takes a section INSTANCE whose clone ids only the client knows, so the follow-up addresses it positionally as an aggregation item - control_by_id ObjectPageLayout /` &&
            ` setSelectedSection / ObjectPageLayout/sections/<index> (the pr/aggregation-item-address mechanism). Selected on the first rendering of a deep link (_onListMatched's bFirstRendering), re-selected when`.
    text1 = text1 && ` columnResize reports the begin column back at one column while the layout reads OneColumn (the original's guard, the DOM-free 575 spelling), and re-issued per rebuild because a rebuilt ObjectPage is` &&
            ` back on its first section while section_ix survives as class state. Like the original, a plain browser Back over ':section:' URLs moves only the URL - only the first rendering and the column resize` &&
            ` re-scroll, and handleClose drops the section from the URL exactly like navTo('list') without arguments (iSectionIndex NaN, here -1). // NOTE: webapp/data/sections.json is seeded verbatim - all twelve` &&
            ` sections with their sectionName; tableName, which no control of this view binds, is carried along as the mock has it. // NOTE: the close Button in the detail page's navigationActions is icon-only in` &&
            ` the sample. The port gives it tooltip="Close column" so it is reachable with a screen reader - the one accessibility addition in this port. // NOTE: not yet verified in a running system: the` &&
            ` mid-column open and close and the bound sections aggregation. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_577.mjs). // NOTE: mvc:View displayBlock="true"`.
    text1 = text1 && ` has no counterpart: it is a Component-app setting that makes the view fill the page when it is the root of a Component, and abap2UI5 owns the view container itself. // NOTE: toDetail sets` &&
            ` MidColumnFullScreen, not TwoColumnsMidExpanded. This sample builds its semantic helper with maxColumnsCount: 1, and getNextUIState short-circuits to MidColumnFullScreen in that case - which is what` &&
            ` hides the begin column and makes the return to OneColumn a begin-column resize from zero width, the event the sample exists to demonstrate.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `FlexibleColumnLayoutColumnResize`              class = `z2ui5_cl_smpc_app_577` path = `src/02/04/z2ui5_cl_smpc_app_577.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `two post-1.71 members are kept for the 1:1 port: sap.f.FlexibleColumnLayout.autoFocus (since UI5 1.76) and restoreFocusOnBackNavigation (since 1.77), both set by the sample's own FCL view.` ) ).

    text1 = `POST-1.71: The sap.f.FlexibleColumnLayout aggregation landmarkInfo and the control sap.f.FlexibleColumnLayoutAccessibleLandmarkInfo it holds are both @since 1.95 - they ARE this sample, so both are` &&
            ` kept 1:1 and the port needs a UI5 runtime >= 1.95. On the 1.71 floor the unknown tag would take the whole view down, which is why the port is filed in src/02. // NOTE: The sample is not a routing` &&
            ` showcase - it drives the columns through an EventBus and the FlexibleColumnLayout's own layout property (setLayout). The port expresses it as ONE view: the List, Detail and DetailDetail views are` &&
            ` inlined into beginColumnPages / midColumnPages / endColumnPages, so the original's nested mvc:XMLView reference in beginColumnPages is dropped and the two lazily created views are declared up front` &&
            ` (app 234 precedent). The EventBus publish/subscribe pairs become three events writing the bound layout field. // NOTE: FlexibleColumnLayout and its FlexibleColumnLayoutAccessibleLandmarkInfo carry` &&
            ` the f: prefix here because the merged single view uses sap.m as its default xmlns for the pages, while the original root view declares sap.f as the default and writes both unprefixed. Same controls,`.
    text1 = text1 && ` different namespace representation (app 234 precedent). // NOTE: The three layout transitions (OneColumn, TwoColumnsMidExpanded, ThreeColumnsEndExpanded) driven by the bound layout property are` &&
            ` unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_449.mjs). // NOTE: The middle column's Page carried backgroundDesign =` &&
            ` 'Transparent', which its original does not have - only DetailDetail.view.xml sets it, and Detail.view.xml is a plain <Page id="detailPage" title="Middle Column">. Page.backgroundDesign defaults to` &&
            ` Standard, so the middle column rendered a different background from the sample. Removed 2026-08-24. No gate saw it: structural-diff has no 'attr extra' kind, so an attribute a port ADDS is never` &&
            ` compared.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `FlexibleColumnLayoutLandmarkInfo`              class = `z2ui5_cl_smpc_app_449` path = `src/02/04/z2ui5_cl_smpc_app_449.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `The sap.f.FlexibleColumnLayout aggregation landmarkInfo and the control sap.f.FlexibleColumnLayoutAccessibleLandmarkInfo it holds are both @since 1.95 - they ARE this sample, so both are kept 1:1 and` &&
                 ` the port needs a UI5 runtime >= 1.95. On the 1.71 floor the unknown tag would take the whole view down, which is why the port is filed in src/02.` ) ).

    text1 = `POST-1.71: The sap.f.FlexibleColumnLayout aggregation landmarkInfo and the control sap.f.FlexibleColumnLayoutAccessibleLandmarkInfo it holds are both @since 1.95 - they ARE this sample, so both are` &&
            ` kept 1:1 and the port needs a UI5 runtime >= 1.95. On the 1.71 floor the unknown tag would take the whole view down, which is why the port is filed in src/02. // NOTE: The sample is not a routing` &&
            ` showcase - it drives the columns through an EventBus and the FlexibleColumnLayout's own layout property (setLayout). The port expresses it as ONE view: the List, Detail and DetailDetail views are` &&
            ` inlined into beginColumnPages / midColumnPages / endColumnPages, so the original's nested mvc:XMLView reference in beginColumnPages is dropped and the two lazily created views are declared up front` &&
            ` (app 234 precedent). The EventBus publish/subscribe pairs become three events writing the bound layout field. // NOTE: FlexibleColumnLayout and its FlexibleColumnLayoutAccessibleLandmarkInfo carry` &&
            ` the f: prefix here because the merged single view uses sap.m as its default xmlns for the pages, while the original root view declares sap.f as the default and writes both unprefixed. Same controls,`.
    text1 = text1 && ` different namespace representation (app 234 precedent). // NOTE: The three layout transitions (OneColumn, TwoColumnsMidExpanded, ThreeColumnsEndExpanded) driven by the bound layout property are` &&
            ` unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_450.mjs). // NOTE: The middle column's Page carried backgroundDesign =` &&
            ` 'Transparent', which its original does not have - only DetailDetail.view.xml sets it, and Detail.view.xml is a plain <Page id="detailPage" title="Middle Column">. Page.backgroundDesign defaults to` &&
            ` Standard, so the middle column rendered a different background from the sample. Removed 2026-08-24. No gate saw it: structural-diff has no 'attr extra' kind, so an attribute a port ADDS is never` &&
            ` compared.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `FlexibleColumnLayoutLandmarkInfoArrow`         class = `z2ui5_cl_smpc_app_450` path = `src/02/04/z2ui5_cl_smpc_app_450.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `The sap.f.FlexibleColumnLayout aggregation landmarkInfo and the control sap.f.FlexibleColumnLayoutAccessibleLandmarkInfo it holds are both @since 1.95 - they ARE this sample, so both are kept 1:1 and` &&
                 ` the port needs a UI5 runtime >= 1.95. On the 1.71 floor the unknown tag would take the whole view down, which is why the port is filed in src/02.` ) ).

    text1 = `NOTE: the sample is NOT a pure-routing showcase - it uses an EventBus (no sap.ui.core.routing Router/Targets) and drives the column visibility purely through the FlexibleColumnLayout's own layout` &&
            ` property (setLayout). So it is expressed as ONE view: the three column views (List, Detail, DetailDetail) are inlined into the FlexibleColumnLayout's beginColumnPages/midColumnPages/endColumnPages` &&
            ` aggregations. Consequently the original's nested mvc:XMLView reference (in beginColumnPages) is dropped - its List page content is placed inline instead. // NOTE: FlexibleColumnLayout is emitted with` &&
            ` the f: namespace prefix (f:FlexibleColumnLayout) because the merged single view uses sap.m as its default xmlns for the pages, whereas the original FlexibleColumnLayout.view.xml declares sap.f as the` &&
            ` default xmlns and writes the control unprefixed. Same control, prefix-only difference from merging four views (with two different default namespaces) into one. // NOTE: each column view holds 26` &&
            ` static, byte-identical navigation rows (List: 'Open mid column', Detail: 'Open end column', DetailDetail: 'No more columns') hardcoded in the XML - there is no JSON model. This is reproduced`.
    text1 = text1 && ` faithfully as a bound aggregation over a 26-row model table per column (one ColumnListItem/Text template each): same 26 rendered rows, so the ColumnListItem count (78 originals) and Text count (78` &&
            ` originals) collapse to the three templates. No data is lost or subset. // NOTE: the controller's imperative setLayout(TwoColumnsBeginExpanded/ThreeColumnsMidExpanded) is replaced by binding the` &&
            ` FlexibleColumnLayout layout property two-way ({/LAYOUT}, initial OneColumn) and changing it server-side in the press handlers (prefer-a-bindable-property over a frontend action; setLayout is not a` &&
            ` whitelisted CONTROL_METHOD). The column-switch round-trip behaviour is not verified in a running system. The lazy view loading (runAsOwner/XMLView.create on demand) is a pure framework optimization` &&
            ` with no user-visible effect and is not reproduced - all three pages exist from the start. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): pressing the first` &&
            ` master list item round-trips LIST_PRESS and the mid column renders (the two-way bound layout flipped to TwoColumnsBeginExpanded).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `FlexibleColumnLayoutSimple`                    class = `z2ui5_cl_smpc_app_234` path = `src/01/04/z2ui5_cl_smpc_app_234.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46`
        notes = text1 ) ).

    text1 = `POST-1.71: one post-1.71 control is kept for the 1:1 port: sap.m.Avatar (since UI5 1.73) on the product page. Unlike its sibling app 579, this sample's FlexibleColumnLayout sets neither autoFocus nor` &&
            ` restoreFocusOnBackNavigation and carries no column-distribution layoutData. // NOTE: the sample is a Component with a ROUTER and five views (FlexibleColumnLayout, List, Detail, DetailDetail,` &&
            ` AboutPage). abap2UI5 serves one view, so the five become one: the FCL declares its begin, mid and end column pages inline and the routing collapses to the FCL's own layout property. onListItemPress` &&
            ` opens the mid column (the helper's level-1 state, TwoColumnsMidExpanded), handleItemPress opens the end column (level 2, ThreeColumnsMidExpanded), handleAboutPress shows the about page (level 3,` &&
            ` which the manifest pins to EndColumnFullScreen), and the three close / full-screen handlers of each column set the layout the helper would hand them. Since 2026-08-31 the URL the router writes IS` &&
            ` reproduced, via the framework's app-owned hash routing (cs_event-hash_attach_changed + hash_set, needs abap2UI5 newer than 1.144.0 - abap2UI5#2693, the app-584 recipe): every routed handler composes`.
    text1 = text1 && ` the original's pattern - '#/detail/{category}/{layout}', '#/detailDetail/{category}/{product}/{layout}', '#/detailDetailDetail/{category}/{product}/{supplier}/{layout}', '#/{layout}' for the list` &&
            ` route - with the category riding as its NAME (spaces URL-encoded as %20, the way the original's navTo encodes them) and product/supplier as INDICES into the full mock collections, and pushes it; a` &&
            ` hash change the app did not write (browser Back/Forward, a manual edit) round-trips as HASH_CHANGED and is parsed back onto the running instance, where hash_apply also swaps the BEGIN column between` &&
            ` the categories and the products page (the begin_page park below) and re-derives the category filter - guarded to the category CHANGING, so a search filtered further is not reset by a routine` &&
            ` rebuild's idempotent re-apply; a deep link / reload re-derives all three columns in view_display( ). The phone branch of the category press (navTo('detail') with OneColumn behind an isPhone probe)` &&
            ` stays unported - the port has no device probe here and ports the desktop branch, as before. Same fold as apps 575, 577, 579, 580 and 584. // NOTE: this sample's hierarchy is one level deeper than its`.
    text1 = text1 && ` siblings': the begin column starts on a CATEGORIES page (List.view.xml, the sample's full-screen start page), pressing a category replaces it with the products page (Detail.view.xml) and opens the` &&
            ` product's ObjectPage (DetailDetail.view.xml) in the mid column, and a supplier opens the supplier-information page (DetailDetailDetail.view.xml) in the end column. The router does the begin-column` &&
            ` swap by targeting a different view; the port declares both pages in beginColumnPages and swaps them with the one frontend action - control_by_id 'to' on the FCL, which navigates whichever column` &&
            ` holds the page. **Corrected 2026-08-27:** that last clause did not hold until the framework was fixed the same day, and the begin-column swap had therefore NEVER worked. ``to`` was whitelisted` &&
            ` ``["controlId", "string"]`` in the framework's core/actions/ControlCall.js, so its first argument reached the container as a Control instance. sap.m.NavContainer.to normalises a Control to its id on` &&
            ` its own first line - which is why every plain NavContainer port worked - but sap.f.FlexibleColumnLayout.to PROBES its three columns with ``getPage( sPageId )``, and getPage compares`.
    text1 = text1 && ` ``aPages[i].getId() == pageId``: a Control never equals an id string, so all three probes missed and the trailing ``else`` navigated the END column. Measured on the transpiled backend 2026-08-27,` &&
            ` pressing category 4: the begin column stayed on ``categoriesPage``, ``dynamicPageId`` had no DOM node at all, ``productsTable`` answered its eleven filtered rows out of the registry with nothing on` &&
            ` screen, and the only trace was a UI5 console warning - "Navigation triggered to page with ID 'mainView--dynamicPageId', but this page is not known/aggregated by Element` &&
            ` sap.m.NavContainer#mainView--fcl-endColumnNav". abap2UI5 commit 977474af adds a narrow ``pageId`` argument kind that resolves the control exactly as ``controlId`` does and then hands the container` &&
            ` ``control.getId()``; against a framework tree built at that commit the same press puts ``getCurrentBeginColumnPage()`` on ``dynamicPageId``, gives the products DynamicPage and its table a DOM node,` &&
            ` and raises no warning. The port was not changed - it is correct on both sides of the fix and simply could not work before it - so the begin-column swap needs abap2UI5 at 977474af or later. **Updated`.
    text1 = text1 && ` 2026-08-27, later the same day:** that commit was squash-merged into abap2UI5 main as ``56ff2a10`` (#2669), which means ``977474af`` is now on NO branch at all - it is reachable only as a dangling` &&
            ` object, so cite ``56ff2a10`` for where the fix lives and ``977474af`` only for what was measured above. The corpus harness (``.abap2UI5``, cloned with A2UI5_BRANCH=main) therefore no longer carries` &&
            ` the broken cast, and the ``ddbdd136`` reading is what it held before the merge, not what it holds now. **Closed 2026-08-28:** the PIN was the separate question and it has caught up: since the` &&
            ` 2026-08-28 bump the pinned framework carries the ``pageId`` kind, so the reproducible builds do the begin-column swap too. The port was never changed by any of this. // NOTE: the begin column's page` &&
            ` is LIVE control state, and view_display( ) loses it. A FlexibleColumnLayout column position lives in the control tree, which view_display( ) destroys and XMLView.create rebuilds, so the begin column` &&
            ` falls back to the FIRST beginColumnPages entry (categoriesPage) - while layout, t_rows and the D_* detail fields are class state that survive the round trip and the draft. Measured 2026-08-27 against`.
    text1 = text1 && ` a framework carrying the FCL ``to`` fix (abap2UI5 977474af): press a category (begin column on dynamicPageId, eleven Laptops rows on screen), press a supplier (no view_display( ), so the position` &&
            ` holds), then search - the rebuilt view came back with the begin column on categoriesPage and dynamicPageId without a DOM node, while layout still read ThreeColumnsMidExpanded and t_rows held the 92` &&
            ` search hits nobody could see. SEARCH and SORT are the two branches that rebuild. The router-driven original has no such reset: its URL carries the route. So the port parks the page the CATEGORY_ITEM` &&
            ` branch navigated to in BEGIN_PAGE and re-issues the same control_by_id 'to' from the end of view_display( ), guarded so a view that has never navigated issues nothing - the app 585 idiom, applied to` &&
            ` an FCL instead of a NavContainer. This was invisible before 977474af, because the ``to`` had never moved the begin column at all: the sweep that fixed the same asymmetry in apps` &&
            ` 167/301/302/303/558/585 excluded this port for exactly that reason. // NOTE: List.controller's onListItemPress looks up the FIRST product of the pressed category and opens it, which is what the port`.
    text1 = text1 && ` does; its phone branch (navTo('detail') with OneColumn) has no counterpart, since a backend cannot read Device.system.phone. The products table has no sorter in this sample, and its title is the` &&
            ` plain 'Products'. // NOTE: the five archived views declare different default namespaces - FlexibleColumnLayout.view.xml and DetailDetail.view.xml have xmlns="sap.f", List.view.xml and` &&
            ` AboutPage.view.xml xmlns="sap.m" with f: for sap.f, and Detail.view.xml xmlns="sap.uxap" with m: for sap.m. One abap2UI5 view can only have one default namespace (sap.m here), so every control` &&
            ` carries a different PREFIX than in its original file even though it is the same control: ObjectPageLayout, ObjectPageDynamicHeaderTitle, ObjectPageSection and ObjectPageSubSection become` &&
            ` uxap:-prefixed; DynamicPage and DynamicPageTitle become f:-prefixed (which is why the f:DynamicPage and f:DynamicPageTitle counts read 3 vs 2 - the sap.f-defaulted DetailDetail page joins the two the` &&
            ` other files already prefix); and FlexBox, VBox, Label, Text, Title, Avatar, ObjectNumber, ObjectIdentifier, ColumnListItem, Link, Button, OverflowToolbarButton and form:SimpleForm lose their m:`.
    text1 = text1 && ` prefix. The whole missing/extra pairing of this port's structural diff is that shift - no control is actually added or dropped. The OverflowToolbarButton count (9 vs 3+6) is the same nine buttons` &&
            ` counted once instead of split across two prefixes. // NOTE: the six navigation-action buttons are shown by the sample through the FCL helper's actionButtonsInfo (visible="{=` &&
            ` ${/actionButtonsInfo/midColumn/fullScreen} !== null }" and so on). The helper is a JavaScript utility, so the port derives the same visibilities from the layout itself: full-screen while that column` &&
            ` is not full screen, exit-full-screen while it is, and close while the column is open. // NOTE: Detail.controller binds the mid column with bindElement('/ProductCollection/<n>') and DetailDetail binds` &&
            ` the supplier row, so all their bindings are relative. The port folds them to root-seeded D_* / DD_TEXT fields (app 229 idiom): the pressed row's ProductId and the pressed supplier's text travel with` &&
            ` the press. The detail Price is pre-composed as '<CurrencyCode> <Price>', which is the composite text the original's ObjectNumber binds. // NOTE: onSort flips a Sorter on the items binding; a thin`.
    text1 = text1 && ` frontend sorts the data it sends, so the ABAP table is re-ordered (app 298 idiom). onSearch filters on Name into a second table so the search can widen again. onAdd's MessageBox.show with the` &&
            ` Information icon and the 'Aw, Snap!' title is ported 1:1 through client->message_box_display. // NOTE: the full mock /ProductCollection is seeded with the fields the three pages bind, plus the twelve` &&
            ` supplier names of /ProductCollectionStats/Filters/1/values that the detail page's Suppliers table lists. The master title's count comes from ProductCollectionStats/Counts/Total (123). // NOTE: the` &&
            ` two Avatar src values are '../../../../../../../{products>ProductPicUrl}' in the sample - a relative path out of the demo kit's own frame. The port binds the mock's ProductPicUrl as it stands,` &&
            ` without the traversal prefix, which is what the corpus does everywhere for these images. // NOTE: the icon-only share Button in the detail page's actions is tooltip-less in the sample, and so are the` &&
            ` add and sort buttons of the list toolbar. The port gives all three a tooltip so they are reachable with a screen reader - the accessibility additions in this port. // NOTE: not yet verified in a`.
    text1 = text1 && ` running system: the three-column navigation, the six full-screen / close actions and the bound column-distribution sizes. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_578.mjs). **Bounded 2026-08-27:** that verification did not cover the BEGIN-column navigation and could not have. The module of the day asserted` &&
            ` ``productsTable.getItems().length === 11`` off a bare ``Element.registry`` find, and a control the FlexibleColumnLayout never rendered answers that identically - so the leg was green while the swap` &&
            ` was dead. Proven both ways against the pre-fix framework on 2026-08-27: the 2026-08-25 module still passes on it, and the module rewritten the same day fails on it with its own sentence, 'the ``to``` &&
            ` never moved the BEGIN column onto the products page'. What 2026-08-25 verified stands for the mid and end columns and the layout states; the begin-column swap is verified as of 2026-08-27 against a` &&
            ` framework built at abap2UI5 977474af, and remains UNVERIFIED against the framework the nightly actually runs (main), where it cannot pass. **Updated 2026-08-27, later the same day:** ``977474af```.
    text1 = text1 && ` merged to main as ``56ff2a10`` (#2669), so that clause expired - the canary path (A2UI5_BRANCH=main) builds a framework that CARRIES the fix. **Closed 2026-08-28:** so does the PINNED path. The` &&
            ` 2026-08-27 bump-a2ui5 run had failed on app 233 (``boot: the PurchaseID input did not render``), which #157 fixed as a real port defect; the next run moved ``A2UI5_PIN`` on, and the begin-column leg` &&
            ` is verified against both paths from now on. // NOTE: mvc:View displayBlock="true" has no counterpart: it is a Component-app setting that makes the view fill the page when it is the root of a` &&
            ` Component, and abap2UI5 owns the view container itself. FlexibleColumnLayout stateChange IS wired since 2026-08-31 (a second pass the same day closed what the first declared open): onStateChanged` &&
            ` fires as the STATE_CHANGED event with isNavigationArrow and the layout as arguments, and only an ARROW change rewrites the URL - via client->hash_replace, the framework's HashChanger#replaceHash twin` &&
            ` of hash_set, so the URL follows with NO new history entry, exactly the original's replace-navTo. Every layout change costs a round trip the client-side original does not pay (the columnResize`.
    text1 = text1 && ` precedent of app 575); non-arrow firings return without touching anything. // NOTE: Component.js raises the model size limit; the port issues cs_event-set_size_limit for MAIN. The collection is 123` &&
            ` rows and the JSONModel caps a bound aggregation at 100, so without it the table stopped 23 rows short of the count its own title reports.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `FlexibleColumnLayoutWithFullscreenPage`        class = `z2ui5_cl_smpc_app_578` path = `src/02/04/z2ui5_cl_smpc_app_578.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 control is kept for the 1:1 port: sap.m.Avatar (since UI5 1.73) on the product page. Unlike its sibling app 579, this sample's FlexibleColumnLayout sets neither autoFocus nor` &&
                 ` restoreFocusOnBackNavigation and carries no column-distribution layoutData.` ) ).

    text1 = `POST-1.71: six post-1.71 members are kept for the 1:1 port: sap.f.FlexibleColumnLayout.autoFocus (since UI5 1.76) and restoreFocusOnBackNavigation (since 1.77), the three column-distribution controls` &&
            ` sap.f.FlexibleColumnLayoutData, FlexibleColumnLayoutDataForDesktop and FlexibleColumnLayoutDataForTablet (all since 1.128), which is what the sample's layoutData block is about, and sap.m.Avatar` &&
            ` (since 1.73) on the detail page. // NOTE: the sample is a Component with a ROUTER and five views (FlexibleColumnLayout, List, Detail, DetailDetail, AboutPage). abap2UI5 serves one view, so the five` &&
            ` become one: the FCL declares its begin, mid and end column pages inline and the routing collapses to the FCL's own layout property. onListItemPress opens the mid column (the helper's level-1 state,` &&
            ` TwoColumnsMidExpanded), handleItemPress opens the end column (level 2, ThreeColumnsMidExpanded), handleAboutPress shows the about page (level 3, which the manifest pins to EndColumnFullScreen), and` &&
            ` the three close / full-screen handlers of each column set the layout the helper would hand them. Since 2026-08-31 the URL the router writes IS reproduced, via the framework's app-owned hash routing`.
    text1 = text1 && ` (cs_event-hash_attach_changed + hash_set, needs abap2UI5 newer than 1.144.0 - abap2UI5#2693, the app-584 recipe): every routed handler composes the original's pattern from route + INDICES into the` &&
            ` mock collections ('#/detail/{product}/{layout}', '#/detailDetail/{product}/{supplier}/{layout}', '#/page2', '#/{layout}' for the list route) and pushes it, a hash change the app did not write` &&
            ` (browser Back/Forward, a manual edit) round-trips as HASH_CHANGED and is parsed back onto the RUNNING instance (search text and sort order survive), and a deep link / reload re-derives the columns in` &&
            ` view_display( ) (hash_apply is idempotent). The about page's close action is 1:1 as well: AboutPage.controller's handleClose is window.history.go(-1), and the port binds the press to the re-added` &&
            ` cs_event-hash_back frontend action - one real step back that CONSUMES the history entry, whose hash change round-trips as HASH_CHANGED; the former ABOUT_CLOSE event, which set the layout directly, is` &&
            ` gone. Same fold as apps 575 and 577. // NOTE: the five archived views declare different default namespaces - FlexibleColumnLayout.view.xml and DetailDetail.view.xml have xmlns="sap.f", List.view.xml`.
    text1 = text1 && ` and AboutPage.view.xml xmlns="sap.m" with f: for sap.f, and Detail.view.xml xmlns="sap.uxap" with m: for sap.m. One abap2UI5 view can only have one default namespace (sap.m here), so every control` &&
            ` carries a different PREFIX than in its original file even though it is the same control: ObjectPageLayout, ObjectPageDynamicHeaderTitle, ObjectPageSection and ObjectPageSubSection become` &&
            ` uxap:-prefixed; DynamicPage and DynamicPageTitle become f:-prefixed (which is why the f:DynamicPage and f:DynamicPageTitle counts read 3 vs 2 - the sap.f-defaulted DetailDetail page joins the two the` &&
            ` other files already prefix); and FlexBox, VBox, Label, Text, Title, Avatar, ObjectNumber, ObjectIdentifier, ColumnListItem, Link, Button, OverflowToolbarButton and form:SimpleForm lose their m:` &&
            ` prefix. The whole missing/extra pairing of this port's structural diff is that shift - no control is actually added or dropped. The OverflowToolbarButton count (9 vs 3+6) is the same nine buttons` &&
            ` counted once instead of split across two prefixes. // NOTE: the six navigation-action buttons are shown by the sample through the FCL helper's actionButtonsInfo (visible="{=`.
    text1 = text1 && ` ${/actionButtonsInfo/midColumn/fullScreen} !== null }" and so on). The helper is a JavaScript utility, so the port derives the same visibilities from the layout itself: full-screen while that column` &&
            ` is not full screen, exit-full-screen while it is, and close while the column is open. // NOTE: the columnsDistribution model the sample keeps (and its onColumnsDistributionChange handler, which` &&
            ` writes the user's drag back into it) becomes twelve bound fields, one per size. The sample seeds only three of them (desktop TwoColumnsMidExpanded 25/75/0, tablet TwoColumnsMidExpanded 40/60/0 and` &&
            ` tablet ThreeColumnsMidExpanded 20/60/20) and leaves the rest to the controls' own defaults; a flat ABAP row would send empty strings for those, so each is seeded with the control's documented` &&
            ` default. columnsDistributionChange itself is not wired: a thin frontend cannot store a drag the control already applied, and the next round trip would push the seeded value back over it. // NOTE:` &&
            ` Detail.controller binds the mid column with bindElement('/ProductCollection/<n>') and DetailDetail binds the supplier row, so all their bindings are relative. The port folds them to root-seeded D_* /`.
    text1 = text1 && ` DD_TEXT fields (app 229 idiom): the pressed row's ProductId and the pressed supplier's text travel with the press. The detail Price is pre-composed as '<CurrencyCode> <Price>', which is the composite` &&
            ` text the original's ObjectNumber binds. // NOTE: onSort flips a Sorter on the items binding; a thin frontend sorts the data it sends, so the ABAP table is re-ordered (app 298 idiom). onSearch filters` &&
            ` on Name into a second table so the search can widen again. onAdd's MessageBox.show with the Information icon and the 'Aw, Snap!' title is ported 1:1 through client->message_box_display. // NOTE: the` &&
            ` full mock /ProductCollection is seeded with the fields the three pages bind, plus the twelve supplier names of /ProductCollectionStats/Filters/1/values that the detail page's Suppliers table lists.` &&
            ` The master title's count comes from ProductCollectionStats/Counts/Total (123). // NOTE: the two Avatar src values are '../../../../../../../{products>ProductPicUrl}' in the sample - a relative path`.
    text1 = text1 && ` out of the demo kit's own frame. The port binds the mock's ProductPicUrl as it stands, without the traversal prefix, which is what the corpus does everywhere for these images. // NOTE: the icon-only` &&
            ` share Button in the detail page's actions is tooltip-less in the sample, and so are the add and sort buttons of the list toolbar. The port gives all three a tooltip so they are reachable with a` &&
            ` screen reader - the accessibility additions in this port. // NOTE: not yet verified in a running system: the three-column navigation, the six full-screen / close actions and the bound` &&
            ` column-distribution sizes. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_579.mjs). // NOTE: mvc:View displayBlock="true" has no counterpart: it is a` &&
            ` Component-app setting that makes the view fill the page when it is the root of a Component, and abap2UI5 owns the view container itself. FlexibleColumnLayout stateChange IS wired since 2026-08-31 (a` &&
            ` second pass the same day closed what the first declared open): onStateChanged fires as the STATE_CHANGED event with isNavigationArrow and the layout as arguments, and only an ARROW change rewrites`.
    text1 = text1 && ` the URL - via client->hash_replace, the framework's HashChanger#replaceHash twin of hash_set, so the URL follows with NO new history entry, exactly the original's replace-navTo. Every layout change` &&
            ` costs a round trip the client-side original does not pay (the columnResize precedent of app 575); non-arrow firings return without touching anything. // NOTE: Component.js raises the model size` &&
            ` limit; the port issues cs_event-set_size_limit for MAIN. The collection is 123 rows and the JSONModel caps a bound aggregation at 100, so without it the table stopped 23 rows short of the count its` &&
            ` own title reports. // NOTE: The items binding carries no client-side sorter. It used to keep the original's sorter: { path: 'NAME' }, which ClientListBinding re-applies on every model change - so the` &&
            ` ABAP sort was undone immediately and the Sort button round-tripped without changing anything. The original replaces the declared sorter through oBinding.sort( ); here the backend owns the order.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `FlexibleColumnLayoutWithOneColumnStart`        class = `z2ui5_cl_smpc_app_579` path = `src/02/04/z2ui5_cl_smpc_app_579.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `six post-1.71 members are kept for the 1:1 port: sap.f.FlexibleColumnLayout.autoFocus (since UI5 1.76) and restoreFocusOnBackNavigation (since 1.77), the three column-distribution controls` &&
                 ` sap.f.FlexibleColumnLayoutData, FlexibleColumnLayoutDataForDesktop and FlexibleColumnLayoutDataForTablet (all since 1.128), which is what the sample's layoutData block is about, and sap.m.Avatar` &&
                 ` (since 1.73) on the detail page.` ) ).

    text1 = `POST-1.71: one post-1.71 control is kept for the 1:1 port: sap.m.Avatar (since UI5 1.73) on the detail page. Unlike its sibling app 579, this sample's FlexibleColumnLayout sets neither autoFocus nor` &&
            ` restoreFocusOnBackNavigation and carries no column-distribution layoutData at all. // NOTE: the sample is a Component with a ROUTER and five views (FlexibleColumnLayout, List, Detail, DetailDetail,` &&
            ` AboutPage). abap2UI5 serves one view, so the five become one: the FCL declares its begin, mid and end column pages inline and the routing collapses to the FCL's own layout property. onListItemPress` &&
            ` opens the mid column (the helper's level-1 state, TwoColumnsMidExpanded), handleItemPress opens the end column (level 2, ThreeColumnsMidExpanded), handleAboutPress shows the about page (level 3,` &&
            ` which the manifest pins to EndColumnFullScreen), and the three close / full-screen handlers of each column set the layout the helper would hand them. Since 2026-08-31 the URL the router writes IS` &&
            ` reproduced, via the framework's app-owned hash routing (cs_event-hash_attach_changed + hash_set, needs abap2UI5 newer than 1.144.0 - abap2UI5#2693, the app-584 recipe): every routed handler composes`.
    text1 = text1 && ` the original's pattern from route + INDICES into the mock collections ('#/detail/{product}/{layout}', '#/detailDetail/{product}/{supplier}/{layout}', '#/page2', '#/{layout}' for the list route) and` &&
            ` pushes it, a hash change the app did not write (browser Back/Forward, a manual edit) round-trips as HASH_CHANGED and is parsed back onto the RUNNING instance (search text and sort order survive), and` &&
            ` a deep link / reload re-derives the columns in view_display( ) - the empty hash re-derives the TWO-column start (initialColumnsCount 2 makes the helper's level-0 state TwoColumnsMidExpanded). The` &&
            ` about page's close action is 1:1 as well: AboutPage.controller's handleClose is window.history.go(-1), and the port binds the press to the re-added cs_event-hash_back frontend action - one real` &&
            ` consumed history step whose hash change round-trips as HASH_CHANGED; the former ABOUT_CLOSE event, which set the layout directly, is gone. Same fold as apps 575, 577 and 579. // NOTE: this sample` &&
            ` starts on TWO columns: its list route targets both the list and the detail view, and Detail.controller's _onProductMatched defaults to product 0. The port seeds the layout TwoColumnsMidExpanded and`.
    text1 = text1 && ` binds the first product before the first render, so the mid column is filled from the start - which is the whole difference from the sibling sample FlexibleColumnLayoutWithOneColumnStart (app 579).` &&
            ` List.controller's own _onProductMatched, which selects the matching row in the table, is the one piece still without a counterpart: since 2026-08-31 the URL DOES carry the product index, but` &&
            ` selecting the row needs a per-row selected binding the original's template does not have - declared instead of added. // NOTE: the five archived views declare different default namespaces -` &&
            ` FlexibleColumnLayout.view.xml and DetailDetail.view.xml have xmlns="sap.f", List.view.xml and AboutPage.view.xml xmlns="sap.m" with f: for sap.f, and Detail.view.xml xmlns="sap.uxap" with m: for` &&
            ` sap.m. One abap2UI5 view can only have one default namespace (sap.m here), so every control carries a different PREFIX than in its original file even though it is the same control: ObjectPageLayout,` &&
            ` ObjectPageDynamicHeaderTitle, ObjectPageSection and ObjectPageSubSection become uxap:-prefixed; DynamicPage and DynamicPageTitle become f:-prefixed (which is why the f:DynamicPage and`.
    text1 = text1 && ` f:DynamicPageTitle counts read 3 vs 2 - the sap.f-defaulted DetailDetail page joins the two the other files already prefix); and FlexBox, VBox, Label, Text, Title, Avatar, ObjectNumber,` &&
            ` ObjectIdentifier, ColumnListItem, Link, Button, OverflowToolbarButton and form:SimpleForm lose their m: prefix. The whole missing/extra pairing of this port's structural diff is that shift - no` &&
            ` control is actually added or dropped. The OverflowToolbarButton count (9 vs 3+6) is the same nine buttons counted once instead of split across two prefixes. // NOTE: the six navigation-action buttons` &&
            ` are shown by the sample through the FCL helper's actionButtonsInfo (visible="{= ${/actionButtonsInfo/midColumn/fullScreen} !== null }" and so on). The helper is a JavaScript utility, so the port` &&
            ` derives the same visibilities from the layout itself: full-screen while that column is not full screen, exit-full-screen while it is, and close while the column is open. // NOTE: Detail.controller` &&
            ` binds the mid column with bindElement('/ProductCollection/<n>') and DetailDetail binds the supplier row, so all their bindings are relative. The port folds them to root-seeded D_* / DD_TEXT fields`.
    text1 = text1 && ` (app 229 idiom): the pressed row's ProductId and the pressed supplier's text travel with the press. The detail Price is pre-composed as '<CurrencyCode> <Price>', which is the composite text the` &&
            ` original's ObjectNumber binds. // NOTE: onSort flips a Sorter on the items binding; a thin frontend sorts the data it sends, so the ABAP table is re-ordered (app 298 idiom). onSearch filters on Name` &&
            ` into a second table so the search can widen again. onAdd's MessageBox.show with the Information icon and the 'Aw, Snap!' title is ported 1:1 through client->message_box_display. // NOTE: the full` &&
            ` mock /ProductCollection is seeded with the fields the three pages bind, plus the twelve supplier names of /ProductCollectionStats/Filters/1/values that the detail page's Suppliers table lists. The` &&
            ` master title's count comes from ProductCollectionStats/Counts/Total (123). // NOTE: the two Avatar src values are '../../../../../../../{products>ProductPicUrl}' in the sample - a relative path out` &&
            ` of the demo kit's own frame. The port binds the mock's ProductPicUrl as it stands, without the traversal prefix, which is what the corpus does everywhere for these images. // NOTE: the icon-only`.
    text1 = text1 && ` share Button in the detail page's actions is tooltip-less in the sample, and so are the add and sort buttons of the list toolbar. The port gives all three a tooltip so they are reachable with a` &&
            ` screen reader - the accessibility additions in this port. // NOTE: not yet verified in a running system: the three-column navigation, the six full-screen / close actions and the bound` &&
            ` column-distribution sizes. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_580.mjs). // NOTE: mvc:View displayBlock="true" has no counterpart: it is a` &&
            ` Component-app setting that makes the view fill the page when it is the root of a Component, and abap2UI5 owns the view container itself. FlexibleColumnLayout stateChange IS wired since 2026-08-31 (a` &&
            ` second pass the same day closed what the first declared open): onStateChanged fires as the STATE_CHANGED event with isNavigationArrow and the layout as arguments, and only an ARROW change rewrites` &&
            ` the URL - via client->hash_replace, the framework's HashChanger#replaceHash twin of hash_set, so the URL follows with NO new history entry, exactly the original's replace-navTo. Every layout change`.
    text1 = text1 && ` costs a round trip the client-side original does not pay (the columnResize precedent of app 575); non-arrow firings return without touching anything. // NOTE: Component.js raises the model size` &&
            ` limit; the port issues cs_event-set_size_limit for MAIN. The collection is 123 rows and the JSONModel caps a bound aggregation at 100, so without it the table stopped 23 rows short of the count its` &&
            ` own title reports. // NOTE: The items binding carries no client-side sorter. It used to keep the original's sorter: { path: 'NAME' }, which ClientListBinding re-applies on every model change - so the` &&
            ` ABAP sort was undone immediately and the Sort button round-tripped without changing anything. The original replaces the declared sorter through oBinding.sort( ); here the backend owns the order.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `FlexibleColumnLayoutWithTwoColumnStart`        class = `z2ui5_cl_smpc_app_580` path = `src/02/04/z2ui5_cl_smpc_app_580.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 control is kept for the 1:1 port: sap.m.Avatar (since UI5 1.73) on the detail page. Unlike its sibling app 579, this sample's FlexibleColumnLayout sets neither autoFocus nor` &&
                 ` restoreFocusOnBackNavigation and carries no column-distribution layoutData at all.` ) ).

    text1 = `POST-1.71: one post-1.71 control is kept for the 1:1 port: sap.m.Avatar (since UI5 1.73), which this sample uses twice on the detail page and once in the ShellBar's profile. Unlike its sibling app` &&
            ` 579, this sample's FlexibleColumnLayout sets neither autoFocus nor restoreFocusOnBackNavigation and carries no column-distribution layoutData. // NOTE: the sample is a Component with a ROUTER and` &&
            ` five views (FlexibleColumnLayout, List, Detail, DetailDetail, AboutPage). abap2UI5 serves one view, so the five become one: the FCL declares its begin, mid and end column pages inline and the routing` &&
            ` collapses to the FCL's own layout property. onListItemPress opens the mid column (the helper's level-1 state, TwoColumnsMidExpanded), handleItemPress opens the end column (level 2,` &&
            ` ThreeColumnsMidExpanded), handleAboutPress shows the about page (level 3, which the manifest pins to EndColumnFullScreen), and the three close / full-screen handlers of each column set the layout the` &&
            ` helper would hand them. Since 2026-08-31 the URL the router writes IS reproduced, via the framework's app-owned hash routing (cs_event-hash_attach_changed + hash_set, needs abap2UI5 newer than`.
    text1 = text1 && ` 1.144.0 - abap2UI5#2693, the app-012 recipe): every routed handler composes the original's pattern from route + INDICES into the mock collections ('#/detail/{product}/{layout}',` &&
            ` '#/detailDetail/{product}/{supplier}/{layout}', '#/page2', '#/{layout}' for the list route) and pushes it, and a hash change the app did not write (browser Back/Forward, a manual edit) round-trips as` &&
            ` HASH_CHANGED, whose handler parses the live hash (s_config-hash) back into route, indices and layout on the RUNNING instance - search text and sort order survive, and a deep link / reload re-derives` &&
            ` the columns in view_display( ) (hash_apply is idempotent). Since later the same day the ShellBar back button is 1:1 too: handleBackButtonPressed is window.history.go(-1), and navButtonPressed binds` &&
            ` the re-added cs_event-hash_back frontend action (the CAPABILITIES row is accurate again) - one real step back that CONSUMES the history entry, whose hash change round-trips as HASH_CHANGED and` &&
            ` restores that route; the former SHELL_BACK event, which composed the back TARGET as an extra entry, is gone. The arrow residue is closed in the same pass: onStateChanged is wired as STATE_CHANGED`.
    text1 = text1 && ` (isNavigationArrow and the layout ride with the event) and an ARROW change rewrites the URL via hash_replace - the replaceHash twin of hash_set - with no new history entry, exactly the original's` &&
            ` replace-navTo (see the stateChange deviation). Browser-verified against the transpiled backend 2026-08-31: boot without a hash, '#/detail/0/TwoColumnsMidExpanded' on the item press,` &&
            ` '#/detailDetail/0/0/ThreeColumnsMidExpanded', Back and Forward through the column states, a reload keeping the route and re-binding the product, and the MidColumnFullScreen hash. Same fold as apps` &&
            ` 575, 577, 579 and 580. // NOTE: this sample wraps the FlexibleColumnLayout in a sap.m.Page whose customHeader is a ShellBar - that is what it demonstrates. The ShellBar's showNavButton is bound to an` &&
            ` expression over the layout ({= ${/layout} === 'EndColumnFullScreen'}), exactly as the sample writes it, and handleBackButtonPressed leaves the full-screen about page. The about page itself has no` &&
            ` close action here (its sibling app 579 puts one in navigationActions); the ShellBar's back button is the way out. // NOTE: the detail page's snappedHeading differs from the sibling sample's: an HBox`.
    text1 = text1 && ` holding the Avatar and a VBox with the product name and its id, instead of two nested FlexBoxes with the name alone. Ported as written. // NOTE: the five archived views declare different default` &&
            ` namespaces - FlexibleColumnLayout.view.xml and DetailDetail.view.xml have xmlns="sap.f", List.view.xml and AboutPage.view.xml xmlns="sap.m" with f: for sap.f, and Detail.view.xml xmlns="sap.uxap"` &&
            ` with m: for sap.m. One abap2UI5 view can only have one default namespace (sap.m here), so every control carries a different PREFIX than in its original file even though it is the same control:` &&
            ` ObjectPageLayout, ObjectPageDynamicHeaderTitle, ObjectPageSection and ObjectPageSubSection become uxap:-prefixed; DynamicPage and DynamicPageTitle become f:-prefixed (which is why the f:DynamicPage` &&
            ` and f:DynamicPageTitle counts read 3 vs 2 - the sap.f-defaulted DetailDetail page joins the two the other files already prefix); and FlexBox, VBox, Label, Text, Title, Avatar, ObjectNumber,` &&
            ` ObjectIdentifier, ColumnListItem, Link, Button, OverflowToolbarButton form:SimpleForm, and the ShellBar's own Menu and its two MenuItems lose their m: prefix. The whole missing/extra pairing of this`.
    text1 = text1 && ` port's structural diff is that shift - no control is actually added or dropped. The OverflowToolbarButton count (9 vs 3+6) is the same nine buttons counted once instead of split across two prefixes.` &&
            ` // NOTE: the six navigation-action buttons are shown by the sample through the FCL helper's actionButtonsInfo (visible="{= ${/actionButtonsInfo/midColumn/fullScreen} !== null }" and so on). The` &&
            ` helper is a JavaScript utility, so the port derives the same visibilities from the layout itself: full-screen while that column is not full screen, exit-full-screen while it is, and close while the` &&
            ` column is open. // NOTE: Detail.controller binds the mid column with bindElement('/ProductCollection/<n>') and DetailDetail binds the supplier row, so all their bindings are relative. The port folds` &&
            ` them to root-seeded D_* / DD_TEXT fields (app 229 idiom): the pressed row's ProductId and the pressed supplier's text travel with the press. The detail Price is pre-composed as '<CurrencyCode>` &&
            ` <Price>', which is the composite text the original's ObjectNumber binds. // NOTE: onSort flips a Sorter on the items binding; a thin frontend sorts the data it sends, so the ABAP table is re-ordered`.
    text1 = text1 && ` (app 298 idiom). onSearch filters on Name into a second table so the search can widen again. onAdd's MessageBox.show with the Information icon and the 'Aw, Snap!' title is ported 1:1 through` &&
            ` client->message_box_display. // NOTE: the full mock /ProductCollection is seeded with the fields the three pages bind, plus the twelve supplier names of /ProductCollectionStats/Filters/1/values that` &&
            ` the detail page's Suppliers table lists. The master title's count comes from ProductCollectionStats/Counts/Total (123). // NOTE: the two Avatar src values are` &&
            ` '../../../../../../../{products>ProductPicUrl}' in the sample - a relative path out of the demo kit's own frame. The port binds the mock's ProductPicUrl as it stands, without the traversal prefix,` &&
            ` which is what the corpus does everywhere for these images. // NOTE: the icon-only share Button in the detail page's actions is tooltip-less in the sample, and so are the add and sort buttons of the` &&
            ` list toolbar. The port gives all three a tooltip so they are reachable with a screen reader - the accessibility additions in this port. // NOTE: not yet verified in a running system: the three-column`.
    text1 = text1 && ` navigation, the six full-screen / close actions and the bound column-distribution sizes. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_584.mjs). // NOTE:` &&
            ` mvc:View displayBlock="true" has no counterpart: it is a Component-app setting that makes the view fill the page when it is the root of a Component, and abap2UI5 owns the view container itself.` &&
            ` FlexibleColumnLayout stateChange IS wired since 2026-08-31 (a second pass the same day closed what the first declared open): onStateChanged fires as the STATE_CHANGED event with isNavigationArrow and` &&
            ` the layout as arguments, and only an ARROW change rewrites the URL - via client->hash_replace, the framework's HashChanger#replaceHash twin of hash_set, so the URL follows with NO new history entry,` &&
            ` exactly the original's replace-navTo. Every layout change costs a round trip the client-side original does not pay (the columnResize precedent of app 575); non-arrow firings return without touching`.
    text1 = text1 && ` anything. // NOTE: Component.js raises the model size limit; the port issues cs_event-set_size_limit for MAIN. The collection is 123 rows and the JSONModel caps a bound aggregation at 100, so without` &&
            ` it the table stopped 23 rows short of the count its own title reports. // NOTE: The items binding carries no client-side sorter. It used to keep the original's sorter: { path: 'NAME' }, which` &&
            ` ClientListBinding re-applies on every model change - so the ABAP sort was undone immediately and the Sort button round-tripped without changing anything. The original replaces the declared sorter` &&
            ` through oBinding.sort( ); here the backend owns the order.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.FlexibleColumnLayout`            name = `ShellBarWithFlexibleColumnLayout`              class = `z2ui5_cl_smpc_app_584` path = `src/02/04/z2ui5_cl_smpc_app_584.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 control is kept for the 1:1 port: sap.m.Avatar (since UI5 1.73), which this sample uses twice on the detail page and once in the ShellBar's profile. Unlike its sibling app 579, this` &&
                 ` sample's FlexibleColumnLayout sets neither autoFocus nor restoreFocusOnBackNavigation and carries no column-distribution layoutData.` ) ).

    text1 = `NOTE: The original binds two named models inside the cards (cities> for the ComboBoxes, products> for the revenue List); abap2UI5 keeps one default model, so those bind directly ({cities>/cities} ->` &&
            ` {/CITIES}, {products>/productItems} -> {/PRODUCTITEMS}, prefix dropped, last segment identical). **Sidecar corrected 2026-08-05**: the rest of the text still described the port before its rework and` &&
            ` called the handlers STATIC toasts. They are not: the three property Switches are two-way bound to the GridContainer's snapToRow / allowDenseFill / inlineBlockLayout (prefer-a-bindable-property, the` &&
            ` app-007/048/128 pattern), columnsChange round-trips its columns parameter, and the GenericTile / f:Card header presses compose their toast from $event.oSource.getMetadata().getName() instead of a` &&
            ` fixed string. What stays dropped: the RevealGrid toggle (a sample-local JS debug overlay, same as apps 145/222/271) The onInit attachLayoutChange handler that swaps sapUiSmallMargin/sapUiTinyMargin`.
    text1 = text1 && ` on the narrow breakpoints was dropped for the same reason until 2026-08-05 - a class swap on a LIVE breakpoint change, for which no wire existed (the device> model was a snapshot: it wrapped the live` &&
            ` sap.ui.Device object but never notified its bindings). The framework now refreshes it on resize/rotation and publishes the current media range as {device>/media/range} (pr/live-device-model,` &&
            ` implemented upstream), and from 2026-08-05 the port put that expression straight into class=. **That could never work, corrected 2026-08-23**: class is not a bindable property - XMLTemplateProcessor` &&
            ` intercepts it before the property branch and accumulates the RAW string for addStyleClass, and CustomStyleClassSupport.addStyleClass returns early on any value containing a quote. The expression` &&
            ` carries six, so the GridContainer got NO class at all: the port lost the original's static sapUiSmallMargin on top of the handler it was meant to reproduce. It is the control's own layoutChange event` &&
            ` now, driving removeStyleClass + addStyleClass (both whitelisted) with the branch inside the argument expression - roundtrip-free, and _applyLayout fires on the first layout determination as well, so`.
    text1 = text1 && ` a phone starts out tiny like the original. The static class is back on the view. This is the one bound class= in the corpus, and the linter skips class as a framework attribute, so no gate could see` &&
            ` it. // NOTE: The ComboBox items keep the original binding-info sorter 1:1 ({path:'/CITIES', sorter:{path:'TEXT'}} - restored 2026-07-27; an earlier version had dropped it although CAPABILITIES marks` &&
            ` binding sorters expressible). The sap.ui.integration Card keeps its original external manifest reference (cardManifest.json). The two long lorem-ipsum filler Texts are abbreviated to short` &&
            ` placeholders. cities.json (7) and products.json (3) are inlined in full (the unbound 'status' mock column stays out of the row type per the rows-not-columns rule). // NOTE: Faked-event-value audit` &&
            ` fix (2026-07-30): the four substituted handlers are now the original behaviours. onSnapToRowChange / onAllowDenseFillChange / onInlineBlockLayoutChange: each Switch state is two-way bound and the` &&
            ` GridContainer binds snapToRow/allowDenseFill/inlineBlockLayout (added attrs) to the same fields, so the toggles drive the grid entirely client-side (the 007/128 pattern) - the three Switch change`.
    text1 = text1 && ` attributes are DROPPED for the binding (they only carried the imperative setter). onGridColumnsChange: the columnsChange wire round-trips ${$parameters>/columns} and recomputes the bound` &&
            ` columnsCountText text ('Current grid columns count: <n>'; text is an added attr on the Text). onPress (tiles + card): client-composed toast 'Press was fired on - {0}' filled by` &&
            ` $event.oSource.getMetadata().getName(), the original's exact text. onRevealGrid: the Reveal Grid button's press attribute is DROPPED, undecorated - RevealGrid is a sample-local JS helper (grid` &&
            ` outline overlay) with no declarative equivalent (the app-145 precedent); the earlier static toasts ('Reveal Grid', 'Snap to row', 'Allow dense fill', 'Inline block layout', 'Columns changed', 'Tile` &&
            ` pressed', 'Card pressed') are gone. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the first GenericTile press toasts 'Press was fired on - sap.m.GenericTile'` &&
            ` (the getMetadata().getName() expression resolves); the switch bindings and columns counter remain unexercised. // POST-1.71: card:Header press is @since 1.86 and is kept 1:1 from the original`.
    text1 = text1 && ` (V.view.xml wires press='.onPress' on the card header itself). sap.f.cards.Header is @1.64 and declares no events of its own; press is declared on its base class sap.f.cards.BaseHeader, which is` &&
            ` @since 1.86 - the relocated-member blind spot the property gate cannot see (the snapshot records the event under BaseHeader, not under Header), so it is declared by policy, the same route app 167` &&
            ` takes for NavigationListItem.expanded. Below 1.86 the header renders but is not pressable and the toast never appears. Undeclared until 2026-08-23; declaring it moves the port from src/01/04 to` &&
            ` src/02/04 per AGENTS section 3. // NOTE: The grid's sap.ui.integration.widgets.Card loads its manifest from an external file. That URL was kept in the original's document-relative form until` &&
            ` 2026-08-24, which an abap2UI5 app has no document root to resolve - so the card 404'd, and the port carried a render_smoke skip reading "the static headless harness cannot serve it". Absolutizing the` &&
            ` URL onto the OpenUI5 host (the corpus asset rule) fixed the render as a side effect, the skip went stale, and the gate demanded its removal - which is exactly what a re-verified skip is for. Both are`.
    text1 = text1 && ` gone now; the port renders clean.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridContainer`                   name = `GridContainer`                                 class = `z2ui5_cl_smpc_app_168` path = `src/02/04/z2ui5_cl_smpc_app_168.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.65`
        is_post171 = abap_true
        notes = text1
        post171 = `card:Header press is @since 1.86 and is kept 1:1 from the original (V.view.xml wires press='.onPress' on the card header itself). sap.f.cards.Header is @1.64 and declares no events of its own; press` &&
                 ` is declared on its base class sap.f.cards.BaseHeader, which is @since 1.86 - the relocated-member blind spot the property gate cannot see (the snapshot records the event under BaseHeader, not under` &&
                 ` Header), so it is declared by policy, the same route app 167 takes for NavigationListItem.expanded. Below 1.86 the header renders but is not pressable and the toast never appears. Undeclared until` &&
                 ` 2026-08-23; declaring it moves the port from src/01/04 to src/02/04 per AGENTS section 3.` ) ).

    text1 = `IMPROVISED: The three sap.ui.integration.widgets.Card children take their content from cardManifests.json through manifest="{manifests>/listContent/...}". A Card manifest is either a JS OBJECT or a` &&
            ` URL to a single manifest file (Card.createManifest), and the sample's three manifests live inside ONE file under a wrapper key, so neither form is reachable from a declarative view. Each w:Card is` &&
            ` therefore rebuilt as a declarative sap.f.Card with the manifest's own card:Header (title, subtitle, icon, status) and a sap.m.List carrying the manifest's own rows, seeded in ABAP. Same three cards` &&
            ` in the same three grid slots with the same content; structural-diff reports w:Card and Card.manifest as missing. The largeList manifest sets maxItems 7, so its eighth row is not seeded - the card` &&
            ` would not show it either. // NOTE: onInit adds the DragInfo and the GridDropInfo in the controller; the port declares both in the grid's dragDropConfig aggregation. // NOTE: The drop handler reorders` &&
            ` the grid's TEN STATIC children in place. Static children cannot be reordered from a backend, so the port keeps an order table of the ten item keys and emits the children in that order; the drop event`.
    text1 = text1 && ` carries the two indices and the drop position, and ABAP applies the original's own index arithmetic (decrement when the item moved forward, increment for an After drop) to that table. Same reorder,` &&
            ` one round-trip, and the children stay the heterogeneous mix the sample uses (GenericTiles, cards and a Text). // IMPROVISED: RevealGrid is a sample-local JS helper module that draws a DOM overlay` &&
            ` outlining the grid cells. It has no declarative equivalent, so the ToggleButton is kept but its press wire is dropped (structural-diff reports ToggleButton.press as attr missing) - same treatment as` &&
            ` app 168, which ships the same helper. // NOTE: attachLayoutChange swaps sapUiSmallMargin for sapUiTinyMargin on the XS and S layouts. The port keeps the sapUiSmallMargin the view declares; the` &&
            ` layoutChange wire has no counterpart and the two GridContainerSettings the view declares (layout and layoutXS) already carry the responsive sizing. // NOTE: The drag-and-drop reorder of the ten grid` &&
            ` items and the three rebuilt cards are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_526.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridContainer`                   name = `GridContainerDragAndDrop`                      class = `z2ui5_cl_smpc_app_526` path = `src/01/04/z2ui5_cl_smpc_app_526.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.65`
        notes = text1 ) ).

    text1 = `NOTE: attachDragAndDrop adds the DragInfo / DropInfo / GridDropInfo configurations in the controller. The port declares the same four configurations in the two dragDropConfig aggregations, which is` &&
            ` why structural-diff reports dnd:DragInfo, dnd:DropInfo and dndgrid:GridDropInfo as control extra - the original's view declares none of them. // NOTE: onDrop moves the row between the two JSON models` &&
            ` and corrects the target index twice (once when source and target are the same model and the row moved forward, once for a drop After). The port sends the source container, the source index, the` &&
            ` target container, the target index and the drop position with the event and does the identical splice in ABAP, so the reorder is a real round-trip rather than a client-side one. // IMPROVISED:` &&
            ` GridDropInfo.dropIndicatorSize is a JavaScript callback that sizes the drop indicator from the dragged row's rows/columns while a List row is dragged over the grid. abap2UI5 cannot register a` &&
            ` callback, so the attribute is dropped and the indicator falls back to its default size; the drop itself, including the row's rows/columns, is unaffected. // IMPROVISED: RevealGrid is a sample-local`.
    text1 = text1 && ` JS helper module that draws a DOM overlay outlining the grid cells. It has no declarative equivalent, so the ToggleButton is kept but its press wire is dropped (structural-diff reports` &&
            ` ToggleButton.press as attr missing) - same treatment as app 168, which ships the same helper. // NOTE: The drag and drop between the List and the GridContainer, in both directions and within each` &&
            ` container, is unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_527.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridContainer`                   name = `GridContainerDragAndDropFromList`              class = `z2ui5_cl_smpc_app_527` path = `src/01/04/z2ui5_cl_smpc_app_527.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.65`
        notes = text1 ) ).

    text1 = `POST-1.71: card:Header press is @since 1.86. sap.f.cards.Header is @1.64 and declares no events of its own; press lives on its base sap.f.cards.BaseHeader, which is @since 1.86 (BaseHeader.js:64, the` &&
            ` event at :182) - the relocated-member blind spot the property gate cannot see, since it finds no member-level @since and passes. Same construct and same declaration as app 168. Below 1.86 the header` &&
            ` renders but is not pressable, so the withAction card's navigation silently does nothing. // IMPROVISED: All SEVEN sap.ui.integration.widgets.Card children take their content from cardManifests.json` &&
            ` through manifest="{manifests>/...}". A Card manifest is either a JS OBJECT or a URL to a single manifest file (Card.createManifest), and the sample's seven manifests live inside ONE file under` &&
            ` wrapper keys, so neither form is reachable from a declarative view. Each w:Card is therefore rebuilt as a declarative sap.f.Card carrying the manifest's own card:Header and its content: a sap.m.List` &&
            ` of StandardListItems for the four List cards (orders, tasks, contacts, withAction), a sap.m.Table with three Columns and a ColumnListItem row for the Table card (employees), a sap.m.List of`.
    text1 = text1 && ` DisplayListItems for the Object card (contact, whose three Navigation actions become the URLHELPER tel:/mailto: wires the manifest spells out) and a VBox with the text and the Go-to-page Link for the` &&
            ` AdaptiveCard (summary). Same seven cards in the same seven grid slots with the same content; structural-diff reports w:Card and Card.manifest as missing and every rebuilt control as extra. The two` &&
            ` CardBadgeCustomData badges on the orders card go with the w:Card - sap.f.Card has no badge counterpart. // IMPROVISED: onBorderReached toasts '<panel header> border reached' and then hands the` &&
            ` KEYBOARD FOCUS to the neighbouring grid, which it finds by comparing the four grids' bounding rectangles against the arrow key that was pressed. Geometry and focus are client facts a backend cannot` &&
            ` reach, so the port keeps the toast - composed on the client, one per grid with that grid's own header text - and drops the focus hand-off. The four borderReached wires are therefore live but only` &&
            ` announce the border. // IMPROVISED: RevealGrid is a sample-local JS helper module that draws a DOM overlay outlining all four grids. It has no declarative equivalent, so the ToggleButton is kept but`.
    text1 = text1 && ` its press wire is dropped (structural-diff reports ToggleButton.press as attr missing) - same treatment as app 168, which ships the same helper. // NOTE: The four grids, the seven rebuilt cards and` &&
            ` the four border-reached toasts are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_528.mjs). // NOTE: The URLHELPER` &&
            ` TRIGGER_EMAIL action takes an OBJECT literal as its third argument - the frontend reads params.EMAIL/params.SUBJECT - so the argument must open with { to stay raw through get_t_arg. A bare string is` &&
            ` wrapped in a JS string literal and params.EMAIL comes out undefined, opening a blank mailto:. The two TRIGGER_TEL wires on the same card already used the object form; the email one did not. The` &&
            ` manifest's action is mailto:{email} with no subject, so only EMAIL travels. // NOTE: The objectContent/contact manifest header carries "icon": { "text": "DM" }, an initials avatar. The rebuilt` &&
            ` card:Header keeps title and subtitle only; sap.f.cards.Header.iconInitials is base-version and would have expressed it.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridContainer`                   name = `GridContainersNavigation`                      class = `z2ui5_cl_smpc_app_528` path = `src/02/04/z2ui5_cl_smpc_app_528.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.65`
        is_post171 = abap_true
        notes = text1
        post171 = `card:Header press is @since 1.86. sap.f.cards.Header is @1.64 and declares no events of its own; press lives on its base sap.f.cards.BaseHeader, which is @since 1.86 (BaseHeader.js:64, the event at` &&
                 ` :182) - the relocated-member blind spot the property gate cannot see, since it finds no member-level @since and passes. Same construct and same declaration as app 168. Below 1.86 the header renders` &&
                 ` but is not pressable, so the withAction card's navigation silently does nothing.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListBasic`                                 class = `z2ui5_cl_smpc_app_111` path = `src/01/04/z2ui5_cl_smpc_app_111.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = `NOTE: The Slider drives the panel width via a roundtrip-free expression binding ({= ${slider} + '%' }) over the two-way slider value, instead of the original onSliderMoved setWidth handler;` &&
                 ` Slider.liveChange is therefore not wired (the width tracks the slider client-side). The full 27-row item set is inlined.` )
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListBoxContainer`                          class = `z2ui5_cl_smpc_app_144` path = `src/01/04/z2ui5_cl_smpc_app_144.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = `NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): one keyboard step on the Slider carries the value into the backend, the round-trip recomposes` &&
                 ` the Panel width and the Panel really renders at 99% - the imperative setWidth is fully replaced. Residual (nothing headless can assert): the visual GridList reflow inside the resized panel. // NOTE:` &&
                 ` GridList with a grid:GridBoxLayout (boxMinWidth 17rem) and a GridListItem template (VBox + FlexItemData, Title, Label). The 27 items are inlined from model/items.json; the template binds` &&
                 ` {TITLE}/{SUBTITLE} 1:1.` ) ).

    text1 = `NOTE: The Slider's ``liveChange`` attribute is dropped (the expression binding needs no event). **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): one` &&
            ` keyboard step on the Slider moves the Panel width to 99% with no round-trip at all - the expression binding replaces onSliderMoved completely. Residual: the grouped GridList's visual reflow. // NOTE:` &&
            ` GridList with a group sorter on items ({path, sorter:{path:'GROUP', descending:false, group:true}}, default group headers), an empty grid:GridBoxLayout customLayout, growing/growingThreshold, and a` &&
            ` custom f:headerToolbar (Toolbar with Title, ToolbarSpacer, SearchField - the SearchField has no handler in the original). The 27 items are inlined 1:1 from the sample's own model/items.json; the` &&
            ` GridListItem template (VBox>VBox+FlexItemData, Title, Label) binds {TITLE}/{SUBTITLE} 1:1.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListBoxContainerGrouping`                  class = `z2ui5_cl_smpc_app_176` path = `src/01/04/z2ui5_cl_smpc_app_176.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `1.71: the Reveal Grid ToggleButton keeps its label and stays in the view, but its press is not wired. RevealGrid.toggle is the sample's OWN JavaScript utility` &&
            ` (sample/GridListBoxContainerReal/RevealGrid/RevealGrid.js plus RevealGrid.css): it reads the computed grid template off each GridList's DOM node and injects an absolutely positioned overlay div per` &&
            ` cell, to visualise the box mesh. It is a debugging aid over the rendered DOM with no control-side counterpart and no server-side state - nothing a backend can turn on. onExit's RevealGrid.destroy` &&
            ` goes with it. Same fold as app 582, which ships the same helper. // NOTE: there is no model: C.controller.js declares only onRevealGrid and onExit, and every box in the three GridLists is written out` &&
            ` literally in V.view.xml (seven f:GridListItems with hard-coded titles, subtitles and descriptions). The port is a static view for the same reason - it is a design gallery of recommended box CONTENT,` &&
            ` not a data-bound list, so nothing is bound and no round trip exists. // NOTE: the three boxWidths (15rem toolbar designs, 22.5rem with icons, 17.5rem with ObjectStatus / InfoLabel) are the point of`.
    text1 = text1 && ` the sample and are carried 1:1 on the grid:GridBoxLayouts, as are the per-box FlexItemData growFactor=1 / shrinkFactor=0 that keep a box from shrinking below its minimum, and the three` &&
            ` OverflowToolbar designs the first list contrasts (Solid with sapContrast, plain Solid, Transparent). // NOTE: the twelve icon-only Buttons of the first and third list are tooltip-less in the sample` &&
            ` (Sort, Group, Edit, Delete and Information). The port gives each a tooltip so they are reachable with a screen reader - the accessibility additions in this port.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListBoxContainerReal`                      class = `z2ui5_cl_smpc_app_581` path = `src/01/04/z2ui5_cl_smpc_app_581.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: The Slider's ``liveChange`` attribute is dropped (the expression binding needs no event). **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): one` &&
            ` keyboard step on the Slider moves the Panel width to 99% with no round-trip - the expression binding replaces onSliderMoved completely. Residual: none of that kind, corrected 2026-08-23 -` &&
            ` GridResponsiveLayout.containerQuery defaults to false and the sample never sets it, so setActiveLayout measures window.innerWidth rather than the panel. Resizing the panel cannot change the` &&
            ` breakpoint on either side. // NOTE: GridResponsiveLayout.layoutChange is wired to a client-composed MessageToast (control_global MESSAGE_TOAST, template 'Layout changed to {0}' filled by` &&
            ` ${$parameters>/layout}) reproducing onLayoutChange 1:1 without a backend round-trip. The layoutChange attribute itself is kept, so structural-diff sees no difference.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListBreakPoints`                           class = `z2ui5_cl_smpc_app_213` path = `src/01/04/z2ui5_cl_smpc_app_213.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: The drop now reorders the list for real. CAPABILITIES marks drag & drop reorder expressible, so the earlier 'reorder logic not reproduced' was a wrong improvisation: the drop event ships the two` &&
            ` row indices and the insert position as client-side resolved $-args (${$parameters>/draggedControl/oParent}.indexOfItem(${$parameters>/draggedControl}), the same for droppedControl, and` &&
            ` ${$parameters>/dropPosition}), and on_event rebuilds the original onDrop arithmetic 1:1 in ABAP - remove the dragged row, decrement the drop index when dragging downwards, insert Before or After, and` &&
            ` the round-trip pushes the reordered model back. Client indices are 0-based, ABAP table rows 1-based, which is the only difference to the JS splice. Both indices are range-checked before use: they` &&
            ` arrive from the frontend, and where JS would splice a nonsense index harmlessly, an ABAP table read on it dumps. Before this rework the drop fired a DROP event this class never dispatched (a dead` &&
            ` wire, pattern-lint dead-event-wire). // NOTE: The GridDropInfo control from sap.f.dnd keeps a hyphen-free 'dndgrid' xmlns alias instead of the original 'dnd-grid' (the alias only names the same`.
    text1 = text1 && ` sap.f.dnd URI; it makes the control statically visible to the checks). // NOTE: 27 items inlined from model/data.json; absent enum fields defaulted (highlight None, type Inactive) so the bound` &&
            ` GridListItem properties stay valid. Template binds counter/highlight/type/unread + title/subtitle 1:1. // NOTE: The reorder is two Array.splice calls in the original (remove at the drag index, insert` &&
            ` at the drop index + 1) and DELETE + INSERT INDEX in the port. They differ in one place, corrected 2026-08-23: JavaScript's splice CLAMPS a start index past the end and appends, ABAP's INSERT ...` &&
            ` INDEX does not - past lines + 1 it sets sy-subrc = 4 and inserts nothing, so the row just deleted would vanish. It is reachable: sap.f.dnd.GridDragOver._calculateDropPosition falls back to the LAST` &&
            ` item with 'After' when the pointer is over no item at all, and that item can be the dragged one, which yields drag_pos = drop_pos = lines - 1 and an insert index of lines + 2. The index is clamped` &&
            ` now. Under the transpiler the runtime splices like JS, so this is one of the defects that only shows on a real system.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListDragAndDrop`                           class = `z2ui5_cl_smpc_app_148` path = `src/01/04/z2ui5_cl_smpc_app_148.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `IMPROVISED: the sample's whole point is the KEYBOARD hand-off between the four grids: onBorderReached reads the arrow key's keyCode plus the row/column the focus left from, walks all four f:GridLists` &&
            ` comparing getBoundingClientRect() geometry to find the one that lies in that direction, and calls focusItemByDirection on it. None of that can travel - it is a DOM-geometry search in the controller` &&
            ` and a control method that takes three runtime parameters the backend never sees. The port keeps the half that can: each grid's borderReached fires the MessageToast the sample also shows, naming that` &&
            ` grid. Arrow keys therefore still navigate WITHIN each grid (that is the control's own two-dimensional navigation, not the sample's code) and still report when they run out of it, but the focus does` &&
            ` not jump into the neighbouring grid. // NOTE: MessageToast.show("Reached border of " + oEvent.getSource().getHeaderText()) composes the text from the event source at runtime. The port wires` &&
            ` borderReached per grid, so each of the four carries its own finished text ("Reached border of GridList 1" .. "4") - the same four strings the sample can produce. // NOTE: onSliderMoved does`.
    text1 = text1 && ` this.byId("container").setWidth(oEvent.getParameter("value") + "%") on every liveChange. The port makes the slider's value a two-way bound property and the CSSGrid's width an expression over it ({=` &&
            ` ${...} + '%' }), so the width follows the slider in the browser with no round trip at all. The liveChange wire itself is therefore dropped: a round trip per keystroke would be slower AND lossy` &&
            ` (events behind one in flight are discarded). // 1.71: the Reveal Grids ToggleButton keeps its label and stays in the view, but its press is not wired. RevealGrid.toggle is the sample's OWN JavaScript` &&
            ` utility (sample/GridListKeyboardArrowsNavigation/RevealGrid/RevealGrid.js plus RevealGrid.css): it injects an absolutely positioned overlay div per grid cell, reading the computed grid template off` &&
            ` each GridList's DOM node, to visualise the grid mesh. It is a debugging aid with no control-side counterpart and no server-side state - nothing a backend can turn on. onExit's RevealGrid.destroy goes` &&
            ` with it. // NOTE: model/data.json carries title, subtitle, counter and type per item, but the GridListItem template binds only {title} - its second line is a Label with the LITERAL text "Subtitle".`.
    text1 = text1 && ` So the port models the title alone (4/4/5/5 items across the four lists, as in the mock) and keeps the literal label. // NOTE: the four GridLists differ only in their GridItemLayoutData spans (2x2,` &&
            ` 3x1, 3x2, 2x3), which is what makes the items different sizes and the two-dimensional navigation interesting; those are carried 1:1. The shared cssgrid:GridBasicLayout (5rem auto-rows,` &&
            ` repeat(auto-fill, minmax(5rem, 1fr)), 0.5rem gap) and the outer GridResponsiveLayout (two columns, one below S) are carried 1:1 as well. // NOTE: not yet verified in a running system: that arrow keys` &&
            ` inside a grid reach its border and raise the toast, and that dragging the slider narrows the container without a round trip. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_582.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListKeyboardArrowsNavigation`              class = `z2ui5_cl_smpc_app_582` path = `src/01/04/z2ui5_cl_smpc_app_582.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: SegmentedButton.selectionChange reproduces onModeChange server-side: GridList.mode is two-way bound to the same field as SegmentedButton.selectedKey and the headerText (bound) is recomputed as` &&
            ` 'GridList with mode ' + key on a backend round-trip. The GridList delete / selectionChange and the GridListItem press / detailPress are wired to client-side MessageToasts (the original toasted the` &&
            ` affected item's runtime id). mode/headerText/selectedKey carry bindings the original set statically. **e2e-verified 2026-08-21** (the 2026-07-30 stamp was WRONG: the interaction's mode leg was` &&
            ` shadowed by a duplicate key in the old in-file INTERACTIONS map and never ran once - found by the review sweep, and the two legs are merged into one module now) (transpiled-framework interaction,` &&
            ` scripts/e2e-smoke.mjs): selecting 'SingleSelectLeft' round-trips the two-way bound mode: the GridList's mode follows and the header text the BACKEND composes ('GridList with mode SingleSelectLeft')` &&
            ` appears, which nothing but a completed round-trip can produce. Until 2026-08-23 the leg clicked 'MultiSelect' - the mode the port already loads in - and SegmentedButton returns early when the pressed`.
    text1 = text1 && ` segment is the selected one, so no selectionChange fired and both assertions were already true at page load: the second stamp was as vacuous as the first, for a different reason. // NOTE: The 11` &&
            ` product rows are inlined from the sample's model/data.json. Fields the original JSON omits are given their UI5 defaults so the bound enum-ish properties stay valid: GridListItem.type -> 'Inactive' (8` &&
            ` rows), highlight/Status -> 'None' (5 rows). Both render identically to the original's undefined values; structural-diff compares only binding paths, not data. // NOTE: the four MessageToast handlers` &&
            ` are reproduced as roundtrip-free CLIENT-COMPOSED toasts (follow_up_action cs_event-control_global MESSAGE_TOAST.show with a {N} template): onSelectionChange as '{0?Selected:Unselected} item with ID` &&
            ` {1}' over ${$parameters>/selected} and ${$parameters>/listItem}.getId(), onDelete as 'Delete item with ID {0}', and onDetailPress/onPress as 'Request details for item with ID {0}' / 'Pressed item` &&
            ` with ID {0}' over $event.oSource.sId. **Corrected 2026-08-01** (faked-event-value audit): all four used to toast a CONSTANT ('Selection changed', 'Delete item', 'Request details', 'Pressed item') and`.
    text1 = text1 && ` dropped the item identity the original shows. The ids are the runtime UI5 ids, exactly as in the original. // NOTE: The sample's own main.css is archived and injected since 2026-08-21. The view` &&
            ` carries class="imageDisplayBlock" and the rule behind it (.imageDisplayBlock{display:block}) was missing entirely - the class name rendered with nothing behind it, which is exactly the gap apps` &&
            ` 122/124 had and closed: 'a sample whose stylesheet is not in ui5/ is an archive gap to close, not a reason to drop the CSS'. main.css now sits in ui5/sap.f/GridListModes/ (closing the AGENTS section` &&
            ` 4 archive gap) and reaches the view through a core:HTML style leaf.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListModes`                                 class = `z2ui5_cl_smpc_app_133` path = `src/01/04/z2ui5_cl_smpc_app_133.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: The Slider resizes the Panel again since 2026-08-05: onSliderMoved sets the width of byId('panelForGridList'), the Panel HAS a width property, so the pair is the roundtrip-free binding idiom of` &&
            ` apps 176/213 (Slider.value two-way bound, Panel.width an expression over it) - the 2026-07-27 review had already established that the earlier 'targets a non-existent id' claim was wrong. The` &&
            ` ToggleButton keeps its dropped press handler: onRevealGrid draws a debug overlay from the sample-local RevealGrid.js, a JS helper with its own CSS that is not an abap2UI5 capability (same drop as` &&
            ` apps 145/271). As a consequence the Slider's liveChange attribute is not emitted either - the resize lives in the binding pair now, exactly as in apps 176/213. // POST-1.71: The GridList customLayout` &&
            ` uses sap.ui.layout.cssgrid.ResponsiveColumnLayout and per-item ResponsiveColumnItemLayoutData with columns/rows (control + members since 1.72), kept 1:1; requires a UI5 release >= 1.72. // NOTE:` &&
            ` Fully static view reproduced 1:1: a ToggleButton, a Slider (value=100), and a Panel (headerToolbar Toolbar+Title) hosting an f:GridList with a ResponsiveColumnLayout and five static f:GridListItems`.
    text1 = text1 && ` (columns/rows 4/4, 3/2, 3/2, 1/1, 1/1), each a VBox with a Title and 1..5 Labels; no bound data.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.GridList`                        name = `GridListResponsiveColumnLayout`                class = `z2ui5_cl_smpc_app_222` path = `src/02/04/z2ui5_cl_smpc_app_222.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        is_post171 = abap_true
        notes = text1
        post171 = `The GridList customLayout uses sap.ui.layout.cssgrid.ResponsiveColumnLayout and per-item ResponsiveColumnItemLayoutData with columns/rows (control + members since 1.72), kept 1:1; requires a UI5` &&
                 ` release >= 1.72.` ) ).

    text1 = `NOTE: The sample's UI is a trigger Button plus two explanatory Texts, and the ProductSwitch itself lives in ProductSwitchPopover.fragment.xml, opened on press. **Rebuilt 1:1 on 2026-08-05** (the` &&
            ` 2026-07-27 review had flagged the 'no JS controller' rationale as refuted): the fragment is a core:FragmentDefinition shown through popover_display( by_id = pSwitchBtn ), the three products come from` &&
            ` model/data.json into a bound ProductSwitch items aggregation, and fnChange - a toast 'Redirecting to <targetSrc>' plus URLHelper.redirect( targetSrc, true ) - is two client actions chained on the one` &&
            ` change event, both reading the pressed item off the event (${$parameters>/itemPressed}.getTargetSrc()). Corrected 2026-08-23 on two counts. (a) The REDIRECT payload was written as a backtick literal,` &&
            ` ``\{ URL: ... \}``, where the backslash is a real character - only a |...| string template treats \{ as an escape. The argument therefore did not start with { , get_t_arg quoted the whole thing, the` &&
            ` ${$parameters>/itemPressed} expression never evaluated, and Browser.evUrlHelper got a string whose URL was undefined: instead of the redirect the user saw 'Invalid redirect URL'. It is a string`.
    text1 = text1 && ` template now, the form CAPABILITIES spells out and apps 073/084/149 use. (b) The closing sentence claimed the port has NO structural difference from the original at all - the third deviation in this` &&
            ` very sidecar records the second Button, so the claim was refuted where it stood. // POST-1.71: sap.f.ProductSwitch and sap.f.ProductSwitchItem are controls @since 1.72 - one minor release past the` &&
            ` 1.71 line. The sample IS the ProductSwitch, so the port is one of the five decided scope exceptions (ui5/scope-exceptions.json, KEEP 2026-07-30); the controls only became visible to the property gate` &&
            ` on 2026-08-05, when the popover fragment was rebuilt instead of substituted by a toast. The app needs a UI5 release >= 1.72. // NOTE: fnOpen's PHONE branch is reproduced since 2026-08-21: the` &&
            ` controller gives the popover an Emphasized 'Close' endButton on a phone only, wired to its closing handler. It was dropped and undeclared - found by the review sweep, and the deviation beside it` &&
            ` claimed the port had 'NO structural difference from the original at all'. The branch stays a branch, the form CAPABILITIES prescribes: the Button is declared once with visible bound to {=`.
    text1 = text1 && ` ${device>/system/phone} }, and closing is the roundtrip-free popover_close follow-up action. That makes the port carry 2 Buttons where the original view declares 1 - the second is the` &&
            ` controller-built one.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.ProductSwitch`                   name = `ProductSwitchNavigation`                       class = `z2ui5_cl_smpc_app_165` path = `src/02/04/z2ui5_cl_smpc_app_165.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.72`
        since_post171 = abap_true
        is_post171 = abap_true
        notes = text1
        post171 = `sap.f.ProductSwitch and sap.f.ProductSwitchItem are controls @since 1.72 - one minor release past the 1.71 line. The sample IS the ProductSwitch, so the port is one of the five decided scope` &&
                 ` exceptions (ui5/scope-exceptions.json, KEEP 2026-07-30); the controls only became visible to the property gate on 2026-08-05, when the popover fragment was rebuilt instead of substituted by a toast.` &&
                 ` The app needs a UI5 release >= 1.72.` ) ).

    text1 = `NOTE: The original keeps header/object data in nested paths on one JSON model (title, showFooter, titleSnappedContent/text, titleExpandedContent/text, objectDescription/category|center|email|status).` &&
            ` abap2UI5 keeps one default model and render-smoke does not mock nested structures, so those paths are folded to flat fields bound via _bind (last path segment identical, which structural-diff` &&
            ` matches). Both the snapped and expanded subheading Text controls bind the same flat 'text' field. The full 125-row ProductCollection mock is inlined. // NOTE: The semantic action handlers are` &&
            ` reproduced since 2026-08-05 instead of raising static toasts: onEdit sets showFooter and hides the Edit action, onSave/onCancel reset both, and onSave shows the original's MessageBox.alert(` &&
            ` 'Successfully saved!' ) - the state lives in ABAP and drives the bound properties, which is the thin-frontend answer to three controller methods that only toggle view state. The controller's onInit` &&
            ` Messaging.addMessages seed (one Error 'Something wrong happened') now goes through the z2ui5.cc.MessageManager companion control (app 065 idiom, pr/message-manager-write), so the MessagesIndicator`.
    text1 = text1 && ` carries its count again, and onMessagesButtonPress is a MessagePopover over the message> model declared in the indicator's dependents and toggled roundtrip-free (app 066 idiom). The DiscussInJam /` &&
            ` SendEmail / SendMessage semantic actions keep their built-in behaviour and the DraftIndicator shows state Saved. Structurally that adds three controls the original view does not declare - the` &&
            ` z2ui5:MessageManager bridge plus the MessagePopover and its MessageItem template - because the original builds the popover in the controller and seeds the message through the Messaging API.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.semantic.SemanticPage`           name = `SemanticPage`                                  class = `z2ui5_cl_smpc_app_166` path = `src/01/04/z2ui5_cl_smpc_app_166.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46.0`
        notes = text1 ) ).

    text1 = `NOTE: The original keeps the header data in nested paths on one JSON model (title, titleSnappedContent/text, objectDescription/category|center|email|status). abap2UI5 keeps one default model, so the` &&
            ` nested paths are folded to flat fields with the identical last segment - the same fold app 166 makes for the sibling sample. titleSnappedContent/text carries literal braces ('Filtered by {Name,` &&
            ` Price, Category}'); it is DATA, not a binding, so the port seeds it in model_init with the braces escaped. // NOTE: onMessagesButtonPress builds a MessagePopover over the message> model in the` &&
            ` controller and toggles it at the button. The port declares the same MessagePopover in the MessagesIndicator's dependents and toggles it roundtrip-free through control_by_id, which is why` &&
            ` structural-diff reports MessagePopover and MessageItem as control extra - the original's view declares neither. The controller's onInit Messaging.addMessages seed (one Error 'Something wrong` &&
            ` happened') goes through the z2ui5.cc.MessageManager companion control (app 065/166 idiom), which is the third control reported extra. That bridge is what feeds the message> model at all: this note`.
    text1 = text1 && ` used to say the message was 'seeded as a message row instead', but seeding t_messages without binding it left the model empty, and sap.f.semantic.SemanticConfiguration binds MessagesIndicator.visible` &&
            ` to a formatter over message>/ - so the indicator did not render and its popover could never be opened. // NOTE: notMobile is Device.system.phone negated, set on the model in onInit. The device model` &&
            ` carries the same fact to the client, so the two full-screen actions bind the expression {= !${device>/system/phone} } rather than a backend field. // NOTE: showFooter is static true in the view and` &&
            ` the ToggleFooter button flips it with setShowFooter in the controller. The port binds showFooter to a flag and the button's press flips that flag in ABAP - same toggle, one round-trip. The two` &&
            ` footerCustomActions Buttons (Save, Cancel) are pressless in the view itself, so they stay pressless here. // NOTE: The titleContent KPI placeholder image is the sample's own test-resources file` &&
            ` (sap/f/images/KPI.png), re-hosted on sdk.openui5.org. // NOTE: The footer toggle, the messages popover and the semantic action bar are unverified in a running system. **e2e-verified 2026-08-25**`.
    text1 = text1 && ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_529.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.semantic.SemanticPage`           name = `SemanticPageFreeStyle`                         class = `z2ui5_cl_smpc_app_529` path = `src/01/04/z2ui5_cl_smpc_app_529.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.ShellBar`                        name = `ShellBar`                                      class = `z2ui5_cl_smpc_app_110` path = `src/02/04/z2ui5_cl_smpc_app_110.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.63`
        is_post171 = abap_true
        notes = `NOTE: First sap.f port. Static ShellBar with a sap.m Menu and a profile Avatar; the homeIcon points at the OpenUI5 host (https://sdk.openui5.org/resources/sap/ui/documentation/sdk/images/logo_sap.png)` &&
                 ` per the asset-URL rule; the original uses the relative ./resources/... path. No controller events are wired in the original view (the controller's handleHomeIconPress is unreferenced there too). //` &&
                 ` POST-1.71: sap.m.Avatar (control @since 1.73) is used 1:1 as the profile avatar (m:Avatar initials='UI'). Newer than UI5 1.71; declared per the property-171 policy (app 152 precedent, same ShellBar` &&
                 ` profile Avatar), so the app needs UI5 >= 1.73 to render the avatar. Added at the 2026-07-27 review sweep - the control-level @since is invisible to the member-level property gate.`
        post171 = `sap.m.Avatar (control @since 1.73) is used 1:1 as the profile avatar (m:Avatar initials='UI'). Newer than UI5 1.71; declared per the property-171 policy (app 152 precedent, same ShellBar profile` &&
                 ` Avatar), so the app needs UI5 >= 1.73 to render the avatar. Added at the 2026-07-27 review sweep - the control-level @since is invisible to the member-level property gate.` ) ).

    text1 = `POST-1.71: three post-1.71 controls are kept for the 1:1 port: sap.f.ProductSwitch and sap.f.ProductSwitchItem (both since UI5 1.72), which are what this sample shows, and sap.m.Avatar (since 1.73) in` &&
            ` the ShellBar's profile aggregation. App 245 (ProductSwitchNavigation) carries the same pair as a documented scope exception. // NOTE: ProductSwitchPopover.fragment.xml is rebuilt as a` &&
            ` core:FragmentDefinition and shown with client->popover_display( by_id ), anchored on the product-switcher button the productSwitcherPressed event ships - which is exactly what fnOpen does with` &&
            ` oEvent.getParameter('button'). fnClose has no counterpart: the popover closes itself on an outside click, and the original only wires a Close button on PHONES (Device.system.phone), which is` &&
            ` device-conditional chrome rather than sample behaviour. // NOTE: fnChange composes its MessageToast from the pressed item's id, targetSrc and target. All three resolve on the client, so the toast` &&
            ` stays there as a roundtrip-free control_global MESSAGE_TOAST show with three placeholders. // NOTE: model/data.json is seeded verbatim - all fourteen entries with their src and title, and the`.
    text1 = text1 && ` subtitle on the seven rows that carry one. The fragment also binds targetSrc and target, which the mock never sets: they are undefined there and empty here, which renders the same. // NOTE: not yet` &&
            ` verified in a running system: the product-switch popover anchored on the ShellBar's switcher button and the change toast. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_583.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.ShellBar`                        name = `ShellBarProductSwitch`                         class = `z2ui5_cl_smpc_app_583` path = `src/02/04/z2ui5_cl_smpc_app_583.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.63`
        is_post171 = abap_true
        notes = text1
        post171 = `three post-1.71 controls are kept for the 1:1 port: sap.f.ProductSwitch and sap.f.ProductSwitchItem (both since UI5 1.72), which are what this sample shows, and sap.m.Avatar (since 1.73) in the` &&
                 ` ShellBar's profile aggregation. App 245 (ProductSwitchNavigation) carries the same pair as a documented scope exception.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.ShellBar`                        name = `ShellBarWithMenuButton`                        class = `z2ui5_cl_smpc_app_152` path = `src/02/04/z2ui5_cl_smpc_app_152.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.63`
        is_post171 = abap_true
        notes = `NOTE: Static sap.f.ShellBar with menu/nav/search/notifications/copilot toggles, homeIcon, notificationsNumber and a profile Avatar (initials), reproduced 1:1. The homeIcon points at the OpenUI5 host` &&
                 ` (https://sdk.openui5.org/resources/sap/ui/documentation/sdk/images/logo_sap.png) per the asset-URL rule; the original uses the relative ./resources/... path (app 110 precedent, same ShellBar` &&
                 ` homeIcon). // POST-1.71: sap.m.Avatar (control @since 1.73) is used 1:1 as the profile avatar - the profile aggregation's m:Avatar with initials. Newer than UI5 1.71; declared per the property-171` &&
                 ` policy (apps 099/244 precedent), so the app needs UI5 >= 1.73 to render the avatar.`
        post171 = `sap.m.Avatar (control @since 1.73) is used 1:1 as the profile avatar - the profile aggregation's m:Avatar with initials. Newer than UI5 1.71; declared per the property-171 policy (apps 099/244` &&
                 ` precedent), so the app needs UI5 >= 1.73 to render the avatar.` ) ).

    text1 = `NOTE: suggest handler (handlerSearchSuggestEvent): the original builds two sap.ui.model.Filter objects with custom JS test functions (case-insensitive substring on ProductId and Name) and applies them` &&
            ` to the SearchManager's suggestionItems binding, then calls oSF.suggest() to reopen the suggestion popup. The custom JS filter functions are inexpressible (control-side JS), but their semantics is the` &&
            ` whitelisted 'Contains' operator (case-insensitive on client bindings when Filter.caseSensitive is unset), so the filter is reproduced 1:1 via cs_event-binding_call (compound OR group over` &&
            ` PRODUCTID/NAME Contains the typed value), CAPABILITIES 'Controller-applied binding filter' / app 022. The typed text is transported as the ${$parameters>/suggestValue} event arg and read with` &&
            ` get_event_arg. Since 2026-07-30 the imperative oSF.suggest() popup-reopen is wired too, as a second follow_up_action( control_by_id, searchField/suggest ) - sap.f.SearchManager.suggest() is a public` &&
            ` non-denied method via the generalized allowlist (closing the 2026-07-27 review-flagged gap). // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real`.
    text1 = text1 && ` browser): typing into the ShellBar search fires the client-composed liveChange toast ('<id> liveChange event value is: <value>', the app-005 idiom over cs_event-control_global). The SearchManager` &&
            ` renders collapsed in the ShellBar, so the interaction expands it through the Search button first. Residual: the search event's own toast and the suggest -> binding_call Contains filter round-trip` &&
            ` with its effect on the suggestion list. (Corrected 2026-08-23: the module asserts the constant middle of the template only, so a failure to substitute either placeholder would still pass.) // NOTE:` &&
            ` The ShellBar homeIcon asset URL './resources/sap/ui/documentation/sdk/images/logo_sap.png' is repointed to the OpenUI5 host 'https://sdk.openui5.org/resources/...' per the asset-URL rule (same as app` &&
            ` 110). The mock's per-row ProductPicUrl values are kept byte-identical to sap/ui/demo/mock/products.json (unbound display data, never rendered by this view). Rows without a DateOfSale in the mock` &&
            ` carry an empty string (the property is not bound). Review finding 2026-07-27: the row type inlines all 20 mock keys although the view binds only Name, Price, CurrencyCode and ProductId - against`.
    text1 = text1 && ` AGENTS.md par.5 'unbound mock keys stay out of the row type'; the unbound columns (category, maincategory, taxtarifcode, suppliername, weightmeasure, weightunit, description, dateofsale,` &&
            ` productpicurl, status, quantity, uom, width, depth, height, dimunit) should be trimmed. // POST-1.71: the sap.m.Avatar control (since UI5 1.73) is newer than 1.71 but kept for the 1:1 port - the app` &&
            ` needs a UI5 release >= 1.73 to render it. @since verified in sap/m/Avatar.js:99 (control-level, which the member-level property gate never saw).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.ShellBar`                        name = `ShellBarWithSearch`                            class = `z2ui5_cl_smpc_app_218` path = `src/02/04/z2ui5_cl_smpc_app_218.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.63`
        is_post171 = abap_true
        notes = text1
        post171 = `the sap.m.Avatar control (since UI5 1.73) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.73 to render it. @since verified in sap/m/Avatar.js:99 (control-level, which` &&
                 ` the member-level property gate never saw).` ) ).

    text1 = `POST-1.71: two post-1.71 members are kept for the 1:1 port: sap.m.Avatar (since UI5 1.73) in the ShellBar's profile aggregation, and sap.tnt.NavigationListItem.expanded (since 1.121), which the` &&
            ` sample's own NavigationList fragment binds - app 407 declares the same property with the note that it predates 1.71 on the control and only reads as 1.121 because it moved to a newer base class. //` &&
            ` NOTE: the sample's two fragments (SideNavigation and the NavigationList item template it uses twice) are built inline; the item template is emitted by one parameterised method, so the reconstructed` &&
            ` view shows one NavigationList where the original has two - a helper's chain is emitted once however often it is called. // NOTE: onMenuButtonPress calls toolPage.setSideExpanded(!getSideExpanded()).` &&
            ` sideExpanded is a bindable property, so the port binds a flag and the handler flips it. onItemSelect's pageContainer.to(key) is the one frontend action (control_by_id 'to'), with the pressed item's` &&
            ` key travelling on the event; the SideNavigation's selectedKey is bound to the same field. **Corrected 2026-08-27**: that binding keeps the highlight across a ROUND TRIP, but it does NOT keep it in`.
    text1 = text1 && ` step with the NavContainer across a view REBUILD - the earlier claim that it does was the defect, not the remedy. The bound key survives the rebuild and the NavContainer does not, so the two came` &&
            ` apart; see the rebuild NOTE below for the re-issue that actually holds them together. // NOTE: model.json is seeded verbatim - the four navigation roots with their icons, keys and children and the` &&
            ` three fixed items. The item template also binds enabled, which the mock never sets: it is seeded true, the NavigationListItem default, because a flat ABAP row would otherwise send abap_false and` &&
            ` disable every item. // NOTE: the second page's Text carries the sample's own multi-paragraph lorem ipsum. An XML attribute value normalizes its line breaks and indentation to single spaces before the` &&
            ` control ever sees it, so the port stores that normalized string - the same text the browser renders. // NOTE: not yet verified in a running system: the side navigation with its nested items, the` &&
            ` menu-button toggle and the page switch. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_585.mjs). // NOTE: A NavContainer's position is LIVE CONTROL STATE`.
    text1 = text1 && ` and does not survive a view rebuild. view_display( ) destroys the MAIN slot and XMLView.create builds a fresh control tree, so pageContainer comes back on the initialPage its XML declares - while` &&
            ` selectedkey (bound to the SideNavigation's selectedKey, and written by the ITEM_SELECT branch), being two-way bound class state, survives the round trip and the draft. The app then showed one page` &&
            ` while its own navigation control claimed another. Fixed 2026-08-27 with the app-000 re-issue idiom: the end of view_display( ) sends the SAME control_by_id 'to' payload the ITEM_SELECT path last` &&
            ` sent, guarded so a fresh view already sitting on the initialPage="page2" issues nothing. The second view_display( ) is reached through the framework's own bookmark restore` &&
            ` (?app_start=<class>#/z2ui5-xapp-state=<draft>, the URL app_state_get_href( ) hands out): that request carries no frontend id, so the backend takes factory_first_start -> db_load(draft),` &&
            ` check_on_navigated( ) is true while check_on_init( ) stays false - the ELSEIF branch, and the only way a port that calls no other app renders twice. The interaction module asserts BOTH halves, the`.
    text1 = text1 && ` surviving key AND the re-issued page; asserting only the reset half would pass on a port that never navigated. Guard: selectedkey IS NOT INITIAL AND selectedkey <> ``page2``. Measured 2026-08-27 on` &&
            ` the built backend, BEFORE the fix: select "Child Item 1" (key page1), restore the very same draft through the bookmark URL - the rebuilt view came back showing mainView--page2 while the` &&
            ` SideNavigation still read page1.`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.ShellBar`                        name = `ShellBarWithSplitApp`                          class = `z2ui5_cl_smpc_app_585` path = `src/02/04/z2ui5_cl_smpc_app_585.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.63`
        is_post171 = abap_true
        notes = text1
        post171 = `two post-1.71 members are kept for the 1:1 port: sap.m.Avatar (since UI5 1.73) in the ShellBar's profile aggregation, and sap.tnt.NavigationListItem.expanded (since 1.121), which the sample's own` &&
                 ` NavigationList fragment binds - app 407 declares the same property with the note that it predates 1.71 on the control and only reads as 1.121 because it moved to a newer base class.` ) ).

    text1 = `NOTE: onToggle vetoes the firing when the matching Switch is on (event.preventDefault) and resets that Switch. **Reproduced 1:1 since 2026-08-23** with s_ctrl-prevent_default_expr: the client` &&
            ` expression (!${$parameters>/expanded} && {/PREVENT_COLLAPSE}) || (${$parameters>/expanded} && {/PREVENT_EXPAND}) is evaluated when the event fires and reads the two two-way bound Switch states, which` &&
            ` is exactly what the original handler does. The event still reaches the backend either way, and on_event raises the matching toast and resets that Switch. Two defects of the earlier shape are gone` &&
            ` with it. (1) The boolean s_ctrl-check_prevent_default is baked into the XML at RENDER time and the Switches carry no event, so a flipped Switch only reached the wire on the next render: the first` &&
            ` toggle after a flip - the one the flip was meant to block - was never vetoed. The sidecar's claim that the direction of the next toggle is known and the round-trip re-bakes the flag was wrong on both` &&
            ` halves. (2) The TOGGLE branch ended in view_display( ), and a re-render hands back a fresh control tree: SidePanel.selectedItem is an association and sideContentExpanded a hidden property, so the`.
    text1 = text1 && ` side content the user had just opened collapsed again on every toggle - the sample's central interaction never persisted. There is no re-render any more; the Switch reset travels with the automatic` &&
            ` model push. // POST-1.71: the SidePanel and SidePanelItem controls (both since UI5 1.107) are newer than 1.71 but are the whole point of this sample and kept for the 1:1 port - the app needs a UI5` &&
            ` release >= 1.107 to render it. @since verified in sap/f/SidePanel.js and sap/f/SidePanelItem.js:34 (control-level, which the member-level property gate never saw).`.
    result = VALUE #( BASE result
      ( module = `sap.f`              control = `sap.f.SidePanel`                       name = `SidePanelSingle`                               class = `z2ui5_cl_smpc_app_136` path = `src/02/04/z2ui5_cl_smpc_app_136.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.107`
        since_post171 = abap_true
        is_post171 = abap_true
        notes = text1
        post171 = `the SidePanel and SidePanelItem controls (both since UI5 1.107) are newer than 1.71 but are the whole point of this sample and kept for the 1:1 port - the app needs a UI5 release >= 1.107 to render` &&
                 ` it. @since verified in sap/f/SidePanel.js and sap/f/SidePanelItem.js:34 (control-level, which the member-level property gate never saw).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ActionListItem`                  name = `ActionListItem`                                class = `z2ui5_cl_smpc_app_001` path = `src/01/01/z2ui5_cl_smpc_app_001.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_001.mjs: the Actions list header and all five ActionListItems render as .sapMALI rows with their texts - a static port, so the render IS the` &&
                 ` check)` )
      ( module = `sap.m`              control = `sap.m.Bar`                             name = `Page`                                          class = `z2ui5_cl_smpc_app_002` path = `src/01/01/z2ui5_cl_smpc_app_002.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_002.mjs: the page title, the nav button plus the Share header action, the subHeader SearchField, the lorem ipsum content and the four footer` &&
                 ` buttons Accept/Reject/Edit/Delete all render)` )
      ( module = `sap.m`              control = `sap.m.Bar`                             name = `ToolbarVsBar`                                  class = `z2ui5_cl_smpc_app_189` path = `src/01/01/z2ui5_cl_smpc_app_189.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: the original wires a change handler on the separator Select (SEP_CHANGE round-trip, removed 2026-07-16): selectedKey and separatorStyle share one two-way model path, so the Select.change` &&
            ` attribute is deliberately MISSING vs the original view. Instant switching confirmed in the 2026-07-20 live check (restamp). // NOTE: the link toast was switched from a message_toast_display` &&
            ` round-trip to a roundtrip-free client-composed toast on 2026-07-22 (control_global MESSAGE_TOAST.show, template ``{0} has been activated`` filled by ${$source>/text}; on_event dropped) - re-verify` &&
            ` each breadcrumb link toasts "<text> has been activated". **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the overflow-picker entry 'Products' fires the link` &&
            ` press and toasts 'Products has been activated' (the headless layout collapses the links into the Breadcrumbs overflow Select; the picker path exercises the same press wire).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Breadcrumbs`                     name = `Breadcrumbs`                                   class = `z2ui5_cl_smpc_app_003` path = `src/01/01/z2ui5_cl_smpc_app_003.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed); RESTAMP after the 2026-07-16 rework (link toast +` &&
                 ` instant separator switch confirmed)`
        notes = text1 ) ).

    text1 = `POST-1.71: the sample's whole point is the currentLocation aggregation of sap.m.Breadcrumbs (since UI5 1.123), which shows the current page as a Link instead of plain text - kept 1:1, so the app needs` &&
            ` a UI5 release >= 1.123 to render it there (on an older release the whole VIEW fails to load). (Corrected 2026-08-23: sap.m.Breadcrumbs has no items aggregation at all - its aggregations are links` &&
            ` (@1.34, the default) and currentLocation (@1.123) - and XMLTemplateProcessor does not fall back to the default aggregation for an unknown named element: it tries to instantiate sap.m.currentLocation` &&
            ` as a control and throws. The declared floor itself was right.) // NOTE: each Link keeps the original's client-side MessageToast.show(evt.getSource().getText() + ' has been clicked') as a` &&
            ` roundtrip-free follow_up_action MESSAGE_TOAST with the template '{0} has been clicked' filled by ${$source>/text} - the text comes from the pressed Link itself, exactly as in the original, and` &&
            ` nothing travels to the backend. The class therefore has no on_event and no model at all.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Breadcrumbs`                     name = `BreadcrumbsWithCurrentPageLink`                class = `z2ui5_cl_smpc_app_286` path = `src/02/01/z2ui5_cl_smpc_app_286.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `the sample's whole point is the currentLocation aggregation of sap.m.Breadcrumbs (since UI5 1.123), which shows the current page as a Link instead of plain text - kept 1:1, so the app needs a UI5` &&
                 ` release >= 1.123 to render it there (on an older release the whole VIEW fails to load). (Corrected 2026-08-23: sap.m.Breadcrumbs has no items aggregation at all - its aggregations are links (@1.34,` &&
                 ` the default) and currentLocation (@1.123) - and XMLTemplateProcessor does not fall back to the default aggregation for an unknown named element: it tries to instantiate sap.m.currentLocation as a` &&
                 ` control and throws. The declared floor itself was right.)` ) ).

    text1 = `NOTE: onPress shows MessageToast.show(oEvent.getSource().getText() + ' has been clicked'). All six Links compose it on the client via follow_up_action control_global MESSAGE_TOAST.show with the` &&
            ` template '{0} has been clicked' filled by ${$source>/text}, so the pressed link's own text is transported rather than faked and no press needs a round-trip. // NOTE: onChange copies` &&
            ` selectedItem.getKey() into /separatorStyle, which the Breadcrumbs binds. The port two-way binds one field on Select.selectedKey and Breadcrumbs.separatorStyle, so the switch happens roundtrip-free` &&
            ` and the Select.change attribute is dropped. // NOTE: The original leaves /separatorStyle unset until the first change, so the Breadcrumbs starts on the control default. An initial ABAP string field` &&
            ` would serialize as "" and an enum-typed property rejects that. (It CAN be made absent - _bind carries omit_initial / omit_initial_paths precisely so an initial field stays out of the model and the` &&
            ` control keeps its own default, live in apps 241/299/308. The port simply does not need it here: Breadcrumbs.separatorStyle defaults to Slash, which is what is seeded, and Select.forceSelection would`.
    text1 = text1 && ` have picked the first item - also Slash - anyway. The claim that a field cannot be absent was wrong and is corrected 2026-08-24.) - so the port seeds the control's own default value Slash` &&
            ` (sap.m.Breadcrumbs.separatorStyle defaultValue), which renders identically and pre-selects the matching Select entry. // NOTE: The client-composed link toasts and the two-way bound separatorStyle` &&
            ` (Select -> Breadcrumbs) are unverified in a running system. The module drives only the Select -> Breadcrumbs field; none of the six MESSAGE_TOAST wires is ever pressed. **e2e-verified 2026-08-22**` &&
            ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_441.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Breadcrumbs`                     name = `BreadcrumbsWithoutCurrentPage`                 class = `z2ui5_cl_smpc_app_441` path = `src/01/01/z2ui5_cl_smpc_app_441.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: the BusyDialog fragment is rebuilt 1:1 as a core:FragmentDefinition shown via client->popup_display (the framework's displayFragment calls .open() on the fragment root, the equivalent of the` &&
            ` controller's Fragment.load + oBusyDialog.open()); one attribute is added vs the original fragment: id="busyDialog", needed so the backend timer event can close the dialog via the whitelisted` &&
            ` control_by_id 'close' method (CONTROL_METHODS in FrontendAction.js, no-arg; the id resolves in the POPUP slot via Fragment.byId('popupId', ...)) - the equivalent of the controller's` &&
            ` oBusyDialog.close(). // NOTE: the controller's simulateServerRequest (setTimeout 3000ms) becomes the framework timer: follow_up_action( cs_event-start_timer, callbackEvent TIMER_FINISHED, 3000 )` &&
            ` started with the popup. A running frontend timer cannot be cleared from ABAP (the original's clearTimeout in onDialogClosed), so after a cancel the timer still fires one backend round-trip; a` &&
            ` protected guard flag (check_busy) ignores it - same visible behavior (no second toast, dialog already closed), one extra no-op round-trip. // NOTE: the controller's syncStyleClass('sapUiSizeCompact',`.
    text1 = text1 && ` view, dialog) is dropped: it only copies the compact-density class when the view itself carries it, which the sample view does not; abap2UI5's popup slot has no per-view density sync, so there is` &&
            ` nothing to propagate.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.BusyDialog`                      name = `BusyDialog`                                    class = `z2ui5_cl_smpc_app_004` path = `src/01/01/z2ui5_cl_smpc_app_004.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_004.mjs: pressing 'Show Busy Dialog' opens the popup_display BusyDialog with 'Loading Data', the START_TIMER(3000) -> TIMER_FINISHED -> control_by_id close chain` &&
                 ` takes it down again on its own and DIALOG_CLOSED toasts 'The operation has been completed'. The Cancel toast word rides on the BOOLEAN cancelPressed arg, which the transpiled runtime delivers as the` &&
                 ` string 'true' - that half stays with the human check)`
        notes = text1 ) ).

    text1 = `NOTE: BusyDialog.fragment.xml is inlined into the l:dependents aggregation - the core:Fragment reference element ('core:Fragment' fragmentName sap.m.sample.BusyDialogLight.BusyDialog) is dropped for` &&
            ` the inlined BusyDialog, per the fragment-inlining rule. handlePress is the app-147 idiom: the SHOW_BUSY round-trip issues follow_up_action control_by_id BusyDialog open plus START_TIMER CLOSE_BUSY` &&
            ` 3000, and the timer round-trip closes the dialog - the setTimeout(close, 3000) equivalent with one round-trip latency added to the 3s. // NOTE: unverified in a running system: the open + 3s timer +` &&
            ` close chain on the dependents-declared BusyDialog. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the full chain runs green - the button opens the BusyDialog` &&
            ` (attached) and the CLOSE_BUSY timer round-trip detaches it after ~3s.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.BusyDialog`                      name = `BusyDialogLight`                               class = `z2ui5_cl_smpc_app_251` path = `src/01/01/z2ui5_cl_smpc_app_251.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Button`                          name = `Button`                                        class = `z2ui5_cl_smpc_app_005` path = `src/01/01/z2ui5_cl_smpc_app_005.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-15): manually verified in a running system - each press toasts the pressed button's client-side control id, read via the event arg $event.oSource.sId, exactly like the original.`
        notes = `NOTE: the toast was switched from a message_toast_display round-trip to a roundtrip-free client-composed toast on 2026-07-22 (control_global MESSAGE_TOAST.show, template ``{0} Pressed`` filled by` &&
                 ` $event.oSource.sId; the app is now init-only) - re-verify each button still toasts "<id> Pressed". **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the 'Default'` &&
                 ` button press toasts 'Pressed' (the $event.oSource.sId template resolves); the other buttons are the identical wire.` ) ).

    text1 = `POST-1.71: The badge API is newer than 1.71 and kept 1:1 per the fidelity-first member policy: sap.m.BadgeCustomData (control @since 1.80, secondary control - sap.m.Button is the in-scope headline` &&
            ` entity, the app-244 Avatar precedent), Button.badgeStyle (@since 1.132) and sap.m.Button's own setBadgeMinValue/setBadgeMaxValue (they are on Button.prototype, not in BadgeEnabler) driven via` &&
            ` control_by_id. Neither is a PROPERTY: Button declares exactly one badge property, badgeStyle (measured 2026-08-26 off the live control metadata), and the two bounds live in the private` &&
            ` _badgeMinValue/_badgeMaxValue - so a binding is not available for them and a control call is the only wire there is. The app needs a UI5 release >= 1.132 to render the badgeStyle; the badge itself` &&
            ` needs >= 1.80. // NOTE: Thin-frontend rewires, all declared: (a) the StepInput's change='.currentChangeHandler' attribute is DROPPED - StepInput.value and BadgeCustomData.value are two-way bound to` &&
            ` the same /BADGECURRENT field, so the badge follows the stepper client-side (the 007/128 pattern; the original copied the value imperatively via getBadgeCustomData().setValue()); BadgeCustomData.value`.
    text1 = text1 && ` is that binding instead of the original's empty literal, and its empty visible literal resolves to FALSE. (b) minChangeHandler/maxChangeHandler run server-side (business logic in ABAP): the two-way` &&
            ` bound min/max round-trip on change, the accepted-range check (1 <= min <= max <= 9999) mirrors the original incl. resetting the field to the last accepted value on an invalid entry, and the accepted` &&
            ` value reaches the button via follow_up_action control_by_id setBadgeMin/MaxValue (public Button.prototype methods via the generalized allowlist). Both setters are ALSO re-issued at the end of` &&
            ` view_display, min first, from min_accepted/max_accepted - added 2026-08-26. A control call does not survive a view rebuild the way a binding does: displaying MAIN destroys the slot and XMLView.create` &&
            ` builds a NEW Button whose init resets _badgeMinValue/_badgeMaxValue to 1/9999, while badgemin/badgemax (bound) and min_accepted/max_accepted (class state) come back untouched - so without the` &&
            ` re-issue the app returned from a draft restore showed min 5 / max 50 in the two Inputs over a Button that was back at 1/9999, badge included (measured 2026-08-26 in the headless e2e). A value equal`.
    text1 = text1 && ` to the Button's own default is skipped, because the setter rejects an unchanged value and logs it as invalid. Order note measured with it: the wire carries the value as a STRING (castArgs infers, and` &&
            ` a numeric string stays a string for a method outside CONTROL_METHODS), and each guard compares it against whatever the other bound already holds - so a pair whose decimal lengths differ, min 9 with` &&
            ` max 50, compares lexicographically and is rejected. That is the pre-existing behaviour of the live MIN_CHANGE/MAX_CHANGE wire too, not something the re-issue introduces. Not copied: the original` &&
            ` maxChangeHandler's quirk of calling setBadgeMaxValue once BEFORE validating - the accepted path is identical. (c) onInit's initial currentChangeHandler() call is unnecessary - the binding seeds the` &&
            ` badge. (d) badgeMin/badgeMax/badgeCurrent are TYPE p LENGTH 8 DECIMALS 0 - the original model carries min/max as strings, but the bound values are numeric-only and the numeric-bound-as-string lint` &&
            ` (app-053 lesson) wants the model to serialize real numbers, which p does exactly as i did; Input.value coerces either way. They were TYPE i until 2026-08-26, and that was a defect: all three are`.
    text1 = text1 && ` two-way bound to a FREE-ENTRY control (two Inputs of type Number, one StepInput), ajson's value_to_abap writes the typed text back with a bare ABAP assignment, and <input type="number"> accepts any` &&
            ` valid floating-point literal - so an eleven-digit entry overflowed i and killed the round-trip with JSON_PARSING_ERROR - attribute 'BADGEMIN' BEFORE on_event could apply the accepted-range check in` &&
            ` (b), i.e. that check was unreachable for exactly the entries it exists to reject. The string-mirror idiom (app 363) is ruled out here by the same app-053 rule this deviation rests on - a mirror would` &&
            ` serialize the model value as a string - so widening is the fix, the type apps 180 and 247 use. (Corrected 2026-08-23: DataType's boolean parseValue is sValue == "true", so visible="" is false, while` &&
            ` BadgeCustomData's declared default is true. The badge nevertheless appears in the original because Button.badgeValueFormatter switches visible back on once the value reaches _badgeMinValue, triggered`.
    text1 = text1 && ` by onInit's currentChangeHandler. Same rendered result, different mechanism - and the mechanism is what this deviation rests on.) // NOTE: unverified in a running system: (a) the shared /BADGECURRENT` &&
            ` binding driving badge and StepInput; (b) the icon/text expression bindings over /BUTTONWITHICON//BUTTONWITHTEXT; (c) the MIN_CHANGE/MAX_CHANGE validation round-trips incl. the reset-on-invalid path` &&
            ` and the setBadgeMin/MaxValue follow-ups. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): (a) is covered - the badge renders data-badge 1 and follows the` &&
            ` StepInput to 2 after ArrowUp+Enter (the change event carries the two-way write); the MIN/MAX validation round-trips remain unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_249.mjs). **measured 2026-08-26** (the wire, not a green leg: the badge bounds were read off the live Button and the re-issue replayed in the page; the interaction` &&
            ` leg that asserts it FAILS against the transpiled backend as it stands and needs an e2e:build before the nightly can confirm it) (meta/interactions/z2ui5_cl_smpc_app_249.mjs): MIN_CHANGE/MAX_CHANGE`.
    text1 = text1 && ` are now exercised - min 5 hides the badge over the current value 2 (proof that setBadgeMinValue landed), max 50 reaches _badgeMaxValue - and the module then restores the saved draft through the` &&
            ` app-state hash (#/z2ui5-xapp-state=), the framework's come-back-to-this-app path, and asserts that the rebuilt Button still carries 5/50.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Button`                          name = `ButtonWithBadge`                               class = `z2ui5_cl_smpc_app_249` path = `src/02/01/z2ui5_cl_smpc_app_249.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `The badge API is newer than 1.71 and kept 1:1 per the fidelity-first member policy: sap.m.BadgeCustomData (control @since 1.80, secondary control - sap.m.Button is the in-scope headline entity, the` &&
                 ` app-244 Avatar precedent), Button.badgeStyle (@since 1.132) and sap.m.Button's own setBadgeMinValue/setBadgeMaxValue (they are on Button.prototype, not in BadgeEnabler) driven via control_by_id.` &&
                 ` Neither is a PROPERTY: Button declares exactly one badge property, badgeStyle (measured 2026-08-26 off the live control metadata), and the two bounds live in the private _badgeMinValue/_badgeMaxValue` &&
                 ` - so a binding is not available for them and a control call is the only wire there is. The app needs a UI5 release >= 1.132 to render the badgeStyle; the badge itself needs >= 1.80.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Carousel`                        name = `Carousel`                                      class = `z2ui5_cl_smpc_app_398` path = `src/02/01/z2ui5_cl_smpc_app_398.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: ariaLabelledBy on sap.m.Carousel (since UI5 1.125, source-verified in Carousel.js) is newer than 1.71 but kept for the 1:1 port - it is what links the Carousel to the sample's Title. The` &&
                 ` app needs a UI5 release >= 1.125 for the association to take effect; the Carousel itself renders on 1.71. // NOTE: the four Images bind src against the demo kit's named img JSONModel` &&
                 ` (img>/products/pic1, pic2, pic3, screw). abap2UI5 serves one default model, so the four fixed values are inlined as literals off one shared base URL and host-absolutized to https://sdk.openui5.org/` &&
                 ` (app 031 precedent) - same images, same order, same rendering.`
        post171 = `ariaLabelledBy on sap.m.Carousel (since UI5 1.125, source-verified in Carousel.js) is newer than 1.71 but kept for the 1:1 port - it is what links the Carousel to the sample's Title. The app needs a` &&
                 ` UI5 release >= 1.125 for the association to take effect; the Carousel itself renders on 1.71.` ) ).

    text1 = `POST-1.71: sap.m.Carousel.ariaLabelledBy is @since 1.125; the original wires the Carousel to its Title through it, so the association is kept 1:1 and the port needs a UI5 runtime >= 1.125 for the` &&
            ` accessibility label. // NOTE: onResizeCarouselContainer sets carousel.setWidth(value + '%') on every Slider liveChange. The port reproduces it roundtrip-free: the Slider value is two-way bound and` &&
            ` the Carousel carries a width expression binding over the same field. The Slider.liveChange attribute is therefore dropped and Carousel.width is added (structural-diff reports attr missing` &&
            ` Slider.liveChange). // NOTE: The Slider value -> Carousel width expression binding is client-side model propagation and is unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_427.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Carousel`                        name = `CarouselEmptyMessages`                         class = `z2ui5_cl_smpc_app_427` path = `src/02/01/z2ui5_cl_smpc_app_427.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Carousel.ariaLabelledBy is @since 1.125; the original wires the Carousel to its Title through it, so the association is kept 1:1 and the port needs a UI5 runtime >= 1.125 for the accessibility` &&
                 ` label.` ) ).

    text1 = `NOTE: the three carousel images bind to a separate named model in the original (img>/products/pic1..3 from sap/ui/demo/mock/img.json); resolved here to static image URLs, as abap2UI5 serves a single` &&
            ` default model. // POST-1.71: ariaLabelledBy on the Carousel (since UI5 1.125) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.125 to render it. // NOTE: The sample's` &&
            ` asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
            ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by` &&
            ` all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since` &&
            ` today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Carousel`                        name = `CarouselWithControls`                          class = `z2ui5_cl_smpc_app_006` path = `src/02/01/z2ui5_cl_smpc_app_006.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-15): manually verified in a running system - renders and scrolls like the original (see the note below on the flattened image model). **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_006.mjs: the Carousel renders its five pages, the bound product list on the fourth page fills from the model (>= 100 items, Notebook Basic 15 among them) and` &&
                 ` ArrowRight on the focused carousel scrolls to the second page)`
        notes = text1
        post171 = `ariaLabelledBy on the Carousel (since UI5 1.125) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.125 to render it.` ) ).

    text1 = `POST-1.71: five post-1.71 members are kept for the 1:1 port, and they are what the sample is FOR: sap.m.Carousel.backgroundDesign (@since 1.110), pageIndicatorBackgroundDesign and` &&
            ` pageIndicatorBorderDesign (both @since 1.115), ariaLabelledBy (@since 1.125) and sap.m.CarouselLayout.scrollMode (@since 1.121). The app needs a UI5 release >= 1.125 for every option it offers to` &&
            ` have an effect. // NOTE: every one of the controller's eleven handlers is a SETTER on a bindable property - setArrowsPlacement, setPageIndicatorPlacement, setBackgroundDesign,` &&
            ` setPageIndicatorBackgroundDesign, setPageIndicatorBorderDesign, setShowPageIndicator, setVisiblePagesCount, setScrollMode, setResponsive, setMinPageWidth and the container's setWidth / setHeight. The` &&
            ` port binds each option control and the Carousel (or its CarouselLayout, or the Panel) to the SAME field, so the bindable-property-beats-frontend-action rule applies throughout and there is not a` &&
            ` single frontend action in this port. The Switches for showPageIndicator and responsive, and the Inputs for the visible page count and minPageWidth, therefore need no handler at all. // NOTE: the five`.
    text1 = text1 && ` enum options are picked by RadioButtonGroups, whose bindable property is selectedIndex - an integer - while the handlers read getSelectedButton().getText() and pass that string to the setter. The` &&
            ` port binds selectedIndex two-way and adds ONE round trip (event OPTIONS, shared by the four groups and the scroll-mode Switch) that turns the five indices into the five enum names the Carousel binds.` &&
            ` The alternative, a ternary chain in an expression binding, was written first and dropped: it pushed two attribute lines past the 255-character abapGit limit. // NOTE: each of the six bound enum` &&
            ` values carries the null fallback ({= ${X} || null }, apps 548/555 idiom): a flat ABAP row serializes every field, so before the first OPTIONS round trip an unset enum would arrive as an empty string` &&
            ` and UI5 would reject the whole view. null maps to the property's default, which is what 'not set' means. model_init also seeds all six from the sample's own initial selections (Translucent` &&
            ` background, page indicator on, Solid indicator background and border). // NOTE: onResizeCarouselContainer sets the Panel's width to <value>% and its height to Math.floor(650 * value / 100) + 'px' on`.
    text1 = text1 && ` every liveChange. Both are expression bindings over the two-way bound Slider value here, so the container follows the slider in the browser with no round trip - and the liveChange wire is dropped,` &&
            ` since a round trip per keystroke would be slower AND lossy (app 582 carries the same fold). // IMPROVISED: _setNumberOfImagesInCarousel destroys the pages aggregation and adds N new sap.m.Image` &&
            ` controls, each bound to img>/images/<i>. An aggregation cannot be rebuilt control-by-control from a backend, so pages is BOUND to a table and the Number input's change (not liveChange - the final` &&
            ` value, per the prefer-a-bindable-property rule) rebuilds it from the nine mock images, keeping the sample's own 1..9 guard. The nine Images carry the alt text addPage composes ('Example picture <n>')` &&
            ` and the same densityAware / decorative flags. // NOTE: the nine image paths come from sap/ui/demo/mock/img.json /images and point at the sdk.openui5.org host per the offline asset-URL rule; the mock` &&
            ` writes them relative as 'test-resources/sap/ui/documentation/sdk/images/...'. // NOTE: num_images, visible_pages and min_page_width are TYPE p LENGTH 8 DECIMALS 0, not i (widened 2026-08-26). Each is`.
    text1 = text1 && ` two-way bound to a FREE-ENTRY Input of type Number and ajson's value_to_abap writes the typed text back with a bare ABAP assignment - there is no CONV to guard - so an eleven-digit entry overflowed i` &&
            ` and killed the round-trip with JSON_PARSING_ERROR - attribute 'NUM_IMAGES' before on_event ran. That made pages_rebuild's own 1..9 guard unreachable for exactly the entries it rejects, and` &&
            ` visible_pages / min_page_width have no guard at all, so nothing at all stood between the keyboard and the conversion for those two. p keeps the JSON node numeric (ajson classes packed as numeric), so` &&
            ` the bound int properties visiblePagesCount and minPageWidth see the same wire as before. Same fix and same type as apps 180, 247 and 249. // NOTE: not yet verified in a running system: that each of` &&
            ` the five radio groups repaints the carousel, that the slider resizes its container and that the image-count input rebuilds the pages. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_604.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Carousel`                        name = `CarouselWithDisplayOptions`                    class = `z2ui5_cl_smpc_app_604` path = `src/02/01/z2ui5_cl_smpc_app_604.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `five post-1.71 members are kept for the 1:1 port, and they are what the sample is FOR: sap.m.Carousel.backgroundDesign (@since 1.110), pageIndicatorBackgroundDesign and pageIndicatorBorderDesign (both` &&
                 ` @since 1.115), ariaLabelledBy (@since 1.125) and sap.m.CarouselLayout.scrollMode (@since 1.121). The app needs a UI5 release >= 1.125 for every option it offers to have an effect.` ) ).

    text1 = `POST-1.71: Members newer than 1.71 kept 1:1: sap.m.Carousel.ariaLabelledBy (@1.125) and sap.m.CarouselLayout.scrollMode (@1.121); the CarouselLayout control itself is @1.62. The app needs a UI5` &&
            ` release >= 1.125 to render both. // NOTE: The two named models are folded onto the one default model (pure prefix-drops, same leaf names): products>/ProductCollection -> /T_PRODUCTS (the full 123-row` &&
            ` sap/ui/demo/mock/products.json with the nine bound fields; onInit's oProductsModel.setSizeLimit(10) rides 1:1 as follow_up_action cs_event-set_size_limit 10 MAIN, so exactly 10 pages render like the` &&
            ` original) and settings>/pagesCount -> /PAGESCOUNT. The Device.system pagesCount branch (desktop 4 / tablet 2 / else 1) was resolved statically to the desktop default 4 when this NOTE was written; it` &&
            ` has been resolved from the device mirror since 2026-08-21 (see the deviation below, which supersedes this sentence and was added beside it instead of correcting it). ProductPicUrl values are` &&
            ` host-absolutized to https://sdk.openui5.org/ per the offline asset rule. Corrected 2026-08-23: COMBI counts as desktop. The original tests Device.system.desktop first, and a touchscreen laptop has`.
    text1 = text1 && ` desktop AND tablet true, so it takes the desktop leg; the mirror collapses that pair into the single string 'combi', and checking 'tablet' alone handed such a machine 2 pages where the sample gives` &&
            ` 4. // NOTE: Thin-frontend folds, both attribute drops declared: onNumberOfPages - the Input's liveChange attribute is DROPPED, Input.value is two-way bound with an added valueLiveUpdate='true' and` &&
            ` CarouselLayout.visiblePagesCount binds the same /PAGESCOUNT field, so typing drives the carousel client-side like the original's per-keystroke setVisiblePagesCount. OnScrollModeChange - the Switch's` &&
            ` change attribute is DROPPED, Switch.state is two-way bound and scrollMode is the expression binding {= state ? 'VisiblePages' : 'SinglePage' } over the same field - the original's imperative` &&
            ` setScrollMode as a binding. **Corrected 2026-08-23**: it did not. sap.m.Input.value is a string property, so the two-way write-back stored the typed text as a STRING, and` &&
            ` CarouselLayout.visiblePagesCount is an int - validateProperty casts implicitly for string properties only, so every keystroke threw '"5" is of type string, expected int' out of the binding and the`.
    text1 = text1 && ` page count never changed. The Input's value now carries type: 'IntegerType' (core:require), which is where the original's Number( sVisiblePageCount ) went. // NOTE: unverified in a running system:` &&
            ` the set_size_limit(10, MAIN) follow-up capping the pages binding, the shared /PAGESCOUNT driving Input and visiblePagesCount, and the scrollMode expression flip. **e2e-verified 2026-07-30**` &&
            ` (transpiled-framework interaction, scripts/e2e-smoke.mjs): exactly 10 carousel items render from the 123 bound rows (the set_size_limit(10, MAIN) follow-up caps the binding) with the first product` &&
            ` card visible; the pages-count input and scrollMode flip remain unexercised but are the proven 007/128 client-side class. // NOTE: /PAGESCOUNT is TYPE i behind a free-entry Input and its two-way` &&
            ` write-back never reaches ABAP - by construction, not by luck, so the widening apps 249/604 took on 2026-08-26 is not needed here. This port wires NO event at all: main handles check_on_init and` &&
            ` check_on_navigated only, there is no client->_event( ) anywhere, and the single follow_up_action is the set_size_limit seed that rides on the initial response. The frontend attaches the model delta`.
    text1 = text1 && ` to a request in exactly one place - the eB( ) event round-trip in the app's view module - so with no wired event there is no request carrying MODEL, and the typed value stays in the browser. An` &&
            ` out-of-range count is therefore a client-side concern only (the carousel is asked for that many pages); note that the type: 'IntegerType' on the Input keeps the node numeric but does not bound it -` &&
            ` Integer.parseValue rejects NaN and nothing else (Integer.js:101-110). // NOTE: onInit's device branch is resolved SERVER-SIDE since 2026-08-21: pagescount comes from client->get( )-s_device-system` &&
            ` (desktop 4 / tablet 2 / else 1), the mirror apps 012/173/302 already read. It was hard-coded to the desktop leg with a NOTE calling that 'resolved statically' - but CAPABILITIES marks the device` &&
            ` model ✅ and the branch is expressible, so declaring it did not make it right. The original seeds the value once in onInit and so does the port.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Carousel`                        name = `CarouselWithMorePages`                         class = `z2ui5_cl_smpc_app_252` path = `src/02/01/z2ui5_cl_smpc_app_252.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `Members newer than 1.71 kept 1:1: sap.m.Carousel.ariaLabelledBy (@1.125) and sap.m.CarouselLayout.scrollMode (@1.121); the CarouselLayout control itself is @1.62. The app needs a UI5 release >= 1.125` &&
                 ` to render both.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.CheckBox`                        name = `CheckBox`                                      class = `z2ui5_cl_smpc_app_155` path = `src/02/01/z2ui5_cl_smpc_app_155.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `NOTE: Fifteen CheckBoxes across all states (selected, partiallySelected, required, enabled, valueState Warning/Error/Information, wrapping) plus a SimpleForm with GridData-laid-out CheckBoxes,` &&
                 ` reproduced 1:1. // POST-1.71: CheckBox.required (@since 1.121, the 'Required option' CheckBox) is used 1:1. Newer than UI5 1.71; declared per the property-171 policy, so the app needs UI5 >= 1.121 to` &&
                 ` render the required state.`
        post171 = `CheckBox.required (@since 1.121, the 'Required option' CheckBox) is used 1:1. Newer than UI5 1.71; declared per the property-171 policy, so the app needs UI5 >= 1.121 to render the required state.` )
      ( module = `sap.m`              control = `sap.m.CheckBox`                        name = `CheckBoxTriState`                              class = `z2ui5_cl_smpc_app_007` path = `src/01/01/z2ui5_cl_smpc_app_007.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-15): manually verified in a running system - the select-all parent checkbox and its tri-state expression bindings behave like the original.; live-checked reference example for:` &&
                 ` expression bindings, two-way bind, boolean event arg. **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_007.mjs: the parent CheckBox boots selected+partiallySelected` &&
                 ` from the seeded true/false/true, ticking German clears partiallySelected and unticking all three clears selected - the two expression bindings re-evaluate client-side with no round trip. The` &&
                 ` PARENT_CLICKED round trip carries a BOOLEAN t_arg the transpiled runtime delivers as the string 'true', so that leg stays with the human check)` ) ).

    text1 = `NOTE: the color-select toast was switched to a roundtrip-free client-composed toast on 2026-07-22 (control_global MESSAGE_TOAST.show, template with {0}/{1} filled by ${$parameters>/value} and` &&
            ` ${$parameters>/defaultAction}; on_event dropped, init-only). **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): picking a colour (the swatch has a` &&
            ` zero-height box headless, so it is focused and activated with Enter) toasts 'Color Selected: value - gold, \n defaultAction - false' - the client-composed template with ${$parameters>/value} and` &&
            ` ${$parameters>/defaultAction} filled in, with no round-trip. // NOTE: The toast's line break is a REAL newline since 2026-08-21. An ABAP backtick literal has no escapes, so the ``\n`` the template` &&
            ` used to carry reached the visible text as two literal characters - the client's formatTemplate only substitutes {N}. The wire concatenates `` ... `` && |\n| && `` ... `` now, the string-template form` &&
            ` where \n IS an escape. Same defect and same fix as app 250, where the review sweep found it.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ColorPalette`                    name = `ColorPalette`                                  class = `z2ui5_cl_smpc_app_008` path = `src/01/01/z2ui5_cl_smpc_app_008.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.54`
        notes = text1 ) ).

    text1 = `NOTE: The controller lazily builds six differently configured ColorPalettePopover instances and openBy()s them; the port declares all six 1:1 in the view's mvc:dependents (ColorPalettePopover x6 -` &&
            ` extra controls vs the original view.xml, controller-built there) with the original ids/configurations, and every button press opens its popover roundtrip-free via follow_up_action control_by_id` &&
            ` openBy ($event.oSource.sId). handleColorSelect is the app-008 client-composed toast: 'Color Selected: value - {0}, \n defaultAction - {1}' filled by ${$parameters>/value} and` &&
            ` ${$parameters>/defaultAction}. The original's same-instance quirk (both displayMode buttons share oColorPaletteDisplayMode) is kept - both open the same declared popover. // POST-1.71: Members newer` &&
            ` than 1.71 kept 1:1 per the fidelity-first policy: sap.m.ColorPalettePopover.selectedColor (@1.122) and showRecentColorsSection (@1.74) on the selected-color popover, and sap.m.Button.ariaHasPopup` &&
            ` (@1.84) on all seven action buttons. The app needs a UI5 release >= 1.122 for the selectedColor variant; the control itself is @1.54. // NOTE: Two of the custom colors cannot ride an XML string[]`.
    text1 = text1 && ` attribute - the parser splits on commas, so hsl(0,100%,71%) and rgb(255,234,234) become their exact hex equivalents #ff6b6b and #ffeaea (same rendered swatches, different notation;` &&
            ` sap.ui.core.CSSColor validation rejects any comma-escaping workaround). // NOTE: handleLiveChange is reproduced since 2026-08-06, and the earlier verdict - 'direct DOM manipulation outside any` &&
            ` bindable property, not expressible in the thin frontend' - was too quick. The original paints the liveChangeButton's ICON by writing rgba(r,g,b,alpha) onto` &&
            ` getDomRef().firstChild.firstChild.style.color, i.e. the icon span, and the framework's ``css`` action deliberately writes only on a control's OWN DOM node (the pr/control-inline-style entry excludes` &&
            ` internal DOM on purpose). Measured instead of assumed (scripts/probes/, real OpenUI5): the icon span INHERITS color from the button root, so setting it there gives the identical computed colour` &&
            ` without touching internal DOM. The wire is roundtrip-free - the rgba() string is composed on the client from the four liveChange parameters, an event argument being a full UI5 expression. Still NOT`.
    text1 = text1 && ` ported: onExit's popover destroy calls - the declared dependents die with the view. **Corrected 2026-08-23**: that is true only for an argument that STARTS with $ or { (or is an .eB/.eBP/.eF call) -` &&
            ` get_t_arg wraps everything else in a single-quoted JS string. The rgba( ) expression opened with a quote, so it reached the css setter as literal text and CSSOM dropped it: the liveChange button's` &&
            ` icon never changed colour. It now leads with ${$parameters>/r} !== undefined, which is inert and buys the raw branch. The same shape had just been written into app 233 and was corrected with it. //` &&
            ` NOTE: unverified in a running system: the six dependents-declared ColorPalettePopover configurations opening anchored via openBy and the colorSelect toast argument resolution. **e2e-verified` &&
            ` 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the first action button opens its ColorPalettePopover anchored (palette content attached); the other five configurations are` &&
            ` the identical wire. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_250.mjs). // NOTE: The colorSelect toast carries a REAL line break since 2026-08-21. The`.
    text1 = text1 && ` original's JS double-quoted "...\n..." is a newline and .sapMMessageToast is white-space: pre-line, so it renders as one; an ABAP BACKTICK literal has no escapes, so the port's ``\n`` was two literal` &&
            ` characters that the client's formatTemplate (which only substitutes {N}) passed straight through to the visible text. All five wires now concatenate `` ... `` && |\n| && `` ... `` - the` &&
            ` string-template form, where \n IS an escape, and the form app 284 already used. Apps 008 and 186 carried the same defect and are fixed with it. Found by the review sweep; no gate compares a literal` &&
            ` toast text. // POST-1.71: sap.m.ColorPalettePopover.liveChange is @since 1.85 and is wired on the fifth popover, 1:1 with the original. Undeclared until 2026-08-23: the POST_171 entry beside it` &&
            ` predates the 2026-08-06 liveChange wire and was never extended, and an EVENT is exactly the member kind the property gate cannot see. The stated >= 1.122 floor is unchanged (selectedColor is the` &&
            ` higher one). // NOTE: The port declares ONE popover for the display-mode samples, so its liveChange handler is attached permanently. In the original the handler is conditional on press order:`.
    text1 = text1 && ` openSampleWithDisplayModeSet creates the instance without liveChange, and openSampleWithDisplayModeSetLiveChange only adds it if the instance does not exist yet - so pressing the 4th button first` &&
            ` leaves the 5th without live change. The shared instance was already declared; that it makes the handler unconditional was not.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ColorPalette`                    name = `ColorPalettePopover`                           class = `z2ui5_cl_smpc_app_250` path = `src/02/01/z2ui5_cl_smpc_app_250.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.54`
        is_post171 = abap_true
        notes = text1
        post171 = `Members newer than 1.71 kept 1:1 per the fidelity-first policy: sap.m.ColorPalettePopover.selectedColor (@1.122) and showRecentColorsSection (@1.74) on the selected-color popover, and` &&
                 ` sap.m.Button.ariaHasPopup (@1.84) on all seven action buttons. The app needs a UI5 release >= 1.122 for the selectedColor variant; the control itself is @1.54. // sap.m.ColorPalettePopover.liveChange` &&
                 ` is @since 1.85 and is wired on the fifth popover, 1:1 with the original. Undeclared until 2026-08-23: the POST_171 entry beside it predates the 2026-08-06 liveChange wire and was never extended, and` &&
                 ` an EVENT is exactly the member kind the property gate cannot see. The stated >= 1.122 floor is unchanged (selectedColor is the higher one).` ) ).

    text1 = `NOTE: the shared demo kit mock model sap/ui/demo/mock/products.json (/ProductCollection, snapshotted in ui5/mock/products.json) is flattened into the default model: all 123 rows are kept verbatim, but` &&
            ` only the bound columns (ProductId, Name, SupplierName, WeightMeasure, WeightUnit, Width, Depth, Height, DimUnit, Price, CurrencyCode) are ported - the unbound columns (Category, MainCategory,` &&
            ` TaxTarifCode, Description, DateOfSale, ProductPicUrl, Status, Quantity, UoM) of the shared mock model are dropped. // NOTE: the controller's onPopinLayoutChanged (ComboBox change handler running a` &&
            ` PopinLayout switch with a Block default) is expressed as bound properties instead of a round-trip (the recipe's 'prefer a bindable property' rule (.claude/skills/port-a-sample/SKILL.md; the linter` &&
            ` enforces it as settable-property-via-action)): the ComboBox's change attribute is dropped, its selectedKey is bound two-way, and the Table gains a popinLayout expression binding that reproduces the` &&
            ` switch including its Block default. // NOTE: the controller's onToggleInfoToolbar (ToggleButton press handler calling getInfoToolbar().setVisible(!pressed)) is expressed as bound properties instead`.
    text1 = text1 && ` of a round-trip: the ToggleButton's press attribute is dropped, its pressed property is bound two-way, and the infoToolbar's OverflowToolbar gains a visible={= !pressed } expression binding. // NOTE:` &&
            ` the controller's onSelect (imperative oTable.setSticky array maintenance from the CheckBox text + selected parameter) becomes a sticky property on the Table bound to a plain string table: each` &&
            ` CheckBox select event round-trips ${$source>/text} and ${$parameters>/selected}, the ABAP handler inserts/removes that option and pushes the model back on the round-trip. This is the bound-array path` &&
            ` working, not a gap: Table.sticky takes an ABAP string table directly, live-verified here, and apps 022/235 were reworked onto the same pattern 2026-08-05/06. // 1.71: the p:ColumnAIAction column` &&
            ` plugin (sap.m.plugins, since UI5 1.136 - far newer than 1.71) is dropped with its dependents aggregation, the xmlns:p namespace and the press toast - the plugin class does not exist on a 1.71` &&
            ` runtime, so keeping it would break view creation there (same decision as app 022). // NOTE: the original derives the ObjectNumber weight state in its frontend Formatter.js (weightState: KG conversion`.
    text1 = text1 && ` + Success/Warning/Error thresholds). That is business logic, so - abap2UI5 being a thin frontend - it is computed in ABAP model_init into a WEIGHT_STATE field and bound state="{WEIGHT_STATE}", not` &&
            ` via a frontend formatter (core:require dropped). Visually 1:1 with the original.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Column`                          name = `Table`                                         class = `z2ui5_cl_smpc_app_009` path = `src/01/01/z2ui5_cl_smpc_app_009.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.12`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_009.mjs: the rows render in NAME order, the ToggleButton pressed TWICE hides and re-shows the infoToolbar through the !pressed expression, and typing 'Grid Small'` &&
                 ` into the ComboBox moves the Table popinLayout to GridSmall through the expression binding. The sticky CheckBoxes carry a BOOLEAN selected arg the transpiled runtime delivers as the string 'true', so` &&
                 ` that leg stays with the human check)`
        notes = text1 ) ).

    text1 = `NOTE: the sample is an OPA-test demo: only the UI app under applicationUnderTest/ (Table.view.xml, Table.controller.js, Formatter.js, products.json) is ported 1:1; the qunit/OPA harness files` &&
            ` (OpaTableTest.qunit.js, Test.qunit.html, testsuite.qunit.*) are test infrastructure without UI and are not ported. // NOTE: the controller's onPopinLayoutChanged (byId(idProductsTable).setPopinLayout` &&
            ` per the ComboBox selectedKey switch) is a pure client-side path: two-way bound ComboBox selectedKey feeding a popinLayout expression binding with the same GridLarge/GridSmall-else-Block fallback` &&
            ` (extra attributes vs the original view, original change handler dropped) - no round-trip, same pattern as app 009; the expression can never emit an empty enum value. // NOTE: the controller-built` &&
            ` sap.m.Dialog of onMessageDialogPress (title Message, type Message, a Text 'Success' as content, an OK beginButton Button that closes it) is rebuilt 1:1 as a core:FragmentDefinition shown via` &&
            ` client->popup_display on the ColumnListItem press event; the OK Button closes roundtrip-free via the frontend action follow_up_action( cs_event-popup_close ), and the original's afterClose destroy is`.
    text1 = text1 && ` the framework's popup lifecycle. The Dialog, its Text and its Button are extra controls vs the original view.xml (controller-created there). // NOTE: model flattening: the sample's LOCAL` &&
            ` applicationUnderTest/products.json (123 rows) is moved into the default model verbatim, unbound columns dropped. Note: the local file is NOT identical to the shared mock ui5/mock/products.json - same` &&
            ` 123 ProductIds, but HT-9995 differs in content (local: Smartphone Cover / 15 EUR vs mock: Tablet Pouch / 20 EUR); the port follows the local file, the sample's actual model source. // NOTE: the` &&
            ` original derives the ObjectNumber weight state in its frontend Formatter.js (weightState: KG conversion + Success/Warning/Error thresholds). That is business logic, so - abap2UI5 being a thin` &&
            ` frontend - it is computed in ABAP model_init into a WEIGHT_STATE field and bound state="{WEIGHT_STATE}", not via a frontend formatter (core:require dropped). Visually 1:1 with the original.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ColumnListItem`                  name = `TableTest`                                     class = `z2ui5_cl_smpc_app_010` path = `src/01/01/z2ui5_cl_smpc_app_010.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        since = `1.12`
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port (2026-08-21 re-read: the port does` &&
                 ` ship a press -> Dialog -> close path, so the 'no interaction paths' clause understates what was there to try.) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_010.mjs: pressing a Navigation row round-trips MESSAGE_DIALOG_PRESS, the popup_display 'Message' Dialog shows 'Success' and its OK button's popup_close closes it)`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBox`                                      class = `z2ui5_cl_smpc_app_011` path = `src/01/01/z2ui5_cl_smpc_app_011.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22` )
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBox2Columns`                              class = `z2ui5_cl_smpc_app_193` path = `src/01/01/z2ui5_cl_smpc_app_193.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22` )
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxClearIcon`                             class = `z2ui5_cl_smpc_app_455` path = `src/02/01/z2ui5_cl_smpc_app_455.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.ComboBox inherits showClearIcon from sap.m.ComboBoxBase, where it is @since 1.96 - it is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.96.`
        post171 = `sap.m.ComboBox inherits showClearIcon from sap.m.ComboBoxBase, where it is @since 1.96 - it is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.96.` )
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxDefaultFiltering`                      class = `z2ui5_cl_smpc_app_463` path = `src/01/01/z2ui5_cl_smpc_app_463.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxFilteringContains`                     class = `z2ui5_cl_smpc_app_470` path = `src/01/01/z2ui5_cl_smpc_app_470.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        notes = `IMPROVISED: onInit calls combobox1.setFilterFunction( ) with a JavaScript callback matching the search term case-insensitively ANYWHERE in the item text OR in its key. An app-authored JS callback is` &&
                 ` the documented boundary, so the port ships UI5's built-in filtering instead. The key half IS recoverable and is taken: filterSecondaryValues (@since 1.46, under the 1.71 floor) makes the default` &&
                 ` filter test additionalText, which this port binds to {KEY}. What remains lost is only the CONTAINS half - UI5's default matches at a word start, so 'many' no longer finds 'Germany' the way the` &&
                 ` sample's regex does.` )
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxFilteringStartsWith`                   class = `z2ui5_cl_smpc_app_471` path = `src/01/01/z2ui5_cl_smpc_app_471.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        notes = `IMPROVISED: onInit calls combobox1.setFilterFunction( ) with a JavaScript callback matching the search term case-insensitively at the START of the item text OR of its key. An app-authored JS callback` &&
                 ` is the documented boundary, so the port ships UI5's built-in filtering, plus filterSecondaryValues (@since 1.46, under the 1.71 floor) so the default filter also tests additionalText, which this port` &&
                 ` binds to {KEY} - that is the sample's or-key half. One residual difference stays: UI5's wordStartsWithValue anchors at ANY word start, the sample's regex only at the string start, so 'Herzegovina'` &&
                 ` matches 'Bosnia and Herzegovina' here and does not in the original.` )
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxGrouping`                              class = `z2ui5_cl_smpc_app_199` path = `src/01/01/z2ui5_cl_smpc_app_199.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxLazyLoading`                           class = `z2ui5_cl_smpc_app_493` path = `src/01/01/z2ui5_cl_smpc_app_493.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        notes = `NOTE: The sample runs a MockServer and an ODataModel, suspends the items binding and resumes it in handleLoadItems. abap2UI5 has no OData model, so the port binds an ABAP table that is EMPTY until the` &&
                 ` loadItems event fires and the backend fills it - the same lazy behaviour (nothing is fetched before the picker opens), expressed with the framework's own round-trip. The binding-info's suspended flag` &&
                 ` has no equivalent and is dropped, and the mock server's artificial 3-second delay is not reproduced. // NOTE: The loadItems wire filling the bound items is unverified in a running system.` &&
                 ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_493.mjs).` )
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxMaxPickerHeight`                       class = `z2ui5_cl_smpc_app_494` path = `src/02/01/z2ui5_cl_smpc_app_494.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.ComboBox inherits maxPickerHeight from sap.m.ComboBoxBase, where it is @since 1.150 - it is the subject of this sample, so all three variants keep it 1:1 and the port needs a UI5` &&
                 ` runtime >= 1.150.`
        post171 = `sap.m.ComboBox inherits maxPickerHeight from sap.m.ComboBoxBase, where it is @since 1.150 - it is the subject of this sample, so all three variants keep it 1:1 and the port needs a UI5 runtime >=` &&
                 ` 1.150.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxSearchBoth`                            class = `z2ui5_cl_smpc_app_479` path = `src/01/01/z2ui5_cl_smpc_app_479.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        notes = `NOTE: fnFormatter joins the ComboBox value and key into 'text (key)' (or whichever one is set). A formatter is business logic, so the text is composed in ABAP and bound; the port adds a change wire on` &&
                 ` the ComboBox to tell the backend when to recompute it, which the original does not need because its formatter re-evaluates on every model change. // NOTE: The change wire and the composed text are` &&
                 ` unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_479.mjs).` ) ).

    text1 = `NOTE: handleChange validates the entry (a typed value with no matching key is an error) and calls setValueState / setValueStateText. Validation is business logic, so the port keeps the change wire,` &&
            ` binds selectedKey, value, valueState and valueStateText, and decides the state in ABAP - the same two branches with the same message. // NOTE: The change wire and the bound valueState Error branch` &&
            ` are e2e-verified 2026-08-22 (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_475.mjs): the module types an unknown country and asserts getValueState( ) === 'Error'. Three things it does` &&
            ` NOT cover and which stay unverified in a running system: valueStateText (never read), the recovery branch (the module never corrects the entry, so a dead ELSE that leaves the state at Error would` &&
            ` pass), and the selectedKey binding (drop it and selected_key stays initial, the error branch still fires and the assertion still passes).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxValidation`                            class = `z2ui5_cl_smpc_app_475` path = `src/01/01/z2ui5_cl_smpc_app_475.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        notes = text1 ) ).

    text1 = `POST-1.71: the formattedValueStateText aggregation of sap.m.ComboBox is @since 1.78 - newer than the 1.71 floor but kept for the 1:1 port, since two of the six ComboBoxes exist to show exactly that (a` &&
            ` value-state message carrying one link, and one carrying two). The app needs a UI5 release >= 1.78; below it the lowercase tag resolves as a control class and the 404 takes the whole view down, which` &&
            ` is why this is an error rather than a warning. // NOTE: every one of the six items bindings is declared ``suspended: true`` and resumed by handleLoadItems on the first open - a lazy-loading trick for` &&
            ` a list the model already holds. abap2UI5 sends the rows with the view, so there is nothing to suspend and nothing to resume: the port binds the 70 countries outright and the loadItems wire is` &&
            ` dropped. What the user sees is the same list. // NOTE: handleValueStateLinkPress toasts the constant 'Link in value state pressed', so all three links are wired round-trip-free as follow_up_action(` &&
            ` control_global, MESSAGE_TOAST show ) (app 005/032 idiom). Their href="" is carried as the sample writes it. // NOTE: the bindings carry a sorter on text; a thin frontend sorts the data it sends, so`.
    text1 = text1 && ` the 70 rows of countriesExtendedCollection.json are seeded verbatim in the mock's order and SORTed by text in model_init - never through SORT BY (field), which the transpiled backend drops (the` &&
            ` 2026-08-22 e2e finding). // NOTE: the two ComboBoxes that carry a formattedValueStateText ALSO carry the plain valueStateText in the sample (the long 'Warning message. Extra long text ...' string) -` &&
            ` UI5 shows the formatted one and ignores the plain one. Both are kept, since both are what the sample writes; app 032 carries the same pair for sap.m.Input.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxValueState`                            class = `z2ui5_cl_smpc_app_616` path = `src/02/01/z2ui5_cl_smpc_app_616.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.22`
        is_post171 = abap_true
        notes = text1
        post171 = `the formattedValueStateText aggregation of sap.m.ComboBox is @since 1.78 - newer than the 1.71 floor but kept for the 1:1 port, since two of the six ComboBoxes exist to show exactly that (a` &&
                 ` value-state message carrying one link, and one carrying two). The app needs a UI5 release >= 1.78; below it the lowercase tag resolves as a control class and the 404 takes the whole view down, which` &&
                 ` is why this is an error rather than a warning.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComboBox`                        name = `ComboBoxWrapping`                              class = `z2ui5_cl_smpc_app_384` path = `src/01/01/z2ui5_cl_smpc_app_384.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22` ) ).

    text1 = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) — that check bounds what was verified: the 2026-08-27` &&
            ` change added a render-time re-issue of both setActivePage calls at the end of view_display( ), which no listed check covered and which only a rebuild (a restored draft, or returning from a called` &&
            ` app) exercises. The port stays checked; what was checked passed, and the new half is unverified rather than re-checked. The 2026-08-31 routing wire (the app-owned hash via hash_attach_changed +` &&
            ` hash_set, the HASH_CHANGED page switch, the cold-#/Page2 re-entry and the render-time page re-issue in view_display) is equally outside what the 2026-07-20 run covered - browser-verified against the` &&
            ` transpiled backend later the same day (start without a hash, Compare to #/Page2, Back with the selection intact, Forward, the empty cold #/Page2, clearing the hash), and confirmed on a live system by` &&
            ` the maintainer; unverified only in the sense that no full interaction checklist re-ran. **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_012.mjs: the binding_call`.
    text1 = text1 && ` filter narrows the table to Laptops, selecting two rows round-trips SELECTION and shows 'Compare (2)', COMPARE navigates rootControl to page-comparison with 'Second Page' and a two-page Carousel` &&
            ` while hash_set writes #/Page2, and browser Back round-trips HASH_CHANGED so the NavContainer comes back to the first page - the 2026-08-31 routing wire, driven for the first time in the nightly)`.
    text2 = `IMPROVISED: the pattern's three views (App.view with sap.m.App id=rootControl, Main.view, Comparison.view) plus manifest routing are merged into ONE view: the App hosts the Main Page and the` &&
            ` Comparison f:DynamicPage (id page-comparison added) directly as its pages; the router's navTo("page2") is mapped to the documented NavContainer frontend action follow_up_action(` &&
            ` cs_event-control_by_id, rootControl//to/page-comparison ) with the default slide transition (CAPABILITIES nav row); the view controllerName attributes are dropped. Since 2026-08-31 the router's HASH` &&
            ` half is reproduced 1:1 with the framework's APP-OWNED hash routing (cs_event-hash_attach_changed, first port to use it; needs abap2UI5 NEWER than 1.144.0 - abap2UI5#2693): the start URL carries NO` &&
            ` hash (like the original), COMPARE pushes the app-owned '#/Page2' via hash_set - the original's URL byte for byte - and a hash change the app did not write (browser Back/Forward, a manual edit)` &&
            ` round-trips as HASH_CHANGED, whose handler shows the page the live hash (s_config-hash) names: NavContainer ``back`` to the First Page - the app instance is untouched, so the selection survives`.
    text2 = text2 && ` exactly like with the original's client-side router - or comparison_build + ``to`` for Forward. A render whose live hash carries /Page2 with the flag still off (a deep link, a reload) enters the` &&
            ` comparison in view_display( ); with nothing selected it stays empty with visiblePagesCount clamped to the UI5 default 1, which is exactly the original's cold '#/Page2' ('no data' lists). The` &&
            ` registration is re-asserted per render (it dies with an app switch), and view_display( ) re-issues the NavContainer ``to`` while check_page2 survives as class state (the carousel re-issue pattern).` &&
            ` The 2026-08-31 morning's draft-route wire (set_nav_routing KEEP + a draft-suffixed push) was replaced the same day: it wrote '#/app/<CLASS>/<DRAFT>' on start and its Back was swallowed by hasher's` &&
            ` cache dedupe - both reported from a live system. Still IMPROVISED: one view instead of three plus a manifest router. // NOTE: named models flattened into the single default model: the Comparison` &&
            ` view's settings> model becomes the bound PAGESCOUNT/ISDESKTOP fields (pagesCount initialized to 1, the CarouselLayout.visiblePagesCount UI5 default, instead of the original's`.
    text2 = text2 && ` undefined-until-routeMatched), and its products> model (Products/Props) becomes T_COMP_PRODUCTS/T_COMP_PROPS; the shared mock products.json default model is flattened into a flat upper-cased row type` &&
            ` (all 20 JSON columns kept; rows without DateOfSale carry an empty string where the original JSON omits the key - a harmless string property, no enum/default override). // NOTE: the '.formatter.url'` &&
            ` iconSrc formatter flattened to absolute https://sdk.openui5.org image URLs - source-verified against the now-archived app/model/formatter.js (it only prefixes '../../../../../../', i.e. resolves to` &&
            ` the test-resources root), so the absolute URLs are the faithful equivalent. // NOTE: compare button: the controller's onSelection setText/setVisible is replaced by bound COMPARE_TEXT/COMPARE_VISIBLE` &&
            ` model properties updated in the SELECTION handler (same count>1 logic, text only refreshed while shown, like the original); the ColumnListItem gains a selected="{SELECTED}" two-way binding - the` &&
            ` sanctioned selection-read pattern (CAPABILITIES 'Controller-read list selection') replacing getSelectedContextPaths - which is an extra attribute vs the original template, and the initial`.
    text2 = text2 && ` visible="false"/absent text of the Button become bindings. // IMPROVISED: the ResizeHandler-driven pagesCount/isDesktop recalculation (_onResize/_getPagesCount) is replaced by a one-shot computation` &&
            ` at COMPARE time from client->get( )-s_device-resize-width using the original's 600/1024 thresholds and the cap at the selected-items count; no live recalculation on window resize. Re-judged` &&
            ` 2026-08-05 against the now-live device model (pr/live-device-model): it does NOT close this one. visiblePagesCount could bind {device>/resize/width} directly, but the count is also capped at the` &&
            ` number of SELECTED products and then drives comparison_props_build, which slices the props table server-side per page window - so a client-side count would desync the props it is supposed to index.` &&
            ` The round-trip decision stays; what the live model closes is the class-swap family (app 168), not this one. Re-judged again 2026-08-06 and reclassified as a BOUNDARY rather than a framework gap:` &&
            ` closing it would need a resize -> BACKEND event wire (a debounced ResizeHandler round-trip), and that is deliberately not offered - it is chatty by construction, and every display-only case it would`.
    text2 = text2 && ` serve is already covered by the live device model without a round-trip. The one case it would genuinely serve is this one, where the count feeds a server-side slice; one port is not enough to file on` &&
            ` (the repo's rule), so the idea is recorded here for the second sample that needs it. // NOTE: Panel expand: the controller's onPanelExpanded (walking the sibling HBox controls and calling setVisible)` &&
            ` is replaced by a bound VISIBLE flag per comparison value row, toggled in the PANEL_EXPANDED event via t_arg ${KEY} + ${$parameters>/expand}; the description HBox's literal visible="false" becomes the` &&
            ` {VISIBLE} binding (initial false). // NOTE: The snapped/expanded Carousel re-sync is reproduced since 2026-08-06. The original's _updateCarouselsActivePage hands each Carousel its own page AT THE` &&
            ` SAME INDEX - carousel.setActivePage( carousel.getPages()[ iFirstItem ] ) - and those pages are aggregation-template CLONES. Measured 2026-08-06 (scripts/probes/aggregation-item-probe.mjs, real` &&
            ` OpenUI5): their ids ARE deterministic and stable, even across a reorder - UI5 mints <templateId>-<parentId>-<index> - but the parent id carries the VIEW PREFIX the framework assigns at runtime`.
    text2 = text2 && ` (v1--tpl-v1--car-0), which the backend never sees, so it cannot spell one. The earlier sidecar called them 'nondeterministic', which the probe refutes; the gap was the prefix, not the determinism.` &&
            ` Wherever a CONTROL_BY_ID argument takes a control id it now also takes an aggregation ITEM, addressed positionally as <id>/<aggregation>/<index> (pr/aggregation-item-address, implemented upstream),` &&
            ` resolved on the client where both the prefix and the aggregation are known. Both carousels are re-synced on every PAGE_CHANGED, as in the original - and, since 2026-08-27, again at the end of` &&
            ` view_display( ) whenever first_item is past page 0, because a rebuilt Carousel is back at page 0 while first_item survives as class state AND drives the bound comparison rows. // NOTE: the` &&
            ` DynamicPageTitle stateChange handler (.onStateChange, add/removeSnappedContent of the snapped Carousel as a Carousel-animation workaround) is dropped together with its stateChange attribute -` &&
            ` imperative aggregation surgery with no framework equivalent; the snapped/expanded content still switches natively with the DynamicPage header state. // NOTE: comparison Props are built from a fixed`.
    text2 = text2 && ` 19-key list in the mock JSON key order (the original iterates the FIRST selected product's own keys, skipping ProductPicUrl - so a first product without DateOfSale would drop that row, and missing` &&
            ` values rendered '<strong>undefined</strong>' where the port renders an empty <strong></strong>); selected products are taken in model row order, not click order; the original's per-product` &&
            ` information cache is unnecessary server-side. The controller's handleButtonPress (MessageBox) is dead code referenced by no view and not ported. // NOTE: the comparison Props row for Price shows` &&
            ` 956.00 where the original shows 956. The mock carries Price as a JSON number (956) and the original prints that value straight into the Props text; the port holds it in a packed field (p LENGTH 8` &&
            ` DECIMALS 2, needed for the ObjectNumber's sap.ui.model.type.Currency binding, which renders 956.00 in BOTH), and an ABAP packed value always serialises with its two decimals. Only the Props text` &&
            ` differs, and only in the trailing zeros - the ObjectNumber above it is identical in both.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ComparisonPattern`               name = `ComparisonPattern`                             class = `z2ui5_cl_smpc_app_012` path = `src/01/01/z2ui5_cl_smpc_app_012.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        ui5_only = abap_true
        checked = text1
        notes = text2 ) ).

    text1 = `IMPROVISED: the i18n> ResourceModel (i18n/i18n.properties) has no framework i18n mechanism here - all texts (dialog title, section titles/summaries/details, button labels) are inlined as the resolved` &&
            ` English literals from the properties file; this covers every i18n>-bound text incl. the Panel headerText 'More Info'. // NOTE: the cookieData> named JSON model is flattened into the default model:` &&
            ` /showCookieDetails becomes the bound abap_bool showcookiedetails; every visible binding and expression binding keeps its original shape over the flattened path (named ABAP-fed JSON models are not` &&
            ` expressible, CAPABILITIES.md). // IMPROVISED: custom:DivContainer (xmlns:custom="sap.ui.documentation", a demo-kit-internal plain-div wrapper control not shipped in any UI5 library) is rebuilt as a` &&
            ` sap.m.VBox with the same class sapUiSmallMargin. // IMPROVISED: the controller's toggleStyleClass/addStyleClass/removeStyleClass('cookiesDetailedView') on the dialog is dropped because the class's` &&
            ` CSS is not part of the sample's shipped files (manifest references css/style.css but does not list it under sample files, and it is not archived), so toggling the class has no visible effect. Since`.
    text1 = text1 && ` 2026-07-22 add/remove/toggleStyleClass ARE whitelisted in CONTROL_METHODS (pr/style-class-toggle), so the toggle could be wired via follow_up_action( cs_event-control_by_id, toggleStyleClass ) once` &&
            ` the CSS is present; it stays dropped here only for the missing CSS. // NOTE: the controller's lazy Fragment.load + cached-instance open()/close() lifecycle maps to client->popup_display (the fragment` &&
            ` XML is rebuilt per open, forcing showCookieDetails=false like the original's openCookieSettingsDialog) and client->popup_destroy in the close handlers; the original's empty 'insert your ... logic` &&
            ` here' placeholders remain as backend event branches (ACCEPT_ALL_COOKIES/REJECT_ALL_COOKIES/SAVE_COOKIES) that only close the dialog. // NOTE: The original's three focus moves are follow_up_action(` &&
            ` cs_event-set_focus ), NOT a control_by_id 'focus' - corrected 2026-08-26, all three were the latter and none of them could land. CONTROL_METHODS.focus is a bare control.focus(), and` &&
            ` sap.ui.core.Element.focus returns immediately when getFocusDomRef() is null; the two buttons it aims at are INVISIBLE at that moment (their visible is bound to showcookiedetails, which the very same`.
    text1 = text1 && ` roundtrip flips), an invisible control renders through InvisibleRenderer as a sap-ui-invisible- placeholder, so the id has no DOM node and the call is a silent no-op. SET_FOCUS wraps Lib.whenRendered` &&
            ` plus a post-re-render re-apply delegate, which is exactly the original's own _focusButton helper ('focus now if the button has a domRef, else add an onAfterRendering delegate and focus once it has');` &&
            ` it resolves the id through ViewSlots.resolveById, which searches every open slot, so the view = cs_view-popup argument goes with the change. Measured in the headless e2e 2026-08-26, with the port's` &&
            ` follow-ups rewritten on the wire at the exact same point of the custom-action phase: with 'focus' the focus after Set Preferences stayed on the dialog content div and after Cancel on the footer's` &&
            ` overflow-button clone; with SET_FOCUS it lands on actionSavePreferences and stays there (the leg in meta/interactions/z2ui5_cl_smpc_app_013.mjs asserts exactly that). The other two land as well but` &&
            ` are then overridden by UI5 itself and are therefore not asserted: on OPEN the dialog's own initial focus takes over right afterwards (the original escapes that only because its handler sits in`.
    text1 = text1 && ` attachAfterOpen, i.e. AFTER the dialog set its initial focus, and abap2UI5 has no afterOpen hook for a popup_display fragment), and after CANCEL_PRESS the footer OverflowToolbar's focus restore,` &&
            ` which UI5 runs after every onAfterRendering delegate, takes it back - the original's _focusButton loses it to the same restore.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.CookieSettingsDialogPattern`     name = `CookieSettingsDialogPattern`                   class = `z2ui5_cl_smpc_app_013` path = `src/01/01/z2ui5_cl_smpc_app_013.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        ui5_only = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) (The focus moves were NOT among the checks: they could` &&
                 ` not have passed - see the SET_FOCUS deviation below, measured 2026-08-26.)`
        notes = text1 ) ).

    text1 = `NOTE: the shared mock model /ProductCollection (ui5/mock/products.json) is flattened into the default model with only the three bound columns (ProductId, Name, ProductPicUrl); all unbound columns are` &&
            ` dropped. The full 123-row set is kept verbatim. // NOTE: the ProductPicUrl values point at the OpenUI5 host (https://sdk.openui5.org/test-resources/...) per the asset-URL rule; they are rebuilt per` &&
            ` row from ProductId in a LOOP in model_init - path and file name are identical to the mock values, only the host prefix is added. // NOTE: the controller-built Dialog of handlePress (Image + Close` &&
            ` button, afterClose destroy) is rebuilt 1:1 as a core:FragmentDefinition -> Dialog shown via client->popup_display (the CAPABILITIES.md pattern, app 042). The Link press ships the row's ProductPicUrl` &&
            ` via t_arg ${PRODUCTPICURL} - the original reads evt.getSource().getTarget(), whose target property is bound to the same column. The Close button maps to client->follow_up_action( popup_close ); the` &&
            ` original's afterClose destroy is handled by the framework's popup lifecycle and is not wired separately.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.CustomListItem`                  name = `CustomListItem`                                class = `z2ui5_cl_smpc_app_014` path = `src/01/01/z2ui5_cl_smpc_app_014.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_014.mjs: the bound rows render, pressing the 'Notebook Basic 15' Link round-trips LINK_PRESS, the popup_display image Dialog opens with an Image and its Close` &&
                 ` button's popup_close closes it)`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.CustomTreeItem`                  name = `CustomTreeItem`                                class = `z2ui5_cl_smpc_app_015` path = `src/01/01/z2ui5_cl_smpc_app_015.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.48.0`
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_015.mjs: the root nodes' Inputs carry the bound TEXT and a real click on the row's icon Button raises the client-composed 'Button pressed'` &&
                 ` toast)`
        notes = `NOTE: the original binds the Tree's items to the whole JSON model root (items="{path: '/'}", the model IS the node array from Tree.json); abap2UI5 serves a single default model, so the array is` &&
                 ` flattened into it as the bound table T_TREE and the binding-info keeps its shape with the bound path substituted for '/' (nested tree binding is expressible per CAPABILITIES.md, proven by app 054).` &&
                 ` // NOTE: the flat ABAP row types serialize an empty NODES array on leaf rows where the original Tree.json simply omits the 'nodes' property (levels 1-4; the level-5 row type carries no NODES field at` &&
                 ` all); ClientTreeBinding treats an empty child array as a childless node, so the rendered tree matches. TEXT and REF are present on every original node - no absent-property/empty-string enum risk.` ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port on both Buttons - the app needs a UI5 release >= 1.84 to render it. // POST-1.71: Link.ariaHasPopup (since` &&
            ` UI5 1.86) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.86 to render it. // POST-1.71: DatePicker.hideInput (since UI5 1.97) is newer than 1.71 but kept for the 1:1` &&
            ` port - the sample's central property (the picker input stays hidden, opened only via the anchor controls); openBy is also since 1.97, so the app needs a UI5 release >= 1.97 to render this sample's` &&
            ` behavior. // NOTE: the 2026-07-22 rework (openBy via follow_up_action( control_by_id, openBy, $event.oSource.sId ) instead of a scheduled round-trip, client-composed date toast, app became init-only)` &&
            ` is live-verified on 2026-08-02 - each anchor opens the hidden DatePicker and picking a date toasts the value. // NOTE: the headless e2e finding of 2026-07-30 (clicking an openBy anchor opens the` &&
            ` popover, then Popover.onfocusin recurses off the hideInput input - Maximum call stack size exceeded) is resolved as HEADLESS-ONLY: the picker opens and works in a real browser (2026-08-02 live`.
    text1 = text1 && ` check). The check covered the visible behaviour; the browser console was not necessarily inspected for a silent recursion warning. The e2e interaction stays deliberately unarmed for this app - app` &&
            ` 091 covers the hidden-picker openBy class.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DatePicker`                      name = `DatePickerHidden`                              class = `z2ui5_cl_smpc_app_016` path = `src/02/01/z2ui5_cl_smpc_app_016.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        since = `1.22.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer), re-check after the 2026-07-22 roundtrip-free rework: each anchor opens the hidden DatePicker and picking` &&
                 ` a date toasts the value. Live-checked reference example for: frontend action (openBy/domRef), $event.oSource.sId anchor transport, POST_171 discipline. **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_016.mjs: the three anchors render with press listeners and the HiddenDP DatePicker exists with hideInput and its change wire - the openBy gesture itself loops in` &&
                 ` Popover.onfocusin headless, so the pick and the 'Date selected' toast stay with the human check)`
        notes = text1
        post171 = `Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port on both Buttons - the app needs a UI5 release >= 1.84 to render it. // Link.ariaHasPopup (since UI5 1.86) is newer` &&
                 ` than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.86 to render it. // DatePicker.hideInput (since UI5 1.97) is newer than 1.71 but kept for the 1:1 port - the sample's central` &&
                 ` property (the picker input stays hidden, opened only via the anchor controls); openBy is also since 1.97, so the app needs a UI5 release >= 1.97 to render this sample's behavior.` ) ).

    text1 = `NOTE: onInit builds the Select-New-Date Dialog, its sap.ui.unified.Calendar and its two Buttons in the CONTROLLER (new Dialog({ ... })). The port builds the same dialog in a chain and shows it with` &&
            ` popup_display, which is why structural-diff reports Dialog, unified:Calendar and two extra Buttons as control extra - the sample's view declares none of them. // NOTE: handleTableSelectionChange` &&
            ` enables the Change Dates button while at least one row is selected. The row selection is bound two-way here, so the selectionChange round-trip only has to recompute the bound enabled flag - the same` &&
            ` rule the original applies from getSelectedItems().length. // NOTE: handleCalendarSelect enables the dialog's OK button once a date is picked, and the OK press writes that date into the DateOfSale of` &&
            ` every selected row. The picked date's local year, month and day travel with the select event, ABAP composes the same yyyy-MM-dd string the sample's DateFormat produces, and the OK button's enabled` &&
            ` state is an expression over the bound date. // NOTE: setSizeLimit( 10 ) caps the bound rows at the first ten of the mock collection, so the port seeds exactly those ten. The original binds them`.
    text1 = text1 && ` through a named products> model; abap2UI5 keeps one default model, so the prefix is dropped and the last path segment is identical. // NOTE: The multi-select, the date dialog and the mass date change` &&
            ` are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_556.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DatePicker`                      name = `DatePickerMassEdit`                            class = `z2ui5_cl_smpc_app_556` path = `src/01/01/z2ui5_cl_smpc_app_556.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    text1 = `NOTE: The onInit-seeded JSON model rides 1:1 in model_init (5 rows, the derived label texts written out); every row carries a valueState so the absent-enum-empty-string trap does not apply, and the` &&
            ` rows without a valueStateText keep the empty string - a string property, where the empty value falls back to the state default text like the original undefined. // NOTE: **e2e-verified 2026-08-01**` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): all five rows render from the real backend model - the labels arrive, each of the four non-None value states reaches the DOM` &&
            ` exactly once (sapMInputBaseContentWrapper<State> on the DatePicker) and the bound valueStateText of the Warning row is written into the value-state node. Residual (nothing headless can assert): the` &&
            ` value-state message popup's wrapping of the long text, which needs the theme CSS the harness does not load.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DatePicker`                      name = `DatePickerValueState`                          class = `z2ui5_cl_smpc_app_253` path = `src/01/01/z2ui5_cl_smpc_app_253.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    text1 = `NOTE: the original controller's JSON model carries UI5Date objects; the ABAP model carries the same dates as ISO 'yyyy-MM-dd' strings, and each sap.ui.model.type.Date part of the DateInterval value` &&
            ` bindings gets an added formatOptions.source pattern 'yyyy-MM-dd' so the strings are parsed to Dates and written back as strings (source-verified: DateInterval sets bUseInternalValues and` &&
            ` Date.getModelFormat returns the source format; CAPABILITIES: Date types over ISO strings need a source pattern). Same rows/values as the sample, only the value type differs. // NOTE: the original` &&
            ` controller's onInit oDRS2.setMinDate(2016-01-01)/setMaxDate(2016-12-31) become minDate/maxDate view attributes on DRS2, bound over ABAP DATS strings with formatter` &&
            ` 'Formatter.DateAbapDateToDateObject' (CAPABILITIES date-object-properties row) - two extra attributes vs the original view.xml. // POST-1.71: core:require (UI5 >= 1.74) on the view root loads` &&
            ` z2ui5/model/formatter for the DRS2 minDate/maxDate Formatter.DateAbapDateToDateObject bindings - the app needs a UI5 release >= 1.74 to render it. // POST-1.71: showCurrentDateButton (since UI5 1.95)`.
    text1 = text1 && ` on DRS3 is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.95 to render it. // NOTE: the change handler's imperative oEventSource.setValueState(None/Error) becomes a` &&
            ` two-way bound valueState attribute on each of the five DateRangeSelections (prefer a bindable property over imperative calls); the backend maps the event-source id to the matching field - five extra` &&
            ` valueState attributes vs the original view.xml, initialized to 'None' (enum property, never an empty string). // NOTE: the change handler's imperative oText.setText(...) becomes a bound text` &&
            ` attribute on the TextEvent Text - one extra attribute vs the original view.xml (there the Text has no text attribute). // NOTE: the controller's _iEvent counter is omitted - it is incremented in` &&
            ` handleChange but never displayed or otherwise used in the sample. // NOTE: The DRS2 minDate/maxDate the original sets imperatively in onInit (UI5Date.getInstance(2016, 0, 1) / (2016, 11, 31)) are` &&
            ` bound instead, and since 2026-08-21 as ABAP DATS strings read through Formatter.DateAbapDateToDateObject. That formatter builds the Date from the parsed parts - local midnight - which is what`.
    text1 = text1 && ` UI5Date.getInstance means and what the DatePicker compares against (CalendarDate.fromLocalJSDate). They were ISO date-only strings through Formatter.DateCreateObject until then, and that is ``new` &&
            ` Date(s)``, which ECMA-262 parses as UTC midnight: west of Greenwich the allowed range began on 2015-12-31, contradicting the sample's own label, which spells the intended bounds out in words. Same` &&
            ` defect and same fix as app 220. Note this affects only these two: every other date in the port travels as a typed binding with a source pattern, which parses locally.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DateRangeSelection`              name = `DateRangeSelection`                            class = `z2ui5_cl_smpc_app_017` path = `src/02/01/z2ui5_cl_smpc_app_017.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.22.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_017.mjs: DRS1 renders the seeded 2014-02-02 start through its DateInterval type, and a range typed into it and committed with Enter round-trips CHANGE so the bound` &&
                 ` event Text names DRS1 and carries the From date. The bound valueState follows the BOOLEAN valid arg, which the transpiled runtime delivers as the string 'true', so that half stays with the human` &&
                 ` check)`
        notes = text1
        post171 = `core:require (UI5 >= 1.74) on the view root loads z2ui5/model/formatter for the DRS2 minDate/maxDate Formatter.DateAbapDateToDateObject bindings - the app needs a UI5 release >= 1.74 to render it. //` &&
                 ` showCurrentDateButton (since UI5 1.95) on DRS3 is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.95 to render it.` ) ).

    text1 = `POST-1.71: sap.m.Button.ariaHasPopup (@1.84) and sap.m.Link.ariaHasPopup (@1.86) kept 1:1 on the three anchors; also hideInput on the picker (@1.97 per the 016 precedent). The app needs a UI5 release` &&
            ` >= 1.97. // NOTE: The 016 hidden-picker pattern on the sibling control: all three anchors (text Button, icon Button, Link) open the hideInput picker roundtrip-free via follow_up_action control_by_id` &&
            ` openBy ($event.oSource.sId), and the change toast is client-composed from ${$parameters>/value} - the controller handlers fold into the two wire forms 1:1. // NOTE: live-verified on 2026-08-02: the` &&
            ` anchored open of the hideInput picker and the change-value toast both work in a real browser. The 016 headless finding (Popover.onfocusin recursion on the hidden input) is therefore headless-only for` &&
            ` this control too; no e2e interaction is armed for THIS APP. Corrected 2026-08-23: the sentence used to end "for the hidden-picker class", which denies coverage that exists - app 091 arms exactly this`.
    text1 = text1 && ` idiom (control_by_id / openBy / $event.oSource.sId on a hideInput picker), its module asserts the Popover opens, and its sidecar records it e2e-verified 2026-08-04. App 016, the precedent both of` &&
            ` these cite, scoped it correctly to itself; the copy dropped the scope.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DateRangeSelection`              name = `DateRangeSelectionHidden`                      class = `z2ui5_cl_smpc_app_256` path = `src/02/01/z2ui5_cl_smpc_app_256.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        since = `1.22.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): all anchors open the hideInput picker and the change toast carries the picked value. **e2e-verified` &&
                 ` 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_256.mjs: the three anchors render with press listeners and the HiddenDRS DateRangeSelection exists with hideInput and its change` &&
                 ` wire - the openBy gesture loops in Popover.onfocusin headless, so the pick and its toast stay with the human check)`
        notes = text1
        post171 = `sap.m.Button.ariaHasPopup (@1.84) and sap.m.Link.ariaHasPopup (@1.86) kept 1:1 on the three anchors; also hideInput on the picker (@1.97 per the 016 precedent). The app needs a UI5 release >= 1.97.` ) ).

    text1 = `NOTE: The onInit-seeded JSON model rides 1:1 in model_init (5 rows, the derived label texts written out); every row carries a valueState so the absent-enum-empty-string trap does not apply, and the` &&
            ` rows without a valueStateText keep the empty string (falls back to the state default text like the original undefined) - the app-253 pattern. // NOTE: **e2e-verified 2026-08-01**` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): all five rows render from the real backend model - the labels arrive, each of the four non-None value states reaches the DOM` &&
            ` exactly once (sapMInputBaseContentWrapper<State> on the DateRangeSelection) and the bound valueStateText of the Warning row is written into the value-state node. Residual (nothing headless can` &&
            ` assert): the value-state message popup's wrapping of the long text, which needs the theme CSS the harness does not load.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DateRangeSelection`              name = `DateRangeSelectionValueState`                  class = `z2ui5_cl_smpc_app_254` path = `src/01/01/z2ui5_cl_smpc_app_254.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    text1 = `IMPROVISED: the JSON model's UI5Date instances become date strings in the flat ABAP model: the sap.ui.model.type.DateTime bindings (DTP2/3/4/5/8) get an added source pattern 'yyyy-MM-dd HH:mm:ss'` &&
            ` format option so the type parses the model string (CAPABILITIES: Date types over ISO strings need a source pattern), and the sap.ui.model.odata.type.DateTimeOffset parts of DTP10/DTP11 get added` &&
            ` constraints {V4: true} so the DateTimeWithTimezone composite receives a parsed Date internal value (source-verified in DateTimeOffset.js getModelFormat/CompositeBinding internal values); valueDTP10` &&
            ` is the UTC string '2023-03-31T10:32:30Z' instead of the original's local-time UI5Date, and DTP3/DTP5 'now' comes from server sy-datum/sy-uzeit instead of the browser clock. // IMPROVISED: the` &&
            ` controller's byId('DTP6').setInitialFocusedDateValue(UI5Date.getInstance(2017, 5, 13, 11, 12, 13)) is expressed as a bound initialFocusedDateValue property with the Formatter.DateCreateObject module` &&
            ` formatter over the model string '2017-06-13T11:12:13' (CAPABILITIES date-object row) - extra initialFocusedDateValue attribute on DTP6 plus xmlns:core and core:require on the view root vs the`.
    text1 = text1 && ` original view.xml. // NOTE: the controller's handleChange becomes a CHANGE round-trip: the source control id, entered value and valid flag travel via $event.oSource.sId / ${$parameters>/value} /` &&
            ` ${$parameters>/valid}; the textResult Text.text becomes a binding and every change-firing picker (DTP1/2/3/4/6/7) gets an added bound valueState attribute, initialized to 'None' so no empty string` &&
            ` reaches the enum-typed property - the original sets both imperatively on the controls. // NOTE: the view-level attachParseError/attachValidationSuccess handlers of the original controller (valueState` &&
            ` Error/None on binding parse errors in the data-binding panel) are covered by the framework's automatic handleValidation registration on every view slot (CAPABILITIES MessageManager row,` &&
            ` pr/message-model) - no port code needed. // NOTE: model flattening: the original model's valueDTP9 is bound by no control in the view and is dropped; valueDTP11 (null in the original) serializes as` &&
            ` an empty string in the flat ABAP model, which the V4 DateTimeOffset model format parses back to null, so DTP11 renders timezone-only like the original. // POST-1.71: showCurrentDateButton (since UI5`.
    text1 = text1 && ` 1.95) on DTP2 is kept for the 1:1 port. // POST-1.71: showCurrentTimeButton (since UI5 1.98) on DTP2 and DTP11 is kept for the 1:1 port. // POST-1.71: showTimezone (since UI5 1.99) on DTP8 and DTP11` &&
            ` is kept for the 1:1 port. // POST-1.71: timezone (since UI5 1.99) on DTP8 is kept for the 1:1 port. // POST-1.71: core:require of the z2ui5/model/formatter module on the view root needs UI5 >= 1.74,` &&
            ` and the sap.ui.model.odata.type.DateTimeWithTimezone binding type of DTP10/DTP11 (since UI5 1.99) is kept 1:1 - the app needs a UI5 release >= 1.99 overall.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DateTimePicker`                  name = `DateTimePicker`                                class = `z2ui5_cl_smpc_app_018` path = `src/02/01/z2ui5_cl_smpc_app_018.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.38.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_018.mjs: DTP2 renders its seeded 2016 value through the DateTime type, and a value typed into DTP1 and committed with Enter round-trips CHANGE so the bound Text` &&
                 ` reads 'Change - Event 1: DateTimePicker ...DTP1:'. The valueState half rides on the BOOLEAN valid arg and stays with the human check)`
        notes = text1
        post171 = `showCurrentDateButton (since UI5 1.95) on DTP2 is kept for the 1:1 port. // showCurrentTimeButton (since UI5 1.98) on DTP2 and DTP11 is kept for the 1:1 port. // showTimezone (since UI5 1.99) on DTP8` &&
                 ` and DTP11 is kept for the 1:1 port. // timezone (since UI5 1.99) on DTP8 is kept for the 1:1 port. // core:require of the z2ui5/model/formatter module on the view root needs UI5 >= 1.74, and the` &&
                 ` sap.ui.model.odata.type.DateTimeWithTimezone binding type of DTP10/DTP11 (since UI5 1.99) is kept 1:1 - the app needs a UI5 release >= 1.99 overall.` ) ).

    text1 = `POST-1.71: sap.m.Button.ariaHasPopup (@1.84) and sap.m.Link.ariaHasPopup (@1.86) kept 1:1 on the three anchors; also hideInput on the picker (@1.97 per the 016 precedent). The app needs a UI5 release` &&
            ` >= 1.97. // NOTE: The 016 hidden-picker pattern on the sibling control: all three anchors (text Button, icon Button, Link) open the hideInput picker roundtrip-free via follow_up_action control_by_id` &&
            ` openBy ($event.oSource.sId), and the change toast is client-composed from ${$parameters>/value} - the controller handlers fold into the two wire forms 1:1. // NOTE: live-verified on 2026-08-02: the` &&
            ` anchored open of the hideInput picker and the change-value toast both work in a real browser. The 016 headless finding (Popover.onfocusin recursion on the hidden input) is therefore headless-only for` &&
            ` this control too; no e2e interaction is armed for THIS APP. Corrected 2026-08-23: the sentence used to end "for the hidden-picker class", which denies coverage that exists - app 091 arms exactly this`.
    text1 = text1 && ` idiom (control_by_id / openBy / $event.oSource.sId on a hideInput picker), its module asserts the Popover opens, and its sidecar records it e2e-verified 2026-08-04. App 016, the precedent both of` &&
            ` these cite, scoped it correctly to itself; the copy dropped the scope.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DateTimePicker`                  name = `DateTimePickerHidden`                          class = `z2ui5_cl_smpc_app_257` path = `src/02/01/z2ui5_cl_smpc_app_257.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        since = `1.38.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): all anchors open the hideInput picker and the change toast carries the picked value. **e2e-verified` &&
                 ` 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_257.mjs: the three anchors render with press listeners and the HiddenDTP DateTimePicker exists with hideInput and its change wire` &&
                 ` - the openBy gesture loops in Popover.onfocusin headless, so the pick and its toast stay with the human check)`
        notes = text1
        post171 = `sap.m.Button.ariaHasPopup (@1.84) and sap.m.Link.ariaHasPopup (@1.86) kept 1:1 on the three anchors; also hideInput on the picker (@1.97 per the 016 precedent). The app needs a UI5 release >= 1.97.` ) ).

    text1 = `NOTE: The onInit-seeded JSON model rides 1:1 in model_init (5 rows, the derived label texts written out); every row carries a valueState so the absent-enum-empty-string trap does not apply, and the` &&
            ` rows without a valueStateText keep the empty string (falls back to the state default text like the original undefined) - the app-253 pattern. // NOTE: **e2e-verified 2026-08-01**` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): all five rows render from the real backend model - the labels arrive, each of the four non-None value states reaches the DOM` &&
            ` exactly once (sapMInputBaseContentWrapper<State> on the DateTimePicker) and the bound valueStateText of the Warning row is written into the value-state node. Residual (nothing headless can assert):` &&
            ` the value-state message popup's wrapping of the long text, which needs the theme CSS the harness does not load.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DateTimePicker`                  name = `DateTimePickerValueState`                      class = `z2ui5_cl_smpc_app_255` path = `src/01/01/z2ui5_cl_smpc_app_255.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.38.0`
        notes = text1 ) ).

    text1 = `NOTE: the original builds its four Dialogs imperatively in the controller (new Dialog({...}).open()); the port expresses each as a core:FragmentDefinition popup shown via client->popup_display on the` &&
            ` button press event - the CAPABILITIES.md 1:1 path for controller-built Dialogs. The four Dialog fragments are therefore extra vs the archived V.view.xml, which holds only the four trigger Buttons;` &&
            ` every fragment control is EXTRA vs the archived V.view.xml (only the four trigger Buttons exist there): Dialog, Text, TextArea, Label, Button and the l:VerticalLayout / l:HorizontalLayout wrappers` &&
            ` inside the dialogs. // NOTE: the controller reads each note imperatively (Element.getElementById(...).getValue()); the port two-way binds the three TextArea values` &&
            ` (reject_note/submit_note/confirm_note) and builds the 'Note is: ...' toasts from the synced ABAP fields in on_event - an extra value attribute per TextArea vs the original construction. // NOTE: the` &&
            ` submission dialog's liveChange handler (enable the Submit button while the note is non-empty, enabled: false initially) is replaced round-trip-free by valueLiveUpdate=true on the TextArea plus an`.
    text1 = text1 && ` expression binding {= ${...}.length > 0 } on the begin button's enabled - same behavior, starts disabled with the empty note (CAPABILITIES.md: prefer a pure expression binding over an event` &&
            ` round-trip). // NOTE: Cancel: the approve dialog (no inputs) closes client-side via follow_up_action popup_close; the three note dialogs cancel through a backend DIALOG_CANCEL event + popup_destroy` &&
            ` so the two-way model sync keeps the typed note across a reopen, matching the original's reused cached dialog instances.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Dialog`                          name = `DialogConfirm`                                 class = `z2ui5_cl_smpc_app_019` path = `src/01/01/z2ui5_cl_smpc_app_019.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed); live-checked reference example for: fragment-popup` &&
                 ` dialogs, roundtrip-free live-enable expression, popup_close paths`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.Dialog.showFullScreenButton is @since 1.149 - it is the very property this sample demonstrates, so dropping it would drop the sample. Kept 1:1 (fidelity-first, the corpus-wide` &&
            ` POST_171 policy); the app needs UI5 >= 1.149 to show the toggle, everything else renders on 1.71. // NOTE: the original builds its three Dialogs imperatively in the controller (new` &&
            ` Dialog({...}).open(), each cached in a controller field and added as a view dependent); the port expresses each as a core:FragmentDefinition popup shown via client->popup_display on the button press` &&
            ` (app 019/273 precedent). Every fragment control is therefore EXTRA vs the archived V.view.xml, which holds only the three trigger Buttons: Dialog, List, StandardListItem and the dialog's own OK/Close` &&
            ` Buttons (Button 3 -> 5, Dialog/List/StandardListItem 0 -> 1 per open dialog). // NOTE: the three dialogs differ only in resizable / draggable / the fixed contentWidth+contentHeight and in the first` &&
            ` one's extra Emphasized OK begin button, so the port builds them through one popup_products_display( resizable draggable sized begin_ok ) method. Each flag is written ONLY when the original passes it`.
    text1 = text1 && ` - the plain dialog gets no resizable='false' and no contentWidth, so no attribute the original never sets is added. // NOTE: the List binds the shared demo mock products.json /ProductCollection (all` &&
            ` 123 rows, Name + Quantity - the only two fields the StandardListItem binds) inlined into model_init; the original loads the same file through a JSONModel. Both dialog buttons (OK and Close) close the` &&
            ` dialog client-side via follow_up_action( cs_event-popup_close ), the direct form of the original's this.oDialog.close(). // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction,` &&
            ` transpiled backend + real browser): the plain dialog opens with its title 'Available Products' and the bound 123-row product list inside it, and the Close button closes it client-side (popup_close).` &&
            ` Residual (nothing headless can assert): the fullscreen toggle itself - corrected 2026-08-23, the stated reason was wrong. The harness does NOT serve an old runtime: e2e-smoke resolves every` &&
            ` sdk.openui5.org request to the installed @openui5 packages, pinned at 1.151, so showFullScreenButton is live headless and Dialog._applyFullscreenButtonToHeader really does add the button (this port`.
    text1 = text1 && ` sets no customHeader and the context is not a phone). The module already opens that dialog, so the button's presence, its tooltip swap and the stretched dialog are all assertable - the leg is open` &&
            ` for want of an assertion, not for want of a runtime and the resizable/draggable behaviour of the other two dialogs, which differ only in the flags the same builder writes.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Dialog`                          name = `DialogFullScreen`                              class = `z2ui5_cl_smpc_app_274` path = `src/02/01/z2ui5_cl_smpc_app_274.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1
        post171 = `sap.m.Dialog.showFullScreenButton is @since 1.149 - it is the very property this sample demonstrates, so dropping it would drop the sample. Kept 1:1 (fidelity-first, the corpus-wide POST_171 policy);` &&
                 ` the app needs UI5 >= 1.149 to show the toggle, everything else renders on 1.71.` ) ).

    text1 = `NOTE: the original builds its five Message Dialogs imperatively in the controller (new Dialog({type: Message, title, state, content: new Text(...), beginButton: new Button(...)}).open()); the port` &&
            ` expresses each as a core:FragmentDefinition popup shown via client->popup_display on the button press event - the CAPABILITIES.md 1:1 path for controller-built Dialogs (app 019 precedent). Every` &&
            ` fragment control is therefore EXTRA vs the archived V.view.xml, which holds only the five trigger Buttons: Dialog, Text and the dialog's own OK Button (Button 5 -> 6, Dialog 0 -> 1, Text 0 -> 1 per` &&
            ` open dialog). // NOTE: the five handlers differ only in title / state / content text, so the port builds them through one popup_message_display( title state text ) method instead of five` &&
            ` near-identical blocks. The ``state`` attribute is written only when the original passes one: onDefaultMessageDialogPress builds its Dialog WITHOUT a state, and writing state='None' explicitly would` &&
            ` be an added attribute rather than the inherited default. // NOTE: the original keeps each Dialog in a controller field and reuses it (if (!this.oDialog) ...); abap2UI5 rebuilds the fragment per open,`.
    text1 = text1 && ` which is the framework's popup lifecycle - the same deviation the other popup ports carry. The OK button's press (this.oDialog.close()) is reproduced roundtrip-free as follow_up_action(` &&
            ` cs_event-popup_close ). // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the Default and the Error dialog each open with their own text and` &&
            ` close again on the OK button. Restated 2026-08-23: the claim used to say "title/state/text" and "the Emphasized OK button", and the module asserts none of those three - it matches the dialog CONTENT` &&
            ` text, locates the button by its accessible name OK, and never looks at the title, at state, or at the button type. The close leg and the second open are genuinely covered (popup_close, no round-trip)` &&
            ` - the second open proves the rebuild-per-open lifecycle works. The other three differ only in the three literals the same builder method fills in, and render-smoke covers their views.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Dialog`                          name = `DialogMessage`                                 class = `z2ui5_cl_smpc_app_273` path = `src/01/01/z2ui5_cl_smpc_app_273.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.Button ariaHasPopup is @since 1.84.0 - newer than the 1.71 floor. All three buttons carry it in the sample, so it is kept and the port is filed under src/02. // NOTE: The three` &&
            ` dialogs, their List and their Buttons are built in the CONTROLLER (new Dialog({ ... })). The port builds the same dialog in a chain and shows it with popup_display, so structural-diff reports Dialog,` &&
            ` List, StandardListItem and the extra Buttons as control extra - the sample's view declares none of them. The three presses differ only in the flags they set (contentWidth/contentHeight, resizable,` &&
            ` draggable) and in whether an OK button is there, which the port passes as parameters. // IMPROVISED: Popup.setWithinArea( withinArea ) is the POINT of the sample: it confines every popup - dialogs` &&
            ` included - to the bordered HBox on the right. It has no declarative counterpart, but it IS whitelisted - the framework ships a CONTROL_GLOBAL target POPUP with methods { setWithinArea: ["within"] }` &&
            ` and a purpose-built "within" argument kind that takes a control id. This note used to say there was no whitelisted frontend action, which was wrong and left the sample's whole subject unported: the`.
    text1 = text1 && ` box was rendered and confined nothing. The port now issues the call once with the view (the app-285 idiom) rather than per press, because a follow-up action runs after the popup of the same` &&
            ` round-trip has already opened - so onExit's reset is dropped, and this app opens no other popup, which makes the effect the same. setWithinArea is @since 1.89, already cleared by this class's` &&
            ` ariaHasPopup @1.84 filing. // NOTE: The three dialogs and their resizable / draggable flags are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_561.mjs). // POST-1.71: POPUP.setWithinArea is a control_global frontend action since UI5 1.89 - newer than the 1.71 floor but kept for the 1:1 port, and already` &&
            ` noted in the method's own comment. Below 1.89 the lazy require returns undefined and the dispatch hits its "not available" guard, so the popup is not confined to the within-area. This class is filed` &&
            ` under src/02 for Button.ariaHasPopup @1.84 already; 1.89 is the higher of the two floors it needs.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Dialog`                          name = `DialogWithinArea`                              class = `z2ui5_cl_smpc_app_561` path = `src/02/01/z2ui5_cl_smpc_app_561.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Button ariaHasPopup is @since 1.84.0 - newer than the 1.71 floor. All three buttons carry it in the sample, so it is kept and the port is filed under src/02. // POPUP.setWithinArea is a` &&
                 ` control_global frontend action since UI5 1.89 - newer than the 1.71 floor but kept for the 1:1 port, and already noted in the method's own comment. Below 1.89 the lazy require returns undefined and` &&
                 ` the dispatch hits its "not available" guard, so the popup is not confined to the within-area. This class is filed under src/02 for Button.ariaHasPopup @1.84 already; 1.89 is the higher of the two` &&
                 ` floors it needs.` ) ).

    text1 = `POST-1.71: four post-1.71 members are kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the page button, the message-popover button and the Reject button; MessagePopover.groupItems (since` &&
            ` UI5 1.73); the Dialog footer aggregation (sap.m.Dialog.footer, since UI5 1.110) which is exactly how MPDialog.fragment.xml puts its Toolbar under the dialog; and the sap-icon://information glyph (in` &&
            ` the icon font since UI5 1.80), the default branch of the ported buttonIconFormatter. // NOTE: the fragment MPDialog.fragment.xml is built in popup_dialog_display and shown with client->popup_display(` &&
            ` ), the original's loadFragment( ) + oDialog.open( ) on the page button's press; _closeDialog becomes client->popup_destroy( ). onOpenPopoverDialog's MessageToast.show('Press "Save" to trigger` &&
            ` validation.') is ported as client->message_toast_display( ) in the same handler, and Messaging.registerObject( formContainer ) has no counterpart - abap2UI5 registers handleValidation on every view`.
    text1 = text1 && ` slot already. // NOTE: the Dialog title is title="Hello {/recipient/name}" but the shared demo kit mock sap/ui/demo/mock/forms.json carries only the top-level keys "forms" and "employment" - there is` &&
            ` no "recipient" node, so the original renders a bare "Hello ". The port keeps the binding shape (title bound to an empty RECIPIENT_NAME field) so it renders identically. // NOTE:` &&
            ` sap/ui/demo/mock/forms.json is not snapshotted under ui5/mock/ (raw download is blocked in this environment). Its content was verified against the upstream file and is byte-equivalent to the sibling` &&
            ` sample's local snapshot ui5/sap.m/MessagePopoverMessageHandling/localService/mockdata/FormsModel.json - the same 8 forms and the single employment row - which is what the port seeds verbatim (Julie` &&
            ` Armstrong, Denise Smith, Richard Wilson, Gerd Becker, John Miller with the built-in invalid ZIP AAA, Stefan Bosch, Maria Fontes with empty email + malformed website, Antonio Ferrari). // NOTE: the` &&
            ` sample's nested JSON model (street/name, zip/code, phone/number) is flattened into a flat ABAP row (STREET_NAME, ZIPCODE, PHONE_NUMBER, ...) - abap2UI5 serves one default model; the bindings use the`.
    text1 = text1 && ` flattened field names. The three f:ColumnElementData layoutData hints are kept 1:1. // NOTE: onChange/handleRequiredField/checkInputConstraints are replaced by the framework: the typed value bindings` &&
            ` with constraints (NAME sap.ui.model.type.String required, ZIPCODE Integer, EMAIL String with the search regex, WEEKLYHOURS Integer maximum 40) collect and clear their validation messages` &&
            ` AUTOMATICALLY into the message> model with no app code. Save ports _generateInvalidUserInput in effect: since abap2UI5 auto-collects validation messages only from USER input and not from` &&
            ` programmatically-set model values, Save injects the four demo issues on the SAME rows the original targets (formContainer.getItems()[4/5/6] = John Miller Name empty, Stefan Bosch ZIP 'AAA', Maria` &&
            ` Fontes email malformed, plus the employment row's weekly hours 400) AND authors the matching four messages (3 Errors + 1 Warning) with their /T_FORMS/4|5|6 and /T_EMPLOYMENT/0 targets, then opens the` &&
            ` MessagePopover via follow_up_action( control_by_id openBy ) anchored to messagePopoverBtn - the original's setTimeout( oMP.openBy(oButton), 100 ). The app-authored messages ride the`.
    text1 = text1 && ` z2ui5.cc.MessageManager companion control (items bound to /T_MESSAGES) - an added control the original does not have (control extra z2ui5:MessageManager), which is how abap2UI5 feeds the message>` &&
            ` model with messages the application itself raises. // NOTE: buttonIconFormatter / buttonTypeFormatter / highestSeverityMessages are RESTORED (app 065 dropped them): the severity ranking Error >` &&
            ` Warning > Success > Info is a domain computation, so - abap2UI5 being a thin frontend - it runs in ABAP (button_severity_set) over the app-authored message table and feeds the button's bound icon,` &&
            ` type and text, matching the original's values exactly for this sample's Save flow (3 Errors -> type Negative, icon sap-icon://error, text '3'). The framework-collected validation messages that the` &&
            ` typed bindings raise on user input are not visible to the backend, so they do not shift the ranking the way the original's formatters (which scan the whole message model) would; visible="{=` &&
            ` !!${message>/}.length }" is kept 1:1 as a frontend expression over the full model. btn_type is seeded 'Default' because an empty string would override the sap.m.ButtonType enum DEFAULT and reject the`.
    text1 = text1 && ` whole view. // NOTE: handleMessagePopoverPress (this.oMP.toggle(oEvent.getSource())) becomes a roundtrip-free client->follow_up_action( cs_event-control_by_id, toggleBy ) on the button press,` &&
            ` anchored to messagePopoverBtn - the same frontend-action pattern as apps 065/066/067. The MessagePopover itself is declared in the button's dependents aggregation, which is where the original adds it` &&
            ` (addDependent). // NOTE: MessageItem grouping (groupItems=true) is RESTORED: the original derives groupName in its controller (getGroupName reads the form titles around the target control). That is a` &&
            ` domain classification, so it is computed in the ABAP backend and carried on the Message code field (sap.ui.core.message.Message has no groupName slot), bound groupName="{message>code}" - Name and ZIP` &&
            ` land in 'Personal, Information', Email in 'Personal, Contact' and Standard Weekly Hours in 'Personal, Rating' (the employment form puts it in its Rating group). // NOTE: activeTitlePress` &&
            ` scroll-to-control navigation is RESTORED (activeTitle=true): pressing a message title fires ACTIVE_TITLE with the message's target control id`.
    text1 = text1 && ` (${$parameters>/item}.getBindingContext('message').getObject().getControlIds()[0]), and the handler runs follow_up_action( scroll_into_view ) + control_by_id close + set_focus on it - the 1:1` &&
            ` equivalent of the sample's scrollToElement + focus. isReachable (which returns true unconditionally) becomes the constant activeTitle="true". // IMPROVISED: the original re-renders the footer button` &&
            ` from an attachChange handler on the MessagePopover's items binding (navigateBack + setType/setIcon/setText). abap2UI5 has no binding-change hook, so the button's three properties are recomputed on` &&
            ` the SAVE round-trip that authors the messages - the only moment they change in this sample. The popover's own navigateBack is not called, and the reason is NOT the one this note used to give:` &&
            ` "MessagePopover.navigateBack has no whitelisted frontend-action counterpart" is false - CONTROL_METHODS carries navigateBack: [] and apps 099/284/294/421/563 call it through control_by_id. It is not` &&
            ` called because it would have nothing to do: abap2UI5 rebuilds the popup fragment on the round trip, so the MessagePopover is opened FRESH and a fresh popover already starts on its list page - the`.
    text1 = text1 && ` same reasoning apps 563/564 write down for their own handleDialogPress/handlePopoverPress. // NOTE: not yet verified in a running system: the Dialog + MessagePopover-in-dependents combination, the` &&
            ` toggleBy/openBy anchoring from inside a popup, and the backend-computed button severity. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_562.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Dialog`                          name = `DialogWithMessagePopover`                      class = `z2ui5_cl_smpc_app_562` path = `src/02/01/z2ui5_cl_smpc_app_562.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `four post-1.71 members are kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the page button, the message-popover button and the Reject button; MessagePopover.groupItems (since UI5 1.73);` &&
                 ` the Dialog footer aggregation (sap.m.Dialog.footer, since UI5 1.110) which is exactly how MPDialog.fragment.xml puts its Toolbar under the dialog; and the sap-icon://information glyph (in the icon` &&
                 ` font since UI5 1.80), the default branch of the ported buttonIconFormatter.` ) ).

    text1 = `NOTE: the shared mock model sap/ui/demo/mock/supplier.json is flattened into the default model: the row type keeps only the six bound columns (SupplierName, Street, HouseNumber, ZIPCode, City,` &&
            ` Country) of the single /SupplierCollection record and drops the unbound columns (Url, Twitter, Tel, Sms, Mobile, Pager, Fax, Email, Rating, Prime, Disposable); the record itself is kept verbatim. //` &&
            ` NOTE: the sample controller loads the shared mock sap/ui/demo/mock/supplier.json - snapshotted byte-identical into ui5/mock/supplier.json (2026-07-20, git sparse checkout of SAP/openui5 master); one` &&
            ` SupplierCollection record, full row set, values verifiable offline. // NOTE: element binding binding="{/T_SUPPLIERS/0}" with relative DisplayListItem value bindings - the binding= context mechanism` &&
            ` is already live-verified via app 041 (checked 2026-07-19); the /0 array-index path variant is a plain JSONModel path resolution, source-decidable, so no LIVE_TEST is carried.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DisplayListItem`                 name = `DisplayListItem`                               class = `z2ui5_cl_smpc_app_020` path = `src/01/01/z2ui5_cl_smpc_app_020.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_020.mjs: the element-bound List resolves its four relative DisplayListItem values - Red Point Stores, Main St 1618, 31415 Maintown, Germany -` &&
                 ` against the serialized model)`
        notes = text1 ) ).

    text1 = `NOTE: the controller's imperative DraftIndicator calls (showDraftSaving/showDraftSaved/clearDraftState on byId('draftIndi')) are not in the FrontendAction CONTROL_METHODS whitelist; per AGENTS 'prefer` &&
            ` a bindable property over a frontend action' the port two-way binds the control's public state property (sap.m.DraftIndicatorState, since 1.32) and sets Saving/Saved/Clear in the three event handlers.` &&
            ` Source-verified equivalent: a binding update calls the public mutator (ManagedObjectBindingSupport.updateProperty -> _sMutator), and DraftIndicator.setState queues exactly what the three methods` &&
            ` queue (setState('Saving') queues Saving+Clear like showDraftSaving, sap/m/DraftIndicator.js). Adds the state attribute on the DraftIndicator vs the original view.xml. // NOTE: pressing the same` &&
            ` button twice in direct succession does not re-run the indicator - the model value is unchanged, so no binding change fires and setState is not called again (the original re-queues on every method` &&
            ` call); pressing a different button in between restores the behaviour.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DraftIndicator`                  name = `DraftIndicator`                                class = `z2ui5_cl_smpc_app_021` path = `src/01/01/z2ui5_cl_smpc_app_021.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.32.0`
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_021.mjs: the DraftIndicator boots Clear, and the three buttons round-trip SET_SAVING_DRAFT / SHOW_DRAFT_SAVED / CLEAR_DRAFT_STATE so the bound` &&
                 ` state reads Saving (with its label shown), Saved and Clear in turn)`
        notes = text1 ) ).

    text1 = `NOTE: onMessagesButtonPress builds a MessagePopover in the controller and toggles it against the MessagesIndicator. The port declares the same MessagePopover with its MessageItem template over the` &&
            ` message> model in the indicator's dependents and toggles it via follow_up_action control_by_id / toggleBy anchored to $event.oSource.sId (app 107 precedent); structural-diff reports both as control` &&
            ` extra because the original builds them in JavaScript. // NOTE: onInit registers a ControlMessageProcessor and seeds one error message through the MessageManager. The port declares the` &&
            ` z2ui5.cc.MessageManager bridge control with the same single message bound to it - an added control the original does not have (control extra z2ui5:MessageManager), which is how abap2UI5 feeds the` &&
            ` message> model. // IMPROVISED: handleLiveChange calls showDraftSaving(), showDraftSaved() and clearDraftState() one after the other in the same tick, so the two intermediate states are never painted` &&
            ` and only the clear survives. The port makes exactly that surviving call on every keystroke (control_by_id / clearDraftState, client-side, so no per-keystroke round-trip), which reproduces what the`.
    text1 = text1 && ` sample shows but not the two calls that have no visible effect. // NOTE: onSemanticButtonPress toasts the source's class name with the LIBRARY prefix stripped (sap.m.semantic.AddAction ->` &&
            ` semantic.AddAction). The class name is transported with $event.oSource.getMetadata().getName() and the strip happens in ABAP, so the toast text is the original's. onNavButtonPress is a constant toast` &&
            ` and is composed on the client. // NOTE: The unused xmlns:ui="sap.ca.ui" namespace declaration of the original view is dropped - sap.ca.ui is not part of OpenUI5 and no control in the sample uses the` &&
            ` prefix. // NOTE: The MessagePopover toggle over the bridged message, the three semantic action toasts, the nav-button toast and the clearDraftState wire are unverified in a running system.` &&
            ` **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_448.mjs). // NOTE: handleLiveChange's three calls do NOT collapse into the last one, which is what the port` &&
            ` assumed while making only the clearDraftState call - so typing showed nothing at all. DraftIndicator is queue-driven with a minDisplayTime of 1500 ms: showDraftSaving pushes [Saving, Clear] and`.
    text1 = text1 && ` _processQueue paints "Saving..." immediately and arms the timer, after which the two later calls - which only queued, because _processQueue returns early while iDelayedCallId is set - drain and paint` &&
            ` "Draft saved" for another 1500 ms. The original therefore visibly shows Saving... -> Draft saved on every keystroke, which is what the sample's own header text advertises. The port now calls` &&
            ` showDraftSaving, which alone queues [Saving, Clear] and so shows the indicator and clears itself, roundtrip-free. The intermediate "Draft saved" label stays unreproduced: an event attribute carries` &&
            ` ONE action, and routing all three through a round-trip instead puts a server hop on every keystroke - the lossy pattern AGENTS section 10 warns about, and it trips the live-event-roundtrip advisory` &&
            ` ratchet, so it was tried and withdrawn.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.DraftIndicator`                  name = `SemanticPageDraftIndicator`                    class = `z2ui5_cl_smpc_app_448` path = `src/01/01/z2ui5_cl_smpc_app_448.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.32.0`
        notes = text1 ) ).

    text1 = `POST-1.71: one post-1.71 member is kept for the 1:1 port: the FacetFilterList search event (since UI5 1.76), which is the whole point of this sample - it is what lets the application replace the` &&
            ` list's built-in filtering with its own. // NOTE: lists="{/ProductCollectionStats/Filters}" stays BOUND here (unlike app 235, which unrolled the same collection into two static lists): the` &&
            ` FacetFilterList is a template over T_FILTERS and its items come from the group's own nested VALUES table, so the port keeps the original's two-level binding shape. The stats entries carry only type +` &&
            ` values (no key field), so the original's key="{key}" resolves undefined against the same mock and the port leaves KEY empty; the value objects carry text + data, so the item template binds counter to` &&
            ` {COUNT} and - like app 235 - key to {TEXT}. // NOTE: handleSearch reads oEvent.getParameter('clearButtonPressed'), which sap.m.FacetFilterList.search does not declare (its only parameter is term), so` &&
            ` the port does not transport it. It makes no difference: the original's ternary builds Filter('text', Contains, '') for the clear button and Filter('text', Contains, term) otherwise, and clearing the`.
    text1 = text1 && ` search field delivers an empty term - so the port simply treats an empty term as the reset, producing the same list both ways. Contains on a client model is case-insensitive, which is why the` &&
            ` original's toLowerCase() has no counterpart; the ABAP search compares upper-cased. // NOTE: handleSearch calls oEvent.preventDefault( ) to suppress the FacetFilterList's internal search filtering so` &&
            ` the custom filter is the only one, and the port reproduces it since 2026-09-04: the search wire carries s_ctrl-check_prevent_default, which IS that call, baked into the wire at render time - the` &&
            ` right form here because the original vetoes on every search rather than per term (prevent_default_expr is the per-firing twin, app 306's idiom). Until then this was an IMPROVISED deviation reading "a` &&
            ` thin frontend cannot cancel a UI5 event", which stopped being true when the flag shipped; the built-in filtering therefore ran on top of the backend-narrowed list, and the port relied on both` &&
            ` applying the same 'text contains term' criterion for the result to match. It no longer relies on that. The custom filtering itself was always ported: the term round-trips and the backend narrows the`.
    text1 = text1 && ` group's values, keeping the selection flags of the values it hides. Found by scripts/probes/improvised-cluster.mjs (the pr/conditional-prevent-default GAP, closed upstream and outlived here as it had` &&
            ` in app 306). // NOTE: selection transport - every FacetFilterItem binds selected two-way; on the FacetFilter confirm (and on reset) the backend reads/clears the flags and re-filters. This is the` &&
            ` documented 1:1 path (CAPABILITIES.md marks controller-read FacetFilter/List multi-select as expressible, with app 022 as its evidence port). handleFacetFilterReset also drops the per-list search` &&
            ` filters, which the port does by restoring the untouched master list. // NOTE: the original's nested items-binding filter (_filterModel: ORs inside each facet group, AND across the groups, model` &&
            ` untouched) is expressed 1:1 as a declarative compound filter: apply_filter builds the groups JSON from the two-way bound selected flags and schedules follow_up_action cs_event-binding_call filter on` &&
            ` idProductsTable/items. The filter column is the list TITLE, exactly as the original uses oList.getTitle() - Category / SupplierName. The filter is issued from apply_filter( ) AND re-issued from`.
    text1 = text1 && ` view_display( ) (via the shared filter_issue( ), guarded by the last-issued payload in filter_live), because it lives on the binding and not in the model - the app-000/607 idiom. // NOTE: onInit` &&
            ` creates the sap.m.sample.Table component, swaps the first cell for an ObjectIdentifier {Name}/{Category} and appends the table to idVBox; that table is rebuilt inline (the same inline rebuild app 235` &&
            ` carries), which is why the whole Table subtree - its header toolbar with the popin-layout ComboBox, the sticky CheckBoxes and the Hide/Show InfoToolbar ToggleButton, the info toolbar, the five` &&
            ` columns and the ColumnListItem cells - counts as extra against the archived FacetFilter.view.xml. The price column keeps the original sap.ui.model.type.Currency composite binding 1:1. // NOTE: the` &&
            ` appended demo table's own controller logic is expressed the way app 235 expresses it: Table.sticky is bound to an ABAP string table maintained from the CheckBox round-trip (the app-009 pattern), the` &&
            ` ComboBox selectedKey is bound two-way and popinLayout is an expression over it (so the ComboBox change attribute is not emitted), and the ToggleButton's pressed flag drives the info toolbar's visible`.
    text1 = text1 && ` expression. // 1.71: the p:ColumnAIAction column plugin of the appended sap.m.sample.Table view (sap.m.plugins, far newer than UI5 1.71) is dropped with its dependents aggregation and press toast -` &&
            ` the plugin class does not exist on a 1.71 runtime, so keeping it would crash view creation there. // NOTE: the original derives the ObjectNumber weight state in the demo table's frontend Formatter.js` &&
            ` (weightState: KG conversion + Success/Warning/Error thresholds). That is business logic, so - abap2UI5 being a thin frontend - it is computed in ABAP model_init into a WEIGHT_STATE field and bound` &&
            ` state="{WEIGHT_STATE}", not via a frontend formatter. // NOTE: not yet verified in a running system: the two-level lists/{VALUES} binding (a nested table inside a bound aggregation row) and the` &&
            ` search round-trip. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_557.mjs). // NOTE: Checked 2026-08-26, no change needed - recorded so the check is not` &&
            ` repeated. apply_filter splices value-text and the list title into the compound-groups JSON without escaping, and this port DOES have a free-text search field, so it reads like the app 218/420/233/499`.
    text1 = text1 && ` exposure. It is not: the search term never reaches the JSON. list_search only uses it as a server-side CS predicate that narrows t_filters, and it rebuilds t_filters from t_filters_all first, so` &&
            ` every TEXT it keeps is one of the 28 seeded literals in model_init and every TYPE is Category or SupplierName. None of them contains a double quote or a backslash, and no control in the view can` &&
            ` write TEXT (FacetFilterItem binds text and key one-way; only selected is two-way). Same conclusion as apps 022 and 235, which carry it as an inline comment. The residual case is a tampered client` &&
            ` model - t_filters is a public attribute and round-trips - but that is true of every bound field in the corpus and its only effect is a filter that fails to apply in the tamperer's own session. //` &&
            ` NOTE: Measured 2026-08-26 with a probe, before the fix: filter to Category=Accessories (34 of 123 products), then restore the very same draft through the framework's own bookmark URL` &&
            ` (?app_start=<class>#/z2ui5-xapp-state=<draft>, what app_state_get_href( ) hands out). That request carries no frontend id, so the backend takes factory_first_start -> db_load(draft):`.
    text1 = text1 && ` check_on_navigated( ) is true while check_on_init( ) stays false, i.e. the ELSEIF branch, which is the only way a port that never calls another app reaches view_display( ) a second time. The rebuilt` &&
            ` view came back with 123 rows and ZERO aFilters while the FacetFilter still read "Accessories" - the client-side filter lives on the LIVE items binding and dies with it, the two-way bound selected` &&
            ` flags are class state and survive. Re-issuing the identical binding_call against the rebuilt binding put the 34 rows back, and the empty form ([] -> buildFilterGroups -> binding.filter([]) in` &&
            ` core/actions/ControlCall.js) clears without error, which is why the guard is "has a filter ever been issued" and not a selection scan. Statement order in ABAP is irrelevant: View1.controller.js` &&
            ` awaits every T_SYSTEM display before it runs a T_CUSTOM follow-up. One consequence worth naming: apply_filter( ) builds its groups from t_filters, the search-NARROWED list, not from the t_filters_all` &&
            ` master - so after a SEARCH that hides a selected value, the rebuild re-issues the narrower filter. That is the port's existing CONFIRM semantics (the next confirm would emit the same thing), not`.
    text1 = text1 && ` something the re-issue introduces.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FacetFilter`                     name = `FacetFilterCustomFilters`                      class = `z2ui5_cl_smpc_app_557` path = `src/02/01/z2ui5_cl_smpc_app_557.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 member is kept for the 1:1 port: the FacetFilterList search event (since UI5 1.76), which is the whole point of this sample - it is what lets the application replace the list's built-in` &&
                 ` filtering with its own.` ) ).

    text1 = `NOTE: the bound lists="{/ProductCollectionStats/Filters}" collection is represented as two static FacetFilterLists (Category, SupplierName) - the original's stats model yields exactly these two lists,` &&
            ` so this is a faithful equivalent; the facet values inside each list stay bound to a FacetFilterItem template. A doubly-nested variant (bind the FacetFilter lists aggregation to a nested table,` &&
            ` FacetFilterList template with a relative items binding over each filter's values) is source-plausible now that nested binding is expressible, but no port proves that aggregation-of-aggregation` &&
            ` template shape yet - LIVE_TEST before adopting it. // NOTE: selection transport - every FacetFilterItem binds selected two-way; on listClose/reset the backend reads/clears the flags and re-filters.` &&
            ` This is the documented 1:1 path (CAPABILITIES.md marks controller-read FacetFilter/List multi-select as expressible with app 022 as its evidence port), not a workaround; the model is applied before` &&
            ` on_event runs. // NOTE: the original controller appends the sap.m.sample.Table component's table with its first cell swapped for an ObjectIdentifier {Name}/{Category}; that table is rebuilt inline.`.
    text1 = text1 && ` The price column keeps the original sap.ui.model.type.Currency composite binding 1:1 (raw binding-info string over a numeric PRICE field). // NOTE: the sticky options Label and the three sticky` &&
            ` CheckBox controls are BACK (2026-08-05). The earlier rationale - 'neither an array property binding nor a setSticky whitelist entry is a proven path' - was wrong on the first half: app 009 binds` &&
            ` Table.sticky to an ABAP string table and is live-verified. This port now uses that same pattern: sticky is bound to t_sticky, each CheckBox round-trips ${$source>/text} + ${$parameters>/selected} and` &&
            ` the backend maintains the set (a set operation is backend work; setSticky was whitelisted upstream in the same round for the imperative case, but the bound path is the thin-frontend one). The three` &&
            ` controls come from the appended sap.m.sample.Table view, which this port rebuilds inline, so they count as extra against the archived FacetFilter original. The neighbouring onPopinLayoutChanged is` &&
            ` unchanged and still expressed as bound properties (the app-009 pattern), so the ComboBox ``change`` attribute stays dropped: its selectedKey is bound two-way and the Table's popinLayout is an`.
    text1 = text1 && ` expression binding over it, including the Block default. // NOTE: Restored 2026-08-05/06. The sticky options Label and the three sticky CheckBoxes are present and work: Table.sticky is an` &&
            ` ARRAY-valued property, and binding it to a plain ABAP string table IS the proven path - app 009 does exactly that and is live-verified. Each CheckBox select round-trips ${$source>/text} and` &&
            ` ${$parameters>/selected}, the ABAP handler inserts or removes that option and pushes the model back, which is the array maintenance the original's onSelect does with setSticky. The sidecar's earlier` &&
            ` claim - that neither an array property binding nor a setSticky whitelist entry was a proven path - was FALSE when it was written and is corrected here rather than quietly deleted; the whitelist entry` &&
            ` that followed (CONTROL_METHODS setSticky) closes a different footgun, an imperative call that silently received a string. // 1.71: the p:ColumnAIAction column plugin (sap.m.plugins, far newer than` &&
            ` UI5 1.71) is dropped with its dependents aggregation and press toast - the plugin class does not exist on a 1.71 runtime, so keeping it would crash view creation there. // NOTE: the original's nested`.
    text1 = text1 && ` items-binding filter (ORs inside each facet group, AND across the groups, model untouched) is expressed 1:1 as a declarative compound filter: apply_filter builds the groups JSON from the two-way` &&
            ` bound selected flags and schedules follow_up_action cs_event-binding_call filter on idProductsTable/items (compound groups implemented upstream 2026-07-20, pr/binding-call-compound-filters); the` &&
            ` earlier ABAP-side model rebuild and the t_products_all mirror are gone. The filter is issued from apply_filter( ) AND re-issued from view_display( ) (via the shared filter_issue( ), guarded by the` &&
            ` last-issued payload in filter_live), because it lives on the binding and not in the model - the app-000/607 idiom. // NOTE: the original derives the ObjectNumber weight state in its frontend` &&
            ` Formatter.js (weightState: KG conversion + Success/Warning/Error thresholds). That is business logic, so - abap2UI5 being a thin frontend - it is computed in ABAP model_init into a WEIGHT_STATE field` &&
            ` and bound state="{WEIGHT_STATE}", not via a frontend formatter (core:require dropped). Visually 1:1 with the original. // NOTE: the controller's onToggleInfoToolbar (ToggleButton press handler`.
    text1 = text1 && ` calling getInfoToolbar().setVisible(!pressed)) is expressed as bound properties instead of a round-trip: the ToggleButton's press attribute is dropped, its pressed property is bound two-way, and the` &&
            ` infoToolbar's OverflowToolbar gains a visible={= !pressed } expression binding. // NOTE: Measured 2026-08-26 with a probe, before the fix: filter to Category=Accessories (34 of 123 products), then` &&
            ` restore the very same draft through the framework's own bookmark URL (?app_start=<class>#/z2ui5-xapp-state=<draft>, what app_state_get_href( ) hands out). That request carries no frontend id, so the` &&
            ` backend takes factory_first_start -> db_load(draft): check_on_navigated( ) is true while check_on_init( ) stays false, i.e. the ELSEIF branch, which is the only way a port that never calls another` &&
            ` app reaches view_display( ) a second time. The rebuilt view came back with 123 rows and ZERO aFilters while the FacetFilter still read "Accessories" - the client-side filter lives on the LIVE items`.
    text1 = text1 && ` binding and dies with it, the two-way bound selected flags are class state and survive. Re-issuing the identical binding_call against the rebuilt binding put the 34 rows back, and the empty form ([]` &&
            ` -> buildFilterGroups -> binding.filter([]) in core/actions/ControlCall.js) clears without error, which is why the guard is "has a filter ever been issued" and not a selection scan. Statement order in` &&
            ` ABAP is irrelevant: View1.controller.js awaits every T_SYSTEM display before it runs a T_CUSTOM follow-up. This also bounds the 2026-07-20 human live check recorded above: it exercised the filter` &&
            ` inside one running app instance, where the view is never rebuilt, so it could not have seen this. The port stays checked - what was checked passed - but the compound-binding_call reference claim now` &&
            ` rests on the re-issue as well.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FacetFilter`                     name = `FacetFilterLight`                              class = `z2ui5_cl_smpc_app_022` path = `src/01/01/z2ui5_cl_smpc_app_022.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed); live-checked reference example for: compound` &&
                 ` binding_call filter, curated formatter module, two-way facet selection`
        notes = text1 ) ).

    text1 = `NOTE: the bound lists="{/ProductCollectionStats/Filters}" collection is represented as two static FacetFilterLists (Category, SupplierName) - the original's stats model yields exactly these two lists,` &&
            ` so this is a faithful equivalent; the facet values inside each list stay bound to a FacetFilterItem template. The stats value objects carry only text + data fields (no key), so the template's key` &&
            ` binds {TEXT} (the original's key='{key}' resolves undefined against the same mock) and the counter binds the data value as {COUNT}. This is the type="Simple" variant of the FacetFilter (app 022 ports` &&
            ` the near-identical type="Light" FacetFilterLight); the difference is the FacetFilter type plus the confirm handling below. // NOTE: selection transport - every FacetFilterItem binds selected two-way;` &&
            ` on the FacetFilter confirm (and on reset) the backend reads/clears the flags and re-filters. This is the documented 1:1 path (CAPABILITIES.md marks controller-read FacetFilter/List multi-select as`.
    text1 = text1 && ` expressible with app 022 as its evidence port), not a workaround; the model is applied before on_event runs. The original's handleConfirm also shows a MessageToast 'confirm event fired' - reproduced` &&
            ` 1:1 via client->message_toast_display after applying the filter. The FacetFilterSimple view does not wire the per-list listClose (only the confirm event drives filtering), so no listClose attribute` &&
            ` is emitted. // NOTE: the original controller appends the sap.m.sample.Table component's table with its first cell swapped for an ObjectIdentifier {Name}/{Category}; that table is rebuilt inline. The` &&
            ` price column keeps the original sap.ui.model.type.Currency composite binding 1:1 (raw binding-info string over a numeric PRICE field). // NOTE: the sticky options Label and the three sticky CheckBox` &&
            ` controls are BACK (2026-08-05). The earlier rationale - 'neither an array property binding nor a setSticky whitelist entry is a proven path' - was wrong on the first half: app 009 binds Table.sticky`.
    text1 = text1 && ` to an ABAP string table and is live-verified. This port now uses that same pattern: sticky is bound to t_sticky, each CheckBox round-trips ${$source>/text} + ${$parameters>/selected} and the backend` &&
            ` maintains the set (a set operation is backend work; setSticky was whitelisted upstream in the same round for the imperative case, but the bound path is the thin-frontend one). The three controls come` &&
            ` from the appended sap.m.sample.Table view, which this port rebuilds inline, so they count as extra against the archived FacetFilter original. The neighbouring onPopinLayoutChanged is unchanged and` &&
            ` still expressed as bound properties (the app-009 pattern), so the ComboBox ``change`` attribute stays dropped: its selectedKey is bound two-way and the Table's popinLayout is an expression binding` &&
            ` over it, including the Block default. // NOTE: Restored 2026-08-05/06. The sticky options Label and the three sticky CheckBoxes are present and work: Table.sticky is an ARRAY-valued property, and` &&
            ` binding it to a plain ABAP string table IS the proven path - app 009 does exactly that and is live-verified. Each CheckBox select round-trips ${$source>/text} and ${$parameters>/selected}, the ABAP`.
    text1 = text1 && ` handler inserts or removes that option and pushes the model back, which is the array maintenance the original's onSelect does with setSticky. The sidecar's earlier claim - that neither an array` &&
            ` property binding nor a setSticky whitelist entry was a proven path - was FALSE when it was written and is corrected here rather than quietly deleted; the whitelist entry that followed` &&
            ` (CONTROL_METHODS setSticky) closes a different footgun, an imperative call that silently received a string. // 1.71: the p:ColumnAIAction column plugin (sap.m.plugins, far newer than UI5 1.71) is` &&
            ` dropped with its dependents aggregation and press toast - the plugin class does not exist on a 1.71 runtime, so keeping it would crash view creation there. // NOTE: the original's nested` &&
            ` items-binding filter (ORs inside each facet group, AND across the groups, model untouched) is expressed 1:1 as a declarative compound filter: apply_filter builds the groups JSON from the two-way` &&
            ` bound selected flags and schedules follow_up_action cs_event-binding_call filter on idProductsTable/items (compound groups implemented upstream 2026-07-20, pr/binding-call-compound-filters). The`.
    text1 = text1 && ` filter is issued from apply_filter( ) AND re-issued from view_display( ) (via the shared filter_issue( ), guarded by the last-issued payload in filter_live), because it lives on the binding and not` &&
            ` in the model - the app-000/607 idiom. // NOTE: the original derives the ObjectNumber weight state in its frontend Formatter.js (weightState: KG conversion + Success/Warning/Error thresholds). That is` &&
            ` business logic, so - abap2UI5 being a thin frontend - it is computed in ABAP model_init into a WEIGHT_STATE field and bound state="{WEIGHT_STATE}", not via a frontend formatter (core:require` &&
            ` dropped). Visually 1:1 with the original. // NOTE: Measured 2026-08-26 with a probe, before the fix: filter to Category=Accessories (34 of 123 products), then restore the very same draft through the` &&
            ` framework's own bookmark URL (?app_start=<class>#/z2ui5-xapp-state=<draft>, what app_state_get_href( ) hands out). That request carries no frontend id, so the backend takes factory_first_start ->` &&
            ` db_load(draft): check_on_navigated( ) is true while check_on_init( ) stays false, i.e. the ELSEIF branch, which is the only way a port that never calls another app reaches view_display( ) a second`.
    text1 = text1 && ` time. The rebuilt view came back with 123 rows and ZERO aFilters while the FacetFilter still read "Accessories" - the client-side filter lives on the LIVE items binding and dies with it, the two-way` &&
            ` bound selected flags are class state and survive. Re-issuing the identical binding_call against the rebuilt binding put the 34 rows back, and the empty form ([] -> buildFilterGroups ->` &&
            ` binding.filter([]) in core/actions/ControlCall.js) clears without error, which is why the guard is "has a filter ever been issued" and not a selection scan. Statement order in ABAP is irrelevant:` &&
            ` View1.controller.js awaits every T_SYSTEM display before it runs a T_CUSTOM follow-up.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FacetFilter`                     name = `FacetFilterSimple`                             class = `z2ui5_cl_smpc_app_235` path = `src/01/01/z2ui5_cl_smpc_app_235.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FeedContent`                     name = `FeedContent`                                   class = `z2ui5_cl_smpc_app_023` path = `src/01/01/z2ui5_cl_smpc_app_023.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_023.mjs: clicking the first FeedContent raises the client-composed 'The feed content is pressed.' toast, and the second renders its value 999)` ) ).

    text1 = `IMPROVISED: the controller's client-side DateFormat.getDateTimeInstance({ style: 'medium' }).format(new Date()) for the new entry's Date is rebuilt server-side in ABAP from sy-datum/sy-uzeit as an` &&
            ` English medium-style timestamp (e.g. 'Jul 20, 2026, 1:23:45 PM') - the locale-dependent client formatter is not available in the backend round-trip; the value is a plain model string exactly like in` &&
            ` the original; note the source differs too: sy-datum/sy-uzeit is the SERVER date/time and timezone, while the original renders the browser-local new Date() - around midnight or across timezones the` &&
            ` displayed timestamp of a new entry can differ from a client-side clock. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an` &&
            ` abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and` &&
            ` RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it`.
    text1 = text1 && ` now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count` &&
            ` was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FeedInput`                       name = `Feed`                                          class = `z2ui5_cl_smpc_app_024` path = `src/01/01/z2ui5_cl_smpc_app_024.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.22`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_024.mjs: the seeded feed renders, senderPress round-trips ${$source>/sender} into the 'Clicked on Link: Alexandrina Victoria' toast, and a posted text round-trips` &&
                 ` POST so a new Reply entry carrying it is inserted at the top of the list)`
        notes = text1 ) ).

    text1 = `POST-1.71: the FeedInput 'actions' aggregation (a Button next to the text area) is @since 1.139 - kept 1:1 on the last two FeedInputs; the app needs a UI5 release >= 1.139 to render it. // NOTE:` &&
            ` onPost: the original controller does MessageToast.show( "Posted new feed entry: " + evt.getParameter( "value" ) ) on every FeedInput; reproduced roundtrip-free as a client-composed toast` &&
            ` (cs_event-control_global MESSAGE_TOAST, template 'Posted new feed entry: {0}' filled by ${$parameters>/value}). The post attribute is kept, so structural-diff sees no difference. // NOTE:` &&
            ` onActionButtonPress: the original builds a Dialog imperatively (new Dialog({...}).open()) with a Text and two buttons; expressed 1:1 as a core:FragmentDefinition (Dialog > content/Text,` &&
            ` beginButton/Button 'Enable Post Button', endButton/Button 'Disable Post Button') shown via popup_display on the actions Button press (ACTION_PRESS). The Dialog + its controls are extra vs the` &&
            ` archived V.view.xml (which holds only the two trigger actions Buttons). // NOTE: the dialog begin/end buttons reproduce oFeedInput.enablePostButton(true/false) 1:1 via follow_up_action control_by_id`.
    text1 = text1 && ` + the whitelisted enablePostButton bool method (framework 2026-07-27, pr/feedinput-enable-post-button) and close the popup server-side (popup_destroy). The owning FeedInput (original:` &&
            ` oEvent.getSource().getParent()) is transported as a static button t_arg literal - the two action-carrying FeedInputs got added ids feedActionPlain/feedActionIcon for it (extra id attributes, not in` &&
            ` the original view). // NOTE: the icon 'test-resources/sap/m/images/george_washington.jpg' is rehosted to the OpenUI5 host (https://sdk.openui5.org/test-resources/...) per the project asset-URL rule;` &&
            ` presence of the icon attribute is unchanged. // NOTE: unverified in a running system: the ENABLE_POST/DISABLE_POST round-trips combining follow_up_action(enablePostButton) with popup_destroy in one` &&
            ` response (the app-019 toast+destroy precedent, but with a control method). **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the action dialog's 'Enable Post` &&
            ` Button' click runs the enablePostButton follow-up and the popup_destroy closes the dialog in the same response.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FeedInput`                       name = `FeedInput`                                     class = `z2ui5_cl_smpc_app_236` path = `src/02/01/z2ui5_cl_smpc_app_236.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.22`
        is_post171 = abap_true
        notes = text1
        post171 = `the FeedInput 'actions' aggregation (a Button next to the text area) is @since 1.139 - kept 1:1 on the last two FeedInputs; the app needs a UI5 release >= 1.139 to render it.` ) ).

    text1 = `NOTE: the original's removeItem derives the entry index from the item's binding-context path (getBindingContext().getPath().split('/').pop()); the port transports the List's indexOfItem(item) instead` &&
            ` - the aggregation index equals the model index for this bound list, so the spliced row is the same. // NOTE: feed.json entries 2 and 4 have no Actions property; the flat ABAP row type serializes` &&
            ` ACTIONS as an empty array ([] instead of undefined) - the actions aggregation renders no actions either way, and an empty array is not an empty-string/enum hazard. // NOTE: the relative AuthorPicUrl` &&
            ` asset paths (test-resources/sap/m/images/*.jpg) are rewritten to absolute https://sdk.openui5.org/test-resources/... per the project rule for runtime asset URLs. // POST-1.71:` &&
            ` sap.m.FeedListItem.actions is carried 1:1 from the original - the sample's whole point is the action sheet on a feed item. The aggregation PREDATES 1.71: at tag 1.71.0 sap.m.FeedListItem declares` &&
            ` ``actions: {type: 'sap.m.FeedListItemAction', multiple: true}`` itself, and it is even that control's defaultAggregation there (sap.m.FeedListItemAction is @since 1.52.0). What changed is that the`.
    text1 = text1 && ` aggregation was GENERALISED and relocated to sap.m.ListItemBase, whose JSDoc carries @since 1.137 - and that is the version any scanner reads, so the property gate reports it. The relocated-member` &&
            ` residual limit AGENTS section 5 names, the same shape as apps 241/299/300/301/302/407. Declared by policy so the gate has something to match. IMPORTANT, because the first version of this entry` &&
            ` (2026-08-21, a few hours earlier) said the opposite and was wrong: the app does NOT need UI5 >= 1.137, it runs on the 1.71 floor. The class nevertheless sits in src/02/01, because AGENTS section 3` &&
            ` derives the package mechanically from 'has a POST_171' - this is the corpus' first port whose ONLY POST_171 is a scanner artefact rather than a real runtime floor, so the folder here records the` &&
            ` declaration, not a requirement.`.
    text2 = `sap.m.FeedListItem.actions is carried 1:1 from the original - the sample's whole point is the action sheet on a feed item. The aggregation PREDATES 1.71: at tag 1.71.0 sap.m.FeedListItem declares` &&
            ` ``actions: {type: 'sap.m.FeedListItemAction', multiple: true}`` itself, and it is even that control's defaultAggregation there (sap.m.FeedListItemAction is @since 1.52.0). What changed is that the` &&
            ` aggregation was GENERALISED and relocated to sap.m.ListItemBase, whose JSDoc carries @since 1.137 - and that is the version any scanner reads, so the property gate reports it. The relocated-member` &&
            ` residual limit AGENTS section 5 names, the same shape as apps 241/299/300/301/302/407. Declared by policy so the gate has something to match. IMPORTANT, because the first version of this entry` &&
            ` (2026-08-21, a few hours earlier) said the opposite and was wrong: the app does NOT need UI5 >= 1.137, it runs on the 1.71 floor. The class nevertheless sits in src/02/01, because AGENTS section 3` &&
            ` derives the package mechanically from 'has a POST_171' - this is the corpus' first port whose ONLY POST_171 is a scanner artefact rather than a real runtime floor, so the folder here records the`.
    text2 = text2 && ` declaration, not a requirement.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FeedListItem`                    name = `FeedListItem`                                  class = `z2ui5_cl_smpc_app_025` path = `src/02/01/z2ui5_cl_smpc_app_025.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_025.mjs: senderPress round-trips PRESSED into 'Pressed on <author>', and the Delete entry of the bound actions sheet round-trips ACTION_PRESSED with the row index` &&
                 ` so the entry is DELETEd and 'Item deleted' toasts)`
        notes = text1
        post171 = text2 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxBasicAlignment`                         class = `z2ui5_cl_smpc_app_392` path = `src/01/01/z2ui5_cl_smpc_app_392.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxCols`                                   class = `z2ui5_cl_smpc_app_426` path = `src/01/01/z2ui5_cl_smpc_app_426.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The sample ships its own style.css (.equalColumns .columns min-height 200px and the .sapMFlexItem padding). abap2UI5 serves no per-sample stylesheet, so the two rules are injected verbatim` &&
                 ` through an extra core:HTML <style> leaf at the top of the view - structural-diff reports control extra core:HTML. Nothing else changes: both classes stay on the same controls the original carries.` )
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxDirectionOrder`                         class = `z2ui5_cl_smpc_app_393` path = `src/01/01/z2ui5_cl_smpc_app_393.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxGap`                                    class = `z2ui5_cl_smpc_app_158` path = `src/02/01/z2ui5_cl_smpc_app_158.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `NOTE: Three Panels demonstrating FlexBox gap / columnGap / rowGap (wrap=Wrap), each with nine typed Buttons, reproduced 1:1. // POST-1.71: FlexBox.gap, FlexBox.columnGap and FlexBox.rowGap (all @since` &&
                 ` 1.134) are used 1:1 - they are the sample's whole point. Newer than UI5 1.71; declared per the property-171 policy, so the app needs UI5 >= 1.134 to render the gaps.`
        post171 = `FlexBox.gap, FlexBox.columnGap and FlexBox.rowGap (all @since 1.134) are used 1:1 - they are the sample's whole point. Newer than UI5 1.71; declared per the property-171 policy, so the app needs UI5` &&
                 ` >= 1.134 to render the gaps.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxNav`                                    class = `z2ui5_cl_smpc_app_474` path = `src/01/01/z2ui5_cl_smpc_app_474.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The sample ships its own style.css - the list styling both FlexBoxes rely on (paddings, the 25% items, the hover transition). abap2UI5 serves no per-sample stylesheet, so the rules are injected` &&
                 ` verbatim through one extra core:HTML <style> leaf at the top of the view (structural-diff reports control extra core:HTML: 6 vs 7 - the six anchor leaves are the original's own). Nothing else` &&
                 ` changes.` )
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxNested`                                 class = `z2ui5_cl_smpc_app_026` path = `src/01/01/z2ui5_cl_smpc_app_026.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-19): verified in a running system - human visual pass 2026-07-19 over all apps: the six flex items and the h2 headings render with their injected-CSS background colours like the` &&
                 ` original. **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_026.mjs: all six h2 items render and the injected style really applies - computed background rgb(209,` &&
                 ` 219, 189) on .item1 and colour rgb(50, 54, 58) on the h2)`
        notes = `NOTE: the original colours .item1..item6 and the h2 headings via a separate style.css; here it is injected as a core:HTML content attribute (a style tag, minified - see CAPABILITIES.md; the EXTRA` &&
                 ` core:HTML control vs the original view). Confirmed rendering via the human visual pass 2026-07-19.` )
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxOpposingAlignment`                      class = `z2ui5_cl_smpc_app_394` path = `src/01/01/z2ui5_cl_smpc_app_394.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxRenderType`                             class = `z2ui5_cl_smpc_app_190` path = `src/01/01/z2ui5_cl_smpc_app_190.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.FlexBox`                         name = `FlexBoxSizeAdjustments`                        class = `z2ui5_cl_smpc_app_514` path = `src/01/01/z2ui5_cl_smpc_app_514.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The sample ships its own style.css - the dashed frame around every flex item and the zero-width variant two of the five panels use. abap2UI5 serves no per-sample stylesheet, so both rules are` &&
                 ` injected verbatim through one extra core:HTML <style> leaf (structural-diff reports control extra core:HTML). Nothing else changes.` )
      ( module = `sap.m`              control = `sap.m.FormattedText`                   name = `FormattedText`                                 class = `z2ui5_cl_smpc_app_154` path = `src/01/01/z2ui5_cl_smpc_app_154.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.38.0`
        notes = `NOTE: FormattedText.htmlText bound to a model field holding the original controller's demo HTML string (headings, link, list, pre, code, cite, dl) 1:1.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTag`                      name = `GenericTag`                                    class = `z2ui5_cl_smpc_app_027` path = `src/02/01/z2ui5_cl_smpc_app_027.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.62.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human pass 2026-07-20: app starts and renders like the original; no interaction paths were open for this port. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_027.mjs: ten or more GenericTags render with their ObjectNumber values (3.5M, 96), the situation tags and the valueState Error icon)`
        notes = `POST-1.71: ariaLabelledBy (since UI5 1.97) on the labeled GenericTag is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.97 to render it. // NOTE: the sample's` &&
                 ` Page.controller.js defines a press handler (MessageToast 'The GenericTag is pressed.') that is not referenced anywhere in Page.view.xml - no GenericTag carries a press attribute - so the port wires` &&
                 ` no event; the rendered view is a faithful 1:1 rebuild.`
        post171 = `ariaLabelledBy (since UI5 1.97) on the labeled GenericTag is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.97 to render it.` ) ).

    text1 = `NOTE: onSliderMoved calls setWidth( value + '%' ) on all TEN toolbars on every liveChange. The port reproduces it without a round-trip: the Slider value is two-way bound and every OverflowToolbar` &&
            ` carries the expression binding width={= ${viewport} + '%' }, so the Slider.liveChange attribute is dropped and ten width attributes are added. // IMPROVISED: The view declares a core:CommandExecution` &&
            ` in mvc:dependents and the Share button presses it with press="cmd:Share". abap2UI5 has no controller to hold a command, so the CommandExecution is dropped (structural-diff reports it missing) and the` &&
            ` Share button's press carries the shareAction toast itself. The toast text is the sample's 'Share action' once instead of repeated 42 times. // NOTE: The slider-driven resize of the ten toolbars, the` &&
            ` overflow priorities and the Share toast are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_530.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTag`                      name = `OverflowToolbarSimple`                         class = `z2ui5_cl_smpc_app_530` path = `src/01/01/z2ui5_cl_smpc_app_530.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.62.0`
        notes = text1 ) ).

    text1 = `NOTE: the sample's own style.css (one rule: .tileLayout floats left) is injected through an extra core:HTML style leaf (app 275 precedent) - one control the archived Page.view.xml does not have` &&
            ` (core:HTML 0 -> 1). The literal braces of the CSS rule are escaped in the ABAP literal because the XMLView parser reads an unescaped brace in an attribute value as a binding. // NOTE: the` &&
            ` controller's only handler is MessageToast.show with the CONSTANT text 'The GenericTile is pressed.', so every press wire is the roundtrip-free client toast (follow_up_action cs_event-control_global` &&
            ` MESSAGE_TOAST.show, app 005/275 idiom) and the app stays init-only. // NOTE: the two SlideTile tiles carry backgroundImage paths into the sample's own images folder` &&
            ` (test-resources/sap/m/demokit/sample/GenericTileAsFeedTile/images/NewsImage1.png and NewsImage2.png); they are kept verbatim and only host-absolutized to https://sdk.openui5.org/ per the runtime` &&
            ` asset-URL rule, so the images resolve against the OpenUI5 host exactly as in the demo kit.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTile`                     name = `GenericTileAsFeedTile`                         class = `z2ui5_cl_smpc_app_388` path = `src/01/01/z2ui5_cl_smpc_app_388.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34.0`
        notes = text1 ) ).

    text1 = `POST-1.71: frameType values OneByHalf / TwoByHalf (since UI5 1.83) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.83 to render them; OneByOne / TwoByOne (1.71) were` &&
            ` never affected. // POST-1.71: systemInfo and appShortcut (since UI5 1.92) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.92 to render them. // POST-1.71: url on the` &&
            ` link tiles (since UI5 1.76) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.76 to render it. // NOTE: the relative test-resources image and backgroundImage paths are` &&
            ` resolved to absolute sdk.openui5.org URLs so the tile images load standalone. // NOTE: the custom CSS class tileLayout (float: left) is kept and its style.css injected via a core:HTML content` &&
            ` attribute (see CAPABILITIES.md; the EXTRA core:HTML control vs the original view). Confirmed rendering via the human visual pass 2026-07-19.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTile`                     name = `GenericTileAsKPITile`                          class = `z2ui5_cl_smpc_app_028` path = `src/02/01/z2ui5_cl_smpc_app_028.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        since = `1.34.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-19): verified in a running system - human visual pass 2026-07-19 over all apps: the KPI tiles float left via the injected tileLayout CSS and render like the original. **e2e-verified` &&
                 ` 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_028.mjs: the injected .tileLayout float:left rule applies and clicking the first GenericTile raises the client-composed 'The tile` &&
                 ` is pressed.' toast)`
        notes = text1
        post171 = `frameType values OneByHalf / TwoByHalf (since UI5 1.83) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.83 to render them; OneByOne / TwoByOne (1.71) were never` &&
                 ` affected. // systemInfo and appShortcut (since UI5 1.92) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.92 to render them. // url on the link tiles (since UI5 1.76)` &&
                 ` is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.76 to render it.` ) ).

    text1 = `NOTE: press toasts a constant text on all eleven tiles - composed on the client, so no press needs a round-trip. // NOTE: onFormSubmit sets every tile to Loading and back to Loaded after the entered` &&
            ` number of seconds. The tile state is one bound property here and the framework's START_TIMER brings it back, so the same two states are shown; the original reads the seconds off the Input` &&
            ` imperatively, the port has them two-way bound. // NOTE: The two sample images (the SAP logo tile image and the profile picture) are the sample's own test-resources files, re-hosted on` &&
            ` sdk.openui5.org. // POST-1.71: GenericTile frameType="OneByHalf" and frameType="TwoByHalf" are @since 1.83, url is @since 1.76 and appShortcut / systemInfo are @since 1.92 - all newer than the 1.71` &&
            ` floor. The sample is built on exactly these members (the launch-tile shapes, the tile as a link and the two info lines), so they are kept and the port is filed under src/02. // NOTE: The` &&
            ` loading-state timer and the eleven tile presses are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_517.mjs). // NOTE: The`.
    text1 = text1 && ` sample's manifest loads a style.css whose single rule is ``.tileLayout { float: left; }``, and all eleven tiles carry that class. The port injects the rule through a core:HTML style leaf (the app-028` &&
            ` form) - without it the class resolves to nothing and the launch tiles stack vertically instead of floating into a tile wall.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTile`                     name = `GenericTileAsLaunchTile`                       class = `z2ui5_cl_smpc_app_517` path = `src/02/01/z2ui5_cl_smpc_app_517.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34.0`
        is_post171 = abap_true
        notes = text1
        post171 = `GenericTile frameType="OneByHalf" and frameType="TwoByHalf" are @since 1.83, url is @since 1.76 and appShortcut / systemInfo are @since 1.92 - all newer than the 1.71 floor. The sample is built on` &&
                 ` exactly these members (the launch-tile shapes, the tile as a link and the two info lines), so they are kept and the port is filed under src/02.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTile`                     name = `GenericTileAsMonitorTile`                      class = `z2ui5_cl_smpc_app_389` path = `src/01/01/z2ui5_cl_smpc_app_389.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34.0`
        notes = `NOTE: the sample's own style.css (one rule: .tileLayout floats left) is injected through an extra core:HTML style leaf (app 275 precedent) - one control the archived Page.view.xml does not have` &&
                 ` (core:HTML 0 -> 1). The literal braces of the CSS rule are escaped in the ABAP literal because the XMLView parser reads an unescaped brace in an attribute value as a binding. // NOTE: the` &&
                 ` controller's only handler is MessageToast.show with the CONSTANT text 'The GenericTile is pressed.', so every press wire is the roundtrip-free client toast (follow_up_action cs_event-control_global` &&
                 ` MESSAGE_TOAST.show, app 005/275 idiom) and the app stays init-only.` ) ).

    text1 = `POST-1.71: three post-1.71 members are kept for the 1:1 port: sap.m.GenericTile.linkTileContents (the property and the aggregation, both @since 1.120) and the control sap.m.LinkTileContent it holds` &&
            ` (@since 1.120). The app needs a UI5 release >= 1.120; below it the lowercase linkTileContents tag resolves as a control class and the 404 takes the whole view down, which is why this is an error` &&
            ` rather than a warning. // NOTE: changeEnforceSmall does model.setProperty('/sizeBehavior', state ? 'Small' : 'Responsive'). Both halves are bindable, so the Switch's state and the seven sizeBehavior` &&
            ` bindings share one round trip that writes one field - the same two values the original writes. The ComboBox's selectedKey and every scope binding likewise share the /scope field the sample already` &&
            ` binds, so switching the tile actions needs no handler at all: that is what the sample's own {/scope} binding does. // NOTE: press and pressSlideTile compose their toast from the pressed tile` &&
            ` (getHeader() || getTooltip()) and from the press action, and say 'Remove action of ...' when the action is Remove. Both travel: the wire carries ${$source>/header} (or ${$source>/tooltip} for the`.
    text1 = text1 && ` news tiles) and ${$parameters>/action}, and the backend composes the same two sentences. The two SlideTiles pass their own tooltip literally, since that is what the handler reads for them. // NOTE:` &&
            ` onLinkPress toasts the constant 'Pressed on Link', so it is wired round-trip-free as follow_up_action( control_global, MESSAGE_TOAST show ) (app 005 idiom). // NOTE: the tiles' state binding is {=` &&
            ` ${state} || 'Loaded' } in the sample - the mock leaves state absent on two of the five tiles and empty on the news tiles. A flat ABAP row would send an empty string into the LoadState enum, so the` &&
            ` fallback is applied at SEED time instead: the absent and empty states are seeded 'Loaded', which is the value the expression produces. The three the mock does set (Loading, Failed, Disabled) are` &&
            ` carried verbatim - they are what the sample shows. // NOTE: tiles.json carries slideTile1 and slideTile2 as two arrays of the SAME shape and the same length, so the data-fidelity gate cannot tell` &&
            ` which ABAP block belongs to which and compares both against slideTile1. The seeds are verbatim: t_slide1 is slideTile1 (NewsImage2 / 'August 22, 2016' / the NFL.com headline first) and t_slide2 is`.
    text1 = text1 && ` slideTile2 (NewsImage1 / 'August 21, 2016' / the Wind Map headline first), each with its own TOOLTIP ('NewsTile n of SlideTile 1' vs '... SlideTile 2'), BACKGROUNDIMAGE, FOOTER, CONTENTTEXT, SUBTITLE` &&
            ` and STATE. Nothing here is changed from the mock beyond the two folds this sidecar already records - the host absolutization on BACKGROUNDIMAGE and the 'Loaded' seed for an empty STATE. // NOTE: the` &&
            ` four background images come from the sample's own images folder and point at the sdk.openui5.org host per the offline asset-URL rule; the mock writes them relative as` &&
            ` 'test-resources/sap/m/demokit/sample/GenericTileLineMode/images/...'. The LinkTileContent hrefs stay the sample's own http://www.sap.com. // NOTE: not yet verified in a running system: the LineMode` &&
            ` row of tiles, the two auto-advancing SlideTiles, the scope switch and the Remove-action toast. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_606.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTile`                     name = `GenericTileLineMode`                           class = `z2ui5_cl_smpc_app_606` path = `src/02/01/z2ui5_cl_smpc_app_606.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34.0`
        is_post171 = abap_true
        notes = text1
        post171 = `three post-1.71 members are kept for the 1:1 port: sap.m.GenericTile.linkTileContents (the property and the aggregation, both @since 1.120) and the control sap.m.LinkTileContent it holds (@since` &&
                 ` 1.120). The app needs a UI5 release >= 1.120; below it the lowercase linkTileContents tag resolves as a control class and the 404 takes the whole view down, which is why this is an error rather than` &&
                 ` a warning.` ) ).

    text1 = `NOTE: the sample's style.css (one rule: .tileLayout floats left) is injected through an extra core:HTML style leaf (app 122/124/270 precedent) - one control the archived Page.view.xml does not have` &&
            ` (core:HTML 0 -> 1). The literal braces of the CSS rule are escaped in the ABAP literal because the XMLView parser reads an unescaped brace in an attribute value as a binding. // NOTE: the` &&
            ` controller's only handler is MessageToast.show('The generic tile is pressed.') with a CONSTANT text, so every press wire is the roundtrip-free client toast (follow_up_action cs_event-control_global` &&
            ` MESSAGE_TOAST.show, app 005 idiom) and the app stays init-only. The FIVE tiles the original leaves without a press handler keep none (corrected 2026-08-23 - the count read four and silently dropped` &&
            ` the GenericTile nested inside the SlideTile; the original has 10 tiles and 5 press wires, and so does the port), so the 'no press event' half of the sample still demonstrates exactly that. // NOTE:` &&
            ` the two SlideTile tiles carry backgroundImage paths that point into ANOTHER sample's folder (test-resources/sap/m/demokit/sample/GenericTileAsFeedTile/images/NewsImage1.png and NewsImage2.png); they`.
    text1 = text1 && ` are kept verbatim and only host-absolutized to https://sdk.openui5.org/ per the runtime asset-URL rule, so the images resolve against the OpenUI5 host exactly as in the demo kit. The sample's own` &&
            ` images/headerImg2.jpg is referenced by no control in the view and is therefore not seeded. // NOTE: live-verified on 2026-08-02 (maintainer live check): the Loading / Failed / Disabled tiles paint` &&
            ` their placeholder, error and disabled visuals and the SlideTile slides between its two tiles.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.GenericTile`                     name = `GenericTileStates`                             class = `z2ui5_cl_smpc_app_275` path = `src/01/01/z2ui5_cl_smpc_app_275.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34.0`
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1 ) ).

    text1 = `NOTE: the Select's literal selectedKey="1" is replaced by a two-way binding {SELECTED_KEY} (seeded '1'); scrollStepByItem on both HeaderContainers is now a pure client-side expression binding over it` &&
            ` ({= ${SELECTED_KEY} === 'px' ? 0 : +${SELECTED_KEY} }), so the original Select's change handler (.scrollChanged) is dropped - no round-trip. This is the prefer-a-bindable-property/expression pattern` &&
            ` (like the app 019). The original controller reads oEvent.getParameter('selectedItem').getKey(), which has no public $parameters path (selectedItem is a control; reading private control internals is` &&
            ` banned). // NOTE: the controller's setScrollStepByItem(0 | Number(key)) on both containers is reproduced by the scrollStepByItem expression binding above (px -> 0, else the number); seeded via` &&
            ` SELECTED_KEY='1' it matches the UI5 property default (HeaderContainer.js defaultValue: 1), so the initial arrow scroll is one item and selecting px sets 0 (px stepping via scrollStep), exactly like` &&
            ` the controller - all client-side, no scroll_step_by_item field. // NOTE: the NumericContent press ('Fire press' MessageToast) is wired roundtrip-free via client->follow_up_action(`.
    text1 = text1 && ` cs_event-control_global, MESSAGE_TOAST/show ) - the original's client-side MessageToast.show 1:1; the app is now init-only (no on_event). An unused xmlns:l namespace was dropped. // NOTE: the` &&
            ` scroll-step selection and the tile-press toast were switched from server round-trips to client-side expression binding / follow_up_action( control_global ) on 2026-07-22 - re-verify in a running` &&
            ` system that selecting an item/px still changes the arrow scroll step on both containers and that pressing a NumericContent still toasts 'Fire press'. **e2e-verified 2026-07-31**` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): pressing the first NumericContent tile raises its client-composed toast ('Fire press'). The scroll-step Select (item/px) is NOT` &&
            ` covered - it sits in the sample's option area and needs a human check. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_029.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.HeaderContainer`                 name = `HeaderContainer`                               class = `z2ui5_cl_smpc_app_029` path = `src/01/01/z2ui5_cl_smpc_app_029.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.44.0`
        notes = text1 ) ).

    text1 = `NOTE: the ObjectHeader carries binding="{/ProductCollection/}", so every one of its bindings is RELATIVE to that single record. The port folds it to root fields (app 229 idiom): DESCRIPTION, NAME,` &&
            ` PRICE, CURRENCYCODE and SUPPLIERNAME, seeded verbatim from the sample's own data.json. The binding attribute itself has no counterpart once the paths are absolute. // IMPROVISED: onScroll is the lazy` &&
            ` loading this sample is about: on every scroll it pushes THREE more tiles onto the model, each with a RANDOM value ((Math.random() * 10 + '').substring(0, 3)), a colour picked from` &&
            ` Math.round(Math.random() * 10) % 3 and a growth direction from the same number % 2. A backend cannot draw the browser's random numbers, and reproducing them with ABAP randomness would make the port` &&
            ` unrepeatable for the gates, so the three tiles that come next are taken from the mock's own eleven, cycling. The behaviour the sample demonstrates is identical - scroll to the end and three more` &&
            ` tiles appear, indefinitely - only the numbers repeat instead of being new each time. // NOTE: press does MessageBox.alert('Link was clicked!'), a constant-text alert, so it is wired round-trip-free`.
    text1 = text1 && ` as follow_up_action( control_global, MESSAGE_BOX alert ) - the tile answers in the browser, as upstream.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.HeaderContainer`                 name = `HeaderContainerLazyLoading`                    class = `z2ui5_cl_smpc_app_605` path = `src/01/01/z2ui5_cl_smpc_app_605.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.44.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.HeaderContainer`                 name = `HeaderContainerNoDividers`                     class = `z2ui5_cl_smpc_app_428` path = `src/01/01/z2ui5_cl_smpc_app_428.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.44.0`
        notes = `NOTE: onInit loops over the eight core:InvisibleTexts and calls headerContainer.addAriaLabelledBy(text<i>). The association is static (the ids never change), so the port writes it as the` &&
                 ` HeaderContainer.ariaLabelledBy attribute listing all eight ids instead of building it imperatively. // NOTE: press shows MessageToast.show('Fire press') - a constant text, so all eight NumericContent` &&
                 ` presses are composed on the client via follow_up_action control_global MESSAGE_TOAST.show and need no round-trip.` )
      ( module = `sap.m`              control = `sap.m.HeaderContainer`                 name = `HeaderContainerOH`                             class = `z2ui5_cl_smpc_app_477` path = `src/01/01/z2ui5_cl_smpc_app_477.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.44.0`
        notes = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/0}. The port seeds the five bound fields (Description, Name, Price, CurrencyCode, SupplierName) at the default-model root and binds` &&
                 ` them absolutely, so the attr ObjectHeader.binding is dropped. // NOTE: press shows MessageBox.alert('Link was clicked!') for all eight NumericContents - one round-trip event raising` &&
                 ` message_box_display with the same text and type. // NOTE: The eight NumericContent presses raising the alert are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction,` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_477.mjs).` ) ).

    text1 = `NOTE: NumericContent presses show a client MessageToast ('Fire press', the original press handler's text - review fixed the earlier text copied from the NumericContentDifColors neighbour). Two` &&
            ` vertical HeaderContainers — one of eight NumericContents, one of five TileContents (each wrapping a NumericContent) — reproduced 1:1. **e2e-verified 2026-07-30** (transpiled-framework interaction,` &&
            ` scripts/e2e-smoke.mjs): pressing the first NumericContent toasts 'Fire press'; the other tiles are the identical wire. // IMPROVISED: The sample's own style.css is neither archived nor reproduced.` &&
            ` HeaderContainerVM is the only sample in the 622 that declares its stylesheet through the OLD UIComponent form - Component.js metadata ``includes: "HeaderContainerVM/style.css"`` - instead of manifest` &&
            ` sap.ui5.resources.css, so the harvest never read the declaration and the file was never fetched; it is not reachable from this environment either (the network policy answers 403 for the OpenUI5` &&
            ` host). What the sheet does cannot be stated: the original view carries no author class at all, so its rules must target UI5's own render classes, and nothing offline says which. The first NOTE's`.
    text1 = text1 && ` "reproduced 1:1" therefore covers the control tree, not the rendering. scripts/check-archive.mjs reads both declaration forms since 2026-08-23 and holds this file in scripts/archive-absent.json; the` &&
            ` entry is to be dropped, and this deviation revisited, as soon as a harvest run brings the sheet in. Found by the reviewed re-read 2026-08-23.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.HeaderContainer`                 name = `HeaderContainerVM`                             class = `z2ui5_cl_smpc_app_157` path = `src/01/01/z2ui5_cl_smpc_app_157.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.44.0`
        notes = text1 ) ).

    text1 = `NOTE: Thin frontend: onFilterSelect builds nested sap.ui.model.Filters and calls oBinding.filter() on the table's items binding; the port carries the selected tab key into on_event via` &&
            ` ${$parameters>/key} and filters the model table in ABAP instead. The controller's thresholds are reproduced exactly - the weight is normalised to grams, below 1000 g is Ok, 1000 to 5000 g is Heavy,` &&
            ` above that is Overweight, and the All tab clears the filter. Same rows in the same sorter order, one round-trip per tab switch. // NOTE: Formatter.weightState is computed in ABAP and bound as a` &&
            ` finished value (state="{WEIGHT_STATE}"), the thin-frontend rule and the same treatment as app 009 on this mock. Its thresholds are KILOGRAMS (fMaxWeightSuccess = 1, fMaxWeightWarning = 5) and a G row` &&
            ` is divided by 1000 first. Until 2026-08-21 both this note and the code named 0/1000/2000 and skipped the unit normalisation, which is not the original's rule at all: every KG row falls below 1000, so` &&
            ` 66 of the 123 rows rendered Success - HT-1000 at 4.2 KG should be Warning, HT-1030 at 21 KG should be Error. Found by the review sweep, which read the note and the formatter side by side. The`.
    text1 = text1 && ` Currency parts binding on the Price ObjectNumber is kept verbatim as a raw binding-info string. // NOTE: The four tab counts bind /ProductCollectionStats/Counts/Total and` &&
            ` .../Weight/{Ok,Heavy,Overweight} of the shared mock - a precomputed stats object, not a collection. abap2UI5 has one flat default model, so they become the four scalar fields COUNT_TOTAL, COUNT_OK,` &&
            ` COUNT_HEAVY, COUNT_OVERWEIGHT seeded with the mock's own 123/53/51/19. Same values, same rendering; the counts stay static exactly as in the original, which never recomputes them either. // NOTE:` &&
            ` Width, Depth and Height are typed as ABAP string rather than packed. The mock mixes integers (30) and one-decimal values (40.8) in these columns and they are only bound into the text template` &&
            ` "{WIDTH} x {DEPTH} x {HEIGHT} {DIMUNIT}", so a fixed DECIMALS would render 30 as 30.0. // NOTE: The IconTabBar gains a selectedKey the original does not have (bound to selected_tab, seeded 'All', set` &&
            ` by table_filter). The original needs none: a UI5 view is built once, so the bar's own tab state simply persists across every filter. This port rebuilds the whole view on check_on_navigated( ), and`.
    text1 = text1 && ` selectedKey defaults to null - the bar would fall back to its FIRST tab while t_products still holds the last filter, showing 'All' over a filtered table, an inconsistency the original cannot reach.` &&
            ` Binding the key is the house idiom (prefer the bindable property over a frontend action); IconTabBar.selectedKey is @since 1.15.0, so nothing post-1.71 is pulled in. Declared here rather than left to` &&
            ` a gate: structural-diff does NOT see an added attribute at all - its attribute pass iterates the ORIGINAL's attrs and reports only what the port is missing, so there is no 'attr extra' kind. An` &&
            ` attribute a port adds is invisible to it.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBar`                                    class = `z2ui5_cl_smpc_app_377` path = `src/01/01/z2ui5_cl_smpc_app_377.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: onBackgroundDesignSelect and onHeaderBackgroundDesignSelect both read the picked RadioButton's TEXT and hand it to a setter. Both properties are bindable, and the three texts ARE the` &&
            ` sap.m.BackgroundDesign members, so the port binds each group's selectedIndex two-way and one shared round trip (event DESIGNS) turns the two indices into the two enum values the IconTabBar binds -` &&
            ` the same fold app 604 uses for its five option groups. // NOTE: both bound enums carry the null fallback ({= ${X} || null }, apps 548/555 idiom): a flat ABAP row serializes every field, so an unset` &&
            ` enum would arrive as an empty string and UI5 would reject the whole view. model_init also seeds both from the groups' own first button, which is what getSelectedButton() returns before any select. //` &&
            ` NOTE: Thin frontend: onFilterSelect builds a sap.ui.model.Filter (WeightUnit EQ KG AND-ed with the tab's measure range: LE 1 for Ok, BT 1..5 for Heavy, GT 5 for Overweight) and calls filter() on the` &&
            ` items binding. The port filters the ABAP table it sends instead (app 298 idiom), over the same three ranges and the same unit guard; the All tab clears the filter. // NOTE: the Weight cell's state is`.
    text1 = text1 && ` a formatter over the row's own measure and unit (Formatter.weightState: under 1 kg Success, under 5 kg Warning, else Error, and None for a non-numeric or negative measure, with grams divided by` &&
            ` 1000). A thin frontend computes it in the backend, so weight_state is a field of the seeded row - and it is seeded on EVERY row, since an empty string in a ValueState terminates the app (the app 566` &&
            ` lesson). // NOTE: the Price cell is a composite currency binding in the sample (parts Price + CurrencyCode through sap.ui.model.type.Currency with showMeasure false). The port binds the two fields` &&
            ` directly on the ObjectNumber's number and unit, which is what the formatter renders, since the mock's prices are plain numbers. // NOTE: the four tab counts come from /ProductCollectionStats/Counts` &&
            ` (123 total, 53 Ok, 51 Heavy, 19 Overweight) and are carried verbatim - they are the mock's own numbers and do NOT match what the filters actually select (the port's own weightState classification` &&
            ` yields 57/45/21), which is true upstream too. // NOTE: the items binding carries a sorter on Name; a thin frontend sorts the data it sends, so the 123 rows are seeded verbatim in the mock's order and`.
    text1 = text1 && ` SORTed by name in model_init - never through SORT BY (field), which the transpiled backend drops (the 2026-08-22 e2e finding). // NOTE: not yet verified in a running system: that each of the four` &&
            ` weight tabs filters the table to its range and that the two RadioButtonGroups repaint the bar's background and its header. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_617.mjs). // NOTE: The price cell keeps the original's composite sap.ui.model.type.Currency binding rather than binding the raw field. The type is not a` &&
            ` pass-through: NumberFormat's currency instance applies grouping and the currency's own fraction digits, so EUR renders 956.00 / 1,249.00 where the bare field renders 956 / 1249. The earlier` &&
            ` justification - that the fold is what the formatter renders because the mock's prices are plain numbers - was wrong on both halves.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarBackgroundDesign`                    class = `z2ui5_cl_smpc_app_617` path = `src/01/01/z2ui5_cl_smpc_app_617.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: two members and one control newer than UI5 1.71 are kept 1:1 because they ARE the sample: sap.m.BadgeCustomData (control since 1.80, the badge marker in every customData aggregation -` &&
            ` declared by policy, the property gate does not resolve it) and sap.m.IconTabFilter.items (since 1.77, the nested sub-tabs of iconTabBar8). The app needs a UI5 release >= 1.80. // NOTE: onInit adds 30` &&
            ` IconTabFilters to iconTabBar0 imperatively (text 'Tab n', key n, a Text 'Content n', and a BadgeCustomData visible on the 19th). The port builds that as a bound aggregation over T_TABS with one` &&
            ` template - so exactly one IconTabFilter, one Text and one BadgeCustomData more than the original view.xml carries, which is the template itself; the 30 instances come from the model, as they come` &&
            ` from the loop in the original. // NOTE: onTabDensityModeSelect loops over all nine bars and calls setTabDensityMode(selectedButton.getText()). tabDensityMode is a bindable property, so every` &&
            ` IconTabBar binds the one field TAB_DENSITY (added attribute, no structural diff) and only the RadioButtonGroup round-trips: its selectedIndex is bound two-way and the three values are derived`.
    text1 = text1 && ` server-side, per the prefer-a-bindable-property rule (linter rule settable-property-via-action). // NOTE: enableTabReordering stays true, 1:1 with the original, but a reorder no longer survives a` &&
            ` round trip. The original adds the 30 filters imperatively, so the aggregation is unbound and IconTabBarDragAndDropUtil's removeAggregation/insertAggregation is permanent; the port binds items to the` &&
            ` model (the 30 instances come from one template), and IconTabBar forwards that binding to its header, so the next model push re-assigns the contexts positionally and the tabs snap back. The app's one` &&
            ` round-trip is the density RadioButtonGroup, so the sequence reorder-then-change-density loses the order. Recorded 2026-08-23.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarBadges`                              class = `z2ui5_cl_smpc_app_287` path = `src/02/01/z2ui5_cl_smpc_app_287.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `two members and one control newer than UI5 1.71 are kept 1:1 because they ARE the sample: sap.m.BadgeCustomData (control since 1.80, the badge marker in every customData aggregation - declared by` &&
                 ` policy, the property gate does not resolve it) and sap.m.IconTabFilter.items (since 1.77, the nested sub-tabs of iconTabBar8). The app needs a UI5 release >= 1.80.` ) ).

    text1 = `POST-1.71: sap.m.IconTabBar.maxNestingLevel is @since 1.79 - the StepInput of this sample exists to change it, so the property is kept 1:1 and the port needs a UI5 runtime >= 1.79. // NOTE: onInit` &&
            ` adds the 30 IconTabFilters (with their content Texts) in a loop with addItem( ). abap2UI5 has no client-side control factory, so the port binds the items aggregation to a table with the same 30 rows` &&
            ` and declares one IconTabFilter template - structural-diff reports both as control extra because the original's view carries only the empty IconTabBar (apps 465-467 precedent). // NOTE:` &&
            ` onMaxNestingLevelChange calls iconTabBar.setMaxNestingLevel(value). The StepInput value and the IconTabBar's maxNestingLevel are the same two-way bound field here, so the StepInput.change attribute` &&
            ` is dropped and the property follows without a round-trip. // NOTE: The shared nesting-level field and the reordering-enabled tab bar are unverified in a running system. **e2e-verified 2026-08-25**` &&
            ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_506.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarDragDrop`                            class = `z2ui5_cl_smpc_app_506` path = `src/02/01/z2ui5_cl_smpc_app_506.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.IconTabBar.maxNestingLevel is @since 1.79 - the StepInput of this sample exists to change it, so the property is kept 1:1 and the port needs a UI5 runtime >= 1.79.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarFiori2`                              class = `z2ui5_cl_smpc_app_378` path = `src/01/01/z2ui5_cl_smpc_app_378.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the original binds expanded="{device>/isNoPhone}" (a demo-kit helper model that abap2UI5 does not carry); expressed over the framework own device> model as the expression {=` &&
                 ` !${device>/system/phone} } - same truth value, different binding text (app 030 precedent, live-verified there on desktop and phone emulation).` )
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarInlineIcons`                         class = `z2ui5_cl_smpc_app_467` path = `src/01/01/z2ui5_cl_smpc_app_467.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The controller builds the IconTabFilters (and their content Texts) in a loop with addItem( ). abap2UI5 has no client-side control factory, so the port binds the items aggregation to a table` &&
                 ` holding the same 12 rows (Tab 1..12 / Content 1..12) and declares one IconTabFilter template with its content Text - structural-diff reports both as control extra because the original's view carries` &&
                 ` only the empty IconTabBar. // NOTE: onInit gives every tab a RANDOM icon out of three (history, home, employee) with Math.random. A backend cannot repeat a client-side random draw, so the port walks` &&
                 ` the same three icons in order (index MOD 3) - same three icons over the same twelve tabs, deterministic instead of random.` )
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarInlineMode`                          class = `z2ui5_cl_smpc_app_379` path = `src/01/01/z2ui5_cl_smpc_app_379.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the original binds expanded="{device>/isNoPhone}" (a demo-kit helper model that abap2UI5 does not carry); expressed over the framework own device> model as the expression {=` &&
                 ` !${device>/system/phone} } - same truth value, different binding text (app 030 precedent, live-verified there on desktop and phone emulation).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarMulti`                               class = `z2ui5_cl_smpc_app_380` path = `src/01/01/z2ui5_cl_smpc_app_380.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the original binds expanded="{device>/isNoPhone}" (a demo-kit helper model that abap2UI5 does not carry); expressed over the framework own device> model as the expression {=` &&
                 ` !${device>/system/phone} } - same truth value, different binding text (app 030 precedent, live-verified there on desktop and phone emulation).` )
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarNoIcons`                             class = `z2ui5_cl_smpc_app_381` path = `src/01/01/z2ui5_cl_smpc_app_381.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the original binds expanded="{device>/isNoPhone}" (a demo-kit helper model that abap2UI5 does not carry); expressed over the framework own device> model as the expression {=` &&
                 ` !${device>/system/phone} } - same truth value, different binding text (app 030 precedent, live-verified there on desktop and phone emulation).` )
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarOverflowSelectList`                  class = `z2ui5_cl_smpc_app_465` path = `src/01/01/z2ui5_cl_smpc_app_465.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The controller builds the IconTabFilters (and their content Texts) in a loop with addItem( ). abap2UI5 has no client-side control factory, so the port binds the items aggregation to a table` &&
                 ` holding the same 30 rows (Tab 1..30 / Content 1..30) and declares one IconTabFilter template with its content Text - structural-diff reports both as control extra because the original's view carries` &&
                 ` only the empty IconTabBar.` ) ).

    text1 = `NOTE: this sample and apps 617 / 619 share one table and one filter idea; what it adds is the PROCESS look of the bar - three IconTabFilters with design='Horizontal', an icon-carrying IconTabSeparator` &&
            ` between each pair (sap-icon://open-command-field) and no All tab. All of that is carried 1:1. // NOTE: the three counts are composed bindings in the sample - '{Ok} of {Total}' and so on - so the port` &&
            ` composes the same three strings from /ProductCollectionStats/Counts (53 of 123, 51 of 123, 19 of 123) and binds one field per tab. // NOTE: Thin frontend: onFilterSelect OR-s a KG filter with a G` &&
            ` one, each AND-ed with its own bounds (1 / 5 kg against 1000 / 5000 g). Comparing in kilograms - grams divided by 1000 - is the same partition, and the port filters the ABAP table it sends (app 298` &&
            ` idiom). Note the sample's own Ok bound is LT, not LE. // NOTE: the Weight cell's state is a formatter over the row's own measure and unit (Formatter.weightState: under 1 kg Success, under 5 kg` &&
            ` Warning, else Error, and None for a non-numeric or negative measure, with grams divided by 1000). A thin frontend computes it in the backend, so weight_state is a field of the seeded row - and it is`.
    text1 = text1 && ` seeded on EVERY row, since an empty string in a ValueState terminates the app (the app 566 lesson). // NOTE: the Price cell is a composite currency binding in the sample (parts Price + CurrencyCode` &&
            ` through sap.ui.model.type.Currency with showMeasure false). The port binds the two fields directly on the ObjectNumber's number and unit, which is what the formatter renders, since the mock's prices` &&
            ` are plain numbers. // NOTE: the four tab counts come from /ProductCollectionStats/Counts (123 total, 53 Ok, 51 Heavy, 19 Overweight) and are carried verbatim - they are the mock's own numbers and do` &&
            ` NOT match what the filters actually select (the port's own weightState classification yields 57/45/21), which is true upstream too. // NOTE: the items binding carries a sorter on Name; a thin` &&
            ` frontend sorts the data it sends, so the 123 rows are seeded verbatim in the mock's order and SORTed by name in model_init - never through SORT BY (field), which the transpiled backend drops (the` &&
            ` 2026-08-22 e2e finding). // NOTE: not yet verified in a running system: that each of the three process tabs filters the table to its weight range. **e2e-verified 2026-08-25** (nightly e2e`.
    text1 = text1 && ` interaction, meta/interactions/z2ui5_cl_smpc_app_618.mjs). // NOTE: The price cell keeps the original's composite sap.ui.model.type.Currency binding rather than binding the raw field. The type is not` &&
            ` a pass-through: NumberFormat's currency instance applies grouping and the currency's own fraction digits, so EUR renders 956.00 / 1,249.00 where the bare field renders 956 / 1249. The earlier` &&
            ` justification - that the fold is what the formatter renders because the mock's prices are plain numbers - was wrong on both halves.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarProcess`                             class = `z2ui5_cl_smpc_app_618` path = `src/01/01/z2ui5_cl_smpc_app_618.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: this sample and apps 617 / 618 share one table and one filter idea; what it adds is the responsive padding - the bar carries class='sapUiResponsivePadding--header` &&
            ` sapUiResponsivePadding--content' - over the same All-plus-three tab set app 617 shows. All of that is carried 1:1. // NOTE: the four tab counts come from /ProductCollectionStats/Counts (123 total, 53` &&
            ` Ok, 51 Heavy, 19 Overweight) and are carried verbatim - they are the mock's own numbers and do NOT match what the filters actually select, which is true upstream too. // NOTE: Thin frontend:` &&
            ` onFilterSelect OR-s a KG filter with a G one, each AND-ed with its own bounds (1 / 5 kg against 1000 / 5000 g). Comparing in kilograms - grams divided by 1000 - is the same partition, and the port` &&
            ` filters the ABAP table it sends (app 298 idiom). Note the sample's own Ok bound is LT, not LE. // NOTE: the Weight cell's state is a formatter over the row's own measure and unit` &&
            ` (Formatter.weightState: under 1 kg Success, under 5 kg Warning, else Error, and None for a non-numeric or negative measure, with grams divided by 1000). A thin frontend computes it in the backend, so`.
    text1 = text1 && ` weight_state is a field of the seeded row - and it is seeded on EVERY row, since an empty string in a ValueState terminates the app (the app 566 lesson). // NOTE: the Price cell is a composite` &&
            ` currency binding in the sample (parts Price + CurrencyCode through sap.ui.model.type.Currency with showMeasure false). The port binds the two fields directly on the ObjectNumber's number and unit,` &&
            ` which is what the formatter renders, since the mock's prices are plain numbers. // NOTE: the items binding carries a sorter on Name; a thin frontend sorts the data it sends, so the 123 rows are` &&
            ` seeded verbatim in the mock's order and SORTed by name in model_init - never through SORT BY (field), which the transpiled backend drops (the 2026-08-22 e2e finding). // NOTE: not yet verified in a` &&
            ` running system: that the All tab puts every row back and that each of the three weight tabs filters the table to its range. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_619.mjs). Corrected 2026-08-26: this used to claim the All tab "shows all 123 rows", which the port does not do and the original does not either - a JSONModel's`.
    text1 = text1 && ` default size limit is 100 and neither the sample nor the port raises it (app 613 shows what raising it looks like), so the unfiltered table renders 100 of the 123 seeded rows. That is exactly what` &&
            ` the interaction module states and asserts, so the sidecar contradicted its own e2e evidence; siblings 617/618 word the same check without a row count. // NOTE: The price cell keeps the original's` &&
            ` composite sap.ui.model.type.Currency binding rather than binding the raw field. The type is not a pass-through: NumberFormat's currency instance applies grouping and the currency's own fraction` &&
            ` digits, so EUR renders 956.00 / 1,249.00 where the bare field renders 956 / 1249. The earlier justification - that the fold is what the formatter renders because the mock's prices are plain numbers -` &&
            ` was wrong on both halves.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarResponsivePadding`                   class = `z2ui5_cl_smpc_app_619` path = `src/01/01/z2ui5_cl_smpc_app_619.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarStartAndEndOverflow`                 class = `z2ui5_cl_smpc_app_466` path = `src/02/01/z2ui5_cl_smpc_app_466.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.IconTabBar.tabsOverflowMode is @since 1.90 and is the subject of this sample (StartAndEnd), so it is kept 1:1 and the port needs a UI5 runtime >= 1.90. // NOTE: The controller builds` &&
                 ` the IconTabFilters (and their content Texts) in a loop with addItem( ). abap2UI5 has no client-side control factory, so the port binds the items aggregation to a table holding the same 50 rows (Tab` &&
                 ` 1..50 / Content 1..50) and declares one IconTabFilter template with its content Text - structural-diff reports both as control extra because the original's view carries only the empty IconTabBar.`
        post171 = `sap.m.IconTabBar.tabsOverflowMode is @since 1.90 and is the subject of this sample (StartAndEnd), so it is kept 1:1 and the port needs a UI5 runtime >= 1.90.` )
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarStretchContent`                      class = `z2ui5_cl_smpc_app_030` path = `src/01/01/z2ui5_cl_smpc_app_030.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed); incl. the phone-emulation device> check. **e2e-verified` &&
                 ` 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_030.mjs: the IconTabBar resolves the device> expression to expanded on desktop, the bound Products list renders with a counter per` &&
                 ` row, and clicking the Attachments filter shows 'Attachments go here ...' with selectedKey attachments)`
        notes = `NOTE: the original binds expanded="{device>/isNoPhone}" (a demo-kit helper model); expressed over the framework's device> model as the expression {= !${device>/system/phone} } - same truth value,` &&
                 ` different binding text. Confirmed on desktop and phone emulation in the 2026-07-20 live check.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarSubTabs`                             class = `z2ui5_cl_smpc_app_382` path = `src/02/01/z2ui5_cl_smpc_app_382.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: the sap.m.IconTabFilter items aggregation (nested sub tabs) is @since 1.77, newer than the 1.71 floor, but it IS the sample - both IconTabBars exist to show sub tabs. Kept for the 1:1 port;` &&
                 ` the app needs a UI5 release >= 1.77.`
        post171 = `the sap.m.IconTabFilter items aggregation (nested sub tabs) is @since 1.77, newer than the 1.71 floor, but it IS the sample - both IconTabBars exist to show sub tabs. Kept for the 1:1 port; the app` &&
                 ` needs a UI5 release >= 1.77.` ) ).

    text1 = `NOTE: onTabDensityModeSelect reads the picked RadioButton's TEXT and calls setTabDensityMode on byId('idIconTabBar' + i) for i = 0..7 - eight of the nine bars. tabDensityMode is bindable, so the port` &&
            ` binds the group's selectedIndex two-way and one round trip (event DENSITY) turns the index into the enum all eight bars bind, the same fold apps 604 and 617 use. The ninth bar, iconTabBarInlineIcons,` &&
            ` is NOT in the sample's loop and is therefore left unbound here too. // NOTE: the bound enum carries the null fallback ({= ${X} || null }, apps 548/555 idiom) and is seeded 'Cozy' from the group's own` &&
            ` first button, so no empty string ever reaches sap.m.IconTabDensityMode. // NOTE: onInit fills two of the nine bars in JavaScript: thirty IconTabFilters on idIconTabBar0 ('Tab n' with a 'Content n'` &&
            ` Text) and twelve on iconTabBarInlineIcons. Both aggregations are bindable, so the port binds a table for each and builds the rows in model_init - which is why the port shows 29 IconTabFilters and 24` &&
            ` Texts against the view's 27 and 22: the two bound TEMPLATES are counted once each on top of the seven bars written out literally. // IMPROVISED: the twelve inline tabs get their icon from`.
    text1 = text1 && ` aIcons[Math.floor(Math.random() * 3)] - a fresh draw per tab on every load. A backend cannot draw the browser's numbers, and a random seed would make the port unrepeatable for the gates, so the three` &&
            ` icons (history, home, employee) CYCLE instead. Every tab still carries one of the three, which is what the sample demonstrates; only the sequence is stable. // NOTE: the other seven bars are written` &&
            ` out literally, with everything the sample sets on them: enableTabReordering on idIconTabBar0, headerMode='Inline' on idIconTabBar6, the four icon-only filters of idIconTabBar4, the four iconColor` &&
            ` values and three icon-carrying IconTabSeparators of idIconTabBar1 (one of which carries icon="" - an empty icon the sample writes and the port keeps), the Horizontal process design of idIconTabBar2` &&
            ` and the showAll tab of idIconTabBar5. // NOTE: not yet verified in a running system: that the three density buttons resize the eight bars and leave the ninth alone. **e2e-verified 2026-08-25**` &&
            ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_620.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabBarTabDensityMode`                      class = `z2ui5_cl_smpc_app_620` path = `src/01/01/z2ui5_cl_smpc_app_620.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.IconTabBar`                      name = `IconTabSeparator`                              class = `z2ui5_cl_smpc_app_383` path = `src/01/01/z2ui5_cl_smpc_app_383.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.IconTabHeader`                   name = `IconTabHeader`                                 class = `z2ui5_cl_smpc_app_055` path = `src/01/01/z2ui5_cl_smpc_app_055.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.15` ) ).

    text1 = `POST-1.71: ariaDetails on sap.m.Image (since UI5 1.79) is newer than 1.71 but kept for the 1:1 port - it links the active-state Image to its explaining Text. The app needs a UI5 release >= 1.79 for` &&
            ` the association to take effect. // NOTE: the controller resolves imageWidth once at init from Device.system.phone (5em on phone, 10em otherwise). The port keeps it a live branch over the framework's` &&
            ` device> model as the expression {= ${device>/system/phone} ? '5em' : '10em' } instead of resolving it to one value (app 031/046 precedent) - a device BRANCH stays a branch. // NOTE: the` &&
            ` img>/products/pic1 and pic3 values and the sample's own images/sap-logo.svg are inlined as literals and host-absolutized to https://sdk.openui5.org/ - abap2UI5 serves one default model and the demo` &&
            ` kit's named img model is not carried. // NOTE: handleImage3Press is MessageToast.show('The image has been pressed') with a CONSTANT text, so the press wire is the roundtrip-free client toast` &&
            ` (follow_up_action cs_event-control_global MESSAGE_TOAST.show, app 005 idiom) and the app stays init-only. // POST-1.71: sap.m.ImageMode.InlineSvg is kept 1:1 from the original view and is newer than`.
    text1 = text1 && ` UI5 1.71. Another enum VALUE in the property gate's blind spot (AGENTS section 5): the member carries no @since in its JSDoc - only a CORS note - so neither the linter nor ui5/properties.json's` &&
            ` enumSince has anything to match, and mode itself is an old property. Measured against the sources rather than inferred: at tag 1.71.0 sap.m.ImageMode has exactly Image and Background, and InlineSvg` &&
            ` first appears in 1.106. On an older runtime validateProperty rejects it. The class already sat in src/02/01 for Image.ariaDetails @1.79. // NOTE: The sample's own sap-logo.svg is re-hosted at the` &&
            ` demo kit URL, and the path needs the demokit/ segment that sap.ui.require.toUrl( 'sap/m/sample/Image/images/sap-logo.svg' ) resolves to - the file lives at` &&
            ` src/sap.m/test/sap/m/demokit/sample/Image/images/. It was missing until 2026-08-21, so both SVG Images fetched a 404. Nothing catches that: scripts/e2e-smoke.mjs resolves only /resources/ paths` &&
            ` locally and passes every other miss over silently. Same construct and same fix as app 288's sample.pdf. Note the InlineSvg image fetches its source by XHR (Image.js _loadSvg -> jQuery.get), so on a`.
    text1 = text1 && ` real system that cross-origin URL is subject to exactly the CORS restriction the enum's own documentation warns about; the plain-mode Image beside it is unaffected.`.
    text2 = `ariaDetails on sap.m.Image (since UI5 1.79) is newer than 1.71 but kept for the 1:1 port - it links the active-state Image to its explaining Text. The app needs a UI5 release >= 1.79 for the` &&
            ` association to take effect. // sap.m.ImageMode.InlineSvg is kept 1:1 from the original view and is newer than UI5 1.71. Another enum VALUE in the property gate's blind spot (AGENTS section 5): the` &&
            ` member carries no @since in its JSDoc - only a CORS note - so neither the linter nor ui5/properties.json's enumSince has anything to match, and mode itself is an old property. Measured against the` &&
            ` sources rather than inferred: at tag 1.71.0 sap.m.ImageMode has exactly Image and Background, and InlineSvg first appears in 1.106. On an older runtime validateProperty rejects it. The class already` &&
            ` sat in src/02/01 for Image.ariaDetails @1.79.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Image`                           name = `Image`                                         class = `z2ui5_cl_smpc_app_399` path = `src/02/01/z2ui5_cl_smpc_app_399.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `POST-1.71: sap.m.IllustratedMessage (control since UI5 1.98, with its description / title / illustrationType members) is newer than 1.71 but kept for the 1:1 port - it is the sample's whole point, the` &&
            ` error fallback. Corrected 2026-08-23: this used to claim "no gate sees it (the control is not in ui5/properties.json), so this entry is declared by policy", and both halves were false.` &&
            ` sap.m.IllustratedMessage IS in ui5/properties.json at since 1.98, with description/title/illustrationType carrying the same version, and the linter reports control-too-new plus three member-too-new` &&
            ` findings on this port. Since view-gates lets only a POST_171/DROPPED_171 excuse a VERSION finding, this declaration is not a formality at all - it is the load-bearing entry that keeps the port green;` &&
            ` the app needs a UI5 release >= 1.98. // NOTE: the controller resolves Device.system.phone once in onInit into the model fields imageHeight / imageWidth (5em on a phone, 10em otherwise). The port` &&
            ` binds height and width to the same branch as an expression over the shared device model instead of seeding one resolved value, so both branches survive - the model layer differs, the rendered size`.
    text1 = text1 && ` does not. // NOTE: the named img> model ({img>/products/pic1} from sap/ui/demo/mock/img.json) folds onto the one default model as the bound field IMAGE_SRC, seeded with the mock's own` &&
            ` HT-7777-large.jpg path absolutized to the sdk.openui5.org host. It stays a bound field because the sample's Set-wrong-src button overwrites it at runtime (with /some/random/url, as in the original).` &&
            ` // NOTE: live-verified on 2026-08-02 (maintainer live check): the initial load leaves HASERROR false and Set-wrong-src fires error, hides the Image and reveals the IllustratedMessage. The error->swap` &&
            ` half is additionally e2e-verified (scripts/e2e-smoke.mjs interaction); the load leg cannot be checked headless (the seeded image sits on sdk.openui5.org).`.
    text2 = `sap.m.IllustratedMessage (control since UI5 1.98, with its description / title / illustrationType members) is newer than 1.71 but kept for the 1:1 port - it is the sample's whole point, the error` &&
            ` fallback. Corrected 2026-08-23: this used to claim "no gate sees it (the control is not in ui5/properties.json), so this entry is declared by policy", and both halves were false.` &&
            ` sap.m.IllustratedMessage IS in ui5/properties.json at since 1.98, with description/title/illustrationType carrying the same version, and the linter reports control-too-new plus three member-too-new` &&
            ` findings on this port. Since view-gates lets only a POST_171/DROPPED_171 excuse a VERSION finding, this declaration is not a formality at all - it is the load-bearing entry that keeps the port green;` &&
            ` the app needs a UI5 release >= 1.98.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Image`                           name = `ImageErrorWithIllustration`                    class = `z2ui5_cl_smpc_app_279` path = `src/02/01/z2ui5_cl_smpc_app_279.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: the original binds src/mode to a JSONModel (img>/products, /imageMode); these fixed sample values are inlined here as literals (mode Background, the HT-7777 / HT-6100 demo images). height/width` &&
            ` are restored over the device> model, see below. // NOTE: the custom CSS class imageContainer (light blue background) of the box4 HBox is kept and the sample's styles.css injected via a core:HTML` &&
            ` content attribute (CAPABILITIES.md CSS row, as apps 028/026; the EXTRA core:HTML control vs the original view). Confirmed rendering via the human visual pass 2026-07-19. // NOTE: The sample's asset` &&
            ` paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/...` &&
            ` instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it',` &&
            ` which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds`.
    text1 = text1 && ` again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Image`                           name = `ImageModeBackground`                           class = `z2ui5_cl_smpc_app_031` path = `src/01/01/z2ui5_cl_smpc_app_031.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed); incl. the phone-emulation device> check. **e2e-verified` &&
                 ` 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_031.mjs: the four Background-mode Images resolve the device> expression to 10em and the injected .imageContainer background` &&
                 ` rgb(169, 234, 255) applies)`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ImageContent`                    name = `ImageContent`                                  class = `z2ui5_cl_smpc_app_056` path = `src/01/01/z2ui5_cl_smpc_app_056.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.38`
        notes = `NOTE: the profile-image and logo ImageContent keep the sample's original demokit test-resources asset paths (test-resources/sap/m/demokit/sample/ImageContent/images/*.png) as the src literal 1:1;` &&
                 ` abap2UI5 does not serve those static assets, so only the first (sap-icon://area-chart) icon renders offline. The images are archived under ui5/sap.m/ImageContent/images/.` ) ).

    text1 = `NOTE: The five core:Fragment references of App.view.xml (Input in the heading, HeaderContent in headerContent, and IllustratedMessage/ProductsTable/SupplierDetails in sections) are inlined 1:1 into` &&
            ` the single port view - abap2UI5 builds one XML view, there is no runtime Fragment reference. So core:Fragment is absent (5 dropped) and every fragment's content is rendered directly. // NOTE: The` &&
            ` dynamic /selectedPurchase JSON object is flattened onto the single default model as root-level fields: has_selection (drives the ObjectPageLayout showHeaderContent/toggleHeaderOnTitleClick and every` &&
            ` section visible expression, replacing the object-truthiness !!${/selectedPurchase}), sel_category/sel_subcategory/sel_suppliername/sel_paymenttype/sel_deliverystatus (HeaderContent),` &&
            ` sel_delivery_state (ObjectStatus) and sel_products (the products Table). The controller's _setSelectedPurchaseAndUpdateInput selection logic is reproduced server-side in set_selection: the` &&
            ` entered/selected PurchaseID resolves a purchase from t_purchases and seeds these fields (or forces has_selection=false so the IllustratedMessage "not found" state shows). The text and Sub-Category`.
    text1 = text1 && ` header bindings therefore bind the flattened fields instead of {/selectedPurchase/...}. // IMPROVISED: i18n named-model bindings folded to literal English values from i18n.properties: the pure` &&
            ` {i18n>...} property bindings on text (Title Receipt Information/Delivery Status/Products, column-header Product/Dimensions/Quantity/Price), title (SelectDialog Purchases) and placeholder (Input Enter` &&
            ` product) are resolved statically (loses runtime language switching, renders identically in English). The IllustratedMessage title/description/illustrationType keep their expression binding {=` &&
            ` ${/inputPopulated} ? ... : ... } with the i18n operands replaced by the literal strings. The Label ...:'-suffixed i18n texts are likewise inlined. // NOTE: formatter.deliveryStatusState` &&
            ` (Shipped->Success, Failed Shipping->Error, else None) is business logic; per the thin-frontend principle it moves out of the frontend formatter into the ABAP model (deliverystatus_state, computed in` &&
            ` model_init per purchase) and the ObjectStatus state binds that precomputed field (sel_delivery_state) directly instead of a formatter binding. // NOTE: handleValueHelpSearch / _getCombinedFilter`.
    text1 = text1 && ` build an OR filter of PurchaseID Contains + SupplierName Contains, and _filterAndOpenValueHelpDialog opens the dialog pre-filtered with the current input value. **Both reproduced - the pre-filter` &&
            ` only since 2026-08-23** against capabilities that had shipped and were unused: the search round-trips its value and the backend issues the COMPOUND binding_call filter (a JSON group with both fields,` &&
            ` ORed inside the group - pr/binding-call-compound-filters; the payload is one JSON string, so the value cannot be substituted client-side, which is why this one wire round-trips like app 022's -` &&
            ` re-verified 2026-08-23: get_t_arg emits an argument raw only when it starts with $ or { (or is an .eB/.eBP/.eF expression), and the compound payload starts with [, so it is quoted as a JS string and` &&
            ` any ${...} inside it stays literal text), and valueHelpRequest now passes $event.oSource.getValue() to open( ), the search-string argument the whitelist already declared. The Input's` &&
            ` setFilterFunction (case-insensitive contains over key+text) stays unreproduced - it is UI5's built-in suggestion filtering. Until then only the open( sInputValue ) half was: SelectDialog.open does`.
    text1 = text1 && ` NOT filter, it just seeds its search field, so the dialog listed everything while the search box showed the typed text. The valueHelpRequest wire now chains the same compound binding_call filter the` &&
            ` search handler uses - built from $event.oSource.getValue( ) inside the argument expression - before the open, which is what _filterAndOpenValueHelpDialog does. // NOTE: Namespace-prefix` &&
            ` representation: the port declares xmlns=sap.m (default) plus xmlns:uxap=sap.uxap, so the uxap controls are emitted uxap:-prefixed. In the originals ObjectPageLayout/ObjectPageDynamicHeaderTitle` &&
            ` (App.view.xml) and the IllustratedMessage-fragment ObjectPageSection/ObjectPageSubSection are unprefixed (their file's default xmlns is sap.uxap), while the ProductsTable/SupplierDetails fragments` &&
            ` already use the uxap: prefix. Same library, identical rendering; structural-diff sees uxap:ObjectPageSection vs ObjectPageSection (and likewise` &&
            ` ObjectPageLayout/ObjectPageDynamicHeaderTitle/ObjectPageSubSection) as control missing/extra. m:IllustratedMessage keeps its m: prefix to match the original. // POST-1.71: sap.m.IllustratedMessage`.
    text1 = text1 && ` (control since UI5 1.98, with illustrationType/title/description) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.98 to render it. property-check does not track` &&
            ` IllustratedMessage members, so this is declared by policy, not gate-forced. // NOTE: sap.m.Input.autocomplete is kept 1:1 from the original (autocomplete="false"). It was declared POST_171 here as` &&
            ` '@since 1.108' until 2026-08-23 - the sources say otherwise: Input.js carries @since 1.61 on that property, below the 1.71 floor, so nothing is owed and the stated minimum runtime was wrong. The port` &&
            ` stays under src/02 for the IllustratedMessage declaration, which is genuine. // NOTE: leg (c) is closed: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real` &&
            ` browser): F4 on the PurchaseID Input (the keyboard form of valueHelpRequest - its icon has no layout box headless) opens the SelectDialog client-side through control_by_id, and the dialog renders its` &&
            ` bound purchase rows. Still unverified in a running system: (a) Input submit resolving the entered PurchaseID and redrawing; (b) suggestionItemSelected transporting`.
    text1 = text1 && ` ${$parameters>/selectedItem}.getKey(); (d) the SelectDialog search (binding_call filter) and its confirm arg - the dialog row has no layout box headless and neither a click nor a keyboard Enter` &&
            ` reaches the confirm; (e) the ObjectPageLayout/IllustratedMessage/Table rendering. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_233.mjs). // NOTE: Two more` &&
            ` differences from the original controller, both corrected 2026-08-23. (a) An EMPTY submit set has_selection = false and collapsed the whole ObjectPage back to the IllustratedMessage; handleInputSubmit` &&
            ` only writes /inputPopulated and leaves the selection alone. (b) The input showed the bare key after a selection, because the port writes the field and re-renders. The original resolves the purchase` &&
            ` through Input.setSelectedKey, and with textFormatMode="KeyValue" the field then reads "(<key>) <text>" - the port composes exactly that string now. Note the original's own follow-on quirk comes with` &&
            ` it: submitting that composed text matches no PurchaseID, so the not-found state shows, on both sides. // NOTE: JSON-escaping of the compound filter payload, 2026-08-26. VH_SEARCH splices the`.
    text1 = text1 && ` SelectDialog search term straight into the compound-groups JSON string, and the term is free text: the search wire carries ${$parameters>/value} from the dialog's SearchField. The earlier note here` &&
            ` is right that get_t_arg quotes a [-leading argument as a JS string literal instead of emitting it raw - but that only escapes the JS layer (escape_js_string handles \\ and ' and CR/LF), never the` &&
            ` JSON layer, and this payload travels the queued follow_up_action path anyway, where get_event_client_ajson tries z2ui5_cl_ajson=>parse and silently falls back to a plain string when the JSON is` &&
            ` malformed. Typing a double quote - '15"' against mock descriptions that literally read '15" LCD' - therefore produced [[["PURCHASEID","Contains","15""],...]], buildFilterGroups' JSON.parse threw, and` &&
            ` it does Lib.logError + return WITHOUT calling binding.filter([]) - so the filter was neither applied nor cleared and the previous one silently stayed on. A backslash is worse and fully silent: \b` &&
            ` parses as a backspace character (filter applies, matches nothing, no log line) and \z or a trailing \ throws. Fixed by the app 218/420 precedent - REPLACE ALL OCCURRENCES of backslash then of the`.
    text1 = text1 && ` double quote, backslash first so the escapes added afterwards are not re-escaped. // NOTE: KNOWN, UNFIXED (2026-08-26): the valueHelpRequest leg composes the same compound-groups JSON CLIENT-SIDE and` &&
            ` cannot be escaped in ABAP. Its t_arg is a raw $-leading UI5 expression that concatenates $event.oSource.getValue() into the JSON string literal three times, so a double quote in the Input's value` &&
            ` breaks the payload exactly as VH_SEARCH used to, with the same outcome: buildFilterGroups logs and returns, the pre-filter never lands and the dialog opens on whatever was filtered before. It is` &&
            ` reachable - type 15" into the PurchaseID Input, then F4. It is left as it is on purpose: the escaping would have to happen inside the UI5 expression grammar` &&
            ` (EventHandlerResolver/BindingParser.parseExpression), which has no regex literal and no verified replaceAll, and the only shape that is certainly safe - round-tripping the value to the backend the` &&
            ` way VH_SEARCH does - would give up the roundtrip-free pre-filter this leg exists to demonstrate and would be a behavioural rework of a reviewed port. Whoever picks this up owns an e2e run, not a`.
    text1 = text1 && ` source-only change. // IMPROVISED: The IllustratedMessage subsection drops the original fragment's sapUxAPObjectPageSubSectionFitContainer class. That class makes the subsection fill its container` &&
            ` and its contract is an ObjectPageLayout with a DEFINITE height; abap2UI5 hosts the view in a content-sized sap.m.NavContainer, so the whole chain (ObjectPageLayout <- the port's XMLView <- sapMNav)` &&
            ` is sized by its own content and there is no definite height to fit. The class then closes a positive feedback loop - the subsection fits the container, which grows the ObjectPageLayout, which grows` &&
            ` the container - measured 2026-08-27: a single window resize took the ObjectPageLayout 329px -> 19,249px -> 60,142px and then pegged the renderer, after which no Playwright wait on the view can` &&
            ` resolve. This is what failed app 233 in CI twice, both times as a layout-dependent assertion timing out while every wire was intact: the IllustratedMessage body-text scan on 2026-08-26, then the` &&
            ` PurchaseID input's visibility wait in bump-a2ui5 run 33087805313. Without the class the same layout holds at 329px across repeated resizes. The loss is cosmetic - the IllustratedMessage sits at its`.
    text1 = text1 && ` content height instead of centred in the remaining space - and the e2e interaction module now drives a resize and asserts the ObjectPageLayout stays bounded, so re-adding the class fails with its own` &&
            ` sentence.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.InitialPagePattern`              name = `InitialPagePattern`                            class = `z2ui5_cl_smpc_app_233` path = `src/02/01/z2ui5_cl_smpc_app_233.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        ui5_only = abap_true
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.IllustratedMessage (control since UI5 1.98, with illustrationType/title/description) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.98 to render it.` &&
                 ` property-check does not track IllustratedMessage members, so this is declared by policy, not gate-forced.` ) ).

    text1 = `NOTE: onValueHelpRequest loads ValueHelpDialog.fragment.xml, pre-filters its items by the Input's current value and opens it. The port builds the same SelectDialog with popup_display and applies the` &&
            ` same pre-filter through follow_up_action binding_call in the same round-trip; onValueHelpSearch and onValueHelpClose are the same two wires as in the fragment. // NOTE: The first Input's value is` &&
            ` two-way bound so the backend knows what to pre-filter by and can write the picked title back - the original reads and writes it imperatively. The second Input (autocomplete off) shares the same value` &&
            ` help, as in the original, but the write-back always targets the first Input, exactly as onValueHelpClose does. // NOTE: The value help, its pre-filter, the search and the value write-back are` &&
            ` unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_515.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo` &&
            ` kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the`.
    text1 = text1 && ` mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration. // NOTE: onInit raises` &&
            ` the model size limit to 100000 ("The default limit of the model is set to 100. We want to show all the entries."). The port issues cs_event-set_size_limit for MAIN; without it both Inputs' bound` &&
            ` suggestionItems would stop at 100 of the 123 products. The SelectDialog's own list grows by paging, so it does not need the popup half.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputAssisted`                                 class = `z2ui5_cl_smpc_app_515` path = `src/01/01/z2ui5_cl_smpc_app_515.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.Input.enableTableAutoPopinMode is @since 1.89 - newer than the 1.71 floor but kept for the 1:1 port, since the second of the three Inputs exists to show exactly that. The app needs a` &&
            ` UI5 release >= 1.89 for that one to behave differently from the first. // NOTE: the three Inputs are the sample: the same 123 suggestion rows and the same four columns, laid out three ways - default,` &&
            ` enableTableAutoPopinMode, and a custom popin configuration (popinDisplay='Inline' with minScreenWidth='Large' and demandPopin on the last two columns). Every column property is carried 1:1, including` &&
            ` the widths the second and third set. // NOTE: the Price cell is a composite currency binding in the sample (parts Price + CurrencyCode through sap.ui.model.type.Currency with showMeasure TRUE, so the` &&
            ` unit is part of the text). The port binds the two fields as one composite text ('{PRICE} {CURRENCYCODE}'), which is what that formatter renders, since the mock's prices are plain numbers. // NOTE:`.
    text1 = text1 && ` the sample has no handler at all - its controller only loads the mock - so the port raises no event either. Typing filters the suggestions in the BROWSER, over the rows the backend already sent. //` &&
            ` NOTE: The price cell keeps the original's composite sap.ui.model.type.Currency binding rather than binding the raw field. The type is not a pass-through: NumberFormat's currency instance applies` &&
            ` grouping and the currency's own fraction digits, so EUR renders 956.00 / 1,249.00 where the bare field renders 956 / 1249. The earlier justification - that the fold is what the formatter renders` &&
            ` because the mock's prices are plain numbers - was wrong on both halves.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputAssistedTabularSuggestions`               class = `z2ui5_cl_smpc_app_621` path = `src/02/01/z2ui5_cl_smpc_app_621.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Input.enableTableAutoPopinMode is @since 1.89 - newer than the 1.71 floor but kept for the 1:1 port, since the second of the three Inputs exists to show exactly that. The app needs a UI5 release` &&
                 ` >= 1.89 for that one to behave differently from the first.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputAssistedTwoValues`                        class = `z2ui5_cl_smpc_app_456` path = `src/01/01/z2ui5_cl_smpc_app_456.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: the Name input's binding carries a sap.ui.model.type.String with minLength 1 / maxLength 10 constraints, and UI5 checks those in the BROWSER on every parse. The port keeps the binding exactly as` &&
            ` written (path plus type plus constraints, the app 549 composite-binding idiom) so that half still works client-side, and the backend checks the same two bounds when it is asked to. // IMPROVISED: the` &&
            ` E-mail input's binding names '.customEMailType', a sap.ui.model.SimpleType the CONTROLLER defines, whose validateValue throws a ValidateException unless the value matches` &&
            ` /^\w+[\w-+\.]*\@\w+([-\.]\w+)*\.[a-zA-Z]{2,}$/. A model type is a JavaScript class - there is nothing to name from a backend - so the type reference is dropped and the same shape is checked in ABAP` &&
            ` instead: exactly one @ with something before it, a dot in the domain with at least two characters after it, and no spaces. The sample's own comment calls its regex demonstration-only, so the two` &&
            ` agree on every value it is meant to show. // NOTE: _validateInput reads the binding's type, calls validateValue and sets the input's valueState to Error or None. valueState IS a bindable property, so`.
    text1 = text1 && ` the port binds one field per input and writes it from the same check. The two valueStateTexts are carried literally, as the sample writes them. // NOTE: onInit registers both inputs with` &&
            ` sap.ui.core.Messaging (registerObject), which is what makes UI5 attach its own validation-error handling and paint the state on a failed parse. There is no counterpart for the registration itself;` &&
            ` what it produces - the Error state on an invalid value - the port writes explicitly. // NOTE: onSubmit's two outcomes are carried 1:1: a MessageToast on success and MessageBox.alert on failure` &&
            ` (message_box_display with the information type, which is what alert shows). // NOTE: not yet verified in a running system: that a name over ten characters paints the Error state and that Submit` &&
            ` distinguishes the two outcomes. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_622.mjs). // NOTE: The confirmation uses message_box_display( type = 'alert'` &&
            ` ). MessageBox.alert is not the information box - it renders icon NONE and the title "Alert" - and the frontend resolves the type by name on the MessageBox module, so 'alert' is the 1:1 form.`.
    text1 = text1 && ` 'information' painted a different icon and title.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputChecked`                                  class = `z2ui5_cl_smpc_app_622` path = `src/01/01/z2ui5_cl_smpc_app_622.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.Input.valueHelpIconSrc is @since 1.84 - the custom value-help icon IS this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.84. // NOTE: handleValueHelp loads` &&
            ` Dialog.fragment.xml and opens it; the port builds the same SelectDialog in its own chain and shows it with popup_display. The dialog gets an id so the search filter can address its items binding. //` &&
            ` NOTE: _handleValueHelpSearch filters the dialog's items by Name, reproduced 1:1 with follow_up_action binding_call; _handleValueHelpClose writes the picked title into the Input (the title travels as` &&
            ` an event arg) and closes the popup. // NOTE: The value-help dialog, its search filter and the value write-back are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_510.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no` &&
            ` document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it,`.
    text1 = text1 && ` one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputCustomValueHelpIcon`                      class = `z2ui5_cl_smpc_app_510` path = `src/02/01/z2ui5_cl_smpc_app_510.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Input.valueHelpIconSrc is @since 1.84 - the custom value-help icon IS this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.84.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputDescription`                              class = `z2ui5_cl_smpc_app_367` path = `src/02/01/z2ui5_cl_smpc_app_367.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: showClearIcon (since UI5 1.94) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.94 to render it. // POST-1.71: ariaDescribedBy (since UI5 1.90 on sap.m.Input)` &&
                 ` is newer than 1.71 but kept for the 1:1 port - it carries the sample's whole point, the InvisibleText referenced by aria-describedby.`
        post171 = `showClearIcon (since UI5 1.94) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.94 to render it. // ariaDescribedBy (since UI5 1.90 on sap.m.Input) is newer than 1.71` &&
                 ` but kept for the 1:1 port - it carries the sample's whole point, the InvisibleText referenced by aria-describedby.` )
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputGrouping`                                 class = `z2ui5_cl_smpc_app_507` path = `src/01/01/z2ui5_cl_smpc_app_507.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: onInit raises the model size limit to 100000 ("the default limit of the model is set to 100. We want to show all the entries."). Without it the bound suggestionItems and suggestionRows stop at` &&
                 ` 100 of the 123 products, and with the declared descending SupplierName sorter the rows cut are whole supplier groups - so the grouping this sample exists to show would render 9 of its 12 groups.` ) ).

    text1 = `NOTE: onValueHelpDialogClose reads the selected item's DESCRIPTION - which the fragment binds to {ProductId} - and writes it to BOTH setSelectedKey on the Input and setText on the indicator. The port` &&
            ` transports getDescription( ) and binds selectedKey rather than writing value: a property-binding update calls the control's own setSelectedKey (ManagedObjectBindingSupport), so` &&
            ` textFormatMode="KeyValue" renders ``(HT-1000) Notebook Basic 15`` from the key alone - which is what this sample is named after - and the state survives a view rebuild, which a control call would` &&
            ` not. One field drives the Input and the indicator, because the original always sets both to the same value. // NOTE: onInit raises the model size limit to 100000 because the JSONModel caps a bound` &&
            ` aggregation at 100 and the mock carries 123 products. The port issues cs_event-set_size_limit twice - once for MAIN (the Input's suggestionItems) and once for POPUP, because a popup slot keeps its` &&
            ` own model and its own limit. // NOTE: The KeyValue suggestions, the value help and the selected-key readout are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_521.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document` &&
            ` root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of` &&
            ` 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputKeyValue`                                 class = `z2ui5_cl_smpc_app_521` path = `src/01/01/z2ui5_cl_smpc_app_521.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        notes = text1 ) ).

    text1 = `IMPROVISED: The original sets a suggestionRowValidator, which is what makes Input.setSelectionRow build an item and route it through _getDisplayText - with textFormatMode="ValueKey" the field then` &&
            ` reads ``Notebook Basic 15 (HT-1000)``. Without the validator UI5 falls back to _DEFAULTRESULT_TABULAR (the first text cell), so the port's field reads ``Notebook Basic 15`` and the `` (HT-1000)``` &&
            ` suffix is lost. This is visible, not invisible: textFormatMode="ValueKey" is kept for fidelity with the original view but is dead in the port, because nothing here can reach _getDisplayText. The` &&
            ` selected-key readout Text below is still correct - it is fed from the transported cell. // NOTE: The tabular suggestions and the selected-key readout are unverified in a running system.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_503.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputKeyValueTabularSuggestions`               class = `z2ui5_cl_smpc_app_503` path = `src/01/01/z2ui5_cl_smpc_app_503.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputPassword`                                 class = `z2ui5_cl_smpc_app_368` path = `src/01/01/z2ui5_cl_smpc_app_368.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: the four Inputs are the sample: not editable, not enabled, editable, and editable with a value help. All four carry their type, placeholder, enabled and editable flags 1:1; only the fourth has` &&
            ` any behaviour. // NOTE: handleValueHelp loads Dialog.fragment.xml once (Fragment.load, kept as a view dependent) and opens it. The port expresses it as a core:FragmentDefinition shown through` &&
            ` client->popup_display and served per request, which is what lets the backend filter its list. The controller also remembers which input asked (this.inputId) so _handleValueHelpClose can write back` &&
            ` into it - here only one Input has a value help, so the picked title goes into its bound field directly. // NOTE: Thin frontend: _handleValueHelpSearch builds a Name Contains filter and calls filter()` &&
            ` on the dialog's items binding; the port filters the ABAP table it sends (app 298 idiom). Both close paths clear the filter for the next open, as the original does. // NOTE: the StandardListItem's` &&
            ` icon binds PICURL where the fragment writes {ProductPicUrl} - the same field under the ABAP component name, with the sanctioned host absolutization onto sdk.openui5.org. Its description binds`.
    text1 = text1 && ` {Productid} in the sample, a lower-case 'id' the mock does not carry (the field is ProductId), so upstream that line is empty; the port binds the field the mock really has.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputStates`                                   class = `z2ui5_cl_smpc_app_623` path = `src/01/01/z2ui5_cl_smpc_app_623.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputSuggestionsCustomFilter`                  class = `z2ui5_cl_smpc_app_460` path = `src/01/01/z2ui5_cl_smpc_app_460.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `IMPROVISED: onInit calls productInput.setFilterFunction( ) with a JavaScript callback - a case-insensitive 'string contains' filter on the item text, which is the sample's entire subject. An` &&
                 ` app-authored JS callback is the documented boundary and there is no bindable substitute here: the suggestion rows are core:Item, not core:ListItem, so filterSecondaryValues does not apply the way it` &&
                 ` does in the ComboBox ports 470/471. The port therefore ships UI5's default word-starts-with filtering and the contains-anywhere behaviour is lost.` ) ).

    text1 = `NOTE: onSuggest builds a StartsWith filter on Name from the typed term (or an empty filter list) and applies it to the suggestionItems BINDING. Reproduced 1:1 through follow_up_action binding_call on` &&
            ` that aggregation, so the model stays untouched (app 022 precedent). The suggest event round-trips per keystroke, which is what the sample is about; abap2UI5 serializes round-trips, and the wire` &&
            ` carries s_ctrl-check_queue_last (since 2026-09-19), so an event fired while one is in flight is kept - the last one - and the list ends on the typed term instead of on the last completed trip. The` &&
            ` wire carries s_ctrl-check_no_busy alongside it (abap2UI5 #2759, in the pin since the bump to ad0a2dd2): the round-trip and the busy STATE are unchanged, only the full-screen overlay stays down. That` &&
            ` is the second half a per-keystroke wire needs - check_queue_last stops the keystrokes from being lost, check_no_busy stops the overlay from flashing over the field while they are typed. It was the` &&
            ` visible half of the gap: the FIRST trip raises the indicator after the usual delay, but every keystroke landing on a trip already in flight raises it with NO delay, so from the second character on`.
    text1 = text1 && ` the original's round-trip-free typing was covered by an overlay the demo kit sample never shows. // NOTE: The suggest wire and its binding_call filter are unverified in a running system.` &&
            ` **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_473.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputSuggestionsDynamic`                       class = `z2ui5_cl_smpc_app_473` path = `src/01/01/z2ui5_cl_smpc_app_473.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: onSuggest asks an OpenSearchProvider that points at a local MockServer and replaces the Input's suggestion items with the answer. abap2UI5 has neither, so the same search runs in ABAP over the` &&
            ` product names and fills the bound suggestionItems aggregation - which also means the port declares one core:Item template the original's view does not have (structural-diff reports control extra` &&
            ` core:Item). // NOTE: The suggest round-trip and the backend search are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_509.mjs). // NOTE: The original never meets a size limit because it builds the suggestions imperatively (new Item + addSuggestionItem); a limit applies to a BOUND` &&
            ` aggregation only. The port binds them, so it raises the limit explicitly - a one-character term matches over 100 products (``e`` matches 106), which is exactly where the default cap of 100 would` &&
            ` start truncating.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputSuggestionsOpenSearch`                    class = `z2ui5_cl_smpc_app_509` path = `src/01/01/z2ui5_cl_smpc_app_509.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputTypes`                                    class = `z2ui5_cl_smpc_app_159` path = `src/01/01/z2ui5_cl_smpc_app_159.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: Five Inputs demonstrating the input types Text / Email / Tel / Number / Url with labelFor Labels, reproduced 1:1.` ) ).

    text1 = `POST-1.71: showClearIcon (since UI5 1.94) on three inputs is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.94 to render it. // POST-1.71: the two formattedValueStateText` &&
            ` aggregations (a FormattedText carrying Links, since UI5 1.78) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.78 to render them. // NOTE: the Links' press` &&
            ` (.onFormattedTextLinkPress) round-trips as event LINK_PRESS and shows the controller's toast text, docked at CenterCenter 1:1 (source-verified in Messages.js; how the two options travel is the` &&
            ` 2026-09 note below). The original's preventDefault is not needed: the Links carry no href, so there is no default navigation to suppress. // NOTE: The toast's my/at options moved from` &&
            ` client->message_toast_display( ) to the whitelisted global call on 2026-09: they are sap.m.MessageToast options, and the option object of follow_up_action( cs_event-control_global, MESSAGE_TOAST ) is` &&
            ` that API 1:1. Same wire, same showToast( ) on the client - the parameters simply left the ABAP signature, which now carries only what an ABAP app decides (text, duration, the backend event).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputValueState`                               class = `z2ui5_cl_smpc_app_032` path = `src/02/01/z2ui5_cl_smpc_app_032.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `showClearIcon (since UI5 1.94) on three inputs is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.94 to render it. // the two formattedValueStateText aggregations (a` &&
                 ` FormattedText carrying Links, since UI5 1.78) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.78 to render them.` ) ).

    text1 = `NOTE: onLiveChange calls this.byId('getValue').setText(newValue) on every keystroke. That is the one leg of this sample that cannot become a binding: the Text has to show what oInput.getValue()` &&
            ` returns even while valueLiveUpdate is off and the model value lags behind, which is exactly what the sample compares. The port wires it ROUND-TRIP-FREE since 2026-08-28 - ``follow_up_action(` &&
            ` control_by_id, getValue / setText / ${$parameters>/value} )`` on the Input's liveChange - so the Text is written on the client on every keystroke, the way the original's controller writes it, and the` &&
            ` port has no on_event and no backend event left. The Text carries no ``text`` of its own, exactly as ``<Text id="getValue"/>`` in the original. This replaces a real round-trip per keystroke (the value` &&
            ` travelled as ${$parameters>/value}), which was LOSSY: abap2UI5 serializes round-trips, so while one was in flight the next liveChange was DROPPED and under fast typing the Text showed the last` &&
            ` completed trip and caught up as soon as typing paused. The round-trip-free form was measured and rejected on 2026-08-23 for one reason: castArgAuto mapped an empty argument to the BOOLEAN false, and`.
    text1 = text1 && ` UI5 casts a non-string implicitly for a string-typed property ( + oValue in ManagedObject.validateProperty), so clearing the input rendered the literal word 'false' - the one keystroke this sample is` &&
            ` about. Filed as control-action-empty-string-arg in abap2UI5's backlog and CLOSED there on 2026-08-24: a setXxx whose property UI5 declares as ``string`` has its first argument passed through` &&
            ` untouched, so an empty argument reaches the control as "". The fix reached the reproducible builds when A2UI5_PIN moved on 2026-08-28, which is what unblocked the swap. // NOTE: /ValueLiveUpdate and` &&
            ` /InputValue are plain model fields in the original too; the port binds the same three fields two-way (Switch state, Input value/valueLiveUpdate, the second Text), so only the getValue Text needs the` &&
            ` wire. // NOTE: The per-keystroke liveChange wire and the valueLiveUpdate switch driving when the model value follows are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_462.mjs). **Rewritten 2026-08-28** for the round-trip-free wire: the module now types with no delay (there is no trip to drop an event any more) and`.
    text1 = text1 && ` asserts the empty-input case explicitly - the Text must go EMPTY, not read the literal word 'false', which is the defect the old wire existed to avoid.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputValueUpdate`                              class = `z2ui5_cl_smpc_app_462` path = `src/01/01/z2ui5_cl_smpc_app_462.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Input`                           name = `InputWrapping`                                 class = `z2ui5_cl_smpc_app_369` path = `src/01/01/z2ui5_cl_smpc_app_369.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.InputListItem`                   name = `InputListItem`                                 class = `z2ui5_cl_smpc_app_057` path = `src/01/01/z2ui5_cl_smpc_app_057.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `IMPROVISED: The original demonstrates late binding of an Input over an OData v2 mock server: fnRebind calls view.bindElement('/Employees(1)') and, on dataReceived (the mock server's autoRespondAfter` &&
            ` is 2000 ms, not the ~3s stated here until 2026-08-23 - the button label's "3 seconds" is the original's own inconsistency, and the port's timer uses 3000), rebinds the input to FirstName if the value` &&
            ` is still untouched. abap2UI5 has no ODataModel roundtrip here, so the OData mock is reduced to ABAP: the button starts a 3s timer (cs_event-start_timer) and the callback sets the bound value to the` &&
            ` Employees(1) FirstName ('Nancy') only when the input still equals its initial value ('Martin'), reproducing the initialValue===currentValue guard. // NOTE: The input value is a two-way binding on the` &&
            ` single default model (the original's named 'initialData' JSONModel /currentValue is flattened). liveChange is wired and the port does NOT reproduce the sample's guard - corrected 2026-08-23, the`.
    text1 = text1 && ` sentence used to claim the value "already flows back via the two-way binding on each roundtrip, so the handler is a no-op", and both halves are wrong. sap.m.Input writes its value binding in onChange` &&
            ` (focusout/Enter), not per keystroke, unless valueLiveUpdate is set - which is exactly why the original has a handleLiveChange at all - so the delta carries nothing and currentvalue still equals` &&
            ` initial_value. And the handler is worse than a no-op: every eB round-trip clears ALL pending timers before it runs, so a single keystroke CANCELS the armed REBIND_DONE and the late bind never happens` &&
            ` at all, where the original's dataReceived always fires and is merely declined by the guard. Carrying ${$parameters>/newValue} as the t_arg (or valueLiveUpdate) would make the guard real, but the` &&
            ` timer cancellation has to be solved with it (the original setProperty('/currentValue', ...) keeps it live between keystrokes).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.InputModelUpdate`                name = `InputModelUpdate`                              class = `z2ui5_cl_smpc_app_102` path = `src/01/01/z2ui5_cl_smpc_app_102.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        ui5_only = abap_true
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Label`                           name = `LabelProperties`                               class = `z2ui5_cl_smpc_app_058` path = `src/01/01/z2ui5_cl_smpc_app_058.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the four controller handlers (onDisplayOnlyChange/onWrappingChange/onHyphenationChange/onWidthChange) are replaced by a roundtrip-free binding model, so the Switch ``change`` and Slider` &&
                 ` ``liveChange`` event attributes are dropped: the three Switches bind their ``state`` two-way to display_only/wrapping/hyphenation, the two result Labels bind ``displayOnly`` and ``wrapping`` to the` &&
                 ` same variables, and ``wrappingType`` (added on the two Labels, not present in the sample view) plus both container ``width`` values derive live via ``{= }`` expression bindings (wrappingType =` &&
                 ` hyphenation ? 'Hyphenated' : 'Normal'; width = slider_value + '%'). Same technique as app 007.` )
      ( module = `sap.m`              control = `sap.m.LightBox`                        name = `LightBox`                                      class = `z2ui5_cl_smpc_app_059` path = `src/01/01/z2ui5_cl_smpc_app_059.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the Image src and LightBoxItem imageSrc keep the sample's original demokit sdk asset paths (test-resources/sap/ui/documentation/sdk/images/...) as literals 1:1; abap2UI5 does not serve those` &&
                 ` static assets, so the thumbnails/large images do not display offline. The LightBox still opens on image click (built-in Image.detailBox behaviour, no controller). The last item deliberately points` &&
                 ` imageSrc at a non-existent file to show the LightBox load-error state, exactly as in the sample. // NOTE: the six identical filler paragraphs use the same ABAP literal via a local ``lorem`` variable;` &&
                 ` XML attribute whitespace (newlines/tabs in the sample's indented multi-line text) is normalised to single spaces (visually identical).` ) ).

    text1 = `NOTE: handleLinkPress opens MessageBox.alert('Link was clicked!') in the original, and both wired Links now do exactly that: the press fires a LINK_PRESSED backend event and on_event calls` &&
            ` client->message_box_display( text = 'Link was clicked!' type = 'alert' ) - the framework's 1:1 form (CAPABILITIES marks sap.m.MessageBox expressible, app 036 is the reference). Until 2026-07-28 the` &&
            ` presses were reduced to a client-composed MessageToast 'Link pressed', which the sidecar itself had already flagged as a wrong improvisation (the app-042 lesson class): neither the control nor the` &&
            ` text matched the original. // POST-1.71: Link.icon (sap-icon://cart, sap-icon://globe) and Link.endIcon (sap-icon://inspect) are @since 1.128 and used 1:1 - the 'Links with Icons' group is the` &&
            ` sample's point. Newer than UI5 1.71; declared per the property-171 policy, so the app needs UI5 >= 1.128 to render the icons.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Link`                            name = `Link`                                          class = `z2ui5_cl_smpc_app_160` path = `src/02/01/z2ui5_cl_smpc_app_160.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        is_post171 = abap_true
        notes = text1
        post171 = `Link.icon (sap-icon://cart, sap-icon://globe) and Link.endIcon (sap-icon://inspect) are @since 1.128 and used 1:1 - the 'Links with Icons' group is the sample's point. Newer than UI5 1.71; declared` &&
                 ` per the property-171 policy, so the app needs UI5 >= 1.128 to render the icons.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Link`                            name = `LinkEmphasized`                                class = `z2ui5_cl_smpc_app_033` path = `src/01/01/z2ui5_cl_smpc_app_033.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        checked = `CHECKED (2026-07-19): verified in a running system - human visual pass 2026-07-19 over all apps: the Currency composite binding renders the formatted price per currency (raw binding-info string over` &&
                 ` PRICE TYPE p). **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_033.mjs: the emphasized Link renders in the product column, the Currency composite over the packed` &&
                 ` PRICE renders 956.00 and the sorted rows render)`
        notes = `NOTE: The 123 ProductPicUrl values are host-absolutized: ui5/mock/products.json carries the demo-kit-relative test-resources/sap/ui/documentation/sdk/images/..., which an abap2UI5 app has no document` &&
                 ` root to resolve against, so they are served from https://sdk.openui5.org/... The port binds that value live, as the original does. Declared 2026-08-21: the deviations array was EMPTY, which asserts a` &&
                 ` byte-for-byte data match this port does not have - data-fidelity tolerates the absolutization, so nothing else would have said so.` )
      ( module = `sap.m`              control = `sap.m.Link`                            name = `LinkSubtle`                                    class = `z2ui5_cl_smpc_app_446` path = `src/01/01/z2ui5_cl_smpc_app_446.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = `NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
                 ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim` &&
                 ` that every port doing it had a declaration.` ) ).

    text1 = `POST-1.71: sap.m.List.itemActionCount and the sap.m.ListItemAction control are both @since 1.137 - they are the subject of this sample, so they are kept 1:1 and the port needs a UI5 runtime >= 1.137.` &&
            ` Also sap.m.List.itemActionPress, @since 1.137 (ListBase.js:602) - the port wires it, and view-gates only excused it by an accidental substring match on the control name. // NOTE: onSliderChange calls` &&
            ` list.setItemActionCount(value). The Slider value and the List's itemActionCount are the same two-way bound field here, so the Slider.change attribute is dropped and the count follows without a` &&
            ` round-trip. // NOTE: onItemActionPress toasts '<action> action is pressed for the Product <name>' where the action is its text or, for the typed actions, its type. Both values travel with the event` &&
            ` (getText() || getType() and the item's title) and ABAP composes the same line. // NOTE: The slider-driven action count and the item-action toast are unverified in a running system. **e2e-verified` &&
            ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_498.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...),`.
    text1 = text1 && ` which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the` &&
            ` rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListActions`                                   class = `z2ui5_cl_smpc_app_498` path = `src/02/01/z2ui5_cl_smpc_app_498.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.List.itemActionCount and the sap.m.ListItemAction control are both @since 1.137 - they are the subject of this sample, so they are kept 1:1 and the port needs a UI5 runtime >= 1.137. Also` &&
                 ` sap.m.List.itemActionPress, @since 1.137 (ListBase.js:602) - the port wires it, and view-gates only excused it by an accidental substring match on the control name.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListCounter`                                   class = `z2ui5_cl_smpc_app_034` path = `src/02/01/z2ui5_cl_smpc_app_034.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-21): verified in a running system - human visual check 2026-07-21: the Products list renders all 123 rows with their Quantity counters (display-only app, no interaction to exercise).` &&
                 ` **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_034.mjs: the first rendered row's counter equals the QUANTITY its binding context carries, with a counter per row` &&
                 ` over the 100 rows the JSONModel sizeLimit yields)`
        notes = `POST-1.71: headerLevel="H2" on the List (since UI5 1.117) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.117 to render it.`
        post171 = `headerLevel="H2" on the List (since UI5 1.117) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.117 to render it.` ) ).

    text1 = `NOTE: handleDelete sends an OData remove for the row's binding path. The port deletes the row from the bound table (the ProductId travels as an event arg), which is the same disappearance; the focus` &&
            ` handling the original attaches to updateFinished has no equivalent and is dropped. // NOTE: The delete wire is unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_524.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document` &&
            ` root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of` &&
            ` 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListDeletion`                                  class = `z2ui5_cl_smpc_app_524` path = `src/01/01/z2ui5_cl_smpc_app_524.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The original controller loads the shared demo products.json and the List uses binding="{/ProductCollection/0}" to element-bind a single record; abap2UI5 serves one default model, so the record` &&
            ` is flattened to top-level default-model fields (name/productid/productpicurl = products.json row 0, Notebook Basic 15 / HT-1000) the relative {NAME}/{PRODUCTID}/{PRODUCTPICURL} bindings resolve` &&
            ` against. Same data, same leaf names, renders identically — the List's binding attribute is dropped. **Corrected 2026-08-01**: those bindings were written RELATIVE ({FIELD}) and a relative path on a` &&
            ` control with no binding context resolves against nothing — the fields rendered EMPTY in the running app (measured with the e2e harness, the app-207 class). They are now bound ABSOLUTELY through` &&
            ` client->_bind( field ), which is what a root-seeded record needs. // NOTE: ProductPicUrl is stored as the absolute https://sdk.openui5.org/test-resources/sap/ui/documentation/sdk/images/HT-1000.jpg` &&
            ` (the mock's host-relative test-resources path rewritten to the OpenUI5 host per the runtime asset-URL rule).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListFooter`                                    class = `z2ui5_cl_smpc_app_195` path = `src/01/01/z2ui5_cl_smpc_app_195.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `IMPROVISED: The items binding names a groupHeaderFactory ('.getGroupHeader') that returns a sap.m.GroupHeaderListItem built in JavaScript. A control-returning factory is a documented boundary of` &&
            ` abap2UI5 (CAPABILITIES), so the port keeps the grouping sorter 1:1 and lets UI5 render its DEFAULT group header - same grouping and the same group key text, but the header control the sample` &&
            ` constructs is not the port's. // NOTE: onToggleContextMenu creates a sap.m.Menu with two MenuItems in JavaScript on press and destroys it again. The port declares the same Menu in the List's` &&
            ` contextMenu aggregation and rebuilds the view on the toggle, so the subtree is emitted only while the button is pressed (app 436 precedent); structural-diff therefore reports the Menu and both` &&
            ` MenuItems as control extra. // NOTE: The context-menu toggle and the grouped list are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_492.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document`.
    text1 = text1 && ` root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of` &&
            ` 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListGrouping`                                  class = `z2ui5_cl_smpc_app_492` path = `src/01/01/z2ui5_cl_smpc_app_492.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: zero structural difference to the archived List.view.xml: growing / growingThreshold=4 / growingScrollToLoad=false are pure client-side paging over the bound aggregation, so the port needs no` &&
            ` wire for them at all and stays init-only. The whole collection travels to the client exactly as in the original, where the JSONModel also holds all 123 rows. // NOTE: the List binds the shared demo` &&
            ` mock products.json /ProductCollection (all 123 rows; Name, ProductId and ProductPicUrl - the three fields the StandardListItem binds) inlined into model_init. The mock's host-relative ProductPicUrl` &&
            ` ('test-resources/...') is resolved to an absolute sdk.openui5.org URL per the runtime asset-URL rule. // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend +` &&
            ` real browser): the list starts at exactly the growingThreshold of 4 rows and the More trigger appends the next page (8 rows) - client-side paging over the whole bound collection, with no backend wire` &&
            ` involved.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListGrowing`                                   class = `z2ui5_cl_smpc_app_276` path = `src/01/01/z2ui5_cl_smpc_app_276.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListGrowingUpwards`                            class = `z2ui5_cl_smpc_app_525` path = `src/01/01/z2ui5_cl_smpc_app_525.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: groupFunction returns the first letter of Name as the group key. A grouping callback has no backend equivalent, so the letter is computed per row in model_init and the binding groups by that` &&
                 ` field and then sorts by Name inside the group - same groups, same order. // NOTE: The MockServer's 2-second delay is not reproduced (the rows are bound directly); growingDirection, the growing` &&
                 ` trigger text and the busy settings are kept 1:1.` ) ).

    text1 = `NOTE: the original wires a change handler on the type Select (handleSelectChange, which loops the list items calling item.setType). selectedKey and every StandardListItem type share one two-way bound` &&
            ` field (LISTTYPE), so a selection re-types all items client-side and the Select.change attribute is deliberately MISSING vs the original view (AGENTS prefer-a-bindable-property rule; app 003` &&
            ` precedent). The added StandardListItem type binding is not in the original view. **Corrected 2026-07-31 (found by the e2e interaction):** the template first bound the shared field RELATIVELY` &&
            ` ({LISTTYPE}), which resolves against the ROW context - the rows carry no such field, so the items stayed Inactive and the Select had no effect at all. The template now binds the absolute path through` &&
            ` client->_bind( listtype ), which is what a shared root-level field needs inside a bound aggregation. // NOTE: the ProductPicUrl icon values are stored as absolute` &&
            ` https://sdk.openui5.org/test-resources/sap/ui/documentation/sdk/images/*.jpg URLs (the mock's host-relative test-resources paths rewritten to the OpenUI5 host per the runtime asset-URL rule). Same`.
    text1 = text1 && ` 123-row set, kept verbatim otherwise. // NOTE: unverified originally: (a) handlePress/handleDetailPress as roundtrip-free client toasts (control_global MESSAGE_TOAST.show of "'press' event fired!" /` &&
            ` "'detailPress' event fired!"); (b) the Select selectedKey <-> item type two-way binding re-typing all rows. **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real` &&
            ` browser): the list renders, every StandardListItem binds its type to the ABSOLUTE path /LISTTYPE (the regression that bit us - a relative {LISTTYPE} would resolve against the row and kill the` &&
            ` Select), AND the click-through now runs: opening the OverflowToolbar's 'Additional Options' popover makes the type Select drivable, and picking 'Navigation' re-types every bound row. The earlier` &&
            ` claim that the popover is not drivable headless was wrong (AGENTS 10, 2026-08-01). Still not covered: the press/detailPress toasts.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListItemTypes`                                 class = `z2ui5_cl_smpc_app_207` path = `src/01/01/z2ui5_cl_smpc_app_207.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The sample runs a MockServer with a 3-second delay behind an ODataModel; abap2UI5 binds the ABAP table directly, so the rows are there at once. The growing, busy-indicator and noDataText` &&
            ` settings are kept 1:1 - what is not reproduced is the artificial delay the sample uses to show the busy state, and refreshDataFromBackend re-reads the same rows from the backend on a round-trip. //` &&
            ` NOTE: The refresh wire is unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_522.mjs). // NOTE: The sample's asset paths are` &&
            ` host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The` &&
            ` values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListLoading`                                   class = `z2ui5_cl_smpc_app_522` path = `src/01/01/z2ui5_cl_smpc_app_522.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListNavType`                                   class = `z2ui5_cl_smpc_app_429` path = `src/01/01/z2ui5_cl_smpc_app_429.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
                 ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim` &&
                 ` that every port doing it had a declaration.` )
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListNoData`                                    class = `z2ui5_cl_smpc_app_035` path = `src/01/01/z2ui5_cl_smpc_app_035.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListSelection`                                 class = `z2ui5_cl_smpc_app_224` path = `src/01/01/z2ui5_cl_smpc_app_224.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The List mode and the Select selectedKey are two-way bound to one default-model field mode (seeded MultiSelect, a valid sap.m.ListMode), reproducing the original controller's` &&
                 ` handleSelectChange/setMode behaviour without a round-trip. The Select's change attribute is dropped (the two-way binding keeps List.mode in sync client-side). structural-diff does not flag the` &&
                 ` literal mode=MultiSelect / selectedKey=MultiSelect becoming bindings. // NOTE: The full /ProductCollection mock (123 rows) is inlined; the row type is restricted to the columns the StandardListItem` &&
                 ` binds (Name, ProductId, ProductPicUrl) - a column subset, not a row subset. ProductPicUrl relative asset paths are rehosted to the OpenUI5 host https://sdk.openui5.org/ per the asset-URL rule.` ) ).

    text1 = `NOTE: onSearch filters the list binding by Name with a Contains filter, reproduced 1:1 through follow_up_action binding_call on the items aggregation (the model stays untouched, app 022 precedent).` &&
            ` The search wire round-trips per keystroke, which is what liveChange asks for. // NOTE: onSelectionChange counts oList.getSelectedContexts(true) and drives the info toolbar's visibility and label. The` &&
            ` port binds the selection per row (StandardListItem.selected), counts the flagged rows in ABAP and writes the two bound properties - the same count across the current filter, since the flag lives on` &&
            ` the row rather than on the rendered item. // NOTE: The search filter and the selection counter are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_499.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document` &&
            ` root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of`.
    text1 = text1 && ` 17 found by re-counting the corpus-wide claim that every port doing it had a declaration. // NOTE: JSON-escaping of the compound filter payload, 2026-08-26. The SEARCH term is genuine free text - the` &&
            ` SearchField's liveChange carries ${$parameters>/newValue}, i.e. raw keystrokes - and it was spliced into the compound-groups JSON unescaped. get_t_arg quotes a [-leading argument as a JS string` &&
            ` (escape_js_string covers backslash, apostrophe and CR/LF) but nothing escapes the JSON layer, so a typed double quote made JSON.parse throw inside buildFilterGroups, which does Lib.logError + return` &&
            ` and never calls binding.filter([]) - the filter was neither applied nor cleared and the previous keystroke's filter stayed on the list. A backslash is the silent variant: \b parses as a backspace` &&
            ` character so the filter applies and matches nothing with no log line at all, while \z or a trailing backslash throws. Fixed by the app 218/420 precedent - REPLACE ALL OCCURRENCES of backslash then of` &&
            ` the double quote, backslash first. The empty-term reset is unaffected: escaping an empty string leaves it empty, so the IS INITIAL branch still emits [] and clears.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListSelectionSearch`                           class = `z2ui5_cl_smpc_app_499` path = `src/01/01/z2ui5_cl_smpc_app_499.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: handleSwipe rewrites the swipe button's text and type from the swipe direction and toasts the direction; the port transports ${$parameters>/swipeDirection} and writes the two bound Button` &&
            ` properties in ABAP, with the same two texts. // NOTE: handleReject removes the swiped item from the aggregation and calls swipeOut( ). The port deletes the row the swipe is on - the index travels` &&
            ` through indexOfItem(getSwipedItem()) on the List - so the bound aggregation loses the same entry; the explicit swipeOut( ) is implicit here, the re-rendered list closes the swipe. // NOTE: The swipe` &&
            ` wire (both directions) and the row removal are unverified in a running system - a swipe gesture is also outside what the e2e harness can drive. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_497.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document` &&
            ` root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of`.
    text1 = text1 && ` 17 found by re-counting the corpus-wide claim that every port doing it had a declaration. // NOTE: ListBase._onSwipe fires the swipe event synchronously BEFORE rendering the swipe content ("fire` &&
            ` event earlier to let the user change swipeContent according to list item"), so upstream the button's text and type are already correct the instant it appears. Here they follow a round-trip, so for` &&
            ` its duration the button still shows the previous direction's text - which is also why swipe_text/swipe_type are seeded rather than left initial (an empty string on the ButtonType-typed property would` &&
            ` need omit_initial_paths). The original's Button carries neither text nor type; both are added here to carry the bound values.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListSwipe`                                     class = `z2ui5_cl_smpc_app_497` path = `src/01/01/z2ui5_cl_smpc_app_497.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: onSelectionFinish maps the picked MultiComboBox keys onto list.setSticky( ). Both are the same two-way bound table here (selectedKeys and sticky), so the sticky options follow the selection` &&
            ` without a round-trip and the MultiComboBox.selectionFinish attribute is dropped. // NOTE: onToggleInfoToolbar hides the info toolbar while the button is pressed. The ToggleButton's pressed state is` &&
            ` two-way bound and the info toolbar's visible is an expression binding over the same field, so the ToggleButton.press attribute is dropped. // NOTE: handleButtonPress and handleInfobarPress toast` &&
            ` constant texts, composed on the client via control_global MESSAGE_TOAST - no press needs a round-trip. // NOTE: The sticky selection, the info-toolbar toggle and the four constant toasts are` &&
            ` unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_508.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo` &&
            ` kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the`.
    text1 = text1 && ` mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListToolbar`                                   class = `z2ui5_cl_smpc_app_508` path = `src/01/01/z2ui5_cl_smpc_app_508.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.List`                            name = `ListUnread`                                    class = `z2ui5_cl_smpc_app_480` path = `src/01/01/z2ui5_cl_smpc_app_480.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: Formatter.randomBoolean decides the unread flag per row with Math.random. A backend cannot repeat a client-side random draw, so unread is a plain model field seeded alternately (every second row` &&
                 ` unread) - same two states over the same 123 rows, deterministic instead of random. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...),` &&
                 ` which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the` &&
                 ` rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.` )
      ( module = `sap.m`              control = `sap.m.MaskInput`                       name = `MaskInput`                                     class = `z2ui5_cl_smpc_app_153` path = `src/02/01/z2ui5_cl_smpc_app_153.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34.0`
        is_post171 = abap_true
        notes = `NOTE: Six MaskInputs across two SimpleForms with mask/placeholderSymbol/placeholder and MaskInputRule (maskFormatSymbol/regex) sub-rules; the {/showClearIcon} bindings are wired to a boolean model` &&
                 ` field. The original sets no model at all, so its {/showClearIcon} binding stays unresolved and the property keeps its default false - the field is therefore seeded false (the earlier 'initial true'` &&
                 ` seed contradicted the sample). // POST-1.71: MaskInput.showClearIcon (@since 1.96) is used 1:1 - literal true on the phone-number MaskInput and the {/showClearIcon} binding on the three second-form` &&
                 ` MaskInputs. Newer than UI5 1.71; declared per the property-171 policy, so the app needs UI5 >= 1.96 to render the clear icons.`
        post171 = `MaskInput.showClearIcon (@since 1.96) is used 1:1 - literal true on the phone-number MaskInput and the {/showClearIcon} binding on the three second-form MaskInputs. Newer than UI5 1.71; declared per` &&
                 ` the property-171 policy, so the app needs UI5 >= 1.96 to render the clear icons.` ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it. // NOTE: the sample loads the Menu from` &&
            ` Menu.fragment.xml on first press and toggles it in the controller (oMenu.isOpen() ? close() : openBy(button)). The port declares the Menu 1:1 inside the Button's ``dependents`` aggregation and` &&
            ` toggles it via client->roundtrip-free follow_up_action( cs_event-control_by_id, toggleBy ) anchored to the button's DOM ref ($event.oSource.sId) - toggleBy (open-if-closed / close-if-open) was added` &&
            ` upstream 2026-07-20 (pr/menu-toggle-openby) precisely so the client-side open state need not be mirrored server-side, making the press-to-toggle 1:1. Frontend-action pattern as app 016. // NOTE:` &&
            ` onMenuAction builds a breadcrumb path by walking the selected MenuItem's parent chain (e.g. 'Create New Site > Official Store'); the port toasts only the selected item's own text, transported via` &&
            ` ${$parameters>/item}.getText(). **Measured 2026-07-31 (browser probe + e2e against the transpiled backend, OpenUI5 1.152), closing pr/menu-item-selected-path as a capability boundary:** sap.m.Menu`.
    text1 = text1 && ` re-parents its items through an internal sap.m.MenuWrapper, and the chain changes with the runtime state - while the submenu is closed the parent MenuItem is TWO hops up (MenuItem -> MenuWrapper ->` &&
            ` MenuItem), once the submenu popover exists it is FOUR (MenuItem -> MenuWrapper -> Popover -> ResponsivePopover -> MenuItem). An event arg IS a full UI5 expression (EventHandlerResolver hands the` &&
            ` whole handler to BindingParser.parseExpression, so method calls / isA() / ternaries resolve - a fixed-hop walk was built and did toast 'Create New Site > Official Store' when the event was fired` &&
            ` programmatically), but an expression has no loop, so no fixed hop count addresses the parent in both states. **Re-verified 2026-08-23 and the reasoning updated**: the framework has since gained a` &&
            ` LOOPING helper reachable from an event argument - $controller.textPath( control, separator ), Lib.getTextPath - so 'an expression has no loop' no longer decides this on its own. It does not change` &&
            ` the outcome, for a reason worth writing down rather than rediscovering: getTextPath breaks at the first ancestor that has no getText( ) rather than skipping it, sap.m.Menu forwards its items`.
    text1 = text1 && ` aggregation to an internal sap.m.MenuWrapper (Menu.js, forwarding idSuffix '-menuWrapper', @since 1.136), and MenuWrapper declares no text property and no getText at all. So the walk runs exactly one` &&
            ` iteration and returns the leaf text - byte-identical to what this port already toasts. Rewiring to textPath would be a no-op. The same wrapper breaks the SAMPLE's own 'while (oItem instanceof` &&
            ` MenuItem) { ... oItem.getParent(); }' loop: the live demo kit sample toasts the leaf text too on this release, so the port is behaviour-identical with upstream - hence NOTE, not IMPROVISED. The e2e` &&
            ` interaction asserts the nested-selection leaf toast to guard the boundary. // NOTE: the selected item's text is read with ${$parameters>/item}.getText() (a method call on the resolved MenuItem` &&
            ` control), NOT ${$parameters>/item/text}: the $parameters model exposes 'item' as the control object and UI5 keeps properties in the control's internal property store, so the path .../item/text reads` &&
            ` an undefined direct field and the toast arrives empty. // NOTE: live-verified 2026-07-22 - confirm in a running system that the Menu opens anchored to the button (openBy) and that`.
    text1 = text1 && ` ${$parameters>/item}.getText() delivers the clicked MenuItem's text to the toast. // NOTE: the Menu open/close is wired roundtrip-free via client->follow_up_action( cs_event-control_by_id, toggleBy )` &&
            ` on the button press (anchored by $event.oSource.sId) - the original's client-side toggle 1:1; the item-selected toast is now roundtrip-free too, composed on the frontend via control_global` &&
            ` MESSAGE_TOAST.show (template + ${$parameters>/item}.getText()), so the app has no on_event at all (init-only). // NOTE: the menu toggle was switched from a follow_up_action round-trip to` &&
            ` roundtrip-free follow_up_action on 2026-07-22 - re-verify the button opens/closes the Menu. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the 'Open Menu'` &&
            ` press opens the Menu anchored (toggleBy). // NOTE: the item-selected toast was switched from a message_toast_display round-trip to a roundtrip-free client-composed toast on 2026-07-22 (control_global` &&
            ` MESSAGE_TOAST.show, template ``Action triggered on item: {0}`` filled by ${$parameters>/item}.getText()) - re-verify selecting a menu item toasts "Action triggered on item: <text>". **e2e-verified`.
    text1 = text1 && ` 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): selecting 'Hide Existing Sites' toasts 'Action triggered on item: Hide Existing Sites'.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Menu`                            name = `Menu`                                          class = `z2ui5_cl_smpc_app_060` path = `src/02/01/z2ui5_cl_smpc_app_060.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-22): verified in a running system 2026-07-22: the Menu opens anchored to the button (openBy) and the selected item text resolves via ${$parameters>/item}.getText().`
        notes = text1
        post171 = `Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it.` ) ).

    text1 = `POST-1.71: sap.m.MenuItem.endContent (aggregation since 1.131) is the sample's subject and is kept 1:1 on the two items that carry transparent icon Buttons; the app needs UI5 >= 1.131 to render them.` &&
            ` // POST-1.71: sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the trigger button; needs UI5 >= 1.84. // NOTE: The controller loads Menu.fragment.xml lazily and toggles it (isOpen -> close,` &&
            ` otherwise openBy(button)). The port declares the same Menu in the Button's dependents aggregation and toggles it roundtrip-free through follow_up_action control_by_id / toggleBy anchored to` &&
            ` $event.oSource.sId, so the separate core:FragmentDefinition is dropped (app 419 precedent). // NOTE: The anchored toggleBy of the menu and the endContent buttons inside its items are unverified in a` &&
            ` running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_440.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Menu`                            name = `MenuEndContent`                                class = `z2ui5_cl_smpc_app_440` path = `src/02/01/z2ui5_cl_smpc_app_440.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.MenuItem.endContent (aggregation since 1.131) is the sample's subject and is kept 1:1 on the two items that carry transparent icon Buttons; the app needs UI5 >= 1.131 to render them. //` &&
                 ` sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the trigger button; needs UI5 >= 1.84.` ) ).

    text1 = `POST-1.71: sap.m.MenuItemGroup (control since 1.127.0) and its itemSelectionMode property (since 1.127.0) are kept 1:1 for the three selectable groups; the app needs UI5 >= 1.127 to render them. //` &&
            ` POST-1.71: sap.m.MenuItem.selected (since 1.127.0) is kept 1:1 on the pre-selected group items; needs UI5 >= 1.127. // POST-1.71: sap.m.MenuItem.endContent (aggregation since 1.131) is kept 1:1 for` &&
            ` the transparent icon Buttons at the end of two items; needs UI5 >= 1.131. // POST-1.71: sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the trigger button; needs UI5 >= 1.84. // NOTE: The` &&
            ` controller's lazy Fragment.load / isOpen / openBy / close toggle is reproduced roundtrip-free via follow_up_action( control_by_id, toggleBy ) anchored to the pressed button ($event.oSource.sId); the` &&
            ` Menu from Menu.fragment.xml is declared in the Button's dependents with an added id 'selectableMenu' (the fragment declares none). The four icon-only endContent Buttons carry an added tooltip (Open` &&
            ` Folder / Favorite) the original lacks, so they stay usable with a screen reader. // NOTE: The MenuItemGroup single/multi selection marks and the endContent buttons are client-side behaviour of the`.
    text1 = text1 && ` 1.127+ menu; unverified in a running system. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_419.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Menu`                            name = `MenuSelectable`                                class = `z2ui5_cl_smpc_app_419` path = `src/02/01/z2ui5_cl_smpc_app_419.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.MenuItemGroup (control since 1.127.0) and its itemSelectionMode property (since 1.127.0) are kept 1:1 for the three selectable groups; the app needs UI5 >= 1.127 to render them. //` &&
                 ` sap.m.MenuItem.selected (since 1.127.0) is kept 1:1 on the pre-selected group items; needs UI5 >= 1.127. // sap.m.MenuItem.endContent (aggregation since 1.131) is kept 1:1 for the transparent icon` &&
                 ` Buttons at the end of two items; needs UI5 >= 1.131. // sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the trigger button; needs UI5 >= 1.84.` ) ).

    text1 = `POST-1.71: the MenuButton ``beforeMenuOpen`` event (since UI5 1.94) is kept 1:1 on the five buttons that carry it (four split-mode ones and the menuPosition=RightBottom button, which has no buttonMode` &&
            ` at all - the earlier wording said 'the split-mode buttons', which is one button short). menuPosition (1.56), buttonMode and useDefaultActionOnly are <= 1.71. // NOTE: onMenuAction builds a breadcrumb` &&
            ` path by walking the selected MenuItem's parent chain (e.g. 'basic > add'); the port toasts only the selected item's own text, transported via ${$parameters>/item}.getText(). **Measured 2026-07-31` &&
            ` (browser probe + e2e against the transpiled backend, OpenUI5 1.152), closing pr/menu-item-selected-path as a capability boundary:** sap.m.Menu re-parents its items through an internal` &&
            ` sap.m.MenuWrapper, and the chain changes with the runtime state - while the submenu is closed the parent MenuItem is TWO hops up (MenuItem -> MenuWrapper -> MenuItem), once the submenu popover exists` &&
            ` it is FOUR (MenuItem -> MenuWrapper -> Popover -> ResponsivePopover -> MenuItem). An event arg IS a full UI5 expression (EventHandlerResolver hands the whole handler to BindingParser.parseExpression,`.
    text1 = text1 && ` so method calls / isA() / ternaries resolve - a fixed-hop walk was built and did toast 'Create New Site > Official Store' when the event was fired programmatically), but an expression has no loop, so` &&
            ` no fixed hop count addresses the parent in both states. **Re-verified 2026-08-23 and the reasoning updated**: the framework has since gained a LOOPING helper reachable from an event argument -` &&
            ` $controller.textPath( control, separator ), Lib.getTextPath - so 'an expression has no loop' no longer decides this on its own. It does not change the outcome, for a reason worth writing down rather` &&
            ` than rediscovering: getTextPath breaks at the first ancestor that has no getText( ) rather than skipping it, sap.m.Menu forwards its items aggregation to an internal sap.m.MenuWrapper (Menu.js,` &&
            ` forwarding idSuffix '-menuWrapper', @since 1.136), and MenuWrapper declares no text property and no getText at all. So the walk runs exactly one iteration and returns the leaf text - byte-identical` &&
            ` to what this port already toasts. Rewiring to textPath would be a no-op. The same wrapper breaks the SAMPLE's own 'while (oItem instanceof MenuItem) { ... oItem.getParent(); }' loop: the live demo`.
    text1 = text1 && ` kit sample toasts the leaf text too on this release, so the port is behaviour-identical with upstream - hence NOTE, not IMPROVISED. The e2e interaction asserts the nested-selection leaf toast to` &&
            ` guard the boundary. // NOTE: onPress toasts the pressed MenuItem's control id (the sample's evt.getSource().getId() + ' Pressed'), transported via $event.oSource.sId. The Edit item's core:CustomData` &&
            ` (key=target, value=p1) is kept 1:1 as inert view metadata. // NOTE: the selected item's text is read with ${$parameters>/item}.getText() (a method call on the resolved MenuItem control), NOT` &&
            ` ${$parameters>/item/text}: the $parameters model exposes 'item' as the control object and UI5 keeps properties in the control's internal property store, so the path .../item/text reads an undefined` &&
            ` direct field and the toast arrives empty. // NOTE: live-verified 2026-07-22 - confirm in a running system that itemSelected/press/defaultAction/beforeMenuOpen all fire their toasts and that` &&
            ` ${$parameters>/item}.getText() delivers the selected MenuItem text. // NOTE: all toasts were switched from message_toast_display round-trips to roundtrip-free client-composed control_global toasts on`.
    text1 = text1 && ` 2026-07-22 (the app is now init-only) - re-verify each button/menu still toasts its text. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): opening the 'File'` &&
            ` MenuButton and selecting 'Save' toasts 'Action triggered on item: Save' (the ${$parameters>/item}.getText() template resolves); the remaining buttons/defaultAction wires are the same class but` &&
            ` unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_061.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MenuButton`                      name = `MenuButton`                                    class = `z2ui5_cl_smpc_app_061` path = `src/02/01/z2ui5_cl_smpc_app_061.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-22): verified in a running system 2026-07-22: itemSelected/press/defaultAction/beforeMenuOpen all fire their toasts and the item text resolves.`
        notes = text1
        post171 = `the MenuButton ``beforeMenuOpen`` event (since UI5 1.94) is kept 1:1 on the five buttons that carry it (four split-mode ones and the menuPosition=RightBottom button, which has no buttonMode at all -` &&
                 ` the earlier wording said 'the split-mode buttons', which is one button short). menuPosition (1.56), buttonMode and useDefaultActionOnly are <= 1.71.` ) ).

    text1 = `NOTE: the sample opens every sap.m.MessageBox from its controller, so there is no such control in the view. The nine buttons are wired to events that call client->message_box_display with the sample's` &&
            ` own method name as type (confirm, alert, error, information, warning, success) - the documented 1:1 path (CAPABILITIES.md marks sap.m.MessageBox as expressible, app 036 is its evidence port), not a` &&
            ` workaround. // NOTE: the onClose handler of the two action boxes composes its toast on the client in the original; here the pressed action rides back through the onclose event and ABAP builds the` &&
            ` same text with message_toast_display - same output, and the thin-frontend direction (the action becomes backend-visible, which is the point of the onclose return path). // POST-1.71: the MessageBox` &&
            ` emphasizedAction option (since UI5 1.75) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.75 to render it. // POST-1.71: the MessageBox dependentOn option (since UI5` &&
            ` 1.124) is restored in the option object of the control_global MESSAGE_BOX call, pointing at the view layout (id messageBoxHost) instead of the view object; the app needs a UI5 release >= 1.124 for`.
    text1 = text1 && ` it. // NOTE: The two boxes carrying dependentOn moved to the whitelisted global call on 2026-09 (dependentOn is a sap.m.MessageBox option and left message_box_display( )). onClose stays a BACKEND` &&
            ` event name, so ACTION_SELECTED reaches the app as before. The third box, which carries no UI5-only option, still uses client->message_box_display( ).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageBox`                      name = `MessageBox`                                    class = `z2ui5_cl_smpc_app_278` path = `src/02/01/z2ui5_cl_smpc_app_278.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        since = `1.21.2`
        is_post171 = abap_true
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings. **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_278.mjs: Confirm round-trips to the confirm MessageBox ('Approve purchase order 12345?', closed with Cancel) and 'Warning with two actions' opens a box whose OK` &&
                 ` carries back through the onclose action as the 'Action selected: OK' toast)`
        notes = text1
        post171 = `the MessageBox emphasizedAction option (since UI5 1.75) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.75 to render it. // the MessageBox dependentOn option (since UI5` &&
                 ` 1.124) is restored in the option object of the control_global MESSAGE_BOX call, pointing at the view layout (id messageBoxHost) instead of the view object; the app needs a UI5 release >= 1.124 for` &&
                 ` it.` ) ).

    text1 = `NOTE: All four MessageBoxes are opened through client->message_box_display with the same title, type, details, contentWidth and styleClass the controller passes; the dependentOn: this.getView() option` &&
            ` has no equivalent (abap2UI5 owns the view) and is dropped - it only decides which control the popup is a dependent of, not what it shows. The per-box ids (messageBoxId1..4) are dropped with it. //` &&
            ` NOTE: onShowJSONInfo hands MessageBox a JavaScript object as details; the port passes the same object serialized as JSON ({"glossary":{"title":"example glossary"}}), which is what MessageBox renders` &&
            ` for an object anyway. // IMPROVISED: onShowTextInfoAsync passes details as a function returning a Promise that resolves after 2 seconds, so the sample shows the details placeholder loading first. The` &&
            ` backend has the text immediately and passes it as a plain string, so the loading state of the details link is not reproduced - only its resolved text. // NOTE: All four message_box_display calls` &&
            ` (text, markup, JSON and the formerly async details) are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_447.mjs). // NOTE:`.
    text1 = text1 && ` Three corrections to the details payloads, 2026-08-24. (a) The JSON details string was written as a BACKTICK literal with escaped braces, ``\{"glossary"...``. Backticks pass the backslash through as` &&
            ` an ordinary character - only |...| templates treat \{ as an escape - and this value never reaches the XMLView parser at all: message_box_display ships it as an ajson option object in a` &&
            ` frontend-action payload. So the dialog literally displayed the backslashes. Escaping removed. (b) The two information boxes now pin actions = ( 'OK' ). The framework remaps the information type onto` &&
            ` MessageBox.show, whose default action set WITH a details option is [OK, CANCEL], while MessageBox.information - which the original calls - pins OK; the port's boxes had grown a Cancel button the` &&
            ` sample has not got. The two error boxes are unaffected, since type = 'error' dispatches to MessageBox.error, which pins CLOSE. (c) Still NOT reproduced, and named here rather than left quiet:` &&
            ` MessageBox._formatDetails wraps an OBJECT in <pre> with JSON.stringify(v, null, '\t') and returns a STRING verbatim, so the original renders a tab-indented monospace block where the port renders one`.
    text1 = text1 && ` unindented line; and every details string passes Lib.sanitizeMessageDetails, which - when the markup contains any <li> - returns only a <ul> of the top-level items' textContent, so the FormattedText` &&
            ` box loses its two <p> paragraphs and its <em>. // NOTE: All four boxes moved to the whitelisted global call on 2026-09, when contentWidth left client->message_box_display( ) with the other pure` &&
            ` sap.m.MessageBox options. Two consequences, both in the port's favour: (a) the two information boxes no longer pin actions = ( 'OK' ) - the display method IS the box type on this path, so` &&
            ` ``information`` reaches MessageBox.information( ), which is what the original calls, and the framework's remap onto show( ) (defaults [OK, CANCEL] WITH details) that grew the Cancel button does not` &&
            ` happen at all; (b) the options travel as JSON, so the details payloads are escaped before they are embedded - a payload the backend cannot parse would be embedded as a plain string and the box would` &&
            ` open with no options, quietly. title, details, contentWidth and styleClass are unchanged in value.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageBox`                      name = `MessageBoxInfo`                                class = `z2ui5_cl_smpc_app_447` path = `src/01/01/z2ui5_cl_smpc_app_447.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.21.2`
        notes = text1 ) ).

    text1 = `NOTE: the sample opens a sap.m.MessageBox from its controller; there is no such control in the view. It is driven by two buttons wired to events that call client->message_box_display - the documented` &&
            ` 1:1 path (CAPABILITIES.md marks sap.m.MessageBox as expressible with app 036 as its evidence port), not a workaround. // POST-1.71: ariaHasPopup="Dialog" on both buttons (since UI5 1.84) is newer` &&
            ` than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it. // POST-1.71: the MessageBox emphasizedAction option (since UI5 1.75) is newer than 1.71 but kept for the 1:1` &&
            ` port - the app needs a UI5 release >= 1.75 to render it. // POST-1.71: the MessageBox dependentOn option (since UI5 1.124) is restored in the option object of the control_global MESSAGE_BOX call,` &&
            ` pointing at the view layout (id messageBoxHost); the app needs a UI5 release >= 1.124 to render it. // NOTE: Both boxes moved to the whitelisted global call on 2026-09: icon and dependentOn are` &&
            ` sap.m.MessageBox options and left message_box_display( ). The display method IS the box type there (warning / show), and the option object is the MessageBox API 1:1 - actions, emphasizedAction,`.
    text1 = text1 && ` initialFocus, dependentOn and styleClass travel in it. dependentOn still ties the box to the view layout (id messageBoxHost) and still needs UI5 1.124.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageBox`                      name = `MessageBoxInitialFocus`                        class = `z2ui5_cl_smpc_app_036` path = `src/02/01/z2ui5_cl_smpc_app_036.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.21.2`
        is_post171 = abap_true
        notes = text1
        post171 = `ariaHasPopup="Dialog" on both buttons (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it. // the MessageBox emphasizedAction option (since` &&
                 ` UI5 1.75) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.75 to render it. // the MessageBox dependentOn option (since UI5 1.124) is restored in the option object of` &&
                 ` the control_global MESSAGE_BOX call, pointing at the view layout (id messageBoxHost); the app needs a UI5 release >= 1.124 to render it.` ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84) kept on the message-popover button. The Button.type value 'Negative' (a sap.m.ButtonType value since 1.73) is what the sample's buttonTypeFormatter` &&
            ` returns for the highest-severity Error message. // NOTE: the MessagePopover (built in the controller and addDependent'ed to the button) is declared 1:1 in the button's ``dependents`` aggregation; the` &&
            ` controller's oMessagePopover.toggle(button) becomes a roundtrip-free client->follow_up_action( cs_event-control_by_id, toggleBy ) on the button press (anchored by id), and onActiveTitlePress's` &&
            ` MessageToast.show becomes a roundtrip-free client->follow_up_action( cs_event-control_global, MESSAGE_TOAST/show ); the app is now init-only (no on_event). Previously client->follow_up_action(` &&
            ` cs_event-control_by_id, toggleBy ) anchored to the button's DOM ref ($event.oSource.sId) - open-if-closed / close-if-open. // NOTE: the sample's three severity formatters` &&
            ` (buttonIconFormatter/buttonTypeFormatter/highestSeverityMessages) compute the button icon/type/count from the highest-severity message; the mock data is static, so the results are precomputed in ABAP`.
    text1 = text1 && ` (error icon, Negative type, count 2 for the two Error messages) and bound as scalars. The sample's root-array JSON model (items path '/') is carried under a /T_MESSAGES field of the single default` &&
            ` model. // NOTE: live-verified 2026-07-22 - confirm in a running system that the button toggles the MessagePopover open/closed (toggleBy) and lists the five messages with their MessageItem link, and` &&
            ` that activeTitlePress toasts on the active (first Error) title. // NOTE: the toggle and the active-title toast were switched from server round-trips to roundtrip-free follow_up_action( control_global` &&
            ` / control_by_id ) on 2026-07-22 - re-verify in a running system that the button still toggles the MessagePopover and that pressing an active title still toasts. **e2e-verified 2026-07-30**` &&
            ` (transpiled-framework interaction, scripts/e2e-smoke.mjs): the button toggles the MessagePopover open (the bound message items render); the active-title toast remains unexercised. **e2e-verified` &&
            ` 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_066.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessagePopover`                  name = `MessagePopover`                                class = `z2ui5_cl_smpc_app_066` path = `src/02/01/z2ui5_cl_smpc_app_066.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        since = `1.28`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-22): verified in a running system 2026-07-22: the button toggles the MessagePopover open/closed (toggleBy) and lists the five messages.`
        notes = text1
        post171 = `Button.ariaHasPopup (since UI5 1.84) kept on the message-popover button. The Button.type value 'Negative' (a sap.m.ButtonType value since 1.73) is what the sample's buttonTypeFormatter returns for the` &&
                 ` highest-severity Error message.` ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84) kept on the message-popover button; the Button.type value 'Negative' is a sap.m.ButtonType value since 1.73. MessageItem.markupDescription (renders the` &&
            ` HTML description) is kept for the async sample's rich-text messages - it is listed here for context only and is NOT post-1.71: corrected 2026-08-23, the property carries no @since in MessageItem.js` &&
            ` (its neighbour activeTitle does, so the absence is meaningful) and the class itself is @since 1.46, so it needs no declaration. The two members that DO carry this deviation are Button.ariaHasPopup` &&
            ` and the Negative button type. // NOTE: the MessagePopover (built in the controller) is declared 1:1 in the button's ``dependents``; the controller's toggle(button) becomes a roundtrip-free` &&
            ` client->follow_up_action( cs_event-control_by_id, toggleBy ) on the button press (anchored by id), and onActiveTitlePress's MessageToast.show becomes roundtrip-free client->follow_up_action(` &&
            ` cs_event-control_global, MESSAGE_TOAST/show ); the app is now init-only. Anchored to $event.oSource.sId. Corrected 2026-08-21: the wire actually passed the hard-coded id ``messagePopoverBtn`` while`.
    text1 = text1 && ` this text promised $event.oSource.sId, so the sidecar described a wire the port did not have - and the original's toggle(oEvent.getSource()) value was replaced by a constant. It now passes` &&
            ` $event.oSource.sId, the form apps 060/105/106/107 use. // NOTE: the sample's oMessagePopover.setAsyncURLHandler(...) - allowed = url.lastIndexOf('http', 0) < 0, i.e. relative links allowed, absolute` &&
            ` http links disabled - is reproduced since 2026-07-30 via the framework's declarative URL policy: follow_up_action control_by_id setAsyncURLHandler 'RELATIVE_ONLY' installs the built-in validator on` &&
            ` init (pr/messagepopover-async-url implemented upstream). Semantic nuance: RELATIVE_ONLY also denies non-http absolute schemes (mailto:) and protocol-relative //host URLs, which the original's` &&
            ` http-prefix test would allow - a strictly safer superset, no demo link is affected. The controller's urlValidated toast ('URL validation has been performed.') is wired 1:1 as an added urlValidated` &&
            ` attribute (declared) with a client-composed toast; the original's setAsyncDescriptionHandler (allow-all) is NOT reproduced - it is gated on a longtextUrl (MessageView only calls it ``if`.
    text1 = text1 && ` (asyncDescHandler && oMessageItem.getLongtextUrl())``) and no mock row has one, so it never fires in the sample either. **Its longtextLoaded toast IS reproduced since 2026-08-23** - that half of the` &&
            ` sentence was wrong. longtextLoaded is a separate, UNGATED event: _navigateToDetails calls fireLongtextLoaded( ) on every drill-down and MessagePopover refires it, so in the original clicking any of` &&
            ` the five messages toasts 'Description validation has been performed.' The port had dropped a user-visible behaviour on a justification that only applied to the other handler; it is now wired like the` &&
            ` urlValidated one. // NOTE: the three severity formatters are precomputed in ABAP from the static mock (error icon, Negative type, count 2); the root-array JSON model is carried under /T_MESSAGES on` &&
            ` the single default model. // NOTE: live-verified 2026-07-22 - confirm the button toggles the MessagePopover (toggleBy) and the first Error message renders its HTML markupDescription (h2/p/ul/ol +` &&
            ` links); activeTitlePress toasts. // NOTE: the toggle and the active-title toast were switched to roundtrip-free follow_up_action on 2026-07-22, and the 2026-07-30 RELATIVE_ONLY URL policy +`.
    text1 = text1 && ` urlValidated toast are new since the 2026-07-22 live check (status reset checked -> generated): re-verify the button toggles the MessagePopover, an active title toasts, and the absolute http link in` &&
            ` the first message renders disabled after validation while relative links stay clickable. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the button toggles the` &&
            ` MessagePopover open and its item list renders (restated 2026-08-23: the assertion matches the list-item TITLE 'Error message', and no markup is on screen at that point - MessageView auto-navigates to` &&
            ` the details page only for a SINGLE item and this sample has five, so opening lands on the list page. By the same mechanism the open covers neither the activeTitlePress toast nor urlValidated, which` &&
            ` fires inside the details-page link sanitizer; all three legs named above still need a click on an item). **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_067.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessagePopover`                  name = `MessagePopoverAsyncMessageHandling`            class = `z2ui5_cl_smpc_app_067` path = `src/02/01/z2ui5_cl_smpc_app_067.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.28`
        is_post171 = abap_true
        notes = text1
        post171 = `Button.ariaHasPopup (since UI5 1.84) kept on the message-popover button; the Button.type value 'Negative' is a sap.m.ButtonType value since 1.73. MessageItem.markupDescription (renders the HTML` &&
                 ` description) is kept for the async sample's rich-text messages - it is listed here for context only and is NOT post-1.71: corrected 2026-08-23, the property carries no @since in MessageItem.js (its` &&
                 ` neighbour activeTitle does, so the absence is meaningful) and the class itself is @since 1.46, so it needs no declaration. The two members that DO carry this deviation are Button.ariaHasPopup and the` &&
                 ` Negative button type.` ) ).

    text1 = `POST-1.71: two post-1.71 members are kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the MessagePopover button, and MessagePopover.groupItems (since UI5 1.73). // NOTE: the controller's` &&
            ` manual MessageManager handling is replaced by two framework mechanisms: (1) the typed value bindings with constraints (sap.ui.model.type.Integer/String, ZIPCODE Integer, WEEKLYHOURS Integer maximum` &&
            ` 40, EMAIL search regex) collect/clear their validation messages AUTOMATICALLY into the message> model (no app code) - replacing onChange/handleRequiredField/checkInputConstraints; (2) app-authored` &&
            ` messages go through the new z2ui5.cc.MessageManager companion control (items bound to /T_MESSAGES), which reconciles them into the message manager with a target + processor. Save ports` &&
            ` generateInvalidUserInput 1:1 in effect: since abap2UI5 auto-collects validation messages only from USER input (not from programmatically-set model values), Save INJECTS the four demo issues on the` &&
            ` SAME rows the original targets (formContainer.getItems()[4/5/6] = John Miller Name empty, Stefan Bosch ZIP 'AAA', Maria Fontes email malformed, plus the employment weekly hours 400) AND authors the`.
    text1 = text1 && ` matching four messages (3 Errors + 1 Warning) with their /T_FORMS/4|5|6 and /T_EMPLOYMENT/0 targets, then opens the MessagePopover via follow_up_action( control_by_id openBy ) anchored to` &&
            ` messagePopoverBtn. // NOTE: the sample's nested JSON model (street/name, zip/code, phone/number) is flattened into a flat ABAP row (STREET_NAME, ZIPCODE, PHONE_NUMBER, ...) - abap2UI5 serves one` &&
            ` default model; the bindings use the flattened field names. The three f:ColumnElementData layoutData hints (cellsSmall/cellsLarge column spans on the street-number, zip-code and the {phone/time} Input` &&
            ` in the Personal form's Contact section (corrected 2026-08-23 - this said 'one employment Input', and the employment SimpleForm carries no ColumnElementData at all; the count of three was right, the` &&
            ` third one's home was not)) are dropped - responsive-span cosmetics with no data/behaviour. // 1.71: controller-only pieces with no view/binding equivalent are dropped:` &&
            ` buttonIconFormatter/buttonTypeFormatter/highestSeverityMessages (the button's severity-based icon/type/count) is reduced to text={=${message>/}.length}, type=Emphasized; core:CommandExecution (the`.
    text1 = text1 && ` Ctrl+Shift+M focus shortcut); isPositionable. // NOTE: the controller's handleMessagePopoverPress (this.oMP.toggle(oEvent.getSource())) becomes a roundtrip-free client->follow_up_action(` &&
            ` cs_event-control_by_id, toggleBy ) wired on the button press (anchored to the button by its id 'messagePopoverBtn'), matching the original's client-side toggle 1:1 - no SHOW_MESSAGES round-trip's DOM` &&
            ` ref ($event.oSource.sId), the MessagePopover carrying id=messagePopover - open-if-closed / close-if-open, same frontend-action pattern as apps 066/067. // NOTE: all 8 forms of the mock` &&
            ` FormsModel.json are loaded verbatim (Julie Armstrong, Denise Smith, Richard Wilson, Gerd Becker, John Miller with the built-in invalid ZIP AAA, Stefan Bosch, Maria Fontes with empty email + malformed` &&
            ` website, Antonio Ferrari) plus the single employment row - the full data set, not a subset. // NOTE: activeTitlePress scroll-to-control navigation is RESTORED (activeTitle=true): pressing a message` &&
            ` title fires ACTIVE_TITLE with the message's target control id (${$parameters>/item}.getBindingContext('message').getObject().getControlIds()[0]), and the handler runs follow_up_action(`.
    text1 = text1 && ` scroll_into_view ) + control_by_id close + set_focus on it - the 1:1 equivalent of the sample's scrollToElement + close + focus. Needs the upstream fix that lets SET_FOCUS/SCROLL_INTO_VIEW resolve a` &&
            ` fully-qualified control id (resolveById), since a UI5 Message carries the view-prefixed id. Scope, added 2026-08-23: the grouping is restored for the APP-AUTHORED messages only. groupName binds` &&
            ` {message>code}, and code is set only on rows the cc MessageManager creates, so a message UI5 collects itself has none and falls to the empty default, landing ungrouped. The original computes the` &&
            ` header from controlIds for every message. // NOTE: onInit's MessageToast.show('Press "Save" to trigger validation.') is ported as client->message_toast_display( ... ) in the check_on_init branch. //` &&
            ` NOTE: MessageItem grouping (groupItems=true + the 'Personal, <section>' group headers) is RESTORED: the original derives groupName in its controller (getGroupName). That is a domain classification,` &&
            ` so - abap2UI5 being a thin frontend - it is computed in the ABAP backend (COND on additionaltext: Email -> 'Personal, Contact', else 'Personal, Information') and carried on the Message code field`.
    text1 = text1 && ` (sap.ui.core.message.Message has no groupName slot), bound groupName="{message>code}" - no frontend expression. Only Email sits in the Contact group, all others in Information, matching the` &&
            ` original's headers. // NOTE: Two of the four Save messages carry the text UI5 itself produces, corrected 2026-08-23. Only two of the original's four are authored by hand - the required-field Error` &&
            ` and the weekly-hours Warning; the ZIP and Email ones come from the registered form container turning the failed two-way updates of setValue('AAA') and setValue('MariaFontes.com') into messages whose` &&
            ` text is the TYPE's (Integer -> EnterInt 'Enter a number with no decimal places', String search constraint -> 'Enter a valid value', both in the OpenUI5 message bundle). The port had paraphrased them` &&
            ` as 'Enter a number without decimals.' and 'Enter a valid email address.', which the sample never shows - and which made the port disagree with ITSELF, since typing AAA into the same field makes` &&
            ` abap2UI5's own auto-collection emit the real bundle text. // NOTE: After Save the ZIP field renders EMPTY where the original shows the invalid 'AAA'. The original calls setValue('AAA') on the`.
    text1 = text1 && ` CONTROL, so the text stays visible while the two-way update throws a ParseException and the model keeps 12345 - showing the bad input next to its message is the point of that step. The port writes` &&
            ` 'AAA' into the MODEL instead, and the field's Integer type formats a non-numeric value to '' (NumberFormat.format returns the empty string when the value is NaN and emptyString is NaN). Reproducing` &&
            ` it needs the value set on the control while the model keeps the valid one, which the current shape has no route for; declared rather than improvised.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessagePopover`                  name = `MessagePopoverMessageHandling`                 class = `z2ui5_cl_smpc_app_065` path = `src/02/01/z2ui5_cl_smpc_app_065.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth` &&
                 ` a close look.`
        since = `1.28`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-27): verified in a running system 2026-07-27 - MessageManager cc: add of app-authored messages reconciled (the REMOVE arm is not exercised by this port - corrected 2026-08-23:` &&
                 ` t_messages is assigned in one place, always to the same four rows, nothing clears or shrinks it, and the cc control's removal path only runs when a row leaves items or the view is destroyed),` &&
                 ` MessagePopover shows both sources. **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_065.mjs: Save round-trips SAVE, the message> model then carries the four` &&
                 ` authored messages (3 Errors + 1 Warning, read off Messaging.getMessageModel), the footer button shows 4 and the openBy follow-up opens the MessagePopover with the 'Personal, Contact' group)`
        notes = text1
        post171 = `two post-1.71 members are kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the MessagePopover button, and MessagePopover.groupItems (since UI5 1.73).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageStrip`                    name = `CustomMessageStripDesign`                      class = `z2ui5_cl_smpc_app_452` path = `src/02/01/z2ui5_cl_smpc_app_452.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.MessageStrip.colorSet and colorScheme are both @since 1.143.0 and are the whole subject of the sample, so all ten strips keep them 1:1 and the port needs a UI5 runtime >= 1.143. //` &&
                 ` NOTE: onColorSetChange / _updateDesignForColorSet loop over the ten strips and set ColorSet1 or ColorSet2 on each. The port two-way binds the Select key and gives every strip the same colorSet` &&
                 ` expression binding over that field, so the switch is roundtrip-free and the Select.change attribute is dropped (the original Select carries no selectedKey either; the port adds the bound one). //` &&
                 ` NOTE: The colorSet expression on all ten strips (Select -> ColorSet1/ColorSet2) is unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction,` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_452.mjs).`
        post171 = `sap.m.MessageStrip.colorSet and colorScheme are both @since 1.143.0 and are the whole subject of the sample, so all ten strips keep them 1:1 and the port needs a UI5 runtime >= 1.143.` ) ).

    text1 = `NOTE: the original's view.xml has no MessageStrip at all: showMsgStrip destroys the previous one and _generateMsgStrip creates a new sap.m.MessageStrip in the controller, adding it to the` &&
            ` VerticalLayout. The port declares that MessageStrip in the view (one control more than the original view.xml) and binds its four varying properties - text, type, showIcon, showCloseButton - plus` &&
            ` visible, which keeps it hidden until the button has been pressed once. Destroy-and-recreate then has no counterpart: the port re-renders the view, which is the destroy-and-create the original does.` &&
            ` **Corrected 2026-08-23**: rebinding alone was NOT the same result. MessageStrip.close( ) sets visible = false on the CONTROL while the model still holds abap_true, and an unchanged model value fires` &&
            ` no binding update - so once the user closed a strip it never came back, and the following presses kept changing text and type behind an invisible control. The original destroys the strip and creates` &&
            ` a new one on every press. // IMPROVISED: the original randomizes: sType = aTypes[Math.round(Math.random() * 3)] and both flags from Math.round(Math.random()). Deciding is backend work, so the choice`.
    text1 = text1 && ` moves to ABAP - and it is made DETERMINISTIC there: a press counter rotates the type through Information/Warning/Error/Success and the two flags through their combinations, so every press still` &&
            ` changes the strip but the port is reproducible (the corpus rule for random/current-date values, apps 164/181). MessageStrip.type carries the control's own default Information until the first press,` &&
            ` because an empty string is rejected on an enum property. // NOTE: the accessibility announcement is reproduced since 2026-08-05: onInit takes an sap.ui.core.InvisibleMessage instance and` &&
            ` _generateMsgStrip calls announce('New Information Bar of type ...', Assertive) on it. InvisibleMessage is a JS singleton, not a control in the view, so no CONTROL_BY_ID wire can address it - the` &&
            ` INVISIBLE_MESSAGE global target added for exactly that (pr/invisible-message-announce) carries the announcement instead: follow_up_action( control_global, INVISIBLE_MESSAGE / announce / <text> /` &&
            ` Assertive ) right after the model update, with the same text the original composes. It is lazy-required, so a runtime older than UI5 1.78 logs 'not available' instead of failing the app. //`.
    text1 = text1 && ` POST-1.71: INVISIBLE_MESSAGE.announce is a control_global frontend action since UI5 1.78 - newer than the 1.71 floor but kept for the 1:1 port: the sample's whole point is that adding a message bar` &&
            ` is announced to a screen reader. Below 1.78 the lazy require returns undefined and the dispatch hits its "not available" guard, so the announcement is silently skipped while the rest of the app works` &&
            ` - the port needs a UI5 release >= 1.78 for the behaviour the sample demonstrates.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageStrip`                    name = `DynamicMessageStripGenerator`                  class = `z2ui5_cl_smpc_app_289` path = `src/02/01/z2ui5_cl_smpc_app_289.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.30`
        is_post171 = abap_true
        notes = text1
        post171 = `INVISIBLE_MESSAGE.announce is a control_global frontend action since UI5 1.78 - newer than the 1.71 floor but kept for the 1:1 port: the sample's whole point is that adding a message bar is announced` &&
                 ` to a screen reader. Below 1.78 the lazy require returns undefined and the dispatch hits its "not available" guard, so the announcement is silently skipped while the rest of the app works - the port` &&
                 ` needs a UI5 release >= 1.78 for the behaviour the sample demonstrates.` ) ).

    text1 = `POST-1.71: the MessageStrip ``controls`` aggregation (since UI5 1.129) is kept 1:1 for the fifth strip's %%0/%%1/%%2 multi-link formatted text (three sap.m.Link). enableFormattedText itself is since` &&
            ` 1.50 (<= 1.71). // POST-1.71: core:require="{Formatter: 'z2ui5/model/formatter'}" wires the curated formatter module (since UI5 1.74) so the inlineIconsHelper strip can use` &&
            ` Formatter.expandInlineIcons; the two added namespace decls (xmlns:core, core:require on the view root) are not in the sample view. // NOTE: the sample's controller builds inlineIconsHelper from` &&
            ` sap.m.MessageStripUtilities.getInlineIcon() concatenations; the port stores the text with %%icon:sap-icon://<name>%% placeholders (the real icon names message-success/sys-enter-2/stethoscope) and` &&
            ` binds it through formatter 'Formatter.expandInlineIcons' - added upstream 2026-07-20 (pr/formatter-inline-icon), it resolves each glyph via IconPool and emits the same sapMMsgStripInlineIcon markup,` &&
            ` so no icon codepoints are guessed. The inlineIconsUnicode strip keeps the sample's own literal &#x....; entities 1:1. // NOTE: the sample's JSONModel of static formatted-text strings is rebuilt as`.
    text1 = text1 && ` ABAP string fields on the single default model and bound 1:1 (text={/...}); field names differ from the JSON keys (default->default_text etc.) but values are display-only, so the binding-value diff` &&
            ` does not apply.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageStrip`                    name = `MessageStripWithEnableFormattedText`           class = `z2ui5_cl_smpc_app_062` path = `src/02/01/z2ui5_cl_smpc_app_062.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        is_post171 = abap_true
        notes = text1
        post171 = `the MessageStrip ``controls`` aggregation (since UI5 1.129) is kept 1:1 for the fifth strip's %%0/%%1/%%2 multi-link formatted text (three sap.m.Link). enableFormattedText itself is since 1.50 (<=` &&
                 ` 1.71). // core:require="{Formatter: 'z2ui5/model/formatter'}" wires the curated formatter module (since UI5 1.74) so the inlineIconsHelper strip can use Formatter.expandInlineIcons; the two added` &&
                 ` namespace decls (xmlns:core, core:require on the view root) are not in the sample view.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageToast`                    name = `MessageToast`                                  class = `z2ui5_cl_smpc_app_037` path = `src/01/01/z2ui5_cl_smpc_app_037.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.2` ) ).

    text1 = `NOTE: the sample's view.xml is only the Page with the Delete Button - everything else is built in onInit. The controller-built Dialog (resizable, state Error, contentHeight/contentWidth 50%,` &&
            ` verticalScrolling false), its customHeader Bar with the nav-back Button and the Title, the MessageView with its MessageItem template and the item's Link, and the Close beginButton are rebuilt 1:1 as` &&
            ` a core:FragmentDefinition shown with popup_display. So Dialog, Bar, Title, MessageView, MessageItem, Link and two of the three Button controls are extra against the original view.xml. // NOTE: the` &&
            ` two controller handlers that reach into controls become bound state, per the prefer-a-bindable-property rule: oBackButton.setVisible(...) is the two-way bound visible={/BACK_VISIBLE} and` &&
            ` oDialogTitle.setText(...) is text={/DIALOG_TITLE}. Only navigateBack has no bindable equivalent - it is the one frontend action (control_by_id, view = cs_view-popup, on the MessageView id` &&
            ` messageView, an unlisted public control method that passes the framework's deny-prefix guard). Re-verified 2026-08-23: still true. navigateBack is a method with no bindable twin, and the port wires`.
    text1 = text1 && ` it as a frontend action - the setVisible/setText this sentence also names are the ones it BOUND instead, which is what makes it read like a stale claim to the stale-impossibility probe.` &&
            ` handleDialogPress's own navigateBack is not needed: popup_display rebuilds the fragment, so the MessageView always opens on its list page; only the header state is reset in ABAP. // NOTE:` &&
            ` activeTitlePress keeps the original's client-side MessageToast.show('Active title pressed') as a roundtrip-free follow_up_action MESSAGE_TOAST show - the text is static, so nothing has to travel. //` &&
            ` NOTE: a flat ABAP row serializes every field, so the properties the mock omits carry their UI5 defaults: markupDescription is false on all six messages (never set in the original), activeTitle false` &&
            ` except on the last one, and the counter of the second message (Warning without description) is 0 - ListItemBaseRenderer.renderCounter only renders a truthy counter, so 0 and 'absent' render` &&
            ` identically.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageView`                     name = `MessageViewInsideDialog`                       class = `z2ui5_cl_smpc_app_284` path = `src/01/01/z2ui5_cl_smpc_app_284.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        notes = text1 ) ).

    text1 = `POST-1.71: one post-1.71 member is kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the Messages button, which the sample's own view.xml carries. // NOTE: the sample's view.xml is only` &&
            ` the Page with the Messages Button - everything else is built in onInit. The controller-built popover, its customHeader Bar with the nav-back Button and the Title, the MessageView with its MessageItem` &&
            ` template and the item's Link, and the Close button are rebuilt 1:1 as a core:FragmentDefinition and shown with client->popover_display( by_id ), anchored to the pressed button exactly as` &&
            ` handlePopoverPress's openBy(oEvent.getSource()) does (app 229 idiom). So the popover, Bar, Title, MessageView, MessageItem, Link and the two extra Buttons are extra against the original view.xml. //` &&
            ` NOTE: the Popover keeps the controller's geometry 1:1 - contentWidth/contentHeight 440px, verticalScrolling false, modal true - and the Close button sits in a footer Bar's contentRight, with the` &&
            ` sapUiTinyMarginEnd class the controller adds with addStyleClass. // NOTE: the two controller handlers that reach into controls become bound state, per the prefer-a-bindable-property rule:`.
    text1 = text1 && ` oBackButton.setVisible(...) is visible={/BACK_VISIBLE} and oPopoverTitle.setText(...) is text={/POPOVER_TITLE}. Only navigateBack has no bindable equivalent - it is the one frontend action` &&
            ` (control_by_id, view = cs_view-popup, on the MessageView id messageView), the same path app 284 uses. Re-verified 2026-08-23: still true. navigateBack is a method with no bindable twin, and the port` &&
            ` wires it as a frontend action - the setVisible/setText this sentence also names are the ones it BOUND instead, which is what makes it read like a stale claim to the stale-impossibility probe.` &&
            ` handlePopoverPress's own navigateBack is not needed: popover_display rebuilds the fragment, so the MessageView always opens on its list page; only the header state is reset in ABAP. // NOTE: the back` &&
            ` button's press also calls oPopover.focus() to keep keyboard focus inside the popover after navigating back. The port does not: the popover keeps focus because it is never re-anchored, and a set_focus` &&
            ` on a popup that is already focused would be a no-op round trip. // NOTE: a flat ABAP row serializes every field, so the properties the mock omits carry their UI5 defaults: markupDescription is false`.
    text1 = text1 && ` on all five messages (the template binds it, the data never sets it), and the second message (Warning without description) has no subtitle and counter 0 - ListItemBaseRenderer.renderCounter only` &&
            ` renders a truthy counter, so 0 and 'absent' render identically. // NOTE: the nav-back Button is icon-only in the original (IconPool.getIconURI('nav-back'), no text and no tooltip). The port gives it` &&
            ` tooltip="Back" so it is reachable with a screen reader - the one accessibility addition in this port. // NOTE: not yet verified in a running system: the anchored popover with a MessageView inside it` &&
            ` and the navigateBack frontend action on the popup view. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_563.mjs). // NOTE: The details popover is opened on` &&
            ` the POPOVER slot, so it is closed with popover_destroy( ) and addressed with cs_view-popover. popup_destroy( )/cs_view-popup target a different slot that this app never opens, so the close and the` &&
            ` in-popover navigation were silent no-ops.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageView`                     name = `MessageViewInsidePopover`                      class = `z2ui5_cl_smpc_app_563` path = `src/02/01/z2ui5_cl_smpc_app_563.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 member is kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the Messages button, which the sample's own view.xml carries.` ) ).

    text1 = `POST-1.71: one post-1.71 member is kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the Messages button, which the sample's own view.xml carries. // NOTE: the sample's view.xml is only` &&
            ` the Page with the Messages Button - everything else is built in onInit. The controller-built popover, its customHeader Bar with the nav-back Button and the Title, the MessageView with its MessageItem` &&
            ` template and the item's Link, and the Close button are rebuilt 1:1 as a core:FragmentDefinition and shown with client->popover_display( by_id ), anchored to the pressed button exactly as` &&
            ` handlePopoverPress's openBy(oEvent.getSource()) does (app 229 idiom). So the popover, Bar, Title, MessageView, MessageItem, Link and the two extra Buttons are extra against the original view.xml. //` &&
            ` NOTE: the ResponsivePopover keeps the controller's geometry 1:1 - contentWidth 20%, contentHeight 40%, verticalScrolling false, modal true - and the Close button is the endButton aggregation (not a` &&
            ` footer), with the sapUiTinyMarginEnd class the controller adds with addStyleClass. This is the only difference from the sibling sample MessageViewInsidePopover (app 563), apart from the button's`.
    text1 = text1 && ` margin class. // NOTE: the two controller handlers that reach into controls become bound state, per the prefer-a-bindable-property rule: oBackButton.setVisible(...) is visible={/BACK_VISIBLE} and` &&
            ` oPopoverTitle.setText(...) is text={/POPOVER_TITLE}. Only navigateBack has no bindable equivalent - it is the one frontend action (control_by_id, view = cs_view-popup, on the MessageView id` &&
            ` messageView), the same path app 284 uses. Re-verified 2026-08-23: still true. navigateBack is a method with no bindable twin, and the port wires it as a frontend action - the setVisible/setText this` &&
            ` sentence also names are the ones it BOUND instead, which is what makes it read like a stale claim to the stale-impossibility probe. handlePopoverPress's own navigateBack is not needed:` &&
            ` popover_display rebuilds the fragment, so the MessageView always opens on its list page; only the header state is reset in ABAP. // NOTE: the back button's press also calls oPopover.focus() to keep` &&
            ` keyboard focus inside the popover after navigating back. The port does not: the popover keeps focus because it is never re-anchored, and a set_focus on a popup that is already focused would be a`.
    text1 = text1 && ` no-op round trip. // NOTE: a flat ABAP row serializes every field, so the properties the mock omits carry their UI5 defaults: markupDescription is false on all five messages (the template binds it,` &&
            ` the data never sets it), and the second message (Warning without description) has no subtitle and counter 0 - ListItemBaseRenderer.renderCounter only renders a truthy counter, so 0 and 'absent'` &&
            ` render identically. // NOTE: the nav-back Button is icon-only in the original (IconPool.getIconURI('nav-back'), no text and no tooltip). The port gives it tooltip="Back" so it is reachable with a` &&
            ` screen reader - the one accessibility addition in this port. // NOTE: not yet verified in a running system: the anchored popover with a MessageView inside it and the navigateBack frontend action on` &&
            ` the popup view. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_564.mjs). // NOTE: The details popover is opened on the POPOVER slot, so it is closed with` &&
            ` popover_destroy( ) and addressed with cs_view-popover. popup_destroy( )/cs_view-popup target a different slot that this app never opens, so the close and the in-popover navigation were silent no-ops.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageView`                     name = `MessageViewInsideResponsivePopover`            class = `z2ui5_cl_smpc_app_564` path = `src/02/01/z2ui5_cl_smpc_app_564.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 member is kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the Messages button, which the sample's own view.xml carries.` ) ).

    text1 = `NOTE: the original registers its four messages on the sap.ui.core.message.MessageManager and the MessageView binds them via the message> model. abap2UI5 carries the message> model on every view slot` &&
            ` since pr/message-model (2026-07-18, auto-collecting control validation messages). The messages are bound here as a plain ABAP table on MessageView items with a MessageItem template (client->_bind(` &&
            ` t_messages )) - the path CAPABILITIES.md explicitly endorses for a static message set. Same rendering as the original. **Corrected 2026-08-23**: this sentence used to justify the choice with 'there` &&
            ` is no ABAP API to push an arbitrary static message set into the message> model', which stopped being true two days after it was written - the z2ui5.cc.MessageManager companion control does exactly` &&
            ` that from a bound ABAP table (upstream pr/message-manager-write, 2026-07-20; app 065 in this corpus wires it). So the plain table is a CHOICE and the right one for a render-only sample, not the only` &&
            ` route. The same sentence also cited a class of the retired z2ui5_cl_demo_app_* naming scheme of abap2UI5/samples, which does not exist; the MessageView/MessagePopover evidence ports are 066/067.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageView`                     name = `MessageViewMessageManager`                     class = `z2ui5_cl_smpc_app_038` path = `src/01/01/z2ui5_cl_smpc_app_038.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46`
        notes = text1 ) ).

    text1 = `NOTE: the three formatters are computed in the backend, which is the point of the thin-frontend rule (apps 009/010/022/092). buttonIconFormatter, buttonTypeFormatter and highestSeverityMessages each` &&
            ` walk the whole message list to find the highest severity (Error > Warning > Success > Information) and count how many messages carry it - a computation over the data, not a presentation format.` &&
            ` model_init does it once and the Button binds the three finished values (sap-icon://message-error, Negative, 5 for this data). The original re-runs the formatters on every model change; here the` &&
            ` values are recomputed wherever the message list changes. // NOTE: the original's view.xml is only the Page with the footer Button; the MessageView with its MessageItem template and Link, the Dialog` &&
            ` with its custom-header Bar, the nav-back Button, the 'Publish order' Text and the Close endButton are all built in onInit. They are rebuilt here as a core:FragmentDefinition shown with popup_display,` &&
            ` so Dialog, Bar, Text, MessageView, MessageItem, Link and two of the three Button controls are extra against the original view.xml. // NOTE: the back button's visible state is two-way bound`.
    text1 = text1 && ` (visible={/BACK_VISIBLE}) instead of driven by setVisible, per the prefer-a-bindable-property rule; only navigateBack, which has no bindable equivalent, stays a control_by_id action on the popup` &&
            ` slot. handleMessageViewPress's own navigateBack is not needed: popup_display rebuilds the fragment, so the MessageView always opens on its list page. // NOTE: the MessageItem template binds` &&
            ` counter={COUNTER} 1:1, but no message in the sample sets a counter - a flat ABAP row serializes it as 0, which ListItemBaseRenderer renders exactly like an absent counter (it only renders a truthy` &&
            ` one). The two ungrouped messages carry no groupName, which is what makes them appear outside the two Purchase Order groups. // POST-1.71: The four sap.m.ButtonType values the highest-severity button` &&
            ` carries - Negative, Critical, Success, Neutral - are all @since 1.73 (sap.m/library.js; the Default/Accept/Reject/Transparent/Emphasized members above them carry none). The port computes them in ABAP` &&
            ` and binds them onto Button.type, exactly the values buttonTypeFormatter returns upstream, so no gate can see them: the value never appears in the view, it arrives through the model. Declared by`.
    text1 = text1 && ` policy, the same route the sibling MessagePopover ports 066/067 take - both of which live under src/02 for it. Undeclared until 2026-08-23; declaring it moves the port from src/01/01 to src/02/01.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MessageView`                     name = `MessageViewWithGrouping`                       class = `z2ui5_cl_smpc_app_294` path = `src/02/01/z2ui5_cl_smpc_app_294.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.46`
        is_post171 = abap_true
        notes = text1
        post171 = `The four sap.m.ButtonType values the highest-severity button carries - Negative, Critical, Success, Neutral - are all @since 1.73 (sap.m/library.js; the Default/Accept/Reject/Transparent/Emphasized` &&
                 ` members above them carry none). The port computes them in ABAP and binds them onto Button.type, exactly the values buttonTypeFormatter returns upstream, so no gate can see them: the value never` &&
                 ` appears in the view, it arrives through the model. Declared by policy, the same route the sibling MessagePopover ports 066/067 take - both of which live under src/02 for it. Undeclared until` &&
                 ` 2026-08-23; declaring it moves the port from src/01/01 to src/02/01.` ) ).

    text1 = `NOTE: handleSelectionChange toasts the changed item and whether it was selected - composed on the client from ${$parameters>/selected} and ${$parameters>/changedItem}.getText(), so it needs no` &&
            ` round-trip. // NOTE: handleSelectionFinish lists every selected item. A UI5 expression has no loop, so the selection travels as the bound selectedKeys and ABAP rebuilds the line - looping the KEYS,` &&
            ` not the product table, because the original reads the selectedItems event parameter and MultiComboBox fills that by addAssociation per pick, i.e. in selection order. Both toasts pass width = ``auto``` &&
            ` as the original's MessageToast.show options do; on the client-composed selectionChange leg that option cannot travel, which is the boundary app 281 also records. // NOTE: Both wires (the` &&
            ` client-composed selectionChange toast and the selectionFinish round-trip) are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_490.mjs). // NOTE: The client-composed toast template is written with BARE {0}/{1} placeholders. It had carried \{0\}, which is wrong in a backtick literal: only`.
    text1 = text1 && ` |...| templates treat \{ as an escape, so the backslashes were real characters and formatTemplate's /\{(\d+)...\}/ never matched - the placeholders rendered verbatim. The escape is not needed at all` &&
            ` on this path: the value lands in an EVENT attribute, which XMLTemplateProcessor routes through EventHandlerResolver without a BindingParser pass. An ordinary attribute is the opposite case and still` &&
            ` needs \{ (see app 523's app:template). // NOTE: The selectionFinished toast moved to the whitelisted global call on 2026-09: width is a sap.m.MessageToast option and left` &&
            ` client->message_toast_display( ), so it now travels in the option object of the call, which is the MessageToast.show( ) API 1:1.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBox`                                 class = `z2ui5_cl_smpc_app_490` path = `src/01/01/z2ui5_cl_smpc_app_490.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.MultiComboBox inherits showClearIcon from sap.m.ComboBoxBase, where it is @since 1.96 - it is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.96.` &&
            ` // NOTE: handleSelectionChange toasts the changed item and whether it was selected - composed on the client from the two event parameters, so it needs no round-trip. // NOTE: handleSelectionFinish` &&
            ` lists the TEXTS of every selected item. A UI5 expression argument cannot map an array of controls, so the port binds selectedKeys two-way and composes the same line in ABAP on a round-trip. // NOTE:` &&
            ` Both wires and the clear icon are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_491.mjs). // NOTE: The client-composed` &&
            ` toast template is written with BARE {0}/{1} placeholders. It had carried \{0\}, which is wrong in a backtick literal: only |...| templates treat \{ as an escape, so the backslashes were real` &&
            ` characters and formatTemplate's /\{(\d+)...\}/ never matched - the placeholders rendered verbatim. The escape is not needed at all on this path: the value lands in an EVENT attribute, which`.
    text1 = text1 && ` XMLTemplateProcessor routes through EventHandlerResolver without a BindingParser pass. An ordinary attribute is the opposite case and still needs \{ (see app 523's app:template). // NOTE: The` &&
            ` selectionFinished toast moved to the whitelisted global call on 2026-09: width is a sap.m.MessageToast option and left client->message_toast_display( ), so it now travels in the option object of the` &&
            ` call, which is the MessageToast.show( ) API 1:1.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxClearIcon`                        class = `z2ui5_cl_smpc_app_491` path = `src/02/01/z2ui5_cl_smpc_app_491.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.MultiComboBox inherits showClearIcon from sap.m.ComboBoxBase, where it is @since 1.96 - it is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.96.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxCustomFiltering`                  class = `z2ui5_cl_smpc_app_481` path = `src/01/01/z2ui5_cl_smpc_app_481.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = `IMPROVISED: onInit gives both MultiComboBoxes a JavaScript setFilterFunction - a case-insensitive starts-with on the first, a contains on the second. A JS filter callback has no bindable or backend` &&
                 ` equivalent (the app-authored-JS-function boundary), so both carry UI5's default filtering and the contrast the sample demonstrates is lost.` )
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxDefaultFiltering`                 class = `z2ui5_cl_smpc_app_459` path = `src/01/01/z2ui5_cl_smpc_app_459.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0` )
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxGrouping`                         class = `z2ui5_cl_smpc_app_039` path = `src/01/01/z2ui5_cl_smpc_app_039.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_039.mjs: F4 on the MultiComboBox opens the picker with 11 GroupHeaderListItems in SUPPLIERNAME order from Alpha Printers to Ultrasonic United, with the products` &&
                 ` grouped under them)`
        notes = `NOTE: the custom groupHeaderFactory '.getGroupHeader' (controller code) is replaced by UI5's default group headers - the sample's factory builds a SeparatorItem with the group key, which is exactly` &&
                 ` what the default renders anyway (CAPABILITIES.md group-sorter row, source-verified on both sides), so this is a faithful 1:1, not a workaround. The items are a bound template with the original's` &&
                 ` sorter (path SUPPLIERNAME, group: true) as a raw binding-info string.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxMaxPickerHeight`                  class = `z2ui5_cl_smpc_app_495` path = `src/02/01/z2ui5_cl_smpc_app_495.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.MultiComboBox inherits maxPickerHeight from sap.m.ComboBoxBase, where it is @since 1.150 - it is the subject of this sample, so all three variants keep it 1:1 and the port needs a UI5` &&
                 ` runtime >= 1.150.`
        post171 = `sap.m.MultiComboBox inherits maxPickerHeight from sap.m.ComboBoxBase, where it is @since 1.150 - it is the subject of this sample, so all three variants keep it 1:1 and the port needs a UI5 runtime >=` &&
                 ` 1.150.` ) ).

    text1 = `POST-1.71: showSelectAll (declared POST_171 BY POLICY; corrected 2026-08-23 - the figure 1.111 stood here and the sources do not carry it: sap/m/MultiComboBox.js declares showSelectAll with no @since` &&
            ` and ui5/properties.json records none, so the linter reports nothing for this entry to excuse. Kept as the AGENTS section 5 by-policy route for a member no gate can see) is the sample's whole point` &&
            ` and kept 1:1 - the app needs a UI5 release >= 1.111 for the select-all checkbox in the picker. // NOTE: both toasts round-trip instead of being composed on the client. selectionChange carries the` &&
            ` changed item's text and the selected flag as event args and ABAP builds the same sentence; selectionFinish needs the whole selection, so the MultiComboBox gains a selectedKeys binding (added, no` &&
            ` counterpart in the original, whose handler reads the selectedItems EVENT PARAMETER (corrected 2026-08-23; it used to say "reads getSelectedItems in the controller", and that control getter appears` &&
            ` nowhere in the sample). The distinction carries the justification: an event parameter can partly ride the event-arg grammar while a control getter cannot - the added binding is still right here,`.
    text1 = text1 && ` because an Item[] cannot be joined into names by that grammar) and ABAP joins the matching product names into the original's ['A','B'] form. The round-trip also carries the original's width: 'auto'` &&
            ` toast option, which the client-composed toast wire cannot pass. // NOTE: live-verified on 2026-08-02 (maintainer live check): the selectionFinish toast lists the selected names and Select All fills` &&
            ` the selectedKeys binding. The selectionChange leg is additionally e2e-verified for the BRANCH and the prefix only - restated 2026-08-23: the module asserts "Event 'selectionChange': Selected '",` &&
            ` which stops at the opening quote, so a toast whose item text arrived empty would pass it. The Selected/Deselected branch is genuinely covered; the transported item text is not. The selectionFinish` &&
            ` leg is NOT machine-checkable: it fires only when the picker CLOSES, and headless neither F4 nor Escape reaches the picker once focus sits in the item list, an outside click does not dismiss it and` &&
            ` getPicker() is null on the registry instance (measured 2026-08-02) - the armed interaction covers the selectionChange leg only. // NOTE: The selectionChange toast moved to the whitelisted global call`.
    text1 = text1 && ` on 2026-09: width is a sap.m.MessageToast option and left message_toast_display( ), so it now travels in the option object of the call, which is the MessageToast.show( ) API 1:1.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxSelectAll`                        class = `z2ui5_cl_smpc_app_281` path = `src/02/01/z2ui5_cl_smpc_app_281.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        since = `1.22.0`
        is_post171 = abap_true
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1
        post171 = `showSelectAll (declared POST_171 BY POLICY; corrected 2026-08-23 - the figure 1.111 stood here and the sources do not carry it: sap/m/MultiComboBox.js declares showSelectAll with no @since and` &&
                 ` ui5/properties.json records none, so the linter reports nothing for this entry to excuse. Kept as the AGENTS section 5 by-policy route for a member no gate can see) is the sample's whole point and` &&
                 ` kept 1:1 - the app needs a UI5 release >= 1.111 for the select-all checkbox in the picker.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxSuggestionsAndValueState`         class = `z2ui5_cl_smpc_app_519` path = `src/02/01/z2ui5_cl_smpc_app_519.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = `NOTE: handleValueStateLinkPress raises MessageToast.show("Link in value state pressed") in the original; the two FormattedText value-state links raise a constant client-composed toast here so the wire` &&
                 ` is not dead. // POST-1.71: MultiComboBox has no formattedValueStateText aggregation before 1.78 - newer than the 1.71 floor. The two FormattedText value states with their links are the point of the` &&
                 ` sample, so the aggregation is kept and the port is filed under src/02. // NOTE: The six value states and the two FormattedText value-state texts with their links are unverified in a running system.` &&
                 ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_519.mjs).`
        post171 = `MultiComboBox has no formattedValueStateText aggregation before 1.78 - newer than the 1.71 floor. The two FormattedText value states with their links are the point of the sample, so the aggregation is` &&
                 ` kept and the port is filed under src/02.` )
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxTwoColumnsLayout`                 class = `z2ui5_cl_smpc_app_458` path = `src/01/01/z2ui5_cl_smpc_app_458.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0` )
      ( module = `sap.m`              control = `sap.m.MultiComboBox`                   name = `MultiComboBoxWrapping`                         class = `z2ui5_cl_smpc_app_385` path = `src/01/01/z2ui5_cl_smpc_app_385.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0` ) ).

    text1 = `NOTE: the controller's onInit pre-sets the tokens on both MultiInputs (Token 1..6 and one long token); they are declared statically in the view's tokens aggregation instead - same rendering` &&
            ` (CAPABILITIES.md marks controller-filled aggregations as expressible, the tokens aggregation is comfortably pre-1.71, so no POST_171 is owed. **Corrected 2026-08-23**: this used to name 'UI5 1.16', a` &&
            ` figure the sources do not carry - sap/m/MultiInput.js declares the tokens aggregation with no @since at all, and the control itself carries none either, so the correct reading is base-version. The` &&
            ` nearest sourced figure is sap.m.Tokenizer @since 1.22, which renders the aggregation, so the aggregation cannot predate 1.22), so this is a faithful 1:1, not a workaround. // NOTE: the controller's` &&
            ` onInit addValidator on multiInput1 and multiInput2 (typing free text + Enter -> new Token({key: text, text})) is wired via the bundled invisible companion control z2ui5.cc.MultiInputExt` &&
            ` (xmlns:z2ui5="z2ui5.cc"): one MultiInputExt per input, referencing it by MultiInputId - the control installs exactly the original's validator (source-verified in app/webapp/cc/MultiInputExt.js). The`.
    text1 = text1 && ` two MultiInputExt elements are extra vs the original view.xml (there the validator lives in the controller); first cc-control usage in these ports (converted 2026-07-18). // NOTE: The original's` &&
            ` stray placeholder attributes on the two Labels (not a Label property) are dropped. // POST-1.71: showClearIcon (since UI5 1.94) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5` &&
            ` release >= 1.94 to render it.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInput`                                    class = `z2ui5_cl_smpc_app_040` path = `src/02/01/z2ui5_cl_smpc_app_040.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed); live-checked reference example for: cc control` &&
                 ` (MultiInputExt), bound aggregation, tokens, sorter binding-info. **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_040.mjs: multiInput1 renders its six pre-set` &&
                 ` tokens and free text committed with Enter becomes a seventh through the z2ui5.cc.MultiInputExt validator; typing Notebook into multiInput opens the bound suggestions and picking one adds it as a` &&
                 ` token)`
        notes = text1
        post171 = `showClearIcon (since UI5 1.94) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.94 to render it.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputCustomFiltering`                     class = `z2ui5_cl_smpc_app_478` path = `src/01/01/z2ui5_cl_smpc_app_478.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `IMPROVISED: onInit gives both MultiInputs a JavaScript setFilterFunction - a case-insensitive starts-with on the first, a contains on the second. A JS filter callback has no bindable or backend` &&
                 ` equivalent (the app-authored-JS-function boundary), so both suggestion lists carry UI5's default filtering and the difference between the two MultiInputs the sample demonstrates is lost.` )
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputDatabinding`                         class = `z2ui5_cl_smpc_app_457` path = `src/01/01/z2ui5_cl_smpc_app_457.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `POST-1.71: sap.m.MultiInput (through sap.m.InputBase) has the formattedValueStateText aggregation only @since 1.78 - newer than the 1.71 floor, but the third MultiInput exists to demonstrate exactly` &&
            ` that value state message with a Link inside it, so it is kept 1:1. The app needs a UI5 release >= 1.78. // NOTE: the controller registers a JS validator with MultiInput.addValidator( ) on multiInput2` &&
            ` and multiInput3: picking a suggestion row builds a Token whose key is the row's first cell (Name) and whose text is 'key(price cell)'. The bundled z2ui5.cc.MultiInputExt companion installs exactly` &&
            ` that validator, so each tabular MultiInput carries one alongside it with TokenKeyCell 0 (Name) and TokenTextCells 3 (the Price cell), and the token is built on the client with the same key and` &&
            ` 'Name(Price Currency)' text as the original. The two companion tags are the only addition vs the original view.xml, and the tokens stay client-side exactly as they do there. multiInput4 has no` &&
            ` validator in the sample either - a core:Item suggestion becomes a token by itself. // NOTE: handleFormattedTextLinkPress calls oEvent.preventDefault( ) and then MessageToast.show with my/at =`.
    text1 = text1 && ` Popup.Dock.CenterCenter. The veto rides along on the wire as s_ctrl-check_prevent_default (baked per wire at render time), the toast is composed on the way back through follow_up_action` &&
            ` control_global MESSAGE_TOAST show. The CenterCenter placement is not reachable through that global target, so the toast keeps its default bottom placement. // NOTE: the first MultiInput's` &&
            ` binding-info carries the sample's sorter { path: 'Name' } 1:1 as sorter { path: 'NAME' } over the backend table; the sorted 123 rows come from sap/ui/documentation/sdk/products.json` &&
            ` /ProductCollection. // NOTE: the e2e interaction module now covers what this deviation used to name: the z2ui5.cc.MultiInputExt companions are asserted on both tabular MultiInputs with TokenKeyCell 0` &&
            ` and TokenTextCells 3, their tokenFromRow( ) builds the sample's token from a real bound suggestion row, the tokens aggregation is empty with no binding behind it, and the Link inside the value state` &&
            ` message fires its preventDefault wire and raises the toast (2026-08-23, headless). What stays unverified in a running system is the interaction a person performs: opening the suggestion popover and`.
    text1 = text1 && ` picking a row so that MultiInput itself calls the registered validator - the assertion reaches the validator's code through the companion rather than through the popup, which measures zero unthemed.` &&
            ` **e2e-verified 2026-08-26** (meta/interactions/z2ui5_cl_smpc_app_612.mjs, four consecutive green runs against a backend built from abap2UI5 main tip ddbdd13 via A2UI5_BRANCH=main - the canary path` &&
            ` the nightly uses, not the A2UI5_PIN one, because the pinned framework of that day predated the TokenKeyCell/TokenTextCells properties this port needs; the pin caught up on 2026-08-28). // NOTE: The` &&
            ` price cell keeps the original's composite sap.ui.model.type.Currency binding rather than binding the raw field. The type is not a pass-through: NumberFormat's currency instance applies grouping and` &&
            ` the currency's own fraction digits, so EUR renders 956.00 / 1,249.00 where the bare field renders 956 / 1249. The earlier justification - that the fold is what the formatter renders because the` &&
            ` mock's prices are plain numbers - was wrong on both halves.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputFilteringSuggestions`                class = `z2ui5_cl_smpc_app_612` path = `src/02/01/z2ui5_cl_smpc_app_612.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.MultiInput (through sap.m.InputBase) has the formattedValueStateText aggregation only @since 1.78 - newer than the 1.71 floor, but the third MultiInput exists to demonstrate exactly that value` &&
                 ` state message with a Link inside it, so it is kept 1:1. The app needs a UI5 release >= 1.78.` ) ).

    text1 = `NOTE: the grouping is the sample: both MultiInputs bind their suggestions with sorter { path: 'SupplierName', group: true, ascending: false }, which the port carries over verbatim as sorter { path:` &&
            ` 'SUPPLIERNAME', group: true, ascending: false } on the backend table, so UI5 builds the descending supplier group headers on the client exactly as before. // NOTE: the controller registers a JS` &&
            ` validator with MultiInput.addValidator( ) on productMIWithTable: picking a suggestion row builds a Token whose key is the row's first cell (Name) and whose text is 'key(price cell)'. The bundled` &&
            ` z2ui5.cc.MultiInputExt companion installs exactly that validator, so the MultiInput carries one alongside it with TokenKeyCell 0 (Name) and TokenTextCells 3 (the Price cell), and the token is built` &&
            ` on the client with the same key and 'Name(Price Currency)' text as the original. That companion tag is the only addition vs the original view.xml, and the tokens stay client-side exactly as they do` &&
            ` there. productMIWithList has no validator in the sample either - a core:Item suggestion becomes a token by itself. // NOTE: the controller's oModel.setSizeLimit(1000000) has no counterpart: the port`.
    text1 = text1 && ` ships all 123 rows of sap/ui/demo/mock/products.json /ProductCollection to the client in one model, so the 100-entry default limit the call works around never applies. // NOTE: the e2e interaction` &&
            ` module now covers what this deviation used to name: the grouping sorter is asserted through the sap.ui.core.SeparatorItem entries it produces in both suggestion aggregations, and the` &&
            ` z2ui5.cc.MultiInputExt companion is asserted on productMIWithTable with TokenKeyCell 0 and TokenTextCells 3, its tokenFromRow( ) building the sample's token from a real bound suggestion row with an` &&
            ` empty, unbound tokens aggregation behind it (2026-08-23, headless). What stays unverified in a running system is the interaction a person performs: opening either suggestion popover, seeing the grey` &&
            ` supplier group headers rendered in it, and picking a row so that MultiInput itself calls the registered validator. **e2e-verified 2026-08-26** (meta/interactions/z2ui5_cl_smpc_app_613.mjs, four` &&
            ` consecutive green runs against a backend built from abap2UI5 main tip ddbdd13 via A2UI5_BRANCH=main - the canary path the nightly uses, not the A2UI5_PIN one, because the pinned framework of that day`.
    text1 = text1 && ` predated the TokenKeyCell/TokenTextCells properties this port needs; the pin caught up on 2026-08-28). // NOTE: The price cell keeps the original's composite sap.ui.model.type.Currency binding rather` &&
            ` than binding the raw field. The type is not a pass-through: NumberFormat's currency instance applies grouping and the currency's own fraction digits, so EUR renders 956.00 / 1,249.00 where the bare` &&
            ` field renders 956 / 1249. The earlier justification - that the fold is what the formatter renders because the mock's prices are plain numbers - was wrong on both halves.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputGrouping`                            class = `z2ui5_cl_smpc_app_613` path = `src/01/01/z2ui5_cl_smpc_app_613.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputMaxTokens`                           class = `z2ui5_cl_smpc_app_461` path = `src/01/01/z2ui5_cl_smpc_app_461.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: the port adds one invisible control the original view does not declare - z2ui5:MultiInputExt (xmlns:z2ui5="z2ui5.cc"), which structural-diff reports as control extra z2ui5:MultiInputExt. It is` &&
            ` where the original's onInit lives; the app 040 shape. Until 2026-08-26 the extra control here was a Token template instead, because the port bound the tokens aggregation to the model - it no longer` &&
            ` does, so the port's MultiInput now declares exactly what the sample's does (width, id, suggestionItems + core:Item, showValueHelp). // NOTE: onInit's addValidator( function(args){ return new` &&
            ` Token({key: args.text, text: args.text}); } ) and its attachTokenUpdate handler are both wired through the bundled invisible companion control z2ui5.cc.MultiInputExt (app 040 idiom): MultiInputId` &&
            ` points it at the input, and it installs that validator VERBATIM - z2ui5_cl_ui5f_multiinp_js.clas.abap:154 is input.addValidator((args) => ... new Token({ key: args.text, text: args.text })), the same` &&
            ` free-text branch this sample registers. This deviation used to read "abap2UI5 cannot register a validator", which was wrong (CAPABILITIES.md marks addValidator expressible, apps 040/612/613 use it,`.
    text1 = text1 && ` and apps 501/504 already carried the corrected wording), and it protected two defects: the port improvised token creation over a change round-trip, so a token only appeared AFTER the server answered,` &&
            ` and its tokenUpdate wire read ${$parameters>/removedTokens}[0].getKey() - only the FIRST key of a multi-token removal. The companion mirrors the WHOLE added / removed list into the bindable` &&
            ` addedTokens / removedTokens properties (as {KEY, TEXT} rows) and fires change, so the backend now replays both branches of the original's handler over the full lists and the model stays in sync for` &&
            ` any number of tokens. What is still not the original: the bundled validator is FIXED, so this route could not express a sample with per-field accept/reject logic (apps 501/504) - it happens to be` &&
            ` exactly this sample's validator. // NOTE: _textFormatter and _keyFormatter only prefix the two values with 'text: ' and 'key: '. A formatter is business logic, so both strings are computed in model` &&
            ` state and bound as plain fields. // NOTE: Adding a token and removing it again (with the List following) is unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_512.mjs). The MultiInputExt rewrite of 2026-08-26 changes how that same behaviour is produced and needs a fresh e2e build before it is re-verified.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputModelUpdate`                         class = `z2ui5_cl_smpc_app_512` path = `src/01/01/z2ui5_cl_smpc_app_512.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The bound tokens aggregation adds a Token template the original's view does not declare (it builds its Tokens in JavaScript), which structural-diff reports as control extra Token. // IMPROVISED:` &&
            ` The original registers an app-authored addValidator callback. abap2UI5 does ship a validator - the bundled companion control z2ui5.cc.MultiInputExt installs addValidator(({text}) => new` &&
            ` Token({key:text, text})) and mirrors added/removed tokens back through addedTokens/removedTokens (CAPABILITIES.md marks it fine, and apps 040/612/613 use it). What it cannot do is run THIS sample's` &&
            ` per-field logic, which is what is improvised here: the bundled validator is fixed, so the port rebuilds the accept/reject decision in ABAP over a change round-trip instead. // NOTE: The validator` &&
            ` switch, both timers and the tokenUpdate toast are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_504.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputTokenUpdate`                         class = `z2ui5_cl_smpc_app_504` path = `src/01/01/z2ui5_cl_smpc_app_504.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `IMPROVISED: The original registers an app-authored addValidator callback. abap2UI5 does ship a validator - the bundled companion control z2ui5.cc.MultiInputExt installs addValidator(({text}) => new` &&
            ` Token({key:text, text})) and mirrors added/removed tokens back through addedTokens/removedTokens (CAPABILITIES.md marks it fine, and apps 040/612/613 use it). What it cannot do is run THIS sample's` &&
            ` per-field logic, which is what is improvised here: the bundled validator is fixed, so the port rebuilds the accept/reject decision in ABAP over a change round-trip instead. // NOTE: The three` &&
            ` MultiInputs get a bound tokens aggregation with a Token template each (structural-diff reports control extra Token: 0 vs 3) - the original creates its Tokens in JavaScript, so its view declares none.` &&
            ` // NOTE: All three token-creation paths, including the confirm round-trip of the second field, are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_501.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputValidators`                          class = `z2ui5_cl_smpc_app_501` path = `src/01/01/z2ui5_cl_smpc_app_501.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: handleValueHelp is reproduced 1:1 through two follow-up actions in the order the controller has them: cs_event-binding_call filters the dialog's items binding by NAME Contains the typed value` &&
            ` (the model stays untouched, exactly like oValueHelpDialog.getBinding('items').filter([...])), then cs_event-control_by_id open( value ) opens it with that same value in its search field -` &&
            ` CONTROL_METHODS declares open's optional string argument, so oValueHelpDialog.open(sInputValue) travels whole. The MultiInput's value is bound two-way so the typed text is on the server when` &&
            ` valueHelpRequest fires. The dialog's own search event keeps the roundtrip-free follow_up_action binding_call form (app 103 idiom). // NOTE: _handleValueHelpClose reads` &&
            ` evt.getParameter('selectedItems') and adds one sap.m.Token per item. A list of CONTROLS cannot travel to the backend, so the selection is read from the data instead: the StandardListItem template` &&
            ` gains a selected={SELECTED} binding (an added attribute) and the handler loops the rows, appends a token per selected row and clears the flag again. The MultiInput's tokens aggregation is bound to`.
    text1 = text1 && ` that token table, so the Token in the view is a template the original does not have (it builds Tokens in the controller) - one control more than the original view.xml. // NOTE: the suggestionItems` &&
            ` binding keeps the original's { path, sorter: { path: 'Name' } } shape 1:1 as a raw binding-info string, with the path from client->_bind and the sorter path switched to the ABAP field NAME. The` &&
            ` Component's oModel.setSizeLimit(1000000) is ported as a cs_event-set_size_limit for MAIN. It is NOT without a counterpart, which is what this note used to claim: the ABAP table does travel in full,` &&
            ` but the client-side JSONModel caps a BINDING at 100 independently of that, and Input.updateSuggestionItems reaches updateAggregation with no explicit length - so without the call the last 23 of the` &&
            ` 123 products never render as suggestions. The SelectDialog on the same view needs no second call: SelectDialog.growing defaults true and GrowingEnablement always passes a length, and` &&
            ` ClientListBinding applies the size limit only when the length is falsy. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an`.
    text1 = text1 && ` abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and` &&
            ` RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it` &&
            ` now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count` &&
            ` was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputValueHelp`                           class = `z2ui5_cl_smpc_app_290` path = `src/01/01/z2ui5_cl_smpc_app_290.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputValueStates`                         class = `z2ui5_cl_smpc_app_386` path = `src/01/01/z2ui5_cl_smpc_app_386.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.MultiInput`                      name = `MultiInputWrapping`                            class = `z2ui5_cl_smpc_app_387` path = `src/01/01/z2ui5_cl_smpc_app_387.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `POST-1.71: NavContainer.navigationFinished (event, since UI5 1.111.0) is kept 1:1 - it is wired to a client-composed MessageToast; the app needs a UI5 release >= 1.111 to fire it. // NOTE: handleNav` &&
            ` does navCon.to( byId(target), animationSelect.getSelectedKey() ) / navCon.back() imperatively. The port keeps the NavContainer id='navCon' and drives navigation via a round-trip: each To-button's` &&
            ` press (NAV) carries its static target page id (p1..p4) as the event arg, and on_event issues follow_up_action( val = cs_event-control_by_id t_arg = ( 'navCon' )( 'to' )( <target> )( animation ) ) -` &&
            ` 'to' is whitelisted with kinds [controlId, string], so the target page and the transition name both reach the method; Back issues ( 'navCon' )( 'back' ). A server round-trip where the original is a` &&
            ` purely client-side imperative call, but faithful. // NOTE: the animation transition comes from a Select whose getSelectedKey() the original reads at click time. The port two-way binds the Select's` &&
            ` selectedKey to a model field {/ANIMATION} (seeded to the first item's key 'slide'), so the current transition is available to the backend on the NAV round-trip. The original Select has no selectedKey`.
    text1 = text1 && ` attribute (it defaults to the first item), so adding the bound selectedKey is an extra attribute (structural-diff only flags MISSING attributes) and is the faithful thin-frontend form of the` &&
            ` imperative getSelectedKey(). // NOTE: the sample's style.css (a border around the NavContainer via the .navContainerControl class) is injected via an added core:HTML leaf carrying a <style> block in` &&
            ` its content attribute (CAPABILITIES 'Custom CSS'); the literal CSS braces are escaped. structural-diff reports this core:HTML as an extra control (named here); the .navContainerControl class stays on` &&
            ` the NavContainer. // NOTE: the four To-buttons keep their core:CustomData (key='target') element 1:1 for structural fidelity, but the port transports the target page id via the button's event arg (a` &&
            ` static literal per button) rather than reading evt.getSource().data('target') - identical result since the target is fixed per button. // NOTE: unverified in a running system: that navCon.to()/back()` &&
            ` navigate the four pages with the selected transition (follow_up_action cs_event-control_by_id 'to'/'back'), that the two-way bound animation Select is applied before NAV runs, and that`.
    text1 = text1 && ` navigationFinished fires the client-composed toast with the resolved page title. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the 'To 2' button navigates to` &&
            ` Page 2 via the control_by_id to() call; the transition-type select and back() remain unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_242.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NavContainer`                    name = `NavContainer`                                  class = `z2ui5_cl_smpc_app_242` path = `src/02/01/z2ui5_cl_smpc_app_242.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `NavContainer.navigationFinished (event, since UI5 1.111.0) is kept 1:1 - it is wired to a client-composed MessageToast; the app needs a UI5 release >= 1.111 to fire it.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NewsContent`                     name = `NewsContent`                                   class = `z2ui5_cl_smpc_app_063` path = `src/01/01/z2ui5_cl_smpc_app_063.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34` ) ).

    text1 = `NOTE: _createNotification builds every NotificationListItem in JavaScript with Math.random over three lists (four titles, three datetimes, four Priority members). abap2UI5 has no client-side control` &&
            ` factory and a backend cannot repeat a client-side random draw, so the port binds the items aggregation to a table it fills with the same three lists walked in order (title index MOD 4 plus the` &&
            ` running number, datetime MOD 3, priority MOD 4). Same 400 rows, same value ranges, deterministic instead of random. // NOTE: iMaxNotifications is 400 on a desktop and 100 otherwise. The row count is` &&
            ` decided in the backend, which does not know the device at that point, so the port always loads 400 - the number the sample's own title (maximum number of notifications reached) is about. // NOTE:` &&
            ` Each created item closes itself with this.destroy(); the port's item close deletes the row it carries (the id travels as an event arg), which is the same disappearance on a bound aggregation. The` &&
            ` group's own close (onItemClose: removeItem(group) plus the 'Item Closed: <title>' toast) becomes a bound visible flag plus message_toast_display, so the row set stays untouched. // NOTE: The load`.
    text1 = text1 && ` wire (400 rows into the bound aggregation), the per-item close and the group close are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_444.mjs). // NOTE: 400 rows is the sample's whole subject, and only 100 of them rendered. sap.ui.model.Model's constructor sets iSizeLimit = 100 and abap2UI5` &&
            ` leaves it there unless the app raises it. The original never meets that limit because it uses no model at all - onLoadNotificationsPress calls addItem( ) 400 times with real control instances, and a` &&
            ` size limit applies to a BOUND aggregation, not to added children. The load branch now issues cs_event-set_size_limit with 400 (the app-252 / app-094 idiom). Nothing could have caught this: the e2e` &&
            ` module asserts only that some .sapMNLI rendered, which a 100-row render passes just as happily.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NotificationListGroup`           name = `MaxNumberOfNotificationsReached`               class = `z2ui5_cl_smpc_app_444` path = `src/01/01/z2ui5_cl_smpc_app_444.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: onItemClose is reproduced 1:1 since 2026-08-05. The original removes the item from its list (oList.removeItem(oItem)) and toasts its title; the port does BOTH client-side on the same event.` &&
            ` **Corrected 2026-08-23 - it was 1:1 for 4 of the 15 close handlers and a SILENT NO-OP for the other 11.** The original resolves the parent per item (oItem.getParent()), and` &&
            ` sap.m.NotificationListGroup declares its own ``items`` aggregation, so a NESTED NotificationListItem's parent is the GROUP, not the outer list. All fifteen wires had been hard-coded to the list id:` &&
            ` for the four group closes that is right, and for the eleven item closes ManagedObject.removeAggregation looped the list's four groups, matched nothing, and returned null - no error, no log, the item` &&
            ` stayed on screen and only the toast fired. Every gate stayed green because the call fails silently. The four groups now carry ids (notificationGroup1..4) and each nested item removes itself from its` &&
            ` own group. The probe this had leaned on measured a FLAT Tokenizer, which is why a two-level aggregation was never covered.: UI5 runs a ';'-separated pair of event handlers (measured with`.
    text1 = text1 && ` scripts/probes/event-arg-expression-probe.mjs), so the wire chains control_by_id removeItem with the client-composed toast. The item travels as its own id ($event.oSource.getId()), which` &&
            ` ManagedObject.removeAggregation accepts (measured in the same probe). The NotificationList gained an ``id`` the original does not carry - removeItem needs a target for the wire; that is the port's` &&
            ` only extra attribute here. The earlier 'static items, so close only toasts' rationale is retired. Group 3's onAcceptErrors keeps its accept toast. // NOTE: the original's showCloseButton="falseue"` &&
            ` typo on two items is corrected to false, and it is a readability fix rather than a repair - corrected 2026-08-23: UI5 does not reject 'falseue'. DataType's boolean parses ``sValue == "true"``, so the` &&
            ` typo silently coerces to false and renders exactly as the corrected value does. // POST-1.71: the NotificationList container control (since UI5 1.90) is newer than 1.71 but kept for the 1:1 port -` &&
            ` the app needs a UI5 release >= 1.90 to render it (control-level, invisible to the member-level property gate). // IMPROVISED: Group 3's onAcceptErrors is reduced to the same generic 'Accept Button`.
    text1 = text1 && ` Pressed' toast the other groups fire. The original shows NO toast there: it clears the processingMessage aggregation on all three items and then puts an error MessageStrip (with the 'SAP CE' link) on` &&
            ` a RANDOMLY chosen one - which is what that group's own title announces, "When 'Accept All' is pressed some of the notifications will show an error". So the port loses the behaviour the group exists` &&
            ` to demonstrate and invents a toast in its place, leaving the title describing something the app does not do. Declared 2026-08-23; the sidecar had said 'Group 3's onAcceptErrors keeps its accept` &&
            ` toast', and there was no accept toast to keep.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NotificationListGroup`           name = `NotificationListGroup`                         class = `z2ui5_cl_smpc_app_077` path = `src/02/01/z2ui5_cl_smpc_app_077.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `the NotificationList container control (since UI5 1.90) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.90 to render it (control-level, invisible to the member-level` &&
                 ` property gate).` ) ).

    text1 = `POST-1.71: sap.m.NotificationList (control since UI5 1.90) is the container the sample binds its groups to and is kept 1:1, like the other notification ports (076/077). templateShareable: true is a` &&
            ` binding-info parameter, not a control member, so no gate can see it - it is kept verbatim on all four aggregation bindings and declared here by policy (apps 264/265 precedent). The app needs a UI5` &&
            ` release >= 1.90. // NOTE: three levels of bound aggregation, which is what the sample is about: NotificationList.items over the groups, NotificationListGroup.items over that group's items, and` &&
            ` buttons over the group's and the item's own button rows. The ABAP model mirrors that as nested tables (ty_t_group > groupitems ty_t_item > itembuttons ty_t_button), and every relative binding inside` &&
            ` a template addresses its own row. // NOTE: four of the five controller handlers compose their toast from the pressed control and stay on the client, roundtrip-free: onGroupClose and onListItemPress` &&
            ` with ${$source>/title}, onGroupButtonPress and onItemButtonPress with ${$source>/text}. Only onItemClose needs the backend, because it REMOVES the row (oList.removeItem): the item's own title travels`.
    text1 = text1 && ` as a $-prefixed event arg and the handler deletes that row from every group before pushing the model back. The titles are unique in this data; a real app would carry a key. // NOTE: the` &&
            ` priorityFormatter is dropped. It maps a value that is not a sap.ui.core.Priority to Priority.None - presentation logic that the thin-frontend rule puts in the backend, and the mock's priorities` &&
            ` (High, Low) are all valid anyway, so the port binds PRIORITY directly. A model_init that seeded an unknown value would have to map it there. // NOTE: the item rows bind datetime={CREATIONDATE}, but` &&
            ` only the GROUPS carry a creationDate in the mock and the group template does not bind it - so the field exists on the item row type and stays empty on both items, exactly as the original renders it.` &&
            ` // NOTE: The two groupItems rows carry showclosebutton = abap_true since 2026-08-21, though the mock gives them no showCloseButton key at all. That is what reproduces the original: an ABSENT JSON` &&
            ` property leaves UI5 to fall back on the control's own default, and sap.m.NotificationListBase declares showCloseButton with defaultValue true, so both items render a close button there. An ABAP`.
    text1 = text1 && ` abap_bool has no absent state - it serialises as a real JSON false - so leaving the field initial did the opposite of the original and, with it, made the ITEM_CLOSE wire unreachable: there was no` &&
            ` button left to press. Found by the review sweep; the sibling absent property creationDate is handled in its own deviation above.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NotificationListGroup`           name = `NotificationListGroupBindings`                 class = `z2ui5_cl_smpc_app_291` path = `src/02/01/z2ui5_cl_smpc_app_291.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.NotificationList (control since UI5 1.90) is the container the sample binds its groups to and is kept 1:1, like the other notification ports (076/077). templateShareable: true is a binding-info` &&
                 ` parameter, not a control member, so no gate can see it - it is kept verbatim on all four aggregation bindings and declared here by policy (apps 264/265 precedent). The app needs a UI5 release >=` &&
                 ` 1.90.` ) ).

    text1 = `NOTE: onToggleCollapse fills a group with three NotificationListItems the first time it is expanded - the port keeps that laziness: each group binds its own table and the onCollapse wire fills it in` &&
            ` ABAP when the group is expanded and still empty. // NOTE: _addItemsToGroup picks title, datetime and priority at RANDOM from three lists. A backend cannot repeat a client-side random draw, so the` &&
            ` port walks the same three lists in order - same values, deterministic instead of random (apps 444/467 precedent). // NOTE: onItemClose removes the item and toasts; onGetItemsCount toasts the group's` &&
            ` item count - both are round-trips over the bound tables. Accept All and Reject All toast constant texts and are composed on the client. The group's own close empties its table, which is the closest` &&
            ` equivalent of removing the group from a static list. // POST-1.71: The sap.m.NotificationList container is @since 1.90 - newer than the 1.71 floor. The sample's two groups live in it, so it is kept` &&
            ` and the port is filed under src/02. // NOTE: The lazy fill on expand, the per-item close, the count toast and the group close are unverified in a running system. **e2e-verified 2026-08-25** (nightly`.
    text1 = text1 && ` e2e interaction, meta/interactions/z2ui5_cl_smpc_app_520.mjs). // NOTE: _addItemsToGroup creates every NotificationListItem with unread: true. unread is inherited from sap.m.ListItemBase with` &&
            ` defaultValue false, so the row template sets it explicitly - without it every notification rendered read (no unread indicator, non-bold title) where the original renders all three unread. Also: the` &&
            ` group's own close reuses onItemClose upstream, so closing a group toasts there too; the port's GROUP_CLOSE empties the table without the toast.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NotificationListGroup`           name = `NotificationListGroupLazyLoading`              class = `z2ui5_cl_smpc_app_520` path = `src/02/01/z2ui5_cl_smpc_app_520.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `The sap.m.NotificationList container is @since 1.90 - newer than the 1.71 floor. The sample's two groups live in it, so it is kept and the port is filed under src/02.` ) ).

    text1 = `NOTE: onItemClose is reproduced 1:1 since 2026-08-05. The original removes the item from its list (oList.removeItem(oItem)) and toasts its title; the port does BOTH client-side on the same event: UI5` &&
            ` runs a ';'-separated pair of event handlers (measured with scripts/probes/event-arg-expression-probe.mjs), so the wire chains control_by_id removeItem with the client-composed toast. The item travels` &&
            ` as its own id ($event.oSource.getId()), which ManagedObject.removeAggregation accepts (measured in the same probe). The NotificationList gained an ``id`` the original does not carry - removeItem` &&
            ` needs a target for the wire; that is the port's only extra attribute here. The earlier 'static items, so close only toasts' rationale is retired. // POST-1.71: the NotificationList container control` &&
            ` (since UI5 1.90) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.90 to render it (control-level, invisible to the member-level property gate). // NOTE: the sample's` &&
            ` demo-kit authorPicture image paths (test-resources/sap/m/images/Woman_04.png, headerImg2.jpg, female_BaySu.jpg) are resolved to absolute sdk.openui5.org URLs. // NOTE: all toasts were switched to`.
    text1 = text1 && ` roundtrip-free client-composed control_global toasts on 2026-07-22 (the app is now init-only) - re-verify press/close/accept/reject/error each toast their text. **e2e-verified 2026-07-30**` &&
            ` (transpiled-framework interaction, scripts/e2e-smoke.mjs): the 'Accept' footer button toasts 'Accept Button Pressed'; item press/close toasts are the identical wire but unexercised. Scope, added` &&
            ` 2026-08-23: since the 2026-08-05 close rewire that is no longer true of CLOSE - it is a chained pair whose control_by_id/removeItem half no interaction module exercises anywhere. It is correct here` &&
            ` because every item is a direct child of the NotificationList, which is exactly what made the same wire wrong in app 077. **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_076.mjs). // IMPROVISED: onErrorPress is reduced to a toast. The original builds a MessageStrip (type Error, showIcon, showCloseButton, text 'Error: Something went` &&
            ` wrong.' and a 'SAP CE' Link to sap.com) and puts it on the item via setProcessingMessage, where it PERSISTS; the port raises a transient client toast with the same text and loses the strip, its`.
    text1 = text1 && ` styling, the link and the processingMessage aggregation. Declared 2026-08-23 - the substitution was carried only by an inline comment and an audit note, neither of which is a deviation.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NotificationListItem`            name = `NotificationListItem`                          class = `z2ui5_cl_smpc_app_076` path = `src/02/01/z2ui5_cl_smpc_app_076.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `the NotificationList container control (since UI5 1.90) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.90 to render it (control-level, invisible to the member-level` &&
                 ` property gate).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.NumericContent`                  name = `NumericContentDifColors`                       class = `z2ui5_cl_smpc_app_156` path = `src/01/01/z2ui5_cl_smpc_app_156.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = `NOTE: The NumericContent / GenericTile presses show a client MessageToast ('The numeric content is pressed.'), matching the original press handler. Four NumericContents (value colors` &&
                 ` Good/Critical/Error, indicators) plus a GenericTile > TileContent > NumericContent are reproduced 1:1. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): pressing` &&
                 ` the first NumericContent toasts 'The numeric content is pressed.'; the other tiles are the identical wire.` )
      ( module = `sap.m`              control = `sap.m.NumericContent`                  name = `NumericContentIcon`                            class = `z2ui5_cl_smpc_app_064` path = `src/01/01/z2ui5_cl_smpc_app_064.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = `NOTE: the second NumericContent keeps the sample's original demokit test-resources image path (test-resources/sap/m/demokit/sample/NumericContentIcon/images/grass.jpg) as the icon literal 1:1;` &&
                 ` abap2UI5 does not serve that static asset, so it does not render offline (the first tile's sap-icon://travel-expense does). The image is archived under ui5/sap.m/NumericContentIcon/images/.` )
      ( module = `sap.m`              control = `sap.m.NumericContent`                  name = `NumericContentWithoutMargin`                   class = `z2ui5_cl_smpc_app_390` path = `src/01/01/z2ui5_cl_smpc_app_390.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectAttribute`                 name = `ObjectAttributeInTable`                        class = `z2ui5_cl_smpc_app_191` path = `src/01/01/z2ui5_cl_smpc_app_191.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = `NOTE: the JSONModel is defined inline in the original controller's onInit (an array of {product, supplier} rows under /modelData); it is moved verbatim into ABAP model_init (all 10 rows kept) and` &&
                 ` bound on the one default model. Pure prefix/root rename (/modelData -> the default model root), same data, renders identically.` ) ).

    text1 = `NOTE: element binding kept 1:1 - the two display ObjectAttributes bind a one-record structure /S_PRODUCT instead of {/ProductCollection/0}; record 0 fields verbatim. // NOTE: handleSAPLinkPressed's` &&
            ` URLHelper.redirect maps to the URLHELPER REDIRECT frontend action (cs_event-urlhelper); handleFeedbacklinkPressed's Dialog (a RatingIndicator + TextArea with Submit/Cancel Button) is rebuilt via` &&
            ` popup_display, the Submit button's 2s setBusy delay dropped. Corrected 2026-08-23 on two counts the sentence had passed over: the Cancel button now carries the original's type=Reject (Submit's Accept` &&
            ` was already there, so the pair had been asymmetric and Cancel lost its red styling), and the toast is no longer a bare one-liner. The original shows it ON the still-open dialog with duration 2000 and` &&
            ` center/center docking and closes the dialog from its onClose; the port had destroyed the popup first and toasted with every default. It now passes duration/my/at and names an onclose event that` &&
            ` destroys the popup, so the ORDER matches too. Only ``of`` stays dropped - it takes a live control and the framework hands the option to MessageToast unresolved, so a control id cannot travel in it.`.
    text1 = text1 && ` // POST-1.71: ObjectAttribute.ariaHasPopup (since UI5 1.97) is kept 1:1 on the feedback attribute; needs UI5 >= 1.97. // NOTE: The feedback toast moved to the whitelisted global call on 2026-09` &&
            ` (my/at are sap.m.MessageToast options and left message_toast_display( )). duration rides in the same option object; onClose stays a BACKEND event name - the frontend turns it into the round-trip that` &&
            ` closes the dialog, exactly as before.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectAttribute`                 name = `ObjectAttributes`                              class = `z2ui5_cl_smpc_app_073` path = `src/02/01/z2ui5_cl_smpc_app_073.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        is_post171 = abap_true
        notes = text1
        post171 = `ObjectAttribute.ariaHasPopup (since UI5 1.97) is kept 1:1 on the feedback attribute; needs UI5 >= 1.97.` ) ).

    text1 = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/0}; the port seeds those fields at the default-model root and binds them absolutely, so the ObjectHeader.binding attribute is dropped.` &&
            ` // NOTE: handleFeedbacklinkPressed builds a feedback Dialog (RatingIndicator + TextArea + Submit/Cancel) in the controller. The port builds the same dialog in a chain and shows it with popup_display,` &&
            ` which is why structural-diff reports Dialog, RatingIndicator, TextArea and two Buttons as control extra - the original's view declares none of them. The Submit button's busy-then-toast choreography` &&
            ` (setBusy, a 2-second MessageToast anchored on the dialog, then close) is reduced to the toast and the close. // NOTE: handleSAPLinkPressed calls URLHelper.redirect('http://www.sap.com', true) -` &&
            ` reproduced 1:1 through the global URLHELPER target. // POST-1.71: ObjectAttribute ariaHasPopup is @since 1.97.0 - newer than the 1.71 floor. It is kept 1:1 from the sample (the feedback attribute` &&
            ` announces its dialog), so the port is filed under src/02. // NOTE: The feedback dialog, its submit path and the URL redirect are unverified in a running system. **e2e-verified 2026-08-25** (nightly`.
    text1 = text1 && ` e2e interaction, meta/interactions/z2ui5_cl_smpc_app_518.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectAttribute`                 name = `ObjectHeaderActiveAttributes`                  class = `z2ui5_cl_smpc_app_518` path = `src/02/01/z2ui5_cl_smpc_app_518.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.12`
        is_post171 = abap_true
        notes = text1
        post171 = `ObjectAttribute ariaHasPopup is @since 1.97.0 - newer than the 1.71 floor. It is kept 1:1 from the sample (the feedback attribute announces its dialog), so the port is filed under src/02.` ) ).

    text1 = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/0}. The port seeds exactly those row-0 fields at the default-model root and binds them absolutely - the record is a fixed index, so no` &&
            ` bind_element follow-up is needed (abap2UI5 has one, cs_event-bind_element, for the per-row case); the relative {Field} form is NOT kept, because without an element binding on the view it would` &&
            ` resolve against nothing and render empty, so intro, number, numberUnit, the ObjectAttribute text, the tab count, the Image src and the dimension text all read the same mock values. // NOTE: The` &&
            ` number binding keeps its parts/type/formatOptions structure 1:1 with the paths switched to the ABAP field names, and the dimensions Text keeps the original's '{Width} x {Depth} x {Height} {DimUnit}'` &&
            ` shape as one composed template over the four bound fields. The picture URL is the mock's own, re-hosted on sdk.openui5.org. // NOTE: The Currency-typed number binding, the bound tab count and the` &&
            ` Attachment link's MessageBox.alert are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_453.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectAttribute`                 name = `ObjectHeaderResponsiveI`                       class = `z2ui5_cl_smpc_app_453` path = `src/01/01/z2ui5_cl_smpc_app_453.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    text1 = `NOTE: the ObjectHeader keeps the original element binding and relative field bindings 1:1 (title, numberUnit, the ObjectAttribute composite texts and the sap.ui.model.type.Currency number binding);` &&
            ` only the binding context path changes - a one-record structure /S_PRODUCT in the default model instead of {/ProductCollection/0}, since the port does not carry the whole collection. // NOTE: the` &&
            ` model holds exactly the bound record /ProductCollection/0 (Notebook Basic 15) of ui5/mock/products.json, verbatim - this is the original sample's own single-record binding {/ProductCollection/0}, not` &&
            ` a shortened data set. // NOTE: the active ObjectAttribute 'www.sap.com' opens via the URLHELPER REDIRECT frontend action (cs_event-urlhelper, { URL, NEW_WINDOW } object param) - not open_new_tab,` &&
            ` which is same-origin-only.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeader`                                  class = `z2ui5_cl_smpc_app_041` path = `src/01/01/z2ui5_cl_smpc_app_041.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        checked = `CHECKED (2026-07-19): verified in a running system - human visual pass 2026-07-19 over all apps: the element binding {/S_PRODUCT} resolves all relative field bindings incl. the Currency number - the` &&
                 ` ObjectHeader renders fully populated. **e2e-verified 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_041.mjs: the element-bound ObjectHeader resolves title, the Currency` &&
                 ` composite number 956.00 EUR, the three relative attribute texts and both statuses)`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderCircleImage`                       class = `z2ui5_cl_smpc_app_400` path = `src/01/01/z2ui5_cl_smpc_app_400.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = `NOTE: the ObjectHeader icon path test-resources/sap/m/images/Woman_04.png is kept verbatim and only host-absolutized to https://sdk.openui5.org/ per the runtime asset-URL rule, so the image resolves` &&
                 ` against the OpenUI5 host exactly as in the demo kit.` )
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderCondensed`                         class = `z2ui5_cl_smpc_app_201` path = `src/01/01/z2ui5_cl_smpc_app_201.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = `NOTE: the ObjectHeader keeps the original element binding and its relative field bindings 1:1 (title {Name}, condensed, numberUnit {CurrencyCode}, the ObjectAttribute composite text and the` &&
                 ` sap.ui.model.type.Currency number binding over Price/CurrencyCode); only the binding context path changes - a one-record structure /S_PRODUCT in the default model instead of {/ProductCollection/0},` &&
                 ` since the port does not carry the whole collection. // NOTE: the model holds exactly the bound record /ProductCollection/0 (Notebook Basic 15) of ui5/mock/products.json, verbatim - this is the` &&
                 ` original sample's own single-record binding {/ProductCollection/0}, not a shortened data set.` ) ).

    text1 = `NOTE: The ObjectHeader's single-record element binding attribute binding="{/ProductCollection/5}" is dropped: abap2UI5 serves one default model, so products.json row 5 is seeded directly onto the` &&
            ` model root (model_init) and the control's relative {NAME}/{PRICE}/… bindings resolve against it — same data, renders identically (single-record fold, AGENTS §5). **Corrected 2026-08-01**: those` &&
            ` bindings were written RELATIVE ({FIELD}) and a relative path on a control with no binding context resolves against nothing — the fields rendered EMPTY in the running app (measured with the e2e` &&
            ` harness, the app-207 class). They are now bound ABSOLUTELY through client->_bind( field ), which is what a root-seeded record needs. // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs` &&
            ` interaction, transpiled backend + real browser): the element-bound record resolves in the running app - the title 'Notebook Professional 15' and the bound description render. This is the port that` &&
            ` exposed the relative-binding defect (see the corrected NOTE above): it rendered EMPTY until the fields were bound absolutely. Residual: the Currency type's locale formatting of the number. (Corrected`.
    text1 = text1 && ` 2026-08-23: both asserted strings sit inside the ObjectAttribute's description literal, so the title binding is not independently covered.) // NOTE: The sample's asset paths are host-absolutized. The` &&
            ` demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise` &&
            ` the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the` &&
            ` corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what` &&
            ` made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderImage`                             class = `z2ui5_cl_smpc_app_206` path = `src/01/01/z2ui5_cl_smpc_app_206.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderMarkers`                           class = `z2ui5_cl_smpc_app_197` path = `src/01/01/z2ui5_cl_smpc_app_197.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = `NOTE: the Page keeps the original element binding and the ObjectHeader relative field bindings 1:1 (title {Name}, numberUnit {CurrencyCode}, the two ObjectAttribute composite texts and the` &&
                 ` sap.ui.model.type.Currency number binding over Price/CurrencyCode); only the binding context path changes - a one-record structure /S_PRODUCT in the default model instead of {/ProductCollection/0},` &&
                 ` since the port does not carry the whole collection. // NOTE: the model holds exactly the bound record /ProductCollection/0 (Notebook Basic 15) of ui5/mock/products.json, verbatim - this is the` &&
                 ` original sample's own single-record binding {/ProductCollection/0}, not a shortened data set.` ) ).

    text1 = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/0}. The port seeds exactly those row-0 fields at the default-model root and binds them absolutely - the record is a fixed index, so no` &&
            ` bind_element follow-up is needed (abap2UI5 has one, cs_event-bind_element, for the per-row case); the relative {Field} form is NOT kept, because without an element binding on the view it would` &&
            ` resolve against nothing and render empty, so intro, number, numberUnit, the ObjectAttribute text, the tab count, the Image src and the dimension text all read the same mock values. // NOTE: The` &&
            ` number binding keeps its parts/type/formatOptions structure 1:1 with the paths switched to the ABAP field names, and the dimensions Text keeps the original's shape as one composed template over the` &&
            ` four bound fields. The picture URL is the mock's own, re-hosted on sdk.openui5.org. // NOTE: The Currency-typed number binding, the bound tab count and the Attachment link's MessageBox.alert are` &&
            ` unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_513.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderResponsiveII`                      class = `z2ui5_cl_smpc_app_513` path = `src/01/01/z2ui5_cl_smpc_app_513.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    text1 = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/2} with the binding attribute. The port seeds exactly those row-2 fields at the default-model root and binds them absolutely, so icon,` &&
            ` iconAlt, intro, title, number, numberUnit and all five ObjectAttribute texts read the same mock values. The record is a fixed index, so no bind_element follow-up is needed (abap2UI5 has one,` &&
            ` cs_event-bind_element, for the per-row case) and the binding attribute itself has no counterpart; the relative {Field} form is NOT kept, because without an element binding on the view it would` &&
            ` resolve against nothing and render empty. // NOTE: The number binding keeps its parts/type/formatOptions structure 1:1 with the paths switched to the ABAP field names; the Weight per unit and` &&
            ` Dimension per unit attributes keep the original's shape as composed templates over the bound fields. The picture URL is the mock's own test-resources path, re-hosted on sdk.openui5.org. // NOTE:` &&
            ` handleTitlePress loads Popover.fragment.xml lazily, adds it to the view's dependents and opens it by the pressed title's domRef. The port declares the same ResponsivePopover (id myPopover, title`.
    text1 = text1 && ` 'About', class sapUiContentPadding, one Text) inline in the ObjectHeader's dependents aggregation and wires titlePress to a roundtrip-free follow_up_action control_by_id openBy over` &&
            ` $event.oSource.sId - the popover controls are therefore EXTRA vs the view.xml, where they live in the fragment file. // NOTE: the IconTabBar in the headerContainer is carried 1:1: four IconTabFilters` &&
            ` (info / attachments / notes / people) with their sap-icon:// icons, the counts 3 and 12 and a Text in each, under selectedKey='key3' and upperCase='true'. selectedKey names a key none of the four` &&
            ` filters carries in the original either, so the bar opens on its first tab. // NOTE: the Currency-typed number binding, the title press opening the popover anchored to the title, and the` &&
            ` fullScreenOptimized='true' layout are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_614.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderResponsiveIII`                     class = `z2ui5_cl_smpc_app_614` path = `src/01/01/z2ui5_cl_smpc_app_614.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    text1 = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/3} with the binding attribute. The port seeds exactly those row-3 fields at the default-model root and binds them absolutely, so icon,` &&
            ` iconAlt, intro, title, number, numberUnit and all five ObjectAttribute texts read the same mock values. The record is a fixed index, so no bind_element follow-up is needed (abap2UI5 has one,` &&
            ` cs_event-bind_element, for the per-row case) and the binding attribute itself has no counterpart; the relative {Field} form is NOT kept, because without an element binding on the view it would` &&
            ` resolve against nothing and render empty. // NOTE: The number binding keeps its parts/type/formatOptions structure 1:1 with the paths switched to the ABAP field names; the Weight per unit and` &&
            ` Dimension per unit attributes keep the original's shape as composed templates over the bound fields. The picture URL is the mock's own test-resources path, re-hosted on sdk.openui5.org. // NOTE:` &&
            ` handleTitlePress loads Popover.fragment.xml lazily, adds it to the view's dependents and opens it by the pressed title's domRef. The port declares the same ResponsivePopover (id myPopover, title`.
    text1 = text1 && ` 'About', class sapUiContentPadding, one Text) inline in the ObjectHeader's dependents aggregation and wires titlePress to a roundtrip-free follow_up_action control_by_id openBy over` &&
            ` $event.oSource.sId - the popover controls are therefore EXTRA vs the view.xml, where they live in the fragment file. // NOTE: the IconTabBar in the headerContainer is carried 1:1: eight` &&
            ` IconTabFilters (info / attachments / notes / people and their '2' twins) with their sap-icon:// icons, the counts 3 and 12 and a Text in each, under selectedKey='key3' and upperCase='true'.` &&
            ` selectedKey names a key none of the eight filters carries in the original either, so the bar opens on its first tab. // NOTE: showTitleSelector='true' adds the down-arrow next to the title; its` &&
            ` titleSelectorPress runs MessageBox.alert('Link was clicked!') in the controller, which the port raises as one round trip closing on client->message_box_display( type = 'alert' ) - the same box, the` &&
            ` same text. // NOTE: the Currency-typed number binding, the title press opening the popover anchored to the title, and the title selector's MessageBox.alert are unverified in a running system.`.
    text1 = text1 && ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_615.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderResponsiveIV`                      class = `z2ui5_cl_smpc_app_615` path = `src/01/01/z2ui5_cl_smpc_app_615.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    text1 = `NOTE: The ObjectHeader's single-record element binding attribute binding="{/ProductCollection/0}" is dropped: abap2UI5 serves one default model, so products.json row 0 (HT-1000, Notebook Basic 15) is` &&
            ` seeded directly onto the model root (model_init) and the control's relative {NAME}/{DESCRIPTION}/{SUPPLIERNAME}/{WIDTH}/… bindings resolve against it — same data, renders identically (single-record` &&
            ` fold, AGENTS §5). The ProductPicUrl asset is served absolute from sdk.openui5.org per the asset-URL rule. **Corrected 2026-08-01**: those bindings were written RELATIVE ({FIELD}) and a relative path` &&
            ` on a control with no binding context resolves against nothing — the fields rendered EMPTY in the running app (measured with the e2e harness, the app-207 class). They are now bound ABSOLUTELY through` &&
            ` client->_bind( field ), which is what a root-seeded record needs. // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the folded single record`.
    text1 = text1 && ` resolves in the running app - the title 'Notebook Basic 15' and the Manufacturer attribute 'Very Best Screens' render. Like 206 this port rendered EMPTY until the fields were bound absolutely (see` &&
            ` the corrected NOTE above). Residual: the ObjectMarker icons, which need the theme CSS the harness does not load. (Corrected 2026-08-23: intro binds DESCRIPTION, whose literal opens with 'Notebook` &&
            ` Basic 15', so that assertion passes with the title binding dead. The Manufacturer half is separable and genuinely covered.)`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderResponsiveV`                       class = `z2ui5_cl_smpc_app_209` path = `src/01/01/z2ui5_cl_smpc_app_209.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderResponsiveVI`                      class = `z2ui5_cl_smpc_app_476` path = `src/01/01/z2ui5_cl_smpc_app_476.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/0}. The port seeds the three fields it actually reads (Price, CurrencyCode, SupplierName) at the default-model root and binds them` &&
                 ` absolutely, so the attr ObjectHeader.binding is dropped. Everything else in this sample is a literal in the view.` ) ).

    text1 = `NOTE: The ObjectHeader binds the fixed record {/ProductCollection/0}. The port seeds the bound fields at the default-model root and binds them absolutely, so the attr ObjectHeader.binding is dropped;` &&
            ` the two ObjectAttribute texts keep the original's composed shape ({WeightMeasure} {WeightUnit} and the dimensions line). // NOTE: handleTitlePress loads Popover.fragment.xml and opens it by the` &&
            ` title's domRef. The port declares the same ResponsivePopover in the ObjectHeader's dependents and opens it anchored to the pressed control through follow_up_action control_by_id / openBy with` &&
            ` $event.oSource.sId - roundtrip-free, and the separate core:FragmentDefinition is dropped. // NOTE: The anchored popover open on the active title is unverified in a running system. **e2e-verified` &&
            ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_486.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderTitleActive`                       class = `z2ui5_cl_smpc_app_486` path = `src/01/01/z2ui5_cl_smpc_app_486.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    text1 = `NOTE: The ObjectHeader binds {/ProductCollection/0} and handleItemSelect moves its binding CONTEXT to the picked row. abap2UI5 has no per-control binding context to move, so the port seeds the shown` &&
            ` record at the model root and copies the picked row into it on the round-trip - the header then shows exactly what the original shows, and the ObjectHeader.binding attribute is dropped. // NOTE:` &&
            ` handleTitleSelectorPress loads Popover.fragment.xml and opens it by the selector's domRef; the port declares the same ResponsivePopover with its List in the header's dependents and opens it anchored` &&
            ` to the pressed control (control_by_id / openBy). The close( ) the select handler performs is a control_by_id follow-up in the same round-trip. // NOTE: The anchored popover, the row selection moving` &&
            ` the header and the close follow-up are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_502.mjs). // NOTE: The selected row is` &&
            ` transported as its title and resolved with READ TABLE ... WITH KEY name. Name is NOT unique in ui5/mock/products.json: ``Jet Scan Professional`` is HT-1082 and HT-1083, ``Tablet Pouch`` is HT-9995`.
    text1 = text1 && ` and HT-9996. Picking the second of either pair shows the first one's price and dimensions. The original moves the row's own binding context, so it is always the right record; transporting an index or` &&
            ` the context path would reproduce that.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectHeader`                    name = `ObjectHeaderTitleSel`                          class = `z2ui5_cl_smpc_app_502` path = `src/01/01/z2ui5_cl_smpc_app_502.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectIdentifier`                name = `ObjectIdentifier`                              class = `z2ui5_cl_smpc_app_071` path = `src/01/01/z2ui5_cl_smpc_app_071.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = `NOTE: element binding kept 1:1 - the VerticalLayout binds a one-record structure /S_PRODUCT in the default model instead of {/ProductCollection/0}; titleClicked's MessageBox.alert becomes` &&
                 ` message_box_display. // NOTE: the model holds exactly the bound record /ProductCollection/0 (Notebook Basic 15) of ui5/mock/products.json, verbatim - the original's own single-record binding.` ) ).

    text1 = `NOTE: the ObjectStatus '.formatter.status' (Status -> ValueState) is precomputed into the STATUS_STATE model field (Available->Success, Out of Stock->Warning, Discontinued->Error, else None). // NOTE:` &&
            ` the press toast was switched to a roundtrip-free client-composed toast on 2026-07-22 (control_global MESSAGE_TOAST.show, template ``Pressed : {0}`` filled by ${NAME}; on_event dropped, init-only) -` &&
            ` re-verify pressing an item toasts "Pressed : <name>". **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): pressing the first ObjectListItem toasts 'Pressed :` &&
            ` <name>'; the other rows are the identical template wire. Tightened 2026-08-23: the module asserted only the 'Pressed :' prefix, so a broken ${NAME} - the one half that can silently fail - would have` &&
            ` passed. It now asserts the whole composed 'Pressed : Notebook Basic 15'.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectListItem`                  name = `ObjectListItem`                                class = `z2ui5_cl_smpc_app_074` path = `src/01/01/z2ui5_cl_smpc_app_074.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectListItem`                  name = `ObjectListItemMarkers`                         class = `z2ui5_cl_smpc_app_198` path = `src/01/01/z2ui5_cl_smpc_app_198.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        notes = `NOTE: the list-item press was built as a roundtrip-free client-composed toast (control_global MESSAGE_TOAST.show, template ``Pressed : {0}`` filled by ${$source>/title}, reproducing the controller's` &&
                 ` MessageToast.show("Pressed : " + oEvent.getSource().getTitle())) - re-verify pressing an item toasts "Pressed : <title>". **e2e-verified 2026-07-30** (transpiled-framework interaction,` &&
                 ` scripts/e2e-smoke.mjs): pressing the first ObjectListItem toasts 'Pressed : <title>' (the ${$source>/title} template resolves). (Corrected 2026-08-23: the module asserts only 'Pressed :', which an` &&
                 ` unresolved template or an empty resolution would also satisfy - the template's resolution is NOT covered.)` )
      ( module = `sap.m`              control = `sap.m.ObjectMarker`                    name = `ObjectMarker`                                  class = `z2ui5_cl_smpc_app_237` path = `src/01/01/z2ui5_cl_smpc_app_237.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.38`
        notes = `NOTE: the Table's items model is the anonymous JSON model set in onInit (new JSONModel({modelData:[...]}), items='{path:/modelData}'); folded to the one default model as client->_bind( t_modeldata )` &&
                 ` -> {/T_MODELDATA}. Same data, last path segment identical, structural-diff 0 diffs. Child bindings {product}/{type}/{additionalInfo} map to the upper-cased fields {PRODUCT}/{TYPE}/{ADDITIONALINFO}.` &&
                 ` // NOTE: onPress (on the third, interactive ObjectMarker): the original does MessageToast.show( evt.getParameter( "type" ) + " marker pressed!" ); reproduced roundtrip-free as a client-composed toast` &&
                 ` (cs_event-control_global MESSAGE_TOAST, template '{0} marker pressed!' filled by ${$parameters>/type}). The press attribute is kept, so structural-diff sees no difference.` ) ).

    text1 = `NOTE: the original binds records {/ProductCollection/0..5} of the shared mock; the port carries exactly those 6 records as a default-model table T_PRODUCTS and element-binds each ObjectNumber to` &&
            ` /T_PRODUCTS/0..5 (index binding), Price+CurrencyCode verbatim. // POST-1.71: ObjectNumber.inverted, ObjectNumber.active and ObjectNumber.press (all since UI5 1.86) are kept 1:1 for the` &&
            ` inverted/interactive variants; needs UI5 >= 1.86 for those, and >= 1.110 for the last section: it keeps the original's sapMObjectNumberLongText class, and ObjectNumber's own class doc dates` &&
            ` inner-text wrapping via that class to 1.110 (contrast sapMObjectNumberLarge, 'With 1.63', which is under the floor). Below 1.110 the class resolves to no rule and that block silently does not wrap -` &&
            ` corrected 2026-08-23, the floor read 1.86 alone. // NOTE: The twelve press handlers are roundtrip-free client toasts (follow_up_action cs_event-control_global MESSAGE_TOAST.show) instead of the` &&
            ` controller's MessageToast.show('PRESS fired!'), so the class stays init-only. Behaviourally identical - the text is the controller's verbatim - and the standard substitution every comparable port`.
    text1 = text1 && ` declares (005, 074, 076, 156, 198). Added 2026-08-23: 072 was the one doing it undeclared.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectNumber`                    name = `ObjectNumber`                                  class = `z2ui5_cl_smpc_app_072` path = `src/02/01/z2ui5_cl_smpc_app_072.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.12`
        is_post171 = abap_true
        notes = text1
        post171 = `ObjectNumber.inverted, ObjectNumber.active and ObjectNumber.press (all since UI5 1.86) are kept 1:1 for the inverted/interactive variants; needs UI5 >= 1.86 for those, and >= 1.110 for the last` &&
                 ` section: it keeps the original's sapMObjectNumberLongText class, and ObjectNumber's own class doc dates inner-text wrapping via that class to 1.110 (contrast sapMObjectNumberLarge, 'With 1.63', which` &&
                 ` is under the floor). Below 1.110 the class resolves to no rule and that block silently does not wrap - corrected 2026-08-23, the floor read 1.86 alone.` ) ).

    text1 = `POST-1.71: the ObjectStatus state values Indication06-Indication08 (since UI5 1.75) and Indication09-Indication20 (since UI5 1.120) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5` &&
            ` release >= 1.120 to render them all (>= 1.75 for Indication06-Indication08). // NOTE: the active status press opens the controller-built Dialog 1:1 (core:FragmentDefinition + popup_display): the` &&
            ` Dialog with its VBox, its one content Text (an EXTRA control vs the archived view.xml, which has no Dialog at all) and its OK Button are EXTRA controls vs the archived view.xml, which only holds the` &&
            ` ObjectStatus rows. Confirmed working in the 2026-07-20 live check. // POST-1.71: the icon ``sap-icon://information`` reached the SAP icon font in 1.80 and is kept 1:1 from the original V.view.xml,` &&
            ` which names it on the two 'Currently closed'/'Information' ObjectStatus rows. Newer than the 1.71 floor: there IconPool resolves nothing and the ObjectStatus renders its text with NO icon, silently -` &&
            ` the app needs a UI5 release >= 1.80 to show it. (The framework's own equivalent is sap-icon://message-information, which exists from 1.71 on; using it here would deviate from the original.)`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ObjectStatus`                    name = `ObjectStatus`                                  class = `z2ui5_cl_smpc_app_042` path = `src/02/01/z2ui5_cl_smpc_app_042.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_042.mjs: the ObjectStatus grid renders and pressing the active 'Damaged' status round-trips STATUS_PRESSED, the popup_display 'Error description' Dialog shows its` &&
                 ` text and OK's popup_close closes it)`
        notes = text1
        post171 = `the ObjectStatus state values Indication06-Indication08 (since UI5 1.75) and Indication09-Indication20 (since UI5 1.120) are newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >=` &&
                 ` 1.120 to render them all (>= 1.75 for Indication06-Indication08). // the icon ``sap-icon://information`` reached the SAP icon font in 1.80 and is kept 1:1 from the original V.view.xml, which names it` &&
                 ` on the two 'Currently closed'/'Information' ObjectStatus rows. Newer than the 1.71 floor: there IconPool resolves nothing and the ObjectStatus renders its text with NO icon, silently - the app needs` &&
                 ` a UI5 release >= 1.80 to show it. (The framework's own equivalent is sap-icon://message-information, which exists from 1.71 on; using it here would deviate from the original.)` ) ).

    text1 = `POST-1.71: two post-1.71 members are kept for the 1:1 port: the control sap.m.OverflowToolbarMenuButton (@since 1.109.0) and its beforeMenuOpen event (@since 1.94.0). The split Export button is one of` &&
            ` the four toolbar-content types this sample lines up, so it cannot be dropped; the app needs a UI5 release >= 1.109. // NOTE: onSliderMoved sets byId('otbSubheader').setWidth(value + '%') and the same` &&
            ` for the footer toolbar, on every liveChange. width IS a bindable property, so both toolbars take an expression over the two-way bound Slider value and follow it in the browser - the liveChange wire` &&
            ` is dropped, since a round trip per keystroke would be slower AND lossy (apps 582/604 carry the same fold). // NOTE: Thin frontend: fnApplyFiltersAndOrdering builds a sap.ui.model.Filter (Name` &&
            ` Contains <query>) and one sap.ui.model.Sorter and calls filter()/sort() on the items binding. The port filters and sorts the ABAP table it sends instead (app 298 idiom): onSort flips the sorter's` &&
            ` descending flag, onGroup switches the sort key between SupplierName and Name, onReset clears the query and both flags. // NOTE: grouping keeps its group headers. onGroup's Sorter carries a _fnGroup`.
    text1 = text1 && ` that returns a {key, text} per row, which is what makes UI5 draw a grey SupplierName header above each block. The port hands the SORTER to the live binding instead of ordering the ABAP table:` &&
            ` binding_call's sort takes [path, descending, group] and builds new Sorter(path, descending, group) on the client, and UI5's DEFAULT group function returns {key, text} of the sorted property - the` &&
            ` same shape _fnGroup returns - so the headers are reproduced rather than approximated. The Name Contains filter stays server-side over the data this thin frontend sends. (App 298 records the header` &&
            ` loss for the same control and predates this; its rework is separate.) // NOTE: onFilter runs on the SearchField's liveChange. The port binds value two-way and filters on search instead - the` &&
            ` final-value event - which is the prefer-a-bindable-property rule: a round trip per keystroke drops the events behind one already in flight. onReset's byId('maxPrice').setValue('') becomes a CLEAR on` &&
            ` the same bound field. // NOTE: onDefaultActionAccept and onBeforeMenuOpen toast constants, so both are wired round-trip-free as follow_up_action( control_global, MESSAGE_TOAST show ). onMenuAction`.
    text1 = text1 && ` walks the MenuItem's parent chain to build a ' > ' path; this menu is one level deep, so the path IS the item's text and the wire carries ${$parameters>/item}.getText(). onTogglePress toasts the` &&
            ` button's generated id plus Pressed / Unpressed - the port gives the button the explicit id 'toggleButton' and binds its pressed state, so the backend can name and read it. // NOTE: the Price cell is` &&
            ` a composite currency binding in the sample (parts Price + CurrencyCode through sap.ui.model.type.Currency with showMeasure false). The port binds the two fields directly on the ObjectNumber's number` &&
            ` and unit, which is what the formatter renders, since the mock's prices are plain integers. // NOTE: not yet verified in a running system: that the slider shrinks both toolbars into their overflow` &&
            ` areas, and that Sort / Group / Reset and the Export menu behave as upstream. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_607.mjs). // NOTE: The price` &&
            ` cell keeps the original's composite sap.ui.model.type.Currency binding rather than binding the raw field. The type is not a pass-through: NumberFormat's currency instance applies grouping and the`.
    text1 = text1 && ` currency's own fraction digits, so EUR renders 956.00 / 1,249.00 where the bare field renders 956 / 1249. The earlier justification - that the fold is what the formatter renders because the mock's` &&
            ` prices are plain numbers - was wrong on both halves.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.OverflowToolbar`                 name = `OverflowToolbarFooter`                         class = `z2ui5_cl_smpc_app_607` path = `src/02/01/z2ui5_cl_smpc_app_607.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.28`
        is_post171 = abap_true
        notes = text1
        post171 = `two post-1.71 members are kept for the 1:1 port: the control sap.m.OverflowToolbarMenuButton (@since 1.109.0) and its beforeMenuOpen event (@since 1.94.0). The split Export button is one of the four` &&
                 ` toolbar-content types this sample lines up, so it cannot be dropped; the app needs a UI5 release >= 1.109.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.OverflowToolbar`                 name = `TitleToolBar`                                  class = `z2ui5_cl_smpc_app_395` path = `src/01/01/z2ui5_cl_smpc_app_395.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.28` )
      ( module = `sap.m`              control = `sap.m.OverflowToolbar`                 name = `ToolbarActive`                                 class = `z2ui5_cl_smpc_app_424` path = `src/01/01/z2ui5_cl_smpc_app_424.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.28`
        notes = `NOTE: onCheckBoxSelect calls toolbar.setActive(selected); the port two-way binds one flag on both CheckBox.selected and OverflowToolbar.active, so the toggle happens roundtrip-free in the model. The` &&
                 ` CheckBox.select attribute is therefore dropped (structural-diff reports attr missing CheckBox.select). // NOTE: onToolbarPress shows MessageToast.show('OverflowToolbar is clicked') - a constant text,` &&
                 ` so it is composed on the client via follow_up_action control_global MESSAGE_TOAST.show and needs no round-trip. // NOTE: Both wires are unverified in a running system: the two-way bound active flag` &&
                 ` (CheckBox -> OverflowToolbar.active) and the client-composed toast on the OverflowToolbar press. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_424.mjs).` )
      ( module = `sap.m`              control = `sap.m.OverflowToolbar`                 name = `ToolbarAlignment`                              class = `z2ui5_cl_smpc_app_396` path = `src/01/01/z2ui5_cl_smpc_app_396.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.28` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.OverflowToolbar`                 name = `ToolbarDesign`                                 class = `z2ui5_cl_smpc_app_086` path = `src/01/01/z2ui5_cl_smpc_app_086.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.28`
        notes = `NOTE: the Select ``change`` handlers onSelectDesign/onSelectStyle (setDesign/setStyle) become two-way bound design/style; bActionContext (design != Info) becomes an expression binding on the Buttons'` &&
                 ` visible.` )
      ( module = `sap.m`              control = `sap.m.OverflowToolbar`                 name = `ToolbarEnabled`                                class = `z2ui5_cl_smpc_app_425` path = `src/01/01/z2ui5_cl_smpc_app_425.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.28`
        notes = `NOTE: onCheckBoxSelect calls toolbar.setEnabled(selected); the port two-way binds one flag on both CheckBox.selected and OverflowToolbar.enabled, so the toggle happens roundtrip-free in the model. The` &&
                 ` CheckBox.select attribute is therefore dropped (structural-diff reports attr missing CheckBox.select). // NOTE: The two-way bound enabled flag (outer CheckBox -> OverflowToolbar.enabled and with it` &&
                 ` every control inside the toolbar) is unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_425.mjs).` ) ).

    text1 = `NOTE: Both halves of the controller are now reproduced instead of faked. (a) onPress toasts oEvent.getSource().getText(); the port transports that value - control_global MESSAGE_TOAST.show with the` &&
            ` template '{0}' filled by ${$source>/text} (the app-003 shape) - instead of hardcoding each button's caption, so the icon-only Print button toasts its (empty) text exactly as the original does. (b)` &&
            ` onOpen Fragment.loads ActionSheet.fragment.xml and calls openBy(button): the port rebuilds that fragment 1:1 in on_event (ActionSheet title 'Menu', showCancelButton, placement Top, the five Buttons` &&
            ` with their icons and visible bindings) and anchors it with client->popover_display( by_id = $event.oSource.sId ) - the overflow Button therefore fires a real backend event instead of a placeholder` &&
            ` 'Overflow' toast. The fragment's {range>/isPhone} / {range>/isPhoneOrTablet} bindings fold onto the default model like the view's (a new isphone field joins the existing range flags). // NOTE: The` &&
            ` original drives the toolbar and ActionSheet visibility from a separate 'range' media JSON model (isNoPhone, isNotPhoneOrTablet, isTablet, isPhoneOrTablet, isPhone), seeded from`.
    text1 = text1 && ` Device.media.getCurrentRange('Std') and kept current by an attachHandler on that range set. **Reworked 2026-08-23**: the flags are UI5 expression bindings over the framework's own device> model - {=` &&
            ` ${device>/media/range} === 'Phone' } and friends - which reads the SAME 'Std' range set and is refreshed on every resize (CAPABILITIES; models.js). Until then the port seeded the five flags once with` &&
            ` the desktop values and nothing ever wrote them again, which did not just freeze the layout: isPhoneOrTablet was false forever, so the overflow Button that opens the ActionSheet was permanently` &&
            ` invisible, its OPEN_SHEET event could never fire, and the 40-line ActionSheet rebuild in on_event was dead code - in a sample whose whole subject is 'toolbar items displayed depending on the current` &&
            ` device'. The justification given for the seeding ('a client-only decision') named a limitation the framework does not have.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.OverflowToolbar`                 name = `ToolbarResponsive`                             class = `z2ui5_cl_smpc_app_163` path = `src/01/01/z2ui5_cl_smpc_app_163.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `NOTE: the token interaction is reproduced since 2026-08-05, in the two shapes the sample needs. The FIRST tokenizer - the one onAddToken/onTokenDelete work on - has its three static tokens folded into` &&
            ` a bound aggregation (the app-085 pattern), so adding appends a row (with the original's empty-input toast and the input cleared afterwards) and deleting removes the row by key; that costs 3 static` &&
            ` Token controls against the original's 24, the port now declaring one bound template instead. The other three tokenizers keep their static tokens and delete them roundtrip-free through control_by_id` &&
            ` removeToken with the token's ID, each chained with the original's 'Token deleted: <text>' toast off the same event - measured with scripts/probes/event-arg-expression-probe.mjs,` &&
            ` ManagedObject.removeAggregation accepts an id and the token really leaves the aggregation - plus the original's 'Token deleted: X' toast. The earlier claim (imperative control mutation over static` &&
            ` tokens is 'not expressible without inventing a bound token model') was half wrong: the bound model is what the sample's own add path needs, and removal by id needs no model at all. Corrected`.
    text1 = text1 && ` 2026-08-21: this sentence claimed all three were wired while tokenizerShowItems carried no tokenDelete at all, and claimed the toast while none of the wires composed one - so the sentence that` &&
            ` satisfies the gate described a port that did not exist. All four tokenizers are wired now and each chains the toast, which is expressible in one attribute (the app-076 idiom). // POST-1.71:` &&
            ` sap.m.OverflowToolbar ariaHasPopup (since 1.79.0) is kept 1:1 from the original view; needs a UI5 release >= 1.79 to render. // POST-1.71: REVIEW FINDING (out of scope, maintainer decision needed):` &&
            ` the ported control sap.m.OverflowToolbarTokenizer itself is tagged @ui5-experimental-since 1.139 in the OpenUI5 source and carries NO plain @since tag, so The control is @ui5-experimental-since 1.139` &&
            ` (OverflowToolbarTokenizer.js). The paragraph that stood here until 2026-08-23 - that scope-of.mjs misreads it as base-version, that the property gate cannot see it, and that a maintainer decision is` &&
            ` still owed - was stale on all three counts: scope-of.mjs matches @ui5-experimental-since (and cites this very port in its own comment), the linter emits control-too-new for it off`.
    text1 = text1 && ` ui5/properties.json, and ui5/scope-exceptions.json carries the KEEP decision dated 2026-07-30. // POST-1.71: sap.m.Tokenizer.tokenDelete is @since 1.82 and is wired on all four tokenizers, 1:1 with` &&
            ` the original. Newer than the 1.71 floor, so the app needs UI5 >= 1.82 for the delete handlers to exist at all. Undeclared until 2026-08-23: the linter does raise member-too-new for it, and the view` &&
            ` gate accepted the port only because its declares( ) check matches the deviation text as a SUBSTRING and the control's own name appears in the two POST_171 texts beside it - AGENTS section 5 requires` &&
            ` a deviation naming the member. // NOTE: onTokenDelete iterates aDeletedTokens: sap.m.Tokenizer fires tokenDelete with getSelectedTokens( ) when there is a selection and with the focused token` &&
            ` otherwise, so several tokens really do arrive at once. Every wire read ${$parameters>/tokens}[0] until 2026-08-23, so selecting three tokens and pressing Delete removed and toasted one. The BOUND` &&
            ` tokenizer now sends the whole array - the frontend marshals each control into its properties - and ABAP loops over it. The three tokenizers whose tokens are static controls still delete the first`.
    text1 = text1 && ` token only: their removal is a client-side control_by_id removeToken, and a client action cannot iterate an array. That residual is the price of keeping those three roundtrip-free, and it is now` &&
            ` written down rather than implied.`.
    text2 = `sap.m.OverflowToolbar ariaHasPopup (since 1.79.0) is kept 1:1 from the original view; needs a UI5 release >= 1.79 to render. // REVIEW FINDING (out of scope, maintainer decision needed): the ported` &&
            ` control sap.m.OverflowToolbarTokenizer itself is tagged @ui5-experimental-since 1.139 in the OpenUI5 source and carries NO plain @since tag, so The control is @ui5-experimental-since 1.139` &&
            ` (OverflowToolbarTokenizer.js). The paragraph that stood here until 2026-08-23 - that scope-of.mjs misreads it as base-version, that the property gate cannot see it, and that a maintainer decision is` &&
            ` still owed - was stale on all three counts: scope-of.mjs matches @ui5-experimental-since (and cites this very port in its own comment), the linter emits control-too-new for it off` &&
            ` ui5/properties.json, and ui5/scope-exceptions.json carries the KEEP decision dated 2026-07-30. // sap.m.Tokenizer.tokenDelete is @since 1.82 and is wired on all four tokenizers, 1:1 with the` &&
            ` original. Newer than the 1.71 floor, so the app needs UI5 >= 1.82 for the delete handlers to exist at all. Undeclared until 2026-08-23: the linter does raise member-too-new for it, and the view gate`.
    text2 = text2 && ` accepted the port only because its declares( ) check matches the deviation text as a SUBSTRING and the control's own name appears in the two POST_171 texts beside it - AGENTS section 5 requires a` &&
            ` deviation naming the member.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.OverflowToolbarTokenizer`        name = `OverflowToolbarTokenizer`                      class = `z2ui5_cl_smpc_app_203` path = `src/02/01/z2ui5_cl_smpc_app_203.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.139`
        since_post171 = abap_true
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Page`                            name = `PageListReportIconTabBar`                      class = `z2ui5_cl_smpc_app_406` path = `src/02/01/z2ui5_cl_smpc_app_406.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: The sample's AnalyticalTable declares its row mode through the rowMode aggregation with a sap.ui.table.rowmodes.Auto instance - both the aggregation (sap.ui.table.Table.rowMode) and the` &&
                 ` control (sap.ui.table.rowmodes.Auto) are @since 1.119, i.e. newer than the 1.71 floor. Kept 1:1 per the fidelity-first policy (AGENTS §5), same as app 164 (the RowModes sample) which declares the` &&
                 ` same aggregation; the app needs a UI5 release >= 1.119 to render the table at its intended row height. The pre-1.119 alternative (visibleRowCountMode='Auto' + rowHeight) is a different, deprecated` &&
                 ` API and would not be the sample.`
        post171 = `The sample's AnalyticalTable declares its row mode through the rowMode aggregation with a sap.ui.table.rowmodes.Auto instance - both the aggregation (sap.ui.table.Table.rowMode) and the control` &&
                 ` (sap.ui.table.rowmodes.Auto) are @since 1.119, i.e. newer than the 1.71 floor. Kept 1:1 per the fidelity-first policy (AGENTS §5), same as app 164 (the RowModes sample) which declares the same` &&
                 ` aggregation; the app needs a UI5 release >= 1.119 to render the table at its intended row height. The pre-1.119 alternative (visibleRowCountMode='Auto' + rowHeight) is a different, deprecated API and` &&
                 ` would not be the sample.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Page`                            name = `PageListReportToolbar`                         class = `z2ui5_cl_smpc_app_405` path = `src/02/01/z2ui5_cl_smpc_app_405.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: The sample's AnalyticalTable declares its row mode through the rowMode aggregation with a sap.ui.table.rowmodes.Auto instance - both the aggregation (sap.ui.table.Table.rowMode) and the` &&
                 ` control (sap.ui.table.rowmodes.Auto) are @since 1.119, i.e. newer than the 1.71 floor. Kept 1:1 per the fidelity-first policy (AGENTS §5), same as app 164 (the RowModes sample) which declares the` &&
                 ` same aggregation; the app needs a UI5 release >= 1.119 to render the table at its intended row height. The pre-1.119 alternative (visibleRowCountMode='Auto' + rowHeight) is a different, deprecated` &&
                 ` API and would not be the sample.`
        post171 = `The sample's AnalyticalTable declares its row mode through the rowMode aggregation with a sap.ui.table.rowmodes.Auto instance - both the aggregation (sap.ui.table.Table.rowMode) and the control` &&
                 ` (sap.ui.table.rowmodes.Auto) are @since 1.119, i.e. newer than the 1.71 floor. Kept 1:1 per the fidelity-first policy (AGENTS §5), same as app 164 (the RowModes sample) which declares the same` &&
                 ` aggregation; the app needs a UI5 release >= 1.119 to render the table at its intended row height. The pre-1.119 alternative (visibleRowCountMode='Auto' + rowHeight) is a different, deprecated API and` &&
                 ` would not be the sample.` )
      ( module = `sap.m`              control = `sap.m.Page`                            name = `PageStandardClasses`                           class = `z2ui5_cl_smpc_app_089` path = `src/01/01/z2ui5_cl_smpc_app_089.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: element binding kept 1:1 - a one-record structure /S_PRODUCT instead of {/ProductCollection/0}. // NOTE: the original binds expanded="{device>/isNoPhone}" (a demo-kit helper model that abap2UI5` &&
                 ` does not carry); expressed over the framework own device> model as the expression {= !${device>/system/phone} } - same truth value, different binding text (app 030 precedent, live-verified there on` &&
                 ` desktop and phone emulation).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Panel`                           name = `Panel`                                         class = `z2ui5_cl_smpc_app_397` path = `src/01/01/z2ui5_cl_smpc_app_397.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = `NOTE: the first Panel's Image binds src="{img>/products/pic1}" against the demo kit's named img JSONModel (sap/ui/demo/mock/img.json). abap2UI5 serves one default model, so the fixed value is inlined` &&
                 ` as a literal and host-absolutized to https://sdk.openui5.org/ (app 031 precedent) - same image, same rendering.` ) ).

    text1 = `NOTE: the Select's change attribute is dropped, and with it the whole controller. onBackgroundDesignChange only reads the selected key and pushes it into the panel with setBackgroundDesign - both ends` &&
            ` are bindable properties, so Select.selectedKey and Panel.backgroundDesign bind the SAME field: the Select writes it, the Panel reads it, and no event and no round-trip are needed at all` &&
            ` (prefer-a-bindable-property, linter rule settable-property-via-action). The class has no on_event and no model_init; the initial Solid comes from the field's own VALUE. // POST-1.71: The Select` &&
            ` offers Contrast, and sap.m.PanelBackgroundDesign.Contrast is newer than UI5 1.71 - which moves the class from src/01 to src/02 (2026-08-21). The enum VALUE is exactly the blind spot AGENTS section 5` &&
            ` names: the property gate matches attribute NAMES against a control's metadata, and backgroundDesign itself is @since 1.30, so nothing flagged this. Neither does an @since tag on the value - there is` &&
            ` none. Measured against the sources instead: at tag 1.71.0 sap.m.Panel.backgroundDesign is typed sap.m.BackgroundDesign, whose three values are Solid, Transparent and Translucent, and`.
    text1 = text1 && ` sap.m.PanelBackgroundDesign does not exist in that library.js at all. The Panel-specific enum, and with it Contrast, came later; the sample's own MessageStrip says the design is only visible in the` &&
            ` Horizon themes. Selecting it on a 1.71 runtime fails validateProperty.`.
    text2 = `The Select offers Contrast, and sap.m.PanelBackgroundDesign.Contrast is newer than UI5 1.71 - which moves the class from src/01 to src/02 (2026-08-21). The enum VALUE is exactly the blind spot AGENTS` &&
            ` section 5 names: the property gate matches attribute NAMES against a control's metadata, and backgroundDesign itself is @since 1.30, so nothing flagged this. Neither does an @since tag on the value -` &&
            ` there is none. Measured against the sources instead: at tag 1.71.0 sap.m.Panel.backgroundDesign is typed sap.m.BackgroundDesign, whose three values are Solid, Transparent and Translucent, and` &&
            ` sap.m.PanelBackgroundDesign does not exist in that library.js at all. The Panel-specific enum, and with it Contrast, came later; the sample's own MessageStrip says the design is only visible in the` &&
            ` Horizon themes. Selecting it on a 1.71 runtime fails validateProperty.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Panel`                           name = `PanelBackgroundDesign`                         class = `z2ui5_cl_smpc_app_292` path = `src/02/01/z2ui5_cl_smpc_app_292.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: the original controller toggles the third panel imperatively (onOverflowToolbarPress -> oPanel.setExpanded(!oPanel.getExpanded())). The port binds Panel.expanded two-way instead and only flips` &&
            ` the flag server-side, per the prefer-a-bindable-property rule: the state then lives in the model, where it survives a view rebuild and a draft restore, and the control's own expand/collapse writes` &&
            ` back into it rather than drifting from a server-side mirror. This REVERSES the 2026-07-18 decision recorded here before (which had replaced the binding with the whitelisted setExpanded to 'match the` &&
            ` original view.xml exactly'); an added attribute is not a structural diff, and the rulebook has since settled the other way. The linter rule settable-property-via-action now gates it corpus-wide. //` &&
            ` NOTE: the two-way expanded binding is not live-verified. The imperative form it replaces WAS: human live check 2026-07-20 following the interaction checklist, all listed checks passed - kept here as` &&
            ` context. A fresh live run (toolbar press expands and collapses the third panel, and a manual expand/collapse of the panel header survives the next round-trip) restamps this port to checked.`.
    text1 = text1 && ` **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_043.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Panel`                           name = `PanelExpanded`                                 class = `z2ui5_cl_smpc_app_043` path = `src/01/01/z2ui5_cl_smpc_app_043.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Panel`                           name = `PanelSticky`                                   class = `z2ui5_cl_smpc_app_487` path = `src/02/01/z2ui5_cl_smpc_app_487.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.Panel.stickyHeader is @since 1.117 and is the subject of the sample (the first Panel has it, the second does not), so it is kept 1:1 and the port needs a UI5 runtime >= 1.117.`
        post171 = `sap.m.Panel.stickyHeader is @since 1.117 and is the subject of the sample (the first Panel has it, the second does not), so it is kept 1:1 and the port needs a UI5 runtime >= 1.117.` ) ).

    text1 = `NOTE: the two PDF paths are the ones onInit resolves with sap.ui.require.toUrl('sap/m/sample/PDFViewerEmbedded/sample.pdf') and '.../sample_nonexisting.pdf'. An abap2UI5 system does not serve the demo` &&
            ` kit module path, so both are pinned to the OpenUI5 host in their SDK form (https://sdk.openui5.org/test-resources/sap/m/demokit/sample/PDFViewerEmbedded/sample.pdf and sample_nonexisting.pdf) per the` &&
            ` runtime asset-URL rule - same two file names, same sample folder, only the host-side prefix differs from the module path the original writes (app 131/152 precedent). The second one is MEANT to 404:` &&
            ` it is how the sample demonstrates the PDFViewer's loading error. // NOTE: the controller keeps the two paths in closure variables and swaps /Source with setProperty. Here they are PROTECTED CONSTANTS` &&
            ` and only the bound SOURCE field is public - a value that exists solely to be assigned is not model data (AGENTS section 10, every PUBLIC attribute is persisted app state).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PDFViewer`                       name = `PDFViewerEmbedded`                             class = `z2ui5_cl_smpc_app_288` path = `src/01/01/z2ui5_cl_smpc_app_288.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.48`
        notes = text1 ) ).

    text1 = `NOTE: The two documents the sample loads (sample.pdf and the deliberately missing sample_nonexisting.pdf) live next to the sample and are addressed with sap.ui.require.toUrl. abap2UI5 ships no such` &&
            ` resource, so the port points both at the same two files on the demo kit host (sdk.openui5.org/test-resources/sap/m/demokit/sample/PDFViewerMultiple/...) - the same documents, re-hosted, and the` &&
            ` missing one still 404s, which is what the second button demonstrates. // NOTE: onCorrectPathClick / onIncorrectPathClick write /Source, which both PDFViewers bind. The port keeps the bound field and` &&
            ` writes it in ABAP on the two press events (a round-trip; the value is backend state, not a client-side toggle). // NOTE: Both source-switch wires and the loading-error state of the second document` &&
            ` are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_442.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PDFViewer`                       name = `PDFViewerMultiple`                             class = `z2ui5_cl_smpc_app_442` path = `src/01/01/z2ui5_cl_smpc_app_442.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.48`
        notes = text1 ) ).

    text1 = `NOTE: the original onInit creates a popup-mode sap.m.PDFViewer and adds it as a view dependent; it is declared 1:1 in the view's mvc:dependents aggregation (an extra PDFViewer element vs the original` &&
            ` view.xml, which never carries it - there it lives in the controller). onPress' setSource/setTitle/open() becomes a bound source updated per click, the constant title declared in the view, and the` &&
            ` whitelisted open via client->follow_up_action( cs_event-control_by_id ) after render - popup mode 1:1, the earlier Dialog embedding workaround is gone (whitelist extended upstream 2026-07-18, see` &&
            ` pr/control-call-whitelist). // NOTE: the per-image JSONModels of onInit (a Source/Preview URL pair per Image) have no server-side equivalent; the Image src (original {/Preview}) is resolved to static` &&
            ` absolute sdk.openui5.org URLs and the Source travels through the one bound pdf_source field - same family as pr/named-json-models. // POST-1.71: the PDFViewer property isTrustedSource (since UI5` &&
            ` 1.121, backported to maintenance patches down to 1.71.63; the original controller passes isTrustedSource: true) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.121 (or`.
    text1 = text1 && ` a patched maintenance release) to render it.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PDFViewer`                       name = `PDFViewerPopup`                                class = `z2ui5_cl_smpc_app_044` path = `src/02/01/z2ui5_cl_smpc_app_044.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a` &&
                 ` close look.`
        since = `1.48`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_044.mjs: the PDFViewer dependent boots with an empty source, a real click on image1 round-trips SHOW_PDF so the bound source ends in sample1.pdf and the` &&
                 ` control_by_id open follow-up opens the 'My Custom Title' Dialog - the PDF body itself cannot render headless, which UI5 reports in its own text)`
        notes = text1
        post171 = `the PDFViewer property isTrustedSource (since UI5 1.121, backported to maintenance patches down to 1.71.63; the original controller passes isTrustedSource: true) is newer than 1.71 but kept for the` &&
                 ` 1:1 port - the app needs a UI5 release >= 1.121 (or a patched maintenance release) to render it.` ) ).

    text1 = `POST-1.71: sap.m.PlanningCalendar rowHeaderPress is @since 1.119.0, primaryCalendarType @since 1.108.0 and secondaryCalendarType @since 1.109.0, and sap.ui.unified.CalendarAppointment ariaHasPopup is` &&
            ` @since 1.150.0 - all newer than the 1.71 floor. The two calendar-type Selects are what this sample adds over the other PlanningCalendar samples, so every one is kept and the port is filed under` &&
            ` src/02. // NOTE: The calendar date properties (startDate, CalendarAppointment startDate/endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and` &&
            ` Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: onCalendarTypeSelect and onCalendarSecondaryTypeSelect` &&
            ` call setPrimaryCalendarType / setSecondaryCalendarType. Both properties are bindable, so each Select shares its key with the calendar and neither handler needs a round-trip. The secondary Select's` &&
            ` None entry means "no secondary type" - the original passes undefined, which an expression binding reproduces. onInit's read-back of getPrimaryCalendarType() is the Gregorian default, seeded here.`.
    text1 = text1 && ` Both Select.change attributes are therefore dropped (structural-diff reports Select.change as attr missing). // NOTE: handleSelectionFinish hands the MultiComboBox's selected keys to setBuiltInViews.` &&
            ` The picked keys travel as one comma-separated argument and ABAP fills the bound string table, so the calendar's built-in views really follow the box. // NOTE: handleAppointmentSelect and` &&
            ` handleRowHeaderPress both open a MessageBox. Every value they read (the appointment title, its selected state, the selected count, the pressed row's id) is client-readable, so it travels with the` &&
            ` event and ABAP composes the text - the box is modal anyway, so the round-trip costs nothing. // NOTE: The two row images are the demo kit's own test-resources files (John_Miller.png,` &&
            ` Donna_Moore.jpg), re-hosted on sdk.openui5.org. // NOTE: The two calendar-type Selects, the built-in views box, the appointment select and the row header press are unverified in a running system.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_536.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendar`                              class = `z2ui5_cl_smpc_app_536` path = `src/02/01/z2ui5_cl_smpc_app_536.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.PlanningCalendar rowHeaderPress is @since 1.119.0, primaryCalendarType @since 1.108.0 and secondaryCalendarType @since 1.109.0, and sap.ui.unified.CalendarAppointment ariaHasPopup is @since` &&
                 ` 1.150.0 - all newer than the 1.71 floor. The two calendar-type Selects are what this sample adds over the other PlanningCalendar samples, so every one is kept and the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.m.PlanningCalendar appointmentHeight and appointmentRoundWidth are @since 1.81.0 and sap.ui.unified.CalendarAppointment description @since 1.81.0 - all newer than the 1.71 floor. The` &&
            ` two Selects that drive them ARE the sample, so all three are kept and the port is filed under src/02. // NOTE: The calendar date properties (the PlanningCalendar startDate and every` &&
            ` CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point` &&
            ` of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: handleAppointmentHeightChange and handleAppointmentRoundingChange call setAppointmentHeight / setAppointmentRoundWidth. Both` &&
            ` properties are bindable, so each Select shares its key with the calendar and neither change handler is needed. // IMPROVISED: handleSortChange hands the calendar a JavaScript comparator through` &&
            ` setCustomAppointmentsSorterCallback (fnAlphabeticalOrder, a case-insensitive title compare). abap2UI5 cannot register a callback, so the port sorts the appointment rows THEMSELVES in ABAP with the`.
    text1 = text1 && ` same rule and re-seeds the original order for the default entry. Same visible order, different mechanism. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on` &&
            ` sdk.openui5.org. // NOTE: The four appointment sizes, the two rounding widths and the alphabetical sort are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_543.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarAppointmentSizes`              class = `z2ui5_cl_smpc_app_543` path = `src/02/01/z2ui5_cl_smpc_app_543.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.PlanningCalendar appointmentHeight and appointmentRoundWidth are @since 1.81.0 and sap.ui.unified.CalendarAppointment description @since 1.81.0 - all newer than the 1.71 floor. The two Selects` &&
                 ` that drive them ARE the sample, so all three are kept and the port is filed under src/02.` ) ).

    text1 = `NOTE: The calendar date properties (the PlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and` &&
            ` Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: canModifyAppointments( name ) is a formatter over the` &&
            ` row's name and the picked role: a manager and an admin may modify every row, everyone else only their own. Both values are on the client, so the three enable flags share ONE expression binding` &&
            ` instead of a formatter. getUserRole( ) maps the Select key through the roles table (donna -> 'Donna Moore'), which the change round-trip does in ABAP; handleRoleChange, which only refreshes the` &&
            ` model, goes with it. // NOTE: handleAppointmentDrop moves - or, with the copy modifier, copies - the appointment to the dropped row and re-dates it, then toasts. The interval's LOCAL date parts, the` &&
            ` appointment's and the row's binding paths and the copy flag travel with the event, and ABAP does the same splice; the paths carry the row and appointment INDEX, which is exactly what the original`.
    text1 = text1 && ` reads off the binding contexts. handleAppointmentCreate pushes a 'New Appointment' into the row the same way. // NOTE: handleAppointmentResize re-dates the appointment unless isAppointmentOverlap` &&
            ` says it would collide - a check the original only ever applies to the MANAGER role. The overlap test is the same three interval comparisons, done in ABAP over the row's own appointments, and both` &&
            ` toasts are kept. // IMPROVISED: handleAppointmentDragEnter calls oEvent.preventDefault() to VETO a drag that would overlap, while the pointer is still moving. A veto has to be decided synchronously` &&
            ` on the client and a backend round-trip cannot answer in time, so the appointmentDragEnter wire is dropped (structural-diff reports it as attr missing). The overlap rule itself survives on the resize` &&
            ` path, which is not synchronous. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The three roles and their per-row enable flags, the` &&
            ` drag-and-drop move and copy, the resize with its overlap refusal and the drag-create are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_546.mjs). // NOTE: The drag-created appointment seeds type = 'Type01'. An ABAP field is never absent, so an unset type reaches CalendarAppointment.type as the` &&
            ` empty string, which is not a CalendarDayType member - validateProperty throws and the binding update takes the view down. The original pushes a JS object with no type key, which falls back to the` &&
            ` property default. The default is Type01, inherited from DateTypeRange.type (DateTypeRange.js:43) - NOT None, which is secondaryType's default. This note said None until 2026-08-27; seeding None` &&
            ` renders a different colour from the original, so the sentence was instructing the next reader to re-introduce the defect.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarDnD`                           class = `z2ui5_cl_smpc_app_546` path = `src/01/01/z2ui5_cl_smpc_app_546.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: The calendar date properties (startDate, minDate, maxDate and the CalendarAppointment startDate/endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and` &&
            ` Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: handleAppointmentSelect opens a MessageBox with the` &&
            ` appointment title, its new selected state and the number of selected appointments - or, when the interval selection hit no appointment, their count. Every value is client-readable, so it travels with` &&
            ` the event and ABAP composes both branches. // NOTE: The sample's appointment rows carry an ariaHasPopup value the view never binds; the port seeds it the same way and leaves it unbound, so the data` &&
            ` stays 1:1 with the mock. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The minDate / maxDate navigation limits and the appointment select` &&
            ` are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_540.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarMinMax`                        class = `z2ui5_cl_smpc_app_540` path = `src/01/01/z2ui5_cl_smpc_app_540.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.ui.unified.CalendarAppointment ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor. It is kept 1:1 from the sample, so the port is filed under src/02. // NOTE: The calendar date` &&
            ` properties (the PlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and` &&
            ` Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: handleAppointmentSelect splits two ways. On a single` &&
            ` appointment it opens the Details popover: the binding path and the selected state travel with the event, ABAP fills the popover's fields from the row, and a deselect closes it. On a collapsed GROUP` &&
            ` it reports how many appointments were selected and whether their types differ - both facts are client-readable, so they travel with the event and ABAP composes the same two sentences. // NOTE: The` &&
            ` Create dialog serves all THREE of the sample's modes - create, create-with-context (pre-set to the clicked interval and row) and edit. The mode, the picked person, the interval-appointment flag and`.
    text1 = text1 && ` the four inputs are bound fields here, so the controller's _setCreateAppointmentDialogContent / _setCreateWithContextAppointmentDialogContent / _setEditAppointmentDialogContent collapse into seeding` &&
            ` those fields before the popup opens. // NOTE: handleDialogSaveButton keeps every branch: an edit rewrites the row in place and moves it to the picked owner when the Select changed` &&
            ` (_appointmentOwnerChange), a create appends to that person's appointments, and the Interval-appointment checkbox routes the new row to their interval HEADERS instead (_convertToHeader).` &&
            ` handleDeleteAppointment removes the appointment behind the popover. // NOTE: _validateDateTimePicker refuses an end date at or before the start and blocks the Save; the port binds the start picker's` &&
            ` valueState and applies the same rule on the change round-trip, and the save is skipped while the state is Error. That rule compares the two bound STRINGS (``d_end <= d_start``), so it only holds` &&
            ` while both stay lexicographically sortable ISO - and until 2026-08-26 it did not: the pickers carried no valueFormat, so the first edit wrote a locale string back ('Jan 10, 2017, 8:00:00 AM'`.
    text1 = text1 && ` measured) and the comparison silently stopped firing, letting an appointment save with its end five days before its start. Both pickers now pin valueFormat="yyyy-MM-dd'T'HH:mm:ss". Note the ORIGINAL` &&
            ` binds no value at all - it works in Date objects through setDateValue/getDateValue - so the string binding, and the format contract it owes, are the port's own. handleAppointmentTypeChange only` &&
            ` re-reads the checkbox, which is bound two-way here, so its select wire is dropped. // IMPROVISED: _handleGroupAppointments opens a Popover ANCHORED on the group's DOM node (openBy(` &&
            ` document.getElementById( domRefId ) )), built in the controller. A backend cannot address a raw DOM node, so the port reports the same sentence as a toast instead of an anchored popover; the counting` &&
            ` and the type comparison behind it are unchanged. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The details popover, the three dialog` &&
            ` modes, the owner change, the interval-header routing, the date validation and the group report are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_547.mjs). // NOTE: A JS callback is not in the UI5 expression grammar - ExpressionParser has no ``function`` keyword and reads { as an object literal, so the whole` &&
            ` handler string failed to parse and every argument was lost. The selection is read from the model instead: PlanningCalendarRow (and CalendarAppointment) declare a bindable ``selected``, so the flags` &&
            ` travel with the rows and ABAP does the work. Here that also covers the "do the selected appointments differ in type" test, which the original computes with .some( ). // NOTE: The details popover is` &&
            ` opened on the POPOVER slot, so it is closed with popover_destroy( ) - popup_destroy( ) tears down the separate POPUP slot and left the popover on screen. The EDIT branch closes it before opening the` &&
            ` dialog, which the original does explicitly ("The sap.m.Popover has to be closed before the sap.m.Dialog gets opened").`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarModifyAppointments`            class = `z2ui5_cl_smpc_app_547` path = `src/02/01/z2ui5_cl_smpc_app_547.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.CalendarAppointment ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor. It is kept 1:1 from the sample, so the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.ui.unified.CalendarAppointment ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor. It is kept 1:1 from the sample, so the port is filed under src/02. // NOTE: The calendar date` &&
            ` properties (the PlanningCalendar startDate and every CalendarAppointment / DateTypeRange startDate and endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and` &&
            ` Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: handleAppointmentSelect opens a MessageBox with the` &&
            ` appointment title, its new selected state and the number of selected appointments - or, when the interval selection hit no appointment, their count. Every value is client-readable, so it travels with` &&
            ` the event and ABAP composes both branches; the box is modal anyway, so the round-trip costs nothing. // NOTE: handleIntervalSelect pushes a 'new appointment' (Type09) into the row it hit, or - this` &&
            ` sample has singleSelection=false - into EVERY selected row. The row index and the selected-row indices travel with the event and ABAP does the same push. The interval's start and end travel as their`.
    text1 = text1 && ` LOCAL parts; a UTC toISOString( ) would shift the day. // NOTE: The two row images are the demo kit's own test-resources files (John_Miller.png, Donna_Moore.jpg), re-hosted on sdk.openui5.org. //` &&
            ` NOTE: The multi-row selection, the appointment select and the interval-select push are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_538.mjs). // NOTE: A JS callback is not in the UI5 expression grammar - ExpressionParser has no ``function`` keyword and reads { as an object literal, so the whole` &&
            ` handler string failed to parse and every argument was lost. The selection is read from the model instead: PlanningCalendarRow (and CalendarAppointment) declare a bindable ``selected``, so the flags` &&
            ` travel with the rows and ABAP does the work.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarMulti`                         class = `z2ui5_cl_smpc_app_538` path = `src/02/01/z2ui5_cl_smpc_app_538.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.CalendarAppointment ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor. It is kept 1:1 from the sample, so the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.m.PlanningCalendar appointmentHeight is @since 1.81.0 and multipleAppointmentsSelection @since 1.97, and sap-icon://select-appointments reached the icon font in 1.96 - all newer than` &&
            ` the 1.71 floor. The one-line appointment height and the multi-selection toggle are what this sample is about, so all three are kept and the port is filed under src/02. // NOTE: The calendar date` &&
            ` properties (the PlanningCalendar startDate and every CalendarAppointment / DateTypeRange startDate and endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and` &&
            ` Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: onPress flips setMultipleAppointmentsSelection and` &&
            ` swaps the button's tooltip. The property is bindable, so the ToggleButton's pressed state and the calendar share one flag and the press needs no handler; the tooltip is a second bound field carrying` &&
            ` the two texts the original sets. // NOTE: handleAppointmentSelect also writes the selected-appointment count into the ToggleButton's BadgeCustomData. The count already travels with the event for the`.
    text1 = text1 && ` MessageBox, so the same round-trip fills the bound badge value. // IMPROVISED: handleSortChange hands the calendar a JavaScript comparator through setCustomAppointmentsSorterCallback` &&
            ` (fnAlphabeticalOrder, a case-insensitive title compare). abap2UI5 cannot register a callback, so the port sorts the appointment rows THEMSELVES in ABAP with the same rule and re-seeds the original` &&
            ` order for the default entry. Same visible order, different mechanism: the calendar keeps its default sorter and the model arrives pre-sorted, so an appointment ADDED after the switch is not re-sorted` &&
            ` until the Select is used again. // NOTE: handleIntervalSelect pushes a 'new appointment' (Type09) into the row it hit, or into every selected row; the indices travel with the event and ABAP does the` &&
            ` same push. The interval's start and end travel as their LOCAL parts; a UTC toISOString( ) would shift the day. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on` &&
            ` sdk.openui5.org. // NOTE: The multi-selection toggle with its badge, the alphabetical sort and the interval-select push are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e`.
    text1 = text1 && ` interaction, meta/interactions/z2ui5_cl_smpc_app_539.mjs). // NOTE: A JS callback is not in the UI5 expression grammar - ExpressionParser has no ``function`` keyword and reads { as an object literal,` &&
            ` so the whole handler string failed to parse and every argument was lost. The selection is read from the model instead: PlanningCalendarRow (and CalendarAppointment) declare a bindable ``selected``,` &&
            ` so the flags travel with the rows and ABAP does the work.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarOneLine`                       class = `z2ui5_cl_smpc_app_539` path = `src/02/01/z2ui5_cl_smpc_app_539.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.PlanningCalendar appointmentHeight is @since 1.81.0 and multipleAppointmentsSelection @since 1.97, and sap-icon://select-appointments reached the icon font in 1.96 - all newer than the 1.71` &&
                 ` floor. The one-line appointment height and the multi-selection toggle are what this sample is about, so all three are kept and the port is filed under src/02.` ) ).

    text1 = `POST-1.71: The whole recurrence stack is newer than the 1.71 floor: sap.m.PlanningCalendar rowHeaderPress @since 1.119.0, sap.m.PlanningCalendarRow nonWorkingPeriods @since 1.128 (property and` &&
            ` aggregation), sap.ui.unified.RecurringNonWorkingPeriod @since 1.127.0, sap.ui.unified.TimeRange @since 1.127, sap.ui.unified.RecurringCalendarAppointment @since 1.149 and` &&
            ` sap.ui.unified.RecurrenceRule @since 1.149. Recurring appointments ARE the sample, so all of them are kept and the port is filed under src/02. // NOTE: The calendar date properties (the` &&
            ` PlanningCalendar startDate, every appointment startDate / endDate and recurrenceEndDate and the non-working period's date) are typed "object" and demand a real JS Date. The model keeps ISO strings` &&
            ` and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). The TimeRange start / end stay plain strings, which is what` &&
            ` its valueFormat expects. // NOTE: The create dialog lives in its own fragment with its own named model (create>). abap2UI5 keeps one default model, so the dialog's fifteen fields are folded to flat`.
    text1 = text1 && ` fields here and every visibility expression the fragment carries reads them directly. // NOTE: _openCreateDialog fills the person Select by ADDING an Item per row of the people table in the` &&
            ` controller. The port binds a small key/text table built from the same rows instead, so the Select carries the same entries declaratively. // NOTE: onAppointmentCreate opens the create dialog pre-set` &&
            ` to the row the drag started in and to the dragged interval; the row's binding path and the interval's LOCAL date parts travel with the event. onCreateDialogSave assembles the appointment and pushes` &&
            ` it into the picked person's row, with the same recurrence branches the original applies. // NOTE: ``recurrencePattern`` carries ``defaultValue: 1`` and ``setRecurrencePattern`` REJECTS anything below` &&
            ` it (``throw new Error("recurrencePattern must be >= 1")``, RecurringCalendarAppointment.js:163). onCreateDialogSave writes the property only inside its ``if (oData.recurrenceType)`` branch, so a` &&
            ` NON-recurring new appointment carries no such key at all and the control keeps its default. An ABAP structure always serializes every field, so the port's initial 0 reached the setter through the`.
    text1 = text1 && ` ``{RECURRENCEPATTERN}`` binding and terminated the app the first time an appointment was created without a recurrence (reported 2026-08-29 from a Developer Tools export on UI5 1.150 -` &&
            ` ``_processAfterRendering: unexpected error - recurrencePattern must be >= 1``; reproduced standalone on OpenUI5 1.151, where pattern 0 throws while the view is created and pattern 1 renders three` &&
            ` appointments). The port therefore seeds the new row with the control's own default, ``recurrencepattern = 1``, and the recurrence branch overwrites it as before. The sibling` &&
            ` ``sap.ui.unified.RecurringNonWorkingPeriod.recurrencePattern`` has the identical setter (``RecurringNonWorkingPeriod.js:145``), but no ABAP writes a non-working row - they are only seeded - so that` &&
            ` one carries the default in the BINDING instead: the port writes ``recurrencePattern="{= ${RECURRENCEPATTERN} || 1 }"`` where the original writes ``{recurrencePattern}``, which is the ``binding` &&
            ` value`` difference structural-diff reports for it. Measured on OpenUI5 1.151: bound 0 throws, the expression renders 0 as the control's 1 and leaves a real 2 alone, and an EMPTY non-working table is`.
    text1 = text1 && ` safe either way because a template with no row behind it keeps the default. // NOTE: onAppointmentDrop shifts the appointment by the dragged DELTA and then copies it, moves it to another row, or just` &&
            ` reschedules it in place - the three branches and their three toasts are kept. The new start and end already carry the delta, so the port re-dates the row from the two interval bounds the event hands` &&
            ` it; the appointment's and the row's binding paths name the source and the target. // IMPROVISED: onCreateAppointment seeds the dialog with the CLIENT's next full hour (UI5Date.getInstance() with` &&
            ` minutes zeroed). A backend has no client clock, so the port seeds the SERVER's local date and next full hour instead. Same shape, a different clock - which matters only if the two are in different` &&
            ` time zones. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The recurring appointments and non-working periods, the create dialog with its` &&
            ` recurrence branches, the drag-create and the drop's copy / move / reschedule branches are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_548.mjs). The dialog's save path - creating an appointment with NO recurrence, the case that used to terminate the app - is covered by the same interaction since` &&
            ` 2026-08-29. // NOTE: The two DateTimePickers and the recurrence DatePicker bind their value UNTYPED, where the original binds them with type sap.ui.model.type.DateTime / sap.ui.model.type.Date and` &&
            ` formatOptions { pattern: 'yyyy-MM-dd HH:mm' } / { pattern: 'yyyy-MM-dd' } (CreateAppointmentDialog.fragment.xml:34,40,118). The port keeps the path and carries the pattern as valueFormat +` &&
            ` displayFormat instead, which is not a cosmetic choice: a typed binding with a source pattern raises on the EMPTY value the recurrence picker seeds with and a cleared picker sends, which is why apps` &&
            ` 549 and 609 settled on the same form. Until 2026-08-26 the port declared neither, and that was a real defect rather than a simplification - an unformatted picker writes a LOCALE string back through` &&
            ` the two-way binding. Measured in de-DE: picking 4 March 2025 stored '04.03.2025, 10:15:00', which Formatter.DateCreateObject's new Date( ) reads month-first, so the appointment was drawn on 3 April.`.
    text1 = text1 && ` structural-diff cannot see this class at all - the port HAS a value attribute, so nothing reads as missing.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarRecurringItem`                 class = `z2ui5_cl_smpc_app_548` path = `src/02/01/z2ui5_cl_smpc_app_548.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `The whole recurrence stack is newer than the 1.71 floor: sap.m.PlanningCalendar rowHeaderPress @since 1.119.0, sap.m.PlanningCalendarRow nonWorkingPeriods @since 1.128 (property and aggregation),` &&
                 ` sap.ui.unified.RecurringNonWorkingPeriod @since 1.127.0, sap.ui.unified.TimeRange @since 1.127, sap.ui.unified.RecurringCalendarAppointment @since 1.149 and sap.ui.unified.RecurrenceRule @since` &&
                 ` 1.149. Recurring appointments ARE the sample, so all of them are kept and the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.m.PlanningCalendarView intervalSize and relative are both @since 1.93 - newer than the 1.71 floor. The two relative views ARE the sample, so both are kept and the port is filed under` &&
            ` src/02. // NOTE: The calendar date properties (the PlanningCalendar startDate and minDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The model` &&
            ` keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: The sample's appointment rows` &&
            ` carry an ariaHasPopup value the view never binds; the port seeds it the same way and leaves it unbound, so the data stays 1:1 with the mock. The controller has no handlers at all - the sample is` &&
            ` purely declarative - so the port has no dispatcher either. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The two relative views (Weeks in` &&
            ` Project and Shift) and the minDate limit are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_545.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarRelativeViews`                 class = `z2ui5_cl_smpc_app_545` path = `src/02/01/z2ui5_cl_smpc_app_545.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.PlanningCalendarView intervalSize and relative are both @since 1.93 - newer than the 1.71 floor. The two relative views ARE the sample, so both are kept and the port is filed under src/02.` ) ).

    text1 = `NOTE: The object-typed calendar date properties (PlanningCalendar.startDate, CalendarAppointment.startDate/endDate) are fed from plain ISO strings in the model and converted at the point of use with` &&
            ` Formatter.DateCreateObject from the curated module (core:require='{Formatter: z2ui5/model/formatter}'). The original's UI5Date.getInstance(year, month0, day, ...) values are normalized to ISO 1:1` &&
            ` (month is 0-based; day/month overflow rolled forward exactly as the JS Date constructor does). // NOTE: appointmentSelect / intervalSelect / the toggleDayNamesLine ToggleButton are reproduced since` &&
            ` 2026-08-05, after the 2026-07-27 review had retyped the entry from NOTE to IMPROVISED because a lost behaviour is not a NOTE. handleAppointmentSelect's MessageBox is composed in ABAP from values the` &&
            ` event carries (${$parameters>/appointment}.getTitle() / .getSelected(), $event.oSource.getSelectedAppointments().length and the appointments array length for the no-appointment branch), so both` &&
            ` branches of the original read exactly as before - the message is modal, so the round-trip costs nothing visible. handleIntervalSelect appends the sample's new appointment ('new appointment', Type09)`.
    text1 = text1 && ` over the selected interval, whose start/end travel as their LOCAL parts (a UTC toISOString( ) would shift the day). toggleDayNamesLine is the bindable showDayNamesLine (@since 1.50) shared with the` &&
            ` ToggleButton's pressed state, so the press attribute is not emitted - the prefer-a-bindable-property rule. // POST-1.71: Formatter.DateCreateObject is referenced via core:require, which needs UI5 >=` &&
            ` 1.74. sap.m.PlanningCalendar itself is since 1.34 (in scope). Also sap.ui.unified.CalendarAppointment.ariaHasPopup (@since 1.150.0) is kept 1:1 from the original view (ariaHasPopup='{ariaHasPopup}');` &&
            ` newer than 1.71, declared. Was undeclared because the property gate was sap.m-only (now extended to all libs, 2026-07-24). // NOTE: The 2026-07-27 live check is retired and the port is back to` &&
            ` ``generated`` (AGENTS section 10: a behavioural rework of a checked port resets the status or is restamped after a fresh live run). Two things were wrong with it. Its note read "verified in a running` &&
            ` system 2026-07-27 - PlanningCalendar renders all appointments/interval headers via Formatter.DateCreateObject; interactions toast as declared" - but this port raises no toast at all: both handlers`.
    text1 = text1 && ` call message_box_display, the faithful reproduction of the original's MessageBox.show, and message_toast_display does not occur in the class. And it certified interactions the port did not have on` &&
            ` that date: appointmentSelect, intervalSelect and the ToggleButton were reproduced on 2026-08-05, which the port's own deviation and docs/history.md both record, so the 2026-07-27 run can only have` &&
            ` covered RENDERING. What still stands from it, and is worth keeping: the PlanningCalendar rendered every appointment and interval header through Formatter.DateCreateObject in a running system. What` &&
            ` needs a fresh live run: the two MessageBox legs and the showDayNamesLine toggle. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_108.mjs). // NOTE: The` &&
            ` intervalHeaders CalendarAppointment template drops the original's icon="{pic}" (Page.view.xml, the intervalHeaders aggregation - the appointments template one block above carries the same attribute` &&
            ` and the port DOES keep that one). Declared rather than added: the sample's three header rows carry no pic key either, so the binding is inert on both sides, and a public model field whose only job is`.
    text1 = text1 && ` to render an empty attribute is exactly the persisted-state cost AGENTS section 10 warns about.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarSingle`                        class = `z2ui5_cl_smpc_app_108` path = `src/02/01/z2ui5_cl_smpc_app_108.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `Formatter.DateCreateObject is referenced via core:require, which needs UI5 >= 1.74. sap.m.PlanningCalendar itself is since 1.34 (in scope). Also sap.ui.unified.CalendarAppointment.ariaHasPopup (@since` &&
                 ` 1.150.0) is kept 1:1 from the original view (ariaHasPopup='{ariaHasPopup}'); newer than 1.71, declared. Was undeclared because the property gate was sap.m-only (now extended to all libs, 2026-07-24).` ) ).

    text1 = `POST-1.71: sap.ui.unified.CalendarAppointment ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor. It is kept 1:1 from the sample, so the port is filed under src/02. // POST-1.71: The` &&
            ` sap.ui.unified.CalendarDayType ENUM VALUE NonWorking is @since 1.121 and is kept 1:1 from the original. The attribute it rides on (DateTypeRange.type) is base-version, so the property gate sees a` &&
            ` known member with a value it does not judge - the enum-value residual limit of AGENTS section 5. Same declaration app 308 carries; found by scripts/probes/post171-blindspot-probe.mjs. The app needs a` &&
            ` UI5 release >= 1.121 for those days to render as non-working. // NOTE: The calendar date properties (the PlanningCalendar startDate and every CalendarAppointment / DateTypeRange startDate and` &&
            ` endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108` &&
            ` uses (needs UI5 >= 1.74). // NOTE: determineControlsVisibility shows the Label only in the nonWorking view and the Select only in the months view on a desktop. viewKey is bindable, so the key is one`.
    text1 = text1 && ` shared field and both controls carry the switch as an expression binding - the Label over the key alone, the Select over the key AND the device model's system/desktop flag. handleViewChange, which` &&
            ` exists only to re-run that method, is dropped with them. // NOTE: handleGroupModeChange calls setGroupAppointmentsMode; the property is bindable, so the Select shares its key with the calendar and` &&
            ` the change handler is dropped. handleSelectionFinish's setBuiltInViews keeps its wire: the picked keys travel as one comma-separated argument and ABAP fills the bound string table. // NOTE:` &&
            ` handleIntervalSelect has two branches. In the nonWorking view handleNonWorkingSpecialDates toggles a NonWorking DateTypeRange on the selected date - the specialDates aggregation is bound to a table` &&
            ` here and the round-trip adds or removes the row, which is the same toggle. Otherwise the handler pushes a 'new appointment' (Type09) into the row it hit, or into every selected row; the row index and` &&
            ` the selected-row indices travel with the event and ABAP does the same push. The interval's start and end travel as their LOCAL parts - a UTC toISOString( ) would shift the day. // IMPROVISED: onPress`.
    text1 = text1 && ` on the 'Toggle custom views' ToggleButton removes the four PlanningCalendarViews from the aggregation and adds them back, by constructing PlanningCalendarView instances in the controller. The views` &&
            ` aggregation is declared in the view here and a backend cannot add or remove aggregation children of a rendered view, so the four views stay declared and the ToggleButton keeps its text but loses its` &&
            ` press wire. // NOTE: The two row images are the demo kit's own test-resources files (John_Miller.png, Donna_Moore.jpg), re-hosted on sdk.openui5.org. // NOTE: The four views, the non-working day` &&
            ` toggle, the interval-select appointment push, the built-in views box and the two nonWorkingDays / nonWorkingHours arrays are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_537.mjs). // NOTE: **e2e-caught 2026-08-22**: the sample's own data carries an upstream typo - UI5Date.getInstance(201, 2, 4, 13, 30), a year of 201` &&
            ` where every neighbouring row says 2017 (Page.controller.js:110). In JavaScript that is a VALID date (4 March 201 AD); as the ISO string the port stores it became '201-03-04T13:30:00', which new`.
    text1 = text1 && ` Date() cannot parse at all, so the appointment reached the calendar as an Invalid Date and CalendarUtils._checkJSDateObject took the whole app down. The year is now written '0201', which is the same` &&
            ` absurd date the original produces and which parses. The typo itself is kept: it is the sample's data. // NOTE: A JS callback is not in the UI5 expression grammar - ExpressionParser has no` &&
            ` ``function`` keyword and reads { as an object literal, so the whole handler string failed to parse and every argument was lost. The selection is read from the model instead: PlanningCalendarRow (and` &&
            ` CalendarAppointment) declare a bindable ``selected``, so the flags travel with the rows and ABAP does the work.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarViews`                         class = `z2ui5_cl_smpc_app_537` path = `src/02/01/z2ui5_cl_smpc_app_537.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.CalendarAppointment ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor. It is kept 1:1 from the sample, so the port is filed under src/02. // The sap.ui.unified.CalendarDayType` &&
                 ` ENUM VALUE NonWorking is @since 1.121 and is kept 1:1 from the original. The attribute it rides on (DateTypeRange.type) is base-version, so the property gate sees a known member with a value it does` &&
                 ` not judge - the enum-value residual limit of AGENTS section 5. Same declaration app 308 carries; found by scripts/probes/post171-blindspot-probe.mjs. The app needs a UI5 release >= 1.121 for those` &&
                 ` days to render as non-working.` ) ).

    text1 = `POST-1.71: sap.m.PlanningCalendar calendarWeekNumbering is @since 1.110.0 - newer than the 1.71 floor. The four week-numbering schemes ARE the sample, so the property is kept and the port is filed` &&
            ` under src/02. // NOTE: The calendar date properties (the PlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The model keeps` &&
            ` ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: onCalendarWeekNUmberingSelect calls` &&
            ` setCalendarWeekNumbering. The property is bindable and the sample's own view already binds the Select to the SAME model path, so the two share one field here and the change handler is dropped. //` &&
            ` NOTE: handleAppointmentSelect opens a MessageBox with the appointment title, its new selected state and the number of selected appointments - or, when the interval selection hit no appointment, their` &&
            ` count. Every value is client-readable, so it travels with the event and ABAP composes both branches. // NOTE: handleSelectionFinish hands the MultiComboBox's selected keys to setBuiltInViews. The`.
    text1 = text1 && ` picked keys travel as one comma-separated argument and ABAP fills the bound string table. // NOTE: The sample's appointment rows carry an ariaHasPopup value the view never binds; the port seeds it` &&
            ` the same way and leaves it unbound, so the data stays 1:1 with the mock. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The four` &&
            ` week-numbering schemes, the appointment select and the built-in views box are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_544.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarWeekNumbering`                 class = `z2ui5_cl_smpc_app_544` path = `src/02/01/z2ui5_cl_smpc_app_544.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.PlanningCalendar calendarWeekNumbering is @since 1.110.0 - newer than the 1.71 floor. The four week-numbering schemes ARE the sample, so the property is kept and the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.ui.unified.DateTypeRange secondaryType is @since 1.81.0 and color @since 1.76.0, and sap.m.PlanningCalendar firstDayOfWeek is @since 1.94 - all newer than the 1.71 floor. The legend's` &&
            ` coloured special dates and the row's secondary type are what this sample shows, so both are kept and the port is filed under src/02. // POST-1.71: The sap.ui.unified.CalendarDayType ENUM VALUE` &&
            ` NonWorking is @since 1.121 and is kept 1:1 from the original. The attribute it rides on (DateTypeRange.type) is base-version, so the property gate sees a known member with a value it does not judge -` &&
            ` the enum-value residual limit of AGENTS section 5. Same declaration app 308 carries; found by scripts/probes/post171-blindspot-probe.mjs. The app needs a UI5 release >= 1.121 for those days to render` &&
            ` as non-working. // NOTE: The calendar date properties (the PlanningCalendar startDate and every CalendarAppointment / DateTypeRange startDate and endDate) are typed "object" and demand a real JS` &&
            ` Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: The original`.
    text1 = text1 && ` keeps the legend-shown flag in a SECOND named model (stateModel>). abap2UI5 keeps one default model, so the flag is a field here and the DynamicSideContent and the ToggleButton share it - the same` &&
            ` two-way link the named model gives them. // NOTE: onChange calls setFirstDayOfWeek( Number( key ) ). The property is bindable, so the Select shares its key with the calendar and the change handler is` &&
            ` dropped; the key values ('-1' .. '6') are the sample's own. firstDayOfWeek is an INT property while a Select key is a string, so the calendar binds an expression that multiplies by 1 - the Number( )` &&
            ` conversion the original does in the handler. // NOTE: onBeforeRendering / handleViewChange / changeStandardItemsPerView exist only to swap the legend's standardItems between [Today, Selected,` &&
            ` NonWorkingDay] on the OneMonth view and [Today, WorkingDay, NonWorkingDay] everywhere else. viewKey and standardItems are both bindable, so the key is one shared field and the legend carries the` &&
            ` switch as an expression binding over it - all three handlers are dropped. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The legend side`.
    text1 = text1 && ` content, the first-day-of-week Select and the view-dependent standard items are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_541.mjs). // NOTE: **e2e-caught 2026-08-22**: one special-date row was still missing its secondarytype seed after the b46 sweep fixed the others - the NonWorking` &&
            ` range on Sophie Miller (2017-01-16 to 2017-01-18). A flat ABAP row serializes every field, so it sent an empty string into the CalendarDayType enum and UI5 terminated the app. It is seeded 'None'` &&
            ` like the rest. The lesson is the sweep's own: the fix has to cover EVERY row of the table, not the ones the first failure named. // NOTE: The three root-level aggregations (specialDates on the` &&
            ` calendar, items and appointmentItems on the legend) are bound ABSOLUTELY via _bind( path = abap_true ). A bare 'T_X' path is relative and resolves against nothing outside a row context, and an` &&
            ` unbound table is not serialized at all - either alone leaves the aggregation empty. App 553 carries the same two fixes; the row-level aggregations inside PlanningCalendarRow stay relative, which is` &&
            ` correct there.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarWithLegend`                    class = `z2ui5_cl_smpc_app_541` path = `src/02/01/z2ui5_cl_smpc_app_541.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.DateTypeRange secondaryType is @since 1.81.0 and color @since 1.76.0, and sap.m.PlanningCalendar firstDayOfWeek is @since 1.94 - all newer than the 1.71 floor. The legend's coloured` &&
                 ` special dates and the row's secondary type are what this sample shows, so both are kept and the port is filed under src/02. // The sap.ui.unified.CalendarDayType ENUM VALUE NonWorking is @since 1.121` &&
                 ` and is kept 1:1 from the original. The attribute it rides on (DateTypeRange.type) is base-version, so the property gate sees a known member with a value it does not judge - the enum-value residual` &&
                 ` limit of AGENTS section 5. Same declaration app 308 carries; found by scripts/probes/post171-blindspot-probe.mjs. The app needs a UI5 release >= 1.121 for those days to render as non-working.` ) ).

    text1 = `NOTE: The calendar date properties (the PlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and` &&
            ` Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: handleAppointmentSelect opens a MessageBox with the` &&
            ` appointment title, its new selected state and the number of selected appointments - or, when the interval selection hit no appointment, their count. Every value is client-readable, so it travels with` &&
            ` the event and ABAP composes both branches. // NOTE: handleSelectionFinish hands the MultiComboBox's selected keys to setBuiltInViews. The picked keys travel as one comma-separated argument and ABAP` &&
            ` fills the bound string table, so the calendar's built-in views really follow the box. // NOTE: The sample's appointment rows carry an ariaHasPopup value the view never binds; the port seeds it the` &&
            ` same way and leaves it unbound, so the data stays 1:1 with the mock. // NOTE: The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The sticky header, the`.
    text1 = text1 && ` week numbers and the built-in views box are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_542.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PlanningCalendar`                name = `PlanningCalendarWithStickyHeader`              class = `z2ui5_cl_smpc_app_542` path = `src/01/01/z2ui5_cl_smpc_app_542.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84) is kept 1:1 on the two view buttons (ariaHasPopup='Dialog'); the app needs a UI5 release >= 1.84 to render it. // NOTE:` &&
            ` handlePopoverPress/handleResizablePopoverPress lazily Fragment.load a Popover and openBy(button). The port builds each popover as a core:FragmentDefinition on the button-press round-trip and shows it` &&
            ` anchored to the pressed button via client->popover_display( xml = ..., by_id = $event.oSource.sId ) - the popover XML parameter is named ``xml`` (not ``val`` like popup_display). This is a` &&
            ` server-round-trip where the original is a client-side Fragment.load, but it is faithful (lazy either way) and matches app 094. // NOTE: the original oPopover.bindElement('/ProductCollection/0') binds` &&
            ` each popover to a single record; the port seeds that record's fields (name, productpicurl) at the default-model root and the popover's relative bindings {NAME}/{PRODUCTPICURL} resolve against the` &&
            ` root - same data, structural-diff matches on the last path segment (app 175 single-record-flatten idiom). No bind_element needed because the record is the static row 0, not a per-row selection.`.
    text1 = text1 && ` **Corrected 2026-08-01**: those bindings were written RELATIVE ({FIELD}) and a relative path on a control with no binding context resolves against nothing — the fields rendered EMPTY in the running` &&
            ` app (measured with the e2e harness, the app-207 class). They are now bound ABSOLUTELY through client->_bind( field ), which is what a root-seeded record needs. // NOTE: handleEmailPress does` &&
            ` byId('myPopover').close() + MessageToast.show('E-Mail has been sent'), and handleClose does byId('myResizablePopover').close(); both close the popover slot via follow_up_action(` &&
            ` cs_event-popover_close ) (there is one popover slot open at a time), the email variant additionally toasting via message_toast_display. // NOTE: the JSONModel is loaded from the shared` &&
            ` sap/ui/demo/mock/products.json; only the single record /ProductCollection/0 is bound, so only its name + picture URL are seeded. The mock's host-relative ProductPicUrl ('test-resources/...') is` &&
            ` resolved to an absolute sdk.openui5.org URL. // NOTE: partly verified 2026-08-01 (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the first popover opens anchored to its button`.
    text1 = text1 && ` (popover_display by_id = $event.oSource.sId) AND the root-seeded record now reaches it - the popover shows 'Notebook Basic 15' (it rendered empty until the relative bindings were made absolute the` &&
            ` same day, see the corrected NOTE above). Still unverified in a running system: the second popover's placement, and that the Email / Close footer buttons close it (popover_close) with the Email one` &&
            ` toasting 'E-Mail has been sent'. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_229.mjs). (Corrected 2026-08-23: handleEmailPress closes the popover and` &&
            ` shows that toast - there is no mailto: and no URLHelper on either side. The port reproduces it exactly; the sidecar had invented a third effect and filed it as an open verification item.)`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Popover`                         name = `Popover`                                       class = `z2ui5_cl_smpc_app_229` path = `src/02/01/z2ui5_cl_smpc_app_229.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `Button.ariaHasPopup (since UI5 1.84) is kept 1:1 on the two view buttons (ariaHasPopup='Dialog'); the app needs a UI5 release >= 1.84 to render it.` ) ).

    text1 = `POST-1.71: Link.ariaHasPopup (since 1.86) is kept 1:1 on the popover link; needs UI5 >= 1.86. // NOTE: the row Popover reproduces the original bindElement: it is built per-press with relative bindings` &&
            ` ({PRODUCTID} title, {NAME}, {PRODUCTPICURL}) and follow_up_action( cs_event-bind_element, view=cs_view-popover ) element-binds the popover slot to t_products/<index>, where the index comes from the` &&
            ` pressed row's binding context ($event.oSource.getBindingContext().getPath()); the popover is anchored by $event.oSource.sId and the Action button reproduces handleActionPress 1:1 (a toast 'Action has` &&
            ` been pressed' + follow_up_action( cs_event-popover_close )). The disable/enable-pointer-events-while-open behaviour is NOT dropped - corrected 2026-08-23, this sentence was left behind by the` &&
            ` 2026-08-21 restoration and contradicted the next deviation, which describes the reproduction: afterOpen/afterClose toggle a style class on idProductsTable via control_by_id, with the rule injected as` &&
            ` a core:HTML style leaf. // NOTE: Two legs of the original restored 2026-08-21, both found undeclared by the review sweep. (a) onInit's oModel.setSizeLimit( 10 ) is reproduced 1:1 through`.
    text1 = text1 && ` follow_up_action( cs_event-set_size_limit ) - the same listed frontend action app 252 uses; the port rendered all 123 rows where the original shows ten. (b) The sample's headline behaviour - table` &&
            ` clicks dead while the popover is open, which the page header advertises in so many words - was dropped. attachAfterOpen -> disablePointerEvents / afterClose -> enablePointerEvents are now the` &&
            ` popover's afterOpen/afterClose toggling a style CLASS on idProductsTable via control_by_id addStyleClass / removeStyleClass, with the rule injected as a core:HTML style leaf. The css control method` &&
            ` could NOT carry this: pointer-events is not on the framework's CSS_PROPERTIES whitelist, which is why the class-toggle is the expressible form rather than a stylistic choice. // NOTE: The sample's` &&
            ` asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
            ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by`.
    text1 = text1 && ` all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since` &&
            ` today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Popover`                         name = `PopoverControllingCloseBehavior`               class = `z2ui5_cl_smpc_app_094` path = `src/02/01/z2ui5_cl_smpc_app_094.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `Link.ariaHasPopup (since 1.86) is kept 1:1 on the popover link; needs UI5 >= 1.86.` ) ).

    text1 = `POST-1.71: one post-1.71 member is kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the Open Popover button, which the sample's own view.xml carries. // NOTE: Popover.fragment.xml is` &&
            ` rebuilt as a core:FragmentDefinition and shown with client->popover_display( by_id ), anchored to the pressed button exactly as onOpenPopover's openBy(oButton) does (app 229 idiom) - so the Popover,` &&
            ` its NavContainer, the two Pages, the List, the StandardListItem, the ObjectHeader with its two ObjectAttributes and the Text are extra against the archived PopoverNavCon.view.xml, which only holds` &&
            ` the l:VerticalLayout and the Button. The original caches the fragment and re-opens the same instance; the port rebuilds it per press, which is why onNavToProduct's own re-anchoring is not needed. //` &&
            ` NOTE: onNavToProduct does oNavCon.to(oDetailPage) plus oDetailPage.bindElement(ctx.getPath()). Navigation is the frontend action (control_by_id, view = cs_view-popup, 'to' on navCon; onNavBack is the` &&
            ` matching 'back'), and the element binding is folded to root-seeded fields: the pressed row's ProductId travels with the press, the backend copies that row into the D_* fields and the detail page`.
    text1 = text1 && ` binds them absolutely. The two ObjectAttribute texts keep their composite shape 1:1 ({WeightMeasure} {WeightUnit} and {Width} x {Depth} x {Height} {DimUnit}), only over the folded fields. // NOTE:` &&
            ` both handlers end with byId('myPopover').focus() so keyboard focus stays inside the popover after a page change. The port does not re-issue it: the popover is never re-anchored across the navigation,` &&
            ` so focus is not taken away, and a set_focus on an already-focused popup would be a no-op round trip. // NOTE: the popover keeps ariaLabelledBy="master-title" 1:1 even though no control with that id` &&
            ` exists in the fragment - the same dangling reference the original ships. // NOTE: the full mock /ProductCollection is seeded with the fields the two pages bind (ProductId, Name, ProductPicUrl,` &&
            ` WeightMeasure, WeightUnit, Width, Depth, Height, DimUnit, Description), all 123 rows. // NOTE: not yet verified in a running system: the anchored popover with a NavContainer inside it and the to/back` &&
            ` frontend actions on the popup view. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_565.mjs). // NOTE: The details popover is opened on the POPOVER slot, so`.
    text1 = text1 && ` it is closed with popover_destroy( ) and addressed with cs_view-popover. popup_destroy( )/cs_view-popup target a different slot that this app never opens, so the close and the in-popover navigation` &&
            ` were silent no-ops.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Popover`                         name = `PopoverNavCon`                                 class = `z2ui5_cl_smpc_app_565` path = `src/02/01/z2ui5_cl_smpc_app_565.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `one post-1.71 member is kept for the 1:1 port: Button.ariaHasPopup (since UI5 1.84) on the Open Popover button, which the sample's own view.xml carries.` ) ).

    text1 = `NOTE: the sample's point - Popup.setWithinArea(this.byId('withinArea')), which confines every popup to the grey VBox instead of the viewport - is reproduced through the CONTROL_GLOBAL target POPUP` &&
            ` with the method setWithinArea (added to abap2UI5 for this port; the pr/popup-within-area request is implemented). Two differences to the original wiring, both without visible effect here: it is set` &&
            ` ONCE together with the view instead of in each press handler, because a follow_up_action runs AFTER the popover of the same round-trip has already opened - and the Popover.afterClose handler of all` &&
            ` three popovers, whose only job was Popup.setWithinArea(null), is therefore dropped. This app opens no other popup, so a permanently set area behaves exactly like the per-press one. Needs UI5 >= 1.89` &&
            ` for setWithinArea. // NOTE: the three popovers live in three *.fragment.xml files the controller loads with Fragment.load and opens with openBy(oButton). They are rebuilt as three` &&
            ` core:FragmentDefinition documents shown with popover_display( xml = ... by_id = ... ), anchored on the pressing button transported as $event.oSource.sId - the documented 1:1 path. // NOTE: flattened`.
    text1 = text1 && ` element binding: each popover gets bindElement('/ProductCollection/0'), and the image popover's Popover.title {Name} and Image src {ProductPicUrl} are relative to it. That record (products.json row` &&
            ` 0: Notebook Basic 15 / HT-1000.jpg) is seeded at the ABAP model root, so both bind ABSOLUTELY via client->_bind - a relative {NAME} on a control with no binding context resolves against nothing and` &&
            ` renders empty. structural-diff pairs the port's Popover.title against the wrong one of the three Popovers (it reports 'original {Name} vs port Products', the list popover's literal title); both` &&
            ` titles are 1:1 with the popover they sit on. // NOTE: the two list popovers also carry bindElement('/ProductCollection/0') in the original, but nothing in them is bound relatively - the List binds` &&
            ` the absolute {/ProductCollection} - so that element binding has no effect and has no counterpart here. // POST-1.71: sap.m.Button.ariaHasPopup (since UI5 1.84) is kept 1:1 on all three buttons - the` &&
            ` app needs a UI5 release >= 1.84 for the aria-haspopup attribute. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app`.
    text1 = text1 && ` has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED` &&
            ` 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 128 ports do it now` &&
            ` (re-measured 2026-08-23, one day after the count above - the drift this note warns about, in one day), and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim` &&
            ` holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken. // POST-1.71: POPUP.setWithinArea is a control_global frontend action since UI5 1.89 -` &&
            ` newer than the 1.71 floor but kept for the 1:1 port: it is what confines the popover to the sample's own area. Below 1.89 the lazy require returns undefined and the dispatch hits its "not available"` &&
            ` guard, so the popover opens unconfined rather than not at all - the app needs a UI5 release >= 1.89 for the sample's behaviour.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Popover`                         name = `PopoverWithinArea`                             class = `z2ui5_cl_smpc_app_285` path = `src/02/01/z2ui5_cl_smpc_app_285.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Button.ariaHasPopup (since UI5 1.84) is kept 1:1 on all three buttons - the app needs a UI5 release >= 1.84 for the aria-haspopup attribute. // POPUP.setWithinArea is a control_global frontend` &&
                 ` action since UI5 1.89 - newer than the 1.71 floor but kept for the 1:1 port: it is what confines the popover to the sample's own area. Below 1.89 the lazy require returns undefined and the dispatch` &&
                 ` hits its "not available" guard, so the popover opens unconfined rather than not at all - the app needs a UI5 release >= 1.89 for the sample's behaviour.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ProgressIndicator`               name = `ProgressIndicator`                             class = `z2ui5_cl_smpc_app_070` path = `src/02/01/z2ui5_cl_smpc_app_070.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.13.1`
        is_post171 = abap_true
        notes = `NOTE: the two interactive ProgressIndicators (pi-with-animation / pi-without-animation) are set to 0/100 via two-way bound percentValue/displayValue fields updated in a SET event, replacing the` &&
                 ` original's controller byId(...).setPercentValue/setDisplayValue calls. // POST-1.71: ProgressIndicator.displayAnimation (since UI5 1.73) is kept 1:1 on the no-animation ProgressIndicator; needs UI5` &&
                 ` >= 1.73.`
        post171 = `ProgressIndicator.displayAnimation (since UI5 1.73) is kept 1:1 on the no-animation ProgressIndicator; needs UI5 >= 1.73.` ) ).

    text1 = `POST-1.71: sap.m.ProgressIndicator.displayAnimation is @since 1.73; the second indicator of the sample exists precisely to show it switched off, so the property is kept 1:1 and the port needs a UI5` &&
            ` runtime >= 1.73. // NOTE: onPIChangeValueButtonPressed sets displayValue and percentValue imperatively on the ProgressIndicator it derives from the pressed button's id. The port binds both properties` &&
            ` per indicator and writes them in ABAP; the id split (everything before '-button' is the indicator, everything after is the value) is transported 1:1 via $event.oSource.sId, so the wire reads the real` &&
            ` source id instead of a fixed value. percentValue is added to both ProgressIndicators, which the original only ever sets from code. // NOTE: InvisibleMessage.getInstance().announce(...) is reproduced` &&
            ` through the global INVISIBLE_MESSAGE target (the singleton has no control id) with the same 'Previous value was X. New value is Y%.' text, composed in ABAP because the previous value is backend` &&
            ` state. The original defers the announcement with setTimeout to let the value change land first; the port announces it in the same round-trip that writes the new value, and passes no mode (the`.
    text1 = text1 && ` original passes none either, so it stays Polite). // NOTE: The six button wires (id split -> bound displayValue/percentValue) and the screen-reader announcement are unverified in a running system.` &&
            ` **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_435.mjs). // POST-1.71: INVISIBLE_MESSAGE.announce is a control_global frontend action since UI5 1.78 - newer` &&
            ` than the 1.71 floor but kept for the 1:1 port: the slider's value change is announced to a screen reader, which is what the original demonstrates. Below 1.78 the lazy require returns undefined and` &&
            ` the dispatch hits its "not available" guard, so the announcement is silently skipped - the app needs a UI5 release >= 1.78.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ProgressIndicator`               name = `ProgressIndicatorWithAnnouncement`             class = `z2ui5_cl_smpc_app_435` path = `src/02/01/z2ui5_cl_smpc_app_435.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.13.1`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.ProgressIndicator.displayAnimation is @since 1.73; the second indicator of the sample exists precisely to show it switched off, so the property is kept 1:1 and the port needs a UI5 runtime >=` &&
                 ` 1.73. // INVISIBLE_MESSAGE.announce is a control_global frontend action since UI5 1.78 - newer than the 1.71 floor but kept for the 1:1 port: the slider's value change is announced to a screen` &&
                 ` reader, which is what the original demonstrates. Below 1.78 the lazy require returns undefined and the dispatch hits its "not available" guard, so the announcement is silently skipped - the app needs` &&
                 ` a UI5 release >= 1.78.` ) ).

    text1 = `NOTE: the incremental backend load is reproduced 1:1: the model starts with the first product and each pull-to-refresh (REFRESH) appends the next until the full /ProductCollection is shown (fill_all +` &&
            ` shown counter). // NOTE: handleRefresh's this.byId('pullToRefresh').hide( ) is reproduced since 2026-08-21 through follow_up_action( cs_event-control_by_id, hide ) - an allowed unlisted control` &&
            ` method. It was dropped and undeclared before, which the review sweep caught: PullToRefresh.onclick puts the control into its busy state (spinner + 'Loading') and ONLY hide( ) resets it to 'Refresh',` &&
            ` so the port span after the first pull forever while the original returned to rest. The original's 1s setTimeout around it is not reproduced - the append happens on the round-trip instead. // NOTE:` &&
            ` The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
            ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by`.
    text1 = text1 && ` all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since` &&
            ` today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PullToRefresh`                   name = `PullToRefresh`                                 class = `z2ui5_cl_smpc_app_081` path = `src/01/01/z2ui5_cl_smpc_app_081.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.2`
        notes = text1 ) ).

    text1 = `NOTE: The sample builds its own named device model with isNoTouch/isTouch from Device.support.touch. abap2UI5 serves the raw sap.ui.Device under device>, so SearchField.showRefreshButton binds {=` &&
            ` !${device>/support/touch} } and PullToRefresh.visible binds {= ${device>/support/touch} } - the same two flags, read from the same Device facts. // NOTE: _pushNewProduct hands out one more record of` &&
            ` products.json per refresh and handleRefresh filters the list binding by the SearchField value with a sap.ui.model.Filter. Both are done in the backend on the one table the List binds: the port keeps` &&
            ` a counter, rebuilds the bound table from the full mock up to that counter and applies the Contains filter on Name in ABAP (thin frontend - no binding_call needed, the refresh is a round-trip anyway).` &&
            ` The SearchField value is two-way bound so it survives the view rebuild. // NOTE: handleRefresh wraps its work in a 1-second setTimeout to simulate a slow backend; the port answers the round-trip` &&
            ` immediately. The PullToRefresh spinner is stopped exactly as the original does it, with hide( ) via control_by_id. // IMPROVISED: onInit moves the search Bar below the PullToRefresh on touch devices`.
    text1 = text1 && ` (page.insertAggregation('content', bar, 1)). The port has no client-side reordering and keeps the desktop order (Bar in subHeader) on every device - the only leg of the sample the port does not` &&
            ` reproduce. // NOTE: The refresh wire (grow-by-one plus the search filter), the PullToRefresh hide( ) follow-up and both device> expressions are unverified in a running system. **e2e-verified` &&
            ` 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_438.mjs). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...),` &&
            ` which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the` &&
            ` rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.PullToRefresh`                   name = `RefreshResponsive`                             class = `z2ui5_cl_smpc_app_438` path = `src/01/01/z2ui5_cl_smpc_app_438.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.2`
        notes = text1 ) ).

    text1 = `NOTE: The original binds four separate named JSONModels (CompanyModel / EmployeeModel / GenericModel / GenericModelNoHeader) and swaps the model on the shared QuickView before openBy. abap2UI5 serves` &&
            ` a single default model, so the four data sets are flattened into four ABAP tables (kept in PROTECTED) and the pressed button copies the relevant one into the bound t_pages before the popover is` &&
            ` opened. Named ABAP-fed JSON models are not expressible (CAPABILITIES). // NOTE: The QuickView popover is built per press and shown 1:1 via client->popover_display( xml, by_id ), anchored to the` &&
            ` pressed button (the original oQuickView.openBy(oButton)). The nested pages/groups/elements are a nested ABAP table; relative child aggregations (groups, elements) keep the original binding-info form.` &&
            ` // NOTE: The navigate toast is simplified to a fixed message (the original shows the clicked link's text via navOrigin, a control reference that is not transportable as an event arg). Elements` &&
            ` without an elementType (Generic pages, and the Address/Slogan rows) get the QuickViewGroupElementType default 'text', pages without displayShape (Generic) get the AvatarShape default 'Circle', and`.
    text1 = text1 && ` every element row seeds target '_blank' (the QuickViewGroupElement.target default) - absent JSON properties must not serialize as empty strings, which would override the UI5 defaults. The` &&
            ` EmployeeData icon (a test-resources image) points at the OpenUI5 host. // POST-1.71: sap.m.Avatar (control since 1.73) is kept 1:1 as the page icon via the QuickViewPage avatar aggregation, which` &&
            ` itself is since UI5 1.92 - so the app needs UI5 >= 1.92 to render the avatar. // POST-1.71: sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the four trigger buttons; needs UI5 >= 1.84. // NOTE:` &&
            ` onNavigate names the clicked link in its toast ("Link '<text>' was clicked") and falls back to 'Back button was clicked' when the event carries no navOrigin. **Corrected 2026-08-01**` &&
            ` (faked-event-value audit): the port used to toast the constant 'A QuickView link was clicked' and dropped both the link identity and the back-button branch. The navigate event now transports` &&
            ` ${$parameters>/navOrigin} ? ${$parameters>/navOrigin}.getText() : '' and the ABAP COND rebuilds the original's if/else. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves`.
    text1 = text1 && ` them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own.` &&
            ` Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past` &&
            ` that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what made it wrong, so` &&
            ` this wording names the date the count was taken. // NOTE: The top-level pages binding drops the original's templateShareable: true; the relative groups/elements bindings keep it. Declared 2026-08-23` &&
            ` - app 412 declares the identical drop and cites this port family as its precedent, while the precedent itself carried no such sentence. Harmless here for the reason 412 gives: each display builds a` &&
            ` fresh fragment, so no template outlives its binding. // NOTE: The original's onAfterRendering overrides the rendered aria-haspopup="dialog" with "true" on three of the four buttons`.
    text1 = text1 && ` (genericQuickViewNoHeader is deliberately left out); the port renders ariaHasPopup="Dialog" on all four and has no equivalent. An accessibility attribute only, and the same shape app 099 declares for` &&
            ` its own dropped onAfterRendering. Declared 2026-08-23.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.QuickView`                       name = `QuickView`                                     class = `z2ui5_cl_smpc_app_100` path = `src/02/01/z2ui5_cl_smpc_app_100.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.28.11`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar (control since 1.73) is kept 1:1 as the page icon via the QuickViewPage avatar aggregation, which itself is since UI5 1.92 - so the app needs UI5 >= 1.92 to render the avatar. //` &&
                 ` sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the four trigger buttons; needs UI5 >= 1.84.` ) ).

    text1 = `POST-1.71: sap.m.Button ariaHasPopup is @since 1.84.0, the QuickViewPage avatar aggregation @since 1.92, sap.m.Avatar itself @since 1.73 and Avatar badgeIcon @since 1.77 - all newer than the 1.71` &&
            ` floor. The sample exists to show the avatar configuration, so every one is kept and the port is filed under src/02. // NOTE: handleButtonPress loads the fragment and calls openBy( button ). The port` &&
            ` builds the same fragment in a chain and opens it with popover_display anchored on the button id, so the QuickView is the sample's popover in the same position. // NOTE: formatBadgeIcon is a formatter` &&
            ` that returns sap-icon://edit for the employee page and null for every other page. A formatter has no counterpart here, so the badge icon is resolved per row in model_init - the employee page carries` &&
            ` it, the company page carries the empty string - and the Avatar binds the field. Same values, resolved in the backend. // NOTE: handleAvatarPress toasts a constant text; the toast is composed on the` &&
            ` client so the avatar press needs no round-trip. // NOTE: The company page's tile image is the sample's own test-resources file (sap-logo.png), re-hosted on sdk.openui5.org; onAfterRendering's`.
    text1 = text1 && ` imperative aria-haspopup attribute is the declarative Button.ariaHasPopup the view already sets. // NOTE: The fragment wires QuickView.navigate to .onNavigate, but the sample's controller declares no` &&
            ` onNavigate method - the wire is dead in the original. The port drops it rather than inventing behaviour for it (structural-diff reports QuickView.navigate as attr missing). // NOTE: The QuickView` &&
            ` popover, its page-link navigation and the avatar press are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_531.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.QuickView`                       name = `QuickViewAvatarConfiguration`                  class = `z2ui5_cl_smpc_app_531` path = `src/02/01/z2ui5_cl_smpc_app_531.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.28.11`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Button ariaHasPopup is @since 1.84.0, the QuickViewPage avatar aggregation @since 1.92, sap.m.Avatar itself @since 1.73 and Avatar badgeIcon @since 1.77 - all newer than the 1.71 floor. The` &&
                 ` sample exists to show the avatar configuration, so every one is kept and the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.m.Button ariaHasPopup is @since 1.84.0, the QuickViewPage avatar aggregation @since 1.92 and sap.m.Avatar itself @since 1.73 - all newer than the 1.71 floor. All three come straight` &&
            ` from the sample, so they are kept and the port is filed under src/02. // NOTE: The sample keeps TWO named models: CardModel holds the two QuickView pages, EmployeeModel holds the four employee` &&
            ` records keyed by name. The port keeps the same split - t_pages is bound to the QuickView, t_employees stays in the backend - so the second model is never sent to the client. // NOTE: onNavigate reads` &&
            ` the clicked link (navOrigin) and splices the matching employee record into page 2 of the card model. The port sends the link's text with the navigate event and does the same replacement in ABAP, so` &&
            ` the second page is filled on the round-trip exactly as the original fills it in the controller. A back navigation has no navOrigin, which the ternary in the event argument reproduces as the empty` &&
            ` string. // NOTE: handleQuickViewBtnPress loads the fragment and calls openBy( button ); the port builds the same fragment in a chain and opens it with popover_display anchored on the button id.`.
    text1 = text1 && ` onAfterRendering's imperative aria-haspopup attribute is the declarative Button.ariaHasPopup the view already sets. // NOTE: The QuickView popover and the navOrigin page swap are unverified in a` &&
            ` running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_532.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.QuickView`                       name = `QuickViewNavOrigin`                            class = `z2ui5_cl_smpc_app_532` path = `src/02/01/z2ui5_cl_smpc_app_532.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.28.11`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Button ariaHasPopup is @since 1.84.0, the QuickViewPage avatar aggregation @since 1.92 and sap.m.Avatar itself @since 1.73 - all newer than the 1.71 floor. All three come straight from the` &&
                 ` sample, so they are kept and the port is filed under src/02.` ) ).

    text1 = `NOTE: The QuickViewCard fragment is inlined into the main view instead of a separate core:Fragment include, so the port has no core:Fragment control. The nested pages/groups/elements are a nested ABAP` &&
            ` table (t_pages) bound 1:1; relative child aggregations (groups, elements) keep the original binding-info form. // NOTE: The external Navigate-Back button drives the card 1:1 via follow_up_action(` &&
            ` cs_event-control_by_id, navigateBack ) — navigateBack was whitelisted in the paired abap2UI5 change. afterNavigate forwards the public isTopPage parameter and enables the button while the card is not` &&
            ` on its top page. The navigate toast is simplified to a fixed message (the original distinguishes the clicked link's text vs the back button via navOrigin, REPRODUCED since 2026-08-23. The` &&
            ` justification that stood here - "navOrigin is a control reference that is not transportable as an event arg" - was disproved by the twin port 100, whose own sidecar records the same defect being` &&
            ` corrected on 2026-08-01. The wire now carries ${$parameters>/navOrigin} ? ${$parameters>/navOrigin}.getText() : '' and a COND rebuilds the original's if/else, so the clicked link is named and the`.
    text1 = text1 && ` back-button branch exists again). // NOTE: Elements without an elementType in the source JSON (Address, Slogan) are filled with the QuickViewGroupElementType default 'text', and every element row` &&
            ` seeds target '_blank' (the QuickViewGroupElement.target default) - absent JSON properties must not serialize as empty strings, which would override the UI5 defaults. onAfterRendering's 320px maxWidth` &&
            ` tweak is dropped. // POST-1.71: sap.m.Avatar (control since 1.73) is kept 1:1 as the page icon via the QuickViewPage avatar aggregation, which itself is since UI5 1.92 - so the app needs UI5 >= 1.92` &&
            ` to render the avatar. // NOTE: The top-level pages binding drops the original's templateShareable: true; the relative groups/elements bindings keep it. Declared 2026-08-23 - app 412 declares the` &&
            ` identical drop and cites this port family as its precedent, while the precedent itself carried no such sentence. Harmless here for the reason 412 gives: each display builds a fresh fragment, so no` &&
            ` template outlives its binding.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.QuickViewCard`                   name = `QuickViewCard`                                 class = `z2ui5_cl_smpc_app_099` path = `src/02/01/z2ui5_cl_smpc_app_099.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.28.11`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar (control since 1.73) is kept 1:1 as the page icon via the QuickViewPage avatar aggregation, which itself is since UI5 1.92 - so the app needs UI5 >= 1.92 to render the avatar.` ) ).

    text1 = `POST-1.71: sap.m.Avatar (control since 1.73) is kept 1:1 as the page icon via the QuickViewPage avatar aggregation, which itself is since UI5 1.92 - so the app needs UI5 >= 1.92 to render the avatar.` &&
            ` // NOTE: The view's core:Fragment include (fragmentName sap.m.sample.QuickViewCardScrollBar.QuickViewCardScrollBar) is inlined into the main view, so the port has no core:Fragment reference element;` &&
            ` the fragment's core:FragmentDefinition root has no counterpart either. // NOTE: onButtonBackClick drives the card 1:1 via follow_up_action( control_by_id, navigateBack ), roundtrip-free; the Back` &&
            ` button's enabled state is two-way bound and kept in sync by the afterNavigate isTopPage transport (sent as the string tokens top/sub - the transpiled e2e runtime hands a JSON boolean event arg` &&
            ` through as 'false' where a real system normalizes it to abap_bool, and the token form reads the same on both) (the original seeds enabled='false' and resets it in onBeforeRendering). // NOTE:` &&
            ` onScrollSwitchChange's setShowVerticalScrollBar becomes an added showVerticalScrollBar attribute two-way bound to the same flag as the Switch state; the Switch change wire only resets the Back button`.
    text1 = text1 && ` server-side (the card re-renders to its first page). // NOTE: onAfterRendering's demo-harness DOM tweaks (the touchmove stopPropagation listener for iPhone scrolling and the jQuery maxWidth 320px on` &&
            ` the container) are dropped - they have no view counterpart. // NOTE: Elements the mock leaves without an elementType (Address, Slogan) seed the QuickViewGroupElementType default 'text', target seeds` &&
            ` the UI5 default '_blank', and page 2 seeds iconVisibility=true - a serialized empty string/false would override the control defaults where the original's undefined picks them. // NOTE: The` &&
            ` HEADER_TOGGLE round-trip mutates page 1 in the model and re-renders the card to its first page (the behaviour the original expects after a header switch); unverified in a running system.` &&
            ` **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_421.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.QuickViewCard`                   name = `QuickViewCardScrollBar`                        class = `z2ui5_cl_smpc_app_421` path = `src/02/01/z2ui5_cl_smpc_app_421.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.28.11`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar (control since 1.73) is kept 1:1 as the page icon via the QuickViewPage avatar aggregation, which itself is since UI5 1.92 - so the app needs UI5 >= 1.92 to render the avatar.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.RadioButton`                     name = `RadioButton`                                   class = `z2ui5_cl_smpc_app_069` path = `src/02/01/z2ui5_cl_smpc_app_069.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: RadioButton.wrapping and RadioButton.wrappingType (both since UI5 1.126) are kept 1:1 on the wrapping-demo group; the app needs a UI5 release >= 1.126 to render them.`
        post171 = `RadioButton.wrapping and RadioButton.wrappingType (both since UI5 1.126) are kept 1:1 on the wrapping-demo group; the app needs a UI5 release >= 1.126 to render them.` )
      ( module = `sap.m`              control = `sap.m.RangeSlider`                     name = `RangeSlider`                                   class = `z2ui5_cl_smpc_app_045` path = `src/01/01/z2ui5_cl_smpc_app_045.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.38`
        notes = `NOTE: the sample binds the composite RangeSlider "range" property (an array [low, high] - range="{/RS1}" / range="0,100"). abap2UI5 binds scalar ABAP fields, so each range is expressed as the` &&
                 ` equivalent value / value2 properties the control keeps in sync - identical rendering.` ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84.0) is kept 1:1 on the two trigger buttons (ariaHasPopup='Dialog'); the app needs a UI5 release >= 1.84 to render it. // NOTE:` &&
            ` handleResponsivePopoverPress/handleResponsivePopoverFooterPress lazily Fragment.load a ResponsivePopover fragment and openBy(button). The port builds each fragment (Popover.fragment.xml ->` &&
            ` ResponsivePopover id='myPopover'; PopoverFooter.fragment.xml -> ResponsivePopover id='myFooterPopover') as a core:FragmentDefinition on the button-press round-trip and shows it anchored to the` &&
            ` pressed button via client->popover_display( xml = ..., by_id = $event.oSource.sId ) - the popover XML parameter is named 'xml' (not 'val' like popup_display). A server round-trip where the original` &&
            ` is a client-side Fragment.load, but faithful (lazy either way) and matches apps 229/238. All ResponsivePopover/Button/Image/OverflowToolbar/ToolbarSpacer controls of both fragments are built in the` &&
            ` port, so the fragment union in structural-diff is satisfied. // NOTE: the original oPopover.bindElement('/ProductCollection/0') binds each popover to a single record; the port seeds that record's`.
    text1 = text1 && ` fields (name, productpicurl) at the default-model root and the fragments' relative bindings {NAME}/{PRODUCTPICURL} resolve against the root - same data, structural-diff matches on the last path` &&
            ` segment. No bind_element follow-up needed because the record is the static row 0, not a per-row selection. **Corrected 2026-08-01**: those bindings were written RELATIVE ({FIELD}) and a relative path` &&
            ` on a control with no binding context resolves against nothing — the fields rendered EMPTY in the running app (measured with the e2e harness, the app-207 class). They are now bound ABSOLUTELY through` &&
            ` client->_bind( field ), which is what a root-seeded record needs. // NOTE: handleCloseButton (Action-A/Action-B) and handleCloseFooterButton (Cancel) each do byId(...).close(); the port routes all` &&
            ` three to one CLOSE event that issues follow_up_action( cs_event-popover_close ) - only one popover slot is open at a time, so a single popover_close covers both fragments. The footer's OK button has` &&
            ` no handler in the original and none here. // NOTE: the JSONModel is loaded from the shared sap/ui/demo/mock/products.json; only the single record /ProductCollection/0 is bound (its Name +`.
    text1 = text1 && ` ProductPicUrl), so only those are seeded. The mock's host-relative ProductPicUrl ('test-resources/...') is resolved to an absolute sdk.openui5.org URL. // NOTE: unverified in a running system: that` &&
            ` both ResponsivePopover fragments open anchored to their button (popover_display by_id = $event.oSource.sId), that {NAME}/{PRODUCTPICURL} resolve from the root-seeded record, and that the` &&
            ` action/footer buttons close the popover (popover_close). **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the 'Popover with Custom Footer' press opens the` &&
            ` ResponsivePopover anchored with its OK/Cancel footer; the phone-Dialog variant remains unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_243.mjs).` &&
            ` // POST-1.71: Found on 2026-08-21, when view-gates stopped letting ANY deviation excuse a version finding: a NOTE that merely contained the member's name had been satisfying the gate, and only` &&
            ` POST_171 / DROPPED_171 carry that claim. sap.m.ResponsivePopover.footer is @since 1.129 and is kept 1:1 (the sample's second popover has a footer Toolbar). This is an AGGREGATION, and the gate is`.
    text1 = text1 && ` explicit about the consequence below the floor: UI5 resolves an unknown tag as a control class instead, and the 404 takes the WHOLE view down rather than just that part - so the app needs UI5 >=` &&
            ` 1.129, not merely renders without a footer. The excusing NOTE was about handleResponsivePopoverFooterPress, a HANDLER name that happens to contain the word.`.
    text2 = `Button.ariaHasPopup (since UI5 1.84.0) is kept 1:1 on the two trigger buttons (ariaHasPopup='Dialog'); the app needs a UI5 release >= 1.84 to render it. // Found on 2026-08-21, when view-gates stopped` &&
            ` letting ANY deviation excuse a version finding: a NOTE that merely contained the member's name had been satisfying the gate, and only POST_171 / DROPPED_171 carry that claim.` &&
            ` sap.m.ResponsivePopover.footer is @since 1.129 and is kept 1:1 (the sample's second popover has a footer Toolbar). This is an AGGREGATION, and the gate is explicit about the consequence below the` &&
            ` floor: UI5 resolves an unknown tag as a control class instead, and the 404 takes the WHOLE view down rather than just that part - so the app needs UI5 >= 1.129, not merely renders without a footer.` &&
            ` The excusing NOTE was about handleResponsivePopoverFooterPress, a HANDLER name that happens to contain the word.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ResponsivePopover`               name = `ResponsivePopover`                             class = `z2ui5_cl_smpc_app_243` path = `src/02/01/z2ui5_cl_smpc_app_243.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.15.1`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: the Image src binds {img>/products/pic1} in the original, a JSON image model not available server-side; a static demo image URL is used instead. // NOTE: The sample's asset paths are` &&
            ` host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The` &&
            ` values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had` &&
            ` stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale` &&
            ` absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ScrollContainer`                 name = `ScrollContainer`                               class = `z2ui5_cl_smpc_app_046` path = `src/01/01/z2ui5_cl_smpc_app_046.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed); incl. the phone-emulation device> check. **e2e-verified` &&
                 ` 2026-09-12** (interaction module meta/interactions/z2ui5_cl_smpc_app_046.mjs: the ScrollContainer renders as a vertical scroller and the Image width resolves the device> expression to 100em)`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SearchField`                     name = `DialogSearch`                                  class = `z2ui5_cl_smpc_app_090` path = `src/01/01/z2ui5_cl_smpc_app_090.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the Dialog (loaded from a fragment in the original) is built and shown via popup_display on the button press; its content is static text, so the bindElement /ProductCollection/0 is a no-op and` &&
                 ` dropped.` ) ).

    text1 = `NOTE: onSuggest is a backend round-trip: the SUGGEST event transports ${$parameters>/suggestValue}, the server applies the compound OR filter ([[PRODUCTID Contains v],[NAME Contains v]]) to the` &&
            ` suggestionItems binding via follow_up_action( binding_call, filter ) and re-opens the popover via follow_up_action( control_by_id, suggest ) - the original's two custom Filter test functions` &&
            ` (case-insensitive indexOf on ProductId and Name) map to the Contains operator, which client JSON models match case-insensitively; an empty value clears the filter like the original's empty aFilters.` &&
            ` // NOTE: onSearch's MessageToast branches are composed on the client, roundtrip-free: a '{0}' template filled by the ternary ${$parameters>/suggestionItem} ? 'Search for: ' + getText() : 'Search is` &&
            ` fired!'. // NOTE: Price is kept TYPE string: it is display-only inside the SuggestionItem description text template {path:'PRICE'} {path:'CURRENCYCODE'}, and a packed field with fixed DECIMALS would` &&
            ` add trailing zeros to the mock's variable-decimal values. // NOTE: The per-keystroke suggest round-trip is serialized, and until 2026-09-19 it was lossy under fast typing (events fired while a trip`.
    text1 = text1 && ` was in flight were dropped; it converged when typing paused). The wire carries s_ctrl-check_queue_last now (abap2UI5 #2739, in the pin since #211): the last SUGGEST fired during a flight is kept and` &&
            ` dispatched after the response, so the popover ends on the typed value. The filter + suggest() follow-up pair is unverified in a running system. **e2e-verified 2026-08-21** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_420.mjs). The wire carries s_ctrl-check_no_busy alongside it (abap2UI5 #2759, in the pin since the bump to ad0a2dd2): the round-trip and the busy STATE are` &&
            ` unchanged, only the full-screen overlay stays down. That is the second half a per-keystroke wire needs - check_queue_last stops the keystrokes from being lost, check_no_busy stops the overlay from` &&
            ` flashing over the field while they are typed. It was the visible half of the gap: the FIRST trip raises the indicator after the usual delay, but every keystroke landing on a trip already in flight` &&
            ` raises it with NO delay, so from the second character on the original's round-trip-free typing was covered by an overlay the demo kit sample never shows.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SearchField`                     name = `SearchFieldSuggestions`                        class = `z2ui5_cl_smpc_app_420` path = `src/01/01/z2ui5_cl_smpc_app_420.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SegmentedButton`                 name = `SegmentedButton`                               class = `z2ui5_cl_smpc_app_047` path = `src/01/01/z2ui5_cl_smpc_app_047.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_047.mjs: clicking Two in SB1 round-trips SELECTION_CHANGE through the two-way bound selectedKey - the toast names 'Two' and the bound preview text reads` &&
                 ` 'getSelectedItem(): Two')`
        notes = `NOTE: the original reads the selected item via oEvent.getParameter("item").getText() / getSelectedItem(). Here the items get keys (one/two/three - an addition, SB1 has none in the sample) and` &&
                 ` selectedKey is two-way bound, so the selection arrives with the event and no private event path is needed - the documented 1:1 path for controller-read selection (CAPABILITIES.md), not a workaround.` )
      ( module = `sap.m`              control = `sap.m.SegmentedButton`                 name = `SegmentedButtonContentModes`                   class = `z2ui5_cl_smpc_app_423` path = `src/02/01/z2ui5_cl_smpc_app_423.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.SegmentedButton.contentMode is @since 1.142.0 and both SegmentedButtons of the sample are built around it (ContentFit vs EqualSized) - the property is kept 1:1, so the port needs a` &&
                 ` UI5 runtime >= 1.142; on an older release both buttons render in the pre-1.142 default width behaviour.`
        post171 = `sap.m.SegmentedButton.contentMode is @since 1.142.0 and both SegmentedButtons of the sample are built around it (ContentFit vs EqualSized) - the property is kept 1:1, so the port needs a UI5 runtime` &&
                 ` >= 1.142; on an older release both buttons render in the pre-1.142 default width behaviour.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SegmentedButton`                 name = `SegmentedButtonDialog`                         class = `z2ui5_cl_smpc_app_489` path = `src/01/01/z2ui5_cl_smpc_app_489.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The controller loads Dialog.fragment.xml lazily and opens it; onCloseDialog is a plain dialog.close( ). The port builds the same fragment in its own chain, shows it with popup_display and closes` &&
                 ` it with the client-side follow_up_action popup_close. // NOTE: The dialog with its SegmentedButton and the popup_close on the Close button are unverified in a running system. **e2e-verified` &&
                 ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_489.mjs).` )
      ( module = `sap.m`              control = `sap.m.SegmentedButton`                 name = `SegmentedButtonLI`                             class = `z2ui5_cl_smpc_app_391` path = `src/01/01/z2ui5_cl_smpc_app_391.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.SegmentedButton`                 name = `SegmentedButtonVSD`                            class = `z2ui5_cl_smpc_app_516` path = `src/01/01/z2ui5_cl_smpc_app_516.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: handleOpenDialog loads Dialog.fragment.xml lazily and opens it; the port builds the same ViewSettingsDialog in its own chain and shows it with popup_display. handleConfirm toasts the dialog's` &&
                 ` filterString, which travels as an event arg, so the toast text is the dialog's own. // NOTE: The ViewSettingsDialog and the filterString toast are unverified in a running system. **e2e-verified` &&
                 ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_516.mjs).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Select`                          name = `Select`                                        class = `z2ui5_cl_smpc_app_048` path = `src/01/01/z2ui5_cl_smpc_app_048.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the original controller seeds three byte-identical product arrays (/ProductCollection, /ProductCollection2, /ProductCollection3) and binds one Select to each; the port folds them into the single` &&
                 ` shared table /T_PRODUCTS feeding all three Selects - same rows, same sorter, each Select keeps its own two-way selectedKey, so rendering and behaviour are identical.` ) ).

    text1 = `POST-1.71: two post-1.71 properties are kept for the 1:1 port, and they are what the sample is FOR: sap.m.Select.columnRatio (@since 1.86) and twoColumnSeparator (@since 1.140). The app needs a UI5` &&
            ` release >= 1.140 for both halves of the sample to work; wrapItemsText and showSecondaryValues are older. // NOTE: four of the controller's five handlers are setters on bindable properties, so the` &&
            ` port binds the control that WRITES and the Select that READS to the same field and drops the handler: the Wrap-items Switch shares wrapItemsText with the first Select, the separator Select's` &&
            ` selectedKey IS select2's twoColumnSeparator, and the Editable Switch's state IS select2's editable. None of the three needs a round trip. // NOTE: setCorrectData is the one that cannot be folded: it` &&
            ` composes three strings from BOTH slider values - columnRatio '<a>:<b>', the same text for text1 and the rounded percentages for text2 - and an expression binding that computes Math.round over two` &&
            ` fields twice would run past the 255-character abapGit line limit. The two Sliders are therefore bound two-way and their change (which is what the sample wires, not liveChange) recomputes the three`.
    text1 = text1 && ` fields in ABAP, rounding the first percentage and taking the second as 100 minus it, exactly as the original does. // NOTE: onInit builds the two ten-item lists in JavaScript with 'First column text` &&
            ` '.repeat(5) + ' ' + i. The port seeds the same twenty rows literally - the repeated text is what makes the column ratio visible, so it is data, not code. // NOTE: the separator Select carries no` &&
            ` selectedKey in the sample, so UI5 shows its first item (Dash) and getSelectedKey() returns 'Dash' before any change. The port seeds that value, which is also what keeps an empty string out of` &&
            ` select2's twoColumnSeparator enum.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Select`                          name = `Select2Columns`                                class = `z2ui5_cl_smpc_app_608` path = `src/02/01/z2ui5_cl_smpc_app_608.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `two post-1.71 properties are kept for the 1:1 port, and they are what the sample is FOR: sap.m.Select.columnRatio (@since 1.86) and twoColumnSeparator (@since 1.140). The app needs a UI5 release >=` &&
                 ` 1.140 for both halves of the sample to work; wrapItemsText and showSecondaryValues are older.` ) ).

    text1 = `POST-1.71: sap.m.Select.liveChange is @since 1.100 and its change event's previousSelectedItem parameter is @since 1.95 - both are what this sample demonstrates, so they are kept 1:1 and the port` &&
            ` needs a UI5 runtime >= 1.100. // NOTE: onChange and onLiveChange only toast the ids of the involved items. Both are composed on the client from ${$parameters>/selectedItem}.getId() and` &&
            ` ${$parameters>/previousSelectedItem}.getId(), so neither needs a round-trip and the texts stay the original's. // NOTE: Both client-composed toasts are unverified in a running system. **e2e-verified` &&
            ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_511.mjs). // NOTE: The client-composed toast template is written with BARE {0}/{1} placeholders. It had carried \{0\}, which` &&
            ` is wrong in a backtick literal: only |...| templates treat \{ as an escape, so the backslashes were real characters and formatTemplate's /\{(\d+)...\}/ never matched - the placeholders rendered` &&
            ` verbatim. The escape is not needed at all on this path: the value lands in an EVENT attribute, which XMLTemplateProcessor routes through EventHandlerResolver without a BindingParser pass. An ordinary`.
    text1 = text1 && ` attribute is the opposite case and still needs \{ (see app 523's app:template).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Select`                          name = `SelectChangeEvents`                            class = `z2ui5_cl_smpc_app_511` path = `src/02/01/z2ui5_cl_smpc_app_511.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Select.liveChange is @since 1.100 and its change event's previousSelectedItem parameter is @since 1.95 - both are what this sample demonstrates, so they are kept 1:1 and the port needs a UI5` &&
                 ` runtime >= 1.100.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Select`                          name = `SelectValueState`                              class = `z2ui5_cl_smpc_app_373` path = `src/01/01/z2ui5_cl_smpc_app_373.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the original controller assigns one and the same aProducts array to four model keys (/ErrorProductCollection, /WarningProductCollection, /SuccessProductCollection, /InformationProductCollection)` &&
                 ` and binds one Select to each; the port folds them into the single shared table /T_PRODUCTS feeding all four Selects - same 16 rows, same sorter on NAME, each Select keeps its own two-way selectedKey,` &&
                 ` so rendering and behaviour are identical (app 048 precedent).` )
      ( module = `sap.m`              control = `sap.m.Select`                          name = `SelectWithIcons`                               class = `z2ui5_cl_smpc_app_205` path = `src/01/01/z2ui5_cl_smpc_app_205.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.Select`                          name = `SelectWithWrappedItemText`                     class = `z2ui5_cl_smpc_app_374` path = `src/01/01/z2ui5_cl_smpc_app_374.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: The original configures one shared SelectDialog imperatively per button (oButton.data() CustomData ->` &&
            ` setMultiSelect/setGrowing/setGrowingThreshold/setRememberSelections/setShowClearButton/setConfirmButtonText/setDraggable/setResizable/toggleStyleClass). abap2UI5 binds those SelectDialog properties` &&
            ` two-way and each button's handler sets them before opening (responsivePadding toggles the style class via control_by_id addStyleClass/removeStyleClass). The core:CustomData is kept on the buttons for` &&
            ` fidelity; the dialog is declared once in mvc:dependents and opened via follow_up_action( cs_event-control_by_id, open ). // NOTE: Search filters the dialog's items binding client-side via` &&
            ` follow_up_action( cs_event-binding_call, filter NAME Contains ${$parameters>/value} ). The valueHelpRequest opens a second SelectDialog (also in dependents) after preselecting the row matching the` &&
            ` input value. The value-help close is reproduced 1:1 since 2026-08-21: the event carries ``${$parameters>/selectedItem} ? ${$parameters>/selectedItem}.getTitle() : ''`` and the backend writes it into`.
    text1 = text1 && ` the two-way bound productInput, so picking a product lands it in the field and closing without a selection clears it - the original's setValue / resetProperty pair. The preselection now compares` &&
            ` against that same bound value rather than the seed literal, which is what _configValueHelpDialog does. Both were wrong before: the sidecar claimed selectedItem was 'a control reference not` &&
            ` transportable as an event arg', which CAPABILITIES contradicts - an arg is a FULL UI5 expression and may call methods on the event's controls (proven 2026-07-31, and app 233 already transported` &&
            ` selectedItem.getDescription()). The CONFIRM toast is reproduced 1:1 since 2026-08-23. It was simplified to a constant on the reasoning that selectedContexts is an ARRAY the original iterates and the` &&
            ` expression grammar has no loop - the loop half is right and the conclusion was not: the frontend marshals a control-valued event parameter into its public properties (Lib.normalizeEventArgs), so the` &&
            ` ARRAY travels whole and the app never needs a loop in the expression. The wire carries ${$parameters>/selectedItems} - the selected StandardListItems, whose title is the bound Name - instead of`.
    text1 = text1 && ` selectedContexts, which is NOT a control and would be handed to JSON.stringify untouched, where a Context's model/binding graph is circular and would take the whole round-trip body down. on_event` &&
            ` composes 'You have chosen A, B, C' and, for the empty case that cancel also produces, 'No new item was selected.' - both texts exactly as onDialogClose builds them. Its last line came with it and had` &&
            ` been missing undeclared: the handler ends with oEvent.getSource().getBinding('items').filter([]), so a search typed into the dialog is cleared when it closes and the next open starts from the full` &&
            ` list. The port issues a binding_call filter with no values, which is that clear (source-verified in the framework's ControlCall.js: the positional filter form clears when value1 and value2 are both` &&
            ` empty - 'the demo kit search pattern: an emptied search field'). // NOTE: The StandardListItem icon binds ProductPicUrl, which is derived in ABAP from the product id (the mock's` &&
            ` test-resources/<id>.jpg) built from a shared base pointing at the OpenUI5 host, like app 006's image flattening. The full 123-row /ProductCollection is inlined. // NOTE: The per-button dialog`.
    text1 = text1 && ` configuration (multi/growing/remember/clear/confirm text/draggable/resizable), the client-side search filter and the value-help selection need an in-system check; machine gates only verify the views` &&
            ` are valid. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the 'Show Select Dialog' popup opens with the 'Select Product' title; the per-button variants, search` &&
            ` and copy-back remain unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_103.mjs). // POST-1.71: sap.m.SelectDialog.searchPlaceholder (since 1.110)` &&
            ` is kept 1:1 on the value-help dialog; needs UI5 >= 1.110. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no` &&
            ` document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23:` &&
            ` the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them`.
    text1 = text1 && ` declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken. // NOTE:` &&
            ` The payload is read by hand - the frontend marshals each control into an object of its ID plus all its public properties, and this port walks that object for the fields it models. abap2UI5 releases` &&
            ` no JSON reader and the vendored ajson copy is framework-internal (the linter's non-released-api rule reports it, correctly), so a targeted reader is the intended shape here; abap2UI5/samples` &&
            ` Z2UI5_CL_SMP_APP_197 and _327 are the same pattern. It reads what the FRAMEWORK wrote, which is flat, and does not resolve escapes - a payload composed from free user input would need that.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SelectDialog`                    name = `SelectDialog`                                  class = `z2ui5_cl_smpc_app_103` path = `src/02/01/z2ui5_cl_smpc_app_103.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.SelectDialog.searchPlaceholder (since 1.110) is kept 1:1 on the value-help dialog; needs UI5 >= 1.110.` ) ).

    text1 = `POST-1.71: sap.m.SelectDialog.initialFocus (since 1.117.0, declared on SelectDialogBase) is kept 1:1; needs UI5 >= 1.117. // POST-1.71: The SelectDialog updateStarted event (since 1.93, declared on` &&
            ` SelectDialogBase) is kept 1:1 as the lazy-loading wire; needs UI5 >= 1.93. // POST-1.71: sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the trigger button; needs UI5 >= 1.84. // NOTE: The` &&
            ` controller's lazy Fragment.load of Dialog.fragment.xml is replaced by declaring the SelectDialog in the Button's mvc:dependents and opening it roundtrip-free via follow_up_action( control_by_id, open` &&
            ` ); the fragment's core:FragmentDefinition root has no counterpart. // NOTE: onUpdateStarted's Growing append is a backend round-trip: the reason parameter is transported, and the server reproduces` &&
            ` the original loop 1:1 - starting at length-1 and appending 30 rows capped at 1001, including the original's off-by-one re-append of the last two rows. // NOTE: onSearch's Contains filter on the items` &&
            ` binding runs client-side via the view-wired follow_up_action( binding_call, filter ) with ${$parameters>/value} - the model stays untouched. // NOTE: The growing lazy-load round-trip (updateStarted`.
    text1 = text1 && ` firing while the dialog list grows, then view_model_update while the popup is open) is unverified in a running system. **e2e-verified 2026-08-21** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_422.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SelectDialog`                    name = `SelectDialogLazyLoading`                       class = `z2ui5_cl_smpc_app_422` path = `src/02/01/z2ui5_cl_smpc_app_422.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.SelectDialog.initialFocus (since 1.117.0, declared on SelectDialogBase) is kept 1:1; needs UI5 >= 1.117. // The SelectDialog updateStarted event (since 1.93, declared on SelectDialogBase) is` &&
                 ` kept 1:1 as the lazy-loading wire; needs UI5 >= 1.93. // sap.m.Button.ariaHasPopup (since 1.84) is kept 1:1 on the trigger button; needs UI5 >= 1.84.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SelectList`                      name = `SelectList`                                    class = `z2ui5_cl_smpc_app_075` path = `src/01/01/z2ui5_cl_smpc_app_075.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26.0` )
      ( module = `sap.m`              control = `sap.m.SelectList`                      name = `SelectListWithIcons`                           class = `z2ui5_cl_smpc_app_211` path = `src/01/01/z2ui5_cl_smpc_app_211.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26.0` ) ).

    text1 = `NOTE: onSemanticButtonPress toasts each action's class name, reproduced by passing the name as a t_arg literal. The SortSelect change toasts what the original composes: onSemanticSelectChange runs the` &&
            ` SAME derivation as onSemanticButtonPress - getMetadata( ).getName( ) minus the LIBRARY, which for a sap.m.semantic.* control is 'sap.m' - and appends the selected item's text, so it prints 'Selected:` &&
            ` semantic.SortSelect by <text>'. Corrected 2026-08-23 with the button literals: the port had toasted the bare selectedKey and lost the class-name half (the value half was always right, since the item` &&
            ` key equals its text here); PagingButton positionChange transports ${$parameters>/newPosition}; the custom footer / share buttons transport $event.oSource.sId. // NOTE: MultiSelectAction and` &&
            ` MessagesIndicator reproduced 1:1 since 2026-07-30. onMultiSelectPress: the press transports ${$source>/pressed} and the backend toasts 'MultiSelect Pressed'/'MultiSelect Unpressed' from the toggle` &&
            ` state exactly like getPressed(). onMessagesButtonPress: the controller-built MessagePopover over the message model is declared as a dependent of the MessagesIndicator (added controls vs the original`.
    text1 = text1 && ` view.xml: MessagePopover + MessageItem with the original's {message>description}/{message>type}/{message>message} template) and toggled roundtrip-free via follow_up_action control_by_id toggleBy` &&
            ` ($event.oSource.sId) - the _messagePopover.toggle(oMessagesButton) equivalent. onInit's MessageManager.addMessages seed ('Something wrong happened', Error) rides on an added z2ui5.cc.MessageManager` &&
            ` bridge control (in an added semantic content aggregation, declared) with its one-row items table - the app-065 idiom. // NOTE: The SortSelect items are bound to a 2-row filter-type table reproducing` &&
            ` /ProductCollectionStats/Filters (the two ``type`` values Category and SupplierName; the per-type ``values`` sub-arrays are unused by the Select). The binding keeps the original sorter { path: 'Name'` &&
            ` } 1:1 — a no-op there too, since the Filters entries carry ``type``, not ``Name``. // NOTE: The semantic-action toast strings were corrected 2026-08-23, together with app 105's. The original derives` &&
            ` the text as getMetadata().getName() minus the LIBRARY name, and the library of a sap.m.semantic.* control is 'sap.m' - so it prints 'Pressed: semantic.AddAction'. These ports had passed the bare`.
    text1 = text1 && ` action name, stripping the namespace as well, so every one of them differed from the original.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.semantic.SemanticPage`           name = `SemanticPage`                                  class = `z2ui5_cl_smpc_app_107` path = `src/01/01/z2ui5_cl_smpc_app_107.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30.0`
        notes = text1 ) ).

    text1 = `NOTE: onSemanticButtonPress toasts each action's class name, reproduced by passing the name as a t_arg literal. The SortSelect change toasts what the original composes: onSemanticSelectChange runs the` &&
            ` SAME derivation as onSemanticButtonPress - getMetadata( ).getName( ) minus the LIBRARY, which for a sap.m.semantic.* control is 'sap.m' - and appends the selected item's text, so it prints 'Selected:` &&
            ` semantic.SortSelect by <text>'. Corrected 2026-08-23 with the button literals: the port had toasted the bare selectedKey and lost the class-name half (the value half was always right, since the item` &&
            ` key equals its text here); PagingButton positionChange transports ${$parameters>/newPosition}; the custom footer / share buttons transport $event.oSource.sId. // NOTE: MultiSelectAction and` &&
            ` MessagesIndicator reproduced 1:1 since 2026-07-30. onMultiSelectPress: the press transports ${$source>/pressed} and the backend toasts 'MultiSelect Pressed'/'MultiSelect Unpressed' from the toggle` &&
            ` state exactly like getPressed(). onMessagesButtonPress: the controller-built MessagePopover over the message model is declared as a dependent of the MessagesIndicator (added controls vs the original`.
    text1 = text1 && ` view.xml: MessagePopover + MessageItem with the original's {message>description}/{message>type}/{message>message} template) and toggled roundtrip-free via follow_up_action control_by_id toggleBy` &&
            ` ($event.oSource.sId) - the _messagePopover.toggle(oMessagesButton) equivalent. onInit's MessageManager.addMessages seed ('Something wrong happened', Error) rides on an added z2ui5.cc.MessageManager` &&
            ` bridge control (in an added semantic content aggregation, declared) with its one-row items table - the app-065 idiom. // NOTE: The SortSelect items are bound to a 2-row filter-type table reproducing` &&
            ` /ProductCollectionStats/Filters (the two ``type`` values Category and SupplierName; the per-type ``values`` sub-arrays are unused by the Select). The binding keeps the original sorter { path: 'Name'` &&
            ` } 1:1 — a no-op there too, since the Filters entries carry ``type``, not ``Name``. // NOTE: Same as SemanticPage but the MasterPage and DetailPage carry floatingFooter='true' and the MasterPage drops`.
    text1 = text1 && ` the PageAccessibleLandmarkInfo (matching the SemanticPageFloatingFooter variant). // NOTE: The semantic-action toast strings were corrected 2026-08-23, together with app 105's. The original derives` &&
            ` the text as getMetadata().getName() minus the LIBRARY name, and the library of a sap.m.semantic.* control is 'sap.m' - so it prints 'Pressed: semantic.AddAction'. These ports had passed the bare` &&
            ` action name, stripping the namespace as well, so every one of them differed from the original.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.semantic.SemanticPage`           name = `SemanticPageFloatingFooter`                    class = `z2ui5_cl_smpc_app_106` path = `src/01/01/z2ui5_cl_smpc_app_106.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30.0`
        notes = text1 ) ).

    text1 = `NOTE: onSemanticButtonPress toasts the pressed control's class name (getMetadata().getName() minus the LIBRARY, which is 'sap.m' and not 'sap.m.semantic' - so the original prints 'Pressed:` &&
            ` semantic.AddAction'. Corrected 2026-08-23: the port had passed the bare 'AddAction', i.e. minus the library AND the namespace, so all ten toasts differed from the original. The same literals were` &&
            ` wrong in the sibling ports 106 and 107 and are fixed with them); reproduced by passing each semantic action's name as a t_arg literal ('AddAction', 'EditAction', ...) and toasting 'Pressed: <name>'.` &&
            ` The custom footer buttons toast the pressed control id via $event.oSource.sId (original onPress). (Removed 2026-08-23: this sentence claimed a simplified MessagesIndicator toast and a dropped` &&
            ` MessagePopover, and the port has neither - the indicator opens the popover via control_by_id toggleBy and the MessagePopover is built in the view, which the next deviation describes correctly. It was` &&
            ` left standing by that rework.) // NOTE: onMessagesButtonPress reproduced 1:1 since 2026-08-21 - the port toasted a bare 'Messages' before, which the review sweep found: the whole message-handling leg`.
    text1 = text1 && ` of the sample was missing, undeclared, while sibling ports 106/107, whose onInit seed and onMessagesButtonPress legs are the same code (corrected 2026-08-23 - the three controllers are not` &&
            ` byte-identical files: FullScreen has onNavButtonPress where the siblings have onSemanticSelectChange/onPositionChange/onMultiSelectPress) already carried it. The controller-built MessagePopover over` &&
            ` the message model is now declared as a dependent of the MessagesIndicator (added controls vs the original view.xml: MessagePopover + MessageItem with the original's {message>description} /` &&
            ` {message>type} / {message>message} template) and toggled roundtrip-free via follow_up_action control_by_id toggleBy ($event.oSource.sId) - the _messagePopover.toggle(oMessagesButton) equivalent.` &&
            ` onInit's MessageManager.addMessages seed ('Something wrong happened', Error) rides on an added z2ui5.cc.MessageManager bridge control in an added semantic content aggregation (declared) with its` &&
            ` one-row items table - the app-065 idiom, identical to apps 106/107. // NOTE: The MessagePopover shows a message where the original's shows none, and the port is the more correct side. The original`.
    text1 = text1 && ` never sets a 'message' model, and MessagePopover only falls back to binding one when it has neither an items binding nor items - which is not this sample - so upstream the popover opens empty.` &&
            ` abap2UI5 attaches the message model to every view slot unconditionally and the cc.MessageManager bridge pushes the seeded row in, so the port displays it. Framework-level, not a porting choice;` &&
            ` declared 2026-08-23 because the neighbouring deviation says "reproduced 1:1" and the visible outcome differs.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.semantic.SemanticPage`           name = `SemanticPageFullScreen`                        class = `z2ui5_cl_smpc_app_105` path = `src/01/01/z2ui5_cl_smpc_app_105.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30.0`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.Button ariaHasPopup is @since 1.84.0 and sap.ui.unified.CalendarAppointment ariaHasPopup @since 1.150.0 - both newer than the 1.71 floor. Both come straight from the sample, so they` &&
            ` are kept and the port is filed under src/02. // NOTE: The calendar date properties (the SinglePlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and` &&
            ` demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE:` &&
            ` The original keeps the toolbar switches in a settings> model and the all-day flag in an allDay> model. abap2UI5 keeps one default model, so all five are fields here; the three ToggleButtons and the` &&
            ` calendar's three enable properties share them exactly as the named model made them share. // NOTE: handleAppointmentSelect opens the Details popover on the picked appointment and closes it when the` &&
            ` appointment is deselected. The appointment's binding path and its selected state travel with the event, and ABAP fills the popover's fields from the row - including _typeFormatter, which maps the`.
    text1 = text1 && ` type key to its legend text over the same supported-items table, and the all-day flag the original computes by comparing the appointment's bounds with their midnight-trimmed copies. // NOTE:` &&
            ` handleEditButton and _createInitialDialogValues open the SAME Modify dialog for an edit and for a create; the port keeps that, with the dialog title and the selected row index deciding which.` &&
            ` handleDialogOkButton writes the fields back into the picked row or pushes a new one, and handlePopoverDeleteButton removes the row behind the popover - all three 1:1. The create seed is the sample's` &&
            ` own default 9-10 hours of the picked day. // NOTE: handleAppointmentDrop copies or moves, handleAppointmentResize re-dates, handleAppointmentCreateDnD pushes a 'New Appointment' - each with the toast` &&
            ` the original shows. The interval's LOCAL date parts and the appointment's binding path travel with every one of them. // IMPROVISED: handleMoreLinkPress does two things: it switches the calendar to` &&
            ` the Day view (setSelectedView) and it moves the start date to the clicked day. The port does the second; the first is lost, because SinglePlanningCalendar.selectedView is an ASSOCIATION - it neither`.
    text1 = text1 && ` binds (the XML parser reads the value as a control id) nor has a whitelisted setter. // NOTE: The three fragments (Details popover, Modify dialog, Legend popover) are built in chains and shown with` &&
            ` popover_display / popup_display. handleViewChange's toast is composed on the client; handleStartDateChange names the new date and keeps its round-trip. The three date-validation helpers` &&
            ` (handleDateTimePickerChange, handleDatePickerChange, updateButtonEnabledState) have THREE error branches, not the single "empty or malformed picker value" this note used to claim: an empty picker` &&
            ` ("Please pick a date"), a value the picker could not parse ("Invalid date", read off the change event's valid parameter), and - the one the pickers' own required flag can NOT substitute for - an end` &&
            ` that is not after the start, which paints BOTH pickers ValueState.Error with "Start date should be before End date" and disables the OK button. The port keeps none of the three and always accepts the` &&
            ` dialog, so it saves an appointment that ends before it starts. Its sibling app 609 implements the third branch in ABAP (date_check, bound valueState / valueStateText / enabled) because its original`.
    text1 = text1 && ` has only that one and computes it from the two dates alone; doing the same here additionally needs the change event's valid parameter carried into the wire and would paint both pickers where the` &&
            ` original paints one. // NOTE: handleCheckBoxSelect is wired (CheckBox select -> ALLDAY). It does more than swap which picker pair is visible: ticking All-day rewrites both times to midnight` &&
            ` (_setHoursToZero) and unticking restores the sample's own default hours 9 and 10 (_getDefaultAppointmentStartHour / _getDefaultAppointmentEndHour), then copies both into the newly visible pair.` &&
            ` Without it the port stored a 09:00-10:00 appointment for an all-day one and the calendar never drew it in the all-day row - the all-day flag is DERIVED on read (start and end both at midnight) and` &&
            ` nothing ever wrote it. sap.m.CheckBox writes selected into the model before it fires select, so the ABAP branch reads the new flag straight off allday. The rewrite needs the four pickers' valueFormat` &&
            ` pinned to yyyy-MM-dd'T'HH:mm:ss, which the original never needs because it sets dateValue imperatively and binds no value at all: measured headless (2026-08-26), an unpinned DateTimePicker READS the`.
    text1 = text1 && ` model's ISO string but writes a LOCALE string back ("Jul 12, 2018, 2:30:00 PM"), and an unpinned DatePicker cannot read it at all - it showed the raw "2018-07-09T09:00:00" with no date value and` &&
            ` wrote back "7/12/18". With the format pinned both pairs read and write the same 19-character ISO string. The midnight test is a CP pattern rather than an offset substring( ), which would dump on the` &&
            ` empty value a cleared picker sends. // NOTE: The details popover, the create/edit dialog, the delete, the legend, the three drag actions and the more-link are unverified in a running system.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_549.mjs). // NOTE: The three feature flags seed abap_true, as the original's setData does - drag-and-drop,` &&
            ` resize and drag-create are the behaviours this sample exists to show, and the three ToggleButtons start pressed. They had been seeded false, and the e2e module asserted that wrong value, so the` &&
            ` module had to be corrected with the port. // NOTE: The details popover is opened on the POPOVER slot, so it is closed with popover_destroy( ) - popup_destroy( ) tears down the separate POPUP slot and`.
    text1 = text1 && ` left the popover on screen. The EDIT branch closes it before opening the dialog, which the original does explicitly ("The sap.m.Popover has to be closed before the sap.m.Dialog gets opened").`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendar`                        class = `z2ui5_cl_smpc_app_549` path = `src/02/01/z2ui5_cl_smpc_app_549.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Button ariaHasPopup is @since 1.84.0 and sap.ui.unified.CalendarAppointment ariaHasPopup @since 1.150.0 - both newer than the 1.71 floor. Both come straight from the sample, so they are kept and` &&
                 ` the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.ui.unified.CalendarAppointment.ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor but written by the sample on every appointment, so it is kept and the port is filed under` &&
            ` src/02. App 549 carries the same property for the same reason. // NOTE: the calendar date properties (the SinglePlanningCalendar startDate and every CalendarAppointment startDate and endDate) are` &&
            ` typed "object" and demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject converts them at the point of use, which is why those binding values read differently from the` &&
            ` sample's plain {startDate} / {endDate} - same field, one formatter (app 549/553 idiom). // NOTE: this sample is app 549's with the toolbar taken off: no sticky-mode Select, no drag-and-drop / resize` &&
            ` / create toggles and no legend, no viewChange or moreLinkPress wire and no Month view. What is left is the create-and-edit app - the Create action, the details popover and the modify dialog - which` &&
            ` is what its name says. The two ports therefore share their whole shape; only this one's appointment list, its Details fragment ids and its type Select differ. // NOTE: the Details fragment differs`.
    text1 = text1 && ` from app 549's in four places, all carried: the labelFor targets and the ids read moreInfoText / startDateText / endDateText / allDayCheckBox, the all-day CheckBox is editable="false" rather than` &&
            ` enabled="false", and the type Text binds {type} directly instead of running app 549's _typeFormatter - there is no legend here to name the types, so the raw key is what shows. // NOTE: the Modify` &&
            ` dialog's type Select binds /types, an array the controller builds by walking the CalendarDayType enum, and shows the KEY as the text ({type} twice). The port seeds the same twenty-one members in the` &&
            ` enum's own order. Its two Inputs also carry no maxLength here, where app 549's sample sets 255, and the port writes none either - it had carried 255, which contradicted this sentence and which` &&
            ` structural-diff cannot see (it only reports attributes the ORIGINAL has and the port lacks, never one the port adds). // NOTE: handleCheckBoxSelect is wired (CheckBox select -> ALLDAY). It does more` &&
            ` than swap which picker pair is visible: ticking All-day rewrites both times to midnight (_setHoursToZero) and unticking restores the sample's own default hours 9 and 10`.
    text1 = text1 && ` (_getDefaultAppointmentStartHour / _getDefaultAppointmentEndHour), then copies both into the newly visible pair. Without it the port stored a 09:00-10:00 appointment for an all-day one and the` &&
            ` calendar never drew it in the all-day row - the all-day flag is DERIVED on read (start and end both at midnight) and nothing ever wrote it. sap.m.CheckBox writes selected into the model before it` &&
            ` fires select, so the ABAP branch reads the new flag straight off allday. The rewrite needs the four pickers' valueFormat pinned to yyyy-MM-dd'T'HH:mm:ss, which the original never needs because it` &&
            ` sets dateValue imperatively and binds no value at all: measured headless (2026-08-26), an unpinned DateTimePicker READS the model's ISO string but writes a LOCALE string back ("Jul 12, 2018, 2:30:00` &&
            ` PM"), and an unpinned DatePicker cannot read it at all - it showed the raw "2018-07-09T09:00:00" with no date value and wrote back "7/12/18". With the format pinned both pairs read and write the same` &&
            ` 19-character ISO string. The midnight test is a CP pattern rather than an offset substring( ), which would dump on the empty value a cleared picker sends. // NOTE: The modify dialog's date validation`.
    text1 = text1 && ` is KEPT, computed in ABAP and bound - it had been dropped entirely and undeclared. Upstream handleDateTimePickerChange and handleDatePickerChange call _setDateValueState and updateButtonEnabledState:` &&
            ` an end that is not after the start paints BOTH pickers ValueState.Error with "Start date should be before End date", and the OK button is disabled for that and for an empty picker;` &&
            ` handleDialogOkButton refuses to write while either picker is in error. The port wires change on all four pickers to a DATE_CHECK event, computes the state in date_check( ) and binds valueState /` &&
            ` valueStateText on the pickers and enabled on the OK button, with the same guard in DIALOG_OK. The two comparisons of the original are kept apart: the DateTimePicker pair rejects an end that is not` &&
            ` strictly after the start (<=), the DatePicker pair only an end BEFORE the start (<), so an all-day appointment may start and end on the same day. Both values are 19-character ISO strings because of` &&
            ` the pinned valueFormat, so ordering them as strings orders them by time. Sibling app 549 does NOT implement this: its original has three error branches, one of which reads the change event's valid`.
    text1 = text1 && ` parameter, and its sidecar declares the drop. // NOTE: handleAppointmentSelect reads the clicked appointment's binding context; the wire carries its path plus its selected flag, and an empty path` &&
            ` means the user clicked empty space, which is what opens the create dialog. handleHeaderDateSelect and the Create button share that dialog. handleStartDateChange toasts the new start date, which the` &&
            ` wire carries as the Date's own toString(). // IMPROVISED: the details popover is anchored on the appointment's DOM node upstream (oEvent.getParameter('appointment')); a thin frontend has no such` &&
            ` node, so it is anchored on the calendar by id. Everything the popover shows - title, additional info, the two formatted dates, the all-day flag and the type - is composed by the backend from the row` &&
            ` the path names. // NOTE: the 35 appointments and the 2018-07-09 start date are seeded verbatim from onInit. Several carry an out-of-range minute (hour 7 minute 9, hour 9 minute 9, hour 13 minute 9)` &&
            ` that JavaScript normalizes; the port stores the same values as ISO strings. // NOTE: not yet verified in a running system: that Create opens the modify dialog, that an appointment click opens the`.
    text1 = text1 && ` details popover with its type, and that saving writes the row back into the calendar. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_609.mjs). // NOTE: The` &&
            ` details popover is opened on the POPOVER slot, so it is closed with popover_destroy( ) and addressed with cs_view-popover. popup_destroy( )/cs_view-popup target a different slot that this app never` &&
            ` opens, so the close and the in-popover navigation were silent no-ops. // NOTE: The row created at runtime seeds the enum-typed field explicitly. An ABAP field is never absent, so an unset one reaches` &&
            ` the client as "" - not a member of the enum - and validateProperty throws, taking the binding update and the view down. The original pushes a JS object without the key at all, which falls back to the` &&
            ` property default.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarCreateApp`               class = `z2ui5_cl_smpc_app_609` path = `src/02/01/z2ui5_cl_smpc_app_609.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.CalendarAppointment.ariaHasPopup is @since 1.150.0 - newer than the 1.71 floor but written by the sample on every appointment, so it is kept and the port is filed under src/02. App 549` &&
                 ` carries the same property for the same reason.` ) ).

    text1 = `NOTE: The object-typed date properties (SinglePlanningCalendar.startDate, CalendarAppointment.startDate/endDate) are fed from ISO strings and converted with Formatter.DateCreateObject (core:require).` &&
            ` The original UI5Date.getInstance values are normalized to ISO 1:1 (0-based months). The first appointment used UI5Date.getInstance() (the current time); it is pinned to the calendar's start date` &&
            ` (2018-07-09) so the port is deterministic. // NOTE: The four calendar events fire backend round-trips that toast, as in the original, and three of them carry their value: weekNumberPress transports` &&
            ` ${$parameters>/weekNumber}, startDateChange ${$parameters>/date}, and selectedDatesChange the WHOLE ${$parameters>/selectedDates} array - the toast texts append them exactly as the original does` &&
            ` ('...\n\nweek number is <n>' / '...\n\nNew start date is <date>' / '...\n\nNew selected dates: \n<i>: <day>' per range). viewChange stays name-only because the original's toast carries no value` &&
            ` either. **selectedDatesChange was name-only until 2026-08-28, and the reason moved three times.** Its parameter is an ARRAY OF DateRange CONTROLS which the original iterates and formats`.
    text1 = text1 && ` (oRange.getStartDate().toDateString() per entry), and the client expression grammar has NO LOOP - measured 2026-08-05 with scripts/probes/event-arg-expression-probe.mjs, which showed that indexed` &&
            ` access and chained calls DO resolve (so ``[0].getStartDate()`` would work for a single range) but a per-entry map does not exist. The frontend then learned to marshal a control-valued event parameter` &&
            ` (Lib.normalizeEventArgs projects each control to its public properties), so the array travels whole and no loop in the expression is needed - but that route still could not serve this port:` &&
            ` scripts/probes/control-valued-event-arg-probe.mjs (candidate dateRange-array, real OpenUI5) showed DateRange.startDate is a Date-typed property, which JSON.stringify wrote through toISOString(), so` &&
            ` LOCAL midnight of 2018-07-09 arrived as "2018-07-08T22:00:00.000Z" in a browser at Europe/Berlin - a day early, where the original prints a LOCAL rendering (west of Greenwich and in UTC it was` &&
            ` correct, so a CI run in UTC reported a false all-clear). abap2UI5 closed that on 2026-08-24 (backlog item event-arg-date-utc-shift): projectValue serializes a Date as its LOCAL parts, an ISO local`.
    text1 = text1 && ` timestamp with no Z, because these properties carry a calendar day and not an instant. The fix reached the reproducible builds when A2UI5_PIN moved on 2026-08-28, and the array is transported since.` &&
            ` App 060's parent-chain breadcrumb is a different boundary and stays open on its own reasoning. What still differs is the RENDERING of the one line: the port prints the yyyy-MM-dd day taken from the` &&
            ` marshalled ISO local timestamp, the original prints getStartDate().toDateString() ('Mon Jul 09 2018') - a JS locale rendering, not a value, and rebuilding it in ABAP would put a formatting decision` &&
            ` in the backend that the sample does not depend on. // POST-1.71: Formatter.DateCreateObject is referenced via core:require (UI5 >= 1.74). sap.m.SinglePlanningCalendar and its` &&
            ` DayView/WorkWeekView/WeekView are since 1.61 and MonthView since 1.69 (corrected 2026-08-23 - all four had been listed at 1.61; both figures sit inside the 1.71 floor, so scope and the version floor` &&
            ` are unaffected, but the pinned fact was wrong). Also the SinglePlanningCalendar events weekNumberPress and selectedDatesChange (@since 1.123) are kept 1:1 from the original view; newer than 1.71,`.
    text1 = text1 && ` declared per the property-171 policy. // POST-1.71: the icon ``sap-icon://select-appointments`` reached the SAP icon font in 1.96 and is kept 1:1 from the original Page.view.xml, which names it on` &&
            ` the 'Enable multi-day selection' ToggleButton. Newer than the 1.71 floor: there IconPool resolves nothing and the button renders with NO icon, silently - the app needs a UI5 release >= 1.96 to show` &&
            ` it. The dateSelectionMode property the same button drives carries no @since of its own, but its ENUM TYPE sap.m.SinglePlanningCalendarSelectionMode is @since 1.113 - invisible to the property gate at` &&
            ` the attribute-name level (AGENTS section 5, the enum-value residual limit), so it is declared here BY POLICY: the app needs a UI5 release >= 1.113 for the multi-day selection to mean anything. //` &&
            ` NOTE: onPress reproduced 1:1 since 2026-08-21. The port toasted a constant 'Day selection mode toggled' before and carried NO dateSelectionMode at all, so the one behaviour` &&
            ` SinglePlanningCalendarDateSelection exists to demonstrate was silently absent and undeclared - found by the review sweep. Both halves of the original handler are bindable properties, so both are held`.
    text1 = text1 && ` in the model and bound two-way rather than driven through a frontend action (the prefer-a-bindable-property rule): dateSelectionMode flips SingleSelect <-> MultiSelect and the ToggleButton's tooltip` &&
            ` follows with 'Enable multi-day selection' / 'Disable multi-day selection', exactly the strings the original's setTooltip uses. // NOTE: The payload is read by hand - the frontend marshals each` &&
            ` control into an object of its ID plus all its public properties, and this port walks that object for the fields it models. abap2UI5 releases no JSON reader and the vendored ajson copy is` &&
            ` framework-internal (the linter's non-released-api rule reports it, correctly), so a targeted reader is the intended shape here; abap2UI5/samples Z2UI5_CL_SMP_APP_197 and _327 are the same pattern. It` &&
            ` reads what the FRAMEWORK wrote, which is flat, and does not resolve escapes - a payload composed from free user input would need that.`.
    text2 = `Formatter.DateCreateObject is referenced via core:require (UI5 >= 1.74). sap.m.SinglePlanningCalendar and its DayView/WorkWeekView/WeekView are since 1.61 and MonthView since 1.69 (corrected` &&
            ` 2026-08-23 - all four had been listed at 1.61; both figures sit inside the 1.71 floor, so scope and the version floor are unaffected, but the pinned fact was wrong). Also the SinglePlanningCalendar` &&
            ` events weekNumberPress and selectedDatesChange (@since 1.123) are kept 1:1 from the original view; newer than 1.71, declared per the property-171 policy. // the icon` &&
            ` ``sap-icon://select-appointments`` reached the SAP icon font in 1.96 and is kept 1:1 from the original Page.view.xml, which names it on the 'Enable multi-day selection' ToggleButton. Newer than the` &&
            ` 1.71 floor: there IconPool resolves nothing and the button renders with NO icon, silently - the app needs a UI5 release >= 1.96 to show it. The dateSelectionMode property the same button drives` &&
            ` carries no @since of its own, but its ENUM TYPE sap.m.SinglePlanningCalendarSelectionMode is @since 1.113 - invisible to the property gate at the attribute-name level (AGENTS section 5, the`.
    text2 = text2 && ` enum-value residual limit), so it is declared here BY POLICY: the app needs a UI5 release >= 1.113 for the multi-day selection to mean anything.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarDateSelection`           class = `z2ui5_cl_smpc_app_109` path = `src/02/01/z2ui5_cl_smpc_app_109.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `POST-1.71: sap.m.SinglePlanningCalendar's drag-and-drop members are all newer than the 1.71 floor and are kept for the 1:1 port, since they are the sample: enableAppointmentsDragAndDrop and the` &&
            ` appointmentDrop event (@since 1.64 on the control, but the sample's whole subject), enableAppointmentsResize with appointmentResize and enableAppointmentsCreate with appointmentCreate. The port is` &&
            ` filed in src/02 for the same reason apps 549-555 are. // NOTE: the calendar's startDate and every CalendarAppointment date are typed "object" and demand a real JS Date. The model keeps ISO strings` &&
            ` and Formatter.DateCreateObject converts them at the point of use, which is why the two binding values read differently from the sample's plain {startDate} / {endDate} - same field, one formatter (app` &&
            ` 549/553 idiom). // NOTE: handleAppointmentDrop, handleAppointmentResize and handleAppointmentCreateDnD all mutate the model: a copy drag pushes a new row carrying the dragged appointment's title,` &&
            ` icon, text and type, a move or a resize rewrites the two dates on the row that was dragged, and a create pushes a bare 'New Appointment'. All three port 1:1 over the bound ABAP table. The wires carry`.
    text1 = text1 && ` the interval's LOCAL date parts (getFullYear / getMonth + 1 / getDate / getHours / getMinutes) rather than a UTC toISOString, which would shift the day west of Greenwich, plus the dragged` &&
            ` appointment's binding path and the copy flag - app 549 carries the identical three wires. // NOTE: the three Switches write a settings> model in the sample and are bound BOTH to that model and to the` &&
            ` calendar's three enable* properties. abap2UI5 serves one default model, so the settings> prefix folds away (same-data prefix-drop) and each Switch shares one field with the property it enables -` &&
            ` which is what the sample's own two-way binding already does, without a handler. All three start on, as the model seeds them. // NOTE: the 36 appointments and the 2018-07-09 start date are seeded` &&
            ` verbatim from onInit's JSON. The mock builds its dates with UI5Date.getInstance('2018', '6', ...) - month 6 is July - and two of them carry an out-of-range minute (hour 7 minute 9, hour 9 minute 9)` &&
            ` that JavaScript normalizes; the port stores the same values as ISO strings. // NOTE: not yet verified in a running system: that dragging an appointment moves it and toasts, that Ctrl-dragging copies`.
    text1 = text1 && ` it, that resizing rewrites its end and that dragging on empty space creates one. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_610.mjs). // NOTE: The row` &&
            ` created at runtime seeds the enum-typed field explicitly. An ABAP field is never absent, so an unset one reaches the client as "" - not a member of the enum - and validateProperty throws, taking the` &&
            ` binding update and the view down. The original pushes a JS object without the key at all, which falls back to the property default.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarDND`                     class = `z2ui5_cl_smpc_app_610` path = `src/02/01/z2ui5_cl_smpc_app_610.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.SinglePlanningCalendar's drag-and-drop members are all newer than the 1.71 floor and are kept for the 1:1 port, since they are the sample: enableAppointmentsDragAndDrop and the appointmentDrop` &&
                 ` event (@since 1.64 on the control, but the sample's whole subject), enableAppointmentsResize with appointmentResize and enableAppointmentsCreate with appointmentCreate. The port is filed in src/02` &&
                 ` for the same reason apps 549-555 are.` ) ).

    text1 = `POST-1.71: The whole recurrence stack is newer than the 1.71 floor: sap.m.SinglePlanningCalendar nonWorkingPeriods @since 1.128 (property and aggregation), sap.ui.unified.RecurringNonWorkingPeriod` &&
            ` @since 1.127.0, sap.ui.unified.TimeRange @since 1.127, sap.ui.unified.RecurringCalendarAppointment @since 1.149 and sap.ui.unified.RecurrenceRule @since 1.149. Recurring appointments ARE the sample,` &&
            ` so all of them are kept and the port is filed under src/02. // NOTE: The calendar date properties (the SinglePlanningCalendar startDate, every appointment startDate / endDate and the` &&
            ` recurrenceEndDate and the non-working period's date) are typed "object" and demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at` &&
            ` the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). The TimeRange start / end stay plain strings, which is what its valueFormat expects. // NOTE: The create dialog lives in its own` &&
            ` fragment with its own named model (create>). abap2UI5 keeps one default model, so the dialog's fourteen fields are folded to flat fields here and every visibility expression the fragment carries`.
    text1 = text1 && ` reads them directly - the fragment's own expression bindings survive one-to-one. // NOTE: onCreateDialogSave assembles the new appointment from the dialog model, pushes it into the appointments` &&
            ` array, closes the dialog and toasts. All four are reproduced on the round-trip, including the recurrence branches (the weekly day list only when Weekly, the rule fields only for Monthly / Yearly and` &&
            ` the month only for Yearly) and the parseInt fallbacks the original applies. onRecurrenceTypeChange clears the parts the picked recurrence does not use, also 1:1. // NOTE: ``recurrencePattern``` &&
            ` carries ``defaultValue: 1`` and ``setRecurrencePattern`` REJECTS anything below it (``throw new Error("recurrencePattern must be >= 1")``, RecurringCalendarAppointment.js:163). onCreateDialogSave` &&
            ` writes the property only inside its ``if (oData.recurrenceType)`` branch, so a NON-recurring new appointment carries no such key at all and the control keeps its default. An ABAP structure always` &&
            ` serializes every field, so the port's initial 0 reached the setter through the ``{RECURRENCEPATTERN}`` binding and terminated the app the first time an appointment was created without a recurrence`.
    text1 = text1 && ` (found 2026-08-29 on app 548, which builds its new appointment the same way; reproduced standalone on OpenUI5 1.151, where pattern 0 throws while the view is created and pattern 1 renders three` &&
            ` appointments). The port therefore seeds the new row with the control's own default, ``recurrencepattern = 1``, and the recurrence branch overwrites it as before. The sibling` &&
            ` ``sap.ui.unified.RecurringNonWorkingPeriod.recurrencePattern`` has the identical setter (``RecurringNonWorkingPeriod.js:145``), but no ABAP writes a non-working row - they are only seeded - so that` &&
            ` one carries the default in the BINDING instead: the port writes ``recurrencePattern="{= ${RECURRENCEPATTERN} || 1 }"`` where the original writes ``{recurrencePattern}``, which is the ``binding` &&
            ` value`` difference structural-diff reports for it. Measured on OpenUI5 1.151: bound 0 throws, the expression renders 0 as the control's 1 and leaves a real 2 alone, and an EMPTY non-working table is` &&
            ` safe either way because a template with no row behind it keeps the default. // IMPROVISED: onCreateAppointment seeds the dialog with the CLIENT's next full hour and the hour after it`.
    text1 = text1 && ` (UI5Date.getInstance() with minutes zeroed). A backend has no client clock, so the port seeds the SERVER's local date and next full hour instead. Same shape, a different clock - which matters only if` &&
            ` the two are in different time zones. // NOTE: handleViewChange toasts a constant text; the toast is composed on the client, so the view change needs no round-trip. // NOTE: The nine recurring` &&
            ` appointments with their rules, the eleven recurring non-working periods, and the create dialog with its recurrence branches are unverified in a running system. **e2e-verified 2026-08-25** (nightly` &&
            ` e2e interaction, meta/interactions/z2ui5_cl_smpc_app_555.mjs). The dialog's save path - creating an appointment with NO recurrence, the case that used to terminate the app - is covered by the same` &&
            ` interaction since 2026-08-29. // NOTE: The two DateTimePickers and the recurrence DatePicker bind their value UNTYPED, where the original binds them with type sap.ui.model.type.DateTime /` &&
            ` sap.ui.model.type.Date and formatOptions { pattern: 'yyyy-MM-dd HH:mm' } / { pattern: 'yyyy-MM-dd' } (CreateAppointmentDialog.fragment.xml:34,40,118). The port keeps the path and carries the pattern`.
    text1 = text1 && ` as valueFormat + displayFormat instead, which is not a cosmetic choice: a typed binding with a source pattern raises on the EMPTY value the recurrence picker seeds with and a cleared picker sends,` &&
            ` which is why apps 549 and 609 settled on the same form. Until 2026-08-26 the port declared neither, and that was a real defect rather than a simplification - an unformatted picker writes a LOCALE` &&
            ` string back through the two-way binding. Measured in de-DE: picking 4 March 2025 stored '04.03.2025, 10:15:00', which Formatter.DateCreateObject's new Date( ) reads month-first, so the appointment` &&
            ` was drawn on 3 April. structural-diff cannot see this class at all - the port HAS a value attribute, so nothing reads as missing.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarRecurringItem`           class = `z2ui5_cl_smpc_app_555` path = `src/02/01/z2ui5_cl_smpc_app_555.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `The whole recurrence stack is newer than the 1.71 floor: sap.m.SinglePlanningCalendar nonWorkingPeriods @since 1.128 (property and aggregation), sap.ui.unified.RecurringNonWorkingPeriod @since` &&
                 ` 1.127.0, sap.ui.unified.TimeRange @since 1.127, sap.ui.unified.RecurringCalendarAppointment @since 1.149 and sap.ui.unified.RecurrenceRule @since 1.149. Recurring appointments ARE the sample, so all` &&
                 ` of them are kept and the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.m.SinglePlanningCalendar firstDayOfWeek is @since 1.98 - newer than the 1.71 floor. The first-day-of-week Select IS the sample's second half, so the property is kept and the port is` &&
            ` filed under src/02. // NOTE: The calendar date properties (the SinglePlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The` &&
            ` model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: The original keeps the` &&
            ` sticky mode in a SECOND named model (settings>) and a third (allDay>) the view never reads. abap2UI5 keeps one default model, so the sticky mode is a field here and the Select and the calendar share` &&
            ` it; the unused allDay model is dropped. // NOTE: onChange calls setFirstDayOfWeek( Number( key ) ). The property is bindable, so the Select shares its key with the calendar and the change handler is` &&
            ` dropped. firstDayOfWeek is an INT property while a Select key is a string, so the calendar binds an expression that multiplies by 1 - the Number( ) conversion the original does in the handler. //`.
    text1 = text1 && ` NOTE: Some appointment rows carry a tentative flag the view never binds; the port seeds it the same way and leaves it unbound, so the data stays 1:1 with the mock. // NOTE: The eight` &&
            ` first-day-of-week keys, the three sticky modes and the four calendar views are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_551.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarSnappingHeader`          class = `z2ui5_cl_smpc_app_551` path = `src/02/01/z2ui5_cl_smpc_app_551.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.SinglePlanningCalendar firstDayOfWeek is @since 1.98 - newer than the 1.71 floor. The first-day-of-week Select IS the sample's second half, so the property is kept and the port is filed under` &&
                 ` src/02.` ) ).

    text1 = `POST-1.71: sap.m.SinglePlanningCalendar calendarWeekNumbering is @since 1.110.0 - newer than the 1.71 floor. The four week-numbering schemes ARE the sample, so the property is kept and the port is` &&
            ` filed under src/02. // NOTE: The calendar date properties (the SinglePlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The` &&
            ` model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE: The original keeps the` &&
            ` sticky mode in a SECOND named model (settings>) and a third (allDay>) the view never reads. abap2UI5 keeps one default model, so the sticky mode is a field here and the Select and the calendar share` &&
            ` it - the same two-way link the named model gives them; the unused allDay model is dropped. // NOTE: onChange calls setCalendarWeekNumbering. The property is bindable, so the Select shares its key` &&
            ` with the calendar and the change handler is dropped. // NOTE: Some appointment rows carry a tentative flag the view never binds; the port seeds it the same way and leaves it unbound, so the data`.
    text1 = text1 && ` stays 1:1 with the mock. // NOTE: The four week-numbering schemes, the three sticky modes and the four calendar views are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_550.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarWeekNumbering`           class = `z2ui5_cl_smpc_app_550` path = `src/02/01/z2ui5_cl_smpc_app_550.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.SinglePlanningCalendar calendarWeekNumbering is @since 1.110.0 - newer than the 1.71 floor. The four week-numbering schemes ARE the sample, so the property is kept and the port is filed under` &&
                 ` src/02.` ) ).

    text1 = `NOTE: The calendar date properties (the SinglePlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings` &&
            ` and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // IMPROVISED: The sample's point is TWO CUSTOM VIEW` &&
            ` CLASSES: CustomThreeDaysView and CustomTenDaysView, JS subclasses of sap.m.SinglePlanningCalendarView that override getEntityCount, getScrollEntityCount and calculateStartDate. A backend cannot` &&
            ` define a control class, so the port declares only the three BUILT-IN views the controller also adds (Day, Work Week, Week) and the '3 Days' and '10 Days' entries are lost. The views aggregation` &&
            ` itself is declarative here rather than five addView calls, which is why structural-diff reports SinglePlanningCalendarDayView, SinglePlanningCalendarWorkWeekView and SinglePlanningCalendarWeekView as` &&
            ` control extra - the sample's view declares none of them. // NOTE: Some appointment rows carry a tentative flag the view never binds; the port seeds it the same way and leaves it unbound, so the data`.
    text1 = text1 && ` stays 1:1 with the mock. // NOTE: The three built-in views and the bound appointments are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_552.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarWithCustomViews`         class = `z2ui5_cl_smpc_app_552` path = `src/01/01/z2ui5_cl_smpc_app_552.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.61`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.ui.unified.DateTypeRange secondaryType is @since 1.81.0 and color @since 1.76.0 - both newer than the 1.71 floor. The coloured and secondary-typed special dates are what the legend` &&
            ` explains, so both are kept and the port is filed under src/02. // NOTE: The calendar date properties (the SinglePlanningCalendar startDate and every CalendarAppointment / DateTypeRange startDate and` &&
            ` endDate) are typed "object" and demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108` &&
            ` uses (needs UI5 >= 1.74). // NOTE: The original keeps the legend-shown flag in a SECOND named model (stateModel>). abap2UI5 keeps one default model, so the flag is a field here and the` &&
            ` DynamicSideContent and the ToggleButton share it. // NOTE: toggleFullDay flips setFullDay. The property is bindable, so the Full Day ToggleButton and the calendar share the flag and the press handler` &&
            ` is dropped - which costs one added ToggleButton.pressed attribute the sample's view does not have. // NOTE: Some appointment rows carry a tentative flag the view never binds; the port seeds it the`.
    text1 = text1 && ` same way and leaves it unbound, so the data stays 1:1 with the mock. // NOTE: The legend side content, the full-day toggle and the seven coloured special dates are unverified in a running system.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_553.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarWithLegend`              class = `z2ui5_cl_smpc_app_553` path = `src/02/01/z2ui5_cl_smpc_app_553.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.DateTypeRange secondaryType is @since 1.81.0 and color @since 1.76.0 - both newer than the 1.71 floor. The coloured and secondary-typed special dates are what the legend explains, so` &&
                 ` both are kept and the port is filed under src/02.` ) ).

    text1 = `POST-1.71: sap.m.SinglePlanningCalendar scaleFactor is @since 1.99 and sap.ui.unified.DateTypeRange color @since 1.76.0 - both newer than the 1.71 floor. The zoom IS the sample, so both are kept and` &&
            ` the port is filed under src/02. // NOTE: The calendar date properties (the SinglePlanningCalendar startDate and every CalendarAppointment / DateTypeRange startDate and endDate) are typed "object" and` &&
            ` demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74). // NOTE:` &&
            ` The original keeps the legend-shown flag in a SECOND named model (stateModel>). abap2UI5 keeps one default model, so the flag is a field here and the DynamicSideContent and the ToggleButton share it.` &&
            ` // NOTE: toggleFullDay flips setFullDay. The property is bindable, so the Full Day ToggleButton and the calendar share the flag and the press handler is dropped - which costs one added` &&
            ` ToggleButton.pressed attribute the sample's view does not have. // NOTE: zoomIn and zoomOut step setScaleFactor up and down. The property is bindable, so the two presses do the same increment in ABAP`.
    text1 = text1 && ` and the calendar follows the bound field; scaleFactor is seeded at the control's own default of 1. // NOTE: The view binds specialDates to /specialDates, a path the sample's own model never fills` &&
            ` (its types array is empty), so the port seeds an empty table there - the same nothing the original renders. // NOTE: Some appointment rows carry a tentative flag the view never binds; the port seeds` &&
            ` it the same way and leaves it unbound, so the data stays 1:1 with the mock. // NOTE: The zoom in/out steps, the full-day toggle and the legend side content are unverified in a running system.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_554.mjs). // NOTE: The two legend aggregations are bound ABSOLUTELY via _bind( path = abap_true ). A bare` &&
            ` 'T_X' path is relative and resolves against nothing outside a row context, and an unbound table never reaches the client model at all - the legend rendered empty on both counts. Same fix as app 553.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SinglePlanningCalendar`          name = `SinglePlanningCalendarWithZoomInZoomOut`       class = `z2ui5_cl_smpc_app_554` path = `src/02/01/z2ui5_cl_smpc_app_554.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.61`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.SinglePlanningCalendar scaleFactor is @since 1.99 and sap.ui.unified.DateTypeRange color @since 1.76.0 - both newer than the 1.71 floor. The zoom IS the sample, so both are kept and the port is` &&
                 ` filed under src/02.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Slider`                          name = `Slider`                                        class = `z2ui5_cl_smpc_app_068` path = `src/01/01/z2ui5_cl_smpc_app_068.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.SlideTile`                       name = `SlideTile`                                     class = `z2ui5_cl_smpc_app_082` path = `src/01/01/z2ui5_cl_smpc_app_082.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = `NOTE: the sample's demo-kit backgroundImage paths are resolved to absolute sdk.openui5.org URLs.` ) ).

    text1 = `NOTE: Master-detail navigation is driven 1:1 via follow_up_action( cs_event-control_by_id ) on the newly whitelisted SplitApp methods: to (Go-to-Detail button), backDetail/backMaster (page` &&
            ` navButtonPress), toMaster (master list item), toDetail (master2 list items) and toDetail (master2 list items). Corrected 2026-08-23: this list used to end "and setMode (RadioButtonGroup)", and no` &&
            ` such wire exists - the handler only assigns the two-way bound mode field, which is what two later deviations in this same sidecar say. The methods were added to the abap2UI5 framework in the same` &&
            ` change set (CONTROL_METHODS). // NOTE: The master2 list navigates via a per-item press event that carries the target page id as a t_arg literal ('detail'/'detailDetail'/'detail2'); the original reads` &&
            ` the pressed item's custom:to CustomData in one List.itemPress handler, which the port has not yet converted. Corrected 2026-08-23: the old wording said the CustomData "is not transportable as an` &&
            ` event arg", and that is wrong - an arg is a full UI5 expression, and the twin port 096 was corrected on 2026-08-21 to one List.itemPress carrying`.
    text1 = text1 && ` ${$parameters>/listItem}.getCustomData()[0].getValue(). The same correction was never applied here. The per-item press is functionally equivalent for Active items, so this is a structural and` &&
            ` documentation divergence rather than a broken click. custom:to is kept on the items for fidelity. // NOTE: The split mode is selected via a two-way selectedIndex binding on the RadioButtonGroup` &&
            ` (mode_idx) and mapped to the SplitAppMode string in ABAP; the original reads the selected RadioButton's custom:splitAppMode CustomData. custom:splitAppMode is kept on the buttons for fidelity. The` &&
            ` onInit setHomeIcon and the onOrientationChange toast are dropped (device-specific cosmetics). // NOTE: Residual, added 2026-08-23: the interaction module clicks the single 'Go to Detail page2' button` &&
            ` and asserts the SplitApp shows 'Detail Detail', which covers the ``to`` wire only - toDetail, toMaster, backDetail and backMaster are never exercised. SplitApp as the root view plus the control_by_id` &&
            ` navigation (to/toDetail/toMaster/backDetail/backMaster - setMode is NOT among them, see the correction above) need an in-system check — machine gates only verify view validity, not the runtime`.
    text1 = text1 && ` navigation roundtrip. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_097.mjs). // NOTE: the mode RadioButtonGroup's handler calls oSplitApp.setMode( ) in` &&
            ` the original; SplitApp.mode is a bindable property, so the port binds it two-way (added attribute, no structural diff) and the handler only assigns the chosen mode - the prefer-a-bindable-property` &&
            ` rule, gated by the linter rule settable-property-via-action. The state then survives a view rebuild instead of living only in the control. // NOTE: Measured 2026-08-27 against the ``pageId`` argument` &&
            ` kind abap2UI5 introduced for ``to`` (``977474af``, squash-merged into main as ``56ff2a10``, #2669 - ``977474af`` itself is on no branch). sap.m.SplitContainer.to probes` &&
            ` ``this._oMasterNav.getPage(pageId)`` and falls through to ``this._oDetailNav.to( )`` (SplitContainer.js:828), and getPage compares ``aPages[i].getId() == pageId`` - a comparison a Control can never` &&
            ` win. So before the fix the master probe ALWAYS missed and every ``to`` went to the detail NavContainer, whose own ``to`` normalises a Control to its id on its first line. This port's target`.
    text1 = text1 && ` ``detailDetail`` is a detailPages entry, so the wrong branch reached the right page and nothing was observably broken. The trap is latent, not active: a ``to`` naming a MASTER page would have` &&
            ` navigated the detail container instead. Corpus-wide there is no such call - 096 and 097 are the only SplitContainer/SplitApp ports that wire ``to``, both to ``detailDetail``, and app 578's` &&
            ` FlexibleColumnLayout was the only container the same probe actually broke.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SplitApp`                        name = `SplitApp`                                      class = `z2ui5_cl_smpc_app_097` path = `src/01/01/z2ui5_cl_smpc_app_097.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: Master-detail navigation is driven 1:1 via follow_up_action( cs_event-control_by_id ) on the newly whitelisted SplitContainer methods: to (Go-to-Detail button), backDetail/backMaster (page` &&
            ` navButtonPress), toMaster (master list item), toDetail (master2 list items) and toDetail (master2 list items). Corrected 2026-08-23: this list used to end "and setMode (RadioButtonGroup)", and no` &&
            ` such wire exists - the handler only assigns the two-way bound mode field, which is what two later deviations in this same sidecar say. The methods were added to the abap2UI5 framework in the same` &&
            ` change set (CONTROL_METHODS). // NOTE: The master2 list navigates through ONE List.itemPress handler, as the original does, carrying the pressed item's CustomData:` &&
            ` ``${$parameters>/listItem}.getCustomData()[0].getValue()``. Corrected 2026-08-21 - until then the port wired a press per item with the target page id as a t_arg LITERAL` &&
            ` ('detail'/'detailDetail'/'detail2') and declared that CustomData 'is not transportable as an event arg', which is wrong: an arg is a FULL UI5 expression (CAPABILITIES, proven 2026-07-31) and indexed`.
    text1 = text1 && ` access into an array-valued getter was measured to resolve by scripts/probes/event-arg-expression-probe.mjs. The custom:to CustomData is what the wire now READS, not decoration kept for fidelity. //` &&
            ` NOTE: The split mode is selected via a two-way selectedIndex binding on the RadioButtonGroup (mode_idx) and mapped to the SplitAppMode string in ABAP; the original reads the selected RadioButton's` &&
            ` custom:splitAppMode CustomData. custom:splitAppMode is kept on the buttons for fidelity. The onAfterRendering parent-height fix and the device-model onInit setup (device model is global in abap2UI5)` &&
            ` are dropped. // NOTE: live-verified 2026-07-27: SplitContainer as the root view plus the control_by_id navigation (to/toDetail/toMaster/backDetail/backMaster - setMode is NOT among them, see the` &&
            ` correction above) need an in-system check — machine gates only verify view validity, not the runtime navigation roundtrip. // NOTE: the mode RadioButtonGroup's handler calls oSplitContainer.setMode(` &&
            ` ) in the original; SplitContainer.mode is a bindable property, so the port binds it two-way (added attribute, no structural diff) and the handler only assigns the chosen mode - the`.
    text1 = text1 && ` prefer-a-bindable-property rule, gated by the linter rule settable-property-via-action. The state then survives a view rebuild instead of living only in the control. // NOTE: the two-way mode binding` &&
            ` replaces the setMode( ) action and is not live-verified. The imperative form it replaces WAS verified: verified in a running system 2026-07-27 - SplitContainer` &&
            ` toDetail/toMaster/backDetail/backMaster/setMode navigation works across modes (2026-07-27) - kept as context. A fresh live run (each radio button switches the split mode and the toast names it)` &&
            ` restamps this port to checked. Residual, added 2026-08-23: the interaction module clicks ONE radio ('hide') and asserts one toast; the other three are never touched and nothing asserts the` &&
            ` SplitContainer's mode actually changed. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_096.mjs). // NOTE: Measured 2026-08-27 against the ``pageId``` &&
            ` argument kind abap2UI5 introduced for ``to`` (``977474af``, squash-merged into main as ``56ff2a10``, #2669 - ``977474af`` itself is on no branch). sap.m.SplitContainer.to probes`.
    text1 = text1 && ` ``this._oMasterNav.getPage(pageId)`` and falls through to ``this._oDetailNav.to( )`` (SplitContainer.js:828), and getPage compares ``aPages[i].getId() == pageId`` - a comparison a Control can never` &&
            ` win. So before the fix the master probe ALWAYS missed and every ``to`` went to the detail NavContainer, whose own ``to`` normalises a Control to its id on its first line. This port's target` &&
            ` ``detailDetail`` is a detailPages entry, so the wrong branch reached the right page and nothing was observably broken. The trap is latent, not active: a ``to`` naming a MASTER page would have` &&
            ` navigated the detail container instead. Corpus-wide there is no such call - 096 and 097 are the only SplitContainer/SplitApp ports that wire ``to``, both to ``detailDetail``, and app 578's` &&
            ` FlexibleColumnLayout was the only container the same probe actually broke.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.SplitContainer`                  name = `SplitContainer`                                class = `z2ui5_cl_smpc_app_096` path = `src/01/01/z2ui5_cl_smpc_app_096.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItem`                              class = `z2ui5_cl_smpc_app_212` path = `src/01/01/z2ui5_cl_smpc_app_212.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: the List element binding {/ProductCollection} and the items' index paths ({0/Name}..{3/Name}) are kept 1:1 against the default-model table /T_PRODUCTS (full 123-row mock inlined). // POST-1.71:` &&
            ` the sap.m.Avatar control (since UI5 1.73) is newer than 1.71 but kept for the 1:1 port on three items. // POST-1.71: the StandardListItem avatar aggregation (since UI5 1.98) is newer than 1.71 but` &&
            ` kept for the 1:1 port - the app needs a UI5 release >= 1.98 to render it. Aggregation/control-level, invisible to the attribute-scanning property gate. // NOTE: The sample's asset paths are` &&
            ` host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The` &&
            ` values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had` &&
            ` stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale`.
    text1 = text1 && ` absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemAvatar`                        class = `z2ui5_cl_smpc_app_083` path = `src/02/01/z2ui5_cl_smpc_app_083.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `the sap.m.Avatar control (since UI5 1.73) is newer than 1.71 but kept for the 1:1 port on three items. // the StandardListItem avatar aggregation (since UI5 1.98) is newer than 1.71 but kept for the` &&
                 ` 1:1 port - the app needs a UI5 release >= 1.98 to render it. Aggregation/control-level, invisible to the attribute-scanning property gate.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemDescription`                   class = `z2ui5_cl_smpc_app_202` path = `src/01/01/z2ui5_cl_smpc_app_202.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemIcon`                          class = `z2ui5_cl_smpc_app_468` path = `src/01/01/z2ui5_cl_smpc_app_468.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
                 ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim` &&
                 ` that every port doing it had a declaration.` )
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemInfo`                          class = `z2ui5_cl_smpc_app_208` path = `src/01/01/z2ui5_cl_smpc_app_208.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the original binds infoState via the presentation-only formatter '.formatter.status' (maps the already-classified Status Available->Success / Out of Stock->Warning / Discontinued->Error / else` &&
                 ` None to a ValueState). abap2UI5 being a thin frontend, the info state is derived in the backend into the INFOSTATE field and bound directly (infoState="{INFOSTATE}"); same output, no loss. All 123` &&
                 ` mock rows (Name/Status) kept verbatim; the List items sorter:{path:'NAME'} is kept 1:1 as a raw binding-info string.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemInfoStateInverted`             class = `z2ui5_cl_smpc_app_204` path = `src/02/01/z2ui5_cl_smpc_app_204.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.StandardListItem infoStateInverted (since 1.74) is kept 1:1 from the original view; needs a UI5 release >= 1.74 to render.`
        post171 = `sap.m.StandardListItem infoStateInverted (since 1.74) is kept 1:1 from the original view; needs a UI5 release >= 1.74 to render.` )
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemNavigated`                     class = `z2ui5_cl_smpc_app_482` path = `src/02/01/z2ui5_cl_smpc_app_482.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.StandardListItem.navigated is @since 1.72 and is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.72. // NOTE: The navigated flag comes from the` &&
                 ` .isNavigated formatter comparing the settings model's navigatedItem with each row's ProductId. A formatter is business logic, so the port binds a plain navigated field per row and the press wire` &&
                 ` (carrying ${PRODUCTID}) sets it in ABAP - exactly one row navigated at a time, like the original. // NOTE: The press wire setting the navigated row is unverified in a running system. **e2e-verified` &&
                 ` 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_482.mjs).`
        post171 = `sap.m.StandardListItem.navigated is @since 1.72 and is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.72.` ) ).

    text1 = `NOTE: onInit calls list.bindElement('/ProductCollection'), which is why the four items address their records as {0/...} .. {3/...}. The port keeps that element binding on the List (binding attribute` &&
            ` over the bound table), so the relative indexed paths resolve exactly as in the original - including the second item's empty description and the fourth's missing one, both reproduced verbatim. //` &&
            ` NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
            ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Added 2026-08-23: this port did the rewrite without declaring it, one of 17 found by re-counting the corpus-wide claim` &&
            ` that every port doing it had a declaration.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemTitle`                         class = `z2ui5_cl_smpc_app_483` path = `src/01/01/z2ui5_cl_smpc_app_483.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: infoIcon (since UI5 1.150) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.150 to render the status icon next to the info text. // POST-1.71:` &&
            ` infoStateInverted (since UI5 1.74) is newer than 1.71 but kept for the 1:1 port - it is what the sample's Toggle Inverted button drives. // POST-1.71: wrapCharLimit (since UI5 1.94) is newer than` &&
            ` 1.71 but kept for the 1:1 port - it is the property the last two rows of the sample demonstrate. // NOTE: the original controller seeds only wrapping: false; /inverted is never seeded, so the Toggle` &&
            ` Inverted button starts from an undefined model value that UI5 reads as not pressed. The port seeds inverted = abap_false explicitly, which is the same initial rendering - a flat ABAP row cannot carry` &&
            ` an absent field. // POST-1.71: the icon ``sap-icon://information`` reached the SAP icon font in 1.80 and is kept 1:1 from the original List.controller.js, which seeds it as the infoIcon of the` &&
            ` wrapping rows. Newer than the 1.71 floor: there IconPool resolves nothing and the row renders with NO info icon, silently - and infoIcon itself is @since 1.150 anyway, so the icon can only ever show`.
    text1 = text1 && ` on a release that has both.`.
    text2 = `infoIcon (since UI5 1.150) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.150 to render the status icon next to the info text. // infoStateInverted (since UI5 1.74) is` &&
            ` newer than 1.71 but kept for the 1:1 port - it is what the sample's Toggle Inverted button drives. // wrapCharLimit (since UI5 1.94) is newer than 1.71 but kept for the 1:1 port - it is the property` &&
            ` the last two rows of the sample demonstrate. // the icon ``sap-icon://information`` reached the SAP icon font in 1.80 and is kept 1:1 from the original List.controller.js, which seeds it as the` &&
            ` infoIcon of the wrapping rows. Newer than the 1.71 floor: there IconPool resolves nothing and the row renders with NO info icon, silently - and infoIcon itself is @since 1.150 anyway, so the icon can` &&
            ` only ever show on a release that has both.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StandardListItem`                name = `StandardListItemWrapping`                      class = `z2ui5_cl_smpc_app_376` path = `src/02/01/z2ui5_cl_smpc_app_376.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: the sample binds a List to the JSON model /modelData and renders one templated CustomListItem per row. **Rebuilt as that ONE bound template on 2026-08-05** (it used to be 14 unrolled static` &&
            ` items): the rows are bound with client->_bind( val = modeldata omit_initial_paths = ... ), the parameter added upstream for exactly this shape (pr/model-empty-vs-default), so a property a row does` &&
            ` not set stays ABSENT from the model and the StepInput keeps its own default instead of receiving ````. The dropped template property valueState is back as well. The omission is SCOPED to the` &&
            ` numeric/string columns because the two BOOLEAN properties must send their explicit false (abap_false is itself initial, so the blanket flag would have dropped it and the disabled / read-only row` &&
            ` would render enabled) - which is why the port binds enabled/editable as plain {ENABLED}/{EDITABLE} again, with no expression binding and no binding-value deviation left. Rows the sample leaves` &&
            ` untouched send the control default true explicitly. // NOTE: the change toast was switched to a roundtrip-free client-composed toast on 2026-07-22 (control_global MESSAGE_TOAST.show, template ``Value`.
    text1 = text1 && ` changed to '{0}'`` with get_t_arg single-quote escaping; on_event dropped, init-only) - re-verify changing a StepInput toasts "Value changed to '<value>'" with the quotes intact. **e2e-verified` &&
            ` 2026-07-30, restated 2026-08-23** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the port's change wire fires and toasts "Value changed to ..."; the other StepInputs are the identical` &&
            ` wire. The earlier wording claimed more than the module does and had drifted from it: ArrowUp + Enter stopped producing a change on the pinned UI5 (the +/- icons render zero-size headless, which is` &&
            ` why the keyboard route was chosen in the first place), so the module now falls back to firing change through the control API after checking that no toast appeared - it still exercises the port's` &&
            ` wire, which is what is under test, but it is not the keyboard path. The assertion is also toContainText("Value changed to") only, so the quoted <value> and its quotes are NOT asserted.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StepInput`                       name = `StepInput`                                     class = `z2ui5_cl_smpc_app_049` path = `src/01/01/z2ui5_cl_smpc_app_049.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.40`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.StepInput`                       name = `StepInputValueState`                           class = `z2ui5_cl_smpc_app_375` path = `src/01/01/z2ui5_cl_smpc_app_375.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.40` )
      ( module = `sap.m`              control = `sap.m.Switch`                          name = `Switch`                                        class = `z2ui5_cl_smpc_app_050` path = `src/01/01/z2ui5_cl_smpc_app_050.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: SALARY is TYPE string, not the ``p LENGTH 8 DECIMALS 2`` it was until 2026-08-26. The original binds ``<Input value="{salary}" description="EUR"/>`` over a JSONModel, which keeps whatever the` &&
            ` user types; a PACKED ABAP field cannot, because a table cell whose text does not convert is swallowed by delta_apply_field's ``CATCH cx_root ##NO_HANDLER`` ("skip just this cell",` &&
            ` z2ui5_cl_ui5_srv_model) - the backend keeps the old number while the browser goes on showing the entry. MEASURED 2026-08-26 against the transpiled backend: typing ``1,455.22`` into the Salary Input` &&
            ` and then taking the app's only round trip (the tab close) returned SALARY 1455.22, the OLD value, with no error and no toast. SALARY carries no typed binding anywhere, so the fix is the type itself` &&
            ` rather than app 570's string-mirror workaround. The seeds are the digits UI5 renders TODAY, not the mock's literals: the packed field serialized as a JSON NUMBER, so ``1189.00`` reached the browser` &&
            ` as 1189 and rendered ``1189`` (measured) - and the original's ``salary: 1189.00`` is likewise the JS number 1189, so ``1189`` is what both the port and the original show. Rendering is therefore`.
    text1 = text1 && ` byte-identical to the pre-change port and faithful to the original. // NOTE: addNewButtonPress appends an empty employee row to the bound /T_EMPLOYEES (the addItem+setSelectedItem equivalent for a` &&
            ` bound aggregation; the new tab is appended but not auto-selected - the new tab is not auto-selected. Corrected 2026-08-23: this used to justify it with "TabContainer.selectedItem is an association a` &&
            ` bound-row clone cannot address from the backend", which the framework lifted on 2026-08-06 - wherever an argument takes a control id it also takes an aggregation ITEM addressed positionally` &&
            ` (``myTabContainer/items/<n>``, 0-based), setSelectedItem is whitelisted with the controlIdOrNull kind, and app 012 already uses exactly that form for the same class of problem. The auto-selection is` &&
            ` therefore expressible and simply not done yet, the one residual difference). itemCloseHandler reproduced 1:1 since 2026-07-30: the itemClose wire carries s_ctrl-check_prevent_default (the original` &&
            ` calls oEvent.preventDefault() unconditionally) and transports ${$parameters>/item}.getName() plus the row index via ${$parameters>/item/oParent}.indexOfItem(${$parameters>/item}) (the dnd-idiom index`.
    text1 = text1 && ` transport); the backend raises MessageBox.confirm (Do you want to close the tab '<name>'?, onclose CLOSE_DECIDE) and on OK deletes the row (the bound-aggregation removeItem) and toasts 'Item closed:` &&
            ` <name>' (duration 500), on Cancel toasts 'Item close canceled: <name>' - exactly the original's onClose branches. The pending name/index live in protected state across the confirm round-trip. The` &&
            ` earlier static 'Close requested' toast (tab never removable) is gone. // NOTE: the prevent-default itemClose + MessageBox.confirm + row-delete chain and the index transport via the oParent` &&
            ` indexOfItem form are unverified in a running system; the e2e interaction covers open-confirm-OK-removes end to end in the transpiled harness. **e2e-verified 2026-07-30** (transpiled-framework` &&
            ` interaction, scripts/e2e-smoke.mjs): covered in part - restated 2026-08-23: the module asserts the MessageBox contains the static prefix "Do you want to close the tab" and that a toast contains "Item` &&
            ` closed:", and neither the item NAME in the box nor the row's REMOVAL is asserted. Since the texts are composed from close_name while the deletion rides on a separately transported index, a wrong`.
    text1 = text1 && ` index (or the -1 case the guard swallows) would leave the module green. What it does cover: close icon -> confirm box with the item name -> OK -> row removed + 'Item closed:' toast (needs the` &&
            ` 2026-07-30 Messages.js onclose fix - under the broken wire the OK action never arrived). **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_093.mjs).` &&
            ` **Corrected 2026-08-26 - that gap is closed.** The interaction now asserts the item NAME in the MessageBox, that the tab count drops 4 -> 3, and that the removed tab is the one that was closed, so a` &&
            ` wrong (or guard-swallowed -1) index can no longer leave the module green. It also types ``1,455.22`` into the Salary Input before closing and asserts the entry survives the round trip - the leg that` &&
            ` FAILS against the packed field (measured). **Needs a backend rebuild (``npm run e2e:build``) before the nightly can confirm it.** // NOTE: The added tab's CONTENT differs, which the auto-selection` &&
            ` deviation had called "the one residual difference". The original constructs a bare TabContainerItem (name + modified) and addItem's it, so it carries no content and the new tab's body is EMPTY; the`.
    text1 = text1 && ` port appends a row to the bound table, so the new tab is a template clone showing the three Labels and three Inputs over a blank row. Declared 2026-08-23.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TabContainer`                    name = `TabContainer`                                  class = `z2ui5_cl_smpc_app_093` path = `src/01/01/z2ui5_cl_smpc_app_093.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: addNewButtonPressHandler creates a TabContainerItem in JavaScript and selects it. The port appends the same row to the bound table; the new tab is appended but not auto-selected, because` &&
            ` TabContainer.selectedItem is an association a bound-row clone cannot address from the backend (app 093's residual difference, same here). // NOTE: itemCloseHandler calls oEvent.preventDefault()` &&
            ` unconditionally and lets MessageBox.confirm decide. Reproduced 1:1: the itemClose wire carries check_prevent_default and transports ${$parameters>/item}.getName() plus the row index via indexOfItem,` &&
            ` the backend raises the confirm with onclose CLOSE_DECIDE, and OK deletes the row and toasts 'Item closed: <name>' (duration 500) while Cancel toasts 'Item close canceled: <name>' (app 093 precedent).` &&
            ` // NOTE: The first employee's icon is the sample's own test-resources image (Woman_04.png), re-hosted on sdk.openui5.org; the other three are icon-font URIs and stay verbatim. The salary values keep` &&
            ` their two decimals (TYPE p DECIMALS 2), rendered as '{SALARY} EUR' exactly as the original Text does. // NOTE: The add wire, the prevented-default itemClose with its confirm round-trip and both toast`.
    text1 = text1 && ` branches are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_451.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TabContainer`                    name = `TabContainerIcons`                             class = `z2ui5_cl_smpc_app_451` path = `src/01/01/z2ui5_cl_smpc_app_451.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: openSelectedItems builds the TabContainer in the controller (new TabContainer with an items binding filtered to the selected ProductIds and a TabContainerItem template holding the Display` &&
            ` fragment) and inserts it into tabContainerPage. The port declares that TabContainer in the view instead and binds its items to T_TABS, the table of open tabs the backend maintains - so the control` &&
            ` extra TabContainer / TabContainerItem against the archived TabContainer.view.xml is the controller-built subtree, not an addition. sap.m.TabContainer has no default aggregation, so the template sits` &&
            ` in an explicit items element beside the items binding. // NOTE: each open tab carries a COPY of the product row (T_TABS), which is exactly what the original edits: handleTabContainerEditItem builds` &&
            ` its edit JSONModel from deepExtend({}, bindingContext.getProperty(path)). Save copies the tab's fields back onto the product (handleTabContainerSaveItem's deepExtend into the model), Cancel throws` &&
            ` the copy away and re-reads the product (handleTabContainerCancelUpdate). The per-tab modified flag is the TabContainerItem.modified property, bound two-way. // NOTE: the Display and Edit fragments`.
    text1 = text1 && ` are not swapped in and out of the tab (removeAllContent/insertContent); both live in the TabContainerItem's content and take turns through visible="{= !${MODIFIED} }" / visible="{= ${MODIFIED} }" - a` &&
            ` declarative expression instead of a controller-driven content swap, with the same rendered result. That is why the Edit fragment's f:SimpleForm, its four Labels, three Inputs and TextArea appear` &&
            ` twice in the port: once inside the tab and once on the add-item page, where the original moves the SAME cached fragment instance between the two. // NOTE: the Edit fragment binds absolute paths` &&
            ` (/Name, /SupplierName, /Price, /Description) because it is put over its own one-record JSONModel. In the tab it binds the tab row's own fields relative to the item context; on the add-item page it` &&
            ` binds the four ADD_* fields the backend keeps for the new record. Only the add-item copy carries the fragment's id=myForm - the tab copy is a template inside a bound aggregation, where an id would be` &&
            ` cloned. // NOTE: the table's selection is read with getSelectedContexts in the original and bound two-way here (ColumnListItem selected="{SELECTED}"), so the backend has it on every round trip;`.
    text1 = text1 && ` onInit's attachSelectionChange becomes the Table's selectionChange attribute, which keeps idOpenSelected's visible flag in sync exactly as fnTableSelectionChange does. onInit also attaches` &&
            ` navButtonPress on the two inner pages; the port wires navButtonPress declaratively on both. // NOTE: _handleTabContainerItemClose calls oEvent.preventDefault( ) and removes the item itself once the` &&
            ` user confirms, and the port reproduces both halves since 2026-09-04: the itemClose wire carries s_ctrl-check_prevent_default, which IS that call, so the control does not close the tab and tab_close(` &&
            ` ) decides. Until then this was an IMPROVISED deviation reading "a thin frontend cannot cancel a UI5 event", which stopped being true when the flag shipped; the port let the control close the tab and` &&
            ` sent the view again from the model, so an unconfirmed close visibly closed and came back. Same two outcomes as before - the tab row survives a cancel and goes for good on a confirm - reached the` &&
            ` original's way round now. Found by scripts/probes/improvised-cluster.mjs (the pr/conditional-prevent-default GAP, closed upstream and outlived here as it had in app 306). // IMPROVISED:`.
    text1 = text1 && ` _handleTabContainerAddNewButtonPress ends with oTabContainer.setSelectedItem(<the new item>) so the fresh tab is the one on screen. TabContainer.selectedItem is an ASSOCIATION to a TabContainerItem` &&
            ` instance, and the item ids of a bound aggregation are generated at runtime, so the port cannot name the item: the new tab is appended and starts modified (in edit mode), but selecting it is left to` &&
            ` the control's own default. // NOTE: the new ProductIds the original generates with Math.random() ('ProductId-' + Math.random(), and a bare Math.random() on the add-item page) become a backend` &&
            ` counter, ProductId-1, ProductId-2, ... - a client-side random draw is not reproducible from a backend and carries no meaning in the sample. // NOTE: _showConfirmation's MessageBox.confirm with the` &&
            ` custom actions ['Leave Page', CANCEL] / ['Close Tab', CANCEL], the Warning icon and initialFocus CANCEL is ported 1:1 through client->message_box_display( actions, initialfocus, onclose ); the chosen` &&
            ` action comes back as the onclose event's argument, as in app 101. // NOTE: _resetUnsavedItems is kept: rows the add-new button created and nobody saved carry an UNSAVED flag and are dropped when the`.
    text1 = text1 && ` user leaves the page (all of them) or closes their tab (just that one), and a tab whose product is gone goes with it. The original's onExit teardown (detaching handlers, nulling members) has no` &&
            ` counterpart - abap2UI5 rebuilds the view per round trip. // NOTE: onInit raises the model size limit to 200 and binds the full mock /ProductCollection; the port seeds all 123 rows verbatim` &&
            ` (ProductId, Name, SupplierName, Description, Price, CurrencyCode - the fields the two views bind). The ObjectHeader keeps the original sap.ui.model.type.Currency composite binding 1:1. // NOTE: the` &&
            ` OverflowToolbarButton that opens the add-item page is icon-only in the original (icon=sap-icon://add, no text and no tooltip). The port gives it tooltip="Add" so it is reachable with a screen reader` &&
            ` - the one accessibility addition in this port. // NOTE: not yet verified in a running system: the TabContainer built over a bound items aggregation, the visible-driven Display/Edit swap inside a tab,` &&
            ` and the close-confirmation round trip that relies on the view being sent again. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_558.mjs). // NOTE: onInit`.
    text1 = text1 && ` calls oModel.setSizeLimit(200) because the collection is 123 rows and the JSONModel caps a bound aggregation at 100. The port issues cs_event-set_size_limit for MAIN; without it the table stopped 23` &&
            ` rows short. // NOTE: A NavContainer's position is LIVE CONTROL STATE and does not survive a view rebuild. view_display( ) destroys the MAIN slot and XMLView.create builds a fresh control tree, so` &&
            ` navCon comes back on its FIRST page - the product list, this NavContainer declaring no initialPage - while selected_tab, save_visible and cancel_visible survive as class state. Four of the five` &&
            ` branches that call view_display( ) are reachable only FROM tabContainerPage (TAB_CANCEL, TAB_CLOSE, CLOSE_TAB_CLOSED, TAB_ADD_NEW), so pressing + on the tab bar created the tab, put the footer into` &&
            ` edit mode and dropped the user on the product list. Fixed 2026-08-27 with the app-000 re-issue idiom: a PROTECTED nav_page parks the target the LIVE navCon was last sent to (PROTECTED, not PUBLIC,` &&
            ` because it is bookkeeping and only PUBLIC attributes are serialized into the view model; not PRIVATE, because the draft serialization walks the attributes with a dynamic ASSIGN obj->(name) that`.
    text1 = text1 && ` cannot reach a PRIVATE one), and the end of view_display( ) re-issues it, guarded by ``nav_page IS NOT INITIAL AND nav_page <> ``table````. Re-issuing the LAST-ISSUED target rather than re-deriving` &&
            ` it is what keeps the branches that DO want the table correct: nav_to_table and the tab_close redirect when the last tab goes both park ``table`` and are skipped by the same guard. Unlike the sibling` &&
            ` ports this one needs no bookmark restore to reach the second view_display( ) IN A BROWSER - TAB_ADD_NEW is one press away from the tab page, and the interaction module clicks that + for real. An` &&
            ` earlier note here claimed firing addNewButtonPress drove no round trip at all in the headless harness; that was a HARNESS ARTEFACT and is withdrawn (corrected 2026-08-27). The frontend's eB DROPS any` &&
            ` event fired while a round trip is still in flight - the listener runs and fireEvent returns cleanly, and nothing goes out on the wire - so a press sent too early reads back as a dead control.` &&
            ` Measured both ways against the built backend: fired while busy, no POST and the TabContainer keeps its two items; fired a second later on an idle frontend, TAB_ADD_NEW goes out, t_tabs grows to three`.
    text1 = text1 && ` and navCon stays on tabContainerPage. The TabContainer's own add button IS rendered (class sapMTSAddNewTabBtn, tooltip "Add New Tab", in the control's TabStrip) and takes a plain click with no force.` &&
            ` Not to be confused with the product table's headerToolbar OverflowToolbarButton (sap-icon://add, tooltip "Add"), which is NEW_ITEM_ADD on a different page. The leg asserts all three halves: the third` &&
            ` tab, its key ProductId-1 (the backend counter, so it is TAB_ADD_NEW's own row), and the re-issued tabContainerPage - and with the guarded re-issue removed from view_display( ) only the last of the` &&
            ` three fails.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TabContainer`                    name = `TabContainerMHC`                               class = `z2ui5_cl_smpc_app_558` path = `src/01/01/z2ui5_cl_smpc_app_558.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableAlternateRowColors`                       class = `z2ui5_cl_smpc_app_210` path = `src/01/01/z2ui5_cl_smpc_app_210.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16` ) ).

    text1 = `POST-1.71: Table.autoPopinMode (since 1.76), Table.hiddenInPopin (since 1.77), Table.popinChanged (since 1.77) and Column.importance (since 1.76), the core of the auto-pop-in demo, are kept 1:1; needs` &&
            ` UI5 >= 1.77. // NOTE: onSelectionFinish (setHiddenInPopin(getSelectedKeys())) is reproduced as a BINDING, and the MECHANISM is equivalent while the MOMENT is not (corrected 2026-08-23 - this said` &&
            ` 1:1). The original wires selectionFinish only, which MultiComboBox fires from onAfterClose, so the columns re-flow once, when the picker closes. Binding selectedKeys and hiddenInPopin to the same` &&
            ` field re-flows them on every tick while the picker is still open, because _handleSelectionLiveChange writes selectedKeys per click and the two-way binding pushes it straight through.: hiddenInPopin` &&
            ` is a bindable property and the MultiComboBox's selectedKeys already IS that Priority array, so both bind the same field T_HIDDEN and the selectionFinish handler only pushes the model - no frontend` &&
            ` action is involved. The added selectedKeys binding has no counterpart in the original (the controller reads getSelectedKeys imperatively). onSliderMoved (byId(idProductsTable).setWidth(value + '%'))`.
    text1 = text1 && ` is reproduced 1:1 without a round-trip: the Slider value is two-way bound to width_pct and the Table gains a width expression binding width={= ${width_pct} + '%' }, so moving the Slider shrinks the` &&
            ` table live and drives the auto-pop-in (the added Table width attribute and the Slider value binding have no counterpart in the original view, where setWidth is imperative; the original Slider` &&
            ` liveChange handler is dropped). autoPopinMode + Column.importance stay declarative and 1:1; popinChanged still toasts. Corrected 2026-08-21: this entry used to describe a hand-built JSON array` &&
            ` forwarded through follow_up_action( cs_event-control_by_id, setHiddenInPopin ) - the mechanism the rework removed - which contradicted the deviation below it. // NOTE: the original derives the` &&
            ` ObjectNumber weight state in its frontend Formatter.js (weightState: KG conversion + Success/Warning/Error thresholds). That is business logic, so - abap2UI5 being a thin frontend - it is computed in` &&
            ` ABAP model_init into a WEIGHT_STATE field and bound state="{WEIGHT_STATE}", not via a frontend formatter (the original reaches its formatter the classic way - ``formatter: Formatter`` in the`.
    text1 = text1 && ` controller plus ``formatter: '.formatter.weightState'`` in the binding; corrected 2026-08-23, this used to say "core:require dropped" and the sample has no core:require at all). Visually 1:1 with the` &&
            ` original. // NOTE: onPopinChanged reproduced 1:1 since 2026-07-30: the popinChanged wire is a roundtrip-free client-composed toast 'Number of hidden pop-ins: {0}' filled by` &&
            ` ${$parameters>/hiddenInPopin}.length (the event parameter array's length, exactly the original's aHiddenInPopin.length) - the earlier static 'Pop-in layout changed' round-trip toast faked the value.` &&
            ` The hiddenInPopin event parameter is part of the POST_171-declared popinChanged event (since 1.77). // NOTE: the original's setHiddenInPopin(oMultiComboBox.getSelectedKeys()) is a binding here:` &&
            ` hiddenInPopin is a bindable property and the MultiComboBox's selectedKeys already IS that Priority array, so both bind the same field T_HIDDEN and the selectionFinish handler only pushes the model.` &&
            ` The JSON array the port used to build by hand for the frontend action is gone (prefer-a-bindable-property rule, linter rule settable-property-via-action).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableAutoPopin`                                class = `z2ui5_cl_smpc_app_092` path = `src/02/01/z2ui5_cl_smpc_app_092.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `Table.autoPopinMode (since 1.76), Table.hiddenInPopin (since 1.77), Table.popinChanged (since 1.77) and Column.importance (since 1.76), the core of the auto-pop-in demo, are kept 1:1; needs UI5 >=` &&
                 ` 1.77.` ) ).

    text1 = `NOTE: the sample keeps its data as a nested hierarchy (/ProductHierarchy/Suppliers/*/Categories/*/Products) and drills into it by REBINDING the table's items aggregation to a deeper path` &&
            ` (_setAggregation). abap2UI5 sends the rows it wants shown, so the hierarchy is flattened into one node table (level 1 Suppliers, 2 Categories, 3 Products, each row carrying its parents' names) and` &&
            ` the backend keeps the current path; rows_refresh emits exactly the branch the original would have bound. All 4 suppliers, 9 categories and 14 products of productHierarchy.json are seeded. // NOTE:` &&
            ` the breadcrumb is built in the controller (addLink per drill-down, destroy on the way back). Breadcrumbs.links is an aggregation, so the port binds it to a crumb table the backend maintains, with a` &&
            ` Link template carrying the target level - the same links appear and disappear, without the imperative add/destroy. That Link is the control extra against the archived Page.view.xml, where the` &&
            ` Breadcrumbs is empty and the controller fills it. // NOTE: _setAggregation also switches the table's mode (MultiSelect on the leaf level, SingleSelectMaster above it) and the visibility of the`.
    text1 = text1 && ` Dimensions and Weight columns. All three are bindable properties, so they become expressions over the current level instead of setter calls. // NOTE: the four frontend formatters are computed in the` &&
            ` ABAP backend (thin-frontend rule) and seeded into the node rows: Formatter.dimensions (join the non-empty Width/Depth/Height and append DimUnit) into DIMENSIONS, Formatter.round2DP into the` &&
            ` two-decimal PRICE string, Formatter.weightState (<0 None, <1000 Success, <2000 Warning, else Error) into WEIGHT_STATE, and Formatter.listItemType (a row with a ProductId is Inactive, otherwise` &&
            ` Navigation) into ROW_TYPE. Formatter.isProductSelected becomes the two-way bound SELECTED flag, which is why the row template's core:require for the Formatter module has no counterpart (attr missing` &&
            ` ColumnListItem.core:require). // NOTE: the Order model (products / count / hasCounts) becomes the SELECTED flag on the product nodes plus the derived ORDER_COUNT and HASCOUNTS fields: the info` &&
            ` toolbar's visible and its '<n> Products Selected' Label bind them, and so does the footer button's enabled - the original's isAnyProductSelected formatter. handleOrderPress toasts the selected`.
    text1 = text1 && ` product names, which the backend composes because it is the one holding the selection. // NOTE: not yet verified in a running system: the two drill-down steps, the breadcrumb links back up, and the` &&
            ` leaf-level multi-selection feeding the order count. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_566.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableBreadcrumb`                               class = `z2ui5_cl_smpc_app_566` path = `src/01/01/z2ui5_cl_smpc_app_566.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `NOTE: onCheckBoxSelect calls setFixedLayout(selected ? 'Strict' : true) on the first table. Table.fixedLayout takes a boolean OR the string 'Strict', and it is a bindable property, so the CheckBox` &&
            ` binds a flag two-way and the table binds the expression {= ${/STRICT_LAYOUT} ? 'Strict' : true } - which yields the same two values the controller sets. The CheckBox select attribute is therefore not` &&
            ` emitted (attr missing CheckBox.select). // NOTE: the sample runs on three named models (products, columns, clone). abap2UI5 serves one default model, so the three become three tables in it: the` &&
            ` products rows, the columns array of the second (wrong-usage) table, and the clone array of the first, whose first column width is 'auto' - the deepExtend copy the controller patches. Each table's` &&
            ` columns aggregation binds its own array, exactly as the original binds columns="{clone>/}" and columns="{columns>/}". // NOTE: the products model is created with setSizeLimit(3), so the original` &&
            ` renders only the first three rows of the mock /ProductCollection; the port seeds exactly those three. // NOTE: onReset (which would put the untouched oData back into the columns model) is wired to`.
    text1 = text1 && ` nothing in the view - the sample declares the handler and never calls it - so it has no counterpart here either. // NOTE: not yet verified in a running system: the bound columns aggregation on both` &&
            ` tables and the Strict-layout switch. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_567.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableColumnWidth`                              class = `z2ui5_cl_smpc_app_567` path = `src/01/01/z2ui5_cl_smpc_app_567.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `NOTE: the whole controller is one responsive rule: onBeforeRendering + _orientationHandler + _showMessageStrip hide the MessageStrip on a PHONE in portrait and show it again in landscape (and onExit` &&
            ` detaches the handler). The port replaces all four methods with one expression binding on the shared device model - visible = !${device>/system/phone} || ${device>/orientation/landscape} - which UI5` &&
            ` keeps current on every rotation. No round-trip, no controller; the added ``visible`` attribute is the only difference to the archived Table.view.xml. // NOTE: contextualWidth='Auto' +` &&
            ` popinLayout='GridSmall' + the per-column minScreenWidth/demandPopin are what the sample demonstrates and are passed through 1:1 on both tables, including the original's mixed casing ('phone'/'tablet'` &&
            ` on two columns, 'Phone' on the third). The ResponsiveSplitter/PaneContainer/SplitPane nesting with requiredParentWidth 500 and 400 is 1:1 as well. // NOTE: both tables bind the shared demo mock` &&
            ` products.json /ProductCollection (all 123 rows; Name, SupplierName, Status and Quantity - the four fields the ColumnListItem binds) from ONE inlined table in model_init; the original loads the same`.
    text1 = text1 && ` file once into the view's model and binds it twice, so the two panes stay in sync exactly as before. // NOTE: live-verified on 2026-08-02 (maintainer live check): the splitter re-flow through` &&
            ` contextualWidth works. NOT covered by that check: the phone-portrait branch of the MessageStrip, which needs a real device rotation - kept as the one open leg. // NOTE: still unverified: the` &&
            ` phone-portrait branch of the MessageStrip expression (!${device>/system/phone} || ${device>/orientation/landscape}) - it needs a real device rotation, which neither the desktop live check nor the` &&
            ` headless harness performs. The 2026-08-04 close stamp that stood here is WITHDRAWN (2026-08-23) and the entry is a LIVE_TEST again: close-live-tests appends that stamp on the rule that the module` &&
            ` drives exactly the wire the deviation names, and this module does the opposite - it asserts the strip is VISIBLE on a desktop viewport, with no rotation, no Device override and no viewport change,` &&
            ` while e2e-smoke uses a plain desktop context where Device.system.phone is always false. So the phone-portrait branch is still open, exactly as the neighbouring deviation says, and it stays open until`.
    text1 = text1 && ` a module rotates or emulates a phone. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_277.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableContextualWidthDynamic`                   class = `z2ui5_cl_smpc_app_277` path = `src/01/01/z2ui5_cl_smpc_app_277.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.16`
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableContextualWidthStatic`                    class = `z2ui5_cl_smpc_app_568` path = `src/01/01/z2ui5_cl_smpc_app_568.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = `NOTE: onPress calls oTable.setContextualWidth('100px'). Table.contextualWidth is a bindable property, so - per the prefer-a-bindable-property rule - it is bound to a field seeded with the view's own` &&
                 ` initial value ('500px') and the button writes '100px' into it. The rendered result is identical and no frontend action is needed. // NOTE: the controller's oData array is seeded verbatim: all 22` &&
                 ` people with their firstName / lastName / birthDate / gender. birthDate is carried as the ISO string the sample stores, which is what its Label binding renders. // NOTE: not yet verified in a running` &&
                 ` system: the contextualWidth switch and the popin layout it drives. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_568.mjs).` ) ).

    text1 = `NOTE: the sample is one container view plus two nearly identical sub-views (mvc:XMLView availableProducts / selectedProducts) with a controller each. abap2UI5 serves one view, so both tables are built` &&
            ` here by one parameterised method - which is why the reconstructed view shows ONE of each control where the archived originals have two (the linter emits a helper's chain once, however often it is` &&
            ` called): Table, Menu, OverflowToolbar, Title, ColumnListItem all count 1 vs 2, Column 3 vs 6 and Text 6 vs 12. The two mvc:XMLView wrappers have no counterpart at all. // NOTE: both tables bind the` &&
            ` SAME collection and differ only in their filter, exactly as the original does: Rank EQ 0 for Available, Rank GT 0 plus a descending Rank sorter for Selected. The whole Utils.ranking arithmetic is` &&
            ` kept - Initial 0, Default 1024, Before(r) = r + 1024, After(r) = r / 2, Between(a,b) = (a + b) / 2 - only in ABAP, and on an integer rank (the sample's own comment caps the scheme at 53 rows, which` &&
            ` integer halving matches). // NOTE: the sample reads the selection with getSelectedItems() and toasts 'Please select a row!' when there is none; the row flag is bound two-way here and the backend does`.
    text1 = text1 && ` the same check. moveSelectedItem's rank swap between a row and its neighbour is done on the same descending order the Selected table renders. // NOTE: the drag and drop configuration is kept 1:1 (the` &&
            ` two groupNames, sourceAggregation/targetAggregation, dropPosition Between, and the DragDropInfo that lets the Selected table re-order itself). The drop handlers need the dragged row, the row it was` &&
            ` dropped on and the drop position; all three travel with the event as UI5 expressions over draggedControl / droppedControl / dropPosition, and the backend then computes the new rank the way` &&
            ` onDropSelectedProductsTable does. // 1.71: the plugins:ContextMenuSetting scope="Selection" plugin (sap.m.plugins, since UI5 1.121) is dropped from both tables together with the beforeOpenContextMenu` &&
            ` handler that pairs with it: the plugin class does not exist on a 1.71 runtime, so keeping it would crash view creation there. Its only effect is that right-clicking a row also selects it before the` &&
            ` context menu opens; the context menus themselves are kept 1:1 and act on the selected row. // NOTE: the full mock /ProductCollection is seeded with the three fields the tables bind (Name, Category,`.
    text1 = text1 && ` Quantity) and the Rank the controller adds to every row (Initial = 0), so every product starts in the Available table. // NOTE: not yet verified in a running system: the drag and drop across the two` &&
            ` tables, the re-ordering inside the Selected one, and the four move buttons. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_569.mjs). // NOTE: The drop guard` &&
            ` tests the binding CONTEXT, not the method: every sap.ui.core.Element has getBindingContext, so ``X.getBindingContext ? ...`` was always truthy. When the drop target is the table rather than a row -` &&
            ` which is what happens on the first drag, since the Selected table starts empty - getBindingContext() is undefined and the chained getProperty threw inside the handler, so nothing moved.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableDnD`                                      class = `z2ui5_cl_smpc_app_569` path = `src/01/01/z2ui5_cl_smpc_app_569.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `IMPROVISED: PRICE is ``p LENGTH 9 DECIMALS 2`` (the read-only template needs it packed for the original's sap.ui.model.type.Currency composite binding), but a PACKED table cell cannot take a two-way` &&
            ` write-back: a cell whose text does not convert is swallowed by delta_apply_field's ``CATCH cx_root ##NO_HANDLER`` ("skip just this cell", z2ui5_cl_ui5_srv_model), so the backend keeps the old number` &&
            ` while the browser goes on showing what the user typed. MEASURED 2026-08-26 against the transpiled backend, editing row 1 (Notebook Basic 15, PRICE 956) and pressing Save: ``1250.00`` -> 1250 (the` &&
            ` write-back itself works), but ``1,250.00``, ``1 250``, ``12.50 EUR``, ``1250,00`` and ``abc`` ALL left the model on its previous value with no error, no toast and no valueState, and ``-``, an emptied` &&
            ` cell and a blanks-only cell each silently became 0.00. Remedy: the editable cell now binds a PRICE_TEXT string mirror (a string cell always arrives), EDIT seeds it from the packed price, and SAVE` &&
            ` parses it back under a digit/character/length guard plus TRY ... CATCH cx_root. This is a deliberate BEHAVIOUR difference from the original, which has no packed field and lets its JSONModel keep any`.
    text1 = text1 && ` string: a row whose text does not convert keeps its old price, has its text put back from that price, and the app STAYS in edit mode with a MessageToast naming the row and the rejected entry - the` &&
            ` entry is reported, never discarded behind the user's back. A converting entry saves and leaves edit mode exactly as before. Same defect class and same string-mirror idiom as app 351's MINSIZE_TEXT.` &&
            ` // NOTE: onEdit / onSave / onCancel rebind the SAME table between a read-only ColumnListItem template (the one the view declares) and an editable one the controller builds in JavaScript. abap2UI5` &&
            ` rebuilds the view on every round trip, so the two templates are the two branches of one IF in view_display, driven by an EDIT_MODE flag - the rendered table carries exactly one of them at a time. The` &&
            ` four Inputs of the editable template are the control extra against the archived Table.view.xml, which only holds the read-only one; they are also why the linter's static reconstruction shows eight` &&
            ` cells in a four-column row - it does not evaluate the IF, and only one branch is ever emitted at runtime. // NOTE: the three toolbar buttons swap visibility through setVisible in the original;`.
    text1 = text1 && ` visible is a bindable property, so Edit binds the negated EDIT_MODE expression and Save/Cancel bind the flag itself - no setter and no extra state. onCancel's deepExtend copy of /ProductCollection is` &&
            ` a plain table copy taken on Edit and put back on Cancel. Corrected 2026-08-26: onSave is no longer a bare ``edit_mode = abap_false`` - it parses the PRICE_TEXT mirror back into the packed PRICE first` &&
            ` and only leaves edit mode when every row converted. // NOTE: the original derives the ObjectNumber weight state in its frontend Formatter.js (weightState: <0 None, <1000 Success, <2000 Warning, else` &&
            ` Error, over the RAW WeightMeasure). That is business logic, so - abap2UI5 being a thin frontend - it is computed in ABAP model_init into a WEIGHT_STATE field and bound state="{WEIGHT_STATE}". //` &&
            ` NOTE: onPaste and onOrder both only show a MessageToast, composed on the client, so both stay there as roundtrip-free control_global MESSAGE_TOAST wires - onPaste with the {0} placeholder for the` &&
            ` pasted data, onOrder with its static text. rebindTable's sKeyboardMode argument is never used by the original (it is accepted and dropped), so it has no counterpart. // NOTE: the full mock`.
    text1 = text1 && ` /ProductCollection is seeded with the fields the two templates bind (ProductId, Name, Quantity, UoM, WeightMeasure, WeightUnit, Price, CurrencyCode); the price column keeps the original` &&
            ` sap.ui.model.type.Currency composite binding and the quantity column its sap.ui.model.type.String one. Corrected 2026-08-26: the EDITABLE template's price cell no longer binds {PRICE} - it binds the` &&
            ` {PRICE_TEXT} string mirror (see the IMPROVISED deviation); only the READ-ONLY template still binds PRICE, which is what carries the Currency composite binding. // NOTE: not yet verified in a running` &&
            ` system: the Edit/Save/Cancel template swap and the Cancel rollback. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_570.mjs). Extended 2026-08-26: the` &&
            ` interaction now also drives the string mirror end to end - a ``1,250.00`` typed into the Price cell must leave the app IN edit mode with a "Not a number" toast and the cell restored from the packed` &&
            ` price, and a following ``1250.00`` must save and reach PRICE 1250. Both legs FAIL against the pre-fix packed binding (measured), so they cannot pass by accident. **Needs a backend rebuild (``npm run`.
    text1 = text1 && ` e2e:build``) before the nightly can confirm them.**`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableEditable`                                 class = `z2ui5_cl_smpc_app_570` path = `src/01/01/z2ui5_cl_smpc_app_570.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `POST-1.71: six post-1.71 members are kept for the 1:1 port, all of them what the sample is about: the column header menu itself - sap.m.table.columnmenu.Menu, QuickSort, QuickSortItem, ActionItem and` &&
            ` QuickAction (all since UI5 1.110) - and the sap.m.Column headerMenu association (since UI5 1.98). App 298 carries the same stack. // IMPROVISED: the sample DEFINES a control class in JavaScript` &&
            ` (CustomMenuAdapter, a sap.m.table.columnmenu.MenuBase subclass that wraps a plain sap.m.Menu and implements IColumnHeaderMenu) and hangs it on three of the five columns. A backend cannot define a` &&
            ` control class - the same wall app 552 hits with its two custom calendar views - so those three menus are expressed with the framework's OWN column menu instead: the Supplier column gets a Menu with` &&
            ` the Toggle Grouping ActionItem, and the Price and Dimensions columns get a Menu whose QuickAction holds the sample's two sort Buttons / three align Buttons, each with the icon and text the original` &&
            ` MenuItems carry. Same entries, same actions, no custom class. (A second reason forces the QuickAction shape for those two: the linter's metadata snapshot records Menu.items without multiple, so it`.
    text1 = text1 && ` treats the aggregation as single - the snapshot follows the UI5 source, which omits the flag and relies on ManagedObject's default of true. QuickAction.content does carry multiple, so it takes the` &&
            ` two and three buttons.) // NOTE: the Product column keeps the built-in QuickSort 1:1. Its change event declares key and sortOrder and fires NEITHER - onChange does fireChange({item: oItem}) and` &&
            ` nothing else - so the port reads the item the control really passes and asks it for its sort order, exactly as app 298 does and as the sample's own handler does. That is what the advisory` &&
            ` unknown-event-parameter finding on this port is. // NOTE: the four setter calls the menus make are all bindable properties, so they become bound fields instead: setSortIndicator on the Product and` &&
            ` Price columns, and setHAlign on the Dimensions column. Sorting itself is done where the data is - the ABAP table is re-ordered (app 298 idiom) instead of sorting the items binding. // NOTE: Toggle` &&
            ` Grouping calls oBinding.sort with a Sorter carrying a group function. The declarative equivalent is the items binding with sorter: { path: 'SUPPLIERNAME', group: true }, so the port switches the`.
    text1 = text1 && ` binding string between the plain and the grouping form - which is what the toggle does. // NOTE: the full mock /ProductCollection is seeded with the fields the view binds (ProductId, Name,` &&
            ` SupplierName, Width, Depth, Height, DimUnit, WeightMeasure, WeightUnit, Price, CurrencyCode); the price column keeps the original sap.ui.model.type.Currency composite binding 1:1. // NOTE: not yet` &&
            ` verified in a running system: the four column header menus, the grouping toggle and the two sort indicators. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_571.mjs). // NOTE: **e2e-caught 2026-08-22**: the sort ran but the table came back in its original order. The cause is the transpiled backend: ``SORT <itab> BY` &&
            ` (field)`` - the DYNAMIC component form - loses its BY clause entirely (the emitted JS is ``abap.statements.sort(t, {})``), so every sort was a no-op. The component is named statically per field now,` &&
            ` in a CASE. Apps 298, 362 and 571 all carried the dynamic form and all three are fixed.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableIColumnHeaderMenu`                        class = `z2ui5_cl_smpc_app_571` path = `src/02/01/z2ui5_cl_smpc_app_571.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `six post-1.71 members are kept for the 1:1 port, all of them what the sample is about: the column header menu itself - sap.m.table.columnmenu.Menu, QuickSort, QuickSortItem, ActionItem and QuickAction` &&
                 ` (all since UI5 1.110) - and the sap.m.Column headerMenu association (since UI5 1.98). App 298 carries the same stack.` ) ).

    text1 = `NOTE: onCheckBoxSelect walks up from the CheckBox to its table and calls setFixedLayout(selected). Table.fixedLayout is a bindable property, so - per the prefer-a-bindable-property rule - each table` &&
            ` binds a flag that its own CheckBox also binds two-way; the CheckBox select attribute is therefore not emitted (attr missing CheckBox.select) and the two tables keep separate flags, exactly as the` &&
            ` original's getParent().getParent() walk gives each CheckBox its own table. // NOTE: Dialog.fragment.xml, which onOpenPressed loads and opens, is rebuilt as a core:FragmentDefinition shown with` &&
            ` popup_display; onClosePressed becomes popup_destroy. syncStyleClass('sapUiSizeCompact', view, dialog) has no counterpart - it copies the demo kit frame's own density class onto the dialog, which is` &&
            ` host chrome rather than sample behaviour. // NOTE: both tables keep their items binding with the Name sorter 1:1 (raw binding-info string over the bound table), so the rows are seeded in the mock's` &&
            ` own order and the frontend sorts them. The full mock /ProductCollection is seeded with the four fields the two views bind (ProductId, Name, SupplierName, Description). // NOTE: not yet verified in a`.
    text1 = text1 && ` running system: the fixedLayout toggle on each of the two tables and the dialog round trip. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_572.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableLayout`                                   class = `z2ui5_cl_smpc_app_572` path = `src/01/01/z2ui5_cl_smpc_app_572.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `NOTE: the two frontend formatters are computed in the ABAP backend (thin-frontend rule): Formatter.weightState (<0 None, <10 Success, <20 Warning, else Error - this sample compares the RAW` &&
            ` WeightMeasure, with thresholds of its own) becomes the WEIGHT_STATE field bound to ObjectNumber.state, and Formatter.formatType (Price > 400 -> Navigation, else Inactive) becomes ROW_TYPE bound to` &&
            ` ColumnListItem.type. // NOTE: the items binding keeps its sorter on SupplierName 1:1 (raw binding-info string over the bound table) - which is what makes the mergeDuplicates columns merge: the rows` &&
            ` are seeded in the mock's own order and the frontend sorts them, so identical suppliers end up adjacent. mergeFunctionName="getNumber" on the Weight column is kept as the literal it is. // NOTE: the` &&
            ` full mock /ProductCollection is seeded with the fields this view binds (ProductId, Name, SupplierName, Width, Depth, Height, DimUnit, WeightMeasure, WeightUnit, Price, CurrencyCode); the price column` &&
            ` keeps the original sap.ui.model.type.Currency composite binding 1:1. // NOTE: not yet verified in a running system: the cell merging across the three mergeDuplicates columns. **e2e-verified`.
    text1 = text1 && ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_573.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableMergeCells`                               class = `z2ui5_cl_smpc_app_573` path = `src/01/01/z2ui5_cl_smpc_app_573.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `POST-1.71: six post-1.71 members are kept for the 1:1 port, all of them what this sample demonstrates: sap.m.Table.itemActionCount, Table.itemActionPress, the ListItemBase actions aggregation and the` &&
            ` sap.m.ListItemAction control (all since UI5 1.137), sap.m.table.Title (since 1.147) and Table.multiSelectMode (since 1.93). // NOTE: the sample's onSelectionChange calls` &&
            ` oTable.setMultiSelectionMode(sMode) - a setter sap.m.Table does not have. The property is multiSelectMode (sap.m.MultiSelectMode, since 1.93), and the ComboBox's own keys (Default / ClearAll) are its` &&
            ` values, so the port binds the ComboBox's selectedKey and the table's multiSelectMode to one field: the ComboBox change needs no handler and the mode actually switches, which in the original it does` &&
            ` not. // NOTE: the ui> view model (totalCount, selectedCount, extViewSwitchEnabled) becomes four backend fields. _updateTotalCount reads oBinding.getCount() and _updateSelectedCount` &&
            ` getSelectedItems().length; the backend holds the rows and the selection flags, so it counts them directly, and the Show-totalCount switch sets the count to -1 exactly as onToggleTotalCount does.`.
    text1 = text1 && ` onToggleExtView's setShowExtendedView and the extended-view Switch share one bound flag, so that handler is not needed either; the totalCount switch keeps its change wire because it drives the` &&
            ` recount. // NOTE: onSearch filters Name, SupplierName and ProductId with an OR filter on the items binding. The port filters in the backend into a second table (T_ROWS) and keeps the full set in` &&
            ` T_PRODUCTS, so the search can widen again; Contains on a client model is case-insensitive, which the ABAP comparison matches by upper-casing both sides. // NOTE: onAddRow builds a product from four` &&
            ` Math.random() draws (id, supplier, three dimensions, price, currency). A client-side random draw is not reproducible from a backend (the corpus rule for now/random values), so the port counts up` &&
            ` instead: PRD-1, Product 1, the three suppliers in turn and dimensions derived from the counter. The row is appended and toasted exactly as the original does. The sample's own new row carries Price` &&
            ` and CurrencyCode fields that no column of this table binds, so they have no counterpart. // NOTE: the items binding keeps its sorter on Name 1:1; its events: { change: '.onBindingChange' } hook has`.
    text1 = text1 && ` no counterpart - it exists to recount after a binding change, which the backend does whenever it changes the rows. // NOTE: the sample writes itemActionPress="onItemActionPress" without the leading` &&
            ` dot, which is not a controller-method reference in a modern XML view; the port wires the event properly, so the delete action works here. // NOTE: not yet verified in a running system: the row` &&
            ` actions, the multi-select mode switch, the extended title view and the search round trip. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_574.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableMultiSelectMode`                          class = `z2ui5_cl_smpc_app_574` path = `src/02/01/z2ui5_cl_smpc_app_574.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `six post-1.71 members are kept for the 1:1 port, all of them what this sample demonstrates: sap.m.Table.itemActionCount, Table.itemActionPress, the ListItemBase actions aggregation and the` &&
                 ` sap.m.ListItemAction control (all since UI5 1.137), sap.m.table.Title (since 1.147) and Table.multiSelectMode (since 1.93).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableNavigated`                                class = `z2ui5_cl_smpc_app_488` path = `src/02/01/z2ui5_cl_smpc_app_488.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.ColumnListItem.navigated is @since 1.72 and is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.72. // NOTE: The navigated flag comes from the` &&
                 ` .isNavigated formatter comparing the settings model's navigatedItem with each row's ProductId. A formatter is business logic, so the port binds a plain navigated field per row and the press wire` &&
                 ` (carrying ${PRODUCTID}) sets it in ABAP - exactly one row navigated at a time, like the original (app 482 precedent). // NOTE: The press wire setting the navigated row is unverified in a running` &&
                 ` system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_488.mjs).`
        post171 = `sap.m.ColumnListItem.navigated is @since 1.72 and is the subject of this sample, so it is kept 1:1 and the port needs a UI5 runtime >= 1.72.` ) ).

    text1 = `NOTE: onInit creates the sap.m.sample.Table COMPONENT, hides its header toolbar and inserts its Table into the empty l:VerticalLayout of this view. abap2UI5 has no component reuse, so the same table` &&
            ` is declared inline in that layout - without the header toolbar the original hides anyway. structural-diff therefore reports the whole table as control extra (Table, 5 Columns, ColumnListItem,` &&
            ` ObjectIdentifier, 2 ObjectNumbers and the column header Texts): the original's view carries only the empty layout. // NOTE: onChange switches the table's showOverlay on (the 'outdated' state the` &&
            ` sample is named after), onSearch filters by the selected supplier and takes it off, onReset clears filter, overlay and selection. The overlay is a bound property here and the filter is applied on the` &&
            ` bound table in ABAP (thin frontend - the press is a round-trip anyway); the ComboBox gets a bound selectedKey the original does not have, because the port needs the value on the server. The original` &&
            ` wires BOTH change and selectionChange on the ComboBox to the same handler; the port keeps change only, so structural-diff reports attr missing ComboBox.selectionChange - a selection is a change here`.
    text1 = text1 && ` too. // NOTE: weightState is business logic (a KG conversion plus Success/Warning/Error thresholds), so the port computes it in model_init rather than in a frontend formatter. TableOutdated reuses` &&
            ` the sap.m.sample.Table COMPONENT, so the rule is that sample's Formatter.js - thresholds 1 KG and 5 KG with G converted - and NOT the 1000/2000 raw thresholds the TableSelectDialog family's own` &&
            ` Formatter.js uses. The two families look interchangeable and are not: seeding all-Success is correct under the 1000/2000 rule and wrong under this one for 66 of the 123 rows. // NOTE: The three` &&
            ` toolbar wires (change -> overlay, Filter, Reset) are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_505.mjs). // NOTE: The` &&
            ` reused table also carries an infoToolbar (an OverflowToolbar with the Label "Wide range of available products") which the original hides nothing of - the controller hides only getHeaderToolbar( ).` &&
            ` The port's inlined table has no infoToolbar, and its Column/dependents/p:ColumnAIAction with its onAIActionPress toast is dropped too (a sap.m.plugins control, out of scope for the 1.71 target).`.
    text1 = text1 && ` structural-diff cannot see either: the original TableOutdated view carries only the empty l:VerticalLayout, so every control the port builds reads as ``control extra``.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableOutdated`                                 class = `z2ui5_cl_smpc_app_505` path = `src/01/01/z2ui5_cl_smpc_app_505.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `POST-1.71: four post-1.71 members are kept for the 1:1 port: sap.f.FlexibleColumnLayout.autoFocus (since UI5 1.76), restoreFocusOnBackNavigation (since 1.77) and the columnResize event (since 1.76),` &&
            ` all three declared by the sample's own FCL view, and the sap.m.Avatar control (since 1.73) that its detail page uses twice. // NOTE: the sample is a Component with a ROUTER and three views` &&
            ` (FlexibleColumnLayout, Master, Detail). abap2UI5 serves one view, so the three become one: the FCL declares its begin and mid column pages inline, and the routing collapses to the FCL's own layout` &&
            ` property - onListItemPress opens the mid column (TwoColumnsMidExpanded), handleFullScreen / handleExitFullScreen / handleClose set MidColumnFullScreen / TwoColumnsMidExpanded / OneColumn. That is` &&
            ` what the router's navTo(layout) does. Since 2026-08-31 the URL it writes IS reproduced too, via the framework's app-owned hash routing (cs_event-hash_attach_changed + hash_set, needs abap2UI5 newer` &&
            ` than 1.144.0 - abap2UI5#2693, the app-012/584 recipe): every routed handler composes the original's pattern from route + the product INDEX into the mock collection ('#/detail/{product}/{layout}',`.
    text1 = text1 && ` '#/{layout}' for the ':layout:' master route) and pushes it, a hash change the app did not write (browser Back/Forward, a manual edit) round-trips as HASH_CHANGED and is parsed back onto the RUNNING` &&
            ` instance (the search text survives, like with the original's client-side router), and a deep link / reload re-derives the columns in view_display( ) (hash_apply is idempotent). This is the corpus's` &&
            ` first sap.f.FlexibleColumnLayout port. // NOTE: the three archived views declare different default namespaces - FlexibleColumnLayout.view.xml has xmlns="sap.f", Master.view.xml xmlns="sap.m" with f:` &&
            ` for sap.f, and Detail.view.xml xmlns="sap.uxap" with m: for sap.m. One abap2UI5 view can only have one default namespace (sap.m here), so every control of the detail page carries a different PREFIX` &&
            ` than in its original file even though it is the same control: ObjectPageLayout, ObjectPageDynamicHeaderTitle, ObjectPageSection and ObjectPageSubSection become uxap:-prefixed; FlexBox, VBox, Label,` &&
            ` Text, Title, Avatar, ObjectNumber, OverflowToolbarButton and form:SimpleForm lose their m: prefix; and the FlexibleColumnLayout gains an f: one. The whole missing/extra pairing of this port's`.
    text1 = text1 && ` structural diff is that shift - no control is actually added or dropped. // NOTE: the three navigation-action buttons are shown by the sample through the FCL helper's actionButtonsInfo (visible="{=` &&
            ` ${/actionButtonsInfo/midColumn/fullScreen} !== null }" and so on). The helper is a JavaScript utility, so the port derives the same three visibilities from the layout itself: full-screen while the` &&
            ` mid column is not full screen, exit-full-screen while it is, and close while any mid column is open. // NOTE: Detail.controller binds the mid column with bindElement('/ProductCollection/<n>'), so all` &&
            ` its bindings are relative. The port folds them to root-seeded D_* fields (app 229 idiom): the pressed row's ProductId travels with the press and the backend copies that product's fields over. The` &&
            ` detail Price is pre-composed as '<CurrencyCode> <Price>', which is the composite text the original's ObjectNumber binds. // IMPROVISED: the sample exists to show sap.m.Table.scrollToIndex, and the` &&
            ` port now makes the call. This note used to say scrollToIndex "is not on the framework's control-method whitelist", which was wrong - CONTROL_METHODS carries scrollToIndex: ["int"] - and it left the`.
    text1 = text1 && ` sample's whole subject unported: the class had no scrollToIndex, no columnResize wire and no follow-up action at all, and the table simply kept whatever scroll position it had. The FCL's columnResize` &&
            ` (the original's onColumnResize) is wired 1:1 and carries the same ${$parameters>/beginColumn} flag the original guards on; onListItemPress stores the pressed row's index and the columnResize` &&
            ` round-trip issues follow_up_action( control_by_id, productsTable, scrollToIndex, <index> ). Two things are IMPROVISED rather than copied. (a) The original reads the index off the control -` &&
            ` oItem.getParent( ).indexOfItem( oItem ) - which no backend can do; a thin frontend computes it instead, and because the items binding sorts on NAME the port sorts its own row table the same way and` &&
            ` takes the position in that order. (b) The original's second guard is a DOM test, oTable.$( ).is( ':visible' ); the begin column is hidden exactly while the layout is MidColumnFullScreen, so the` &&
            ` backend derives the same answer from LAYOUT. The columnResize event fires once per column whose width changed (FlexibleColumnLayout._cacheColumnWidth over COLUMN_ORDER begin/mid/end) and also on the`.
    text1 = text1 && ` first render and on a window resize, so the port pays a round trip for firings that do nothing - the handler returns immediately until a row has been pressed, exactly as the original's returns until` &&
            ` this.iIndex is set. The begin column is cached FIRST, which is also the one firing the client's busy guard keeps when several arrive in the same tick. // NOTE: the table's` &&
            ` sticky="ColumnHeaders,HeaderToolbar" is a literal in the sample, so the port keeps it as one - no bound string table is needed here (app 235 binds it because its check boxes change it). // NOTE:` &&
            ` onSearch filters the table's items on Name; the port filters in the backend into a second table so the search can widen again. Contains on a client model is case-insensitive, which the ABAP` &&
            ` comparison matches by upper-casing both sides. The master title's count comes from ProductCollectionStats/Counts/Total (123), seeded as a field. // NOTE: the two Avatar src values are` &&
            ` '../../../../../../../{products>ProductPicUrl}' in the sample - a relative path out of the demo kit's own frame. The port binds the mock's ProductPicUrl as it stands, without the traversal prefix,`.
    text1 = text1 && ` which is what the corpus does everywhere for these images. // NOTE: not yet verified in a running system: the FlexibleColumnLayout column changes, the master/detail fold and the ObjectPage header.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_575.mjs). The columnResize -> scrollToIndex wire added 2026-08-26 is NOT covered by that module and needs a` &&
            ` fresh e2e build before it can be verified. // NOTE: two attributes of the archived views have no counterpart. mvc:View displayBlock="true" is a Component-app setting (it makes the view fill the page` &&
            ` when it is the root of a Component) and abap2UI5 owns the view container itself. FlexibleColumnLayout stateChange IS wired since 2026-08-31 (a second pass the same day closed what the first declared` &&
            ` open): onStateChanged fires as the STATE_CHANGED event with isNavigationArrow and the layout as arguments, and only an ARROW change rewrites the URL - via client->hash_replace, the framework's` &&
            ` HashChanger#replaceHash twin of hash_set, so the URL follows with NO new history entry, exactly the original's replace-navTo. Every layout change costs a round trip the client-side original does not`.
    text1 = text1 && ` pay (the columnResize precedent of app 575); non-arrow firings return without touching anything. columnResize is wired too (see the scrollToIndex deviation).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableScrollToIndex`                            class = `z2ui5_cl_smpc_app_575` path = `src/02/01/z2ui5_cl_smpc_app_575.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `four post-1.71 members are kept for the 1:1 port: sap.f.FlexibleColumnLayout.autoFocus (since UI5 1.76), restoreFocusOnBackNavigation (since 1.77) and the columnResize event (since 1.76), all three` &&
                 ` declared by the sample's own FCL view, and the sap.m.Avatar control (since 1.73) that its detail page uses twice.` ) ).

    text1 = `POST-1.71: sap.m.plugins.CellSelector is @since 1.119 and sap.m.plugins.CopyProvider is @since 1.110 (its visible property @since 1.114) - all newer than the 1.71 floor. The CellSelector is kept, so` &&
            ` the port is filed under src/02. // IMPROVISED: The CopyProvider is DROPPED. Its extractData is a JavaScript callback that reads each column's app:bindings CustomData and formats it with app:template,` &&
            ` and the control REFUSES to be created without one ('extractData property must be defined for Element sap.m.plugins.CopyProvider') - so it is not a matter of copying in a default format, the plugin` &&
            ` takes the whole view down. abap2UI5 cannot register such a callback, so the dependents aggregation keeps only the CellSelector and the cell copy is lost (structural-diff reports the CopyProvider` &&
            ` element and its extractData as missing). The app: CustomData attributes stay on the four columns 1:1, so nothing about the sample's data description is lost. // NOTE: onVisibleChange /` &&
            ` onEnabledChange / onSparseChange call three setters on the CopyProvider. The three CheckBoxes are two-way bound to three flags here instead, so the three CheckBox.select attributes are dropped; with`.
    text1 = text1 && ` the plugin gone the flags drive nothing, but the toolbar the sample shows is unchanged. The copy BUTTON the controller takes from the plugin and adds to the toolbar goes with it. // NOTE: The` &&
            ` CellSelector and the three CheckBoxes are unverified in a running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_523.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableSelectCopy`                               class = `z2ui5_cl_smpc_app_523` path = `src/02/01/z2ui5_cl_smpc_app_523.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.plugins.CellSelector is @since 1.119 and sap.m.plugins.CopyProvider is @since 1.110 (its visible property @since 1.114) - all newer than the 1.71 floor. The CellSelector is kept, so the port is` &&
                 ` filed under src/02.` ) ).

    text1 = `POST-1.71: sap.m.Table autoPopinMode (since UI5 1.76) kept 1:1 from the original; needs a UI5 release >= 1.76 to render. // POST-1.71: sap.m.plugins.ColumnResizer (since UI5 1.91) kept 1:1 in the` &&
            ` Table dependents aggregation; needs a UI5 release >= 1.91 to render. The Table fixedLayout='Strict' value is also newer than 1.71 (a value-level extension of the 1.22 property, invisible to the` &&
            ` property gate; the source JSDoc carries no value-level @since) - covered by the same >= 1.91 floor. // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real` &&
            ` browser): the sorter inside the raw binding-info string really sorts: the first rendered row is '10" Portable DVD player', not the model's first record ('Notebook Basic 15'). Residual (nothing` &&
            ` headless can assert deterministically): the Currency type's locale formatting of Price/CurrencyCode.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableStrictLayout`                             class = `z2ui5_cl_smpc_app_225` path = `src/02/01/z2ui5_cl_smpc_app_225.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Table autoPopinMode (since UI5 1.76) kept 1:1 from the original; needs a UI5 release >= 1.76 to render. // sap.m.plugins.ColumnResizer (since UI5 1.91) kept 1:1 in the Table dependents` &&
                 ` aggregation; needs a UI5 release >= 1.91 to render. The Table fixedLayout='Strict' value is also newer than 1.71 (a value-level extension of the 1.22 property, invisible to the property gate; the` &&
                 ` source JSDoc carries no value-level @since) - covered by the same >= 1.91 floor.` ) ).

    text1 = `NOTE: the sample writes type="{Text}" and fieldWidth="{60%}" on the Quantity Input. Both are PATH bindings, not literals: no such property exists in its model, so both resolve to undefined and the` &&
            ` Input renders with its defaults (type Text, fieldWidth 50%). Carrying them over would mean binding to a field the rows do not have - which the property gate rejects, and rightly so - and would change` &&
            ` nothing on screen, so the two attributes are dropped (attr missing Input.type, Input.fieldWidth). // NOTE: the original derives the ObjectNumber weight state in its frontend Formatter.js` &&
            ` (weightState: <0 None, <1000 Success, <2000 Warning, else Error - note this sample compares the RAW WeightMeasure, without the KG conversion its siblings do). That is business logic, so - abap2UI5` &&
            ` being a thin frontend - it is computed in ABAP model_init into a WEIGHT_STATE field and bound state="{WEIGHT_STATE}". // NOTE: the items binding keeps its sorter on Name 1:1 (raw binding-info string` &&
            ` over the bound table), so the rows are seeded in the mock's own order and the frontend sorts them. The full mock /ProductCollection is seeded with the fields this view binds (ProductId, Name,`.
    text1 = text1 && ` Quantity, UoM, WeightMeasure, WeightUnit, Price, CurrencyCode); the price column keeps the original sap.ui.model.type.Currency composite binding. // NOTE: not yet verified in a running system: the` &&
            ` vAlign="Middle" row alignment with the Input in a cell. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_576.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableVerticalAlignment`                        class = `z2ui5_cl_smpc_app_576` path = `src/01/01/z2ui5_cl_smpc_app_576.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `POST-1.71: The whole column-header menu is newer than the 1.71 floor: sap.m.table.columnmenu.Menu, QuickSort, QuickSortItem and ActionItem are @since 1.110, QuickResize is @since 1.137, and the` &&
            ` sap.m.Column headerMenu association that points at it is @since 1.98.0. The sample's ColumnMenu fragment is nothing but those controls, so a faithful port cannot avoid them - the app needs a UI5` &&
            ` release >= 1.137 for the menu, the rest of the table renders on 1.71. // NOTE: All four controller-loaded fragments (ColumnMenu, SortDialog, GroupDialog, FilterDialog) are declared in the view's` &&
            ` mvc:dependents aggregation and opened 1:1 via follow_up_action( cs_event-control_by_id, <id> open ). The three ViewSettingsDialogs are anonymous in the original (the controller keeps them in a map` &&
            ` keyed by fragment name); the port gives each the id it is opened by - sortDialog, groupDialog, filterDialog. // NOTE: The confirm handlers read the selected ViewSettingsItem CONTROLS` &&
            ` (${$parameters>/sortItem}, ${$parameters>/groupItem}, ${$parameters>/filterItems}) rather than a display string: the frontend marshals each control into its id plus its public properties, so the`.
    text1 = text1 && ` backend gets the real keys. That is the same information the original's mParams.sortItem.getKey() / oItem.getKey() reads. // NOTE: Thin frontend: sorting, filtering and the filter-bar text are` &&
            ` computed in ABAP over the model table instead of through sap.ui.model.Sorter/Filter on the items binding. The filter item keys keep the original's own encoding (WeightMeasure___LE___1000___X - field,` &&
            ` operator, value1, value2 separated by three underscores), which the port splits exactly like the controller's handleFilterDialogConfirm does. The infoToolbar visible flag and the filter label are` &&
            ` bound instead of set imperatively on vsdFilterBar / vsdFilterLabel. // NOTE: weightState is computed in ABAP rather than in a frontend formatter (thin frontend). **Corrected 2026-08-23**: this` &&
            ` sample's own Formatter.js takes ONE argument and compares the RAW WeightMeasure - < 0 None, < 1000 Success, < 2000 Warning, else Error - and the view binds a single part, so the unit never reaches` &&
            ` it. On 2026-08-21 the KG rule of a DIFFERENT sample was imported here (sap.m.Table / app 009, whose formatter takes measure AND unit, with boundaries 1 and 5 KG). Every one of the 123 mock rows is`.
    text1 = text1 && ` below 1000, so the original renders 123x Success while the port rendered 57 Success / 45 Warning / 21 Error - 66 rows wrong, and the note that justified it had the truth inverted. App 377 keeps the` &&
            ` KG rule and is right to: its sample's own model/formatter.js IS the two-argument KG one. // NOTE: The QuickSort change wire reads ${$parameters>/item} and asks it for getKey( ) and getSortOrder( ),` &&
            ` which is what the sample's own onSortChange does. The event's METADATA declares key and sortOrder instead, and QuickSort.onChange fires neither - it does fireChange({ item: oItem }) and nothing else` &&
            ` (QuickSort.js:85). An earlier draft used the declared names because the view gate's event-parameter check rejects ``item``; that check is wrong here, both arguments then arrived EMPTY on every` &&
            ` firing, and the handler's fallback sorted Name ascending whichever column was clicked. The gate finding is carried as a deliberate unknown-event-parameter budget entry with that reasoning -` &&
            ` satisfying the rule would mean breaking the port. sortOrder None is handled the way oBinding.sort( ) with no argument behaves: the rows go back to model order. // IMPROVISED: Grouping loses its group`.
    text1 = text1 && ` headers. The original's mGroupFunctions build a sap.ui.model.Sorter group key per row - a plain supplier name, and for Price a bucketed '100 EUR or less' / 'Between 100 and 1000 EUR' / 'More than` &&
            ` 1000 EUR' label. abap2UI5 has no client-side grouping function, so the port sorts the table by the chosen field instead: the rows end up in the same order, but without the grey group header rows. The` &&
            ` reset button restores model order, which is what the original's resetGroupDialog plus oBinding.sort( ) amounts to; until 2026-08-21 it only cleared a field that GROUP_CONFIRM reassigns before` &&
            ` reading, so it changed nothing observable. // IMPROVISED: The context-menu ToggleButton reports its state as a toast instead of attaching a sap.m.Menu as the table's contextMenu aggregation. The` &&
            ` original's onToggleContextMenu builds the Menu imperatively in the controller and swaps it in and out; the aggregation itself is not driven by any bindable property, and the sample's menu carries no` &&
            ` behaviour beyond opening. // NOTE: Device.system.desktop adds sapUiSizeCompact to each dialog in the original. The port drops that: abap2UI5 exposes the device data as {device>/system/desktop}, but a`.
    text1 = text1 && ` style class is not a bindable property, and the density is a global app decision rather than a per-dialog one here. // NOTE: The payload is read by hand - the frontend marshals each control into an` &&
            ` object of its ID plus all its public properties, and this port walks that object for the fields it models. abap2UI5 releases no JSON reader and the vendored ajson copy is framework-internal (the` &&
            ` linter's non-released-api rule reports it, correctly), so a targeted reader is the intended shape here; abap2UI5/samples Z2UI5_CL_SMP_APP_197 and _327 are the same pattern. It reads what the` &&
            ` FRAMEWORK wrote, which is flat, and does not resolve escapes - a payload composed from free user input would need that. // NOTE: onResize's oColumn.setWidth( width + 'px' ) is reproduced through a` &&
            ` bound Column.width: the QuickResize change event carries ${$parameters>/width} and the backend writes the CSS size. An unset value is a valid CSSSize (the type's 0* branch matches the empty string),` &&
            ` so the column keeps its automatic width until the first drag, as in the original. Until 2026-08-21 this wire round-tripped only to show a 'Column resized' toast the original does not have, behind a`.
    text1 = text1 && ` comment claiming onResize merely logs. // IMPROVISED: onBeforeColumnMenuOpen is dropped, and the Menu.beforeOpen attribute that wired it with it. It does oQuickResize.setWidth( parseInt(` &&
            ` getComputedStyle( oColumn.getDomRef( ) ).width ) ) - it reads the column's RENDERED width out of the DOM so the resize control opens showing the current size. A rendered pixel width has no` &&
            ` server-side equivalent, and the round-trip that used to be wired here did nothing at all with it, so the QuickResize opens at its own default instead. The port previously kept the wire as an empty` &&
            ` RETURN branch, beside a comment saying the original 'only inspects the opener'. // NOTE: **e2e-caught 2026-08-22 on app 571 (this port has no interaction module of its own; the generic` &&
            ` boot-and-render gate is all that runs for it)**: the sort ran but the table came back in its original order. The cause is the transpiled backend: ``SORT <itab> BY (field)`` - the DYNAMIC component` &&
            ` form - loses its BY clause entirely (the emitted JS is ``abap.statements.sort(t, {})``), so every sort was a no-op. The component is named statically per field now, in a CASE. Apps 298, 362 and 571`.
    text1 = text1 && ` all carried the dynamic form and all three are fixed.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Table`                           name = `TableViewSettingsDialog`                       class = `z2ui5_cl_smpc_app_298` path = `src/02/01/z2ui5_cl_smpc_app_298.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `The whole column-header menu is newer than the 1.71 floor: sap.m.table.columnmenu.Menu, QuickSort, QuickSortItem and ActionItem are @since 1.110, QuickResize is @since 1.137, and the sap.m.Column` &&
                 ` headerMenu association that points at it is @since 1.98.0. The sample's ColumnMenu fragment is nothing but those controls, so a faithful port cannot avoid them - the app needs a UI5 release >= 1.137` &&
                 ` for the menu, the rest of the table renders on 1.71.` ) ).

    text1 = `NOTE: The original configures a single shared dialog imperatively per button (oButton.data() CustomData ->` &&
            ` setMultiSelect/setDraggable/setResizable/setRememberSelections/setConfirmButtonText/addStyleClass). abap2UI5 binds those SelectDialog properties two-way and each button's handler sets them before` &&
            ` opening (responsivePadding toggles the style class via control_by_id addStyleClass/removeStyleClass). The core:CustomData is kept on the buttons for fidelity; the dialog is declared once in` &&
            ` mvc:dependents and opened via follow_up_action( cs_event-control_by_id, open ). // NOTE: The ObjectNumber weightState is business logic (parseFloat thresholds), so it is computed in ABAP` &&
            ` (WEIGHT_STATE, thin-frontend principle) and bound state='{WEIGHT_STATE}' instead of the frontend Formatter.weightState; core:require is therefore dropped and the Currency binding keeps the full` &&
            ` standard type path 'sap.ui.model.type.Currency' 1:1. The full 123-row /ProductCollection is inlined (ProductPicUrl is not needed — the table cells carry no icon; nor are TaxTarifCode, DateOfSale,`.
    text1 = text1 && ` Status and UoM, which this sample's cells do not use either — the list was completed 2026-08-23). // NOTE: Search filters the dialog's items binding client-side via follow_up_action(` &&
            ` cs_event-binding_call, filter NAME Contains ${$parameters>/value} ). The valueHelpRequest opens a second TableSelectDialog (also in dependents) after preselecting the row matching the input value.` &&
            ` The confirm/valueHelpClose toasts are simplified — selectedContexts / selectedItem are control references not transportable as event args (original composes the chosen product names / copies the` &&
            ` selected title into the input). CORRECTED 2026-08-21: the value-help close is reproduced 1:1. The event carries ``${$parameters>/selectedItem} ? ${$parameters>/selectedItem}.getCells()[0].getTitle()` &&
            ` : ''`` - the probe-measured indexed-access-into-an-array-valued-getter shape - and the backend writes it into the two-way bound productInput, so picking a row lands its name in the field and closing` &&
            ` without a selection clears it (setValue / resetProperty). The preselection compares against that same bound value instead of the seed literal, which is what _configValueHelpDialog does. The earlier`.
    text1 = text1 && ` claim that selectedItem is 'not transportable as an event arg' was wrong: an arg is a FULL UI5 expression and may call methods on the event's controls. // NOTE: The per-button dialog configuration,` &&
            ` the client-side search filter, multi-select confirm and the value-help selection copy-back need an in-system check; machine gates only verify the views are valid. **e2e-verified 2026-07-30**` &&
            ` (transpiled-framework interaction, scripts/e2e-smoke.mjs): the dialog opens and the client-side Contains search finds 'Gladiator MX' - restated 2026-08-23: the module asserts one row is present` &&
            ` before the search and that the searched row is present after it, never that the other 122 are gone, so nothing in it would fail if the filter did not apply; multi-select confirm and the value-help` &&
            ` copy-back remain unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_104.mjs). // POST-1.71: sap.m.TableSelectDialog.searchPlaceholder (since 1.110)` &&
            ` is kept 1:1 on the value-help dialog; needs UI5 >= 1.110. // IMPROVISED: handleClose's toast is not reproduced. The original names every chosen product - selectedContexts.map( getObject( ).Name`.
    text1 = text1 && ` ).join( ', ' ) - and stays silent when nothing was chosen; the port toasts a constant 'Selection confirmed' on confirm AND on cancel, which the original does not do either. Declared properly` &&
            ` 2026-08-23: the sidecar had covered it with "selectedContexts / selectedItem are control references not transportable as event args", a claim the same sidecar retracts a paragraph later. The real` &&
            ` obstacles are different and specific: a binding CONTEXT is not a control, so the frontend projection does not marshal it and JSON.stringify would hit its circular model graph; selectedItems ARE` &&
            ` controls but project to the ColumnListItem's own properties, not to the cell texts the names live in; and the two-way ``selected`` route app 490 uses is blocked here because both dialogs bind the` &&
            ` SAME t_products and the value help already writes that field for its preselection, so it would need a second flag. The filter reset in the same handler IS reproduced since today.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TableSelectDialog`               name = `TableSelectDialog`                             class = `z2ui5_cl_smpc_app_104` path = `src/02/01/z2ui5_cl_smpc_app_104.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.TableSelectDialog.searchPlaceholder (since 1.110) is kept 1:1 on the value-help dialog; needs UI5 >= 1.110.` ) ).

    text1 = `POST-1.71: initialFocus is @since 1.117.0 - it is not TableSelectDialog's own property but is inherited from sap.m.SelectDialogBase (SelectDialogBase.js:56), which is why it reads as a plain` &&
            ` TableSelectDialog attribute. The sample sets it to SearchField on the growing variant, so the port needs UI5 >= 1.117. No gate reported it: the value is a COND #( ), and the linter's view` &&
            ` reconstructor drops a variable-valued attribute entirely, so member-too-new never saw it. // NOTE: The fragment wires confirm=".handleClose" and cancel=".handleClose", but the controller defines no` &&
            ` handleClose at all - both handlers resolve to nothing in the original, and the dialog closes itself either way. The port drops both attributes rather than inventing a behaviour (structural-diff` &&
            ` reports attr missing TableSelectDialog.confirm and .cancel). // NOTE: The fragment's core:require pulls the Currency type and the sample's Formatter module. The port writes the type's full name in` &&
            ` the binding-info (sap.ui.model.type.Currency) and computes Formatter.weightState in ABAP, so the core:require attribute is dropped (structural-diff reports attr missing`.
    text1 = text1 && ` TableSelectDialog.core:require). // NOTE: Formatter.weightState of THIS sample compares the RAW measure (<0 None, <1000 Success, <2000 Warning, else Error - no unit conversion, unlike the shared demo` &&
            ` kit formatter). It is business logic, so the state is computed per row in model_init and bound as a plain field. // NOTE: handleTableSelectDialogPress reads the growing flag off the pressed button's` &&
            ` CustomData and sets growing plus initialFocus (SearchField when growing, List otherwise) before open( ). The port keeps the CustomData on the first button 1:1 but raises two events instead, and` &&
            ` builds the same fragment with those two properties set - one popup, two values, everything else identical. // NOTE: handleSearch filters the dialog's items binding with a Contains filter on Name.` &&
            ` Reproduced 1:1 through follow_up_action binding_call on the dialog's items aggregation, so the model stays untouched (app 022 precedent). // NOTE: Both dialog variants (growing on/off with their` &&
            ` initialFocus) and the binding_call search filter are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_454.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TableSelectDialog`               name = `TableSelectDialogGrowing`                      class = `z2ui5_cl_smpc_app_454` path = `src/02/01/z2ui5_cl_smpc_app_454.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `initialFocus is @since 1.117.0 - it is not TableSelectDialog's own property but is inherited from sap.m.SelectDialogBase (SelectDialogBase.js:56), which is why it reads as a plain TableSelectDialog` &&
                 ` attribute. The sample sets it to SearchField on the growing variant, so the port needs UI5 >= 1.117. No gate reported it: the value is a COND #( ), and the linter's view reconstructor drops a` &&
                 ` variable-valued attribute entirely, so member-too-new never saw it.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Text`                            name = `Text`                                          class = `z2ui5_cl_smpc_app_051` path = `src/01/01/z2ui5_cl_smpc_app_051.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.m.Text`                            name = `TextEmptyIndicator`                            class = `z2ui5_cl_smpc_app_439` path = `src/02/01/z2ui5_cl_smpc_app_439.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: sap.m.Text.emptyIndicatorMode is @since 1.87 and is the whole point of the sample (On vs Auto), so both Texts keep it 1:1 and the port needs a UI5 runtime >= 1.87. // NOTE: onCssClassChange` &&
                 ` calls containerAuto.toggleStyleClass('sapMShowEmpty-CTX'). The port makes the same call on the same control id through follow_up_action control_by_id, so the Switch needs no round-trip` &&
                 ` (toggleStyleClass is a public control method the frontend action dispatches). // NOTE: The Switch -> toggleStyleClass wire and with it the Auto indicator appearing inside the sapMShowEmpty-CTX Panel` &&
                 ` are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_439.mjs).`
        post171 = `sap.m.Text.emptyIndicatorMode is @since 1.87 and is the whole point of the sample (On vs Auto), so both Texts keep it 1:1 and the port needs a UI5 runtime >= 1.87.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Text`                            name = `TextHyphenation`                               class = `z2ui5_cl_smpc_app_445` path = `src/01/01/z2ui5_cl_smpc_app_445.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: onHyphenationChange loops over the five Texts and sets wrappingType to Hyphenated or Normal. The port two-way binds the Switch state and gives every Text the same wrappingType expression binding` &&
                 ` over that flag, so the switch works roundtrip-free and the Switch.change attribute is dropped. // NOTE: onSliderMoved sets containerLayout.setWidth(value + '%'). The Slider value is two-way bound and` &&
                 ` the Panel width follows it in an expression binding; the Slider.liveChange attribute is dropped (app 418 precedent). // NOTE: The hyphenation expression on all five Texts and the slider-driven Panel` &&
                 ` width are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_445.mjs).` )
      ( module = `sap.m`              control = `sap.m.Text`                            name = `TextMaxLines`                                  class = `z2ui5_cl_smpc_app_372` path = `src/01/01/z2ui5_cl_smpc_app_372.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    text1 = `NOTE: sap.m.Text.renderWhitespace is what the sample demonstrates and is kept 1:1 (bound here). It was declared a POST_171 as '@since 1.89' until 2026-08-24, which is wrong: the JSDoc reads @since` &&
            ` 1.51 (sap/m/Text.js) and ui5/properties.json agrees. 1.89 is the @since of a different member entirely, sap.m.Link.emptyIndicatorMode. Nothing in this port is post-1.71 - every other member used is` &&
            ` base version or 1.16.3/1.34 - so the deviation is withdrawn and the class moved from src/02/01 back to src/01/01, where an in-scope port belongs (AGENTS section 3). No gate could see this: view-gates` &&
            ` only lets a POST_171 EXCUSE a version finding, nothing flags an excuse with nothing behind it. The sibling port 249 uses renderWhitespace on six Texts and correctly declares nothing for it. // NOTE:` &&
            ` onWrappingChange and onRenderWhitespaceChange flip the Text property their Switch sits next to. Both are two-way bound on Switch.state and on the Text instead, so the toggles work roundtrip-free and`.
    text1 = text1 && ` both Switch.change attributes are dropped. // NOTE: onSliderMoved sets containerLayout.setWidth(value + '%'); the Slider value is two-way bound and the Panel width follows it in an expression binding` &&
            ` (Slider.liveChange dropped, app 418 precedent). // NOTE: The demonstrated text carries the whitespace itself - the original writes it as &#xA; / &#x9; entities in the view attribute; the port writes` &&
            ` the same characters as \n and \t in an ABAP string template, so the rendered string is byte-identical. // NOTE: Both two-way bound Text properties and the slider-driven Panel width are unverified in` &&
            ` a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_443.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Text`                            name = `TextRenderWhitespace`                          class = `z2ui5_cl_smpc_app_443` path = `src/01/01/z2ui5_cl_smpc_app_443.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TextArea`                        name = `TextArea`                                      class = `z2ui5_cl_smpc_app_052` path = `src/01/01/z2ui5_cl_smpc_app_052.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.0` )
      ( module = `sap.m`              control = `sap.m.TextArea`                        name = `TextAreaGrowing`                               class = `z2ui5_cl_smpc_app_370` path = `src/01/01/z2ui5_cl_smpc_app_370.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.0` ) ).

    text1 = `IMPROVISED: handleLiveChange recomputes each TextArea's valueState from the current length on every keystroke, and the original wires it on TWO of them. On the first, valueState is already an` &&
            ` expression binding over the same bound value in the original view, so the port keeps that expression and drops the attribute - the state follows the value, only at change granularity rather than per` &&
            ` keystroke, because valueLiveUpdate is false. On the SECOND the value is a view literal with a static valueState="Warning", so dropping liveChange leaves nothing behind: its Warning state is frozen` &&
            ` and shortening the text below 40 characters no longer clears it, where the original clears it live. Binding that value would contradict the sample's own "without data binding" label and a` &&
            ` per-keystroke round-trip is lossy, so the port keeps the literal and accepts the loss. // NOTE: The second and third TextAreas carry their value as a literal in the view, exactly as the original does` &&
            ` (only the first one is model-bound); structural-diff reports the literal against the first TextArea's {/value} because it compares the attribute across all three.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TextArea`                        name = `TextAreaMaxLength`                             class = `z2ui5_cl_smpc_app_485` path = `src/01/01/z2ui5_cl_smpc_app_485.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TextArea`                        name = `TextAreaValueStates`                           class = `z2ui5_cl_smpc_app_371` path = `src/01/01/z2ui5_cl_smpc_app_371.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.0` ) ).

    text1 = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings. Both legs are additionally e2e-verified (scripts/e2e-smoke.mjs` &&
            ` interaction, transpiled backend + real browser): with the Switch off the liveChange round-trip fills GET_VALUE while model.getProperty() stays behind, and after flipping it the model follows.` &&
            ` Restated 2026-08-23: the e2e half of that is narrower than it reads. The module asserts the PRE-flip text in the second Text after the flip, and clicking the Switch blurs the TextArea, which fires` &&
            ` change and writes the model regardless of valueLiveUpdate - so that assertion passes even if the flip did nothing. The off-leg is genuinely covered; the model-follows leg rests on the human live` &&
            ` check alone. Asserting the POST-flip text would close it.`.
    text2 = `NOTE: the sample's whole point is the difference between the control's own value and the model property while valueLiveUpdate is off, so the getValue Text must NOT be bound to the same field as the` &&
            ` TextArea. The port keeps them apart the same way: liveChange carries ${$parameters>/value} into the backend, which writes it to the separate field GET_VALUE - the second Text stays bound to the` &&
            ` TextArea's own model field, which only updates when valueLiveUpdate is on (or on blur). // NOTE: behavioural limit of the liveChange round-trip, measured 2026-08-02 in the e2e harness: abap2UI5` &&
            ` serializes round-trips, and until 2026-09-19 a keystroke typed while one was in flight was DROPPED, not queued - typing abc with no delay left GET_VALUE at a (the value of the last COMPLETED` &&
            ` round-trip), while the TextArea itself held abc; the e2e interaction therefore typed with a delay. CLOSED 2026-09-19: the wire carries s_ctrl-check_queue_last (abap2UI5 #2739, in the pin since #211),` &&
            ` which keeps the LAST event fired during a flight and dispatches it once the response has landed - one round-trip at a time, order kept, the backend ends on the TextArea's current value. Intermediate`.
    text2 = text2 && ` values can still be skipped under fast typing (the original updates its Text client-side on every keystroke); the last one is not lost, and the interaction module types with no delay. The wire` &&
            ` carries s_ctrl-check_no_busy alongside it (abap2UI5 #2759, in the pin since the bump to ad0a2dd2): the round-trip and the busy STATE are unchanged, only the full-screen overlay stays down. That is` &&
            ` the second half a per-keystroke wire needs - check_queue_last stops the keystrokes from being lost, check_no_busy stops the overlay from flashing over the field while they are typed. It was the` &&
            ` visible half of the gap: the FIRST trip raises the indicator after the usual delay, but every keystroke landing on a trip already in flight raises it with NO delay, so from the second character on` &&
            ` the original's round-trip-free typing was covered by an overlay the demo kit sample never shows. The interaction module measures it rather than trusting it: it hooks BusyIndicator.show( ) before` &&
            ` typing and asserts ZERO raises. That assertion is the kind that passes when it is broken - a watch that failed to attach reads zero forever - so the helper self-tests the hook before the module`.
    text2 = text2 && ` relies on it, and the count was checked against an ordinary wire (app 101's Cancel press raises exactly 1).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TextArea`                        name = `TextAreaValueUpdate`                           class = `z2ui5_cl_smpc_app_280` path = `src/01/01/z2ui5_cl_smpc_app_280.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.9.0`
        checked = text1
        notes = text2 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TileContent`                     name = `TileContent`                                   class = `z2ui5_cl_smpc_app_078` path = `src/01/01/z2ui5_cl_smpc_app_078.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34.0` ) ).

    text1 = `NOTE: openTimePicker (byId('HiddenTP').openBy(source.getDomRef())) is the app-016 openBy pattern: the source sId is transported via $event.oSource.sId and replayed as a control_by_id/openBy follow-up` &&
            ` action. // POST-1.71: Button.ariaHasPopup (since 1.84), Link.ariaHasPopup (since 1.86) and TimePicker.hideInput (since 1.97) are kept 1:1; needs a UI5 release providing them. // NOTE: the hidden` &&
            ` TimePicker openBy is wired roundtrip-free via client->follow_up_action( cs_event-control_by_id, openBy ) on each anchor press ($event.oSource.sId) - the original's` &&
            ` byId('HiddenTP').openBy(getDomRef()) 1:1; the change toast is now roundtrip-free too (control_global MESSAGE_TOAST.show), so the app is init-only. // NOTE: the openBy was switched to roundtrip-free` &&
            ` follow_up_action on 2026-07-22 - re-verify each anchor opens the hidden TimePicker. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the 'Open Time Picker'` &&
            ` anchor opens the hidden TimePicker popover; the remaining anchors are the identical wire. // NOTE: the change toast was switched to a roundtrip-free client-composed toast on 2026-07-22`.
    text1 = text1 && ` (control_global MESSAGE_TOAST.show, template ``Time selected: {0}`` filled by ${$parameters>/value}; on_event dropped, init-only) - re-verify picking a time toasts "Time selected: <value>" - scope,` &&
            ` added 2026-08-23: the interaction module covers the openBy half ONLY. It clicks the button and asserts the Popover appears; it never opens the clock, never picks a time and never looks at a toast, so` &&
            ` the change wire could be deleted and the module would still pass. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_091.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TimePicker`                      name = `TimePickerHidden`                              class = `z2ui5_cl_smpc_app_091` path = `src/02/01/z2ui5_cl_smpc_app_091.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.32`
        is_post171 = abap_true
        notes = text1
        post171 = `Button.ariaHasPopup (since 1.84), Link.ariaHasPopup (since 1.86) and TimePicker.hideInput (since 1.97) are kept 1:1; needs a UI5 release providing them.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TimePicker`                      name = `TimePickerValueState`                          class = `z2ui5_cl_smpc_app_404` path = `src/01/01/z2ui5_cl_smpc_app_404.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.32` ) ).

    text1 = `NOTE: The picked time is transported to the backend via a two-way value binding on TimePickerSliders (an extra attribute; the original reads it imperatively with oTP.getValue()). OK composes the` &&
            ` result text from the current time_value, Cancel restores the pre-open value captured on OPEN_DIALOG (the attachAfterOpen equivalent). // NOTE: The result text uses the static control id 'TPS2'` &&
            ` because the original's runtime-generated oTP.getId() cannot be reproduced. The collapseAll( ) on the sliders as the dialog closes is dropped. Re-verified 2026-08-23: both halves still hold, with` &&
            ` sharper reasons. The runtime id is oTP.getId( ) AFTER UI5 prefixes it with the view id assigned at runtime (__xmlview0--TPS2), which the backend never sees - the same boundary` &&
            ` pr/aggregation-item-address had to resolve on the client - and the OK button's event reaches the Button, not the TimePickerSliders, so no expression addresses it either. collapseAll IS callable today` &&
            ` (it is not on the frontend denylist, and app 365 calls exactly that method on a TreeTable); it is dropped because there is nothing for it to collapse: the original keeps ONE dialog instance as a view`.
    text1 = text1 && ` dependent and reopens it, so without the collapse the sliders would reopen expanded, while popup_display rebuilds the fragment on every open and the sliders are collapsed by construction.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.TimePickerSliders`               name = `TimePickerSliders`                             class = `z2ui5_cl_smpc_app_095` path = `src/01/01/z2ui5_cl_smpc_app_095.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.54`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Title`                           name = `TitleLink`                                     class = `z2ui5_cl_smpc_app_079` path = `src/02/01/z2ui5_cl_smpc_app_079.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.27.0`
        is_post171 = abap_true
        notes = `POST-1.71: the Link nested inside the Title uses the Title content aggregation (since UI5 1.87) - newer than 1.71 but kept for the 1:1 port (the sample's whole point); the app needs a UI5 release >=` &&
                 ` 1.87 to render it. Aggregation-level, invisible to the attribute-scanning property gate.`
        post171 = `the Link nested inside the Title uses the Title content aggregation (since UI5 1.87) - newer than 1.71 but kept for the 1:1 port (the sample's whole point); the app needs a UI5 release >= 1.87 to` &&
                 ` render it. Aggregation-level, invisible to the attribute-scanning property gate.` ) ).

    text1 = `NOTE: The three controller handlers are replaced by bindings, roundtrip-free: onWrappingChange's Title.setWrapping toggle becomes the same two-way bound flag on Switch.state and Title.wrapping (the` &&
            ` Switch change attribute is dropped on both Switches), onHyphenationChange becomes an added wrappingType expression binding following the second Switch, and onSliderMoved's setWidth(value+'%') becomes` &&
            ` an expression binding on the Panel width fed by the two-way bound Slider value (the Slider liveChange attribute is dropped). // NOTE: The three roundtrip-free wires (wrapping toggle,` &&
            ` Hyphenated/Normal wrappingType expression, Panel width percent expression) are client-side model propagation and are unverified in a running system. **e2e-verified 2026-08-21** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_418.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Title`                           name = `TitleWrapping`                                 class = `z2ui5_cl_smpc_app_418` path = `src/01/01/z2ui5_cl_smpc_app_418.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.27.0`
        notes = text1 ) ).

    text1 = `NOTE: onPress toasts the source control id + Pressed/Unpressed; both arrive via $event.oSource (sId and getPressed()) - the earlier ${$source>/pressed} binding did not resolve at runtime. // NOTE: the` &&
            ` press toast was switched to a roundtrip-free client-composed toast on 2026-07-22 using the conditional placeholder (control_global MESSAGE_TOAST.show, template ``{0} {1?Pressed:Unpressed}``; on_event` &&
            ` dropped, init-only) - re-verify pressing a ToggleButton toasts "<id> Pressed" when down and "<id> Unpressed" when up. **e2e-verified 2026-07-30** (transpiled-framework interaction,` &&
            ` scripts/e2e-smoke.mjs): pressing the 'Pressed' ToggleButton (down -> up) toasts '<id> Unpressed' - scope, added 2026-08-23: the module asserts only the '{1?Pressed:Unpressed}' half. The {0} half,` &&
            ` $event.oSource.sId, is not covered, and formatTemplate does String(values[0]), so an unresolved id would render the literal 'undefined Unpressed' and still satisfy the assertion - the` &&
            ` {1?Pressed:Unpressed} conditional placeholder resolves from the toggle state; the other buttons are the identical wire.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ToggleButton`                    name = `ToggleButton`                                  class = `z2ui5_cl_smpc_app_080` path = `src/01/01/z2ui5_cl_smpc_app_080.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tokenizer`                       name = `TokenizerBasic`                                class = `z2ui5_cl_smpc_app_085` path = `src/02/01/z2ui5_cl_smpc_app_085.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-27): verified in a running system 2026-07-27 - Tokenizer add appends, delete removes by key, texts correct. **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_085.mjs: 'Add Token' round-trips ADD so the typed text becomes a token, toasts and clears the bound input; focusing that token and pressing Delete round-trips` &&
                 ` DELETE with its key so it is removed and 'Token deleted' toasts)`
        notes = `NOTE: the first Tokenizer's tokens are now model-bound (t_tokens): onAddToken appends the input value, onTokenDelete removes by key (the deleted key arrives via` &&
                 ` $event.getParameter('tokens')[0].getKey(); the delete toast shows the token text looked up by that key, like the original's oToken.getText()); the second, disabled Tokenizer keeps its 3 static` &&
                 ` tokens, so the port shows one bound Token template + 3 static Token vs the original's 3+3. // NOTE: the CheckBox select handler becomes a live two-way selected/editable bind on the first Tokenizer.` &&
                 ` // POST-1.71: tokenDelete (since UI5 1.82) on the first Tokenizer is newer than 1.71 but kept for the 1:1 port (the original wires the same event) - the app needs a UI5 release >= 1.82 to render it.`
        post171 = `tokenDelete (since UI5 1.82) on the first Tokenizer is newer than 1.71 but kept for the 1:1 port (the original wires the same event) - the app needs a UI5 release >= 1.82 to render it.` ) ).

    text1 = `POST-1.71: sap.m.Tokenizer.tokenDelete is @since 1.82; the original wires it, so the port keeps it 1:1. Two more members are newer still and were undeclared until 2026-08-24: multiLine and` &&
            ` showClearAll are both @ui5-experimental-since 1.142 (sap/m/Tokenizer.js) - and they ARE the sample, so the real release floor is >= 1.142, not the >= 1.82 this deviation used to state. No gate could` &&
            ` see them: generate-metadata.mjs parses only @since, so ui5/properties.json carries both with no since key at all and the property gate passes them silently - the AGENTS section 5 'backstop, not a` &&
            ` guarantee' case. On anything older than 1.142 the two properties are ignored by XMLTemplateProcessor and the port renders as a plain single-line Tokenizer with no Clear All. This is the only port in` &&
            ` the corpus using either property. Being experimental, they may also change without notice. // NOTE: The 19 Tokens the original declares statically become one bound Token template over a 19-row table` &&
            ` (structural-diff reports control missing Token: original 19 vs port 1). Same 19 texts and keys, and the model binding is what makes onTokenDelete's removeToken( ) expressible in the backend (app 085`.
    text1 = text1 && ` precedent). // IMPROVISED: onTokenDelete loops over EVERY token the event carries. A UI5 expression argument cannot map an array of controls to their keys (the grammar has no function literal), so` &&
            ` the wire transports the FIRST deleted key plus the number of deleted tokens. NOTE the grammar half is true but the conclusion does not follow, and the corpus has already corrected this once: the` &&
            ` array does not have to be MAPPED in the expression, it can travel whole. Lib.js's projectControl/normalizeEventArg marshals a control array into plain objects carrying every public property - and key` &&
            ` and text are public properties of sap.m.Token - which the backend then stringifies into the event arg. App 103 does exactly that with ${$parameters>/selectedItems} and unmarshals with z2ui5_cl_ajson` &&
            ` under a named linter waiver, and app 103's own sidecar records this same inference being corrected on 2026-08-23. So the subset leg below is reproducible with a known idiom rather than out of reach:` &&
            ` Clear All (count = all rows) clears the table and the per-token X (count 1) deletes that key. A multi-select delete of a strict SUBSET therefore removes only the first of the selected tokens - the`.
    text1 = text1 && ` one leg of the original that is not reproduced. // NOTE: Both delete paths (the per-token X and the Clear All button) are unverified in a running system. The module presses only the per-token X - it` &&
            ` asserts 19 tokens render and that one click drops it to 18, i.e. the del_count = 1 branch. Clear All is never pressed, so the del_count >= lines( t_tokens ) branch is unexercised and a port with a` &&
            ` dead Clear-All leg passes it. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_432.mjs).`.
    text2 = `sap.m.Tokenizer.tokenDelete is @since 1.82; the original wires it, so the port keeps it 1:1. Two more members are newer still and were undeclared until 2026-08-24: multiLine and showClearAll are both` &&
            ` @ui5-experimental-since 1.142 (sap/m/Tokenizer.js) - and they ARE the sample, so the real release floor is >= 1.142, not the >= 1.82 this deviation used to state. No gate could see them:` &&
            ` generate-metadata.mjs parses only @since, so ui5/properties.json carries both with no since key at all and the property gate passes them silently - the AGENTS section 5 'backstop, not a guarantee'` &&
            ` case. On anything older than 1.142 the two properties are ignored by XMLTemplateProcessor and the port renders as a plain single-line Tokenizer with no Clear All. This is the only port in the corpus` &&
            ` using either property. Being experimental, they may also change without notice.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tokenizer`                       name = `TokenizerMultiLine`                            class = `z2ui5_cl_smpc_app_432` path = `src/02/01/z2ui5_cl_smpc_app_432.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Toolbar`                         name = `ToolbarShrinkable`                             class = `z2ui5_cl_smpc_app_053` path = `src/01/01/z2ui5_cl_smpc_app_053.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        checked = `CHECKED (2026-07-20): verified in a running system - human live check 2026-07-20 following the interaction checklist (all listed checks passed) **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_053.mjs: the three toolbars boot at 100% and one ArrowLeft on the focused slider handle moves all three widths to 80% through the expression binding with no round` &&
                 ` trip)`
        notes = `NOTE: the sample's controller onSliderLiveChange resizes the toolbars in JS; there is no width in the source XML (the port adds the width attribute, the original wires liveChange instead). Rebuilt as` &&
                 ` a client-side expression binding {= slider + '%' } on each Toolbar width - no event round-trip, resizes instantly like the original; the documented preferred path (CAPABILITIES.md), not a workaround.` )
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `Tree`                                          class = `z2ui5_cl_smpc_app_054` path = `src/01/01/z2ui5_cl_smpc_app_054.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        checked = `CHECKED (2026-07-19): verified in a running system - human visual pass 2026-07-19 over all apps: the nested-table hierarchy renders as an expandable Tree like the original. **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_054.mjs: the two root nodes render and dispatching the mouse sequence on Node1's expander reveals the nested Node1-1)` ) ).

    text1 = `NOTE: onInit does oTree.setMode('MultiSelect') imperatively. mode IS a bindable property, so the port writes it in the view instead of calling the setter through a frontend action - the` &&
            ` bindable-property-beats-frontend-action rule (app 401/437 precedent). The mode attribute is therefore present where the original view does not write it, and the tree comes up multi-select as it does` &&
            ` upstream. // IMPROVISED: onDrop rewrites the JSON MODEL in place: it reads the dragged contexts out of the drag session's complex data, pushes each one's data into the drop target's ``categories``` &&
            ` array and sets the source path to undefined, which is what preserves the expand/collapse state. A thin frontend has neither the drag session nor a context, so the drop transports the two node TITLES` &&
            ` instead (${$parameters>/draggedControl}.getTitle() and the same for droppedControl, app 569 idiom) and the backend reparents that node. To make a reparenting expressible at all the model is kept` &&
            ` twice: a FLAT table (text, ref, parent) is the source of truth and the five-level nested table the Tree binds is rebuilt from it after every drop. Lost with the drag session: dragging a`.
    text1 = text1 && ` MULTI-selection moves only the node under the cursor here, since the selection never reaches the backend. The sample's own guard against dropping a node into its own child is reproduced` &&
            ` (is_descendant walks the parent chain). // NOTE: onDragStart's oEvent.preventDefault() vetoes a drag that starts on a row OUTSIDE the current selection. The veto is wired, but not the way the flag` &&
            ` would do it: s_ctrl-check_prevent_default (app 241 idiom) is baked per wire at RENDER time, and whether the dragged row is selected is known only at the moment of the drag - so the port carries` &&
            ` prevent_default_expr instead, a client expression evaluated on each firing, and DRAG_START reaches on_event unhandled because the decision has already been made. What stays dropped is the sample's` &&
            ` second half: stashing the dragged contexts in the drag session, which has no counterpart here. // NOTE: the sample's onDrop pushes into a ``categories`` array while Tree.json nests its children under` &&
            ` ``nodes`` and the view binds path '/' - so upstream a dropped node lands in an array the tree does not read and simply disappears from the visible hierarchy. The port reparents under ``nodes``, i.e.`.
    text1 = text1 && ` it does what the comment in the sample says it is doing ('the children of a node are inside an array with the name categories') rather than what the file actually contains. // NOTE: not yet verified` &&
            ` in a running system: that dragging a node onto another reparents it and the tree redraws with the node under its new parent. **e2e-verified 2026-08-23** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_600.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `TreeDnD`                                       class = `z2ui5_cl_smpc_app_600` path = `src/01/01/z2ui5_cl_smpc_app_600.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `NOTE: onExpandMultiPress and onCollapseMultiPress collect the selected items' INDICES (oTree.indexOfItem per selected item) and hand the array to oTree.expand / oTree.collapse. The port issues one` &&
            ` round-trip-free follow_up_action( control_by_id, Tree, expandSelected | collapseSelected ) instead, and the frontend does that mapping. The original's spelling is not portable: an event argument is` &&
            ` resolved by UI5's BindingParser, whose ExpressionParser has no ``function`` keyword and reads ``{`` as an object literal, so ``.getSelectedItems().map(function (o) { ... })`` does not fail on that` &&
            ` one argument - the WHOLE handler fails to parse and every argument is lost. app 248 carries the same interaction on sap.ui.table.TreeTable, which has getSelectedIndices( ) and therefore needs no` &&
            ` loop; sap.m.Tree has no callback-free equivalent, which is why abap2UI5 grew the two synthetic CONTROL_METHODS entries. // NOTE: onInit does this.byId('Tree').expandToLevel(1). expandToLevel IS` &&
            ` whitelisted, so it is issued once per render as a round-trip-free follow_up_action (app 248/365 idiom) - the tree comes up with its first level open, as upstream. // NOTE: onSelectionFinish maps the`.
    text1 = text1 && ` MultiComboBox's selected keys onto oTree.setSticky(). sticky IS a bindable property and selectedKeys is bindable too, so the two share ONE bound string table (app 508 precedent): picking a key writes` &&
            ` the table, the table is the tree's sticky value, and no round trip or setter call is needed. The selectionFinish wire is therefore absent - the binding does what the handler did. // NOTE:` &&
            ` onToggleInfoToolbar does oTree.getInfoToolbar().setVisible(!oEvent.getParameter('pressed')). Both halves are bindable: the ToggleButton's pressed state is bound two-way to one flag and the info` &&
            ` toolbar's visible is an expression over the same flag ({= !${...} }), so the toolbar hides the moment the button is pressed, in the browser. The press wire is therefore absent, and the toolbar starts` &&
            ` visible with the button unpressed, exactly as upstream. // NOTE: Tree.json is the 44-node variant this sample and TreeExpandTo ship (six Node1-n branches, five levels deep), not the ten-node one` &&
            ` TreeSelection / TreeDnD use. It is seeded verbatim into five nested ABAP structure levels - the depth of the data is the depth of the type (app 437 precedent).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `TreeExpandMulti`                               class = `z2ui5_cl_smpc_app_601` path = `src/01/01/z2ui5_cl_smpc_app_601.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `NOTE: handleSelectChange does this.byId('Tree').expandToLevel(oEvent.getParameter('selectedItem').getKey()) and onCollapseAllPress does oTree.collapseAll(). Both methods ARE whitelisted in` &&
            ` CONTROL_METHODS, so both are round-trip-free follow_up_action( control_by_id, ... ) wires (app 248/365 idiom): the Select's change carries ${$parameters>/selectedItem}.getKey() straight into` &&
            ` expandToLevel, and the button calls collapseAll with no argument at all. // NOTE: onInit does the same expandToLevel(1) a second time, before any interaction, so the tree comes up with its first` &&
            ` level open - which is what the toolbar's own 'Initially expand to Level1' text and the Select's selectedKey='1' announce. It is issued once per render as a frontend action. // NOTE: Tree.json is the` &&
            ` 44-node variant this sample and TreeExpandMulti ship (six Node1-n branches, five levels deep). It is seeded verbatim into five nested ABAP structure levels - the depth of the data is the depth of the` &&
            ` type (app 437 precedent).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `TreeExpandTo`                                  class = `z2ui5_cl_smpc_app_602` path = `src/01/01/z2ui5_cl_smpc_app_602.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `NOTE: onToggleContextMenu creates a sap.m.Menu with one MenuItem in JavaScript on press and destroys it again when the button is un-pressed. abap2UI5 has no client-side control factory, so the same` &&
            ` Menu/MenuItem is declared in the view's contextMenu aggregation and the view is rebuilt on the toggle: the pressed state arrives via ${$parameters>/pressed} and the backend emits the subtree only` &&
            ` while it is on. structural-diff therefore reports control extra Menu and control extra MenuItem - the original's are built in the controller, not in the view. // NOTE: The context-menu toggle` &&
            ` (pressed -> rebuilt view with/without the contextMenu subtree) and the MenuItem's {TEXT} binding against the pressed node are unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_436.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `TreeIcon`                                      class = `z2ui5_cl_smpc_app_436` path = `src/01/01/z2ui5_cl_smpc_app_436.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `NOTE: loadData pretends to fetch from a backend with a 2-second setTimeout and replaces the expanded node's dummy child with the next two nodes. In the port the fetch is real: toggleOpenState` &&
            ` transports itemIndex, the item context PATH (${$parameters>/itemContext}.getPath()) and the expanded flag, and ABAP appends the two nodes under the addressed node. The artificial delay and the` &&
            ` accompanying Tree.setBusy(true/false) are dropped - the round-trip is the wait. // NOTE: The port's node ladder is typed to tree level 5, so it stops two levels short of the original, which reaches` &&
            ` level 7 - expanding a level-5 item there yields level-6 children whose second node carries a level-7 ``Last node`` child. In the port ``is_last`` can never become true (level is at most 4) and the` &&
            ` WHEN 5 branch emits no dummy child, so its deepest node is not expandable and the ``Last node`` string is unreachable. A faithful depth would need two more nested row types. // NOTE: The port's node` &&
            ` ladder is typed to tree level 5, so it stops two levels short of the original, which reaches level 7 - expanding a level-5 item there yields level-6 children whose second node carries a level-7`.
    text1 = text1 && ` ``Last node`` child. In the port ``is_last`` can never become true (level is at most 4) and the WHEN 5 branch emits no dummy child, so its deepest node is not expandable and the ``Last node`` string` &&
            ` is unreachable. A faithful depth would need two more nested row types. // NOTE: The toggle wire (path parsing, the appended nodes and the three-value toast) is unverified in a running system.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_496.mjs). // NOTE: The client sends a binding path of the shape /T_NODES/1/NODES/0 - the root segment is the` &&
            ` bound ATTRIBUTE's name and the model is serialized upper-cased by z2ui5_cl_ajson_mapping=>create_upper_case( ). It is not the original sample's /1/nodes/0. The parser therefore keeps the numeric` &&
            ` segments and ignores the named ones (app 546's index_of does the same); matching a literal lowercase ``nodes`` never fired and drove every segment into CONV i( ). // NOTE: loadData in the original` &&
            ` only refills when the expanded node still holds its dummy child (bChildIsDummyNode). The port guards on ``expanded`` alone and assigns the children outright, so collapsing an already-expanded node`.
    text1 = text1 && ` and re-expanding it resets that subtree to the two default nodes where the original leaves it alone. Also: the original's toast passes width: "auto" alongside duration; the port passes duration only,` &&
            ` so the three-line toast wraps in the default 15em.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `TreeJSONLazyLoading`                           class = `z2ui5_cl_smpc_app_496` path = `src/01/01/z2ui5_cl_smpc_app_496.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    text1 = `IMPROVISED: the sample's items come from an OData v2 tree binding over a mock service: the Nodes entity is a FLAT list carrying HierarchyLevel / NodeID / ParentNodeID / DrillState, and the service` &&
            ` METADATA carries the matching sap:hierarchy-* annotations, which the ODataTreeBinding reads on its own to build the tree (the binding parameter countMode: 'Inline' goes with it). abap2UI5 serves one` &&
            ` JSON model and has no ODataTreeBinding, so the same sixteen nodes are re-shaped into a nested table whose ``children`` arrays ARE the hierarchy - which is exactly how app 366 ports the same mock for` &&
            ` sap.ui.table.TreeTable, and this port reuses its shape and its data verbatim. // NOTE: the flat fields the annotations use are kept on every node (nodeid, hierarchylevel, parentnodeid, drillstate)` &&
            ` even though the view binds only Description, because they are the mock's own data and they are what makes the improvisation checkable against the original: the nesting the port builds is the nesting` &&
            ` those ids and levels describe. The root nodes' ParentNodeID is null in the JSON and becomes the empty string in the ABAP row. // NOTE: the mock server (localService/mockserver.js over`.
    text1 = text1 && ` localService/metadata.xml) and the Component's ODataModel on '/here/goes/your/odata/service/url/' have no counterpart in an abap2UI5 app, whose backend IS the service. The MessageStrip that warns` &&
            ` about the data volume sap.m.Tree can carry is ported 1:1, closable as upstream.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `TreeOData`                                     class = `z2ui5_cl_smpc_app_603` path = `src/01/01/z2ui5_cl_smpc_app_603.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Tree`                            name = `TreeSelection`                                 class = `z2ui5_cl_smpc_app_437` path = `src/01/01/z2ui5_cl_smpc_app_437.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.42`
        notes = `NOTE: handleSelectChange calls tree.setMode(selectedItem.getKey()). The port two-way binds one field on Select.selectedKey and Tree.mode, so the mode switch happens roundtrip-free in the model and the` &&
                 ` Select.change attribute is dropped (structural-diff reports attr missing Select.change). // NOTE: The shared two-way bound mode field (Select -> Tree.mode across all five selection modes) is` &&
                 ` unverified in a running system. **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_437.mjs).` ) ).

    text1 = `NOTE: **Rebuilt on 2026-08-05** - it was a breadth probe with THREE INVENTED rows (Screenshot.png / Notes.txt / Report.doc) and a reduced toolbar. Both rows of the sample's own items.json are there.` &&
            ` Corrected 2026-08-23: row 2 had carried an invented mediaType='image/png' - the mock's second row has mimeType, which nothing in the sample binds, so the bound mediaType property is undefined there.` &&
            ` It is empty now, and the row is verbatim (Business Plan Agenda.doc with its five ObjectMarkers and four ObjectStatuses, Picture of a woman.png with its two), the item template carries the` &&
            ` markers/statuses aggregations and uploadState, the UploadSet its terminationEnabled / maxFileNameLength / maxFileSize / mediaTypes / uploadUrl, and the toolbar its three buttons plus the` &&
            ` UploadSetToolbarPlaceholder. The asset URLs point at the OpenUI5 host per the offline asset rule. The port has no structural difference from the original left, so its breadth-probe structural_diff` &&
            ` skip is gone. Upload itself is not wired at all - the port keeps the original's relative uploadUrl and nothing else, so in an abap2UI5 deployment that URL resolves to nothing. Corrected 2026-08-23:`.
    text1 = text1 && ` the sentence used to point at "the abap2UI5 FileUploader path", and the framework ships a purpose-built companion for exactly this control (z2ui5.cc.UploadSetExt, which reads each added file as a` &&
            ` base64 data URL into bindable properties so the backend needs no upload endpoint; CAPABILITIES.md lists it). Converting this port to it is open work, and the control stays a decided scope exception` &&
            ` (sap.m.upload.UploadSet is deprecated, KEEP 2026-07-30). // POST-1.71: UploadSet.mode (since UI5 1.100) and UploadSet.afterItemRemoved (since UI5 1.83) kept for the 1:1 port - surfaced when the` &&
            ` property gate gained the sap.m/upload sub-package (control-level source scan 2026-07-26). The whole control is deprecated out-of-scope debt anyway (pr/scope-since-from-source). // POST-1.71: Found on` &&
            ` 2026-08-21, when view-gates stopped letting ANY deviation excuse a version finding: a NOTE that merely contained the member's name had been satisfying the gate, and only POST_171 / DROPPED_171 carry` &&
            ` that claim. sap.m.upload.UploadSetToolbarPlaceholder is @since 1.103.0 and is kept 1:1 from the sample's toolbar. It is a CONTROL-level finding, so nothing about it appears as an attribute; the port`.
    text1 = text1 && ` already sits in src/02/01 for sap.m.upload.UploadSet itself, and the app needs UI5 >= 1.103. // NOTE: The items' ``url`` field is host-absolutized: items.json carries the demo kit's relative` &&
            ` 'demokit/sample/UploadCollection/LinkedDocuments/Business Plan Agenda.doc', and the port serves it from https://sdk.openui5.org/test-resources/sap/m/... because an abap2UI5 app has no demo-kit` &&
            ` document root to resolve a relative path against. Declared explicitly on 2026-08-21: data-fidelity used to clear this because an unrelated deviation contained the word 'url', and it now requires the` &&
            ` field to be named as a field. // IMPROVISED: Three of the original's five handlers are still not reproduced, and the two that are were reworked 2026-08-23. WHAT WORKS NOW: onAfterItemRemoved really` &&
            ` deletes the row (it used to only toast, so a removed file came back on the next render - the wire transports the item's file name, since the sample matches on an id this row type does not carry), and` &&
            ` onSelectionChange drives the version button's enabled state for exactly one selection (it had been hard-coded false, so the button the sample's selection logic exists to demonstrate was permanently`.
    text1 = text1 && ` dead). The five invented toasts are gone with them; the original raises no MessageToast anywhere. STILL DROPPED: uploadItem( ) on the two toolbar buttons and download( true ); onInit's FileUploader` &&
            ` customisation (setButtonOnly(false) / setIconOnly(true) / the attachment icon), so the add control renders as the default upload button rather than the bare icon the sample is recognisable by; and` &&
            ` uploadCompleted, which unshifts the newly uploaded file into the list.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.upload.UploadSet`                name = `UploadSet`                                     class = `z2ui5_cl_smpc_app_121` path = `src/02/01/z2ui5_cl_smpc_app_121.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.63`
        is_post171 = abap_true
        is_deprecated = abap_true
        dep_text = `Deprecated since 1.129: replaced by sap.m.plugins.UploadSetwithTable`
        notes = text1
        post171 = `UploadSet.mode (since UI5 1.100) and UploadSet.afterItemRemoved (since UI5 1.83) kept for the 1:1 port - surfaced when the property gate gained the sap.m/upload sub-package (control-level source scan` &&
                 ` 2026-07-26). The whole control is deprecated out-of-scope debt anyway (pr/scope-since-from-source). // Found on 2026-08-21, when view-gates stopped letting ANY deviation excuse a version finding: a` &&
                 ` NOTE that merely contained the member's name had been satisfying the gate, and only POST_171 / DROPPED_171 carry that claim. sap.m.upload.UploadSetToolbarPlaceholder is @since 1.103.0 and is kept 1:1` &&
                 ` from the sample's toolbar. It is a CONTROL-level finding, so nothing about it appears as an attribute; the port already sits in src/02/01 for sap.m.upload.UploadSet itself, and the app needs UI5 >=` &&
                 ` 1.103.` ) ).

    text1 = `NOTE: element binding kept 1:1 - a one-record structure /S_SUPPLIER instead of {/SupplierCollection/0}. // NOTE: URLHelper.triggerTel/triggerSms/triggerEmail/redirect map 1:1 to the URLHELPER frontend` &&
            ` action (cs_event-urlhelper): TRIGGER_TEL/TRIGGER_SMS take a { TEL: '...' } object-literal t_arg like the other two (corrected 2026-08-23 - this said 'take the number as a plain string param', and the` &&
            ` port was wired that way, so BOTH fired with no number at all: evUrlHelper reads args[2] as an OBJECT and takes params.TEL, so a primitive string yields undefined, URLHelper.formatTel returns '' for a` &&
            ` non-string, and the item navigated to a bare tel:/sms:. Note the key for SMS is TEL as well, not SMS - the handler reads params.TEL for both), TRIGGER_EMAIL/REDIRECT take a { EMAIL/URL, ... }` &&
            ` object-literal t_arg (get_t_arg emits {-prefixed args raw as UI5 event-handler object literals). open_new_tab is NOT used - it is same-origin-only (isValidRedirectURL). // NOTE: The 2026-07-27 live`.
    text1 = text1 && ` check is retired and the port is back to ``generated`` (AGENTS section 10: a behavioural rework of a checked port resets the status or is restamped after a fresh live run). Its note read "verified in` &&
            ` a running system 2026-07-27 - URLHelper tel/sms/email triggers and REDIRECT all fire correctly" - but tel and sms could not have fired with a number on that code: both were wired with a primitive` &&
            ` string where evUrlHelper reads params.TEL off an object, so URLHelper received undefined and formatTel returned the empty string. The email and redirect halves of that run stand; the tel and sms` &&
            ` halves need a fresh one now that the wires carry { TEL: '...' }. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_084.mjs). // NOTE: Reviewed 2026-08-26 and` &&
            ` deliberately LEFT AS IS: the four URLHELPER wires interpolate model data into the RAW object-literal branch of get_t_arg ({ TEL: '<value>' }), which is not escaped by design - get_t_arg emits an` &&
            ` argument raw whenever it starts with { or $, and only quotes/escapes the others via escape_js_string. An apostrophe or a backslash in TEL/SMS/EMAIL/URL would therefore close the JS string literal`.
    text1 = text1 && ` inside the press handler and take the whole handler expression down when UI5 parses it. NOT REACHABLE here: s_supplier is written exactly once, by model_init, from four literals copied verbatim from` &&
            ` ui5/mock/supplier.json (+49 6227 747474 / +49 173 123456 / john.smith@sap.com / http://www.sap.com); this class has no on_event method at all, no control in the view is editable (the four` &&
            ` DisplayListItems bind value one-way), and the interpolation happens at view-build time on the server. The data is fixed by construction, not merely quote-free today. Nor is the usual mitigation` &&
            ` available: evUrlHelper reads args[2] as an OBJECT and takes params.TEL / params.EMAIL / params.URL off it, so the value cannot be moved into a separate quoted argument without a framework change, and` &&
            ` the queued (AJSON) form is no help either - { TEL: '...' } is not valid JSON, so it would fall back to a plain string and params.TEL would be undefined, which is exactly the bug corrected on` &&
            ` 2026-08-23. Escaping the two characters in ABAP would be a speculative change to a working, e2e-verified port whose input cannot vary. If this port ever grows an editable supplier record, or is`.
    text1 = text1 && ` copied as the template for one, the escaping has to be added at that point - an apostrophe is realistic in a real email local part (o'brien@...), just not in a phone number and not in this seed.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.URLHelper`                       name = `UrlHelper`                                     class = `z2ui5_cl_smpc_app_084` path = `src/01/01/z2ui5_cl_smpc_app_084.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.10`
        notes = text1 ) ).

    text1 = `NOTE: The three controller-loaded fragments (Dialog / DialogPreselected / DialogPreset) are declared in the view's mvc:dependents aggregation and opened 1:1 via follow_up_action(` &&
            ` cs_event-control_by_id, open [pageKey] ) — the whitelisted ViewSettingsDialog open with the optional page key ('filter'). confirm forwards the public filterString event parameter` &&
            ` (${$parameters>/filterString}) and toasts it when non-empty, like the original handleConfirm. // NOTE: The DialogPreset presetFilterItems are declared as three ViewSettingsItem entries (text + key)` &&
            ` instead of the original _presetFiltersInit which adds them imperatively with sap.ui.model.Filter arrays as CustomData. The Filter payload is inert in this sample (no list is bound — confirm only` &&
            ` shows the filterString), so only the three visible preset filter options are reproduced. This adds 3 ViewSettingsItem over the original fragment count.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ViewSettingsDialog`              name = `ViewSettingsDialog`                            class = `z2ui5_cl_smpc_app_098` path = `src/01/01/z2ui5_cl_smpc_app_098.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `NOTE: The controller-loaded Dialog fragment is declared in the view's mvc:dependents aggregation and opened 1:1 via follow_up_action( cs_event-control_by_id, settingsDialog open ).` &&
            ` confirm/cancel/resetFilters become backend events; confirm forwards the public filterString event parameter (${$parameters>/filterString}) and toasts it together with the slider value, like the` &&
            ` original handleConfirm. // NOTE: The Slider carries a bound value attribute the original does not have: the sample seeds and restores it imperatively (oSlider.setValue in the fragment-load callback,` &&
            ` in handleCancel and in handleResetFilters). A bound ABAP field is the abap2UI5 equivalent of all three, so the initial value 50 comes from model_init and cancel/reset just write the field. // NOTE:` &&
            ` ViewSettingsCustomItem carries bound filterCount and selected attributes the original does not have. The original drives both imperatively (setFilterCount/setSelected in handleSliderChange,` &&
            ` handleCancel and handleResetFilters); both are ordinary bindable properties, so the port binds them and computes the state in ABAP. // NOTE: Thin frontend: the comparisons that decide the filter`.
    text1 = text1 && ` state - new slider value against the last confirmed one (handleSliderChange), and the confirmed value against the reset value 50 (handleCancel) - are computed in ABAP instead of in the controller.` &&
            ` filterResetValue/filterPreviousValue become the constant c_reset_value and the protected previous_value; behaviour is identical.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ViewSettingsDialog`              name = `ViewSettingsDialogCustom`                      class = `z2ui5_cl_smpc_app_295` path = `src/01/01/z2ui5_cl_smpc_app_295.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `NOTE: The controller-loaded Dialog fragment is declared in the view's mvc:dependents aggregation and opened 1:1 via follow_up_action( cs_event-control_by_id, settingsDialog open ), the equivalent of` &&
            ` the controller's _getDialog().then(oDialog => oDialog.open()). // NOTE: The ViewSettingsDialog carries a bound filterSearchOperator attribute the original does not have. The original calls` &&
            ` setFilterSearchOperator( mLibrary.StringFilterOperator.Contains / AnyWordStartsWith ) imperatively right before opening; it is an ordinary bindable property, so the port binds it and the two buttons` &&
            ` just write the ABAP field. // IMPROVISED: The third button, 'Search Case Sensitive Contains', loses its case sensitivity. The original installs a live JS function through setFilterSearchCallback(` &&
            ` caseSensitiveStringContains ) - a callback the frontend evaluates per keystroke, which no backend payload can carry and which the framework deliberately has no route for (a runtime-compiled JS string` &&
            ` was rejected for security). The port falls back to the closest built-in operator, the case-INSENSITIVE Contains, so the button still filters but matches regardless of case. The other two buttons are`.
    text1 = text1 && ` unaffected.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ViewSettingsDialog`              name = `ViewSettingsDialogCustomFilterDetails`         class = `z2ui5_cl_smpc_app_296` path = `src/01/01/z2ui5_cl_smpc_app_296.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16`
        notes = text1 ) ).

    text1 = `NOTE: All three controller-loaded fragments (Dialog / DialogSingleCustomTab / DialogSingleCustomTabWithDatePicker) are declared in the view's mvc:dependents aggregation and opened 1:1 via` &&
            ` follow_up_action( cs_event-control_by_id, <id> open ). confirm forwards the public filterString event parameter (${$parameters>/filterString}) and toasts it when non-empty, like the original` &&
            ` handleConfirm. // POST-1.71: ViewSettingsDialog.beforeClose is @since 1.132 - newer than the 1.71 floor. It is the event the original's handleBeforeClose hangs on to veto closing while the DatePicker` &&
            ` is invalid, so a faithful port cannot drop it; the app needs a UI5 release >= 1.132 for that third dialog. // NOTE: The beforeClose veto is reproduced with s_ctrl-prevent_default_expr: a client` &&
            ` expression over the bound date_value_state field decides per firing whether the close is cancelled, which is what the original's conditional oEvent.preventDefault() does. The event still reaches the` &&
            ` backend, which adds the 'The entered date is invalid' toast - so the decision travels as data, not as a controller branch. Corrected 2026-08-23: the expression was built as |${ client->_bind( path =`.
    text1 = text1 && ` abap_true ) } === 'Error'|, and an ABAP string template treats the following brace as an embedded expression, so what reached the wire was "$/DATE_VALUE_STATE === 'Error'" - $ followed by a path is` &&
            ` not a token the UI5 event-handler parser knows, and the veto could never have fired. The braced _bind( ) form yields ${/DATE_VALUE_STATE}, which is what the expression needs. The corpus' other` &&
            ` prevent_default_expr wires read $parameters (247/306/354) or - in app 136 - a model path built by backtick concatenation, where _bind supplies its own braces; none was affected. // NOTE: The` &&
            ` DatePicker carries bound valueState and valueStateText attributes the original does not have, and its change event reports the public valid parameter (${$parameters>/valid}). The original's` &&
            ` validateDate sets both imperatively in the controller; both are bindable properties, so the port computes the state in ABAP (thin frontend) and binds it. The error text is identical. // NOTE: The` &&
            ` three fragments each use id='app-settings' for their ViewSettingsCustomTab, and the first two additionally repeat the whole Settings-tab id set (VSDThemeLabel, VSDThemeButtons, VSDsap_hcb,`.
    text1 = text1 && ` VSDsap_quartzlight, VSDCompactModeButtons, VSDcompactOn, VSDcompactOff, VSDRTLButtons, VSDRTLOn, VSDRTLOff). In UI5 those are separate fragment id scopes; abap2UI5 renders all three dialogs into ONE` &&
            ` view, where the duplicates would collide. The second and third dialog therefore carry suffixed ids (app-settings-single, app-settings-datepicker, and the Single suffix on the repeated Settings-tab` &&
            ` ids). Only the id VALUES differ - every control and attribute is the original's. // NOTE: The Settings tab body is written out twice instead of through a helper method, mirroring the original, which` &&
            ` also carries one copy per fragment. A builder-returning helper would hide the open/shut tree from the static view gates. // IMPROVISED: The custom tabs are inert, exactly as in the original: the` &&
            ` SegmentedButtons for Theme, Compact Content Density and Right To Left Mode carry no handler in the sample either, so nothing is applied when they are pressed. // NOTE: The sample's own style.css is` &&
            ` injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). It was archived but never reached the view, so the SegmentedButton's vsdSetting margin and the`.
    text1 = text1 && ` DatePicker's vsd-dp insets did nothing - a class name with no rule behind it, the gap apps 122/124/133 closed. Found by scripts/probes/orphan-style-class-probe.mjs.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.ViewSettingsDialog`              name = `ViewSettingsDialogCustomTabs`                  class = `z2ui5_cl_smpc_app_297` path = `src/02/01/z2ui5_cl_smpc_app_297.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.16`
        is_post171 = abap_true
        notes = text1
        post171 = `ViewSettingsDialog.beforeClose is @since 1.132 - newer than the 1.71 floor. It is the event the original's handleBeforeClose hangs on to veto closing while the DatePicker is invalid, so a faithful` &&
                 ` port cannot drop it; the app needs a UI5 release >= 1.132 for that third dialog.` ) ).

    text1 = `NOTE: The sample's default model is a bare JSON array (oModel.setData([...])), bound as the root ({/} on the suggestionItems / items aggregations) and by absolute index (/9/text, /9/key on the second` &&
            ` form). abap2UI5's single default model is an object, so the ten rows live in the T_ITEMS member: {/} becomes {/T_ITEMS} and /9/text / /9/key become /T_ITEMS/9/TEXT / /T_ITEMS/9/KEY. The last path` &&
            ` segment is identical, which structural-diff matches; same data, renders identically. // NOTE: The original applies an app-authored '.whitespace2Char' formatter on every text/additionalText binding -` &&
            ` it replaces each doubled space with space + U+00A0 (non-breaking space) so consecutive whitespaces stay visible. abap2UI5 is a thin frontend, so this presentation transform is computed once in` &&
            ` model_init (REPLACE ALL OCCURRENCES OF two spaces WITH space + nbsp) and the finished text is bound plainly ({TEXT}, {ADDITIONALTEXT}); the 'formatter' key is dropped from the bindings. Output is` &&
            ` identical.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.WhitespacePattern`               name = `WhitespacePattern`                             class = `z2ui5_cl_smpc_app_185` path = `src/01/01/z2ui5_cl_smpc_app_185.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        ui5_only = abap_true
        notes = text1 ) ).

    text1 = `NOTE: Step validation (additionalInfoValidation) is reproduced in ABAP: on the ProductName/ProductWeight liveChange (and ProductInfoStep activate) the name-length>=6 / weight checks (the weight one` &&
            ` was NARROWER than the original until 2026-08-23: it required all digits, while the original tests parseInt( ) IS NaN, which skips leading blanks and a sign and stops at the first non-digit - so` &&
            ` '12.5', '-5' and '12abc' are valid there and were rejected here, and the Input is type=Number, so a decimal is exactly what a user types). Both failing branches also call setCurrentStep(` &&
            ` ProductInfoStep ), which the port had dropped and now issues as a frontend action set the two valueStates and the ProductInfoStep validated property, which is bound two-way (step2_validated) instead` &&
            ` of the original imperative validateStep/invalidateStep. The other steps keep their literal validated='true'. **The setCurrentStep frontend action was a SILENT NO-OP until abap2UI5 registered it,` &&
            ` reported from a real system 2026-09-22.** ``setCurrentStep`` was missing from the frontend's CONTROL_METHODS while all three of its Wizard siblings (discardProgress, setNextStep, goToStep) were in`.
    text1 = text1 && ` it, so the call took the unlisted-method path, which hands the RAW ABAP literal over. sap.m.Wizard resolves a string argument with Element.getElementById( ), and ``_pageStack``-style ids carry the` &&
            ` runtime view prefix the backend never sees - so oStep came back undefined, ``_isStepReachable`` never ran, and UI5 answered "The given step could not be set as current step." once per failing` &&
            ` keystroke while doing nothing. No wrong target, no exception: exactly the shape of the backToPage gap recorded in the navigation NOTE below, on this same port, one method over - and equally invisible` &&
            ` to every gate here, since the view renders and the handler runs. **The PORT was never wrong** and is unchanged by the fix. It needs an abap2UI5 newer than the pin this repository carries; until the` &&
            ` pin moves, the console error stands and the failing-validation branch does not force the wizard back to ProductInfoStep. // NOTE: The two liveChange wires round-trip per keystroke, which the original` &&
            ` does NOT: additionalInfoValidation is a plain controller function there, so typing in the demo kit sample is instant and raises nothing. The round-trip is unavoidable while the check lives in ABAP;`.
    text1 = text1 && ` the OVERLAY was not. The wires carry s_ctrl-check_queue_last (so no keystroke is dropped) and, since the pin bump to ad0a2dd2, s_ctrl-check_no_busy (abap2UI5 #2759) - the round-trip and the busy` &&
            ` STATE are unchanged, only the full-screen busy overlay stays down. Without the second flag the FIRST trip raises the indicator after the usual delay, but every keystroke landing on a trip already in` &&
            ` flight raises it with NO delay, so from the second character on the overlay sat over the very field being typed into - which is also what forced the waitForIdle before the first click in` &&
            ` meta/interactions/z2ui5_cl_smpc_app_101.mjs. // NOTE: The step-1 SegmentedButton gets item keys (Mobile/Desktop/Other) and a two-way selectedKey binding so the chosen product type reaches the model` &&
            ` directly; the original reads evt.getParameters().item.getText() in setProductTypeFromSegmented. selectionChange is still wired. The PricingStep activate/complete handlers (which only toggle the` &&
            ` unused navApiEnabled flag) are wired for fidelity but do nothing. // NOTE: Navigation is via follow_up_action( cs_event-control_by_id ). Corrected 2026-08-23: it used to say 1:1 while using ``to```.
    text1 = text1 && ` for all three legs. Only the Wizard-complete leg is ``to`` in the original; the Edit and Cancel/Submit legs are backToPage, a REVERSE transition that unwinds the NavContainer stack rather than` &&
            ` pushing onto it, and they now use it. The original's afterNavigate deferral - it attaches a listener and calls goToStep only inside it - stays dropped: Wizard complete -> NavContainer 'to' the review` &&
            ` page; each Edit link -> 'to' the content page then Wizard 'goToStep' the target step (whitelisted). Cancel and Submit open a MessageBox (warning/confirm) with YES/NO; on YES the wizard resets via` &&
            ` 'to' the content page + 'discardProgress' ProductTypeStep, matching _handleMessageBoxOpen. **Bounded 2026-08-27, CLOSED 2026-08-28.** For five days "they now use it" was true of the port and false of` &&
            ` what a reader got when they ran it. ``backToPage`` was not in the frontend's ``CONTROL_METHODS`` until abap2UI5 ``329e0c84`` (#2670), so it took the unlisted-method path, which infers argument types` &&
            ` and hands the RAW ABAP literal over. ``sap.m.NavContainer.backToPage`` does not rescue that: ``_backTo`` normalises only a Control, then ``_findClosestPreviousPageInfo`` compares ``info.id ===`.
    text1 = text1 && ` sRequestedPreviousPageId`` strictly against a ``_pageStack`` whose entries were all pushed as ``page.getId()`` and therefore carry the runtime view prefix the backend never sees. Measured 2026-08-27` &&
            ` on this port against the live Node backend (#2670): after the complete leg the stack read ["mainView--wizardContentPage", "mainView--wizardReviewPage"], the Edit link fired` &&
            ` ``backToPage("wizardContentPage")``, and UI5 answered "Cannot navigate backToPage('wizardContentPage') because target page was not found among the previous pages." with the review page still on` &&
            ` screen. A SILENT no-op - no wrong target, no exception - which is why nothing here caught it. The four Edit links and the Cancel/Submit reset therefore left the review page up; only the` &&
            ` Wizard-complete ``to`` leg, and the ``goToStep``/``discardProgress`` that follow the dead back-navigation, did anything. The fix is the ``pageId`` argument kind, which resolves the control and hands` &&
            ` over ``control.getId()``; it reached the reproducible builds when ``A2UI5_PIN`` moved on 2026-08-28, so all six legs are live here now. **The PORT was never changed by any of this** - it was correct`.
    text1 = text1 && ` on both sides of the fix and simply could not work before it. Note also which way this cuts: the 2026-08-23 correction ABOVE, which switched Edit and Cancel/Submit from ``to`` to ``backToPage`` for` &&
            ` fidelity with the original, is what made them stop working - ``to`` was listed and normalised its argument, so the pre-2026-08-23 port navigated (with the wrong transition direction). Fidelity was` &&
            ` bought with function for five days, and this is the record of that. The e2e interaction module asserts the NavContainer's CURRENT page after an Edit link since 2026-08-28, which is the only thing` &&
            ` that separates a working back-navigation from a dead one - a ``goToStep`` assertion passes on both. // NOTE: the cancel leg is closed: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction,` &&
            ` transpiled backend + real browser): the first wizard step renders and the footer Cancel really round-trips - message_box_display opens the MessageBox 'Are you sure you want to cancel your report?'` &&
            ` with its question text (restated 2026-08-23: the module asserts the dialog text only; it never locates a YES or NO button and never exercises onclose, so that leg is not covered). Still needs an`.
    text1 = text1 && ` in-system check: step validation gating the Next button, the complete/edit navigation, the goToStep scroll and the submit/cancel reset itself. **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_101.mjs). **Extended 2026-08-28:** the module now completes the wizard, asserts the review page, presses the first Edit link and asserts the NavContainer is back` &&
            ` on the wizard content page with the wizard on ProductTypeStep - the backToPage leg the pin bump made live. // NOTE: Two step body Texts had been TRUNCATED and are restored 2026-08-23:` &&
            ` ProductInfoStep's ran 475 of the original's 955 characters and OptionalInfoStep's 385 of 575, each a clean prefix that stopped mid-paragraph. OptionalInfoStep had also silently corrected the` &&
            ` original's own typo "Donec ppellentesque" to "pellentesque". Both now carry the full text; the original's trailing TAB is written as the single space XML attribute-value normalisation turns it into.` &&
            ` No gate compares long text bodies, which is why this survived.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Wizard`                          name = `Wizard`                                        class = `z2ui5_cl_smpc_app_101` path = `src/01/01/z2ui5_cl_smpc_app_101.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = text1 ) ).

    text1 = `NOTE: The original keeps the form data in nested paths on one JSON model (/CreditCard/Name, /CashOnDelivery/FirstName, /BillingAddress/Address ...). abap2UI5 keeps one default model, so the nested` &&
            ` paths are folded to flat fields whose last segment is identical - the same fold app 166 makes. /CashOnDelivery/Phone Number, whose last segment carries a space, becomes phonenumber. // IMPROVISED:` &&
            ` goToPaymentStep and billingAddressComplete branch the wizard from the COMPLETE handler, and that cannot be reproduced 1:1. WizardStep._complete fires complete and then calls` &&
            ` Wizard._handleNextButtonPress in the SAME tick (sap/m/WizardStep.js), so a nextStep that only arrives with the abap2UI5 round trip is one press too late. Measured against real OpenUI5 in headless` &&
            ` Chromium with the port's eight step declarations: with the branch left to the complete round trip the FIRST press on PaymentTypeStep threw 'The wizard is in branching mode, and no next step is` &&
            ` defined for the current step, please set one' out of Wizard._getNextStep, the progress stayed on PaymentTypeStep, and only the SECOND press advanced; with the branch already set the same press`.
    text1 = text1 && ` advanced straight to CreditCardStep. Unlike app 534 the button really is reachable - PaymentTypeStep declares no validated, so it defaults to true and WizardStep.setButtonVisibility displays its Next` &&
            ` button from the first render (measured buttonDisplayed=true). The port therefore sends the branch BEFORE the press instead of on it: PaymentTypeStep carries nextStep=CreditCardStep - the payment` &&
            ` default model_init seeds - as a declared association the original does not have, and branch_payment re-sends the current choice from the SegmentedButton's selectionChange and from the YES branch of` &&
            ` the discard confirmation. Both branch methods are ALSO called unconditionally at the end of view_display( ), which is what makes the branch survive a rebuild - without it PaymentTypeStep falls back` &&
            ` to its static nextStep whatever the user chose, and BillingStep comes back with no branch at all, which is the "wizard is in branching mode and no next step is defined" throw. Deleting those two` &&
            ` calls as unaccounted-for restores it. BillingStep needs no declared association: its Next button is gated behind validated, only a CHECK_BILLING answer can raise that flag, and CHECK_BILLING is also`.
    text1 = text1 && ` the step's activate wire - so the branch_delivery call it carries always lands before the button can appear (measured: at the activate event validated=false and the button is hidden; the deferred` &&
            ` answer sets nextStep, and only then does validated flip). The complete wires stay in the view and re-assert the same value, as goToPaymentStep and billingAddressComplete do. Cost of the declared` &&
            ` association: the progress navigator reads 4 (varying) at load where the original reads 2 (varying) - the port shows from the start what the original shows once the payment branch is chosen.` &&
            ` BillingStep's own reveal is unchanged, and declaring its nextStep too would have cost the varying indication altogether (measured 5 (fixed)), which is why it is not declared. // NOTE:` &&
            ` checkCreditCardStep / checkCashOnDeliveryStep / checkBillingStep call validateStep and invalidateStep imperatively. Each step's validated property is bound two-way here and the same liveChange wires` &&
            ` recompute it in ABAP with the same rule (at least three characters), so the Next button gates exactly as in the original. // IMPROVISED: setDiscardableProperty only asks for confirmation while the`.
    text1 = text1 && ` wizard's PROGRESS STEP is past the step being discarded (getProgressStep() !== discardStep). A backend cannot read getProgressStep(), so the port keeps two flags that stand in for the two` &&
            ` comparisons: each is raised by the activate wire of the step that follows the compared one, and cleared again when the progress is discarded. This costs two added activate attributes` &&
            ` (BankAccountStep, DeliveryAddressStep and DeliveryTypeStep, which the sample leaves unwired) and gets the question asked in the same situations. The YES branch discards the progress through` &&
            ` control_by_id and remembers the new value; the NO branch restores the remembered one, both as in the original. // NOTE: handleDelete removes the row whose title the delete event carries and never` &&
            ` removes the last one; the port sends the title with the event and does the same, then recomputes the total the way calcTotal does. attachRequestCompleted keeps the first FIVE rows of the mock product` &&
            ` collection and seeds the payment and delivery defaults - the port seeds exactly those five rows. // NOTE: completedHandler, _navBackToStep and _handleMessageBoxOpen navigate the NavContainer and the`.
    text1 = text1 && ` wizard imperatively. All three go through control_by_id here ('to', 'goToStep', 'discardProgress' - all whitelisted), the same idiom app 101 uses for the sibling sample. // NOTE: The five product` &&
            ` images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org. // NOTE: The **e2e-verified 2026-08-25** marker on this port overstated what the nightly covered: the interaction` &&
            ` module of that day asserted the cart rows, the calcTotal sum, the eight steps and the row delete and never pressed Next, so no branch was ever exercised and the broken first press went unseen.` &&
            ` meta/interactions/z2ui5_cl_smpc_app_535.mjs now walks ContentsStep -> PaymentTypeStep -> CreditCardStep and fails if one press does not reach the branch, and it pins PaymentTypeStep's declared` &&
            ` nextStep so a rebuild that loses the association is caught. Still unverified in a running system: the Bank Transfer and Cash on Delivery branches, the delivery-address branch, the two discard` &&
            ` confirmations and the review page's six Edit links.`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Wizard`                          name = `WizardBranching`                               class = `z2ui5_cl_smpc_app_535` path = `src/01/01/z2ui5_cl_smpc_app_535.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = text1 ) ).

    text1 = `IMPROVISED: The sample is THREE views: a container view whose Page content holds two mvc:XMLViews (view.Linear and view.Branching), each with its own controller. abap2UI5 has no named XMLViews to` &&
            ` instantiate, so both wizards are inlined directly into the Page's content, each keeping the visible expression its mvc:XMLView carries ({= ${/selectedShowCase} === 'linear'} / '=== branching').` &&
            ` structural-diff reports mvc:XMLView as missing; every control inside the two views is present. The Branching controller's _findParentView walk exists only to reach across that view boundary and has` &&
            ` nothing to do here. // NOTE: onCurrentStepChangeLinear and onCurrentStepChangeBranching call setCurrentStep on the respective wizard. Wizard.currentStep is an ASSOCIATION and cannot be bound, so both` &&
            ` Selects drive the wizard through the framework's whitelisted control_by_id goToStep call. The branching one's try/catch (which toasts the error, resets the Select and re-applies the last path) has no` &&
            ` counterpart: goToStep on an unreachable step is a no-op with a UI5 log rather than a thrown exception here. // NOTE: applyPath reads the picked radio button's TEXT and walks the arrow-separated path`.
    text1 = text1 && ` calling setNextStep on each step, clearing the last one's. The port keeps the three paths as data and issues the same setNextStep calls through control_by_id (whitelisted), so enableBranching still` &&
            ` follows the picked path. discardAndApplyPath's discardProgress and the Select reset are reproduced the same way. // NOTE: validateProdInfoStep reads the two Inputs imperatively and calls` &&
            ` setValidated; the port binds ProductInfoStep.validated and the two value states and recomputes all three in ABAP on the same change wires. The Linear view declares no validated attribute at all, so` &&
            ` the step starts VALIDATED here too - that is what lets currentStep="PricingStep" hold on startup. onActivate syncs the Select with the activated step, which the port does by sending each step's id` &&
            ` with its activate wire (an added activate attribute on the steps the sample leaves unwired). // NOTE: The Select drives the Wizard's backgroundDesign through a two-way binding. The property does` &&
            ` exist and is base-version - sap.m.Wizard declares backgroundDesign with no @since (Wizard.js:156) and a real public setBackgroundDesign (:685) - so the earlier claim that it is absent from the 1.71`.
    text1 = text1 && ` metadata and has no whitelisted setter was wrong on both halves. // NOTE: The two showcases, the two current-step Selects, the three branching paths and the step-2 validation are unverified in a` &&
            ` running system. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_534.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Wizard`                          name = `WizardCurrentStep`                             class = `z2ui5_cl_smpc_app_534` path = `src/01/01/z2ui5_cl_smpc_app_534.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.Wizard renderMode is @since 1.84 and navigationChange @since 1.101 - both newer than the 1.71 floor. renderMode="Page" is what makes this a one-step-at-a-time wizard and` &&
            ` navigationChange is what drives the footer, so both are kept and the port is filed under src/02. // NOTE: Wizard.currentStep is an ASSOCIATION: the XML parser reads its value as a control id and` &&
            ` never as a binding, so the port cannot bind it. The Next / Previous / Review buttons and the four Edit links therefore drive the wizard through the framework's whitelisted control_by_id goToStep(` &&
            ` step, true ) call - the same goToStep the controller uses, and the same idiom app 101 already uses for its Edit links. // NOTE: handleButtonsVisibility switches the five footer buttons on the` &&
            ` selected step index. The port keeps that index as one bound field, updated by navigationChange (which carries the step's TITLE, unique across the five steps) and by every goToStep, and each button` &&
            ` carries the switch's own condition as an expression binding - so the visibility follows the wizard without a handler. // NOTE: additionalInfoValidation reads the two Inputs imperatively, sets the two`.
    text1 = text1 && ` value states, validates or invalidates the step and enables the Next button. All five are bound fields here and the two liveChange wires recompute them in ABAP - same rule (a name of at least six` &&
            ` characters, a numeric weight), same effect, one round-trip per keystroke as in the original. // NOTE: _handleMessageBoxOpen asks YES/NO and, on YES, discards the wizard progress, closes the dialog` &&
            ` and resets the model to the initial oData. The port raises the same MessageBox with the same two actions and does all three on the YES branch (discardProgress through control_by_id, model_init,` &&
            ` popup_destroy). // NOTE: handleOpenDialog loads the fragment and opens it; the port builds the same fragment in a chain and shows it with popup_display, which is why structural-diff reports nothing` &&
            ` missing for the Dialog itself. optionalStepActivation's toast is kept 1:1. setProductTypeFromSegmented also calls validateStep on the first step, which is already validated="true" in the view, so the` &&
            ` port's selectionChange only keeps the two-way bound key. // NOTE: The review step's Texts bind the same model fields as the original - productType, productName, productWeight, productManufacturer,`.
    text1 = text1 && ` productDescription, manufacturingDate, availabilityType, size, measurement, productPrice, discountGroup and productVAT - through client->_bind, which the static comparison cannot resolve to a path;` &&
            ` the one LITERAL Text in the same step (the Lorem ipsum under '3. Optional Information', literal in the sample too) is what structural-diff is left holding, so it reports every one of those bindings` &&
            ` against it. // NOTE: The wizard navigation, the five footer buttons, the step-2 validation and the cancel/submit message boxes are unverified in a running system. **e2e-verified 2026-08-25** (nightly` &&
            ` e2e interaction, meta/interactions/z2ui5_cl_smpc_app_533.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.m.Wizard`                          name = `WizardSingleStep`                              class = `z2ui5_cl_smpc_app_533` path = `src/02/01/z2ui5_cl_smpc_app_533.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.30`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Wizard renderMode is @since 1.84 and navigationChange @since 1.101 - both newer than the 1.71 floor. renderMode="Page" is what makes this a one-step-at-a-time wizard and navigationChange is what` &&
                 ` drives the footer, so both are kept and the port is filed under src/02.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.ui.core.ContainerPadding`          name = `ContainerNoPadding`                            class = `z2ui5_cl_smpc_app_087` path = `src/01/01/z2ui5_cl_smpc_app_087.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: the /ProductCollectionStats/Counts values are flattened to the default model fields /TOTAL, /OK, /HEAVY, /OVERWEIGHT (verbatim counts).` ) ).

    text1 = `NOTE: The controller loads Dialog.fragment.xml lazily and calls syncStyleClass('sapUiSizeCompact', view, dialog) before open(). The port builds the same fragment in its own chain and shows it with` &&
            ` popup_display; the compact-density sync is dropped. Re-verified 2026-08-23 with a better reason than the one that stood here ('has no equivalent ... abap2UI5 sets the density on the shell, not per` &&
            ` popup'): it is a NO-OP in abap2UI5, not an inexpressible call. syncStyleClass copies sapUiSizeCompact from the view onto the dialog because a UI5 dialog renders in the static area outside the view;` &&
            ` abap2UI5 puts that class on the <body> itself (z2ui5_cl_ui5_http_handler / z2ui5_cl_ui5f_index_html both emit class='sapUiBody sapUiSizeCompact'), so the static area is already inside it and the` &&
            ` dialog is already compact. addStyleClass is whitelisted, so the call COULD be made - it would just set a class that is already in effect. // NOTE: onDialogCloseButton on both buttons is reproduced by` &&
            ` the client-side follow_up_action popup_close, so closing the dialog needs no round-trip. // NOTE: The popup_display of the fragment and both popup_close wires are unverified in a running system.`.
    text1 = text1 && ` **e2e-verified 2026-08-22** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_434.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.ui.core.ContainerPadding`          name = `ContainerPadding`                              class = `z2ui5_cl_smpc_app_434` path = `src/01/01/z2ui5_cl_smpc_app_434.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.ui.core.ContainerPadding`          name = `ContainerPaddingAndMargin`                     class = `z2ui5_cl_smpc_app_433` path = `src/01/01/z2ui5_cl_smpc_app_433.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The three Image srcs read {img>/products/pic1} from the named img model (ui5/mock/img.json); the single value is folded into the view - the mock's own HT-7777-large.jpg, re-hosted on` &&
                 ` sdk.openui5.org, so the Images render identically. // NOTE: onInit seeds /widthS /widthM /widthL from Device.system.phone (2em/4em/6em on a phone, 5em/10em/15em otherwise) and the view binds them.` &&
                 ` The port expresses the same branch over the framework's device> model in an expression binding on each Image width, so the widths stay device-dependent instead of being resolved once on the server` &&
                 ` (app 031 precedent).` )
      ( module = `sap.m`              control = `sap.ui.core.ContainerPadding`          name = `ContainerResponsivePadding`                    class = `z2ui5_cl_smpc_app_431` path = `src/01/01/z2ui5_cl_smpc_app_431.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The Image src reads {img>/products/pic1} from the named img model (ui5/mock/img.json). abap2UI5 serves one default model, so the single value is folded into the view literal - the mock's own` &&
                 ` HT-7777-large.jpg, re-hosted on sdk.openui5.org, so the Image renders identically. The products.json model the controller also sets is never bound by this view and is therefore not seeded.` )
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardMarginsAll`                            class = `z2ui5_cl_smpc_app_088` path = `src/01/01/z2ui5_cl_smpc_app_088.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardMarginsCollapse`                       class = `z2ui5_cl_smpc_app_469` path = `src/01/01/z2ui5_cl_smpc_app_469.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardMarginsEnforceWidthAuto`               class = `z2ui5_cl_smpc_app_484` path = `src/01/01/z2ui5_cl_smpc_app_484.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The IconTabBar binds expanded to the demo kit's own device model flag {device>/isNoPhone}. abap2UI5 serves the raw sap.ui.Device under device>, so the port expresses the same branch as {=` &&
                 ` !${device>/system/phone} } (apps 030/089 precedent) - same value on the same devices.` )
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardMarginsResponsive`                     class = `z2ui5_cl_smpc_app_472` path = `src/01/01/z2ui5_cl_smpc_app_472.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardMarginsSingleSided`                    class = `z2ui5_cl_smpc_app_430` path = `src/01/01/z2ui5_cl_smpc_app_430.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardMarginsTwoSided`                       class = `z2ui5_cl_smpc_app_464` path = `src/01/01/z2ui5_cl_smpc_app_464.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` )
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardNegativeMarginsTwoSided`               class = `z2ui5_cl_smpc_app_403` path = `src/01/01/z2ui5_cl_smpc_app_403.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.m`              control = `sap.ui.core.StandardMargins`           name = `StandardNoMargins`                             class = `z2ui5_cl_smpc_app_500` path = `src/01/01/z2ui5_cl_smpc_app_500.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The two ObjectHeaders bind the fixed records {/ProductCollection/0} and {/ProductCollection/1}. The port seeds both records' bound fields at the default-model root under a per-header prefix and` &&
                 ` binds them absolutely, so both ObjectHeader.binding attributes are dropped and the composed attribute texts read the same values. // NOTE: The last ObjectAttribute of each header is the literal` &&
                 ` 'www.sap.com' with active="true", which structural-diff reports against the {Description} attribute of the same control because it compares the attribute across all four - both texts are present, in` &&
                 ` the original's order.` )
      ( module = `sap.tnt`            control = `sap.tnt.InfoLabel`                     name = `InfoLabel`                                     class = `z2ui5_cl_smpc_app_113` path = `src/02/05/z2ui5_cl_smpc_app_113.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.54`
        is_post171 = abap_true
        notes = `NOTE: Ten sap.tnt InfoLabels across color schemes / render modes / icon / displayOnly, in FlexBox rows - a full 1:1 rebuild of the sample view (static, no controller logic). Originally generated as a` &&
                 ` breadth-probe; the 2026-07-27 review fixed the last row's label text ('Any Color Scheme in Display Only Mode' - had been invented as 'Color Scheme 10') and removed the stale structural_diff skip. //` &&
                 ` POST-1.71: sap.tnt.InfoLabel.icon (@since 1.74) is used 1:1 (one InfoLabel carries icon='sap-icon://home-share'). Newer than UI5 1.71; declared per the property-171 policy. Previously undeclared` &&
                 ` because the property gate is blind to sap.tnt; found by the non-sap.m @since audit 2026-07-24.`
        post171 = `sap.tnt.InfoLabel.icon (@since 1.74) is used 1:1 (one InfoLabel carries icon='sap-icon://home-share'). Newer than UI5 1.71; declared per the property-171 policy. Previously undeclared because the` &&
                 ` property gate is blind to sap.tnt; found by the non-sap.m @since audit 2026-07-24.` ) ).

    text1 = `NOTE: The two toggles the original drives from its controller are now bound and dispatched instead of decorative. onToggleFooter flips DynamicPage.showFooter - the port binds showFooter to a boolean` &&
            ` member (an attribute the original view does not carry: it relies on the property default false and the controller's setter). toggleAreaPriority alternates DynamicPageTitle.areaShrinkRatio between the` &&
            ` property default '1:1.6:1.6' (read from the metadata in the original, verified in sap/f/DynamicPageTitle.js) and '1.6:1:1.6'; the port carries that as an expression binding over a boolean flag, so` &&
            ` the title reflects exactly the same two states. Both Button press wires now reach an on_event dispatcher that flips the flag - one backend round-trip per press, the abap2UI5 equivalent of the` &&
            ` controller call. Before this rework both were wired to TOGGLE_PRIO/TOGGLE_FOOTER events this class never dispatched (dead wires, pattern-lint dead-event-wire). // NOTE: f:DynamicPage with title` &&
            ` (heading, expanded/snapped tnt:InfoLabel, actions), pinnable header (ObjectAttributes), content (two long Texts) and footer. The footer message Button binds text and visible='{=`.
    text1 = text1 && ` !!${/MESSAGESLENGTH}}' to a model field (initial 0), reproducing the original {/messagesLength} wiring. // NOTE: The two long body Texts are SHORTENED representative Lorem, not the original's full` &&
            ` strings (the first runs to 1372 characters upstream and ends here at 631, the second 913 -> 410). Declared 2026-08-21 after the review sweep found it undeclared: neither structural_diff nor` &&
            ` data_fidelity compares literal text VALUES, so nothing else would ever catch it. App 138 declares the same shortening on the same shape.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.InfoLabel`                     name = `InfoLabelInDynamicPage`                        class = `z2ui5_cl_smpc_app_143` path = `src/01/05/z2ui5_cl_smpc_app_143.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.54`
        notes = text1 ) ).

    text1 = `NOTE: the controller's onPopinLayoutChanged (ComboBox change handler running a PopinLayout switch with a Block default) is expressed as bound properties instead of a round-trip (the recipe's 'prefer a` &&
            ` bindable property' rule (.claude/skills/port-a-sample/SKILL.md; the linter enforces it as settable-property-via-action), as app 009): the ComboBox's change attribute is dropped, its selectedKey is` &&
            ` bound two-way, and the Table gains a popinLayout expression binding that reproduces the switch including its Block default. The ComboBox is NOT pre-selected, corrected 2026-08-23: the original` &&
            ` carries a placeholder and no selectedKey, so the input starts empty; seeding popin_layout = 'Block' made setSelectedKey resolve the static item and write 'Block' into the input, suppressing the` &&
            ` placeholder. The popinLayout expression already falls through to 'Block' for an empty value, and sap.m.Table.popinLayout defaults to Block anyway - app 009, the checked reference for this idiom,` &&
            ` seeds nothing either. // NOTE: the original binds the InfoLabel colorScheme via a frontend formatter (Formatter.availableState: an already-classified Status string mapped to a colorScheme index`.
    text1 = text1 && ` 8/3/5/9). abap2UI5 being a thin frontend, that mapping is computed in ABAP model_init into a COLOR_SCHEME field and bound colorScheme="{COLOR_SCHEME}" (with path 'Status'), not via a frontend` &&
            ` formatter. Visually 1:1 with the original. // NOTE: the sample's local mock model/data.json (/ProductCollection, 9 rows) is moved verbatim into ABAP model_init on the one default model; all 9 rows` &&
            ` and all bound columns kept. Width/Depth/Height are TYPE string (display-only values in a text template with variable decimals like 3.1/1.8 - packed with fixed DECIMALS would add trailing zeros). Pure` &&
            ` prefix-drop, renders identically.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.InfoLabel`                     name = `InfoLabelInTable`                              class = `z2ui5_cl_smpc_app_192` path = `src/01/05/z2ui5_cl_smpc_app_192.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.54`
        notes = text1 ) ).

    text1 = `NOTE: The two toolbar buttons reproduce the original controller behaviour server-side: 'Toggle Collapse/Expand' flips NavigationList.expanded (bound to a boolean model field), 'Show/Hide SubItem 3'` &&
            ` flips subItemThree.visible. The original used byId().setExpanded/setVisible; here the properties are two-way bound and toggled on a backend round-trip. The 'expanded' attribute on NavigationList and` &&
            ` the 'visible' attribute on subItemThree are added to carry these bindings (the original set them imperatively). **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend +` &&
            ` real browser): 'Show/Hide SubItem 3' removes one of the two same-named sub items - scope, added 2026-08-23: the module asserts the count drops BELOW 2, so a run in which both disappeared would pass` &&
            ` just as well; 'exactly one' is not what it checks (the bound visible flag flipped on a round-trip) and 'Toggle Collapse/Expand' collapses the NavigationList so the item texts disappear - both toolbar` &&
            ` buttons reproduce the original byId().setVisible/setExpanded server-side.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.NavigationList`                name = `NavigationList`                                class = `z2ui5_cl_smpc_app_123` path = `src/01/05/z2ui5_cl_smpc_app_123.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: The two buttons reproduce the controller behaviour server-side: 'Toggle Collapse/Expand' flips SideNavigation.expanded (bound to a boolean model field, initial false as in the original) and` &&
            ` 'Show/Hide "Walked"' flips the 'walked' NavigationListItem.visible. The original used byId().setExpanded/setVisible; here the properties are two-way bound and toggled on a backend round-trip. The` &&
            ` 'visible' attribute added to the walked item carries that binding (the original toggled it imperatively). **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the` &&
            ` 'Toggle Collapse/Expand' round-trip flips the two-way bound expanded (the sapTntSideNavigationNotExpanded class disappears); the 'Show/Hide Walked' visible flip is the same two-way idiom but not` &&
            ` exercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_128.mjs). // POST-1.71: NavigationListItem.selectable (@since 1.116) is kept 1:1 from the original` &&
            ` (selectable=false on the two 'External Link' fixedItem entries). Newer than UI5 1.71; declared per the property-171 policy. Was previously undeclared, and the reason recorded here until 2026-08-23 -`.
    text1 = text1 && ` "the property gate is blind to sap.tnt (properties.json holds sap.m only)" - was false: ui5/properties.json carries 11 sap.tnt controls and lists` &&
            ` controls["sap.tnt.NavigationListItem"].members.selectable = "1.116" itself, as does the linter's own copy. The gate does see it; that is exactly why the declaration is needed to excuse the view-gates` &&
            ` finding. // POST-1.71: sap.tnt.NavigationListGroup (control @since 1.121) is used 1:1 for the 'New', 'Recently used' and 'Restricted' groups. Newer than UI5 1.71; declared per the property-171 policy` &&
            ` (control-level, app 152 Avatar precedent; the gate checks controls as well as members - the linter emits control-too-new straight off the class @since, and view-gates lists that type first, so this` &&
            ` is a declaration the gate reads, not a courtesy note), so the app needs UI5 >= 1.121 to render the groups. Found by the 2026-07-27 review sweep. // POST-1.71: the icon ``sap-icon://people-connected``` &&
            ` reached the SAP icon font in 1.96 and is kept 1:1 from the original V.view.xml, which names it on the 'People' NavigationListItem of the 'New' group. Newer than the 1.71 floor: there IconPool`.
    text1 = text1 && ` resolves nothing and the item renders with NO icon, silently - the app needs a UI5 release >= 1.96 to show it.`.
    text2 = `NavigationListItem.selectable (@since 1.116) is kept 1:1 from the original (selectable=false on the two 'External Link' fixedItem entries). Newer than UI5 1.71; declared per the property-171 policy.` &&
            ` Was previously undeclared, and the reason recorded here until 2026-08-23 - "the property gate is blind to sap.tnt (properties.json holds sap.m only)" - was false: ui5/properties.json carries 11` &&
            ` sap.tnt controls and lists controls["sap.tnt.NavigationListItem"].members.selectable = "1.116" itself, as does the linter's own copy. The gate does see it; that is exactly why the declaration is` &&
            ` needed to excuse the view-gates finding. // sap.tnt.NavigationListGroup (control @since 1.121) is used 1:1 for the 'New', 'Recently used' and 'Restricted' groups. Newer than UI5 1.71; declared per` &&
            ` the property-171 policy (control-level, app 152 Avatar precedent; the gate checks controls as well as members - the linter emits control-too-new straight off the class @since, and view-gates lists` &&
            ` that type first, so this is a declaration the gate reads, not a courtesy note), so the app needs UI5 >= 1.121 to render the groups. Found by the 2026-07-27 review sweep. // the icon`.
    text2 = text2 && ` ``sap-icon://people-connected`` reached the SAP icon font in 1.96 and is kept 1:1 from the original V.view.xml, which names it on the 'People' NavigationListItem of the 'New' group. Newer than the` &&
            ` 1.71 floor: there IconPool resolves nothing and the item renders with NO icon, silently - the app needs a UI5 release >= 1.96 to show it.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigation`                                class = `z2ui5_cl_smpc_app_128` path = `src/02/05/z2ui5_cl_smpc_app_128.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: onCollapseExpandPress (byId('sideNavigation').setExpanded(!expanded)) is reproduced by two-way binding SideNavigation.expanded to a boolean model field, seeded false exactly like the original` &&
            ` literal, and flipping it on the TOGGLE_EXPAND round-trip. // NOTE: quickActionPress dialog: the controller-built sap.m.Dialog is expressed as a core:FragmentDefinition shown via` &&
            ` client->popup_display. Its Dialog, Label, Input and Button controls are extra vs the archived V.view.xml, which declares none of them; the two Inputs are two-way bound (create_name/create_icon)` &&
            ` instead of read imperatively via Element.getElementById(...).getValue(). // NOTE: the Create button does sideNavigation.getItem().addItem( new NavigationListItem({ text, expanded: true, icon }) ), so` &&
            ` the main tnt:NavigationList is a BOUND aggregation here (the app-241 pattern): the four static tnt:NavigationListItem entries (Home/Building/Mileage/Transport) collapse into one bound template, so` &&
            ` three NavigationListItem controls are missing vs the original count, and creating appends a row with the same defaults ('New Navigation Item' / sap-icon://building). The rows bind with`.
    text1 = text1 && ` omit_initial_paths for EXPANDED so an item that does not set it keeps the control default. // POST-1.71: NavigationListItem.selectable (@since 1.116) is kept 1:1 (selectable=false on the Quick Create` &&
            ` and External Link fixedItem entries). @since verified in sap.tnt/src/sap/tnt/NavigationListItem.js. // POST-1.71: NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both` &&
            ` @since 1.133) are kept 1:1 on the Quick Create item. @since verified in sap.tnt/src/sap/tnt/NavigationListItem.js. // NOTE: not yet run in a system: the TOGGLE_EXPAND round-trip (collapse/expand),` &&
            ` the quick-create popup round-trip - NOT covered by the interaction module (scope corrected 2026-08-23) and the Create append (new row shows up in the bound NavigationList with the typed name/icon or` &&
            ` the defaults). **e2e-verified 2026-08-16** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_299.mjs). // POST-1.71: sap.tnt.NavigationListItem.expanded is kept 1:1 from the original`.
    text1 = text1 && ` view. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its JSDoc there carries @since 1.121, which is the version any scanner reads - the relocated-member residual` &&
            ` limit AGENTS section 5 names. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigationActions`                         class = `z2ui5_cl_smpc_app_299` path = `src/02/05/z2ui5_cl_smpc_app_299.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `NavigationListItem.selectable (@since 1.116) is kept 1:1 (selectable=false on the Quick Create and External Link fixedItem entries). @since verified in sap.tnt/src/sap/tnt/NavigationListItem.js. //` &&
                 ` NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both @since 1.133) are kept 1:1 on the Quick Create item. @since verified in` &&
                 ` sap.tnt/src/sap/tnt/NavigationListItem.js. // sap.tnt.NavigationListItem.expanded is kept 1:1 from the original view. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase` &&
                 ` and its JSDoc there carries @since 1.121, which is the version any scanner reads - the relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21` &&
                 ` (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it.` ) ).

    text1 = `NOTE: onToggleSideNav does Fragment.load('Popover.fragment.xml') + openBy(the ShellBar menu button). The port rebuilds the fragment 1:1 in popover_sidenav_display and shows it with` &&
            ` client->popover_display( xml, by_id ), anchoring on the menu button id transported as ${$parameters>/button}.getId() in the TOGGLE_SIDE_NAV t_arg. The original's toggle branch (close when already` &&
            ` open) is not reproduced: the backend cannot know whether the popover is still open (the user can dismiss it by clicking outside), so every menuButtonPressed re-opens it. // NOTE: onItemSelect sets` &&
            ` the target page's Text imperatively (oText.setText('Fired event to load page ' + key.replace('page',''))) and calls oNavCon.to(page). The port two-way binds the Text controls instead - the home page` &&
            ` keeps its own field (home_text, seeded with the original lorem-ipsum literal) and the twelve page1..page12 Texts share one field (page_text), which is observationally identical because the` &&
            ` NavContainer shows exactly one page and every page is reached through its own select. Navigation itself is the 1:1 frontend action follow_up_action( control_by_id, 'pageContainer', 'to', <page id> ).`.
    text1 = text1 && ` The added text bindings are extra attributes on the originally empty <Text/> elements. // NOTE: onQuickActionPress builds a sap.m.Dialog imperatively (new Dialog({...}).open()); expressed as a` &&
            ` core:FragmentDefinition shown via client->popup_display. Its Dialog, Text and two Button controls are extra vs the archived V.view.xml/Popover.fragment.xml. The Create button only closes the dialog,` &&
            ` exactly like the original. // POST-1.71: sap.m.Avatar (control @since 1.73) is kept 1:1 in the ShellBar's f:profile aggregation (initials 'SN'). Newer than UI5 1.71 (app 152 precedent, control-level` &&
            ` declaration). // POST-1.71: sap.tnt.NavigationListGroup (control @since 1.121) is used 1:1 for the 'Business Areas for selected user role' group. Newer than UI5 1.71. // POST-1.71:` &&
            ` NavigationListItem.selectable (@since 1.116) is kept 1:1 (selectable=false on Manufacturing management, Employee Services, Create, App Finder and Legal). // POST-1.71:` &&
            ` NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both @since 1.133) are kept 1:1 on the Create item; SideNavigation.design="Plain" (@since 1.134) is kept 1:1 on the`.
    text1 = text1 && ` popover's side navigation. (The 1.132 recorded here until 2026-08-23 understated the floor by two minors: both the property and the sap.tnt.SideNavigationDesign enum carry @since 1.134.) // NOTE: not` &&
            ` yet run in a system: the ShellBar menuButtonPressed -> anchored popover round-trip, the itemSelect -> NavContainer 'to' frontend action plus the bound page text, and the quick-create popup.` &&
            ` **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_301.mjs). // NOTE: two URLs are re-hosted per the repository's offline asset/host rule: the ShellBar homeIcon` &&
            ` './resources/sap/ui/documentation/sdk/images/logo_sap.png' is absolutized to https://sdk.openui5.org/resources/sap/ui/documentation/sdk/images/logo_sap.png (app 110 precedent), and the 'App Finder'` &&
            ` href https://openui5.hana.ondemand.com/demoapps points at the commercial SAPUI5 host, so it is served from https://sdk.openui5.org/demoapps instead. // POST-1.71: sap.tnt.SideNavigation.width @since` &&
            ` 1.120 is kept 1:1 from the original view. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs) - the app needs a UI5 release >= 1.120 for the width to apply. // POST-1.71:`.
    text1 = text1 && ` sap.tnt.NavigationListItem.expanded is kept 1:1 from the original view. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its JSDoc there carries @since 1.121, which` &&
            ` is the version any scanner reads - the relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it. //` &&
            ` POST-1.71: sap.tnt.NavigationListItem's press EVENT is @since 1.133 and is wired 1:1 from the original. It is not declared on NavigationListItem at all any more - it lives on the new base class` &&
            ` NavigationListItemBase, which is the relocated-member shape no gate can see (the blind-spot probe has no row for it either). The sidecar declared design and ariaHasPopup on the same item and stopped` &&
            ` there; the event that makes the item do anything was undeclared until 2026-08-23. The port already lives under src/02/05 for the other declarations. // NOTE: The ResponsivePopover carries` &&
            ` showHeader="{= ${device>/system/phone}}", an attribute the original fragment does not declare - it reproduces the controller's setShowHeader( Device.system.phone ). ResponsivePopover.showHeader`.
    text1 = text1 && ` defaults to true, so binding it is what makes both arms behave like the original. Declared 2026-08-23; the sibling added attributes were declared, this one was not. // NOTE: A NavContainer's position` &&
            ` is LIVE CONTROL STATE and does not survive a view rebuild. view_display( ) destroys the MAIN slot and XMLView.create builds a fresh control tree, so pageContainer comes back on the initialPage its` &&
            ` XML declares - while BOTH selectedkey (which the rebuilt popover's SideNavigation reads back) and page_text, which ITEM_SELECT writes onto the TARGET page and the home page does not even bind, being` &&
            ` two-way bound class state, survives the round trip and the draft. The app then showed one page while its own navigation control claimed another. Fixed 2026-08-27 with the app-000 re-issue idiom: the` &&
            ` end of view_display( ) sends the SAME control_by_id 'to' payload the ITEM_SELECT path last sent, guarded so a fresh view already sitting on the initialPage="home" issues nothing. The second` &&
            ` view_display( ) is reached through the framework's own bookmark restore (?app_start=<class>#/z2ui5-xapp-state=<draft>, the URL app_state_get_href( ) hands out): that request carries no frontend id,`.
    text1 = text1 && ` so the backend takes factory_first_start -> db_load(draft), check_on_navigated( ) is true while check_on_init( ) stays false - the ELSEIF branch, and the only way a port that calls no other app` &&
            ` renders twice. The interaction module asserts BOTH halves, the surviving key AND the re-issued page; asserting only the reset half would pass on a port that never navigated. Guard: selectedkey IS NOT` &&
            ` INITIAL AND selectedkey <> ``home``. The class already fixed exactly this asymmetry for its POPOVER (the bound selectedkey that survives the per-open fragment rebuild) and missed the main view one` &&
            ` level up. Measured 2026-08-27 on the built backend, BEFORE the fix: select "Sales Order" (key page7), restore that draft - the rebuilt view came back on mainView--home, i.e. showing the home lorem` &&
            ` ipsum, while page_text still read "Fired event to load page 7".`.
    text2 = `sap.m.Avatar (control @since 1.73) is kept 1:1 in the ShellBar's f:profile aggregation (initials 'SN'). Newer than UI5 1.71 (app 152 precedent, control-level declaration). //` &&
            ` sap.tnt.NavigationListGroup (control @since 1.121) is used 1:1 for the 'Business Areas for selected user role' group. Newer than UI5 1.71. // NavigationListItem.selectable (@since 1.116) is kept 1:1` &&
            ` (selectable=false on Manufacturing management, Employee Services, Create, App Finder and Legal). // NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both @since 1.133)` &&
            ` are kept 1:1 on the Create item; SideNavigation.design="Plain" (@since 1.134) is kept 1:1 on the popover's side navigation. (The 1.132 recorded here until 2026-08-23 understated the floor by two` &&
            ` minors: both the property and the sap.tnt.SideNavigationDesign enum carry @since 1.134.) // sap.tnt.SideNavigation.width @since 1.120 is kept 1:1 from the original view. Declared by policy 2026-08-21` &&
            ` (scripts/probes/post171-blindspot-probe.mjs) - the app needs a UI5 release >= 1.120 for the width to apply. // sap.tnt.NavigationListItem.expanded is kept 1:1 from the original view. The property`.
    text2 = text2 && ` PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its JSDoc there carries @since 1.121, which is the version any scanner reads - the relocated-member residual limit AGENTS section` &&
            ` 5 names. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it. // sap.tnt.NavigationListItem's press EVENT is @since 1.133 and is wired 1:1 from the` &&
            ` original. It is not declared on NavigationListItem at all any more - it lives on the new base class NavigationListItemBase, which is the relocated-member shape no gate can see (the blind-spot probe` &&
            ` has no row for it either). The sidecar declared design and ariaHasPopup on the same item and stopped there; the event that makes the item do anything was undeclared until 2026-08-23. The port already` &&
            ` lives under src/02/05 for the other declarations.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigationOverlayMode`                     class = `z2ui5_cl_smpc_app_301` path = `src/02/05/z2ui5_cl_smpc_app_301.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `POST-1.71: NavigationListItemBase.press event (@since 1.133) is the whole point of this sample; wired 1:1 on every NavigationListItem (press) to a backend ITEM_PRESS event. @since verified in` &&
            ` fork-openui5/src/sap.tnt/src/sap/tnt/NavigationListItemBase.js:74-79. Requires a UI5 release >= 1.133. // POST-1.71: The press event parameters ctrlKey/shiftKey/altKey/metaKey (@since 1.137) are` &&
            ` transported via ${$parameters>/ctrlKey} etc. in the ITEM_PRESS t_arg and echoed into the toast, exactly as the original itemPress reads them. @since verified NavigationListItemBase.js:88-109.` &&
            ` Requires a UI5 release >= 1.137 (the property gate is blind to sap.tnt event params). // POST-1.71: NavigationListItem.selectable (@since 1.116) is used 1:1 (selectable=false) on Link 1/Link 2, Quick` &&
            ` Create and External Link. @since verified NavigationListItem.js:101-103. // POST-1.71: NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both @since 1.133.0) are kept` &&
            ` 1:1 on the Quick Create item. @since verified NavigationListItem.js:131-139. // NOTE: itemPress preventDefault reproduced 1:1 since 2026-07-30: every NavigationListItem press wire carries`.
    text1 = text1 && ` s_ctrl-check_prevent_default = prevent_default, so when the 'preventDefaultCheckbox' is selected the handler cancels the item's built-in default client-side (oEvent.preventDefault() before the` &&
            ` round-trip - selection stays, href navigation is suppressed) exactly like the original. The flag is baked into the handler expression at render time, so the CheckBox got an added select wire` &&
            ` (PREVENT_TOGGLE, declared) that redraws the view when toggled; the redraw resets the client-side selection - a small toggle-time caveat the original (which never redraws) does not have. The checkbox` &&
            ` state stays two-way bound (selected={/PREVENT_DEFAULT}); the toast heading ('Default was prevented:' vs 'Item Pressed:') is composed server-side from the same flag. (Corrected 2026-08-23: the earlier` &&
            ` wording said the selection resets 'back to selectedKey=walked'. No item declares a key on either side, and _getUniqueKey falls back to the generated control id, so that selectedKey selects nothing -` &&
            ` in the original too. What the redraw loses is the user's selection, not a named resting state.) // NOTE: quickActionPress dialog: reproduced as a core:FragmentDefinition shown via`.
    text1 = text1 && ` client->popup_display - the controller-built sap.m.Dialog with two Label + Input pairs (Name/Icon, the Inputs two-way bound to create_name/create_icon) and Create/Cancel Buttons. These` &&
            ` Dialog/Label/Input/Button controls are extra vs the original view.xml, which declared none of them. **The Create button now works 1:1 (2026-08-05)**: the original does` &&
            ` sideNavigation.getItem().addItem( new NavigationListItem({ text, expanded, icon }) ), so the main NavigationList is a BOUND aggregation here (the app-085/203 pattern) and creating appends a row with` &&
            ` the same defaults ('New Navigation Item' / sap-icon://building). Consequently the five static tnt:NavigationListItem declarations became one template plus a nested one for Mileage's children - 4` &&
            ` instead of 9 against the original count - and the rows bind with omit_initial_paths so an item without an icon/href keeps the control default, while SELECTABLE stays outside that list because the two` &&
            ` external links must send their explicit false. The earlier 'dynamic addItem is not expressible' rationale is retired. // NOTE: The 2026-07-27 live check predates the 2026-07-30 prevent-default rewire`.
    text1 = text1 && ` (status reset checked -> generated per the invalidation rule): re-verify that with the checkbox set a press does NOT change the selection (eBP cancels the default) and still toasts 'Default was` &&
            ` prevented:', that with the checkbox clear selection changes normally, and that the PREVENT_TOGGLE redraw keeps working (expanded state and checkbox survive it via their two-way bindings).` &&
            ` onCollapseExpandPress (expanded two-way + TOGGLE_EXPAND round-trip), the ITEM_PRESS modifier-key transport and the popup round-trips were live-verified 2026-07-27. **e2e-verified 2026-07-30**` &&
            ` (transpiled-framework interaction, scripts/e2e-smoke.mjs): with the checkbox set, the PREVENT_TOGGLE redraw re-bakes the wires and pressing 'Building' toasts 'Default was prevented:' - the eBP wire` &&
            ` fires and round-trips; the visual no-selection-change and the popup paths remain for the live check. **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_241.mjs). // POST-1.71: sap.tnt.NavigationListItem.expanded is carried by the port's bound item template. Corrected 2026-08-21: this said 'kept 1:1 from the`.
    text1 = text1 && ` original view', which is not true - no NavigationListItem in the original declares ``expanded`` at all. The attribute exists here to carry the Create dialog's expanded item, and the SideNavigation's` &&
            ` own ``expanded`` (which the original DOES declare) is a different, base-version property needing no declaration. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its` &&
            ` JSDoc there carries @since 1.121, which is the version any scanner reads - the relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21` &&
            ` (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it. // NOTE: EXPANDED joins omit_initial_paths since 2026-08-21. The original declares ``expanded`` on NO NavigationListItem, so UI5's` &&
            ` own default (true) applies and 'Mileage' opens showing Driven and Walked; the port bound the attribute to a field no seeded row sets, and ajson emitted a real JSON false that collapsed it - the` &&
            ` sample's only parent item silently lost its children, with selectedKey pointing at a hidden child. Omitting the INITIAL value restores the default while the Create dialog's explicit abap_true still`.
    text1 = text1 && ` travels. Found by the review sweep. // NOTE: The nested items aggregation carried no binding on the row template between the 2026-08-05 static-to-bound rewrite and 2026-08-23. The outer` &&
            ` NavigationList bound {/T_NAV_ITEMS}, but the inner tnt:items element held a STATIC NavigationListItem, so it was cloned with every row and its {TEXT} resolved in the row's own context: Mileage's two` &&
            ` children (Driven, Walked) never rendered, and all five rows grew one spurious child bearing their own label. The row template now carries items="{ITEMS}", the shape app 167 uses on the same control.` &&
            ` No gate could see it - structural-diff was satisfied by the declared control count, and the interaction module only presses one top-level item.`.
    text2 = `NavigationListItemBase.press event (@since 1.133) is the whole point of this sample; wired 1:1 on every NavigationListItem (press) to a backend ITEM_PRESS event. @since verified in` &&
            ` fork-openui5/src/sap.tnt/src/sap/tnt/NavigationListItemBase.js:74-79. Requires a UI5 release >= 1.133. // The press event parameters ctrlKey/shiftKey/altKey/metaKey (@since 1.137) are transported via` &&
            ` ${$parameters>/ctrlKey} etc. in the ITEM_PRESS t_arg and echoed into the toast, exactly as the original itemPress reads them. @since verified NavigationListItemBase.js:88-109. Requires a UI5 release` &&
            ` >= 1.137 (the property gate is blind to sap.tnt event params). // NavigationListItem.selectable (@since 1.116) is used 1:1 (selectable=false) on Link 1/Link 2, Quick Create and External Link. @since` &&
            ` verified NavigationListItem.js:101-103. // NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both @since 1.133.0) are kept 1:1 on the Quick Create item. @since verified` &&
            ` NavigationListItem.js:131-139. // sap.tnt.NavigationListItem.expanded is carried by the port's bound item template. Corrected 2026-08-21: this said 'kept 1:1 from the original view', which is not`.
    text2 = text2 && ` true - no NavigationListItem in the original declares ``expanded`` at all. The attribute exists here to carry the Create dialog's expanded item, and the SideNavigation's own ``expanded`` (which the` &&
            ` original DOES declare) is a different, base-version property needing no declaration. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its JSDoc there carries @since` &&
            ` 1.121, which is the version any scanner reads - the relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); no gate can` &&
            ` raise it.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigationPressEvent`                      class = `z2ui5_cl_smpc_app_241` path = `src/02/05/z2ui5_cl_smpc_app_241.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `IMPROVISED: The control-returning item factory is resolved server-side. The original binds the main list as items="{path: '/navigation', factory: '.navigationItemFactory'}" - a JS factory that returns` &&
            ` a NavigationListGroup for a type=group row and a NavigationListItem otherwise; abap2UI5 cannot ship a frontend factory function. The port declares the /navigation top level statically (its shape is` &&
            ` fixed in the mock: one plain Home item, then the two groups) and keeps everything below model-bound: each tnt:NavigationListGroup binds its items table ({/T_GROUP1} / {/T_GROUP2}) with the factory's` &&
            ` _createNavigationItem as a bound tnt:NavigationListItem template (nested {ITEMS} sub-template and sap.m.ObjectStatus tag with the original's visible="{= !!${TAGTEXT} }" expression, both levels). So` &&
            ` the port view carries extra controls the original view built at runtime - NavigationListGroup (2), NavigationListItem (Home plus the two-level template per group), ObjectStatus tags - and the main`.
    text1 = text1 && ` NavigationList drops the original's items/factory attribute in favour of the static skeleton. On the data side this means no single mock array corresponds to a port table: the group tables carry rows` &&
            ` from NESTED levels of /navigation - t_group1_full's first row is Favorites ITSELF, carrying 'My Accounts' and 'My Orders' in its nested items - those two live at navigation[1].items[0].items[] in` &&
            ` data.json. (This sentence described them as t_group1_full's own first two rows until 2026-08-24, which is not the table's shape.) data-fidelity compares a block against the one best-matching FLAT` &&
            ` array and picked /fixedNavigation for this one, so those two verbatim rows read as invented data; naming them here is what makes the declaration true rather than merely quiet. // IMPROVISED: The` &&
            ` search keeps filtered fixed items in the fixedItem section. The original's _executeSearch merges navigation and fixedNavigation into ONE filtered array, sets it as /navigation and empties` &&
            ` /fixedNavigation, so a matching fixed item is re-rendered inside the scrollable main list. The port filters each list in place instead: the group tables and the fixed table ({/T_FIXED} instead of`.
    text1 = text1 && ` {/fixedNavigation}) are reduced to the matching rows (title or tagText contains, case-insensitive; a matching parent keeps all children; empty groups and a non-matching Home hide via bound visible` &&
            ` flags). Same visible rows, but a matching fixed item stays pinned at the bottom instead of moving into the main list. // NOTE: Thin frontend: onLiveChange's _executeSearch and setHighlightedText` &&
            ` become one LIVE_CHANGE round-trip that filters the tables in ABAP and updates the model - NavigationList.highlightedText is a bindable property, so it shares the search field's two-way bound value` &&
            ` instead of the setter. onSearch's announceSearchMatchCount stays a control method (no bindable equivalent) and is invoked via follow_up_action( control_by_id, navigationList,` &&
            ` announceSearchMatchCount, <count> ) with the count computed in ABAP like the original's recursive title-only _countItems. abap2UI5 serializes round-trips, so very fast typing can skip intermediate` &&
            ` filter states; the wire carries s_ctrl-check_queue_last (since 2026-09-19), so the LAST keystroke is never dropped and the filter ends on the typed value, and the search field's value is two-way`.
    text1 = text1 && ` bound so the filter state survives the round-trip. The wire carries s_ctrl-check_no_busy alongside it (abap2UI5 #2759, in the pin since the bump to ad0a2dd2): the round-trip and the busy STATE are` &&
            ` unchanged, only the full-screen overlay stays down. That is the second half a per-keystroke wire needs - check_queue_last stops the keystrokes from being lost, check_no_busy stops the overlay from` &&
            ` flashing over the field while they are typed. It was the visible half of the gap: the FIRST trip raises the indicator after the usual delay, but every keystroke landing on a trip already in flight` &&
            ` raises it with NO delay, so from the second character on the original's round-trip-free typing was covered by an overlay the demo kit sample never shows. // NOTE: onMenuTogglePress toggles` &&
            ` ToolPage.sideExpanded imperatively; the property is bindable, so the port two-way binds it (sideExpanded, an attribute the original view does not declare) and flips it on the MENU_TOGGLE round-trip -` &&
            ` collapsing also clears the bound search value and restores the unfiltered lists, like the original's reset of the search field, the highlight and the model (app 302 precedent). // NOTE: onItemSelect`.
    text1 = text1 && ` does navContainer.to(createId(key)) guarded by getPage(); the port transports ${$parameters>/item}.getKey() and calls the same method through follow_up_action( control_by_id, navContainer, to, <key>` &&
            ` ) guarded by the four page ids (home, myAccounts, myOrders, CustomerManagement) - the NavContainer has no bindable current-page property (app 302 precedent). onItemPress reads the pressed item's key` &&
            ` from ${$source>/key} (the factory wires press on every created item, the port wires it on the Home item, both group templates, the nested sub-item template and the fixed template); only the` &&
            ` quickCreate key acts, opening the controller-built Create Item Dialog 1:1 via popup_display (type Message, a content Text and two Button controls - the Emphasized Create and the Cancel - both just` &&
            ` close, so the port view carries that extra Dialog/Text/Button set). // NOTE: The ENABLED, HASEXPANDER, EXPANDED, SELECTABLE, ARIAHASPOPUP, DESIGN and TAGSTATE fields the templates bind are partly` &&
            ` absent from model/data.json rows; every ABAP row carries the UI5 property default explicitly (true / true / true / true / None / Default / None) - a flat row would otherwise serialize an empty value,`.
    text1 = text1 && ` which UI5 rejects on the genuinely enum-typed properties - ARIAHASPOPUP (sap.ui.core.aria.HasPopup) and DESIGN (sap.tnt.NavigationListItemDesign) - and which would override the boolean defaults.` &&
            ` TAGSTATE is NOT one of them: sap.m.ObjectStatus.state is declared { type: 'string', defaultValue: ValueState.None }, so an empty value there would not be rejected. Seeding None is still right, the` &&
            ` reason just does not extend to that field. The header Image keeps the original's relative src ./images/SAP_Logo.png verbatim (the sample folder itself ships no such file upstream). // POST-1.71: The` &&
            ` sample's core feature is newer than the 1.71 floor and is kept 1:1: sap.tnt.SideNavigationSearchField (control @since 1.151), the SideNavigation.filterSection aggregation that hosts it (@since` &&
            ` 1.151), NavigationList.highlightedText (@since 1.151), the NavigationList.announceSearchMatchCount control method (@since 1.151, invoked via follow_up_action - a method is invisible to the property` &&
            ` gate, declared by policy) and the SearchField ariaControls association (@since 1.150). The app needs UI5 >= 1.151; the repo's @openui5 runtime pin was raised from 1.150.0 to 1.151.0 with this port so`.
    text1 = text1 && ` the render and e2e gates exercise it. // POST-1.71: Kept 1:1 but newer than UI5 1.71 in the navigation tree: sap.tnt.NavigationListGroup (control @since 1.121, the two group rows);` &&
            ` NavigationListItem.selectable (@since 1.116); the tag aggregation (@since 1.149) with its sap.m.ObjectStatus and the IndicationColor enum values Indication15/16/17/18/20 (@since 1.120); design and` &&
            ` ariaHasPopup (@since 1.133.0, incl. the design=Action / ariaHasPopup=Dialog values on the Quick Create row); and the press EVENT (@since 1.133), wired 1:1 on all six item templates - it is not` &&
            ` declared on NavigationListItem at all any more, it lives on the new base class NavigationListItemBase, which is the relocated-member shape no gate can see. That last one was named only inside NOTEs` &&
            ` until 2026-08-24, and a NOTE describing what a member DOES is not a declaration that it is too new - the sibling tnt ports 300 and 301 closed the same gap on 2026-08-23 and this port was missed.` &&
            ` expanded also lives on that base class and reads as @since 1.121, predating 1.71 on NavigationListItem itself, declared per the relocated-member note. hasExpander is NOT in that group: it carries no`.
    text1 = text1 && ` @since tag at all on NavigationListItemBase, so it reads as base version - naming it here was an over-declaration (harmless, since the port's floor is already set by the members above, but wrong` &&
            ` about the source). // NOTE: not yet run in a system: the LIVE_CHANGE filter round-trip (bound group tables, visible flags, highlightedText), the SEARCH announceSearchMatchCount frontend action, the` &&
            ` to-page action (which arrives as ITEM_PRESS, see above), the quickCreate popup and the MENU_TOGGLE collapse-resets-search path. **e2e-verified 2026-08-21** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_407.mjs) - with two halves NOT covered by that module. highlightedText is never asserted: it checks which rows survive the filter, never that any text is` &&
            ` emphasized, so deleting the highlightedText binding leaves it green. And the selectable guard is only covered positively: it clicks 'My Orders' (selectable, must navigate) and asserts the page` &&
            ` appears, but never clicks 'Customer Management' (selectable:false) to assert that nothing happens - which is the whole reason that guard exists. Relaxing the ELSEIF on get_event_arg( 2 ) to an`.
    text1 = text1 && ` unconditional ELSE passes the module unchanged. // NOTE: announceSearchMatchCount receives the match count as a STRING. It is not in the framework's CONTROL_METHODS, so no arg kinds are registered` &&
            ` and castArgAuto passes anything that is not X/true/false through unchanged. NavigationList._announceSearchMatchCount then does ``iCount === 1 ? SIDE_NAVIGATION_SEARCH_MATCH_FOUND :` &&
            ` SIDE_NAVIGATION_SEARCH_MATCHES_FOUND``, and "1" === 1 is false - so a single match is announced with the plural text where the original announces the singular. Closing this needs an ["int"] entry for` &&
            ` the method upstream, the same shape as scrollToIndex; noted 2026-08-21. // NOTE: Each item's press wire carries ${$source>/selectable} beside its key, and ITEM_PRESS navigates only when it is true.` &&
            ` NavigationListItem._selectItem fires ``select`` unconditionally but reaches the list's _selectItem - and so itemSelect, and so the original's navigation - only if getSelectable( ) is true, while` &&
            ` ``press`` fires either way. Customer Management is selectable:false in the mock, so upstream it navigates nowhere; the port navigated it until 2026-08-21, making a page unreachable in the original` &&
            ` reachable here.`.
    text2 = `The sample's core feature is newer than the 1.71 floor and is kept 1:1: sap.tnt.SideNavigationSearchField (control @since 1.151), the SideNavigation.filterSection aggregation that hosts it (@since` &&
            ` 1.151), NavigationList.highlightedText (@since 1.151), the NavigationList.announceSearchMatchCount control method (@since 1.151, invoked via follow_up_action - a method is invisible to the property` &&
            ` gate, declared by policy) and the SearchField ariaControls association (@since 1.150). The app needs UI5 >= 1.151; the repo's @openui5 runtime pin was raised from 1.150.0 to 1.151.0 with this port so` &&
            ` the render and e2e gates exercise it. // Kept 1:1 but newer than UI5 1.71 in the navigation tree: sap.tnt.NavigationListGroup (control @since 1.121, the two group rows); NavigationListItem.selectable` &&
            ` (@since 1.116); the tag aggregation (@since 1.149) with its sap.m.ObjectStatus and the IndicationColor enum values Indication15/16/17/18/20 (@since 1.120); design and ariaHasPopup (@since 1.133.0,` &&
            ` incl. the design=Action / ariaHasPopup=Dialog values on the Quick Create row); and the press EVENT (@since 1.133), wired 1:1 on all six item templates - it is not declared on NavigationListItem at`.
    text2 = text2 && ` all any more, it lives on the new base class NavigationListItemBase, which is the relocated-member shape no gate can see. That last one was named only inside NOTEs until 2026-08-24, and a NOTE` &&
            ` describing what a member DOES is not a declaration that it is too new - the sibling tnt ports 300 and 301 closed the same gap on 2026-08-23 and this port was missed. expanded also lives on that base` &&
            ` class and reads as @since 1.121, predating 1.71 on NavigationListItem itself, declared per the relocated-member note. hasExpander is NOT in that group: it carries no @since tag at all on` &&
            ` NavigationListItemBase, so it reads as base version - naming it here was an over-declaration (harmless, since the port's floor is already set by the members above, but wrong about the source).`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigationSearch`                          class = `z2ui5_cl_smpc_app_407` path = `src/02/05/z2ui5_cl_smpc_app_407.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `POST-1.71: sap.tnt.NavigationListItem.selectable (@since 1.116) is used 1:1 on the unselectable parent items (Building/Mileage/Transport) and the fixed External Link item - it is the whole point of` &&
            ` this sample. Kept per the members-newer-than-1.71 fidelity policy; @since verified by hand against fork-openui5/src/sap.tnt/src/sap/tnt/NavigationListItem.js line 101-103 (property gate is blind to` &&
            ` sap.tnt). Requires a UI5 release >= 1.116 to render. // NOTE: onCollapseExpandPress reproduced server-side: SideNavigation.expanded is two-way bound (initial false as in the original` &&
            ` expanded="false") and flipped on a backend round-trip (TOGGLE_EXPAND) instead of the original imperative byId().setExpanded(!expanded). Same technique as app 128. The literal expanded="false"` &&
            ` becoming a {/EXPANDED} binding needs a live render check. **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the Toggle Collapse/Expand round-trip` &&
            ` expands the SideNavigation - the sub items 'Office 01'/'Office 02' appear, so the two-way bound expanded flag really flipped in ABAP. // NOTE: onItemSelect reproduced roundtrip-free: the`.
    text1 = text1 && ` SideNavigation itemSelect event fires a client-composed MessageToast (control_global MESSAGE_TOAST/show, template 'Item selected: {0}' filled by the client-resolved ${$parameters>/item}.getText()) -` &&
            ` the 1:1 equivalent of the original MessageToast.show(``Item selected: ${sText}``). Same idiom as app 060; CAPABILITIES marks it expressible, live render/click check pending. **e2e-verified` &&
            ` 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): selecting 'Office 01' raises the client-composed 'Item selected: Office 01' toast.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigationUnselectableParents`             class = `z2ui5_cl_smpc_app_172` path = `src/02/05/z2ui5_cl_smpc_app_172.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.tnt.NavigationListItem.selectable (@since 1.116) is used 1:1 on the unselectable parent items (Building/Mileage/Transport) and the fixed External Link item - it is the whole point of this sample.` &&
                 ` Kept per the members-newer-than-1.71 fidelity policy; @since verified by hand against fork-openui5/src/sap.tnt/src/sap/tnt/NavigationListItem.js line 101-103 (property gate is blind to sap.tnt).` &&
                 ` Requires a UI5 release >= 1.116 to render.` ) ).

    text1 = `NOTE: The 'Toggle Collapse/Expand' button flips SideNavigation.expanded (bound to a boolean model field, initial true as in the original). The original toggled it imperatively via byId().setExpanded;` &&
            ` the property is two-way bound here and toggled on a backend round-trip. Every tnt:tag ObjectStatus (IndicationColor states 15-20, inverted) and the NavigationListGroup / fixedItem structure are` &&
            ` reproduced 1:1. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the Toggle Collapse/Expand round-trip collapses the two-way bound SideNavigation (the` &&
            ` NotExpanded class appears). // POST-1.71: Two sap.tnt.NavigationListItem members newer than UI5 1.71 are kept 1:1: selectable (@since 1.116, selectable=false on the Favorites parent item) and the tag` &&
            ` aggregation (@since 1.149, a sap.m.ObjectStatus per item, used 8x). Declared per the property-171 policy. Previously undeclared because the property gate is blind to sap.tnt (properties.json holds` &&
            ` sap.m only); selectable found by the app-172 cold-read probe and tag by the non-sap.m @since audit, 2026-07-24. // POST-1.71: Kept 1:1 but newer than UI5 1.71 and invisible to the property gate:`.
    text1 = text1 && ` sap.tnt.NavigationListGroup (control @since 1.121, the 'Business Operations' group); NavigationListItem.expanded (expanded=true on the Favorites and Projects items - reads as @since 1.121 because the` &&
            ` property was relocated to the newer base class NavigationListItemBase, the property itself predates 1.71, declared per the relocated-member note); and the IndicationColor enum values Indication15,` &&
            ` Indication16, Indication17, Indication18, Indication19, Indication20 (@since 1.120) carried by all 8 ObjectStatus tag states (enum values are invisible at the attribute-name level). The app needs UI5` &&
            ` >= 1.149 to render as the sample does: the group is 1.121, but the tag aggregation this port uses eight times is @since 1.149, and on 1.121-1.148 every tnt:tag is an unknown aggregation and all eight` &&
            ` status tags silently vanish. (Said 1.121 here until 2026-08-23, contradicting this sidecar's own earlier deviation and the port's inline comment, both of which name 1.149.) Found by the 2026-07-27` &&
            ` review sweep.`.
    text2 = `Two sap.tnt.NavigationListItem members newer than UI5 1.71 are kept 1:1: selectable (@since 1.116, selectable=false on the Favorites parent item) and the tag aggregation (@since 1.149, a` &&
            ` sap.m.ObjectStatus per item, used 8x). Declared per the property-171 policy. Previously undeclared because the property gate is blind to sap.tnt (properties.json holds sap.m only); selectable found` &&
            ` by the app-172 cold-read probe and tag by the non-sap.m @since audit, 2026-07-24. // Kept 1:1 but newer than UI5 1.71 and invisible to the property gate: sap.tnt.NavigationListGroup (control @since` &&
            ` 1.121, the 'Business Operations' group); NavigationListItem.expanded (expanded=true on the Favorites and Projects items - reads as @since 1.121 because the property was relocated to the newer base` &&
            ` class NavigationListItemBase, the property itself predates 1.71, declared per the relocated-member note); and the IndicationColor enum values Indication15, Indication16, Indication17, Indication18,` &&
            ` Indication19, Indication20 (@since 1.120) carried by all 8 ObjectStatus tag states (enum values are invisible at the attribute-name level). The app needs UI5 >= 1.149 to render as the sample does:`.
    text2 = text2 && ` the group is 1.121, but the tag aggregation this port uses eight times is @since 1.149, and on 1.121-1.148 every tnt:tag is an unknown aggregation and all eight status tags silently vanish. (Said` &&
            ` 1.121 here until 2026-08-23, contradicting this sidecar's own earlier deviation and the port's inline comment, both of which name 1.149.) Found by the 2026-07-27 review sweep.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigationWithTags`                        class = `z2ui5_cl_smpc_app_132` path = `src/02/05/z2ui5_cl_smpc_app_132.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: onCollapseExpandPress (byId('sideNavigation').setExpanded(!expanded)) is reproduced by two-way binding SideNavigation.expanded to a boolean model field, seeded true exactly like the original` &&
            ` literal, and flipping it on the TOGGLE_EXPAND round-trip. // NOTE: quickActionPress dialog: the controller-built sap.m.Dialog is expressed as a core:FragmentDefinition shown via` &&
            ` client->popup_display. Its Dialog, Label, Input and Button controls are extra vs the archived V.view.xml, which declares none of them; the two Inputs are two-way bound (create_name/create_icon)` &&
            ` instead of read imperatively via Element.getElementById(...).getValue(). // IMPROVISED: the Create button of the quick-create dialog does sideNavigation.getItem().addItem( new NavigationListItem(...)` &&
            ` ) in the original. Here it only closes the dialog: the main tnt:NavigationList mixes tnt:NavigationListItem and tnt:NavigationListGroup children, and abap2UI5 binds an aggregation with ONE template` &&
            ` control, so a bound list (the app-241/299 pattern) cannot reproduce that mixed structure. The 24 static NavigationListItem entries and both NavigationListGroups are kept 1:1 instead - view fidelity`.
    text1 = text1 && ` wins over the append behaviour, which is the loss declared here. // POST-1.71: sap.tnt.NavigationListGroup (control @since 1.121) is used 1:1 for the 'System & Administration Management' and` &&
            ` 'Business operations' groups. Newer than UI5 1.71; the app needs UI5 >= 1.121 to render them. // POST-1.71: NavigationListItem.selectable (@since 1.116) is kept 1:1 (selectable=false on the` &&
            ` training-portal, service-management, financial-reports, CRM, Quick Create and SAP Support entries). // POST-1.71: NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both` &&
            ` @since 1.133) are kept 1:1 on the Quick Create item. // NOTE: not yet run in a system: the TOGGLE_EXPAND round-trip (collapse/expand of the 20rem side navigation) and the quick-create popup` &&
            ` round-trip - NOT covered by the interaction module (scope corrected 2026-08-23). **e2e-verified 2026-08-16** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_300.mjs). // POST-1.71:` &&
            ` sap.tnt.SideNavigation.width @since 1.120 is kept 1:1 from the original view. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs) - the app needs a UI5 release >= 1.120 for the`.
    text1 = text1 && ` width to apply. // POST-1.71: sap.tnt.NavigationListItem.expanded is kept 1:1 from the original view. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its JSDoc` &&
            ` there carries @since 1.121, which is the version any scanner reads - the relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21` &&
            ` (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it. // POST-1.71: sap.tnt.NavigationListItem's press EVENT is @since 1.133 and is wired 1:1 from the original. It is not declared on` &&
            ` NavigationListItem at all any more - it lives on the new base class NavigationListItemBase, which is the relocated-member shape no gate can see (the blind-spot probe has no row for it either). The` &&
            ` sidecar declared design and ariaHasPopup on the same item and stopped there; the event that makes the item do anything was undeclared until 2026-08-23. The port already lives under src/02/05 for the` &&
            ` other declarations.`.
    text2 = `sap.tnt.NavigationListGroup (control @since 1.121) is used 1:1 for the 'System & Administration Management' and 'Business operations' groups. Newer than UI5 1.71; the app needs UI5 >= 1.121 to render` &&
            ` them. // NavigationListItem.selectable (@since 1.116) is kept 1:1 (selectable=false on the training-portal, service-management, financial-reports, CRM, Quick Create and SAP Support entries). //` &&
            ` NavigationListItem.design="Action" and NavigationListItem.ariaHasPopup="Dialog" (both @since 1.133) are kept 1:1 on the Quick Create item. // sap.tnt.SideNavigation.width @since 1.120 is kept 1:1` &&
            ` from the original view. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs) - the app needs a UI5 release >= 1.120 for the width to apply. //` &&
            ` sap.tnt.NavigationListItem.expanded is kept 1:1 from the original view. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its JSDoc there carries @since 1.121, which` &&
            ` is the version any scanner reads - the relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it. //`.
    text2 = text2 && ` sap.tnt.NavigationListItem's press EVENT is @since 1.133 and is wired 1:1 from the original. It is not declared on NavigationListItem at all any more - it lives on the new base class` &&
            ` NavigationListItemBase, which is the relocated-member shape no gate can see (the blind-spot probe has no row for it either). The sidecar declared design and ariaHasPopup on the same item and stopped` &&
            ` there; the event that makes the item do anything was undeclared until 2026-08-23. The port already lives under src/02/05 for the other declarations.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.SideNavigation`                name = `SideNavigationWrapping`                        class = `z2ui5_cl_smpc_app_300` path = `src/02/05/z2ui5_cl_smpc_app_300.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: the two 'Prоduct Name' Title texts (V.view.xml:19, the productName Title, and :72 - there is no Menu and no Select in this sample; the sidecar said "menu/select" until 2026-08-23) contain` &&
            ` CYRILLIC SMALL LETTER O (U+043E) in place of the Latin o - reproduced verbatim from the original V.view.xml, which carries the same homoglyph (upstream defect kept for 1:1 fidelity; do not ASCII-fix` &&
            ` it). // NOTE: The SAP-logo Image and profile Avatar presses show client-side MessageToasts ('Logo pressed!' / 'Avatar pressed!'), matching the original onLogoPressed / onAvatarPressed. The original's` &&
            ` Device.media handler does two things per screen range, and the port reproduces neither: it toggles productName/secondTitle/searchField/searchButton visibility, AND every one of its four branches` &&
            ` raises a MessageToast ('Screen width is corresponding to <range>'). onInit calls the handler immediately, and the StdExt range set always resolves to one of the four names, so the ORIGINAL SHOWS A` &&
            ` TOAST ON EVERY LOAD and the port shows none - a user-visible message lost at load time, not only on a breakpoint crossing. The visibility branch keeps its static initial state (searchButton`.
    text1 = text1 && ` visible='false'). Three of the four ranges could be expressed declaratively through the {device>/media/range} model (CAPABILITIES, shipped 2026-08-05, Std set: Phone/Tablet/Desktop); LargeDesktop is` &&
            ` the 1440px point of StdExt and has no counterpart, and a load-time toast has no declarative form at all. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the` &&
            ` logo Image press toasts 'Logo pressed!'; the Avatar press is the identical wire but not exercised, and the Device.media visibility branch stays out of scope. **e2e-verified 2026-08-04** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_134.mjs). // NOTE: Both ToolHeaders, all OverflowToolbarLayoutData priorities/groups, the ToolHeaderUtilitySeparator and the OverflowToolbarButtons` &&
            ` are reproduced 1:1, including the original's Cyrillic-o typo in 'Prоduct Name'. The SAP_Logo.png and Woman_avatar_01.png srcs are the original's host-relative test-resources/sap/tnt/images/ paths` &&
            ` rewritten to the OpenUI5 host https://sdk.openui5.org/test-resources/sap/tnt/images/ per the runtime asset-URL rule (app 152 precedent). // POST-1.71: sap.m.Avatar (control @since 1.73) is used 1:1`.
    text1 = text1 && ` as the profile avatar in both ToolHeaders (src image, displaySize XS, press). Newer than UI5 1.71; declared per the property-171 policy (control-level, app 152 precedent), so the app needs UI5 >=` &&
            ` 1.73 to render the avatars. Found by the 2026-07-27 review sweep. // POST-1.71: the icon ``sap-icon://da`` reached the SAP icon font in 1.96 and is kept 1:1 from the original V.view.xml, which names` &&
            ` it on the 'Joule' OverflowToolbarButton. Newer than the 1.71 floor: there IconPool resolves nothing and the button renders with NO icon, silently - the app needs a UI5 release >= 1.96 to show it.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.ToolHeader`                    name = `ToolHeader`                                    class = `z2ui5_cl_smpc_app_134` path = `src/02/05/z2ui5_cl_smpc_app_134.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar (control @since 1.73) is used 1:1 as the profile avatar in both ToolHeaders (src image, displaySize XS, press). Newer than UI5 1.71; declared per the property-171 policy (control-level,` &&
                 ` app 152 precedent), so the app needs UI5 >= 1.73 to render the avatars. Found by the 2026-07-27 review sweep. // the icon ``sap-icon://da`` reached the SAP icon font in 1.96 and is kept 1:1 from the` &&
                 ` original V.view.xml, which names it on the 'Joule' OverflowToolbarButton. Newer than the 1.71 floor: there IconPool resolves nothing and the button renders with NO icon, silently - the app needs a` &&
                 ` UI5 release >= 1.96 to show it.` ) ).

    text1 = `NOTE: The three home Buttons reset their sibling IconTabHeader again since 2026-08-05. The original reaches into the DOM (event.oSource.getParent().getDomRef()...) only to FIND that header - each` &&
            ` button knows it statically here - and selectedKey is a BINDABLE property, so the reset is a bound value rather than a frontend action (the prefer-a-bindable-property rule, which the linter enforces):` &&
            ` each header's selectedKey is two-way bound, the press round-trips and the backend writes 'invalidKey' back. The three headers keep that as their seeded value, as in the original view. // POST-1.71:` &&
            ` IconTabFilter interactionMode='SelectLeavesOnly' (property since 1.121) is kept 1:1 on the parent filters of the second ToolHeader's IconTabHeader (iconTabHeaderOneClickArea); requires a UI5 release` &&
            ` >= 1.121. // POST-1.71: IconTabFilter items (the nested sub-filter aggregation, since 1.77 per IconTabFilter.js JSDoc) is kept 1:1 in the second and third ToolHeaders' IconTabHeaders (nested` &&
            ` User/Identity/Monitoring filters); requires a UI5 release >= 1.77. Aggregation-level member, invisible to the property gate (it scans attributes only) - declared by policy, found in review`.
    text1 = text1 && ` 2026-07-27. // NOTE: Fully static view reproduced 1:1: three tnt:ToolHeaders, each with a home Button, an IconTabHeader (mode='Inline', backgroundDesign='Transparent', selectedKey='invalidKey') with` &&
            ` OverflowToolbarLayoutData, search/comment Buttons and a MenuButton with a Menu (Edit/Save). The second/third ToolHeaders use nested IconTabFilter items; no bound data.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.ToolHeader`                    name = `ToolHeaderIconTabHeader`                       class = `z2ui5_cl_smpc_app_221` path = `src/02/05/z2ui5_cl_smpc_app_221.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = `IconTabFilter interactionMode='SelectLeavesOnly' (property since 1.121) is kept 1:1 on the parent filters of the second ToolHeader's IconTabHeader (iconTabHeaderOneClickArea); requires a UI5 release` &&
                 ` >= 1.121. // IconTabFilter items (the nested sub-filter aggregation, since 1.77 per IconTabFilter.js JSDoc) is kept 1:1 in the second and third ToolHeaders' IconTabHeaders (nested` &&
                 ` User/Identity/Monitoring filters); requires a UI5 release >= 1.77. Aggregation-level member, invisible to the property gate (it scans attributes only) - declared by policy, found in review` &&
                 ` 2026-07-27.` ) ).

    text1 = `NOTE: NavigationListItem.selectable is the expression binding {= ${items}.length > 3} in the original; per the thin-frontend rule that presentation logic is computed in ABAP into a flat 'selectable'` &&
            ` field (children count > 3) and bound directly. **Sidecar corrected 2026-08-05**: the text still described the port as it looked before its rework and claimed the controller handlers were STATIC` &&
            ` toasts - they are not, and have not been for a while. onSideNavButtonPress is the two-way bound ToolPage.sideExpanded (prefer-a-bindable-property); onItemPress carries the item text into a` &&
            ` client-composed toast (${$parameters>/item}.getText(), the app-060/172 idiom); onItemSelect navigates the NavContainer roundtrip-free through control_by_id 'to' with the key resolved on the client;` &&
            ` handleUserNamePress rebuilds the controller's Popover (no header, Bottom, three transparent buttons) and opens it at the pressed button; onQuickActionPress reproduces the design=Action guard and the` &&
            ` Create Item dialog. What remains is the selectable computation above - a bound field instead of the original's expression - which is the thin-frontend rule working. // POST-1.71: Several members`.
    text1 = text1 && ` newer than UI5 1.71 are kept 1:1 from the original. sap.tnt.NavigationListItem: selectable (@since 1.116), design (@since 1.133.0, sap.tnt.NavigationListItemDesign), press (event, @since 1.133 on` &&
            ` NavigationListItemBase), ariaHasPopup (@since 1.133.0). sap.m.Button.ariaHasPopup (@since 1.84) on the header 'Alan Smith' button. Declared per the property-171 policy; the tnt members were` &&
            ` previously mis/under-declared (the earlier note cited only sap.m.Button 1.84 for ariaHasPopup, which is the Button version, not the tnt member's 1.133) because the property gate is blind to sap.tnt -` &&
            ` corrected by the non-sap.m @since audit 2026-07-24. NavigationListItem.expanded is also kept 1:1; its @since reads 1.121 because the property was relocated to the new sap.tnt.NavigationListItemBase` &&
            ` in 1.121 (the property itself predates 1.71), but the property gate resolves the base-class version, so it is declared here for the gate. // NOTE: The full 14-item navigation tree (with its nested` &&
            ` child lists, incl. Root Item 3's 38 children) and the 4-item fixed navigation are inlined from data.json. Where data.json omits a property the UI5 default is seeded explicitly (enabled true, expanded`.
    text1 = text1 && ` false, and on Fixed Item 1-3 the enum fields ariaHasPopup None / design Default - an empty string would throw in validateProperty). The page2 ScrollContainer's multi-paragraph lorem-ipsum filler text` &&
            ` is abbreviated to a short placeholder. // NOTE: Faked-event-value audit fix (2026-07-30): four substituted handlers are now the original behaviours. onItemPress: client-composed toast 'Fired` &&
            ` itemPress, item: {0}' filled by ${$parameters>/item}.getText() (was the static 'Item pressed'). onItemSelect: pageContainer.to(item key page) via roundtrip-free follow_up_action control_by_id 'to'` &&
            ` with ${$parameters>/item}.getKey() (was a static toast; a key without a matching page logs a NavContainer error client-side exactly like the original's createId miss). onSideNavButtonPress:` &&
            ` ToolPage.sideExpanded is two-way bound (added attr, declared) and flipped on the SIDE_TOGGLE round-trip; the button's tooltip is bound (added attr, declared) and set from the PRE-toggle state` &&
            ` ('Large/Small Size Navigation') exactly like _setToggleButtonTooltip; init seeds the desktop default 'Small Size Navigation' (the Device.system.desktop branch resolved statically).`.
    text1 = text1 && ` handleUserNamePress: the controller-built Popover (showHeader false, Bottom, Feedback/Help/Logout transparent buttons, sapMOTAPopover sapTntToolHeaderPopover classes) is rebuilt 1:1 via` &&
            ` popover_display by_id = $event.oSource.sId - Popover + 3 Buttons are extra controls vs the original view.xml (controller-built). onQuickActionPress: the design guard runs server-side on the` &&
            ` transported ${$source>/design} and a design=Action item opens the 'Create Item' Message Dialog (Text 'Create New Navigation List Item', Create/Cancel closing via popup_close) via popup_display -` &&
            ` Dialog/Text/Button extra controls declared here too. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): Child Item 1 press toasts 'Fired itemPress, item: Child` &&
            ` Item 1' and the Alan Smith press opens the Feedback/Help/Logout popover; the side toggle, key navigation target and Quick Create dialog remain unexercised. // NOTE: Fixed Item 1-3 are seeded` &&
            ` selectable = abap_true since 2026-08-21. data.json omits the property for those three rows and sap.tnt.NavigationListItem declares ``selectable: { defaultValue: true }``, so the UI5 default is TRUE -`.
    text1 = text1 && ` the port seeded false and silently took their selection behaviour and hover state away. The port's own rule is that a property data.json omits is seeded with the control's default explicitly; this is` &&
            ` that rule applied correctly. Only Quick Create keeps selectable = abap_false, which data.json does set. The same class caught Root Item 2 on 2026-08-23: it is the one navigation row with no items key` &&
            ` at all, so the original's selectable="{= ${items}.length > 3}" THROWS rather than evaluating to false - ExpressionParser wraps every formatter in a try/catch that logs and returns undefined, and` &&
            ` validateProperty then falls back to the declared default true. The port computed 0 > 3 = false, and since NavigationListItem only calls _selectItem when getSelectable( ) is true, that made the root2` &&
            ` page - a whole ScrollContainer the port declares - unreachable. Root Items 1 and 3 are unaffected: they carry items, so the expression really does evaluate. // NOTE: A NavContainer's position is LIVE` &&
            ` CONTROL STATE and does not survive a view rebuild. view_display( ) destroys the MAIN slot and XMLView.create builds a fresh control tree, so pageContainer comes back on the initialPage its XML`.
    text1 = text1 && ` declares - while selectedkey (bound to the SideNavigation's selectedKey), being two-way bound class state, survives the round trip and the draft. The app then showed one page while its own navigation` &&
            ` control claimed another. Fixed 2026-08-27 with the app-000 re-issue idiom: the end of view_display( ) sends the SAME control_by_id 'to' payload the ITEM_SELECT path last sent, guarded so a fresh view` &&
            ` already sitting on the initialPage="page2" issues nothing. The second view_display( ) is reached through the framework's own bookmark restore (?app_start=<class>#/z2ui5-xapp-state=<draft>, the URL` &&
            ` app_state_get_href( ) hands out): that request carries no frontend id, so the backend takes factory_first_start -> db_load(draft), check_on_navigated( ) is true while check_on_init( ) stays false -` &&
            ` the ELSEIF branch, and the only way a port that calls no other app renders twice. The interaction module asserts BOTH halves, the surviving key AND the re-issued page; asserting only the reset half`.
    text1 = text1 && ` would pass on a port that never navigated. Guard: selectedkey IS NOT INITIAL AND selectedkey <> ``page2``. This port's itemSelect is roundtrip-free, so the key reaches the backend only on the NEXT` &&
            ` event - the interaction module uses the user-name popover press for that before it takes the draft. Measured 2026-08-27 on the built backend, BEFORE the fix: select "Child Item 1" (key page1, a` &&
            ` roundtrip-free itemSelect), press the user-name button so the model reaches the backend, restore that draft - the rebuilt view came back showing mainView--page2 while the SideNavigation still read` &&
            ` page1.`.
    text2 = `Several members newer than UI5 1.71 are kept 1:1 from the original. sap.tnt.NavigationListItem: selectable (@since 1.116), design (@since 1.133.0, sap.tnt.NavigationListItemDesign), press (event,` &&
            ` @since 1.133 on NavigationListItemBase), ariaHasPopup (@since 1.133.0). sap.m.Button.ariaHasPopup (@since 1.84) on the header 'Alan Smith' button. Declared per the property-171 policy; the tnt` &&
            ` members were previously mis/under-declared (the earlier note cited only sap.m.Button 1.84 for ariaHasPopup, which is the Button version, not the tnt member's 1.133) because the property gate is blind` &&
            ` to sap.tnt - corrected by the non-sap.m @since audit 2026-07-24. NavigationListItem.expanded is also kept 1:1; its @since reads 1.121 because the property was relocated to the new` &&
            ` sap.tnt.NavigationListItemBase in 1.121 (the property itself predates 1.71), but the property gate resolves the base-class version, so it is declared here for the gate.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.ToolPage`                      name = `ToolPage`                                      class = `z2ui5_cl_smpc_app_167` path = `src/02/05/z2ui5_cl_smpc_app_167.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: onItemSelect does byId('pageContainer').to(createId(item.getKey())); the port transports the key with ${$parameters>/item}.getKey() and calls the same method through follow_up_action(` &&
            ` control_by_id, 'pageContainer', 'to', <key> ) - the NavContainer has no bindable current-page property. // IMPROVISED: _handleMediaChange is dropped. The controller attaches a sap.ui.Device.media` &&
            ` handler and, per StdExt range (LargeDesktop/Desktop/Tablet/Phone), toggles the visibility of productName/secondTitle/searchField/searchButton and shows a MessageToast naming the range. abap2UI5 does` &&
            ` serve the device> model, but it publishes the media range on the Std set only ({device>/media/range} = Phone/Tablet/Desktop), which cannot tell StdExt's LargeDesktop from Desktop - the very` &&
            ` distinction two of the four branches turn on - and a resize does not round-trip, so the range toasts have no equivalent either. The four controls therefore keep the visibility the view declares` &&
            ` (searchButton visible=false, the rest visible) and no range toast is shown. // POST-1.71: sap.tnt.ToolPage aggregation subHeader (@since 1.93) carries the horizontal IconTabHeader navigation - it is`.
    text1 = text1 && ` the whole point of this sample and is kept 1:1. Newer than UI5 1.71. // POST-1.71: IconTabFilter.interactionMode="SelectLeavesOnly" (the UI5 sources tag it @ui5-experimental-since 1.121, not @since -` &&
            ` an EXPERIMENTAL property that arrived in 1.121, so it is above the floor AND may still change) is kept 1:1 on the top-level filter template, and sap.m.Avatar (control @since 1.73) is kept 1:1 as the` &&
            ` profile avatar of the ToolHeader. Both newer than UI5 1.71. // NOTE: the profile Avatar src 'test-resources/sap/tnt/images/Woman_avatar_01.png' is absolutized to the OpenUI5 host` &&
            ` (https://sdk.openui5.org/test-resources/sap/tnt/images/Woman_avatar_01.png) per the repository asset-URL rule; the original uses the relative path. // NOTE: not yet run in a system: the IconTabHeader` &&
            ` select -> NavContainer 'to' frontend action and the two-way bound selectedKey - NOT covered by the interaction module (scope corrected 2026-08-23). **e2e-verified 2026-08-16** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_303.mjs). // POST-1.71: sap.m.IconTabFilter.items @since 1.77 - the nested sub-filter aggregation - is kept 1:1 from the original view. An`.
    text1 = text1 && ` AGGREGATION-level member, which the property gate does not see at the attribute-name level; app 221 declares the same member with the same rationale. Declared by policy 2026-08-21` &&
            ` (scripts/probes/post171-blindspot-probe.mjs). // NOTE: A NavContainer's position is LIVE CONTROL STATE and does not survive a view rebuild. view_display( ) destroys the MAIN slot and XMLView.create` &&
            ` builds a fresh control tree, so pageContainer comes back on the initialPage its XML declares - while selectedkey (bound to the IconTabHeader's selectedKey), being two-way bound class state, survives` &&
            ` the round trip and the draft. The app then showed one page while its own navigation control claimed another. Fixed 2026-08-27 with the app-000 re-issue idiom: the end of view_display( ) sends the` &&
            ` SAME control_by_id 'to' payload the ITEM_SELECT path last sent, guarded so a fresh view already sitting on the initialPage="page1" issues nothing. The second view_display( ) is reached through the` &&
            ` framework's own bookmark restore (?app_start=<class>#/z2ui5-xapp-state=<draft>, the URL app_state_get_href( ) hands out): that request carries no frontend id, so the backend takes factory_first_start`.
    text1 = text1 && ` -> db_load(draft), check_on_navigated( ) is true while check_on_init( ) stays false - the ELSEIF branch, and the only way a port that calls no other app renders twice. The interaction module asserts` &&
            ` BOTH halves, the surviving key AND the re-issued page; asserting only the reset half would pass on a port that never navigated. Guard: selectedkey IS NOT INITIAL AND selectedkey <> ``page1``.` &&
            ` Measured 2026-08-27 on the built backend, BEFORE the fix: select "Applications" (key page2), restore the very same draft through the bookmark URL - the rebuilt view came back showing mainView--page1` &&
            ` while the IconTabHeader still read page2.`.
    text2 = `sap.tnt.ToolPage aggregation subHeader (@since 1.93) carries the horizontal IconTabHeader navigation - it is the whole point of this sample and is kept 1:1. Newer than UI5 1.71. //` &&
            ` IconTabFilter.interactionMode="SelectLeavesOnly" (the UI5 sources tag it @ui5-experimental-since 1.121, not @since - an EXPERIMENTAL property that arrived in 1.121, so it is above the floor AND may` &&
            ` still change) is kept 1:1 on the top-level filter template, and sap.m.Avatar (control @since 1.73) is kept 1:1 as the profile avatar of the ToolHeader. Both newer than UI5 1.71. //` &&
            ` sap.m.IconTabFilter.items @since 1.77 - the nested sub-filter aggregation - is kept 1:1 from the original view. An AGGREGATION-level member, which the property gate does not see at the attribute-name` &&
            ` level; app 221 declares the same member with the same rationale. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.ToolPage`                      name = `ToolPageHorizontalNavigation`                  class = `z2ui5_cl_smpc_app_303` path = `src/02/05/z2ui5_cl_smpc_app_303.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: onItemSelect does byId('pageContainer').to(createId(item.getKey())); the port transports the key with ${$parameters>/item}.getKey() and calls the same method through follow_up_action(` &&
            ` control_by_id, 'pageContainer', 'to', <key> ) - the NavContainer has no bindable current-page property. The IconTabHeader and the SideNavigation share the one two-way bound selectedKey, exactly like` &&
            ` the original's {/selectedKey}. // NOTE: onSideNavButtonPress sets ToolPage.sideExpanded imperatively; the property is bindable, so the port two-way binds it (sideExpanded, an attribute the original` &&
            ` view does not declare) and flips it on the SIDE_NAV_TOGGLE round-trip. _setToggleButtonTooltip is reproduced the same way: the toggle Button's tooltip is bound and updated from the pre-toggle state,` &&
            ` seeded at init from !Device.system.desktop via the device data the framework mirrors server-side (client->get( )-s_device-system). // NOTE: the device> named model bindings visible="{=` &&
            ` ${device>/system/phone}}" (toggle Button, side SideNavigation) and visible="{=! ${device>/system/phone}}" (subHeader ToolHeader) are kept 1:1 - abap2UI5 serves the device> model on every view slot`.
    text1 = text1 && ` (CAPABILITIES.md), so the original's phone branch stays a branch. // IMPROVISED: _handleMediaChange is dropped. The controller attaches a sap.ui.Device.media handler and, per StdExt range` &&
            ` (LargeDesktop/Desktop/Tablet/Phone), toggles the visibility of productName/secondTitle/searchField/searchButton and shows a MessageToast naming the range. The device> model publishes the media range` &&
            ` on the Std set only ({device>/media/range} = Phone/Tablet/Desktop), which cannot tell StdExt's LargeDesktop from Desktop - the distinction two of the four branches turn on - and a resize does not` &&
            ` round-trip, so the range toasts have no equivalent either. The four controls keep the visibility the view declares (searchButton visible=false, the rest visible). // POST-1.71: sap.tnt.ToolPage` &&
            ` aggregation subHeader (@since 1.93) carries the horizontal IconTabHeader navigation and is kept 1:1; IconTabFilter.interactionMode="SelectLeavesOnly" (the UI5 sources tag it @ui5-experimental-since` &&
            ` 1.121, not @since - an EXPERIMENTAL property that arrived in 1.121, so it is above the floor AND may still change) is kept 1:1 on the top-level filter template; sap.m.Avatar (control @since 1.73) is`.
    text1 = text1 && ` kept 1:1 as the ToolHeader profile avatar. All newer than UI5 1.71. // NOTE: the profile Avatar src 'test-resources/sap/tnt/images/Woman_avatar_01.png' is absolutized to the OpenUI5 host` &&
            ` (https://sdk.openui5.org/test-resources/sap/tnt/images/Woman_avatar_01.png) per the repository asset-URL rule; the original uses the relative path. The ENABLED/EXPANDED fields the templates bind are` &&
            ` absent from model/data.json, so every row carries the UI5 property default (true) explicitly - a flat ABAP row would otherwise serialize them as empty and override that default. // NOTE: not yet run` &&
            ` in a system: the IconTabHeader / SideNavigation select -> NavContainer 'to' frontend action, the two-way bound selectedKey - NOT covered by the interaction module (scope corrected 2026-08-23) and the` &&
            ` SIDE_NAV_TOGGLE round-trip (sideExpanded plus the tooltip flip). **e2e-verified 2026-08-16** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_302.mjs). // POST-1.71:` &&
            ` sap.m.IconTabFilter.items @since 1.77 - the nested sub-filter aggregation - is kept 1:1 from the original view. An AGGREGATION-level member, which the property gate does not see at the attribute-name`.
    text1 = text1 && ` level; app 221 declares the same member with the same rationale. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs). // POST-1.71: sap.tnt.NavigationListItem.expanded is kept` &&
            ` 1:1 from the original view. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase and its JSDoc there carries @since 1.121, which is the version any scanner reads - the` &&
            ` relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it. // NOTE: A NavContainer's position is LIVE` &&
            ` CONTROL STATE and does not survive a view rebuild. view_display( ) destroys the MAIN slot and XMLView.create builds a fresh control tree, so pageContainer comes back on the initialPage its XML` &&
            ` declares - while selectedkey (bound to the IconTabHeader's and the phone SideNavigation's selectedKey), being two-way bound class state, survives the round trip and the draft. The app then showed one` &&
            ` page while its own navigation control claimed another. Fixed 2026-08-27 with the app-000 re-issue idiom: the end of view_display( ) sends the SAME control_by_id 'to' payload the ITEM_SELECT path last`.
    text1 = text1 && ` sent, guarded so a fresh view already sitting on the initialPage="page1" issues nothing. The second view_display( ) is reached through the framework's own bookmark restore` &&
            ` (?app_start=<class>#/z2ui5-xapp-state=<draft>, the URL app_state_get_href( ) hands out): that request carries no frontend id, so the backend takes factory_first_start -> db_load(draft),` &&
            ` check_on_navigated( ) is true while check_on_init( ) stays false - the ELSEIF branch, and the only way a port that calls no other app renders twice. The interaction module asserts BOTH halves, the` &&
            ` surviving key AND the re-issued page; asserting only the reset half would pass on a port that never navigated. Guard: selectedkey IS NOT INITIAL AND selectedkey <> ``page1``. Measured 2026-08-27 on` &&
            ` the built backend, BEFORE the fix: select "Applications" (key page2), restore the very same draft through the bookmark URL - the rebuilt view came back showing mainView--page1 while the IconTabHeader` &&
            ` still read page2.`.
    text2 = `sap.tnt.ToolPage aggregation subHeader (@since 1.93) carries the horizontal IconTabHeader navigation and is kept 1:1; IconTabFilter.interactionMode="SelectLeavesOnly" (the UI5 sources tag it` &&
            ` @ui5-experimental-since 1.121, not @since - an EXPERIMENTAL property that arrived in 1.121, so it is above the floor AND may still change) is kept 1:1 on the top-level filter template; sap.m.Avatar` &&
            ` (control @since 1.73) is kept 1:1 as the ToolHeader profile avatar. All newer than UI5 1.71. // sap.m.IconTabFilter.items @since 1.77 - the nested sub-filter aggregation - is kept 1:1 from the` &&
            ` original view. An AGGREGATION-level member, which the property gate does not see at the attribute-name level; app 221 declares the same member with the same rationale. Declared by policy 2026-08-21` &&
            ` (scripts/probes/post171-blindspot-probe.mjs). // sap.tnt.NavigationListItem.expanded is kept 1:1 from the original view. The property PREDATES 1.71, but it now lives on sap.tnt.NavigationListItemBase` &&
            ` and its JSDoc there carries @since 1.121, which is the version any scanner reads - the relocated-member residual limit AGENTS section 5 names. Declared by policy 2026-08-21`.
    text2 = text2 && ` (scripts/probes/post171-blindspot-probe.mjs); no gate can raise it.`.
    result = VALUE #( BASE result
      ( module = `sap.tnt`            control = `sap.tnt.ToolPage`                      name = `ToolPageNavigation`                            class = `z2ui5_cl_smpc_app_302` path = `src/02/05/z2ui5_cl_smpc_app_302.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.34`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: Breadth-probe: the ACE-based sap.ui.codeeditor CodeEditor (a wrapped third-party editor). **Both findings of the 2026-07-27 review are fixed (2026-08-05)**: the value carries all TWELVE language` &&
            ` entries of the original verbatim (it had been shortened to four, against the verbatim-data rule), and the brace escaping is right. The original's own view.xml writes value='\{…' - the backslash has` &&
            ` to SURVIVE into the serialized attribute or the XMLView parser reads the JSON braces as a binding - so the braces come from ``backtick`` literals (taken verbatim) while the body uses |…| templates` &&
            ` for the real newlines; inside a template \{ would collapse to a bare brace. That distinction is now a linter rule of its own (escaped-brace-in-backtick, the mirror of collapsed-brace-in-style). //` &&
            ` NOTE: The editor renders 13 tab-indented lines where the ORIGINAL renders one collapsed line, and the port is the source-faithful side rather than the display-faithful one. The original puts literal`.
    text1 = text1 && ` LF and TAB characters into an XML attribute value, and XML attribute-value normalisation turns each into a single space before the value reaches CodeEditor.value - so the demo kit shows the JSON on` &&
            ` one long line. abap2UI5's builder deliberately emits &#xA;/&#x9; character references instead (a character reference is NOT normalised), precisely so a two-line noDataText survives, and here that` &&
            ` preserves the formatting the sample's source has and its rendering loses. Declared 2026-08-23 rather than collapsed to match: reproducing the original's RENDERING would mean deliberately writing` &&
            ` spaces where the sample wrote newlines.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.codeeditor`  control = `sap.ui.codeeditor.CodeEditor`          name = `CodeEditor`                                    class = `z2ui5_cl_smpc_app_114` path = `src/01/02/z2ui5_cl_smpc_app_114.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46`
        notes = text1 ) ).

    text1 = `NOTE: The original's onSelectTab swaps the CodeEditor value per selected key imperatively (A -> example2, B -> example1, any other key -> setValue() i.e. empty) and its onInit seeds the hint '//` &&
            ` select tabs to see value of CodeEditor changing'. Rebuilt on the client with no round-trip: selectedKey is two-way bound to selected_key (seeded with the original's literal 'invalidKey') and the` &&
            ` CodeEditor carries a value expression binding that is the same switch - selected_key === 'A' ? code_a : (selected_key === 'B' ? code_b : code_init). Two consequences for the view: the select` &&
            ` attribute is dropped (it was wired to a SELECT_TAB backend event no branch of this class dispatched - a dead wire, pattern-lint dead-event-wire), and a value attribute is added to ce:CodeEditor,` &&
            ` which the original XML does not carry because its controller sets the value. The original's unreachable default branch (a key that is neither A nor B can only be the initial 'invalidKey') therefore` &&
            ` shows the init hint rather than an empty editor; the two example strings are inlined verbatim, including the original's own A/B swap.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.codeeditor`  control = `sap.ui.codeeditor.CodeEditor`          name = `CodeEditorIconTabHeader`                       class = `z2ui5_cl_smpc_app_150` path = `src/01/02/z2ui5_cl_smpc_app_150.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.46`
        notes = text1 ) ).

    text1 = `NOTE: Since 2026-07-30 the global sap.ui.core.BusyIndicator behaviour is reproduced 1:1: each button fires a backend event whose response chains follow_up_action( control_global BUSY_INDICATOR show` &&
            ` [delay] ) with the original's delay (default / 0 / 2000) plus follow_up_action( start_timer HIDE_BUSY duration ) (4000 / 4000 / 1000); the HIDE_BUSY timer round-trip then issues BUSY_INDICATOR hide -` &&
            ` the frontend-action form of showBusyIndicator(iDuration, iDelay) + setTimeout(hideBusyIndicator). START_TIMER's single-slot semantics (a new timer replaces the pending one) match the original's` &&
            ` clearTimeout of _sTimeoutId. Timing nuance: the hide fires after duration + one round-trip latency, and the show after the press round-trip - a few ms later than the original's purely client-side` &&
            ` call; the press attributes are rewired from the controller handler names to backend events (the earlier MessageToast substitution is gone). // NOTE: the BUSY_INDICATOR show/hide chain and the` &&
            ` START_TIMER HIDE_BUSY round-trips are unverified in a running system - verify the overlay appears (default and zero delay), disappears after ~4s, and never appears in the 2s-delay/1s-duration case.`.
    text1 = text1 && ` **e2e-verified 2026-07-30, sharpened 2026-08-23** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the module used to assert only that the overlay appears after the click and is gone later,` &&
            ` which the FRAMEWORK satisfies on its own - View1.controller.js shows the same global singleton on every request and hides it in the finally of _processAfterRendering, before the port's follow-up JS` &&
            ` runs at all. It now measures how long the overlay stays up and requires an episode of at least 2s: the framework's own episode ends when rendering finishes, so only the port's show(0) + START_TIMER(` &&
            ` HIDE_BUSY, 4000 ) can produce one. The default-delay and 2s-delay/1s-duration buttons are the identical wire with different literals and stay unasserted.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.BusyIndicator`             name = `BusyIndicator`                                 class = `z2ui5_cl_smpc_app_147` path = `src/01/02/z2ui5_cl_smpc_app_147.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The three core:CommandExecution controls (CE_SAVE, CE_DELETE in the Page dependents, CE_SAVE_POPOVER in the popoverCommand dependents) are DROPPED as controls - abap2UI5 renders no` &&
            ` CommandExecution element. Their behaviour IS reproduced since 2026-07-30: the manifest shortcuts Save=Ctrl+S / Delete=Ctrl+D are registered on init via cs_event-keyboard_shortcut (follow_up_action,` &&
            ` combo -> named backend event), and every command-triggering button (press='cmd:Save' / press='cmd:Delete') fires the same backend SAVE/DELETE/PSAVE events, so shortcut and button share one command` &&
            ` path like the original command bus; the backend gates each command on its enabled/visible flags, so a disabled command's button press or shortcut does nothing, as with a disabled CommandExecution.` &&
            ` The last residual closed 2026-08-06: the shortcut registry used to be document-GLOBAL, so the original's popover-local command scope (CE_SAVE_POPOVER shadowing CE_SAVE for the same Save command while` &&
            ` the popover is open) could not be expressed - Ctrl+S always hit the page-level command and its flags. cs_event-keyboard_shortcut now takes an optional third t_arg scoping the registration`.
    text1 = text1 && ` (implemented upstream, pr/keyboard-shortcut-scope). The scope here is the CONTROL id: this Popover is declared in the view's dependents and opened with control_by_id openBy, so it never enters the` &&
            ` framework's popover SLOT - the slot-only form of the request would never have fired, which the e2e interaction found immediately and the upstream change then covered. Ctrl+S is therefore registered` &&
            ` twice - unscoped -> SAVE, and scoped to the control id popoverCommand -> PSAVE - so the popover's own enabled/visible flags gate the command exactly while it is open, and the page-level one applies` &&
            ` again once it closes. The three CommandExecution CONTROLS themselves stay dropped (abap2UI5 renders no such element); what they DO is now fully reproduced. **e2e-verified 2026-08-06**` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): with the popover's own Save switched off and the popover open, Ctrl+S goes silent, while the page-level command is still` &&
            ` enabled and fires with the popover closed. **Corrected 2026-08-23**: enabled and visible are not the same veto. setEnabled( false ) keeps the shortcut registered and CommandExecution.trigger swallows`.
    text1 = text1 && ` it; setVisible( false ) calls Shortcut.unregister at that parent, so the key PROPAGATES to the ancestor CommandExecution - which is what the class documentation describes and what this sample's` &&
            ` popover command exists to show. The port gated both the same way, so with the popover's visible switch off Ctrl+S went silent where the original falls through to the page's CE_SAVE. The PSAVE branch` &&
            ` now falls through to the page command's gate. // NOTE: The $cmd> command model bindings enabled='{$cmd>Save/enabled}' / enabled='{$cmd>Delete/enabled}' on the popover footer buttons have no command` &&
            ` model to bind to (CommandExecution dropped), so they are folded to default-model booleans: the popover buttons bind enabled to {/SAVE_ENABLED}/{/DELETE_ENABLED} (page popover) and` &&
            ` {/PSAVE_ENABLED}/{/DELETE_ENABLED} (command popover). Correspondingly the controller's onToggleSave/onToggleDelete/onTogglePopoverSave (byId('CE_SAVE').setEnabled(state)) and the *Visibility variants` &&
            ` (setVisible) are reproduced as two-way bindings: each Switch state is bound two-way to the matching boolean and the popover buttons additionally bind visible to`.
    text1 = text1 && ` {/SAVE_VISIBLE}/{/DELETE_VISIBLE}/{/PSAVE_VISIBLE}, so the switches drive the command-buttons client-side with no round-trip. Consequently the Switch change attributes (change='.onToggleSave' etc., 6` &&
            ` of them) are DROPPED and replaced by the two-way state binding; the enabled binding value differs from the original $cmd> path. The flags travel with the next event's two-way model update, which is` &&
            ` how the server-side command gating reads the current switch state. // NOTE: The manifest-declared viewModel named JSON model (bindings {viewModel>/value}, {viewModel>/countries},` &&
            ` {viewModel>/selected}, {viewModel>key}, {viewModel>text}) is folded onto the one default model as value/t_countries/selected with the same leaf names and the controller's addData seed values` &&
            ` (HelloWorld!, DZ Algeria / AR Argentina) - a pure prefix-drop, renders identically, structural-diff 0 diffs. // POST-1.71: sap.m.Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for` &&
            ` the 1:1 port - the two Open-Popover buttons carry ariaHasPopup='Dialog' as in the original; the app needs a UI5 release >= 1.84 to render it. // NOTE: unverified in a running system: (a) the two`.
    text1 = text1 && ` Open-Popover buttons open the popover anchored to the button via control_by_id openBy ($event.oSource.sId); (b) the Ctrl+S / Ctrl+D keyboard_shortcut registrations firing the backend SAVE/DELETE` &&
            ` round-trips and their enabled/visible server-side gating; (c) the switch-driven two-way enabled/visible of the popover buttons. The e2e interaction (keyboard Ctrl+S -> save toast) covers (b) for the` &&
            ` page-level Save. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): Ctrl+S fires the SAVE round-trip and toasts 'CTRL+S: save triggered on controller'.` &&
            ` **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_232.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.CommandExecution`          name = `Commands`                                      class = `z2ui5_cl_smpc_app_232` path = `src/02/02/z2ui5_cl_smpc_app_232.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.70`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the two Open-Popover buttons carry ariaHasPopup='Dialog' as in the original; the app needs a UI5 release >=` &&
                 ` 1.84 to render it.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.Control`                   name = `ControlBusyIndicator`                          class = `z2ui5_cl_smpc_app_130` path = `src/01/02/z2ui5_cl_smpc_app_130.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The 'Toggle Busy State' button sets a boolean model field bound to Panel1.busy and the Icon.busy (both added to carry the binding; the original set busy imperatively via byId().setBusy). The 5s` &&
                 ` auto-reset of the original setTimeout is reproduced with START_TIMER, which fires a CLEAR_BUSY round trip that clears the same field. **e2e-verified 2026-07-30** (transpiled-framework interaction,` &&
                 ` scripts/e2e-smoke.mjs): the button round-trip turns the bound controls busy (sapUiLocalBusy present after the click).` ) ).

    text1 = `NOTE: The sample's own idiom is sap.ui.core.Control's fieldGroupIds plus the SimpleForm's validateFieldGroup event, both passed through 1:1: every Input/Select/ComboBox keeps its fieldGroupIds` &&
            ` ('Billing Information' / 'Discount Code' / 'Credit Card' / 'Online') and the form's validateFieldGroup is wired to a backend event carrying ${$parameters>/fieldGroupIds}. The parameter reaches the` &&
            ` backend as the JSON array (["Billing Information"], measured 2026-08-01), so the arg indexes it - ${$parameters>/fieldGroupIds}[0], literally the original's aFieldGroup[0] - and the ABAP CASE is the` &&
            ` controller's mMessageMapping table (group -> strip id + type). // NOTE: The controller sets three properties per MessageStrip imperatively (byId(id).setType(t).setText(s).setVisible(true)); the port` &&
            ` binds that triple (type/text/visible) per strip instead, so the round-trip that classifies the group also updates the strip. onMsgStripClose (oEvt.getSource().setVisible(false)) becomes one close` &&
            ` event per strip - each target is known statically, so no source lookup is needed. // NOTE: onReset does getModel().setData({}) - reproduced by CLEARing the fourteen bound field values.`.
    text1 = text1 && ` onAccept/onCancel/onReset all call hideMessages() first, kept as a helper method. The four MessageToast texts are passed through verbatim ('Validation of field group '<g>' triggered.', 'Accept` &&
            ` triggered', 'Cancel triggered', 'Reset triggered'); the original's duration:500 is carried through since 2026-08-23. It used to be dropped on the reasoning that "abap2UI5's message_toast_display has` &&
            ` no duration parameter", which was never true: the interface declares an optional duration (z2ui5_if_client, and already at the pinned commit), z2ui5_cl_ui5_client forwards it and the client spreads` &&
            ` it into MessageToast.show - and five ports in this corpus were already passing it, app 451 with this very value. The four toasts had been lingering 3000 ms against the original's 500. // NOTE: The` &&
            ` original controller loads SampleData.json and element-binds the whole model (bindElement('/')), which makes its {BillingName} & co relative. The port seeds the same fields at the model root and binds` &&
            ` them ABSOLUTELY (client->_bind) - a relative path on a control with no binding context resolves against nothing (AGENTS 5, the class fixed corpus-wide 2026-08-01). SampleData.json's single value`.
    text1 = text1 && ` (Email) is bound by no control in the view, so every input starts empty exactly as in the original. // NOTE: live-verified on 2026-08-02 (maintainer live check): legs (c) and (d) too - each strip` &&
            ` closes only itself, and Submit/Cancel/Reset behave as the original.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.Control`                   name = `FieldGroup`                                    class = `z2ui5_cl_smpc_app_272` path = `src/01/02/z2ui5_cl_smpc_app_272.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        checked = `CHECKED (2026-08-02): verified in a running system - human live check 2026-08-02 (maintainer): app started and exercised, no findings.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.HTML`                      name = `Html`                                          class = `z2ui5_cl_smpc_app_120` path = `src/01/02/z2ui5_cl_smpc_app_120.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: Raw HTML injected via the sap.ui.core.HTML content attribute, written as decoded markup (the builder xml-escapes it into the attribute value; the original view.xml carries it entity-encoded).` &&
                 ` The 2026-07-27 review restored the full Lorem-ipsum text 1:1 (had been shortened by the original breadth-probe), so the sample's root Html.view.xml is now rebuilt fully.` ) ).

    text1 = `NOTE: The original's onSliderMoved sets the three Panel widths imperatively (setWidth(value + "%")). Rebuilt 1:1 on the client, the thin-frontend way (same shape as the checked app 053): the Slider` &&
            ` value is two-way bound to slider_value (seeded 100, the original's literal) and each Panel width carries the expression binding {= slider_value + '%' } instead of the literal 100%. The liveChange` &&
            ` attribute is therefore dropped - no backend round-trip fires while dragging, and the resize behaves as in the original. Before this rework the liveChange was wired to a SLIDER backend event the class` &&
            ` never dispatched (dead wire, pattern-lint dead-event-wire). // IMPROVISED: The three core:HTML panels stay empty. The original controller fills them from sap/ui/core/hyphenation/Hyphenation - it` &&
            ` awaits hyph.initialize() per language and then writes the hyphenated text into each HTML control. That is an asynchronous client-side API with a promise chain and no declarative form: it is neither a` &&
            ` binding nor one of the whitelisted control methods, and the hyphenation itself cannot be done on the ABAP side (the algorithm and its language dictionaries live in the browser). Re-verified`.
    text1 = text1 && ` 2026-08-23: still true. initialize( ) returns a Promise whose RESULT has to be written into a control, and no declarative abap2UI5 action can carry a promise callback - CONTROL_GLOBAL/CONTROL_BY_ID` &&
            ` call a method and are done. Nothing in the framework has changed that. The port therefore keeps the view 1:1 with the original XML, which also carries content="" on all three, and loses the sample's` &&
            ` actual payload - the visible hyphenated text. This is the sample's whole point, so the port is a structural port only.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.hyphenation.Hyphenation`   name = `HyphenationAPI`                                class = `z2ui5_cl_smpc_app_146` path = `src/01/02/z2ui5_cl_smpc_app_146.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: The sample's style.css is now shipped with the port: it is injected through a core:HTML leaf carrying a <style> block (the documented CAPABILITIES form, as in apps 026/028), so the port adds one` &&
            ` core:HTML control the original view does not have - the original loads the stylesheet through its manifest's sap.ui5/resources/css entry instead. The five size classes and the <600px media query are` &&
            ` verbatim, only whitespace-collapsed; literal braces are escaped \{ \} in a backtick literal because the XMLView parser reads an unescaped brace as a binding. Until 2026-07-28 the port carried the` &&
            ` size1..size5 class names with no stylesheet behind them, so every Icon rendered at the default size - the sample is about icon SIZES, so that was its whole point. The stylesheet was also missing from` &&
            ` ui5/sap.ui.core/Icon/ (a §4 archive gap) and is archived in the same change. // NOTE: The stethoscope Icon press is wired client-side to MessageToast.show('Over budget!') via the control_global` &&
            ` frontend action, matching the original controller's handleStethoscopePress (MessageToast.show). **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the Icon press`.
    text1 = text1 && ` (dispatched click - the icon row renders zero-height headless) toasts 'Over budget!'.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.Icon`                      name = `Icon`                                          class = `z2ui5_cl_smpc_app_122` path = `src/01/02/z2ui5_cl_smpc_app_122.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.11.1`
        notes = text1 ) ).

    text1 = `NOTE: The four buttons announce the pressed button's type+text to the sap.ui.core.InvisibleMessage a11y service and echo the sentence into the status Text; the port reproduces both. The original's` &&
            ` 'Infromation' button-text typo is kept 1:1. **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser), re-checked 2026-08-23: pressing Success round-trips and` &&
            ` the bound status Text carries 'Button with type Accept and text Success is pressed' inside '... was sent to the invisible messaging service.' - the two strings` &&
            ` meta/interactions/z2ui5_cl_smpc_app_141.mjs actually asserts. What stays unverifiable by machine is the announcement itself - a live region has no assertable rendering. // NOTE: The pressed button's` &&
            ` identity is transported since 2026-08-21: each press carries $event.oSource.getType() and $event.oSource.getText(), and ABAP composes the original's sentence verbatim. Before that the status text was` &&
            ` a constant - a faked event value found by the review sweep. The announce( ) call itself was dropped until 2026-08-23 on the claim that it has no control_global entry; that was wrong - ControlCall.js`.
    text1 = text1 && ` defines INVISIBLE_MESSAGE with announce: ["string","string"], CAPABILITIES.md marks the idiom expressible, and apps 289/435 use it. on_event now fires follow_up_action( control_global,` &&
            ` INVISIBLE_MESSAGE / announce / <the composed sentence> / Assertive ), matching InvisibleMessageMode.Assertive in the original. Nothing of the sample is dropped any more - which matters here more than` &&
            ` elsewhere, since ui5/scope-exceptions.json keeps this post-1.71 sample precisely because the accessibility-announcement idiom exists nowhere else in the corpus. // POST-1.71:` &&
            ` INVISIBLE_MESSAGE.announce is a control_global frontend action since UI5 1.78 - newer than the 1.71 floor and the reason ui5/scope-exceptions.json keeps this post-1.71 sample at all. Below 1.78 the` &&
            ` lazy require returns undefined and the dispatch hits its "not available" guard: the status Text still updates, the announcement itself does not happen. The app needs a UI5 release >= 1.78 - which` &&
            ` sap.ui.core.InvisibleMessage, @since 1.78 itself, already requires.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.InvisibleMessage`          name = `InvisibleMessage`                              class = `z2ui5_cl_smpc_app_141` path = `src/02/02/z2ui5_cl_smpc_app_141.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.78`
        since_post171 = abap_true
        is_post171 = abap_true
        notes = text1
        post171 = `INVISIBLE_MESSAGE.announce is a control_global frontend action since UI5 1.78 - newer than the 1.71 floor and the reason ui5/scope-exceptions.json keeps this post-1.71 sample at all. Below 1.78 the` &&
                 ` lazy require returns undefined and the dispatch hits its "not available" guard: the status Text still updates, the announcement itself does not happen. The app needs a UI5 release >= 1.78 - which` &&
                 ` sap.ui.core.InvisibleMessage, @since 1.78 itself, already requires.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.InvisibleText`             name = `InvisibleText`                                 class = `z2ui5_cl_smpc_app_127` path = `src/01/02/z2ui5_cl_smpc_app_127.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.27.0`
        notes = `NOTE: onPress toasts evt.getSource().getId() + ' Pressed'. All twelve presses reproduce that client-side and roundtrip-free (control_global MESSAGE_TOAST.show) with the template '{0} Pressed' filled` &&
                 ` by $event.oSource.sId - the documented transport for the pressed control's runtime id. Until 2026-07-28 the port toasted a bare 'Pressed' on the rationale that the generated id is 'not reproducible` &&
                 ` statically', which was the wrong conclusion: the id does not have to be reproduced, it is read off the event on the client. The id string itself is a UI5 runtime value and will differ from the` &&
                 ` original sample's (the view prefix differs), exactly as it differs between two runs of the original.` ) ).

    text1 = `IMPROVISED: the model is resolved statically. The original controller builds it in the browser: _fetchClasses walks document.styleSheets and collects every cssRule whose selectorText contains` &&
            ` sapTheme, and it re-runs on every Theming.attachApplied. Neither is reachable from ABAP (no DOM, no stylesheet), so the 26 rows are seeded in model_init from the OpenUI5 base theme source` &&
            ` themes/base/parameterClasses.less - same classes, same declaration blocks, but they no longer follow a theme switch at runtime. The two font rows carry the CSS custom property form` &&
            ` (var(--sapFontSize)) where the less source uses the @sapUiFontSize variable, which only exists after theme compilation. // NOTE: the original's per-row border flag plus its onAfterRendering DOM patch` &&
            ` (querySelectorAll('.sampling')[i].style.borderStyle = 'solid') become the computed model column BORDERSTYLE, bound into the core:HTML div's style attribute - the same visible result without touching` &&
            ` the DOM from the frontend. The original's companion elem.style.borderWidth = '1xp' is an upstream typo (no such CSS unit, the browser drops it), so it is not reproduced; the solid border renders at`.
    text1 = text1 && ` the CSS default width in both. // NOTE: the core:HTML content keeps the original's {styleClass} binding as a REAL binding ({STYLECLASS} on the ABAP field), so its braces stay unescaped - the opposite` &&
            ` of the literal-CSS case (app 028), where braces inside a core:HTML content must be escaped. The markup is written decoded (<div ...>) because the builder re-escapes it on stringify. // NOTE: the` &&
            ` third cell's empty class="" attribute of the original Text is dropped - the a( ) call is simply left out, because an empty class list adds nothing to the rendered control. Note this is a CHOICE, not` &&
            ` something the builder does: z2ui5_cl_ui5_view_builder emits every declared pair, empty value included (render( ) writes | { pair-n }="{ xml_escape( pair-v ) }"| for the whole t_pair with no emptiness` &&
            ` test), which is why app 287 can reproduce its original's IconTabSeparator icon="" verbatim with a( n = ``icon`` v = ```` ).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.theming`                   name = `ThemeCustomClasses`                            class = `z2ui5_cl_smpc_app_283` path = `src/01/02/z2ui5_cl_smpc_app_283.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.core.theming.Parameters`        name = `BasicThemeParameters`                          class = `z2ui5_cl_smpc_app_131` path = `src/01/02/z2ui5_cl_smpc_app_131.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: The sample itself is just a MessageStrip + Link pointing at the Theme Parameter Toolbox (the real demo content lives in that external tool); reproduced 1:1. The Link href is the original's` &&
                 ` host-relative 'test-resources/sap/m/demokit/theming/webapp/index.html' rewritten to the OpenUI5 host https://sdk.openui5.org/test-resources/sap/m/demokit/theming/webapp/index.html per the runtime` &&
                 ` asset-URL rule (an abap2UI5 system does not serve the demokit path; app 152 precedent).` ) ).

    text1 = `POST-1.71: The sample's whole point is the binding-info parameter 'boundFilters' - filter values that are binding expressions, so the aggregation re-filters whenever a value changes through data` &&
            ` binding. It is 'Supported since 1.146.0' (source: fork-openui5/src/sap.ui.core/src/sap/ui/base/ManagedObject.js:3711, the sample's manifest declares minUI5Version 1.146.0) and is passed through 1:1` &&
            ` in the rows binding-info string. A binding-info parameter is invisible to the property gate (it is not a control member), so this is a by-policy declaration; the app needs a UI5 release >= 1.146. //` &&
            ` NOTE: Named-model fold: the controller registers two JSONModels, 'filter' (departmentPrefix / locationPrefix / firstName / lastName, all undefined initially) and 'ui' (showOrganizational: true).` &&
            ` abap2UI5 serves one default model, so both fold onto the default-model root with the same leaf names ({filter>/departmentPrefix} -> {/DEPARTMENTPREFIX}, {ui>/showOrganizational} ->` &&
            ` {/SHOWORGANIZATIONAL}) - same data, same leaves, renders identically, so NOTE and not IMPROVISED (AGENTS §5 settled policy 2026-07-24). The Inputs keep their odata String type 1:1 (core:require`.
    text1 = text1 && ` StringType), which maps empty input to null. // NOTE: onToggleFilters flips ui>/showOrganizational and applies the other filter pair with oListBinding.filter(aFilters, FilterType.ApplicationBound).` &&
            ` abap2UI5 bakes binding info at render time, so the TOGGLE_FILTERS round-trip flips the bound flag and redraws the view with the other boundFilters list in the rows binding (app 241 precedent, 'the` &&
            ` flag is baked into the handler at render time'). Same observable behaviour - the visible Inputs switch via the bound visible expressions and the table re-filters on the other pair - through a redraw` &&
            ` instead of an imperative filter() call; the entered prefixes survive because they are two-way bound. The controller's aOrgFilters order (department, location) is the view's boundFilters order` &&
            ` reversed (location, department); the port keeps the view's order, which is semantically identical (AND-combined). // NOTE: Unverified in a running system: that the bound filter values re-filter the` &&
            ` sap.ui.table rows as the user types (boundFilters + odata String type mapping empty to null, i.e. an empty prefix must drop the filter rather than match nothing), and that the TOGGLE_FILTERS redraw`.
    text1 = text1 && ` swaps the filter pair without losing the entered prefixes. **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): typing 'Manage' into the department` &&
            ` prefix re-filters the bound rows (no 'Development' row left), clearing the prefix brings every row back (the odata String type maps '' to null, so an empty prefix drops the filter instead of matching` &&
            ` nothing), and 'Show Personal Filters' re-bakes the binding into the personal pair (the first/last name inputs appear). That is the whole boundFilters + re-bake-redraw claim.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.Filter`                   name = `BoundFilters.FilterBar`                        class = `z2ui5_cl_smpc_app_264` path = `src/02/02/z2ui5_cl_smpc_app_264.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `The sample's whole point is the binding-info parameter 'boundFilters' - filter values that are binding expressions, so the aggregation re-filters whenever a value changes through data binding. It is` &&
                 ` 'Supported since 1.146.0' (source: fork-openui5/src/sap.ui.core/src/sap/ui/base/ManagedObject.js:3711, the sample's manifest declares minUI5Version 1.146.0) and is passed through 1:1 in the rows` &&
                 ` binding-info string. A binding-info parameter is invisible to the property gate (it is not a control member), so this is a by-policy declaration; the app needs a UI5 release >= 1.146.` ) ).

    text1 = `POST-1.71: The sample's whole point is the binding-info parameter 'boundFilters', 'Supported since 1.146.0' (source: fork-openui5/src/sap.ui.core/src/sap/ui/base/ManagedObject.js:3711; the manifest` &&
            ` declares minUI5Version 1.146.0). Here it filters each row's Select items by the RELATIVE row field value1: '{REGION}', so every row lists only the account managers of its own region and re-filters` &&
            ` when that row's region changes. Passed through 1:1 as a raw binding-info string; a binding-info parameter is invisible to the property gate, so this is a by-policy declaration and the app needs UI5` &&
            ` >= 1.146. // NOTE: No named models in this sample - both arrays (/customers, /accountManagers) already live in the default model and are bound with client->_bind (the Select's items path via _bind(` &&
            ` val = t_accountmanagers path = abap_true ), never as text). Inside the binding-info strings the paths use the ABAP upper-cased field names: 'REGION' in the boundFilters entry and {parts:` &&
            ` ['FIRSTNAME', 'LASTNAME']} in the core:Item text, matching the original's 'region' / ['firstName', 'lastName']. Full mock row set inlined (20 customers, 13 account managers, byte-identical to`.
    text1 = text1 && ` Component.js). // NOTE: Unverified in a running system: that each row's Select really shows only its region's account managers (per-row bound filter over a relative value), that` &&
            ` forceSelection='false' plus selectedKey={ACCOUNTMANAGERID} preselects the right manager, and that the composite {parts: [...]} item text renders 'First Last'. **e2e-verified 2026-07-31**` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the first row's Select lists 'John Smith' (Americas, the row's own region) and NOT 'Yuki Tanaka' (APJ) - the per-row bound` &&
            ` filter over the relative value1 '{REGION}' works. Residual: the forceSelection='false' / selectedKey preselection is not separately asserted.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.Filter`                   name = `BoundFilters.FilteredListInTable`              class = `z2ui5_cl_smpc_app_265` path = `src/02/02/z2ui5_cl_smpc_app_265.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `The sample's whole point is the binding-info parameter 'boundFilters', 'Supported since 1.146.0' (source: fork-openui5/src/sap.ui.core/src/sap/ui/base/ManagedObject.js:3711; the manifest declares` &&
                 ` minUI5Version 1.146.0). Here it filters each row's Select items by the RELATIVE row field value1: '{REGION}', so every row lists only the account managers of its own region and re-filters when that` &&
                 ` row's region changes. Passed through 1:1 as a raw binding-info string; a binding-info parameter is invisible to the property gate, so this is a by-policy declaration and the app needs UI5 >= 1.146.` ) ).

    text1 = `NOTE: Composite data-type binding paradigm: the Currency type is pulled in via core:require and every Input/Text binds a composite parts:['/amount','/currency'] with type:'CurrencyType' plus` &&
            ` formatOptions (showMeasure/showNumber/preserveDecimals/currencyCode/style) 1:1. The two model fields amount ('123456789.123') and currency ('USD') are serialized by abap2UI5 as /AMOUNT and /CURRENCY;` &&
            ` the paths are generated via _bind (never hardcoded). // POST-1.71: the Currency type's formatOptions { showNumber: false } on the third Input is @since 1.89 (sap/ui/model/type/Currency.js: "since` &&
            ` 1.89.0, if the format option showNumber is set to false, the amount is not shown in the control") and is kept 1:1 from the original. A format option is a binding-info parameter, not a control member,` &&
            ` so no gate can ever see it - declared by policy, the same route apps 264/265 take for boundFilters. Below 1.89 the option is ignored and that Input renders the full amount plus the currency instead` &&
            ` of the bare 'USD', which is exactly what the field is in the sample to show. Undeclared until 2026-08-23; declaring it moves the port from src/01/02 to src/02/02 per AGENTS section 3.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.type.Currency`            name = `TypeCurrency`                                  class = `z2ui5_cl_smpc_app_135` path = `src/02/02/z2ui5_cl_smpc_app_135.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `the Currency type's formatOptions { showNumber: false } on the third Input is @since 1.89 (sap/ui/model/type/Currency.js: "since 1.89.0, if the format option showNumber is set to false, the amount is` &&
                 ` not shown in the control") and is kept 1:1 from the original. A format option is a binding-info parameter, not a control member, so no gate can ever see it - declared by policy, the same route apps` &&
                 ` 264/265 take for boundFilters. Below 1.89 the option is ignored and that Input renders the full amount plus the currency instead of the bare 'USD', which is exactly what the field is in the sample to` &&
                 ` show. Undeclared until 2026-08-23; declaring it moves the port from src/01/02 to src/02/02 per AGENTS section 3.` ) ).

    text1 = `NOTE: the sample's whole point is a JSON model holding a Date OBJECT (UI5Date.getInstance()), which is exactly what an abap2UI5 model can never carry - the model travels as JSON, so a date is a` &&
            ` string. Every binding therefore keeps the original { path, type: 'DateType', formatOptions: { style / relative } } 1:1 and only ADDS formatOptions.source { pattern: 'yyyy-MM-dd' }, which makes` &&
            ` sap.ui.model.type.Date parse the string model value and write it back in the same form (same paradigm as the sibling port 181 TypeDateAsString and app 017). Without the source pattern the type raises` &&
            ` a FormatException on every binding and the view stays empty. // NOTE: the original seeds the CURRENT date (UI5Date.getInstance()); a fixed date (2026-08-02) is used here so the port is deterministic` &&
            ` - a display-only value. The Relative Time form therefore shows the distance to that fixed day, not to today.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.type.Date`                name = `TypeDateAsDate`                                class = `z2ui5_cl_smpc_app_282` path = `src/01/02/z2ui5_cl_smpc_app_282.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.type.Date`                name = `TypeDateAsString`                              class = `z2ui5_cl_smpc_app_181` path = `src/01/02/z2ui5_cl_smpc_app_181.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: Data-type binding paradigm: sap.ui.model.type.Date is pulled via core:require={DateType: 'sap/ui/model/type/Date'} and every DatePicker/Text keeps the original { path, type: 'DateType',` &&
                 ` formatOptions: { style, source: { pattern: 'yyyy-MM-dd' } } } binding 1:1 as a raw binding-info string (braces escaped, path via _bind). The single model field is a yyyy-MM-dd string. // NOTE: The` &&
                 ` original seeds the current date (UI5Date.getInstance().toISOString().slice(0,10)); a fixed date (2026-07-24) is used here so the port is deterministic - a client-only display value.` ) ).

    text1 = `IMPROVISED: The original model value /dtValue is a JS Date object (UI5Date.getInstance()) and every DateTimeType binding has NO source formatOption (the type's default model format is a Date object).` &&
            ` abap2UI5 cannot hold a JS Date object in the JSON model, so a source formatOption { source: { pattern: 'yyyy-MM-dd HH:mm:ss' } } is added to each DateTimeType binding and the field dtvalue is a` &&
            ` parseable datetime string ('2026-07-24 13:30:00') - the abap2UI5 equivalent of the Date-object model. Without it view creation crashes ('Date must be a JavaScript or UI5Date date object'). The` &&
            ` display style/pattern/UTC/relative formatOptions stay 1:1 on top of the added source. Same idiom as app 182 (TypeTimeAsTime) and the CAPABILITIES date-object row. // NOTE: The original seeds the` &&
            ` current time (UI5Date.getInstance()); a fixed datetime (2026-07-24 13:30:00) is used here so the port is deterministic - a client-only display value. The /dtPattern Input placeholder, which the` &&
            ` original controller derives at runtime from the DateTimeType's getPlaceholderText(), is likewise seeded as a static representative value ('e.g. Dec 31, 2026, 11:59:58 PM' - the en medium-style`.
    text1 = text1 && ` DateTime placeholder) since the port has no controller to compute it.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.type.DateTime`            name = `TypeDateTime`                                  class = `z2ui5_cl_smpc_app_183` path = `src/01/02/z2ui5_cl_smpc_app_183.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.type.FileSize`            name = `TypeFileSize`                                  class = `z2ui5_cl_smpc_app_180` path = `src/02/02/z2ui5_cl_smpc_app_180.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: core:require on the view root (module preloading for the FileSizeType alias) is kept 1:1 from the original view.xml; core:require needs UI5 >= 1.74 (declared by policy - the property gate` &&
                 ` cannot see view-root require attributes). // NOTE: Data-type binding paradigm: the FileSize type module is pulled in with core:require='{FileSizeType: sap/ui/model/type/FileSize}' and the Input/Text` &&
                 ` bindings carry type:'FileSizeType' plus formatOptions (min/maxIntegerDigits, min/maxFractionDigits) 1:1. The single model field 'fileSize' (initial 100, kept numeric like the sample's JSON) is` &&
                 ` serialized by abap2UI5 as /FILESIZE, so the original raw '/fileSize' paths are written as '/FILESIZE'.`
        post171 = `core:require on the view root (module preloading for the FileSizeType alias) is kept 1:1 from the original view.xml; core:require needs UI5 >= 1.74 (declared by policy - the property gate cannot see` &&
                 ` view-root require attributes).` ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.type.Float`               name = `TypeFloat`                                     class = `z2ui5_cl_smpc_app_179` path = `src/02/02/z2ui5_cl_smpc_app_179.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = `POST-1.71: core:require on the view root (module preloading for the FloatType alias) is kept 1:1 from the original view.xml; core:require needs UI5 >= 1.74 (declared by policy - the property gate` &&
                 ` cannot see view-root require attributes). // NOTE: Data-type binding paradigm: the Float type module is pulled in with core:require='{FloatType: sap/ui/model/type/Float}' and the Input/Text bindings` &&
                 ` carry type:'FloatType' plus formatOptions (min/maxIntegerDigits, min/maxFractionDigits, preserveDecimals) 1:1. The single model field 'number' (initial '123.456', kept as a string like the sample's` &&
                 ` JSON) is serialized by abap2UI5 as /NUMBER, so the original raw '/number' paths are written as '/NUMBER'.`
        post171 = `core:require on the view root (module preloading for the FloatType alias) is kept 1:1 from the original view.xml; core:require needs UI5 >= 1.74 (declared by policy - the property gate cannot see` &&
                 ` view-root require attributes).` )
      ( module = `sap.ui.core`        control = `sap.ui.model.type.Integer`             name = `TypeInteger`                                   class = `z2ui5_cl_smpc_app_129` path = `src/01/02/z2ui5_cl_smpc_app_129.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `NOTE: Data-type binding paradigm: the Integer type module is pulled in with core:require='{IntegerType: sap/ui/model/type/Integer}' and the Input/Text bindings carry type:'IntegerType' plus` &&
                 ` formatOptions (min/maxIntegerDigits) 1:1. The single model field 'number' (initial '123') is serialized by abap2UI5 as /NUMBER, so the original raw '/number' paths are written as '/NUMBER'.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.core`        control = `sap.ui.model.type.Time`                name = `TypeTimeAsTime`                                class = `z2ui5_cl_smpc_app_182` path = `src/01/02/z2ui5_cl_smpc_app_182.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = `IMPROVISED: The original model value is a JS Date object (UI5Date.getInstance()) and every TimeType binding has NO source formatOption (the type's default model format is a Date object). abap2UI5` &&
                 ` cannot hold a JS Date object in the JSON model, so a source formatOption { source: { pattern: 'HH:mm:ss' } } is added to each TimeType binding and the field is a parseable time string ('13:30:00') -` &&
                 ` the abap2UI5 equivalent of the Date-object model. Without it view creation crashes ('Time must be a Date object'). Same idiom applies to any TypeTime/TypeDateTime sample whose model is an object; a` &&
                 ` plain string binding is not enough (CAPABILITIES date-object row). // NOTE: The original seeds the current time; a fixed time (13:30:00) is used here so the port is deterministic - a client-only` &&
                 ` display value.` ) ).

    text1 = `NOTE: Rebuilt 1:1 from the breadth probe 2026-08-06: the f:ShellBar (title/secondTitle/homeIcon/copilot/search/notifications/productSwitcher/notificationsNumber, its f:menu Menu+MenuItem and its` &&
            ` f:profile Avatar), the four-tab IconTabBar and both f:GridContainers with all EIGHT sap.ui.integration widgets:Card instances, each with its f:GridContainerItemLayoutData and the sample's own` &&
            ` layout/layoutS GridContainerSettings. The earlier port was a single Card with an invented manifest. // NOTE: The eight card manifests come from model/cardManifests.json verbatim. The original carries` &&
            ` them in a named ``manifests>`` model and binds {manifests>/timeline}; abap2UI5 has one default model, so each manifest is a field bound with _bind - the last path segment stays identical, which is` &&
            ` what structural-diff matches. The seven INLINE manifests are bound with json = abap_true, so they reach the Card as JSON objects: a string manifest is read as a manifest URL (Card.createManifest),` &&
            ` which is what the eighth ('component') card genuinely is, so that one stays a plain string bind. Corrected 2026-08-10 with pr/card-manifest-object - before the flag existed the seven inline manifests`.
    text1 = text1 && ` were quoted strings the control would have fetched as URLs. The JSON never enters the view XML (a raw { would be read as a binding), it travels as model data. Asset URLs inside the manifests are` &&
            ` absolutized to the OpenUI5 host per the asset-URL rule; the mock carries them host-relative. The model's unused ninth entry ('table') is not seeded - the sample's view binds eight. // NOTE: The` &&
            ` controller's three members are all resolved without a round-trip. resolveCardUrl is a formatter turning the component card's relative manifest path into a URL - computed in ABAP instead` &&
            ` (thin-frontend rule). onAction toasts the navigation URL off the event, composed through control_global MESSAGE_TOAST. The original guards on the action TYPE and only toasts for Navigation; a` &&
            ` view-wired client action always fires, so the port toasts for every CardActionType. That is reachable here - the Calendar card fires a DateChange action whose parameters carry a selectedDate and no` &&
            ` url - and until 2026-08-23 it printed the literal 'URL: undefined' on every date click. The url argument is guarded now, so a non-Navigation action shows the prefix with an empty value instead;`.
    text1 = text1 && ` suppressing the toast entirely would need a per-firing veto the client action form does not have. onInit's date is DateFormat over UI5Date.getInstance(), a MOVING value, so it is anchored on a fixed` &&
            ` date here (the corpus rule for now/random values, apps 164/181/289). // POST-1.71: sap.f.cards.CardBadgeCustomData (@since 1.128, with icon/state/announcementText/visibilityMode) is kept 1:1 on the` &&
            ` three badged cards, and sap.m.Avatar in the ShellBar's profile is @since 1.73; the Card ``action`` event's ``parameters`` parameter is @since 1.76. All newer than UI5 1.71, declared per the` &&
            ` property-171 policy - the app needs a runtime that has them. // NOTE: The 'component' Card keeps its manifest, which is a URL to a UI5 COMPONENT card (componentCard/manifest.json) rather than an` &&
            ` inline card definition. It points at the OpenUI5 host, so whether the component itself loads depends on that host's CORS policy - the card renders either way, and abap2UI5 has no place to ship a UI5` &&
            ` Component of its own. Declared rather than dropped, so the port keeps the sample's eight cards. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative`.
    text1 = text1 && ` (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared` &&
            ` 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that` &&
            ` day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what made it wrong, so this` &&
            ` wording names the date the count was taken. // NOTE: The root mvc:View carries height="100%", which this original does not set (unlike the neighbouring sap.ui.table / sap.uxap / sap.f samples, whose` &&
            ` roots do). Declared 2026-08-23; structural_diff is skipped for this port, so no gate compares the root attributes.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.integration` control = `sap.ui.integration.Card`               name = `CardsLayout`                                   class = `z2ui5_cl_smpc_app_118` path = `src/02/02/z2ui5_cl_smpc_app_118.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.62`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.f.cards.CardBadgeCustomData (@since 1.128, with icon/state/announcementText/visibilityMode) is kept 1:1 on the three badged cards, and sap.m.Avatar in the ShellBar's profile is @since 1.73; the` &&
                 ` Card ``action`` event's ``parameters`` parameter is @since 1.76. All newer than UI5 1.71, declared per the property-171 policy - the app needs a runtime that has them.` ) ).

    text1 = `NOTE: The sample itself is a Link + Image pointing at the Card Explorer tool (the actual integration Cards live in that tool); Link and Image reproduced 1:1. Since 2026-07-30 the Image press is the` &&
            ` original onImagePress 1:1: URLHelper.redirect('test-resources/sap/ui/integration/demokit/cardExplorer/index.html', true) via follow_up_action( cs_event-urlhelper, REDIRECT + { URL, NEW_WINDOW: true }` &&
            ` ) - the earlier MessageToast substitution is gone. The relative target resolves against the serving origin exactly like the original's (and the Link's href); outside an OpenUI5-hosted origin it 404s` &&
            ` in the new tab, as the original would. // NOTE: the urlhelper REDIRECT on the Image press (new-window open of the relative Card Explorer URL) is unverified in a running system; the same REDIRECT` &&
            ` action class is live-verified in app 084. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_149.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.integration` control = `sap.ui.integration.widgets.Card`       name = `CardExplorer`                                  class = `z2ui5_cl_smpc_app_149` path = `src/01/02/z2ui5_cl_smpc_app_149.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.62`
        notes = text1 ) ).

    text1 = `NOTE: The eleven card manifests come from manifests/cardManifests.json verbatim. The original carries them in a named ``manifests>`` model and the controller pushes them into the Cards on submit;` &&
            ` abap2UI5 has one default model, so each manifest is a bound field on it (there is no view-side counterpart to match: the original's view declares its eleven ``<w:Card>`` tags with only an ``id`` and` &&
            ` a ``<w:layoutData>``, and never binds ``manifest`` at all - the named model is read exclusively in the controller, ``getModel('manifests').getData()`` then ``oCard.setManifest(...)``. So` &&
            ` structural-diff sees no manifest attribute on the original side and nothing here rests on its last-path-segment rule; had the original bound one, the rule would NOT have covered it either, since` &&
            ` MANIFEST_LISTTEST normalizes to ``manifestlisttest`` against the key's ``listtest``.). model_init is deliberately NOT called from main: the sample's Cards start without a manifest and only get one on` &&
            ` the first 'Start loading' press, so the model is seeded in on_event and pushed on that round-trip - the manifest-less initial state is reproduced 1:1. // NOTE: The manifests' relative data request`.
    text1 = text1 && ` URLs (sap/ui/integration/sample/CardsLoading/manifests/listData1.json and friends) are absolutized to the OpenUI5 host` &&
            ` (https://sdk.openui5.org/test-resources/sap/ui/integration/demokit/sample/CardsLoading/manifests/...), because the original resolves them at runtime through sap.ui.require.toUrl inside its sinon stub` &&
            ` and that resolution has no server-side equivalent. The deliberate typo of the 'error' card (listDat2.json instead of listData2.json - it is the sample's error test case) is kept verbatim so the card` &&
            ` still demonstrates the request-failure state. // IMPROVISED: The sample's actual point - an ARTIFICIAL loading delay - is not reproducible: the controller monkey-patches` &&
            ` sap.ui.integration.util.RequestDataProvider._fetch with a sinon stub (sap/ui/thirdparty/sinon-4) that wraps every card data request in a setTimeout of the entered number of seconds. There is no` &&
            ` server-side equivalent for delaying a request the CLIENT issues to a foreign host, and stubbing a UI5 private module from the backend would be exactly the kind of frontend logic the thin-frontend`.
    text1 = text1 && ` principle forbids. The Input keeps its literal value="-1" from the view (the original view holds the value too; only the client stub reads it), so the loading placeholders are still visible for as` &&
            ` long as the real request takes, just not artificially stretched. Consequently onExit's stub restore has no counterpart either. // NOTE: onFormSubmit's two branches are both reproduced: the first` &&
            ` press publishes the manifests through the model (the original's setManifest branch), every later press issues a follow_up_action control_by_id <cardId>/refresh for each of the eleven Cards (the` &&
            ` original's oCard.refresh() branch). refresh( ) is a public sap.ui.integration.widgets.Card method that does not match the FrontendAction deny regex, so it runs through the unlisted-method path. The` &&
            ` second branch is only reached on a LATER press, which is why the e2e module has to press twice - see deviation 5. // NOTE: Unverified in a running system: whether the eleven spliced JSON manifests` &&
            ` render their Cards, whether a manifest seeded as the JSON literal ``null`` shows the original's manifest-less placeholder before the first press (NOT an empty string - that is not JSON and the ajson`.
    text1 = text1 && ` sanity check raises on it, which is why c_no_manifest is ``null``), and the control_by_id refresh wire on the second press. **e2e-verified 2026-08-17** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_341.mjs) - but that run covered the first two only: until 2026-08-21 the module pressed ONCE and then re-counted the cards, so the refresh branch never executed.` &&
            ` The module presses twice since, so the wire is covered from that date, not from the 08-17 stamp. // NOTE: The eleven manifests reach the Cards as real JSON OBJECTS through client->_bind( val =` &&
            ` manifest_x json = abap_true ). This port is what raised that flag: sap.ui.integration.widgets.Card.createManifest branches on the JS type and reads a STRING manifest as a manifest URL (``if (typeof` &&
            ` vManifest === "string") { mOptions.manifestUrl = vManifest; }``, Card.js), so an inline manifest has to arrive as an object - and a model value could not be one: every value is typed ABAP data, and a` &&
            ` manifest's keys (``sap.app``, ``sap.card``, ``_version``) are not valid ABAP field names, so it cannot be modelled as a structure either. The framework now splices such a string into the model as a`.
    text1 = text1 && ` JSON node instead of quoting it (pr/card-manifest-object, implemented upstream), which is why CardsLoading works at all: unlike LazyLoading (app 342) it keeps all eleven manifests in one combined` &&
            ` manifests/cardManifests.json, so there is no per-card URL to bind instead.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.integration` control = `sap.ui.integration.widgets.Card`       name = `CardsLoading`                                  class = `z2ui5_cl_smpc_app_341` path = `src/01/02/z2ui5_cl_smpc_app_341.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.62`
        notes = text1 ) ).

    text1 = `NOTE: The original's GridContainer is empty in the view and the controller creates the Cards in JS (new Card({ manifest, layoutData: new GridContainerItemLayoutData({ columns, minRows: 4 }), dataMode` &&
            ` })) on every submit. The port binds the GridContainer's items aggregation instead and declares the w:Card + f:GridContainerItemLayoutData as its template, so the same cards come out of the model - a` &&
            ` thin frontend with no imperative control creation. structural-diff therefore reports the added controls w:Card and f:GridContainerItemLayoutData, which the original view does not contain; they are` &&
            ` exactly the controller-built ones. // NOTE: The ten manifests are bound as URLs, not as inline JSON: sap.ui.integration.widgets.Card.createManifest reads a STRING manifest as a manifest URL (``if` &&
            ` (typeof vManifest === "string") { mOptions.manifestUrl = vManifest; }``), and LazyLoading keeps one manifest per file, so the URL form is the faithful 1:1 here. The paths are absolutized to the` &&
            ` OpenUI5 host (https://sdk.openui5.org/test-resources/sap/ui/integration/demokit/sample/LazyLoading/manifests/...) because the original resolves them through sap.ui.require.toUrl. The controller's`.
    text1 = text1 && ` baseUrl: './' is dropped with them - with an absolute manifest URL, UI5 uses that URL as the base, which is what the relative resources inside those manifests need. (The bundled-manifest shape that` &&
            ` CANNOT be expressed this way is app 341's; see pr/card-manifest-object.) // NOTE: The controller's aSamples array (key, columns, manifest per card) is inlined verbatim in cards_build as a LOCAL` &&
            ` table: it is never bound, so it stays out of the model that travels on every round-trip. The port keeps the original's cycling (aSamples[i % aSamples.length]) and its dataMode split (Active when the` &&
            ` CheckBox is selected, Auto otherwise), both driven from the two-way bound form fields. manifests/listManifestError.json is archived but not used - the original's aSamples does not list it either. //` &&
            ` IMPROVISED: The artificial request delay is not reproducible and is dropped: the controller monkey-patches sap.ui.integration.util.RequestDataProvider._fetch with a sinon stub` &&
            ` (sap/ui/thirdparty/sinon-4) that wraps every card data request in a setTimeout of the entered number of seconds, and onExit restores it. There is no server-side equivalent for delaying a request the`.
    text1 = text1 && ` CLIENT issues to a foreign host, and stubbing a UI5 private module from the backend is exactly the frontend logic the thin-frontend principle forbids. The 'Time for requesting the card data' Input is` &&
            ` kept 1:1 (the original view gives it no value either) but nothing reads it; the loading placeholders are still visible for as long as the real request takes. // NOTE: Unverified in a running system:` &&
            ` whether the bound items aggregation instantiates the Cards with their per-row manifest URL, columns and dataMode, and whether the number-of-cards Input round-trip rebuilds the container as the` &&
            ` original's destroyItems/addItem loop does. The module counts the rendered Cards against the entered number and no more, so only the COUNT half is covered: a wrong manifest base URL, a dropped columns` &&
            ` or an inverted dataMode renders the same number of (error) cards and passes it. **e2e-verified 2026-08-17** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_342.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.integration` control = `sap.ui.integration.widgets.Card`       name = `LazyLoading`                                   class = `z2ui5_cl_smpc_app_342` path = `src/01/02/z2ui5_cl_smpc_app_342.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.62`
        notes = text1 ) ).

    text1 = `NOTE: BlockLayout with six rows / seven cells across background color shades A-F; the Select (11 ColorSet items) and six of the seven cells' backgroundColorSet (the row-2 image cell carries none, in` &&
            ` the original as in the port) are two-way bound to one model field (initial ColorSet5), reproducing the original {/colorSet} wiring. Cell body texts are reproduced 1:1 (long ones split with &&). //` &&
            ` POST-1.71: sap.m.Label.showColon is used (since UI5 1.98). The BlockLayout entity itself is in scope; showColon renders the label's trailing colon 1:1. // NOTE: The sample's own resources/sample.css` &&
            ` is archived and injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). The view carries customCellImageBackground and the rule behind it - the white` &&
            ` text and the night-sky background - was missing entirely. Its background-image is relative in the original and is absolutized to the OpenUI5 host per the asset-URL rule, like every other image in` &&
            ` this corpus. Found by scripts/probes/orphan-style-class-probe.mjs. // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5`.
    text1 = text1 && ` app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED` &&
            ` 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of` &&
            ` them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.BlockLayout`             name = `BlockLayoutCustomBackground`                   class = `z2ui5_cl_smpc_app_140` path = `src/02/02/z2ui5_cl_smpc_app_140.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-27): verified in a running system 2026-07-27 - BlockLayout renders all 6 rows/7 cells with color shades after the attribute-line repair. **e2e-verified 2026-09-12** (interaction` &&
                 ` module meta/interactions/z2ui5_cl_smpc_app_140.mjs: 6 rows / 7 cells render, the six bound cells boot at ColorSet5 with the sapUiBlockLayoutCellColor5A class, and picking ColorSet2 from the Select` &&
                 ` repaints all six with no round trip)`
        notes = text1
        post171 = `sap.m.Label.showColon is used (since UI5 1.98). The BlockLayout entity itself is in scope; showColon renders the label's trailing colon 1:1.` ) ).

    text1 = `NOTE: The six core:Fragment references (sap.ui.layout.sample.BlockLayoutCustomBackgroundPerCell.ColorSelect) are inlined: abap2UI5 serves one view per round-trip, so a fragmentName has no file to` &&
            ` resolve. The fragment's content (VBox with the two Labels and the two Selects over eleven ColorSet and six ColorShade core:Item entries) is written into each of the six cells, so the rendered result` &&
            ` is identical. structural-diff reports the dropped core:Fragment references (6 vs 0) and separately the core:HTML style leaf below; the five-fold multiplication that inlining into six cells produces -` &&
            ` VBox 2 vs 7, Label 5 vs 15, Select 2 vs 12, core:Item 17 vs 102 in the rendered view - is no longer a static count, because the fragment is built once in color_select( ) and its items in loops (see` &&
            ` the loop NOTE). All of them are this one substitution; the control names stay in this prose so the gate reads them as declared. // NOTE: Each cell keeps its own element binding: binding="{/cellN}" is` &&
            ` built from client->_bind( val = cellN path = abap_true ) rather than a hard-coded path, and the model carries one nested structure per cell (colorset/colorshade) so the fragment's relative`.
    text1 = text1 && ` {COLORSET}/{COLORSHADE} bindings resolve against the cell context exactly as in the original - no flattening to the model root was needed, and the two Selects write back into their own cell. // NOTE:` &&
            ` resources/sample.css is injected through a core:HTML <style> leaf (abap2UI5 ships no separate stylesheet), so the port adds one core:HTML control the original view does not have. The CSS braces are` &&
            ` escaped \{ \} in a backtick literal so the XMLView parser does not read them as bindings, and the background-image url("Night_sky.jpg") is absolutized to the OpenUI5 host` &&
            ` (https://sdk.openui5.org/test-resources/sap/ui/layout/demokit/sample/BlockLayoutCustomBackgroundPerCell/resources/Night_sky.jpg) per the asset-URL rule - relative to the served app it would not` &&
            ` resolve. // NOTE: Unverified in a running system: whether the per-cell context binding round-trips (a Select changing its cell's colorSet/colorShade writes back into the nested ABAP structure) and` &&
            ` whether the injected stylesheet paints the image cell. **e2e-verified 2026-08-17** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_343.mjs) - but that run proved nothing: the module`.
    text1 = text1 && ` picked ColorSet6, which model_init seeds into EVERY cell, so the pressed Select was asserted to hold the value it already had (true even with no binding at all, since SelectList sets the pressed item` &&
            ` locally) and the neighbour was asserted unchanged when nothing had asked it to change. A port with a completely dead binding passed it. The module picks ColorSet3 since 2026-08-24, so the round-trip` &&
            ` half is covered from that date; the stylesheet half is still only covered by the render. // NOTE: The ColorSelect fragment is built by one helper and in loops (the app 520/599 idiom, the helper form` &&
            ` of app 569): view_display writes the six cells one statement each and hands every picker cell to color_select( ), which adds the fragment's VBox with its two Labels and two Selects to the handle it` &&
            ` is given; the eleven ColorSet core:Items are a DO over sy-index (key and text ColorSet1..ColorSet11, the last text with its theme note) and the six shade core:Items a LOOP AT over a local table of`.
    text1 = text1 && ` key/text rows. structural-diff therefore reads the port as dynamic - loop-built control counts cannot match statically - and the render gate replays the helper per call with one pass of each loop.` &&
            ` The rendered view is identical to the unrolled form it replaced (same controls, attributes, ids, order and values; both expanded and diffed, 2026-09-12): a rewrite of the source, not of the` &&
            ` behaviour, so the status is untouched.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.BlockLayout`             name = `BlockLayoutCustomBackgroundPerCell`            class = `z2ui5_cl_smpc_app_343` path = `src/01/02/z2ui5_cl_smpc_app_343.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: The Slider's ``liveChange`` attribute is dropped (the expression binding needs no event). **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): one` &&
            ` keyboard step on the Slider moves the containerLayout (VerticalLayout) width to 99% with no round-trip - the expression binding replaces onSliderMoved completely. Residual: the BlockLayout's own` &&
            ` reflow inside the resized layout. // NOTE: The SegmentedButton.selectedKey and the BlockLayout.background are both two-way bound to the same /SELECTEDBACKGROUND field, so changing the segmented` &&
            ` button updates the BlockLayout background entirely on the client with no round-trip (the app-048/003 shared-binding idiom, matching the original {/selectedBackground} on both). The field is seeded` &&
            ` with the enum default Default (the original leaves /selectedBackground undefined; an empty string would fail BlockBackgroundType validateProperty). Live sync unverified. **e2e-verified 2026-08-04**` &&
            ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_214.mjs). (Scope corrected 2026-08-23: that module runs sliderDrivenWidth only - it never clicks a SegmentedButtonItem and never reads`.
    text1 = text1 && ` BlockLayout.getBackground( ), so the shared-binding claim beside it is unverified. The mechanism is sound: SegmentedButton writes selectedKey through setProperty, and ManagedObject.setProperty runs` &&
            ` updateModelProperty regardless of the suppress flag.) // NOTE: The 123 /ProductCollection rows are inlined from the shared mock ui5/mock/products.json; only the columns the table binds are kept` &&
            ` (productid,name,suppliername,width,depth,height,dimunit,weightmeasure,weightunit,price,currencycode). Dimension and weight fields are TYPE string to preserve exact decimals for the display templates;` &&
            ` price is TYPE p DECIMALS 2 for the sap.ui.model.type.Currency binding. Data verified against the mock.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.BlockLayout`             name = `BlockLayoutDefault`                            class = `z2ui5_cl_smpc_app_214` path = `src/01/02/z2ui5_cl_smpc_app_214.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: The Slider's imperative onSliderMoved handler (setWidth on the container) is replaced by the thin-frontend equivalent: the Slider value is two-way bound and the VerticalLayout containerLayout` &&
            ` width is an expression binding {= ${/SLIDERVALUE} + '%' }. The Slider's liveChange attribute is dropped (no event round-trip needed). structural-diff does not flag the literal value=100 becoming a` &&
            ` binding. // NOTE: The SegmentedButton selectedKey and the BlockLayout background are two-way bound to one default-model field selectedbackground, reproducing the original {/selectedBackground}` &&
            ` wiring; it is seeded with the enum default Default (a valid BlockBackgroundType) because an empty enum value crashes validateProperty. The original controller loads sap/ui/demo/mock/products.json as` &&
            ` the unnamed model but no view binding references it, so it is not reproduced. // NOTE: This port rehosts NOTHING - the sentence that stood here until 2026-08-23 claimed the sample's asset paths were` &&
            ` host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The`.
    text1 = text1 && ` values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had` &&
            ` stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale` &&
            ` absolute count is what made it wrong, so this wording names the date the count was taken. The archived sample contains no relative asset path at all: its five hrefs are already absolute upstream and` &&
            ` are copied verbatim, and the port inlines no mock. The sweep that added the sentence to 126 ports tagged this one too many.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.BlockLayout`             name = `BlockLayoutLinkTitle`                          class = `z2ui5_cl_smpc_app_223` path = `src/01/02/z2ui5_cl_smpc_app_223.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.34`
        notes = text1 ) ).

    text1 = `NOTE: The sample's css/main.css is now shipped with the port: injected through a core:HTML leaf carrying a <style> block (the documented CAPABILITIES form), so the port carries six core:HTML controls` &&
            ` where the original view has five - the original loads the stylesheet through its manifest's sap.ui5/resources/css entry. The single .sapUiLayoutCSSGrid .stylePageLayout rule is verbatim,` &&
            ` whitespace-collapsed, braces escaped \{ \} in a backtick literal. Until 2026-07-28 the five grid tiles carried the stylePageLayout class with no stylesheet behind them, so they rendered as unstyled` &&
            ` text instead of the blue rounded boxes the sample shows. The stylesheet was also missing from ui5/sap.ui.layout/CSSGrid/ (a §4 archive gap) and is archived in the same change. // NOTE: onSliderMoved` &&
            ` sets the Panel width to value + "%" imperatively. Rebuilt on the client (the app-053 shape): the Slider value is two-way bound to slider_value, seeded with the original's literal 100, and the Panel` &&
            ` width carries the expression binding {= slider_value + '%' } instead of the literal 100%. The liveChange attribute is therefore dropped. Until 2026-07-28 the port routed liveChange to a SLIDER_MOVED`.
    text1 = text1 && ` backend event that recomputed the width server-side - correct but one full round-trip per drag step; the expression binding is the thin-frontend form and needs none. // NOTE: The five core:HTML tiles` &&
            ` carry raw HTML in the content attribute (the builder xml-escapes it, matching the original's escaped &lt;header&gt;/&lt;aside&gt;/&lt;article&gt;/&lt;footer&gt; content 1:1, including the original's` &&
            ` quirks: the double space in '<aside  ...>Navigation</aside >' and the mismatched '<aside ...>Related Links</article>' close tag).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `CSSGrid`                                       class = `z2ui5_cl_smpc_app_124` path = `src/01/02/z2ui5_cl_smpc_app_124.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: The original's onRadioButtonSelected switches the CSSGrid gridAutoFlow per selectedIndex imperatively (0 Column, 1 ColumnDense, 2 Row, 3 RowDense). Rebuilt on the client with no round-trip: the` &&
            ` RadioButtonGroup gets a two-way bound selectedIndex (an attribute the original does not carry - its controller reads the index off the event) seeded 0, and gridAutoFlow carries the same switch as an` &&
            ` expression binding instead of the literal Column. The select attribute is therefore dropped; before this rework it was wired to an RB_SELECT backend event this class never dispatched (a dead wire,` &&
            ` pattern-lint dead-event-wire). // IMPROVISED: The 'Reveal Grid' ToggleButton loses its press attribute. The original calls RevealGrid.toggle('grid1', view) from` &&
            ` sap/ui/layout/sample/GridAutoFlow/RevealGrid/RevealGrid - a helper module that ships inside the sample folder, not in any UI5 library: it walks the grid's DOM and overlays absolutely positioned` &&
            ` outline elements over every grid cell. There is no control, property or whitelisted control method that expresses it, and the module itself is not loadable in an abap2UI5 app, so the button stays in`.
    text1 = text1 && ` the view (structurally 1:1) but does nothing. Previously the press fired a REVEAL backend event no branch handled, which looked like behaviour and was none. The sample's actual subject - gridAutoFlow` &&
            ` - is fully reproduced; the reveal overlay is a debugging aid around it. // NOTE: grid:CSSGrid with gridAutoFlow + 10 VBox demo boxes, four carrying GridItemLayoutData row/column spans, 1:1. // NOTE:` &&
            ` The sample's own css/main.css is archived and injected since 2026-08-21. All ten grid tiles carry class="demoBox" and the rules behind it (.sapMFlexBox.demoBox rounded blue box, .demoBox .sapMText` &&
            ` white) were missing entirely, so the tiles rendered as bare text instead of the blue rounded boxes the sample is a picture of - the same gap apps 122/124/133 had, and the sibling ports 346/347/271` &&
            ` already inject this very stylesheet. css/main.css now sits in ui5/sap.ui.layout/GridAutoFlow/css/. That did NOT close the AGENTS section 4 archive gap, as this sidecar claimed until 2026-08-23: the` &&
            ` manifest also lists RevealGrid/RevealGrid.js - the controller's only dependency - and it was absent while RevealGrid.css was there. It is archived since 2026-08-23, taken from the two sibling`.
    text1 = text1 && ` sap.ui.layout samples that carry the same module byte-identically (GridResponsiveness, GridResponsiveColumnLayout), the network being out of reach here. scripts/check-archive.mjs now gates the whole` &&
            ` class of gap. It reaches the view through an added core:HTML style leaf, which the original view does not declare.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `GridAutoFlow`                                  class = `z2ui5_cl_smpc_app_145` path = `src/01/02/z2ui5_cl_smpc_app_145.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `IMPROVISED: The 'Reveal Grid' ToggleButton keeps its label but loses its press attribute: onRevealGrid calls RevealGrid.toggle(['grid1','grid2'], view) from the sample-local module` &&
            ` RevealGrid/RevealGrid.js, which measures the rendered grid and draws an absolutely positioned outline overlay over every grid cell. That is a sample-only JS helper with its own CSS, not a control,` &&
            ` property or whitelisted control method, and it is not loadable in an abap2UI5 app - the same drop as apps 145/222/271. onExit's RevealGrid.destroy goes with it, and RevealGrid/RevealGrid.css is not` &&
            ` injected either since nothing renders those classes. // NOTE: css/main.css (.sapMFlexBox.demoBox, .demoBox .sapMText, .sapMText.message) is injected through an extra core:HTML <style> leaf, because` &&
            ` abap2UI5 ships no separate stylesheet - without the rules behind them the 26 demo boxes would render as unstyled text (the app-122/124 lesson). The CSS braces are escaped \{ \} in a backtick literal` &&
            ` so the XMLView parser does not read them as bindings; the view also declares xmlns:core for it, which the original does not need. // NOTE: Fully static port: the sample has no model and - once the`.
    text1 = text1 && ` RevealGrid toggle is dropped - no event either, so the class is the app-051 shape (view_display on check_on_init and again on check_on_navigated (the house dispatcher shape), no model_init, no` &&
            ` on_event).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `GridAutoRows`                                  class = `z2ui5_cl_smpc_app_346` path = `src/01/02/z2ui5_cl_smpc_app_346.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: Both controller handlers become plain bindings, so the port drops the Slider.liveChange and the three Input.change attributes that only carried the imperative setters. (a) onSliderMoved calls` &&
            ` byId('panelCSSGrid').setWidth(fValue + '%'); sap.m.Panel HAS a width property, so the Slider's value is two-way bound and the Panel's width is an expression binding over it - roundtrip-free, the` &&
            ` app-222/176 idiom. (b) onInputChanged switches on the Input id and calls setGridGap / setGridRowGap / setGridColumnGap; each Input is two-way bound to its own field and the CSSGrid binds the matching` &&
            ` property to the SAME field, so typing a value reaches the grid with no dispatcher at all. // NOTE: css/main.css (.sapMFlexBox.demoBox, .demoBox .sapMText, .sapMText.infoText) is injected through an` &&
            ` extra core:HTML <style> leaf - abap2UI5 ships no separate stylesheet, and without the rules the ten demo boxes render unstyled. The CSS braces are escaped \{ \} in a backtick literal so the XMLView` &&
            ` parser does not read them as bindings; the view declares xmlns:core for it, which the original does not need. // NOTE: Unverified in a running system: whether an empty gridRowGap / gridColumnGap`.
    text1 = text1 && ` serializes acceptably for the CSSGrid's CSSSize properties (the two Inputs carry no value attribute in the original either, so the grid starts on gridGap alone), and whether the Panel width` &&
            ` expression follows the slider live. **e2e-verified 2026-08-17** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_347.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `GridGap`                                       class = `z2ui5_cl_smpc_app_347` path = `src/01/02/z2ui5_cl_smpc_app_347.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.ui.layout.cssgrid.ResponsiveColumnLayout (the CSSGrid customLayout this sample is about) and sap.ui.layout.cssgrid.ResponsiveColumnItemLayoutData (its per-card columns/rows layout data)` &&
            ` are both @since 1.72, one minor release past the 1.71 floor, and so is the layout's layoutChange event with its layout parameter. All are kept 1:1 - the sample's owning control` &&
            ` sap.ui.layout.cssgrid.CSSGrid is in scope and the sample is nothing without them; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.72. // IMPROVISED: The` &&
            ` 'Reveal Grid' ToggleButton keeps its label but loses its press attribute: onRevealGrid calls RevealGrid.toggle('grid1', view) from the sample-local module RevealGrid/RevealGrid.js, which measures the` &&
            ` rendered grid and draws an absolutely positioned outline overlay over every grid cell. That is a sample-only JS helper with its own CSS, not a control, property or whitelisted control method, and it` &&
            ` is not loadable in an abap2UI5 app - the same drop as apps 145/222/271/346. onExit's RevealGrid.destroy goes with it. // NOTE: Slider.liveChange is dropped for a binding: onSliderMoved calls`.
    text1 = text1 && ` byId('panelCSSGrid').setWidth(fValue + '%'), and sap.m.Panel HAS a width property, so the Slider's value is two-way bound and the Panel's width is an expression binding over it - roundtrip-free, the` &&
            ` app-222/176 idiom. onLayoutChange stays a real round-trip because there is nothing to bind against: the layout reports its breakpoint in the layoutChange event, so the wire carries` &&
            ` ${$parameters>/layout} to the backend and on_event writes it into the field the 'Current breakpoint' Text binds - the same write the original's setProperty('/currentBreakpoint', ...) does. // NOTE:` &&
            ` The original's model has one field (currentBreakpoint, initially empty) and the view binds it as {/currentBreakpoint}; folded onto the one default model it is client->_bind( currentbreakpoint ) -` &&
            ` same data, same leaf name. It is seeded empty like the original, so the Text reads 'Current breakpoint: ' until the first layoutChange fires. // NOTE: Unverified in a running system: whether the` &&
            ` layoutChange round-trip fills the breakpoint Text as the panel is resized, and whether the Panel width expression follows the slider live. **e2e-verified 2026-08-25** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_348.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `GridResponsiveColumnLayout`                    class = `z2ui5_cl_smpc_app_348` path = `src/02/02/z2ui5_cl_smpc_app_348.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.layout.cssgrid.ResponsiveColumnLayout (the CSSGrid customLayout this sample is about) and sap.ui.layout.cssgrid.ResponsiveColumnItemLayoutData (its per-card columns/rows layout data) are both` &&
                 ` @since 1.72, one minor release past the 1.71 floor, and so is the layout's layoutChange event with its layout parameter. All are kept 1:1 - the sample's owning control sap.ui.layout.cssgrid.CSSGrid` &&
                 ` is in scope and the sample is nothing without them; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.72.` ) ).

    text1 = `NOTE: The sample's css/main.css (.demoBox tile colours, .sapMText.message bold) is injected through a core:HTML <style> leaf (app 122/124 precedent), so that core:HTML control is EXTRA against the` &&
            ` original view (original 0 vs port 1). The CSS is reproduced verbatim, only whitespace-collapsed. // NOTE: The Slider's liveChange attribute is dropped: onSliderMoved calls` &&
            ` byId('panelCSSGrid').setWidth(fValue + '%'), and sap.m.Panel has a width property, so the value is two-way bound and the width is the expression binding {= ${/SLIDER_VALUE} + '%' } (app 214/270` &&
            ` form). Same rendered behaviour without a round-trip. // NOTE: The SegmentedButton's selectionChange attribute is dropped: onSegmentedButtonChange calls grid1.getCustomLayout().setContainerQuery(key` &&
            ` === 'true'). The button's selectedKey and the GridResponsiveLayout's containerQuery share one two-way bound field here - selectedKey={/CONTAINER_QUERY} and containerQuery={= ${/CONTAINER_QUERY} ===` &&
            ` 'true' }, the string-to-boolean step the controller does in JS - so switching the segment reconfigures the layout client-side, with no round-trip and no imperative setter. // IMPROVISED: The 'Reveal`.
    text1 = text1 && ` Grid' ToggleButton keeps its label but loses its press handler: onRevealGrid calls RevealGrid.toggle('grid1', view) from the sample-local module RevealGrid/RevealGrid.js, which draws a debug overlay` &&
            ` of the grid lines on top of the rendered DOM. That is a sample-only JS helper with its own CSS, not an abap2UI5 capability (same drop as app 145's RevealGrid); the button therefore does nothing.` &&
            ` onExit's RevealGrid.destroy goes with it. // NOTE: onAfterRendering seeds the info Text from the layout that is active after the first render` &&
            ` (getCustomLayout().getActiveGridSettings().sParentAggregationName). The backend cannot read that, so the port ships the view's own initial text ('Layout size is: ') and the first real value arrives` &&
            ` with the first layoutChange event - which UI5 fires whenever the active GridSettings change, but NOT on the initial evaluation. **Corrected 2026-08-23**: onGridAfterRendering calls setActiveLayout` &&
            ` with bTriggerLayoutChange = false, and _sActiveLayout starts as 'layout' while _getLayoutToApply falls back to 'layout' for any missing aggregation - this sample supplies only`.
    text1 = text1 && ` layoutS/layout/layoutXL, so Tablet and Desktop both resolve to 'layout' and hit the early exit. The info Text therefore shows the bare 'Layout size is: ' at every load, and between 600 and 1440px no` &&
            ` event ever arrives to fill it, where the original's onAfterRendering seeds it from getActiveGridSettings( ). // NOTE: Unverified in a running system: (a) the layoutChange round-trip` &&
            ` (${$parameters>/layout}) writing 'Layout size is: layoutS/layoutM or layoutL/layoutXL' into the bound Text; (b) that the containerQuery expression binding really re-evaluates the GridResponsiveLayout` &&
            ` when the segment switches; (c) that the three GridSettings breakpoints lay the twelve tiles out as in the original. **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_271.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `GridResponsiveness`                            class = `z2ui5_cl_smpc_app_271` path = `src/01/02/z2ui5_cl_smpc_app_271.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: Both controller handlers become plain bindings, so the port drops the Slider.liveChange and the two ComboBox.change attributes that only carried the imperative setters. (a) onSliderMoved calls` &&
            ` byId('panelCSSGrid').setWidth(fValue + '%'); sap.m.Panel HAS a width property, so the Slider's value is two-way bound and the Panel's width is an expression binding over it - roundtrip-free, the` &&
            ` app-222/176 idiom. (b) onInputChanged calls setGridTemplateRows / setGridTemplateColumns depending on which ComboBox fired; each ComboBox value is two-way bound and the CSSGrid binds the matching` &&
            ` property to the SAME field, so picking or typing a template reaches the grid with no dispatcher. // NOTE: The grid's gridTemplateColumns starts from the cTem ComboBox's preselected item text "1fr` &&
            ` 1fr" rather than the view's literal "repeat(2,1fr)": both express the same two equal columns, and binding the grid to the ComboBox field is what makes the selection drive the grid at all.` &&
            ` gridTemplateRows keeps "1fr 2fr 1fr", which is both the view literal and the rFr item text. The ComboBoxes keep their selectedKey attributes 1:1. // NOTE: css/main.css (.sapMFlexBox.demoBox and the`.
    text1 = text1 && ` white .sapMTitle/.sapMText inside it) is injected through an extra core:HTML <style> leaf - abap2UI5 ships no separate stylesheet, and without the rules the six demo boxes render unstyled. The CSS` &&
            ` braces are escaped \{ \} in a backtick literal so the XMLView parser does not read them as bindings. // NOTE: Unverified in a running system: whether a ComboBox free-text entry (the original reads` &&
            ` the change event's value, not the selected key) reaches the bound field and re-templates the grid. Note there is no round TRIP: this port has no event wire at all, so the effect is pure client-side` &&
            ` two-way binding on the ComboBox value with the CSSGrid's gridTemplateRows bound to the same field. **e2e-verified 2026-08-17** - but that run settled nothing: the module asserted box.inputValue( ),` &&
            ` the DOM input's own value that Playwright had just typed (true with no binding at all), plus that some element carrying a sapUiLayoutCSSGrid class is visible (true for any rendered grid). Neither` &&
            ` read gridTemplateRows. The module reads it off the control since 2026-08-24 (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_349.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `GridTemplateRows`                              class = `z2ui5_cl_smpc_app_349` path = `src/01/02/z2ui5_cl_smpc_app_349.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: The sample's css/main.css (the .demoBox / .demoInnerBox colours and radii the grid tiles carry) is injected through a core:HTML leaf with a <style> element in its content attribute - the app` &&
            ` 122/124 precedent, since a port ships no stylesheet. That core:HTML control is therefore EXTRA against the original view (original 0 vs port 1); the CSS itself is reproduced verbatim, only` &&
            ` whitespace-collapsed. // NOTE: The Slider's liveChange attribute is dropped: onSliderMoved calls byId('panelCSSGrid').setWidth(fValue + '%'). sap.m.Panel HAS a width property, so the slider value is` &&
            ` two-way bound ({/SLIDER_VALUE}) and the Panel width is the expression binding {= ${/SLIDER_VALUE} + '%' } - the app 214 form, which needs no round-trip and no imperative setter. Same rendered` &&
            ` behaviour, so NOTE rather than IMPROVISED. // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): driving the Slider with the keyboard resizes` &&
            ` the Panel with no round-trip: the value goes 100 -> 99 and the Panel's width follows as '99%' through the expression binding, so the controller's jQuery width is fully replaced. The nested CSSGrid`.
    text1 = text1 && ` renders its E/F/G boxes. Residual (nothing headless can assert): the visual grid placement from GridItemLayoutData (gridColumn '1 / 3'), which needs the theme CSS the harness does not load.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `NestedGrids`                                   class = `z2ui5_cl_smpc_app_270` path = `src/01/02/z2ui5_cl_smpc_app_270.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.60`
        notes = text1 ) ).

    text1 = `NOTE: The four core:Fragment references (view/Group1..Group4) are inlined into the one port view: abap2UI5 serves one view per round-trip, so a fragmentName has no file to resolve. Their content (the` &&
            ` VBox with its cssgrid:ResponsiveColumnItemLayoutData, the group Title and the f:GridContainer with all its Cards) is reproduced verbatim, so the rendered result is identical; the dropped` &&
            ` core:Fragment elements are what structural-diff reports. All four fragments declare sap.m as their default xmlns like the main view, so no control changes prefix. // POST-1.71:` &&
            ` sap.ui.layout.cssgrid.ResponsiveColumnLayout and sap.ui.layout.cssgrid.ResponsiveColumnItemLayoutData are @since 1.72 (with the layout's layoutChange event and its layout parameter), sap.m.Avatar is` &&
            ` @since 1.73, and sap.f.GridContainer's columnsChange event carries its columns parameter. All are kept 1:1 - the sample's owning control sap.ui.layout.cssgrid.CSSGrid is in scope and the layout is` &&
            ` what the sample is about; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.73. // NOTE: The named ``home>`` model (model/home.json) is folded onto the one`.
    text1 = text1 && ` default model: homeIconSrc, user/iconSrc, currentBreakpoint and /layout/groupN/columns/current become fields, two of them with the same leaf names and two NOT: home>/user/iconSrc becomes USER_ICONSRC` &&
            ` (the leaf alone would be ICONSRC) and /layout/groupN/columns/current becomes GROUP1_COLUMNS..GROUP4_COLUMNS (the leaf alone would be CURRENT); homeiconsrc and currentbreakpoint do keep theirs.` &&
            ` structural-diff is not confirming this either way - the originals are complex binding-infos ({path:'home>/user/iconSrc', formatter:'.formatSrc'}), which SIMPLE_BIND excludes, so the gate skips them` &&
            ` rather than matching them. The per-breakpoint column table is NOT shipped to the frontend - onLayoutChangeMain's lookup ('the value for this breakpoint, else default') is arithmetic, so it lives in` &&
            ` ABAP (columns_apply) and only the four resulting column counts are bound, per the thin-frontend principle. onColumnsChange is the same move: the grid's columns parameter reaches the backend, which` &&
            ` decides 4 vs 5 columns for the usersCard/upfCard exactly as the original's ``iGridColumns < 14 ? 4 : 5``. // NOTE: /layout/<group>/columns/current is null in model/home.json and the original only`.
    text1 = text1 && ` fills it on the first layoutChange; the port seeds each group with its own ``default`` value (10/4/7/4) instead, so the grid has a valid column count on the very first render rather than an unset` &&
            ` one. The formatSrc formatter (sap.ui.require.toUrl over the sample folder) is resolved in ABAP - the two images and the two card manifest URLs are absolutized to the OpenUI5 host per the asset-URL` &&
            ` rule. // NOTE: The two sap.ui.integration widgets:Card instances (usersCard, logonRequestsCard) get their manifest as a bound URL, which is the shape Card.createManifest reads a string manifest as -` &&
            ` the original does exactly the same in onInit (setManifest(this.formatSrc('cards/UsersCard/manifest.json'))). Unlike app 341's bundled manifests this needs no framework change; see` &&
            ` pr/card-manifest-object for the case that does. // IMPROVISED: The two 'Reveal Grid' ToggleButtons keep their labels but lose their press attributes: onRevealGridMain/onRevealGridGroups call` &&
            ` RevealGrid.toggle from the sample-local module RevealGrid/RevealGrid.js, which measures the rendered grid and draws an absolutely positioned outline overlay over every grid cell. That is a`.
    text1 = text1 && ` sample-only JS helper with its own CSS (the manifest's only resources/css entry), not a control, property or whitelisted control method - the same drop as apps 145/222/271/346/348, and RevealGrid.css` &&
            ` is not injected either since nothing renders those classes. onExit's RevealGrid.destroy goes with it. // NOTE: The Avatar.press and Link.press attributes of the five 'Frequent Operations' tiles are` &&
            ` dropped: the original wires them all to onTilePress, which is an EMPTY function. Keeping them would mean a backend round-trip that does nothing (the dead-event-wire class), so the attributes go away` &&
            ` rather than being wired to a no-op. // NOTE: Unverified in a running system: the layoutChange and columnsChange round-trips re-laying out the four groups and widening the two group-1 cards, and` &&
            ` whether the two card manifest URLs load from the OpenUI5 host (their images are relative to the manifest, which is what an absolute manifest URL makes resolvable). **e2e-verified 2026-08-25**` &&
            ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_350.mjs). // NOTE: The original's Component.js init calls IconPool.registerFont({ fontFamily: 'SAP-icons-TNT', fontURI:`.
    text1 = text1 && ` sap.ui.require.toUrl('sap/tnt/themes/base/fonts/') }), and that registration is the only thing that makes the sap-icon://SAP-icons-TNT/... URI in Group2.fragment.xml resolvable. An abap2UI5 app has` &&
            ` no Component of its own to run that in, and IconPool is a module-level SINGLETON rather than a control, so control_by_id cannot address it. The port issues it as a four-line follow_up_action(` &&
            ` control_global, ICON_POOL / registerFont / SAP-icons-TNT / sap/tnt/themes/base/fonts/ ) from the init branch since 2026-08-28 - the frontend resolves the module path through sap.ui.require.toUrl and` &&
            ` registers a family once per session, so a repeat call costs nothing. The tile therefore renders its glyph. This was an IMPROVISED deviation until then: the URI was kept verbatim and the first` &&
            ` Frequent Operations tile rendered without its glyph - in a real system as well as in the harness, since neither sap.tnt's library.js nor abap2UI5 registers the collection (both grepped 2026-08-21,` &&
            ` zero hits). Closing it needed an upstream registerFont-style global target, which abap2UI5 gained on 2026-08-24 and which reached the reproducible builds when A2UI5_PIN moved on 2026-08-28; the`.
    text1 = text1 && ` pinned linter's GLOBAL_TARGETS mirror carries ICON_POOL as of @abap2ui5/linter 0.5.1. The render_smoke skip beside this stays, and for a NEW reason it now states: a follow_up_action is not part of` &&
            ` the view, so the linter's render harness cannot execute it and the harness alone still sees an unregistered collection. The e2e interaction module is where the registration is proven. // NOTE: The` &&
            ` sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
            ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by` &&
            ` all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since` &&
            ` today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.cssgrid.CSSGrid`         name = `ProductHomeLayout`                             class = `z2ui5_cl_smpc_app_350` path = `src/02/02/z2ui5_cl_smpc_app_350.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.60`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.layout.cssgrid.ResponsiveColumnLayout and sap.ui.layout.cssgrid.ResponsiveColumnItemLayoutData are @since 1.72 (with the layout's layoutChange event and its layout parameter), sap.m.Avatar is` &&
                 ` @since 1.73, and sap.f.GridContainer's columnsChange event carries its columns parameter. All are kept 1:1 - the sample's owning control sap.ui.layout.cssgrid.CSSGrid is in scope and the layout is` &&
                 ` what the sample is about; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.73.` ) ).

    text1 = `NOTE: Two of the original's three controller behaviours are now reproduced rather than wired to nothing. (a) breakpointChanged carries its own currentBreakpoint event parameter to the backend (t_arg` &&
            ` ${$parameters>/currentBreakpoint}) and on_event enables the Toggle button exactly when it is 'S' - what _updateToggleButtonState does; the port therefore adds an enabled attribute to the Toggle` &&
            ` Button, which the original sets from the controller. (b) handleToggleClick calls DynamicSideContent.toggle( ), which is driven directly through control_by_id ( DynamicSideContent / toggle ) since` &&
            ` 2026-08-21. It was a bound showSideContent flipped in on_event until then, on the claim that showSideContent is the property toggle( ) writes - and that is the wrong way round, so the button changed` &&
            ` nothing on screen. sap/ui/layout/DynamicSideContent.js shows toggle( ) leaving showSideContent alone while it is at its true default and swapping the private _MCVisible/_SCVisible pair instead;` &&
            ` setShowSideContent, conversely, re-derives those flags through _setResizeData( S ) and ends with _MCVisible = true, so the flip could never reveal the side content on the one breakpoint where the`.
    text1 = text1 && ` button is enabled. An unlisted public method is callable through the wire (the denylist covers teardown, model/binding swaps, event-handler tampering and the render lifecycle), and toggle( ) takes no` &&
            ` argument, so nothing can be lost in the cast. Residual, recorded 2026-08-23: _updateToggleButtonState has TWO call sites upstream - handleBreakpointChanged and onAfterRendering - and only the first` &&
            ` is reproduced. DynamicSideContent fires breakpointChanged exactly once, in _setBreakpointFromWidth, and only when the new breakpoint differs from the previous one; on the first determination` &&
            ` _currentBreakpoint is undefined, so the event never fires for the initial layout. An app opened at breakpoint S therefore renders with the Toggle button disabled - the one breakpoint where toggle( )` &&
            ` does anything - until the container crosses a breakpoint and comes back. There is no declarative substitute: the S breakpoint is the CONTAINER's (containerQuery=true, width < 720px), not a device` &&
            ` media range, so the device> model cannot express it. // NOTE: The width Slider's liveChange is reproduced roundtrip-free since 2026-08-05. The original's handleSliderChange delegates to`.
    text1 = text1 && ` updateControlWidth, whose body is var $DSCContainer = this.byId('sideContentContainer').$(); if (iValue) { $DSCContainer.width(iValue + '%'); } - i.e. it writes a CSS width straight onto the rendered` &&
            ` DOM node, and sap.m.Page has no width property to bind. The ``css`` control method added upstream for this class of gap (pr/control-inline-style) does the same write from the wire: follow_up_action(` &&
            ` control_by_id, sideContentContainer / css / width / ${$parameters>/value} + '%' ), the arg being a full UI5 expression (CAPABILITIES). The Slider keeps its value='100' literal and now resizes the` &&
            ` page again. Residual, recorded 2026-08-23: the guard is NOT reproduced. sap.m.Slider.min defaults to 0, so the left stop is reachable; the original skips the write there and the container keeps its` &&
            ` last width, while the port writes width: 0% and collapses it. A wire that writes a computed value has no way to express "write nothing" - the alternatives would either invent a value the original` &&
            ` never shows or add a round-trip the rest of this port does not need. // NOTE: The original's onBeforeRendering sets setVisible( !Device.system.phone ) on the width Slider and on the hint Text alike`.
    text1 = text1 && ` (the view's literal visible="getVisible()" is a sample quirk that resolves to nothing). Both are expressed over the framework's device> model as {= !${device>/system/phone} } - same truth value,` &&
            ` different binding text (app 030/344 precedent). The two long body texts are shortened representative Lorem (not gate-compared, static). // NOTE: The sample's own style.css is archived and injected` &&
            ` since 2026-08-21. The view carries class="sapUiDSCExplored" and the rule behind it (.sapUiDSC.sapUiDSCExplored h1{font-size:2rem}) was missing entirely - a class name with nothing behind it, the same` &&
            ` gap apps 122/124/133 had. style.css now sits in ui5/sap.ui.layout/DynamicSideContent/ (closing the AGENTS section 4 archive gap) and reaches the view through a core:HTML style leaf.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.DynamicSideContent`      name = `DynamicSideContent`                            class = `z2ui5_cl_smpc_app_138` path = `src/01/02/z2ui5_cl_smpc_app_138.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = text1 ) ).

    text1 = `IMPROVISED: The named img> model ({img>/products/pic1} and {img>/products/pic3}, loaded from sap/ui/demo/mock/img.json) is resolved STATICALLY to the two image URLs it points at (HT-7777-large.jpg for` &&
            ` the main content, HT-6100-large.jpg for the side content, on the sdk.openui5.org host per the offline asset rule). abap2UI5 serves one default model and these are pure display assets that never` &&
            ` change, so a lossy-by-definition static fold (app 006 precedent) rather than a NOTE-worthy prefix drop - the live model indirection is gone. // NOTE: The Slider's liveChange is reproduced` &&
            ` roundtrip-free since 2026-08-05: handleSliderChange resizes the DynamicSideContent's container by writing a percentage width onto its DOM node (byId('sideContentContainer').$().width(iValue + '%')),` &&
            ` and the container is a sap.m.Page, which has no width property. The ``css`` control method (pr/control-inline-style) performs that write from the wire - follow_up_action( control_by_id,` &&
            ` sideContentContainer / css / width / ${$parameters>/value} + '%' ) - so the drag has its original effect; apps 213/214 keep the bound-property path where the target HAS a width. // NOTE:`.
    text1 = text1 && ` **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): shrinking the viewport into the S range makes the DynamicSideContent fire breakpointChanged, the` &&
            ` round-trip transports 'S' and the footer Toggle button becomes enabled through the two-way bound flag - the whole _updateToggleButtonState wire. The main/side content render side by side beforehand.` &&
            ` Residual (nothing headless can assert): that toggle() then really swaps main and side content visually, and the Slider's device-model visible flag on a real phone. // NOTE: The sample's own style.css` &&
            ` is injected since 2026-08-23 through an added core:HTML style leaf (no counterpart in the original view, which loads the sheet through its manifest). Both rules of the sheet apply here:` &&
            ` sapUiDSCRightText sits on the side-content Text and sapUiDSCExplored on the DynamicSideContent. The port carried the class names with nothing behind them until then - the gap CAPABILITIES names as` &&
            ` one of the two most common generation mistakes, and the same one the 2026-08-21 sweep closed in apps 122/124/133/138/145. \{ \} escaped: the XMLView parser reads an unescaped brace as a binding. //`.
    text1 = text1 && ` NOTE: Residual, recorded 2026-08-23: the Toggle button's INITIAL enablement is not reproduced. _updateToggleButtonState has two call sites upstream - handleBreakpointChanged and onAfterRendering -` &&
            ` and only the first is wired. DynamicSideContent fires breakpointChanged from _setBreakpointFromWidth only when the new breakpoint differs from the previous one, and _currentBreakpoint is undefined on` &&
            ` the first determination, so the event never fires for the initial layout. An app opened at breakpoint S therefore starts with the Toggle button disabled - the one breakpoint where toggle( ) does` &&
            ` anything - until the container crosses a breakpoint and comes back. There is no declarative substitute: S is the CONTAINER's breakpoint (containerQuery), not a device media range. Same residual as` &&
            ` app 138.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.DynamicSideContent`      name = `DynamicSideContentEqualSplit`                  class = `z2ui5_cl_smpc_app_267` path = `src/01/02/z2ui5_cl_smpc_app_267.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = text1 ) ).

    text1 = `NOTE: All three controller behaviours are reproduced. (a) _updateToggleButtonState has TWO call sites, and both are covered. breakpointChanged carries its own currentBreakpoint event parameter to the` &&
            ` backend (t_arg ${$parameters>/currentBreakpoint}) and on_event enables the Toggle button exactly when it is 'S'. But onAfterRendering calls it a second time, seeding the state from` &&
            ` getCurrentBreakpoint( ) at LOAD, and the round-trip cannot cover that: _setBreakpointFromWidth guards the fire with ``if (sCurrentBreakpoint !== undefined)`` and _currentBreakpoint is undefined until` &&
            ` that very call, so breakpointChanged provably never fires on the first render. Binding TOGGLE_ENABLED alone therefore left the button disabled on load - permanently so on a narrow viewport, which is` &&
            ` breakpoint S, exactly where the toggle is the only way to reach the side content (corrected 2026-08-24). The enabled attribute is now an expression ORing the flag with ${device>/resize/width} <= 720,` &&
            ` the control's own S_M_BREAKPOINT. That is sound in both directions: containerQuery measures the CONTAINER, which can never exceed the window, so window <= 720 implies breakpoint S; the converse case`.
    text1 = text1 && ` - a wide window whose container the Slider shrank below 720 - is what the breakpointChanged round-trip still handles, which is why that wire stays; the port therefore adds an enabled attribute to the` &&
            ` Toggle Button, which the original sets from the controller. (b) handleToggleClick calls DynamicSideContent.toggle( ), which is driven directly through control_by_id ( DynamicSideContent / toggle )` &&
            ` since 2026-08-21. It was a bound showSideContent flipped in on_event until then, on the claim that showSideContent is the property toggle( ) writes - and that is the wrong way round, so the button` &&
            ` changed nothing on screen. sap/ui/layout/DynamicSideContent.js shows toggle( ) leaving showSideContent alone while it is at its true default and swapping the private _MCVisible/_SCVisible pair` &&
            ` instead; setShowSideContent, conversely, re-derives those flags through _setResizeData( S ) and ends with _MCVisible = true, so the flip could never reveal the side content on the one breakpoint` &&
            ` where the button is enabled. An unlisted public method is callable through the wire (the denylist covers teardown, model/binding swaps, event-handler tampering and the render lifecycle), and toggle(`.
    text1 = text1 && ` ) takes no argument, so nothing can be lost in the cast. (c) handleSliderChange resizes the containing Page through jQuery - this.byId('sideContentContainer').$().width(iValue + '%') - and sap.m.Page` &&
            ` has no width property to bind, so the port uses the ``css`` control method roundtrip-free: follow_up_action( control_by_id, sideContentContainer / css / width / ${$parameters>/value} + '%' ). Same` &&
            ` wiring as app 138, which ports the plain DynamicSideContent sample; what this sample adds is sideContentPosition="Begin", kept 1:1. // NOTE: style.css (.sapUiDSC.sapUiDSCExplored h1 { font-size:` &&
            ` 1.5rem }) is injected through a core:HTML <style> leaf, so the port adds one core:HTML control the original view does not have - abap2UI5 ships no separate stylesheet. The CSS braces are escaped \{` &&
            ` \} in a backtick literal so the XMLView parser does not read them as bindings. // NOTE: onBeforeRendering hides the width Slider and the hint Text on a phone (Device.system.phone). Both are bound to` &&
            ` the shared device model instead - visible="{= !${device>/system/phone} }" - which is the live equivalent and needs no round-trip; the original's literal visible="getVisible()" on the hint Text (a`.
    text1 = text1 && ` sample quirk that evaluates to nothing) is replaced by that binding. The two body texts are the sample's full Lorem paragraphs verbatim, with the XML attribute's line breaks normalized to single` &&
            ` spaces as an XML parser would. // NOTE: Unverified in a running system: the breakpointChanged round-trip enabling the Toggle button on S, the Toggle press reaching the control's own toggle( ), and` &&
            ` the Slider's css width write on the container Page. The e2e module drives the first two only - it never touches the Slider, so the css wire stays uncovered by it; the same wire IS live-verified on` &&
            ` app 138 below. The same three wires are live-verified on app 138 (the plain DynamicSideContent sample), which uses them identically. **e2e-verified 2026-08-17** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_344.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.DynamicSideContent`      name = `DynamicSideContentPosition`                    class = `z2ui5_cl_smpc_app_344` path = `src/01/02/z2ui5_cl_smpc_app_344.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = text1 ) ).

    text1 = `IMPROVISED: The named img> model ({img>/products/pic1}, from sap/ui/demo/mock/img.json) is resolved STATICALLY to the URL it points at (HT-7777-large.jpg, on the sdk.openui5.org host per the offline` &&
            ` asset rule) - same fold and same reasoning as app 267: a pure display asset that never changes, but the live model indirection is gone. // NOTE: The Slider's liveChange is reproduced roundtrip-free` &&
            ` since 2026-08-05: handleSliderChange writes a percentage width straight onto the container's DOM node, and that container is a sap.m.Page with no width property. The ``css`` control method` &&
            ` (pr/control-inline-style) does the same write from the wire - follow_up_action( control_by_id, sideContentContainer / css / width / ${$parameters>/value} + '%' ). Apps 213/214 keep the bound-property` &&
            ` path where the target control HAS a width. // NOTE: The controller's media model (new JSONModel(Device.system)) is abap2UI5's shared device> model, so every {media>/phone} binding becomes {=` &&
            ` !${device>/system/phone}} on the same data - the Close button, the Slider and the width hint Text keep their original visibility rules. A device-branch fold onto the framework's own model, no loss.`.
    text1 = text1 && ` // NOTE: handleSideContentHide / handleSideContentShow call oDSC.setShowSideContent(false/true) in the original. DynamicSideContent.showSideContent is a bindable property, so the port binds it` &&
            ` two-way and only flips the flag server-side (added attribute, no structural diff) - the prefer-a-bindable-property rule, now gated by the linter rule settable-property-via-action. The state then` &&
            ` survives a view rebuild, and the control's own toggle( ) - the Toggle button's client-side wire, kept as it is - does NOT write the bound flag back: it swaps the private _MCVisible/_SCVisible pair` &&
            ` and calls setShowSideContent only while showSideContent is already false, immediately setting _SCVisible = false again (DynamicSideContent.js). With the flag at its seeded true the property is never` &&
            ` written, so a client-side toggle leaves the model unchanged and the next redraw restores the pre-toggle state. The bound show_side_content therefore reflects the two backend press handlers and` &&
            ` nothing else - this entry claimed the opposite until 2026-08-21. // IMPROVISED: updateShowSideContentButtonVisibility is reproduced only by its breakpoint half. The original computes !(breakpoint ===`.
    text1 = text1 && ` 'S' || oDSC.isSideContentVisible()), i.e. it also hides the 'Open Side Content' button whenever the side content happens to be visible; isSideContentVisible() is client state the backend cannot read.` &&
            ` The port keeps the button's visibility bound to the breakpoint (visible unless 'S') and flips the same flag in the two press handlers (hide -> button shows, show -> button hides), which now matches` &&
            ` the original in every path the sample offers - but a side-content visibility change caused by anything else (e.g. the Toggle button) does not update it. Its handleSideContentHide half was wrong until` &&
            ` 2026-08-23: the port set the button visible unconditionally, where updateShowSideContentButtonVisibility computes !(breakpoint === 'S' || isSideContentVisible( )) and therefore leaves it HIDDEN at S.` &&
            ` The breakpoint the control last reported is kept in a field now, because the original reads getCurrentBreakpoint( ) at every call site, not only in the event handler. // NOTE: **e2e-verified` &&
            ` 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the feed list renders all four feed.json rows, the side content's Close button hides the side content through the`.
    text1 = text1 && ` SIDE_CONTENT_HIDE round-trip that flips the two-way-bound showSideContent (setShowSideContent false), the Open Side Content button then shows (its visible flag comes from the breakpointChanged` &&
            ` round-trip) and brings it back through SIDE_CONTENT_SHOW. Residual (nothing headless can assert): the FeedListItem layout with its remote author pictures, and toggle() on a real S-breakpoint device.` &&
            ` // NOTE: The sample's own style.css is injected since 2026-08-23 through an added core:HTML style leaf (no counterpart in the original view, which loads the sheet through its manifest). The view` &&
            ` carries sapUiDSCExplored and the rule behind it has to come with it. The port carried the class names with nothing behind them until then - the gap CAPABILITIES names as one of the two most common` &&
            ` generation mistakes, and the same one the 2026-08-21 sweep closed in apps 122/124/133/138/145. \{ \} escaped: the XMLView parser reads an unescaped brace as a binding. // NOTE: Residual, recorded` &&
            ` 2026-08-23: the Toggle button's INITIAL enablement is not reproduced. _updateToggleButtonState has two call sites upstream - handleBreakpointChanged and onAfterRendering - and only the first is`.
    text1 = text1 && ` wired. DynamicSideContent fires breakpointChanged from _setBreakpointFromWidth only when the new breakpoint differs from the previous one, and _currentBreakpoint is undefined on the first` &&
            ` determination, so the event never fires for the initial layout. An app opened at breakpoint S therefore starts with the Toggle button disabled - the one breakpoint where toggle( ) does anything -` &&
            ` until the container crosses a breakpoint and comes back. There is no declarative substitute: S is the CONTAINER's breakpoint (containerQuery), not a device media range. Same residual as app 138. //` &&
            ` NOTE: the five wikimedia asset URLs (the Avatar icon and the authorpicurl column) are served over https here where the original writes http://upload.wikimedia.org: an abap2UI5 app is served over TLS,` &&
            ` and a browser blocks an http image on a TLS page as mixed content, so the http form would render nothing; wikimedia serves the same files on https. The linter reports the http form as` &&
            ` insecure-asset-url from 0.7 on.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.DynamicSideContent`      name = `DynamicSideContentProduct`                     class = `z2ui5_cl_smpc_app_269` path = `src/01/02/z2ui5_cl_smpc_app_269.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.FixFlex`                 name = `FixFlexFixedSize`                              class = `z2ui5_cl_smpc_app_338` path = `src/01/02/z2ui5_cl_smpc_app_338.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.25.0`
        notes = `NOTE: The sample's own css/style.css is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). It was archived but never reached the view, so the fixed` &&
                 ` and flexible halves lost the two blues (#D7E9FF / #A9CFFF) the sample is a picture of - the sibling port 119 already injected the same pair. Found by scripts/probes/orphan-style-class-probe.mjs. The` &&
                 ` bare ``column1`` class stays as it is: the ORIGINAL view carries it with no rule anywhere either, so reproducing it bare is fidelity.` ) ).

    text1 = `NOTE: The original binds the Image src against a separate 'img' JSON model ({img>/products/pic1}) loaded from sap/ui/demo/mock/img.json. abap2UI5 serves one default model, so the picture path is` &&
            ` folded into it and the src binds it directly (client->_bind( pic1 )) - the 'img>' prefix is dropped and the last path segment is identical, which structural-diff matches. The mock's host-relative` &&
            ` path is resolved to the OpenUI5 host (sdk.openui5.org) per the asset-URL rule. // NOTE: The sample's style.css (background colours on the FixFlex fixed/flexible areas, referenced by` &&
            ` class='fixFlexHorizontal') is loaded via the manifest in the original; abap2UI5 ships no separate css file, so it is injected via a core:HTML <style> node with the CSS braces escaped. This adds a` &&
            ` core:HTML control not present in the original view - an extra control IS flagged by structural-diff (control extra), declared here by name.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.FixFlex`                 name = `FixFlexHorizontal`                             class = `z2ui5_cl_smpc_app_219` path = `src/01/02/z2ui5_cl_smpc_app_219.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.25.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.FixFlex`                 name = `FixFlexMinFlexSize`                            class = `z2ui5_cl_smpc_app_215` path = `src/01/02/z2ui5_cl_smpc_app_215.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.25.0` ) ).

    text1 = `NOTE: The named-model image path (img>/products/pic1) is resolved to the static OpenUI5 product image https://sdk.openui5.org/test-resources/sap/ui/documentation/sdk/images/HT-7777-large.jpg (the` &&
            ` mock's actual pic1 value, host-absolutized; wrong-neighbour HT-1000 copy fixed by the 2026-07-24 data audit). Image.src carries the static value where the original binds {img>/products/pic1} - the` &&
            ` app-006 static-fold class. // NOTE: The sample's css/style.css (the .fixFlexVertical > .sapUiFixFlexFixed / .sapUiFixFlexFlexible background colours, loaded via the manifest resources.css) is` &&
            ` INJECTED since 2026-08-05 through a core:HTML content attribute - the documented way to carry a stylesheet into an abap2UI5 view (CAPABILITIES 'Custom CSS', apps 026/028). The class attributes were` &&
            ` already 1:1, so the decorative backgrounds are back; the core:HTML leaf is the one control the original view does not declare. The 2026-07-27 review flagged exactly this as rework.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.FixFlex`                 name = `FixFlexVertical`                               class = `z2ui5_cl_smpc_app_119` path = `src/01/02/z2ui5_cl_smpc_app_119.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.25.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_320.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form_Column_oneGroup`                          class = `z2ui5_cl_smpc_app_320` path = `src/01/02/z2ui5_cl_smpc_app_320.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_321.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form_Column_oneGroup234`                       class = `z2ui5_cl_smpc_app_321` path = `src/01/02/z2ui5_cl_smpc_app_321.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_322.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form_Column_threeGroups234`                    class = `z2ui5_cl_smpc_app_322` path = `src/01/02/z2ui5_cl_smpc_app_322.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_323.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form_Column_threeGroups346`                    class = `z2ui5_cl_smpc_app_323` path = `src/01/02/z2ui5_cl_smpc_app_323.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_324.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form_Column_twoGroups234`                      class = `z2ui5_cl_smpc_app_324` path = `src/01/02/z2ui5_cl_smpc_app_324.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches; the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets edit/save/cancel visible` &&
            ` imperatively; the port binds those three visible attributes to the same edit_mode flag (expression binding for the negated Edit case). handleEditPress clones the record into six server-side backup` &&
            ` fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads sap/ui/demo/mock/supplier.json and does`.
    text1 = text1 && ` bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them ABSOLUTELY (client->_bind( suppliername ) etc.)` &&
            ` rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject returns undefined and the control renders EMPTY` &&
            ` (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for the per-row case where the index arrives from` &&
            ` the event, not for a fixed index. The two composite Texts ({Street} {HouseNumber}, {ZIPCode} {City}) keep their composite form with the absolute paths. // NOTE: the Edit button starts enabled="true"` &&
            ` instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the mock request completes is the`.
    text1 = text1 && ` state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the` &&
            ` POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module` &&
            ` was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_312.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form354`                                       class = `z2ui5_cl_smpc_app_312` path = `src/01/02/z2ui5_cl_smpc_app_312.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches; the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets edit/save/cancel visible` &&
            ` imperatively; the port binds those three visible attributes to the same edit_mode flag (expression binding for the negated Edit case). handleEditPress clones the record into six server-side backup` &&
            ` fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads sap/ui/demo/mock/supplier.json and does`.
    text1 = text1 && ` bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them ABSOLUTELY (client->_bind( suppliername ) etc.)` &&
            ` rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject returns undefined and the control renders EMPTY` &&
            ` (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for the per-row case where the index arrives from` &&
            ` the event, not for a fixed index. The two composite Texts ({Street} {HouseNumber}, {ZIPCode} {City}) keep their composite form with the absolute paths. // NOTE: not yet run in a system: the` &&
            ` Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that` &&
            ` Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each`.
    text1 = text1 && ` phase; that half awaits the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_313.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form354wide`                                   class = `z2ui5_cl_smpc_app_313` path = `src/01/02/z2ui5_cl_smpc_app_313.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches; the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets edit/save/cancel visible` &&
            ` imperatively; the port binds those three visible attributes to the same edit_mode flag (expression binding for the negated Edit case). handleEditPress clones the record into eight server-side backup` &&
            ` fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads sap/ui/demo/mock/supplier.json and does`.
    text1 = text1 && ` bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them ABSOLUTELY (client->_bind( suppliername ) etc.)` &&
            ` rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject returns undefined and the control renders EMPTY` &&
            ` (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for the per-row case where the index arrives from` &&
            ` the event, not for a fixed index. The two composite Texts ({Street} {HouseNumber}, {ZIPCode} {City}) keep their composite form with the absolute paths. // NOTE: not yet run in a system: the` &&
            ` Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that` &&
            ` Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each`.
    text1 = text1 && ` phase; that half awaits the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_314.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form354wideDual`                               class = `z2ui5_cl_smpc_app_314` path = `src/01/02/z2ui5_cl_smpc_app_314.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: not yet run` &&
            ` in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted` &&
            ` merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE` &&
            ` in each phase; that half awaits the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_315.mjs). // NOTE: onInit's oSplitContainer.toDetail( this.createId('page') ) is`.
    text1 = text1 && ` reproduced through a control_by_id follow-up action on the init round-trip. The original comments it as "to navigate to the page on phone and not show the split screen items": initialDetail names the` &&
            ` detail page but does not put a PHONE into detail mode, so without the call a phone opens on the master list (Item 1 / Item 2) where the sample opens on the form. toDetail is a listed control method` &&
            ` taking a controlId, so it travels as-is. It was dropped silently until 2026-08-21, and the family's generator now READS Page.controller.js for it: it used to advertise that it knows this family's` &&
            ` controller while never opening the file, so this call was invisible to the emitter as well as to structural-diff, which compares views only. The two *471 samples are the only members of the 26 whose` &&
            ` controller differs materially from the shared one, and both ports had lost the same behaviour.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form471`                                       class = `z2ui5_cl_smpc_app_315` path = `src/01/02/z2ui5_cl_smpc_app_315.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: not yet run` &&
            ` in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted` &&
            ` merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE` &&
            ` in each phase; that half awaits the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_316.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form480`                                       class = `z2ui5_cl_smpc_app_316` path = `src/01/02/z2ui5_cl_smpc_app_316.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the` &&
            ` fragments bind {EMail} while the mock key is Email, so in the original that field renders empty. The port binds the real Email value (john.smith@sap.com) - structural-diff matches either way (it` &&
            ` compares the last path segment case-insensitively), but the address is actually shown here. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible` &&
            ` flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated`.
    text1 = text1 && ` flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_317.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form480_12120`                                 class = `z2ui5_cl_smpc_app_317` path = `src/01/02/z2ui5_cl_smpc_app_317.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: not yet run` &&
            ` in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted` &&
            ` merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE` &&
            ` in each phase; that half awaits the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_318.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form480_12120Dual`                             class = `z2ui5_cl_smpc_app_318` path = `src/01/02/z2ui5_cl_smpc_app_318.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: Rating` &&
            ` (RatingIndicator.value) and Disposable (ProgressIndicator.percentValue) are numeric UI5 properties, so their ABAP fields are typed i, not string - the mock carries them as JSON numbers (Rating: 4,` &&
            ` Disposable: 30) and i is the faithful representation. NOT because a string would be rejected: these two controls are precisely the ones that opt OUT of the generic numeric check, coercing in their` &&
            ` own setter before validateProperty runs (RatingIndicator.setValue does Number(vValue) 'to support oData v2 Edm.Double', ProgressIndicator.setPercentValue does parseFloat) - a property binding reaches`.
    text1 = text1 && ` the control through that public mutator (ManagedObjectBindingSupport), so a JSON string would work here. The generic rule (DataType isValid: typeof vValue === 'number') is real, but it is what the` &&
            ` plain float/int properties enforce, not these two. i is also safe on the way back: RatingIndicator rounds every user selection to a whole star. // NOTE: not yet run in a system: the Edit/Save/Cancel` &&
            ` round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the` &&
            ` restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits` &&
            ` the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_319.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `Form480_Trial`                                 class = `z2ui5_cl_smpc_app_319` path = `src/01/02/z2ui5_cl_smpc_app_319.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.Form`               name = `FormToolbar`                                   class = `z2ui5_cl_smpc_app_142` path = `src/01/02/z2ui5_cl_smpc_app_142.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = `NOTE: sap.ui.layout.form.Form with two FormContainers, per-container toolbars, ResponsiveGridLayout, FormElements with GridData layoutData and a Select. The original bound an element context` &&
                 ` (/SupplierCollection/0 from the shared demo supplier.json); flattened here to top-level model fields the {…} bindings resolve against. **Corrected 2026-08-01**: those bindings were written RELATIVE` &&
                 ` ({FIELD}) and a relative path on a control with no binding context resolves against nothing — the fields rendered EMPTY in the running app (measured with the e2e harness, the app-207 class). They are` &&
                 ` now bound ABSOLUTELY through client->_bind( field ), which is what a root-seeded record needs.` ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_333.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm_Column_oneGroup`                    class = `z2ui5_cl_smpc_app_333` path = `src/01/02/z2ui5_cl_smpc_app_333.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_334.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm_Column_oneGroup234`                 class = `z2ui5_cl_smpc_app_334` path = `src/01/02/z2ui5_cl_smpc_app_334.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_335.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm_Column_threeGroups234`              class = `z2ui5_cl_smpc_app_335` path = `src/01/02/z2ui5_cl_smpc_app_335.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_336.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm_Column_threeGroups346`              class = `z2ui5_cl_smpc_app_336` path = `src/01/02/z2ui5_cl_smpc_app_336.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_337.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm_Column_twoGroups234`                class = `z2ui5_cl_smpc_app_337` path = `src/01/02/z2ui5_cl_smpc_app_337.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_325.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm354`                                 class = `z2ui5_cl_smpc_app_325` path = `src/01/02/z2ui5_cl_smpc_app_325.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_326.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm354wide`                             class = `z2ui5_cl_smpc_app_326` path = `src/01/02/z2ui5_cl_smpc_app_326.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_327.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm354wideDual`                         class = `z2ui5_cl_smpc_app_327` path = `src/01/02/z2ui5_cl_smpc_app_327.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_328.mjs). // NOTE: onInit's oSplitContainer.toDetail( this.createId('page') ) is reproduced through a control_by_id follow-up action on the init round-trip. The` &&
            ` original comments it as "to navigate to the page on phone and not show the split screen items": initialDetail names the detail page but does not put a PHONE into detail mode, so without the call a` &&
            ` phone opens on the master list (Item 1 / Item 2) where the sample opens on the form. toDetail is a listed control method taking a controlId, so it travels as-is. It was dropped silently until` &&
            ` 2026-08-21, and the family's generator now READS Page.controller.js for it: it used to advertise that it knows this family's controller while never opening the file, so this call was invisible to the` &&
            ` emitter as well as to structural-diff, which compares views only. The two *471 samples are the only members of the 26 whose controller differs materially from the shared one, and both ports had lost` &&
            ` the same behaviour.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm471`                                 class = `z2ui5_cl_smpc_app_328` path = `src/01/02/z2ui5_cl_smpc_app_328.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the Edit` &&
            ` button starts enabled="true" instead of the original's enabled="false" plus attachRequestCompleted: the ABAP model is seeded synchronously in model_init, so the state the original reaches after the` &&
            ` mock request completes is the state the port starts in. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore).` &&
            ` **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered`.
    text1 = text1 && ` at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_329.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm480`                                 class = `z2ui5_cl_smpc_app_329` path = `src/01/02/z2ui5_cl_smpc_app_329.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: the` &&
            ` fragments bind {EMail} while the mock key is Email, so in the original that field renders empty. The port binds the real Email value (john.smith@sap.com) - structural-diff matches either way (it` &&
            ` compares the last path segment case-insensitively), but the address is actually shown here. // NOTE: not yet run in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible` &&
            ` flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the restored Text appear, which a port whose negated`.
    text1 = text1 && ` flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits the next nightly (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_330.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm480_12120`                           class = `z2ui5_cl_smpc_app_330` path = `src/01/02/z2ui5_cl_smpc_app_330.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: not yet run` &&
            ` in a system: the Edit/Save/Cancel round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted` &&
            ` merely that Save/the Inputs/the restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE` &&
            ` in each phase; that half awaits the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_331.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm480_12120Dual`                       class = `z2ui5_cl_smpc_app_331` path = `src/01/02/z2ui5_cl_smpc_app_331.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: _showFormFragment swaps the Page content between Display.fragment.xml and Change.fragment.xml (removeAllContent + insertContent). Both fragments are inlined into the one port view instead and` &&
            ` switched by the two-way bound edit_mode flag on each VBox's visible attribute, so no core:Fragment reference is left and the Page gains a content aggregation the original view.xml does not declare.` &&
            ` structural-diff unions both fragments into the original side, so the control set matches exactly (0 diffs); the added visible attributes carry the switch. // NOTE: _toggleButtonsAndView sets` &&
            ` edit/save/cancel visible imperatively; the port binds those three visible attributes to the same edit_mode flag (an expression binding for the negated Edit case). handleEditPress clones the record` &&
            ` into server-side backup fields so handleCancelPress can restore it exactly like the original's Object.assign clone; handleSavePress just leaves edit mode. // NOTE: the original loads` &&
            ` sap/ui/demo/mock/supplier.json and does bindElement('/SupplierCollection/0'); the record is a FIXED index, so the house idiom seeds those row-0 fields at the default-model root and binds them`.
    text1 = text1 && ` ABSOLUTELY (client->_bind( suppliername ) etc.) rather than keeping the fragments' relative {SupplierName} - with no element binding on the view a relative path has no context, JSONModel._getObject` &&
            ` returns undefined and the control renders EMPTY (linter rule relative-binding-without-context). abap2UI5 does have element binding (client->cs_event-bind_element, live in app 094); it is reserved for` &&
            ` the per-row case where the index arrives from the event, not for a fixed index. Composite texts such as {Street} {HouseNumber} keep their composite form with the absolute paths. // NOTE: Rating` &&
            ` (RatingIndicator.value) and Disposable (ProgressIndicator.percentValue) are numeric UI5 properties, so their ABAP fields are typed i, not string - the mock carries them as JSON numbers (Rating: 4,` &&
            ` Disposable: 30) and i is the faithful representation. NOT because a string would be rejected: these two controls are precisely the ones that opt OUT of the generic numeric check, coercing in their` &&
            ` own setter before validateProperty runs (RatingIndicator.setValue does Number(vValue) 'to support oData v2 Edm.Double', ProgressIndicator.setPercentValue does parseFloat) - a property binding reaches`.
    text1 = text1 && ` the control through that public mutator (ManagedObjectBindingSupport), so a JSON string would work here. The generic rule (DataType isValid: typeof vValue === 'number') is real, but it is what the` &&
            ` plain float/int properties enforce, not these two. i is also safe on the way back: RatingIndicator rounds every user selection to a whole star. // NOTE: not yet run in a system: the Edit/Save/Cancel` &&
            ` round-trips (form swap through the bound visible flags and the Cancel restore). **e2e-verified 2026-08-16** for the POSITIVE half only - that run's module asserted merely that Save/the Inputs/the` &&
            ` restored Text appear, which a port whose negated flag was dead (both forms rendered at once) also passes. The module was widened 2026-08-24 to assert what must be GONE in each phase; that half awaits` &&
            ` the next nightly (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_332.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleForm480_Trial`                           class = `z2ui5_cl_smpc_app_332` path = `src/01/02/z2ui5_cl_smpc_app_332.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.form.SimpleForm`         name = `SimpleFormToolbar`                             class = `z2ui5_cl_smpc_app_175` path = `src/01/02/z2ui5_cl_smpc_app_175.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = `NOTE: The original controller loads the shared demo supplier.json and element-binds a single record (/SupplierCollection/0); flattened here to top-level default-model fields the {…} SimpleForm` &&
                 ` bindings resolve against. Values are supplier.json SupplierCollection row 0 (Red Point Stores, Maintown); unbound columns (Tel, Sms, Rating, …) are dropped. **Corrected 2026-08-01**: those bindings` &&
                 ` were written RELATIVE ({FIELD}) and a relative path on a control with no binding context resolves against nothing — the fields rendered EMPTY in the running app (measured with the e2e harness, the` &&
                 ` app-207 class). They are now bound ABSOLUTELY through client->_bind( field ), which is what a root-seeded record needs.` ) ).

    text1 = `NOTE: The eight Sliders drive their grid wrappers again since 2026-08-05. The original controller resizes the next wrapper by walking the DOM (oSlider.$().parent().next().find('.gridWrapper'), then` &&
            ` Element.closestTo(...).setWidth(value + '%')), but each wrapper is statically known in the view, so the pair is roundtrip-free: every Slider.value is two-way bound and the matching` &&
            ` VerticalLayout.width is an expression binding {= ${/SLIDER_VALUE_n} + '%' } - exactly how app 176 ports the same onSliderMoved idiom. The liveChange attribute stays dropped because the behaviour now` &&
            ` lives in the bindings; the 2026-07-27 review called the earlier 'no bindable equivalent' rationale wrong, and it was. // NOTE: The sample's styles.css (.GridDataSample .exampleDiv /` &&
            ` .propertiesDisplay / p) is injected via a single core:HTML <style> leaf as the first child of the outer VerticalLayout (abap2UI5 ships no separate css file). CSS braces are escaped \{ \} in a` &&
            ` backtick literal so the backslash survives to the serialized attribute, otherwise the XMLView binding parser would read them as bindings and crash view creation. This adds one core:HTML control not`.
    text1 = text1 && ` present in the original view.xml. // NOTE: The core:HTML 'content' divs and the FormattedText 'htmlText' are written as the decoded literal markup (e.g. <div class=exampleDiv></div>); the original` &&
            ` view.xml carries them HTML-entity-encoded (&lt;div ...&gt;) and the builder re-escapes the literal on stringify. Multi-line div contents in the original are written single-line here (whitespace-only` &&
            ` difference, not a binding).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Grid`                    name = `GridData`                                      class = `z2ui5_cl_smpc_app_169` path = `src/01/02/z2ui5_cl_smpc_app_169.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.15.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Grid`                    name = `GridInfo`                                      class = `z2ui5_cl_smpc_app_194` path = `src/01/02/z2ui5_cl_smpc_app_194.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.15.0` ) ).

    text1 = `NOTE: The eleven Sliders lose their Slider.liveChange attribute: the original's onSliderMoved walks the DOM to find the NEXT grid wrapper (oSlider.$().parent().next().find('.gridWrapper')) and calls` &&
            ` setWidth(iValue + '%') on it. The port binds each Slider's value two-way instead and makes the matching wrapper's width an expression binding over it - width="{= ${/SLIDERnn} + '%' }" - so the resize` &&
            ` happens entirely on the client, with no round-trip, no DOM walk and no frontend action. That is the bindable-property route the porting recipe asks for over an imperative setWidth` &&
            ` (sap.ui.layout.VerticalLayout.width is a plain bindable property). // NOTE: resources/styles.css (.GridPropertiesSample .exampleDiv and .contrastColor) is injected through an extra core:HTML <style>` &&
            ` leaf, so the port has 38 core:HTML controls where the original view has 37 - abap2UI5 ships no separate stylesheet. The CSS braces are escaped \{ \} in a backtick literal so the XMLView parser does` &&
            ` not read them as bindings; without the rules behind them the 37 example divs would render as unstyled empty boxes (the app-122/124 lesson). // NOTE: Unverified in a running system: whether the`.
    text1 = text1 && ` expression binding over the Slider value resizes its own wrapper live while dragging (it needs no round-trip, so it should follow the slider continuously, unlike the original's per-event DOM write).` &&
            ` **e2e-verified 2026-08-17** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_345.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Grid`                    name = `GridProperties`                                class = `z2ui5_cl_smpc_app_345` path = `src/01/02/z2ui5_cl_smpc_app_345.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.15.0`
        notes = text1 ) ).

    text1 = `NOTE: The original binds against the default JSONModel (no named model); the InformationCollection array is reproduced 1:1 on the one abap2UI5 default model. The Grid binding={/InformationCollection}` &&
            ` element-binding and the index-relative child bindings ({0/introText1}, {3/ProductPicUrl2}, {1/ProductPicUrl}, {2/ProductPicUrl}, {1/ProductPicUrl2}, {0/Description1}, {0/Description2}) are kept` &&
            ` verbatim with upper-cased field names. // NOTE: Image asset URLs are host-prefixed to https://sdk.openui5.org/ per the project asset-URL rule: the static demoAppsTeaser.png src, and the bound` &&
            ` ProductPicUrl/ProductPicUrl2 values seeded in model_init (originally host-relative test-resources/ paths). // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend` &&
            ` + real browser): the Grid element-binding to the array path resolves against the serialized default model: the {0/INTROTEXT1} index-relative binding renders its real text in the running app.` &&
            ` Residual: the GridData breakpoint behaviour (visibleOnSmall / moveForward), which needs real viewport rendering.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Grid`                    name = `GridXL`                                        class = `z2ui5_cl_smpc_app_226` path = `src/01/02/z2ui5_cl_smpc_app_226.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.15.0`
        notes = text1 ) ).

    text1 = `NOTE: The original uses a separate 'img' JSON model for the image src ({img>/products/pic1}) alongside the default model for the widths. abap2UI5 has one default model, so the picture path is folded` &&
            ` into it and the src binds it directly - the 'img>' prefix is dropped and the path flattened to a single field (pic1); the last path segment is identical, which structural-diff matches. The widths` &&
            ` reproduce the original's Device.system.phone branch since 2026-08-23 (2em/4em/6em against 5em/10em/15em) from the server-side device mirror client->get( )-s_device, the shape the twin sample 173 has` &&
            ` used since it was written. The port carried the desktop leg only until then, justified as 'a client-only Device decision' - which was never true on either side of the wire: the mirror is in` &&
            ` z2ui5_if_client, and the device> model expresses the same branch declaratively (CAPABILITIES, apps 277/279). // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative` &&
            ` (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared`.
    text1 = text1 && ` 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that` &&
            ` day's snapshot - 131 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since that day, so the claim holds again - re-measured 2026-08-23, when the re-count itself` &&
            ` turned out to be 5 low, which is the same failure mode one sentence later. A stale absolute count is what made the original wording wrong, so this one names the date it was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.HorizontalLayout`        name = `HorizontalLayout`                              class = `z2ui5_cl_smpc_app_162` path = `src/01/02/z2ui5_cl_smpc_app_162.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: The original keeps the three pane sizes in a separate 'sizes' JSON model ({sizes>/pane1}, {sizes>/pane2}, {sizes>/pane3}) bound on the three SplitterLayoutData.size properties and echoed in each` &&
            ` pane's Text label. abap2UI5 has one default model, so pane1/pane2/pane3 are folded into it and bound directly - the 'sizes>' prefix is dropped and the last path segment is identical, which` &&
            ` structural-diff matches. Same data, renders identically. // NOTE: The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined into model_init with the three columns the` &&
            ` sample binds - ProductId, Name and Quantity (the List's title/counter and the Select's Item key/text). Quantity is the mock integer; the full row set is kept. // NOTE: The two PaneContainer resize` &&
            ` handlers (resize='.onRootContainerResize' on the root container and resize='.onInnerContainerResize' on the inner one) compose an informational MessageToast listing the oldSizes/newSizes pane-size` &&
            ` arrays. **Reproduced roundtrip-free since 2026-08-05**: measured with scripts/probes/event-arg-expression-probe.mjs, ``.join( ',' )`` over an ARRAY-valued event parameter resolves inside an event`.
    text1 = text1 && ` arg, so both arrays travel into a client-composed toast with the original's message. The earlier rationale - 'an array-valued container-resize event has no bound-model equivalent' - looked for a` &&
            ` MODEL equivalent where the client expression was the answer. Corrected 2026-08-23: the guard is a LINE guard, not a value guard - the original appends the whole "\nOld panes sizes = [...]" line only` &&
            ` when aOldSizes has entries. The port's template carried that line unconditionally and merely emptied the value, so the first resize - which really does arrive with an empty array, since` &&
            ` _sizeArraysDiffer compares [] against the new sizes - printed "Old panes sizes = []" where the original prints nothing. The line is composed inside the argument expression now and the template` &&
            ` carries the placeholder alone. // NOTE: The toast's line break is a REAL newline since 2026-08-21. An ABAP backtick literal has no escapes, so the ``\n`` the template used to carry reached the` &&
            ` visible text as two literal characters - the client's formatTemplate only substitutes {N}. The wire concatenates `` ... `` && |\n| && `` ... `` now, the string-template form where \n IS an escape.`.
    text1 = text1 && ` Same defect and same fix as app 250, where the review sweep found it.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.ResponsiveSplitter`      name = `ResponsiveSplitter`                            class = `z2ui5_cl_smpc_app_186` path = `src/01/02/z2ui5_cl_smpc_app_186.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.38`
        notes = text1 ) ).

    text1 = `NOTE: The Splitter's content areas become a BOUND aggregation over one model table, so the three declared areas and every area the buttons add come from the same place. structural-diff therefore sees` &&
            ` fewer declared controls than the original: Button 7 vs 5 and l:SplitterLayoutData 3 vs 1 - the three content Buttons with their three SplitterLayoutData collapse into one bound template (the four` &&
            ` action Buttons of the HorizontalLayout are unchanged). This is what lets btnAddContentArea / btnRemoveContentArea work without addContentArea/removeContentArea calls: they insert or delete a row. //` &&
            ` NOTE: showLayoutOptions( ) builds the whole options panel in JS (an HBox per content area with a title Text, 'Resizable:' + CheckBox, 'Size (CSS):' + Input, 'Min-Size: (in px)' + Input) and re-builds` &&
            ` it after every change. The port binds the mainOptions VerticalLayout's content to the SAME table with that HBox as its template, so the option row and the layout data are two views of one model row -`.
    text1 = text1 && ` a CheckBox or Size Input writes straight into the SplitterLayoutData with no per-row change closure. structural-diff reports the added controls the original view does not declare: HBox, CheckBox, two` &&
            ` Input and four more Text (five vs one). // NOTE: Min-Size needs one round-trip and gets one: sap.ui.layout.SplitterLayoutData.minSize is an INTEGER while sap.m.Input.value is a string, so the row` &&
            ` carries both (minsize_text bound to the Input, minsize bound to the layout data) and the Input's change event triggers the parse - the ABAP equivalent of the original's` &&
            ` parseInt(oEvent.getParameter('value')). No row index has to travel: the whole table returns two-way, so the handler re-parses every row. Size and Resizable need no event at all - they are plain` &&
            ` two-way bindings shared by the Input/CheckBox and the layout data. // NOTE: btnChangeOrientation flips Splitter.orientation, which IS a bindable property, so the port binds it and flips the model` &&
            ` field instead of calling the setter (the prefer-a-bindable-property rule). btnInvalidateSplitter has no bindable equivalent either - invalidate( ) forces a re-render without changing a property - and`.
    text1 = text1 && ` it needs none: invalidate is denied by the frontend action allowlist (the render lifecycle belongs to the framework, CONTROL_METHOD_DENY_PREFIXES), and the slot is re-displayed instead, which IS a` &&
            ` re-render and restores the sizes off the model. Until 2026-08-24 the branch was EMPTY, on the reasoning that the round-trip the button already makes re-renders the slot by itself - it does not:` &&
            ` main_end sends a model only when a slot displayed XML or when main( ) actually changed the model, and an empty branch changes nothing, so the response carried an empty model and the button was a` &&
            ` strict no-op. So the button keeps its wire and its effect, with no control method called. The Splitter's resize event is wired to the backend, which increments the counter and writes the timestamped` &&
            ` 'Resize # n' text the original's handler writes. // IMPROVISED: createExampleContent randomizes the added area (Math.random for size 'auto' vs 50-350px and for maxSize), which the corpus does not` &&
            ` reproduce - the port alternates deterministically between 'auto' and '150px' so it renders the same every run. Its maxSize is dropped entirely for a different reason: sap.ui.layout.SplitterLayoutData`.
    text1 = text1 && ` has NO maxSize property (the original's object literal sets a key the control ignores), and writing it makes XMLView.create reject the view. Its height="100%" is dropped too - the bound Button` &&
            ` template is shared with the three declared areas, which set only width. // NOTE: css/splitter.css (.options, .options .paddingRight, .optionTitle) is injected through an extra core:HTML <style> leaf,` &&
            ` since abap2UI5 ships no separate stylesheet; the class names are on the controls the original also puts them on. The eventStatus Text keeps its 'Nothing happened so far...' initial label, and the` &&
            ` resize timestamp uses the ABAP system date/time in the user's format where the original uses UI5Date.getInstance().toLocaleString() - a real event timestamp, not an anchored one. // NOTE: Unverified` &&
            ` in a running system: whether inserting/deleting a row re-renders the bound contentAreas aggregation, whether the shared table keeps the option row and the layout data in sync while dragging a` &&
            ` splitter bar, and whether the Invalidate button's plain round-trip re-renders the Splitter the way the original's invalidate( ) does. **e2e-verified 2026-08-21** for the FIRST clause only - the`.
    text1 = text1 && ` module drives Add/Remove and really does watch getContentAreas().length move. It says itself that it cannot drive a splitter-bar drag, and its Invalidate check is only ``count() !== 3``, i.e. "the` &&
            ` areas survived", which the no-op above also passed. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_351.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Splitter`                name = `Splitter`                                      class = `z2ui5_cl_smpc_app_351` path = `src/01/02/z2ui5_cl_smpc_app_351.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Splitter`                name = `Splitter2`                                     class = `z2ui5_cl_smpc_app_125` path = `src/01/02/z2ui5_cl_smpc_app_125.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0` )
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Splitter`                name = `Splitter3`                                     class = `z2ui5_cl_smpc_app_339` path = `src/01/02/z2ui5_cl_smpc_app_339.clas.abap`
        score = 1
        score_tip = `Rating 1 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0` )
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Splitter`                name = `Splitter4`                                     class = `z2ui5_cl_smpc_app_340` path = `src/01/02/z2ui5_cl_smpc_app_340.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = `NOTE: the original view writes minSize="200px" on the third SplitterLayoutData, but sap.ui.layout.SplitterLayoutData.minSize is typed int - UI5 would parse the px suffix fine (int.parseValue is` &&
                 ` parseInt); what rejects it is the corpus linter's invalid-property-value rule for a non-numeric literal on a numeric type. The port writes the numeric value 200 the original means.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.Splitter`                name = `SplitterNested1`                               class = `z2ui5_cl_smpc_app_266` path = `src/01/02/z2ui5_cl_smpc_app_266.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = `NOTE: the original view writes minSize="200px" on the Content 5 SplitterLayoutData, but sap.ui.layout.SplitterLayoutData.minSize is typed int - UI5 would parse the px suffix fine (int.parseValue is` &&
                 ` parseInt, so "200px" yields 200 on both sides); what rejects it is the corpus linter's invalid-property-value rule for a non-numeric literal on a numeric type, and the chain linter rejects the` &&
                 ` literal outright (invalid-property-value, expected int). The port writes the numeric value 200 the original means, which renders identically. Same as app 340, which declared it and this one did not:` &&
                 ` structural-diff never compares a LITERAL attribute value, so the sidecar's empty deviations array was the only thing claiming a clean 1:1 port, and it was wrong. Declared 2026-08-21.` ) ).

    text1 = `NOTE: The original binds the image src against a separate 'img' JSON model ({img>/products/pic1} from sap/ui/demo/mock/img.json) alongside the default model for the widths. abap2UI5 has one default` &&
            ` model, so the picture path is folded into it and the src binds it directly (client->_bind( pic1 )) - the 'img>' prefix is dropped, the last path segment (pic1) is identical and the value is the` &&
            ` mock's own HT-7777-large.jpg absolutized on the sanctioned sdk.openui5.org host, so this is the same-data prefix-drop NOTE case (retyped from IMPROVISED per the settled 2026-07-24 policy). // NOTE:` &&
            ` The original computes widthS/M/L from Device.system.phone in the controller (phone: 2/4/6em, else 5/10/15em). The bindings {/widthS..L} are ported 1:1 on the default model and the seed reproduces the` &&
            ` device branch server-side from client->get( )-s_device-system (app 012 precedent, review fix 2026-07-27); the phone-branch seeding is unverified in a running system. **e2e-verified 2026-08-04**` &&
            ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_173.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.layout`      control = `sap.ui.layout.VerticalLayout`          name = `VerticalLayout`                                class = `z2ui5_cl_smpc_app_173` path = `src/01/02/z2ui5_cl_smpc_app_173.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.16.0`
        notes = text1 ) ).

    text1 = `NOTE: Both filters are applied in ABAP and the Table binds the RESULT, so the controller's Filter objects and its binding.filter( ) calls become one server-side selection - the thin-frontend move. The` &&
            ` SearchField's query filters Name OR Status (the original's _oTxtFilter), the facet selections are ANDed across the two lists and ORed inside each one (its _oFacetFilter), and only a list with a` &&
            ` selection takes part - exactly the two nested Filter groups. handleFacetFilterReset clears every selection and clearAllFilters (the noData Link) clears both filters at once. // NOTE: handleListClose` &&
            ` reads the selected items off the control; the port binds each FacetFilterItem's selected flag two-way instead (an added attribute), so listClose only has to tell the backend that the flags are back -` &&
            ` the app-022 idiom. FacetFilterList.key and FacetFilterItem.key are bound to {TYPE} and {TEXT} rather than the original's {key}: the mock's /ProductCollectionStats/Filters carries no ``key`` field at`.
    text1 = text1 && ` all (the original's binding resolves to nothing), and the list's type / the value's text ARE the identity the filter works on. // NOTE: The named ``ui>`` model (filterValue) is folded onto the one` &&
            ` default model, prefix dropped and leaf name kept. /ProductCollectionStats/Filters is inlined verbatim - both facet lists (Category with 16 values, SupplierName with 12) with their counts. The full` &&
            ` 123-row catalog is returned by a method rather than held in a public attribute: only the filtered rows are bound, so only they belong in the model that travels on every round-trip (the overview-app` &&
            ` lesson in AGENTS section 10). The controller's formatAvailableToObjectState is precomputed into the AVAILABLESTATE column, since business logic belongs in the backend. // IMPROVISED: The footer` &&
            ` OverflowToolbar stays empty: onInit lazily requires sap/ui/table/sample/TableExampleUtils and appends a ToolbarSpacer plus its createInfoButton( ) to it. That helper lives in the demo kit's own` &&
            ` sample folder, not in any UI5 library, and only opens a popover pointing at the sample's source. Every sap.ui.table sample of this batch drops it the same way. // NOTE: Unverified in a running`.
    text1 = text1 && ` system: whether the two-way bound FacetFilterItem selected flags return with the listClose round-trip and produce the expected server-side selection, and whether the noData Link appears when the` &&
            ` filters leave no rows - that second half is NOT covered by the module and cannot be: it treats an empty result as a FAILURE (``the filtered table is empty``), so the noData path can never be reached` &&
            ` there. Closing it would be easy - the Category facet carries 'Graphics Card' and 'Telekommunikation', neither of which matches any product's Category ('Graphic Cards' / 'Telecommunications'), so` &&
            ` either selection yields zero rows. **e2e-verified 2026-08-17** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_352.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `Aggregations`                                  class = `z2ui5_cl_smpc_app_352` path = `src/01/02/z2ui5_cl_smpc_app_352.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: Rebuilt 1:1 from the breadth probe 2026-08-05: all THIRTEEN columns (Text/Input/Label/ObjectStatus/u:Currency/ComboBox/Link/Button/CheckBox/Select/MultiInput/c:Icon/DatePicker templates) over` &&
            ` the full 123-row /ProductCollection of the shared demo mock (sap/ui/demo/mock/products.json), with the Suppliers and Categories arrays the controller derives from it in initSampleDataModel.` &&
            ` ProductPicUrl values are absolutized to the OpenUI5 host (https://sdk.openui5.org/test-resources/...) per the asset-URL rule; the mock carries them host-relative. // NOTE: The controller's two` &&
            ` formatters (Available -> 'Success'/'Error' via ObjectStatus.state, Available -> 'sap-icon://accept'/'sap-icon://decline' via Icon.src) are computed in ABAP into the derived fields AVAILABLESTATE /` &&
            ` AVAILABLEICON and bound plainly (thin-frontend rule). The Available and Heavy fields initSampleDataModel derives (Status === 'Available', WeightMeasure > 1000) are derived in model_init the same way;` &&
            ` Heavy stays a STRING because the CheckBox binding declares type 'sap.ui.model.type.String' in the original. // NOTE: DeliveryDate: the original computes Date.now() - (i % 10 * 4 days), a moving`.
    text1 = text1 && ` value, so it is anchored on a FIXED base date here (the corpus rule for now/random values, apps 164/181/289) and carried as an ISO date STRING rather than a JS epoch number. The DatePicker keeps its` &&
            ` typed binding (sap.ui.model.type.Date) but its formatOptions source pattern becomes 'yyyy-MM-dd' instead of 'timestamp' - the model field type changed, the control binding did not (CAPABILITIES date` &&
            ` row). CORRECTED 2026-08-21: the arithmetic produced a DAY NUMBER, not a date. A date operand inside an expression converts to its internal day count, so CONV string( CONV d( ``20260101`` ) - n )` &&
            ` yielded 739618 and the yyyy-MM-dd offsets cut that into '7.39-61-80' - measured on the running port, every row carried a value the DatePicker's sap.ui.model.type.Date binding cannot parse. Nothing` &&
            ` caught it: the port BOOTS (the generic e2e gate passes), structural_diff compares structure and data_fidelity does not compare a DERIVED field. The arithmetic now lands in a TYPE d field before it is` &&
            ` formatted. // NOTE: The MultiInput's suggestionItems template carries key="{ProductId}" in the original although it is bound over /Categories, whose rows only have a Name - so a token added from a`.
    text1 = text1 && ` suggestion gets an empty key there. The quirk is ported verbatim (key={PRODUCTID}) rather than repaired; the tokenUpdate handler removes the token the event names. Corrected 2026-08-23 on two counts.` &&
            ` The wire THREW on every token ADD: MultiInput fires tokenUpdate with type 'added' and removedTokens = [], and the unguarded ${$parameters>/removedTokens}[0].getKey() raised a TypeError in the` &&
            ` expression parser before any round-trip started, so a suggestion pick produced a console exception instead of the original's model write. It is guarded now. And the sentence used to claim the port` &&
            ` "mirrors the original's filter-by-removed-key, which drops the empty-key tokens together", which it cannot: the original REWRITES the whole list from the post-update aggregation on every firing and` &&
            ` that write is the only thing that ever puts a token into the model, while the port only DELETEs, so the bound table stays empty for all 123 rows and the delete matches nothing, and additionally gates` &&
            ` on the event's update type ('removed') because the original recomputes from the post-update token list instead of reacting to a type. // NOTE: Two of the controller's three handlers are display-only`.
    text1 = text1 && ` and resolve on the client (corrected 2026-08-23 - this said all three, and the next clause of the same sentence already said updateMultipleSelection mutates the model and stays a backend event):` &&
            ` handleDetailsPress toasts the row's ProductId, onPaste toasts the pasted data - both composed through control_global MESSAGE_TOAST with the row/parameter value as an event argument, so neither needs` &&
            ` a round-trip. updateMultipleSelection genuinely mutates the model, so it stays a backend event (TOKEN_UPDATE): the removed token key and the row's binding context path travel and ABAP deletes the` &&
            ` row's token entry. // 1.71: sap.m.plugins.ColumnAIAction (@since 1.136, on the Product Name column, with the controller's onAIActionPress showing an AI-hint MessageToast (corrected 2026-08-23 - it` &&
            ` was described as a dialog; the plugin also sits in the view's dependents rather than a plugin aggregation)) is dropped: sap.m.plugins does not exist in UI5 1.71, which the corpus targets. The column` &&
            ` itself is present, only the plugin aggregation and its press handler are missing. // NOTE: The inline comment above the row type said the token table "grows through the tokenUpdate wire", and it`.
    text1 = text1 && ` cannot: on_event only DELETEs from it. Corrected 2026-08-23 together with the wire guard. Growing it the way the original does would mean rewriting the row's token list from the post-update` &&
            ` aggregation, which needs the whole list transported rather than one removed key.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `Basic`                                         class = `z2ui5_cl_smpc_app_115` path = `src/01/02/z2ui5_cl_smpc_app_115.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: onColumnResize is reproduced 1:1 since 2026-08-05, when the framework gained s_ctrl-prevent_default_expr (pr/conditional-prevent-default, implemented upstream). The original vetoes resizing ONE` &&
            ` column (byId('deliverydate')) and reports every other one; the flag form of the veto is baked per WIRE at render time and would have frozen the whole table, so the port carries the condition as a` &&
            ` client expression instead: ``${$parameters>/column}.getId().indexOf('deliverydate') >= 0``. The event is sent either way (that is eBP's contract), so the if/else of the original's handler lives in` &&
            ` on_event: the vetoed column reports nothing, every other one gets the sample's own text with its LABEL and the new width, both transported off the event parameter. Two things the earlier reduction` &&
            ` dropped are therefore back - the per-column veto and the column label. What stays dropped is the 50ms BUFFER: the original collects the messages of a burst and toasts them joined, a debounce with no` &&
            ` abap2UI5 equivalent, so each resize toasts on its own. The toast also moved from a client MESSAGE_TOAST to the backend, because the decision whether to toast at all is the same if/else.`.
    text1 = text1 && ` **e2e-verified 2026-08-06** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): one wire, two columns, opposite outcomes - firing columnResize on the delivery-date column returns` &&
            ` false (vetoed) and on any other column returns true, and the non-vetoed one toasts its label and width. (Corrected 2026-08-23: the module asserts 'was resized to' only - neither the label nor the` &&
            ` width.) // NOTE: Named-model fold with a nested config object: the original binds each Column width to the ui>/widths/{name,category,image,quantity,date} single object on a separate JSONModel.` &&
            ` abap2UI5 has one default model; a nested single-object bind is proven only for row-relative sub-paths (CAPABILITIES 'Nested single (non-array) structure', app 171) - the fold is a convenience, not a` &&
            ` necessity, and writing '/WIDTHS/NAME' as a literal path is disallowed. So the widths object is folded to five top-level string fields named exactly name/category/image/quantity/date (the sanctioned` &&
            ` named-model prefix-drop idiom), each bound via client->_bind so the last path segment still matches the original (structural-diff normalizes on the last segment). onColumnWidthsChange`.
    text1 = text1 && ` (SegmentedButton Static/Flexible/Mixed) is reproduced faithfully: the selected key is transported via ${$parameters>/item}.getKey() and the width set is recomputed in ABAP (thin-frontend) and pushed` &&
            ` back on the round-trip. (Corrected 2026-08-23: the reason recorded here - that a top-level struct-component _bind is unproven and that writing '/WIDTHS/NAME' is disallowed - was false on both halves.` &&
            ` get_client_name turns an attribute's - into /, so client->_bind( widths-name ) yields {/WIDTHS/NAME} through the sanctioned API, and app 248 in this same batch binds catalog-clothing exactly that` &&
            ` way.) // NOTE: DeliveryDate is seeded deterministically. The original computes DeliveryDate = Date.now() - (i % 10 * 4 days) per load (non-deterministic timestamps); the port uses a fixed base` &&
            ` (2026-07-25 in epoch ms) minus the same (i % 10 * 4 days) offset, kept as an epoch-ms value bound through the original {path:'DeliveryDate', type:'sap.ui.model.type.Date',` &&
            ` formatOptions:{source:{pattern:'timestamp'}}} typed binding. // NOTE: ProductPicUrl is resolved to the OpenUI5 host: the mock stores the host-relative`.
    text1 = text1 && ` 'test-resources/sap/ui/documentation/sdk/images/HT-xxxx.jpg'; the port stores the absolute 'https://sdk.openui5.org/test-resources/...' per the asset-URL rule, keeping the original {ProductPicUrl}` &&
            ` binding. Full 123-row ProductCollection inlined (Name/Category/ProductPicUrl/Quantity/DeliveryDate). // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real` &&
            ` browser): the SegmentedButton width switch really round-trips: picking 'Flexible' sends WIDTHS_CHANGE with ${$parameters>/item}.getKey(), the backend recomputes the widths and the Table's columns` &&
            ` come back at 25%. The button sits in an OverflowToolbar, where it renders as a Select - drivable once the 'Additional Options' popover is open (AGENTS 10). Residual: the columnResize client toast (a` &&
            ` real column drag) and the timestamp-typed DeliveryDate formatting. // NOTE: filterProperty carry the UPPER-CASED ABAP field names, not the original's mixed-case ones: abap2UI5 derives the model paths`.
    text1 = text1 && ` from the ABAP component names, which is why the cells on the same columns bind {NAME}/{CATEGORY} and friends. Copied verbatim from the original until 2026-08-23, and UI5 generates the column menu's` &&
            ` filter entries off the property merely being SET - so the entries were there and resolved to undefined on every row: sorting a no-op, filtering empty. Corpus convention, same sweep that fixed apps` &&
            ` 137/164/174/247/353. (This sample sets no sortProperty at all - only filterProperty, on two columns.) // NOTE: onInit's TableExampleUtils info button is dropped, as in every other sap.ui.table port` &&
            ` (164/352/353/354/355/357/362 all declare it). The original appends a ToolbarSpacer and a createInfoButton( ) into the footer toolbar the view gives the id 'infobar'; the port keeps that id - the only` &&
            ` thing the original needs it for - and adds neither. Declared 2026-08-23; it was the one port of the family that dropped it silently.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `ColumnResizing`                                class = `z2ui5_cl_smpc_app_247` path = `src/01/02/z2ui5_cl_smpc_app_247.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.m.plugins.ContextMenuSetting (@since 1.121, scope="Selection") is kept 1:1 in both tables' dependents - it is what makes the table's context menu act on the selected row, which the` &&
            ` sample's two context menus rely on. Newer than UI5 1.71; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.121. // NOTE: The original keeps ONE collection and` &&
            ` splits the two tables by a Rank filter (Rank = 0 available, Rank > 0 selected, sorted by Rank descending), moving a row by rewriting its Rank; the rank arithmetic (initialRank / defaultRank / the` &&
            ` Before-Between-After algorithm) exists only to encode an ORDER inside one array. With abap2UI5's one default model the port keeps two model tables instead, so a move is an insert plus a delete and` &&
            ` the row order IS the rank - same rendered result, no rank column. Getting that equivalence right needs the insert POSITION to match, and until 2026-08-24 it did not: all three of the move-back call` &&
            ` sites used ``INSERT ... INTO TABLE``, which on a STANDARD TABLE WITH EMPTY KEY appends. The original does the opposite at both ends. Into table 2 it inserts as the FIRST row -`.
    text1 = text1 && ` ``Before(firstRowRank)`` with the table sorted descending by Rank, and the source even comments "insert always as a first row" - so the button now inserts at index 1. Back into table 1 the row must` &&
            ` return to its ORIGINAL place, because that table binds ``filters: Rank EQ 0`` with NO sorter, so a row whose Rank goes back to 0 reappears where it sits in ProductCollection. Two tables throw that` &&
            ` order away, so ty_s_product carries an ordinal (stamped after the seeded literals, which stay exactly the mock's) and available_restore( ) re-inserts by it. Consequently each Table's rows binding is` &&
            ` a plain aggregation binding rather than the original's filters/sorter binding-info. // NOTE: Both DragInfo/DragDropInfo lose their dragStart attribute: onDragStart only stashes the dragged row's` &&
            ` binding context in the drag session so the drop handler can find it again, which is a client-side workaround for something the drop event already carries. The port ships it on the drop instead -` &&
            ` ${$parameters>/draggedControl}.getIndex(), plus ${$parameters>/droppedControl}.getIndex() and ${$parameters>/dropPosition} for table 2 - and does the move/reorder arithmetic in ABAP (the app-148`.
    text1 = text1 && ` idiom). A fourth argument tells the two cases apart, because table 2 receives both an incoming move and an internal reorder through the same handler:` &&
            ` ${$parameters>/draggedControl}.getParent().getId().indexOf('table2') >= 0, an event arg being a full UI5 expression. Client indices are 0-based and ABAP rows 1-based, and every index is range-checked` &&
            ` before it is used as a table index - a nonsense index splices harmlessly in JS but dumps in ABAP (the app-148 lesson). // NOTE: getSelectedRowContext reads the selected row off the control; the port` &&
            ` mirrors it into the backend instead - each table's rowSelectionChange carries ${$parameters>/rowIndex}, and onBeforeOpenContextMenu (which the original uses to select the row under the cursor before` &&
            ` opening the menu) carries the same parameter. The two arrow buttons and the context-menu items then work off that, with the original's 'Please select a row!' toast when nothing is selected.` &&
            ` moveUp/moveDown become an index swap in the selected table, which is what the original's Between-rank arithmetic achieves. What the mirror does NOT reproduce is the original's four setSelectedIndex(`.
    text1 = text1 && ` ) calls - moveToTable2 selects the inserted row, moveSelectedRow selects the sibling it swapped with, moveToTable1 steps the selection back when it removed the last row. The port updates only its own` &&
            ` selected_1/selected_2, never the control, so after a Move up the highlight stays on the old index while the backend believes the moved row is selected, and a second Move up acts on a different row` &&
            ` than the highlighted one. Binding it is not the fix either: sap.ui.table.Table.selectedIndex is @deprecated as of 1.69. Declared rather than improvised. // IMPROVISED: The footer info button is` &&
            ` dropped: onInit lazily requires sap/ui/table/sample/TableExampleUtils and appends a ToolbarSpacer plus its createInfoButton( ) to the toolbar. That helper lives in the demo kit's own sample folder,` &&
            ` not in any UI5 library, and only opens a popover pointing at the sample's source. Every sap.ui.table sample of this batch drops it the same way. // NOTE: The shared 123-row demo ProductCollection` &&
            ` (sap/ui/demo/mock/products.json) is inlined with the three columns both tables bind; every product starts in the available table, which is exactly what the original's initialRank = 0 means. The`.
    text1 = text1 && ` Quantity columns keep the original's typed complex binding with the path switched to the ABAP field. // NOTE: Unverified in a running system: whether the drop wires deliver the row indices and drop` &&
            ` position as expected, whether the internal-vs-external drag expression resolves, and whether the context menu's beforeOpenContextMenu round-trip arrives before the menu item press. **e2e-verified` &&
            ` 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_353.mjs). // NOTE: sortProperty/filterProperty carry the UPPER-CASED ABAP field names, not the original's mixed-case ones:` &&
            ` abap2UI5 derives the model paths from the ABAP component names, which is why the cells on the same columns bind {NAME}/{CATEGORY} and friends. Copied verbatim from the original until 2026-08-23, and` &&
            ` UI5 generates the column menu's sort and filter entries off the property merely being SET - so the entries were there and resolved to undefined on every row: sorting a no-op, filtering empty. Corpus` &&
            ` convention, same sweep that fixed apps 137/164/174/247/353. // NOTE: Every wire in this port transports a POSITIONAL index - ${$parameters>/rowIndex} and ${$parameters>/draggedControl}.getIndex() -`.
    text1 = text1 && ` which the backend maps straight onto an ABAP table index. rowIndex is oRow.getIndex(), the index in the BINDING, so it follows any sorter or filter the user applies. The port keeps` &&
            ` sortProperty/filterProperty on table 1's columns (see the deviation below), which is exactly what makes UI5 offer the column-menu Sort and Filter entries - so once the user sorts or filters table 1,` &&
            ` the index no longer addresses the same row in t_available and a move acts on the wrong product. The original is immune because it resolves the index through getContextByIndex( ) and then works on the` &&
            ` CONTEXT, which no reordering invalidates. Carrying a context is not something the wire can do, so this is a boundary of the index-mirroring idiom rather than a fixable slip - recorded here because` &&
            ` nothing else in the sidecar names it.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `DnD`                                           class = `z2ui5_cl_smpc_app_353` path = `src/02/02/z2ui5_cl_smpc_app_353.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.plugins.ContextMenuSetting (@since 1.121, scope="Selection") is kept 1:1 in both tables' dependents - it is what makes the table's context menu act on the selected row, which the sample's two` &&
                 ` context menus rely on. Newer than UI5 1.71; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.121.` ) ).

    text1 = `NOTE: Every filter of this sample is applied in ABAP and the Table binds the RESULT, so the controller's Filter objects and its binding.filter( ) calls become one server-side selection - the` &&
            ` thin-frontend move. The global SearchField filter (Name OR Category contains the query), the availability toggle and the price band are ANDed with them. (_filter( ) itself combines only the global` &&
            ` and the price filter, both Application-type; the availability toggle goes through the COLUMN's filter( 'X' ) and the binding ANDs it as a Control filter. Same net result, different mechanism), and` &&
            ` clearAllFilters resets all three. The Table's filter event carries the pressed column's filterProperty and the entered value (${$parameters>/column}.getFilterProperty() and ${$parameters>/value}) and` &&
            ` vetoes the built-in filter for the PRICE COLUMN ONLY, through s_ctrl-prevent_default_expr - which is what the original does by returning from filterPrice before oEvent.preventDefault( ) for every` &&
            ` other column. Until 2026-08-21 this was the boolean check_prevent_default, and that is baked per WIRE rather than per firing: it vetoed all five columns while the backend implemented only the price`.
    text1 = text1 && ` rule, so Name, Category, Available and Quantity filtered nothing at all and the enableCellFilter feature was inert everywhere but on Price. The four now keep the table's own client-side filtering,` &&
            ` including Category's defaultFilterOperator StartsWith and Available's EQ, which is the behaviour the original has and no gate can see. Worked precedent for the conditional form: app 247's` &&
            ` columnResize. The price column keeps its special rule - a +/- 20 BAND around the entered value rather than an exact match, which is the point of the original's filterPrice. // NOTE: The named ``ui>``` &&
            ` model (globalFilter, availabilityFilterOn, cellFilterOn) is folded onto the one default model, prefix dropped and leaf names kept. cellFilterOn needs no handler at all - the ToggleButton's pressed` &&
            ` and the Table's enableCellFilter bind the same field, which is what the original's two-way {ui>/cellFilterOn} binding already does. The availability ToggleButton keeps its pressed binding AND its` &&
            ` press wire, because pressing it has to re-run the server-side selection. The filterProperty values are the ABAP (upper-cased) field names, since that is what the backend switches on. // NOTE: The`.
    text1 = text1 && ` full 123-row catalog is returned by a method rather than held in a public attribute: only the filtered rows are bound, so only they belong in the model that travels on every round-trip (the` &&
            ` overview-app lesson in AGENTS section 10). The controller's formatAvailableToObjectState is precomputed into the AVAILABLESTATE column, since business logic belongs in the backend, and the boolean` &&
            ` Available is kept as its own column because the availability filter needs it. ProductPicUrl values point at the OpenUI5 host per the asset-URL rule; the mock carries them host-relative. //` &&
            ` IMPROVISED: The footer OverflowToolbar stays empty: onInit lazily requires sap/ui/table/sample/TableExampleUtils and appends a ToolbarSpacer plus its createInfoButton( ) to it. That helper lives in` &&
            ` the demo kit's own sample folder, not in any UI5 library, and only opens a popover pointing at the sample's source. Every sap.ui.table sample of this batch drops it the same way. // NOTE: The price` &&
            ` band's guard tests the SHAPE of the entered text, not only which characters it contains (2026-08-26). ``price_filter CO `` 0123456789.```` admitted ``.``, ``12.``, ``1.2.3`` and ``1 2``, none of`.
    text1 = text1 && ` which is valid ABAP numeric text, so CONV decfloat34 raised CX_SY_CONVERSION_NO_NUMBER and the round-trip died with no way back but a reload. They are one Enter away: sap.ui.table.Column.filter( )` &&
            ` fires the table's filter event with the RAW value BEFORE it parses anything (Column.js:914), and the price column declares no filterType, so the column menu's text field submits whatever was typed` &&
            ` and nothing on the client validates it. The guard now SPLITs the condensed text at the point and requires digits on each side with at least one digit present, which decides the shape completely. For` &&
            ` ``.``, ``..``, ``1 2`` and an empty entry that is the original's own NaN branch - clear( ), i.e. no price filter. It DEVIATES from the original for mid-string garbage: JS parseFloat('1.2.3') is 1.2` &&
            ` and parseFloat('12abc') is 12, so upstream bands around the prefix where the port now leaves the rows unfiltered. Rebuilding parseFloat's prefix parse in ABAP is not worth it for an entry the sample` &&
            ` has no meaning for, and the CO guard already carried that same deviation for '12abc'. // NOTE: Unverified in a running system: whether the price column's prevented default plus the server-side band`.
    text1 = text1 && ` show the expected rows, whether the other four columns still filter client-side through the conditional veto (the leg the 2026-08-21 review found broken), and whether the enableCellFilter binding` &&
            ` lets a cell filter fire that same event. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_354.mjs). // NOTE: The sample's asset paths are host-absolutized.` &&
            ` The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are` &&
            ` otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being` &&
            ` true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute` &&
            ` count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `Filtering`                                     class = `z2ui5_cl_smpc_app_354` path = `src/01/02/z2ui5_cl_smpc_app_354.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The two menu-entry toggles need no handler at all: each ToggleButton's pressed and the Table property it drives (enableColumnFreeze / enableCellFilter) bind the SAME field on the one default` &&
            ` model, which is exactly what the original's two-way {ui>/showFreezeMenuEntry} and {ui>/enableCellFilter} bindings do - only the ``ui>`` prefix is dropped. The THIRD toggle (onToggleContextMenu) does` &&
            ` round-trip, because what it switches is a SUBTREE of the view and not a property; see the context-menu deviation. // IMPROVISED: Two of the three CONTROLLER-BUILT menus are still dropped; the third` &&
            ` is ported since 2026-08-26. (a) associateHeaderMenus creates a sap.m.table.columnmenu.Menu and associates it with the name/productId/quantity columns via setHeaderMenu. This used to be called "a` &&
            ` control-returning factory, the capability boundary CAPABILITIES marks as not expressible", and that was wrong twice: there is no factory - onInit builds TWO menus ONCE and statically (an EMPTY new` &&
            ` ColumnMenu() for name and productId, and one new ColumnMenu({items: [ActionItem, ActionItem]}) with two fixed entries for quantity) - and Column headerMenu is a plain ASSOCIATION taking a control id,`.
    text1 = text1 && ` not an aggregation of built controls. The real obstacle is VERSION: sap.ui.table.Column headerMenu is @since 1.110 and sap.m.table.columnmenu.Menu is @since 1.110 too, so keeping them is a POST_171` &&
            ` decision that would move this class out of src/01, and onQuantitySort's this.byId('table').sort( oColumn, sOrder, bAdd ) additionally wants a COLUMN INSTANCE as its first argument, which the` &&
            ` control-call wire casts to a string. It is a decision left open, not an impossibility. (b) onToggleContextMenu builds a sap.m.Menu with two bound MenuItems and hands it to setContextMenu /` &&
            ` destroyContextMenu. This used to say "an aggregation of controls built at runtime has no bindable equivalent either", which was false: the sap.ui.table.Table contextMenu aggregation is @since 1.54 -` &&
            ` BELOW the 1.71 floor - and app 436 has been porting exactly this shape on sap.m.Tree. The port now does the same: the toggle's pressed state travels on a round trip and the backend emits the` &&
            ` contextMenu > m:Menu > two m:MenuItem subtree ({NAME} and {PRODUCTID}, the original's {Name}/{ProductId}) or leaves it out, which is setContextMenu / destroyContextMenu. (c)`.
    text1 = text1 && ` onProductIdCellContextMenu is still dropped, and with it the Table's beforeOpenContextMenu attribute: it calls oEvent.preventDefault( ), builds a sap.ui.unified.Menu per cell and opens it docked to` &&
            ` the clicked cell's DOM ref with Popup.Dock - a DOM anchor and a per-cell veto the wire has no counterpart for. Its handler goes with it. The columns, their sortProperty/filterProperty and the table's` &&
            ` own header menus are unaffected. One waiver rides with (b): the two MenuItem texts are RELATIVE bindings on a control the linter sees no binding context for, so both lines carry an` &&
            ` abap2ui5lint-disable-next-line for relative-binding-without-context. It is a static blind spot, not a broken binding - sap.ui.table's _MenuUtils._openCustomContentCellContextMenu does` &&
            ` oContextMenu.setBindingContext( TableUtils.getBindingContextOfRow( oRow ), ... ) before it opens the menu, which is why the original binds {Name} and {ProductId} there in the first place. // NOTE:` &&
            ` The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined with the five columns the sample binds. The original computes DeliveryDate from Date.now() with an i-mod-10`.
    text1 = text1 && ` offset in 4-day steps; a fixed base date (2026-07-23) is used here so the port is deterministic - the corpus convention of app 164. The Quantity and Delivery Date columns keep the original's typed` &&
            ` complex bindings with the path switched to the ABAP field, and sortProperty/filterProperty carry the ABAP (upper-cased) field names. ProductPicUrl values point at the OpenUI5 host per the asset-URL` &&
            ` rule. // IMPROVISED: The footer OverflowToolbar stays empty: onInit lazily requires sap/ui/table/sample/TableExampleUtils and appends a ToolbarSpacer plus its createInfoButton( ) to it. That helper` &&
            ` lives in the demo kit's own sample folder, not in any UI5 library, and only opens a popover pointing at the sample's source. Every sap.ui.table sample of this batch drops it the same way. // NOTE:` &&
            ` The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at` &&
            ` https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by`.
    text1 = text1 && ` all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since` &&
            ` today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `Menus`                                         class = `z2ui5_cl_smpc_app_355` path = `src/01/02/z2ui5_cl_smpc_app_355.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: sap.ui.table grid Table with multi-level column headers (multiLabels + headerSpan '3,2'/'2') and an extension OverflowToolbar. The 5 contact rows are inlined from the controller's JSON model;` &&
            ` column templates bind {SUPPLIER}/{STREET}/{CITY}/{PHONE}/{OPENORDERS} 1:1. sortProperty/filterProperty carry the UPPER-CASED ABAP field names (SUPPLIER/STREET/CITY/PHONE), not the original's` &&
            ` lowercase ones: abap2UI5 derives the model paths from the ABAP component names, which is why the cell bindings on the same columns read {SUPPLIER} etc. The port copied the original's lowercase paths` &&
            ` until 2026-08-23, and Column.isSortableByMenu/isFilterableByMenu only need the property to be SET - so the menu entries were generated and then sorted and filtered on a path that resolves to` &&
            ` undefined on every row: sorting a no-op, filtering empty. Same convention as app 357.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `MultiHeader`                                   class = `z2ui5_cl_smpc_app_137` path = `src/01/02/z2ui5_cl_smpc_app_137.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The sample serves its rows from an in-page OData MockServer (sap/ui/core/util/MockServer over metadata.xml, autoRespondAfter 2000, operationMode Server, threshold 15). An abap2UI5 app has a real` &&
            ` ABAP backend, so the mock service is replaced by the model itself: all 115 rows of the sample's own ProductSet.json are inlined and the Table binds them directly. What that costs is the sample's` &&
            ` server-side paging illusion - the rows arrive in one model instead of in threshold-sized batches - and with it the enableBusyIndicator's visible effect; both attributes are kept 1:1 on the control.` &&
            ` ProductSet.json lives in the sibling OData sample folder upstream (the MockServer's sMockdataBaseUrl points there); it is archived into this sample's folder too so the port is verifiable offline. //` &&
            ` NOTE: The named ``config>`` model (limit, showHeaderSelector, selectionModes, selectionMode) is folded onto the one default model, prefix dropped and leaf names kept. Two of the three config controls`.
    text1 = text1 && ` then need no handler at all: the selection-mode Select's selectedKey and the plugin's selectionMode share one field, and the ToggleButton's pressed and the plugin's showHeaderSelector share another -` &&
            ` which is what the original's two-way {config>/...} bindings already do. The limit keeps its change wire because onLimitChange is not a plain setter: it validates the entry, and the port carries the` &&
            ` limit twice (an integer for the plugin, the Input's string next to it) because sap.m.Input.value is a string property while the plugin's limit is an integer - the original bridges that with a typed` &&
            ` binding, the port parses it in ABAP and snaps the Input back with the same message on a bad entry. // NOTE: onSelectionChange reads state that lives in the plugin, so the wire carries it:` &&
            ` ${$parameters>/limitReached} and ${$source>}.getSelectedIndices().length (an event arg is a full UI5 expression), and the backend composes the same three messages - 'Selection cleared.', 'n row(s)` &&
            ` selected.' and the limit-reached variant naming the current limit. // NOTE: The six column labels are metadata bindings in the original ({/#Product/Name/@sap:label} and friends), which only an OData`.
    text1 = text1 && ` model can resolve. They are replaced by the literal sap:label texts from the sample's own metadata.xml (Product Name, Product ID, Prod. Cat., Company Name, Unit Price, Dimensions) - the same strings` &&
            ` the OData model would have produced. sortProperty / filterProperty carry the ABAP (upper-cased) field names. The Price cell keeps the original's typed String binding and the dimensions cell its` &&
            ` {WIDTH}x{HEIGHT}x{DEPTH} {DIMUNIT} template; the numeric columns stay TYPE string so the mock's exact decimals survive (the display-only rule of the porting recipe). // NOTE: This port carried a` &&
            ` POST_171 deviation until 2026-08-21 claiming MultiSelectionPlugin.enableNotification @since 1.71 and selectionMode @since 1.100, and it was the only thing holding the class in src/02. Neither half` &&
            ` stands. enableNotification IS @1.71, which is the floor itself, not above it - the linter's withinFloor accepts v <= floor. And selectionMode carries no @since at all (MultiSelectionPlugin.js, and` &&
            ` ui5/properties.json lists a since only for enableNotification), so the 'needs UI5 >= 1.100' conclusion had nothing behind it. The plugin itself is @1.64. Every other member the view uses resolves at`.
    text1 = text1 && ` or below 1.71, so the class now sits in src/01/02 where the scope rule puts it. // NOTE: Unverified in a running system: whether the plugin honours the bound limit / selectionMode /` &&
            ` showHeaderSelector without a round-trip. Two of those three are genuinely covered - limit (seeded 20 against the plugin's own default of 200) and selectionMode (driven to 'Single' through the` &&
            ` Select). showHeaderSelector is NOT: it is asserted only at seed time, and true is the plugin's own default, so a port that bound the ToggleButton and the plugin to different fields - or bound nothing` &&
            ` - passes it. The ToggleButton is never pressed, so the no-round-trip leg is untested for that field. The binding itself is correct; the gap is in the coverage claim, and whether the selectionChange` &&
            ` expression argument reports the selected count. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_356.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `MultiSelectionPlugin`                          class = `z2ui5_cl_smpc_app_356` path = `src/01/02/z2ui5_cl_smpc_app_356.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.ui.table.Table.scrollThreshold (@since 1.128) is kept 1:1 from the sample's view - it is one of the two paging knobs (with threshold) this sample is about. Newer than UI5 1.71; declared` &&
            ` per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.128. // NOTE: The sample serves its rows from an in-page OData MockServer (sap/ui/core/util/MockServer over` &&
            ` metadata.xml, autoRespondAfter 2000, operationMode Server, threshold 15, scrollThreshold 50). An abap2UI5 app has a real ABAP backend, so the mock service is replaced by the model itself: all 115` &&
            ` rows of the sample's own ProductSet.json are inlined and the Table binds them directly. What that costs is the server-side paging illusion - the rows arrive in one model instead of in threshold-sized` &&
            ` batches - and with it the dataRequested/dataReceived handlers that swap the noData BusyIndicator in and out; the noData BusyIndicator, threshold, scrollThreshold and enableBusyIndicator are all kept` &&
            ` 1:1 on the control. // IMPROVISED: onOperationModeChange re-binds the rows with the picked sap.ui.model.odata.OperationMode (Server / Client / Auto) - a binding parameter of an OData model, which has`.
    text1 = text1 && ` no counterpart when the rows come from an ABAP model. The SegmentedButton is kept 1:1 with its FOUR items - Default, Server, Client, Auto, the order the controller's for..in walks the enum. The` &&
            ` Default entry was missing until 2026-08-21, while this very text called the three-item set 1:1. The selectedKey stays two-way bound, and picking a mode still re-reads the rows (the other half of what` &&
            ` the handler does, via onModelRefresh), but the mode itself no longer changes how the data is fetched. One deliberate render divergence: the port preselects Server, the original renders Default. The` &&
            ` original's ui model does hold selectedOperationMode: OperationMode.Server, but its view binds selectedKey="{ui>selectedOperationMode}" RELATIVELY - no leading slash, unlike` &&
            ` items="{ui>/operationModes}" on the next line - so with no binding context on that model the path resolves to nothing, selectedKey falls back to "" and _selectDefaultButton picks the first item. The` &&
            ` port follows the model's stated intent rather than that sample quirk, the same way the DynamicSideContentPosition port replaces the literal visible="getVisible()" on its hint Text. No gate can see`.
    text1 = text1 && ` this: structural-diff skips binding-value comparison when the port side is an ABAP expression. onModelRefresh's binding.refresh( true ) is the plain re-read. // NOTE: The named ``ui>`` model` &&
            ` (operationModes, selectedOperationMode) is folded onto the one default model and the prefix dropped. The leaf names are NOT all kept: selectedOperationMode becomes operation_mode, i.e.` &&
            ` {/OPERATION_MODE}. (operationModes -> t_operationmodes is only the house t_ table prefix.) Nothing checks this - structural-diff compares binding values only for SIMPLE_BIND attributes, and both of` &&
            ` these sit in complex binding-infos or ABAP expressions. The six column labels are metadata bindings in the original ({/#Product/Name/@sap:label} and friends), which only an OData model can resolve;` &&
            ` they are replaced by the literal sap:label texts from the sample's own metadata.xml (Product Name, Product ID, Prod. Cat., Company Name, Unit Price, Dimensions). sortProperty / filterProperty carry` &&
            ` the ABAP (upper-cased) field names. The Price cell keeps the original's typed String binding and the dimensions cell its {WIDTH}x{HEIGHT}x{DEPTH} {DIMUNIT} template; the numeric columns stay TYPE`.
    text1 = text1 && ` string so the mock's exact decimals survive. Two of the six header texts were the OData2 sample's labels, not this one's, until 2026-08-21: Category where this metadata.xml says "Prod. Cat.", and` &&
            ` "Supplier Company Name" where it says "Company Name" - while this very deviation asserted the texts came from the sample's own metadata. Corrected against the archived file. // IMPROVISED: The` &&
            ` footer's info button is dropped: onInit lazily requires sap/ui/table/sample/TableExampleUtils and appends a ToolbarSpacer plus its createInfoButton( ) to the toolbar that also carries the` &&
            ` operation-mode SegmentedButton. That helper lives in the demo kit's own sample folder, not in any UI5 library, and only opens a popover pointing at the sample's source. Every sap.ui.table sample of` &&
            ` this batch drops it the same way. // NOTE: Unverified in a running system: whether the 115-row model renders with the sample's threshold/scrollThreshold settings, and whether the refresh and` &&
            ` operation-mode round-trips re-read the rows. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_357.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `OData`                                         class = `z2ui5_cl_smpc_app_357` path = `src/02/02/z2ui5_cl_smpc_app_357.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.table.Table.scrollThreshold (@since 1.128) is kept 1:1 from the sample's view - it is one of the two paging knobs (with threshold) this sample is about. Newer than UI5 1.71; declared per the` &&
                 ` fidelity-first property-171 policy, so the app needs a UI5 release >= 1.128.` ) ).

    text1 = `IMPROVISED: The original binds the Table.columns aggregation to the OData METAMODEL and builds every Column in a JS factory (columnFactory), so its view declares no columns at all. A control-returning` &&
            ` factory is the capability boundary CAPABILITIES marks as not expressible - and the decision it makes belongs in the backend anyway (thin frontend). The port therefore drops the Table.columns binding` &&
            ` attribute and writes out the twenty-one Columns the factory produces from the sample's own metadata.xml, with the same rules: visible unless sap:visible="false" or a unit-of-measure / currency-code` &&
            ` semantic, the width derived from maxLength (>50 -> 15rem, >9 -> 10rem, else 5rem, absent maxLength counting as 10), sortProperty / filterProperty only where sap:sortable / sap:filterable is true, End` &&
            ` alignment for the Edm.Decimal columns, the sap:label as the header text, and a u:Currency template for the one property whose sap:unit carries the currency-code semantic (Price over CurrencyCode) -`.
    text1 = text1 && ` every other one a plain non-wrapping Text. structural-diff therefore reports 21 extra Column, 21 extra m:Label, 20 extra m:Text and one extra u:Currency, all of them the controller-built ones. //` &&
            ` NOTE: The sample serves its rows from an in-page OData MockServer (sap/ui/core/util/MockServer over metadata.xml, autoRespondAfter 2000, threshold 15). An abap2UI5 app has a real ABAP backend, so the` &&
            ` mock service is replaced by the model itself: all 115 rows of ProductSet.json are inlined with every property the metadata declares, because the metadata-driven columns render all of them. What that` &&
            ` costs is the server-side paging illusion and with it the dataRequested/dataReceived handlers that swap the noData BusyIndicator in and out; the noData BusyIndicator, threshold and enableBusyIndicator` &&
            ` are kept 1:1 on the control. ProductSet.json lives in the sibling OData sample folder upstream (the MockServer's sMockdataBaseUrl points there); it is archived into this sample's folder too so the`.
    text1 = text1 && ` port is verifiable offline. The numeric columns stay TYPE string so the mock's exact decimals survive. // NOTE: Fully static port apart from the model: once the column factory is resolved in ABAP the` &&
            ` sample has no event at all, so the class is the app-051 shape (a bare check_on_init branch with model_init and view_display, no on_event).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `OData2`                                        class = `z2ui5_cl_smpc_app_358` path = `src/01/02/z2ui5_cl_smpc_app_358.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.ui.table.RowSettings.navigated (@since 1.72) is used 1:1 - it is the property the sample's 'Toggle Navigated Indicators' button drives. Newer than UI5 1.71; declared per the` &&
            ` fidelity-first property-171 policy, so the app needs a UI5 release >= 1.72. // NOTE: The original builds a DIFFERENT RowAction template per mode in the controller (five handlers returning a fresh` &&
            ` RowAction with its RowActionItems, handed to setRowActionTemplate/setRowActionCount). Building controls at runtime is the capability boundary CAPABILITIES marks as not expressible, and the decision` &&
            ` behind it is data, so the port declares the UNION of the items once - Navigation, Delete, Attachment, Search, Edit, Analyze - and switches them with bound visible flags plus a bound rowActionCount` &&
            ` that the backend sets from the picked mode. Every mode renders exactly the items the original's handler would return: Navigation (1 action), Navigation & Delete, Navigation & Custom (the Edit item),` &&
            ` Multiple Actions (all four), No Actions (count 0). The Navigation item keeps the original's per-row visible="{Available}" condition, ANDed with the mode flag in an expression binding. structural-diff`.
    text1 = text1 && ` sees the rowActionTemplate subtree as added controls, which is what the controller built. // NOTE: onNavIndicatorsToggle sets or destroys a RowSettings template; the port declares the RowSettings` &&
            ` once and makes its navigated an expression binding over the ToggleButton's two-way bound state AND the row's own NavigatedState - so the toggle needs no handler at all and its press attribute goes` &&
            ` away. The original marks the SECOND product as navigated (i === 1), which the model reproduces. // NOTE: handleActionPress is client-composed and roundtrip-free: control_global MESSAGE_TOAST with` &&
            ` 'Item {0} pressed for product with id {1}', filled by ${$parameters>/item}.getText() || ${$parameters>/item}.getType() and ${$parameters>/row}.getBindingContext().getProperty('PRODUCTID') - the same` &&
            ` two values the original reads off the event, an event arg being a full UI5 expression. The named ``modes>`` model is folded onto the one default model, prefix dropped and leaf names kept. // NOTE:` &&
            ` The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined with the four columns the sample binds plus the derived Available flag the Navigation item's visible binding`.
    text1 = text1 && ` needs - the controller derives it the same way (Status === 'Available'). // NOTE: Unverified in a running system: whether the bound rowActionCount plus the per-item visible flags reproduce each` &&
            ` mode's row actions, and whether the two-argument client-composed toast fills both placeholders. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_359.mjs). //` &&
            ` NOTE: switchState always calls setRowActionTemplate BEFORE setRowActionCount, and that ordering is load-bearing: setRowActionTemplate ends in invalidateRowsAggregation( ), setRowActionCount only` &&
            ` setProperty( ). A row's _rowAction is attached exclusively in _getRowClone( ), guarded by hasRowActions( ) which needs getRowActionCount( ) > 0 - so any row created while the count is 0 has NO` &&
            ` _rowAction, and merely raising the count afterwards never re-creates the clones. The port drives only the count from the model and had no counterpart for that invalidation, which left a dead end` &&
            ` reachable without anything exotic: pick 'No Actions', leave and come back, and check_on_navigated rebuilds the view with mode_key still 'None' - every row is then created actionless and no later mode`.
    text1 = text1 && ` switch brings the actions back. invalidateRowsAggregation( ) cannot be wired (the frontend action allowlist denies the whole invalidate* prefix), so since 2026-08-24 a mode change that raises the` &&
            ` count off 0 re-displays the slot instead, rebuilding the Table with the new count already set. Note the e2e module's 2026-08-21 experiment - setRowActionCount(2) + invalidate( ) direct on the table -` &&
            ` did NOT rule the port out as that comment claimed: neither call re-creates a row clone, so it reproduced this very gap.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `RowAction`                                     class = `z2ui5_cl_smpc_app_359` path = `src/02/02/z2ui5_cl_smpc_app_359.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.table.RowSettings.navigated (@since 1.72) is used 1:1 - it is the property the sample's 'Toggle Navigated Indicators' button drives. Newer than UI5 1.71; declared per the fidelity-first` &&
                 ` property-171 policy, so the app needs a UI5 release >= 1.72.` ) ).

    text1 = `NOTE: The three toolbar handlers imperatively mutate Table properties in the original controller: onSelectionModeChange calls setSelectionMode, onAlternateToggle calls setAlternateRowColors,` &&
            ` onHighlightToggle swaps the rowSettingsTemplate between a RowSettings and null. abap2UI5 is a thin frontend, so all three are reproduced as two-way property bindings on the one default model with no` &&
            ` round-trip (the recipe's 'prefer a bindable property' rule (.claude/skills/port-a-sample/SKILL.md; the linter enforces it as settable-property-via-action)): the Select selectedKey and the Table` &&
            ` selectionMode both bind SELECTION_MODE; the 'Toggle Alternate Row Colors' ToggleButton pressed and the Table alternateRowColors both bind ALTERNATE_ROW_COLORS (the alternateRowColors attribute is` &&
            ` added to the Table - it is not in the original view, which only sets it via the controller); the 'Toggle Highlights' ToggleButton pressed binds SHOW_HIGHLIGHTS, which the RowSettings highlight reads` &&
            ` through an expression binding {= ${/SHOW_HIGHLIGHTS} ? ${STATUS} : 'None'} to reproduce the template-null hide. As a consequence the original event-handler attributes are dropped: the Select change`.
    text1 = text1 && ` and both ToggleButton press handlers are not emitted (their behaviour now lives in the bindings). // NOTE: The highlight state per row (RowSettings highlight={Status}, highlightText={StatusText}) is` &&
            ` computed by the original controller in initSampleDataModel (fixed states for the first five rows, then Success/Warning/Error/Information/Indication01/None derived from the Price thresholds, with the` &&
            ` matching custom highlightText). This is business logic, so per the thin-frontend principle it is computed in ABAP model_init into the STATUS / STATUSTEXT model fields and bound directly` &&
            ` (highlight={STATUS} via the expression above, highlightText={STATUSTEXT}) rather than in a frontend formatter. // NOTE: The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is` &&
            ` inlined verbatim into model_init with the five columns the sample binds (Name, ProductId, Quantity, Price, CurrencyCode). Price is typed as a packed ABAP field (p LENGTH 13 DECIMALS 2) because the` &&
            ` u:Currency value property is numeric (float) - UI5 2.x strict-type validation would reject a string there. The mock's own Status field (all 'Available') is intentionally not carried, since the sample`.
    text1 = text1 && ` overwrites Status with the highlight classification above. // NOTE: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): all three toolbar controls` &&
            ` really drive the grid Table through their bindings - the Toggle Alternate Row Colors button flips Table.alternateRowColors to true, turning Toggle Highlights off puts every RowSettings highlight back` &&
            ` to 'None' through the expression binding, and picking 'Single' in the SelectionMode Select sets Table.selectionMode. They all sit in the OverflowToolbar, which IS drivable once its 'Additional` &&
            ` Options' popover is open (AGENTS 10). All @since-checked members are <= 1.71 so no POST_171 is needed (RowSettings 1.48, RowSettings.highlightText 1.62, Table.alternateRowColors 1.52, u:Currency` &&
            ` 1.21.1). // NOTE: sortProperty/filterProperty carry the UPPER-CASED ABAP field names, not the original's mixed-case ones: abap2UI5 derives the model paths from the ABAP component names, which is why` &&
            ` the cells on the same columns bind {NAME}/{CATEGORY} and friends. Copied verbatim from the original until 2026-08-23, and UI5 generates the column menu's sort and filter entries off the property`.
    text1 = text1 && ` merely being SET - so the entries were there and resolved to undefined on every row: sorting a no-op, filtering empty. Corpus convention, same sweep that fixed apps 137/164/174/247/353.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `RowHighlights`                                 class = `z2ui5_cl_smpc_app_174` path = `src/01/02/z2ui5_cl_smpc_app_174.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The original splits UI state into a separate 'ui' JSON model and binds the row mode from it ({ui>/rowMode}) in two places - the Table's rowMode aggregation and the footer SegmentedButton's` &&
            ` selectedKey - while the grid rows come from the default model ({/ProductCollection}). abap2UI5 has one default model, so the row mode is folded into it and both places bind it directly - the 'ui>'` &&
            ` prefix is dropped and the path flattened to a single field (rowMode); the last path segment is identical, which structural-diff matches. // NOTE: The shared 123-row demo ProductCollection` &&
            ` (sap/ui/demo/mock/products.json) is inlined into model_init with the five columns the sample binds (Name, Category, ProductPicUrl, Quantity, DeliveryDate), in the mock's own row order (review` &&
            ` 2026-07-27 fixed a reordered tail and one invented 'Smartphone Cover' row - the mock has two 'Tablet Pouch' rows). ProductPicUrl values point at the OpenUI5 host` &&
            ` (https://sdk.openui5.org/test-resources/...) per the asset-URL rule; the mock carries them host-relative. The original computes DeliveryDate from Date.now() with an i-mod-10 offset; a fixed base date`.
    text1 = text1 && ` (2026-07-23) is used here so the port is deterministic - a client-only display decision. The Quantity and DeliveryDate columns keep the original typed complex bindings (sap.ui.model.type.Integer /` &&
            ` .Date with timestamp source). // POST-1.71: sap.ui.table.Table.rowMode (aggregation, @since 1.119) is used 1:1 (the RowModes sample binds rowMode to the folded 'ui>' state). Newer than UI5 1.71;` &&
            ` declared per the property-171 policy. Previously undeclared because the property gate is blind to sap.ui.table (properties.json holds sap.m only); found by the non-sap.m @since audit 2026-07-24. //` &&
            ` NOTE: onInit lazily requires sap/ui/table/sample/TableExampleUtils and appends a ToolbarSpacer plus its createInfoButton( ) to the footer toolbar; the port drops both (it keeps the toolbar's id` &&
            ` infobar, which the original addresses to append them). The button opens the demo kit's own sample-info popover, which has no counterpart outside the demo kit. Every sap.ui.table sample of this batch` &&
            ` drops it the same way - apps 352/353/354/355/357/362 declare exactly this; 164 had been dropping it undeclared until 2026-08-23. // NOTE: sortProperty/filterProperty carry the UPPER-CASED ABAP field`.
    text1 = text1 && ` names, not the original's mixed-case ones: abap2UI5 derives the model paths from the ABAP component names, which is why the cells on the same columns bind {NAME}/{CATEGORY} and friends. Copied` &&
            ` verbatim from the original until 2026-08-23, and UI5 generates the column menu's sort and filter entries off the property merely being SET - so the entries were there and resolved to undefined on` &&
            ` every row: sorting a no-op, filtering empty. Corpus convention, same sweep that fixed apps 137/164/174/247/353.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `RowModes`                                      class = `z2ui5_cl_smpc_app_164` path = `src/02/02/z2ui5_cl_smpc_app_164.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        checked = `CHECKED (2026-07-27): verified in a running system 2026-07-27 - sap.ui.table RowModes renders the regenerated 123-row set; rowMode switching works. **e2e-verified 2026-09-12** (interaction module` &&
                 ` meta/interactions/z2ui5_cl_smpc_app_164.mjs: the rows binding carries the 123-row set, the table boots in Fixed, and picking Auto from the row-mode control (a Select inside the footer's overflow` &&
                 ` popover at the smoke's viewport) switches the Table rowMode through the shared bound field)`
        notes = text1
        post171 = `sap.ui.table.Table.rowMode (aggregation, @since 1.119) is used 1:1 (the RowModes sample binds rowMode to the folded 'ui>' state). Newer than UI5 1.71; declared per the property-171 policy. Previously` &&
                 ` undeclared because the property gate is blind to sap.ui.table (properties.json holds sap.m only); found by the non-sap.m @since audit 2026-07-24.` ) ).

    text1 = `POST-1.71: Three post-1.71 members are kept 1:1 because the sample is built on them: sap.ui.table.Table.rowMode (aggregation, @since 1.119) with the control sap.ui.table.rowmodes.Fixed it holds,` &&
            ` sap.m.plugins.PasteProvider (@since 1.91) in the toolbar Button's dependents, and sap.ui.table.plugins.MultiSelectionPlugin's selectionMode property (@since 1.100). Declared per the fidelity-first` &&
            ` property-171 policy, so the app needs a UI5 release >= 1.119. (The selectionMode @since 1.100 clause this entry used to carry was dropped 2026-08-21: MultiSelectionPlugin.selectionMode has no @since` &&
            ` at all. rowMode @1.119 is real and is what keeps the class in src/02.) // NOTE: The sample writes its two plugin controls with a DOTTED element name and no namespace prefix` &&
            ` (<plugins.MultiSelectionPlugin> under the sap.ui.table default xmlns, <m:plugins.PasteProvider>), which UI5 resolves as a sub-package of the element's namespace. The port declares real prefixes for` &&
            ` those two packages instead (xmlns:tp="sap.ui.table.plugins", xmlns:mp="sap.m.plugins") and writes tp:MultiSelectionPlugin / mp:PasteProvider - the same two controls, a namespace-representation`.
    text1 = text1 && ` difference only. structural-diff compares the qualified name, so it reports the dotted names as missing and the prefixed ones as extra. // NOTE: onSelectChange disappears: the selection-mode Select's` &&
            ` selectedKey and the MultiSelectionPlugin's selectionMode bind the SAME field, so picking a mode drives the plugin with no round-trip - the prefer-a-bindable-property rule. The named ``ui>`` model` &&
            ` (the three modes and the initial one) is folded onto the one default model, prefix dropped and leaf names kept. // IMPROVISED: The copy half of the sample is dropped, and with it the toolbar's copy` &&
            ` Button: onInit creates a sap.m.plugins.CellSelector and a sap.m.plugins.CopyProvider in JS (guarded by window.isSecureContext), adds both to the table's dependents and appends the CopyProvider's` &&
            ` generated copy Button to the toolbar. Building controls at runtime is the capability boundary CAPABILITIES marks as not expressible, and the CopyProvider's extractData/copy callbacks are app-authored`.
    text1 = text1 && ` JS formatters. The declared PasteProvider Button of the view is kept 1:1. onPaste consequently loses its cell-range branch - the CellSelector that would supply the range is gone - so the port reports` &&
            ` the pasted data at TABLE level, which is the same handler's other branch, with the original's message text. // NOTE: The sample serves its rows from an in-page OData MockServer (over the sibling` &&
            ` OData sample's metadata.xml and mock data). An abap2UI5 app has a real ABAP backend, so the mock service is replaced by the model itself: all 115 rows of ProductSet.json are inlined and the Table` &&
            ` binds them directly - the server-side paging illusion is what that costs, while threshold, enableBusyIndicator and the noData BusyIndicator stay 1:1. ProductSet.json lives in the sibling OData sample` &&
            ` folder upstream; it is archived into this sample's folder too so the port is verifiable offline. The six column labels are metadata bindings in the original ({/#Product/Name/@sap:label} and friends),` &&
            ` which only an OData model can resolve, and are replaced by the literal sap:label texts from metadata.xml. The numeric columns stay TYPE string so the mock's exact decimals survive. Two of the six`.
    text1 = text1 && ` header texts were the OData2 sample's labels, not this one's, until 2026-08-21: Category where this metadata.xml says "Prod. Cat.", and "Supplier Company Name" where it says "Company Name" - while` &&
            ` this very deviation asserted the texts came from the sample's own metadata. Corrected against the archived file. // NOTE: Unverified in a running system: whether the paste event delivers the pasted` &&
            ` data array to get_event_arg, and whether the bound selectionMode reaches the plugin without a round-trip. **e2e-verified 2026-08-21** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_360.mjs). // NOTE: The paste event's data parameter is typed string[][], so an array/object event argument reaches the app as serialized JSON - [["Pasted` &&
            ` Name","Pasted Id"]]. The original builds its message as "..." + aData, and JS coerces the array to Pasted Name,Pasted Id. The port toasted the raw JSON until 2026-08-24, showing brackets and quotes` &&
            ` the user never sees upstream; the brackets and quotes are now stripped, which reproduces the coercion - the same substitution the sibling port 361 already declares for its index array. The e2e module`.
    text1 = text1 && ` could not see the difference: it asserts toContainText('Pasted Name'), which passes for both renderings.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `SelectCopyPaste`                               class = `z2ui5_cl_smpc_app_360` path = `src/02/02/z2ui5_cl_smpc_app_360.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = `Three post-1.71 members are kept 1:1 because the sample is built on them: sap.ui.table.Table.rowMode (aggregation, @since 1.119) with the control sap.ui.table.rowmodes.Fixed it holds,` &&
                 ` sap.m.plugins.PasteProvider (@since 1.91) in the toolbar Button's dependents, and sap.ui.table.plugins.MultiSelectionPlugin's selectionMode property (@since 1.100). Declared per the fidelity-first` &&
                 ` property-171 policy, so the app needs a UI5 release >= 1.119. (The selectionMode @since 1.100 clause this entry used to carry was dropped 2026-08-21: MultiSelectionPlugin.selectionMode has no @since` &&
                 ` at all. rowMode @1.119 is real and is what keeps the class in src/02.)` ) ).

    text1 = `NOTE: Three of the four controller setters become bound properties. The behaviour Select's selectedKey and the Table's selectionBehavior share one field, and the Switch's state and the Table's` &&
            ` enableSelectAll share another - so onBehaviourModeChange and onSwitchChange disappear and their wires with them: the behaviour Select keeps no change attribute and the Switch loses Switch.change` &&
            ` entirely (the only attribute structural-diff reports missing). onSelectionModeChange is the one that KEEPS its change wire, because it is not a plain setter: it REFUSES the deprecated All mode with a` &&
            ` MessageToast and leaves the table on its previous mode. The port therefore keeps the Select's key and the Table's selectionMode as two separate fields and copies one into the other in the handler,` &&
            ` snapping the Select back when All was picked - exactly the original's behaviour. // NOTE: getSelectedIndices / getContextByIndex read state that lives in the CONTROL, not in the model. Rather than` &&
            ` faking a value, the port mirrors the selection into the model: the Table's rowSelectionChange event carries ${$parameters>/rowIndices} and ${$parameters>/rowIndex} to the backend, and the two buttons`.
    text1 = text1 && ` then report from that - the index list, or 'no item selected' when it is empty, exactly as the original branches. getContextByIndex toasts the last selected row's model path (/T_PRODUCTS/<n>), which` &&
            ` is what a sap.ui.model.Context renders as when the original passes it to MessageToast. clearSelection stays imperative because the selection is control state: follow_up_action control_by_id` &&
            ` table1/clearSelection, plus clearing the mirrored fields. // NOTE: The named ``selectionmodel>`` model (the SelectionMode / SelectionBehavior enum values the controller enumerates at runtime,` &&
            ` skipping Multi) is folded onto the one default model as two tables; the values are the sap.ui.table enum members as of the current release. The controller's two formatters are computed in ABAP per` &&
            ` the thin-frontend principle: formatAvailableToObjectState and formatAvailableToIcon become the precomputed AVAILABLESTATE (Success/Error) and AVAILABLEICON (sap-icon://accept / sap-icon://decline)` &&
            ` columns. handleDetailsPress is client-composed and roundtrip-free: control_global MESSAGE_TOAST with 'Details for product with id {0}' and the row's ${PRODUCTID}. Corrected 2026-08-21: the first list`.
    text1 = text1 && ` carried a fourth entry ``All``, which SelectionMode does not define, and both lists were in an order the enums do not have. Unlike app 356, where the same wrong entry crashes on validateProperty,` &&
            ` on_event refuses ``All`` here before it reaches selection_mode - it still showed an item the original never shows and made a dead upstream branch reachable. // NOTE: The Suppliers and Categories` &&
            ` collections the controller derives from the products (the distinct SupplierName / Category values in first-appearance order) are inlined as their own tables, and the two in-cell dropdowns keep the` &&
            ` original's binding-info form with templateShareable - only the path switches to the ABAP table. The Heavy CheckBox and the DeliveryDate DatePicker keep their typed complex bindings 1:1 with the path` &&
            ` pointed at the ABAP field. // NOTE: The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined with the columns the twelve table columns bind. The original computes` &&
            ` DeliveryDate from Date.now() with an i-mod-10 offset in 4-day steps; a fixed base date (2026-07-23) is used here so the port is deterministic - the corpus convention of app 164. ProductPicUrl values`.
    text1 = text1 && ` point at the OpenUI5 host per the asset-URL rule; the mock carries them host-relative. // NOTE: Unverified in a running system: whether rowSelectionChange delivers the index array as JSON to` &&
            ` get_event_arg, whether the two bound Selects drive selectionMode/selectionBehavior without a round-trip, and the control_by_id clearSelection wire. **e2e-verified 2026-08-17** - but that stamp` &&
            ` predates the wire it certifies. On 2026-08-21 the t_arg was rewritten from ${$parameters>/rowIndices} to ${$source>}.getSelectedIndices( ) (see the deviation below), and the module was not changed:` &&
            ` it selected exactly ONE row, the single case where the changed-rows parameter and the current selection coincide, so it kept passing across the rewrite without ever exercising either case that` &&
            ` motivated it. The module selects a second row and checks both indices are reported since 2026-08-24 (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_361.mjs). // NOTE: The three buttons` &&
            ` read the table's CURRENT selection, so rowSelectionChange transports ${$source>}.getSelectedIndices() and ${$source>}.getSelectedIndex() - what the original's getSelectedIndices( ) and`.
    text1 = text1 && ` getContextByIndex( ) ask the control for. Until 2026-08-21 the wire carried ${$parameters>/rowIndices} instead, and that parameter is documented as "array of row indices which selection has been` &&
            ` CHANGED (either selected or deselected)": ctrl-clicking a second row reported [1] where the original reports [0,1], and deselecting the only selected row reported [0] where the original says "no item` &&
            ` selected". The interaction module selected exactly one row, the single case where the two sets coincide, so the nightly saw nothing. The toast also strips the JSON brackets now - MessageToast coerces` &&
            ` the original's array to "0,1". // NOTE: The sample's asset paths are host-absolutized. The demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve` &&
            ` against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim` &&
            ` the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17`.
    text1 = text1 && ` carry the declaration since today, so the claim holds again; a stale absolute count is what made it wrong, so this wording names the date the count was taken.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `Selection`                                     class = `z2ui5_cl_smpc_app_361` path = `src/01/02/z2ui5_cl_smpc_app_361.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: Every sort of this sample happens in ABAP. The three toolbar buttons (sortCategoriesAndName / sortCategories / clearAllSortings) and the column header menu all fire a backend event, the model` &&
            ` comes back SORTed and each Column's sortOrder AND its sorted flag are bound so the indicator follows. Binding sortOrder alone did not render one: TableRenderer._addColumnSortAndFilterClasses derives` &&
            ` bSorted from getSortOrder( ) but then, in a block still present through 1.151, does ``if (!oColumn.getSorted()) { bSorted = false; }`` - and Column.sorted defaults to false. UI5's own doc on the` &&
            ` property says it outright: "Even though the sorted property has been deprecated, is still required in the UI5 1.x versions to show the sort indicator." So sorted is bound to ``!== 'None'`` (corrected` &&
            ` 2026-08-24). The same change removes an enum hazard rather than declaring it: sortOrder is now an expression yielding only 'Ascending' or 'Descending', never 'None'. None is a member of` &&
            ` sap.ui.core.SortOrder, which the property was only retyped to at 1.120 - before that it was sap.ui.table.SortOrder, which has Ascending and Descending and nothing else. Seeding 'None' would therefore`.
    text1 = text1 && ` have made the port need UI5 >= 1.120 (an enum VALUE is invisible to the property gates, per AGENTS section 5); driving the visibility through sorted instead keeps it inside the 1.71 floor, which is` &&
            ` where the original runs - the thin-frontend equivalent of the original's oTable.sort( column, order, extend ) and setSortOrder calls. clearAllSortings re-seeds the model, which restores the mock's` &&
            ` own row order, and resets every indicator to None (the original's binding.sort(null) + _resetSortingState). sortCategories keeps the original's alternating ascending/descending on repeated presses.` &&
            ` // NOTE: The Table's sort event carries the pressed column's sortProperty and the requested sortOrder to the backend (${$parameters>/column}.getSortProperty() and ${$parameters>/sortOrder} - an event` &&
            ` arg is a full UI5 expression) and sets s_ctrl-check_prevent_default, so the control's own client-side sort never runs. The original vetoes it only for the delivery-date column; here the model is the`.
    text1 = text1 && ` single source of truth for every column, so all of them are vetoed and sorted server-side. The delivery-date column is why the original needs a veto at all: its cells hold dd/MM/yyyy STRINGS, which` &&
            ` no text compare can order, so it installs a custom Sorter.fnCompare that parses them. The port sorts the underlying timestamp instead - the same result without any frontend logic. 'No multi-column` &&
            ` sorting' is kept: every other indicator is reset before the pressed column is sorted. // NOTE: The row type carries the raw DeliveryDate timestamp next to the formatted DeliveryDateStr, because that` &&
            ` timestamp is the sort key the original's custom compare function reconstructs by parsing. The bound sortProperty values are the ABAP (upper-cased) field names (NAME, CATEGORY, QUANTITY,` &&
            ` DELIVERYDATESTR) rather than the original's JSON key names, since that is what the backend switches on. // NOTE: The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined` &&
            ` with the five columns the sample binds. The original computes DeliveryDate from Date.now() with an i-mod-10 offset in 4-day steps; a fixed base date (2026-07-23) is used here so the port is`.
    text1 = text1 && ` deterministic - the corpus convention of app 164 - and DeliveryDateStr is that timestamp formatted dd/MM/yyyy, exactly what the controller's DateFormat produces. ProductPicUrl values point at the` &&
            ` OpenUI5 host per the asset-URL rule; the mock carries them host-relative. The Quantity and Delivery Date columns keep the original's typed complex bindings 1:1, with their path switched to the ABAP` &&
            ` field name. // IMPROVISED: The footer OverflowToolbar stays empty: onInit lazily requires sap/ui/table/sample/TableExampleUtils and appends a ToolbarSpacer plus its createInfoButton( ) to it. That` &&
            ` helper lives in the demo kit's own sample folder, not in any UI5 library, and only opens a popover pointing at the sample's source - there is nothing to port it to. Every sap.ui.table sample of this` &&
            ` batch drops it the same way. // NOTE: Unverified in a running system: whether the sort event's prevented default plus the server-side SORT produce the expected order for each column, and whether the` &&
            ` bound Column.sorted/sortOrder pair renders the header indicator. (Reopened 2026-08-21: this had been closed as live-verified, but the interaction module only presses the three toolbar buttons - it`.
    text1 = text1 && ` never fired the column sort event, so its prevented default was never exercised, and it asserted getSortOrder( ) rather than the rendered sapUiTableColSorted class, which is the half that was` &&
            ` actually broken. The toolbar legs it does drive are genuinely covered.) Both gaps are closed in the module since 2026-08-25: every indicator assertion reads sapUiTableColSorted / sapUiTableColSortedD` &&
            ` off the column's own header cell, and the last leg calls Column._sort( ) - the method the header menu's Sort entry calls, which ends in oTable.fireSort( ) and returns before setSortOrder when a` &&
            ` handler vetoes - so the check_prevent_default wire and both event args are driven exactly as a menu press drives them. Pending one green nightly run. **e2e-verified 2026-08-25** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_362.mjs). // NOTE: sortCategories passes bAdd = true to oTable.sort( ), and Table.pushSortedColumn appends the column to the active sorter list rather` &&
            ` than replacing it - so whatever was already sorting keeps precedence and Category is added behind it. The port keeps that list as ordered model state (the equivalent of _aSortedColumns) - on every`.
    text1 = text1 && ` path that sorts, which until 2026-08-24 was only the toolbar one. Column._sort ends in oTable.pushSortedColumn( this ), and onInit's oTable.sort( ) pushes Name, so both the column-header menu and the` &&
            ` initial sort put a column on that list. The port's sort_clear( ) emptied it and neither path put anything back, so a "Sort Categories in addition to current sorting" press straight after load, or` &&
            ` after any column-menu sort, ordered by Category ALONE while sort_name still displayed Ascending - the same header-lies-about-the-rows defect this deviation records as fixed for the toolbar path,` &&
            ` still live on the other two. Both now append their column and applies it from the last key to the first with SORT ... STABLE, since one ABAP SORT expresses one key. Until 2026-08-21 the handler` &&
            ` issued a fresh single-key SORT, which reordered the whole table while leaving the other columns' bound sortOrder indicators standing: the header claimed Name-ascending while the rows were` &&
            ` Category-ascending, and the button's own tooltip - "Sort Categories in addition to current sorting" - was kept while the behaviour was not. // NOTE: The sample's asset paths are host-absolutized. The`.
    text1 = text1 && ` demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise` &&
            ` the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the` &&
            ` corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what` &&
            ` made it wrong, so this wording names the date the count was taken. // NOTE: **e2e-caught 2026-08-22**: the sort ran but the table came back in its original order. The cause is the transpiled backend:` &&
            ` ``SORT <itab> BY (field)`` - the DYNAMIC component form - loses its BY clause entirely (the emitted JS is ``abap.statements.sort(t, {})``), so every sort was a no-op. The component is named` &&
            ` statically per field now, in a CASE. Apps 298, 362 and 571 all carried the dynamic form and all three are fixed.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `Sorting`                                       class = `z2ui5_cl_smpc_app_362` path = `src/01/02/z2ui5_cl_smpc_app_362.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.ui.table.Table.rowMode (aggregation, @since 1.119) and the control sap.ui.table.rowmodes.Fixed it holds are used 1:1 - the sample declares them in its view and the freeze demo drives` &&
            ` the row mode's fixedTopRowCount / fixedBottomRowCount. Both are newer than UI5 1.71; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.119. // NOTE: buttonPress` &&
            ` becomes a clamp instead of three setters: the three count Inputs are two-way bound and the Table's fixedColumnCount plus the rowMode's fixedTopRowCount / fixedBottomRowCount bind the SAME fields, so` &&
            ` the Apply press only has to run the original's validation (fixed column count against the column total, fixed top + bottom rows against the visible row total) and push the corrected values back -` &&
            ` with the same two MessageToast texts, verbatim. The two totals the original reads off the live control (getColumns().length, getRows().length) are constants here: twelve columns as declared in the` &&
            ` view, and the ten visible rows the Fixed row mode defaults to. // NOTE: The controller's two formatters are computed in ABAP, per the thin-frontend principle: formatAvailableToObjectState and`.
    text1 = text1 && ` formatAvailableToIcon become the precomputed AVAILABLESTATE (Success/Error) and AVAILABLEICON (sap-icon://accept / sap-icon://decline) columns the ObjectStatus and core:Icon bind. handleDetailsPress` &&
            ` is client-composed and needs no round-trip at all: control_global MESSAGE_TOAST with 'Details for product with id {0}' and the row's ${PRODUCTID} as the argument, which is what the original builds` &&
            ` from the row's binding context. // NOTE: The Suppliers and Categories collections the controller derives from the products (the distinct SupplierName / Category values in first-appearance order) are` &&
            ` inlined as their own tables, and the two in-cell dropdowns keep the original's binding-info form with templateShareable - only the path switches to the ABAP table. The Heavy CheckBox and the` &&
            ` DeliveryDate DatePicker keep their typed complex bindings 1:1 with the path pointed at the ABAP field. // NOTE: The shared 123-row demo ProductCollection (sap/ui/demo/mock/products.json) is inlined` &&
            ` with the columns the twelve table columns bind. The original computes DeliveryDate from Date.now() with an i-mod-10 offset in 4-day steps; a fixed base date (2026-07-23) is used here so the port is`.
    text1 = text1 && ` deterministic - the corpus convention of app 164. Heavy is WeightMeasure > 1000 as the string the typed CheckBox binding expects. ProductPicUrl values point at the OpenUI5 host per the asset-URL` &&
            ` rule; the mock carries them host-relative. // NOTE: Unverified in a running system: whether the bound fixedColumnCount and the bound rowMode counts freeze the expected columns/rows, and whether the` &&
            ` Apply clamp round-trip updates the Inputs. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_363.mjs). // NOTE: The three count Inputs start EMPTY, as in the` &&
            ` original, which gives them no value attribute at all - so the placeholders ("fixed column count" and friends) are what the user sees, and buttonPress reads them as getValue( ) || 0. Until 2026-08-21` &&
            ` they were seeded from the freeze counts, i.e. with "0", and sap.m.Input hides the placeholder as soon as a value is set: the port opened with three zeroes where the sample opens with three hints. The` &&
            ` interaction module could not see it because every assertion started by filling over whatever was there; it asserts the empty start now. // NOTE: The sample's asset paths are host-absolutized. The`.
    text1 = text1 && ` demo kit serves them relative (test-resources/...), which an abap2UI5 app has no document root to resolve against, so the port points at https://sdk.openui5.org/... instead. The values are otherwise` &&
            ` the mock's own. Declared 2026-08-21 for consistency, and RE-COUNTED 2026-08-23: the sentence used to claim the rewrite was 'declared by all 77 ports that do it', which had stopped being true as the` &&
            ` corpus grew past that day's snapshot - 126 ports do it now, and 17 of them declared it nowhere. Those 17 carry the declaration since today, so the claim holds again; a stale absolute count is what` &&
            ` made it wrong, so this wording names the date the count was taken. // NOTE: Two corrections to the Apply handler, both 2026-08-24. (a) The port treated an EMPTY Input like a non-numeric one and kept` &&
            ` the previous count, which left no way to UN-freeze: the original reads ``getValue() || 0``, so a cleared Input parseInts to 0 and Apply sets the count to 0. count_read( ) now separates the two -` &&
            ` empty is 0, a clean number is that number, and only a non-empty non-numeric entry keeps the last value, which is where the original would hand the setter a NaN. (b) The corrected numbers were written`.
    text1 = text1 && ` back into all three Inputs unconditionally; the original calls setValue( ) only INSIDE its two clamp branches. So the first Apply on a freshly loaded app - all three Inputs legitimately empty,` &&
            ` nothing clamped - stamped '0' into each and dropped all three placeholders, re-introducing on the first press exactly the defect the initial render already avoids. The write-backs now sit inside the` &&
            ` branches, as in the original. Neither is visible to the e2e module, which always fill( )s before pressing Apply.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.Table`                    name = `TableFreeze`                                   class = `z2ui5_cl_smpc_app_363` path = `src/02/02/z2ui5_cl_smpc_app_363.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.table.Table.rowMode (aggregation, @since 1.119) and the control sap.ui.table.rowmodes.Fixed it holds are used 1:1 - the sample declares them in its view and the freeze demo drives the row` &&
                 ` mode's fixedTopRowCount / fixedBottomRowCount. Both are newer than UI5 1.71; declared per the fidelity-first property-171 policy, so the app needs a UI5 release >= 1.119.` ) ).

    text1 = `IMPROVISED: The sample's rows come from an OData v2 tree binding over a mock service: the Nodes entity is a FLAT list carrying NodeID / HierarchyLevel / Description / ParentNodeID / DrillState, and` &&
            ` the binding's treeAnnotationProperties (hierarchyLevelFor, hierarchyNodeFor, hierarchyParentNodeFor, hierarchyDrillStateFor) tell the ODataTreeBinding how to assemble it into a tree. abap2UI5 serves` &&
            ` one JSON model, so there is no ODataTreeBinding and no annotation to read: the same sixteen nodes are modelled NESTED instead (one children table per level) and bound with the arrayNames parameter -` &&
            ` the JSON tree binding the framework does support (the app-248 idiom). The rendered tree is identical, every node keeps all five of its own fields (NodeID, HierarchyLevel, Description, ParentNodeID,` &&
            ` DrillState) and the four RENDERED columns are unchanged - the fifth field, ParentNodeID, is what the nesting replaces, so it is carried but not shown; what is lost is the annotation-driven assembly` &&
            ` itself, and with it the countMode: 'Inline' parameter, which is an OData request option. // NOTE: localService/mockdata/Nodes.json is inlined in full - all sixteen nodes with the mock's own ids,`.
    text1 = text1 && ` levels, descriptions, parent ids and drill states; the root nodes' ParentNodeID is null in the JSON and becomes the empty string in the flat ABAP row type. The mock server itself` &&
            ` (localService/mockserver.js over localService/metadata.xml) has no counterpart in an abap2UI5 app, whose backend IS the service. // NOTE: Unverified in a running system: whether the nested model plus` &&
            ` arrayNames renders the same expandable tree the OData tree binding produces. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_364.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.TreeTable`                name = `TreeTable.BasicODataTreeBinding`               class = `z2ui5_cl_smpc_app_364` path = `src/01/02/z2ui5_cl_smpc_app_364.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: Collapse all and Expand first level are reproduced 1:1 as frontend actions: onCollapseAll / onExpandFirstLevel call the TreeTable's own collapseAll( ) and expandToLevel( 1 ), which have no` &&
            ` bindable equivalent (the expansion state lives in the control), so they go through follow_up_action control_by_id TreeTable/collapseAll and TreeTable/expandToLevel with the level as its argument -` &&
            ` roundtrip-free, exactly what the buttons do in the original. Re-verified 2026-08-23: still true and still the right shape - 'no bindable equivalent' is about a bindable PROPERTY (the expansion state` &&
            ` lives in the control), not about reachability, and the port already calls both methods. // IMPROVISED: The hierarchy MAINTENANCE half of the sample - Cut, Paste and the drag & drop re-parenting - is` &&
            ` dropped, and with it the DragDropInfo's dragStart and drop attributes. The control itself stays, switched off with enabled="false" (@since 1.56, inside the floor) - an attribute the original does not` &&
            ` carry, added 2026-08-21 because UI5's DropInfo.isDroppable never asks whether anyone is listening: with the two handlers gone but the configuration active, the port shipped draggable rows and a live`.
    text1 = text1 && ` drop indicator that silently discarded every drop. The Cut and Paste buttons keep their labels, icons and the Paste button's enabled="false" from the view but do nothing. All three handlers move a` &&
            ` node to an ARBITRARY new parent (onDrop pushes the dragged row's data into the dropped row's ``categories`` array and blanks the source; onCut/onPaste do the same through a clipboard). A JSON tree` &&
            ` binding needs the children nested under their parent, and an ABAP nesting is TYPED and fixed-depth (article inside group inside area inside root, as the JSON's own four levels are), so a node cannot` &&
            ` be re-parented to a level of a different type - which is precisely what these handlers do. The row-to-node resolution itself would be transportable (a drop event can ship` &&
            ` ${$parameters>/draggedControl}.getBindingContext().getPath()), so this is a limit of modelling a tree as typed ABAP data, not of the event wire. // NOTE: Clothing.json is inlined 1:1 - the full tree` &&
            ` (Women/Men/Girls/Boys) with every leaf's amount/currency/size - as nested types, one per level, so absent leaf fields exist only where the JSON carries them and the Size Select stays hidden via the`.
    text1 = text1 && ` original's !!${size} guard. It is byte-identical to the file the sibling TreeTable.JSONTreeBinding sample uses, which app 248 ports; the model and its types are shared with that port. The rows` &&
            ` binding keeps the original's arrayNames parameter with the ABAP (upper-cased) array name. // NOTE: Unverified in a running system: the two control_by_id wires (collapseAll, and expandToLevel with its` &&
            ` numeric argument - a listed CONTROL_METHODS entry, so the argument is declared and reaches the method). **e2e-verified 2026-08-21** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_365.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.TreeTable`                name = `TreeTable.HierarchyMaintenanceJSONTreeBinding` class = `z2ui5_cl_smpc_app_365` path = `src/01/02/z2ui5_cl_smpc_app_365.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: The onInit JSONModel('.../Clothing.json') load is folded into model_init: the full tree (Women/Men/Girls/Boys, every article with amount/currency/size) plus the /sizes list, 1:1 from the mock.` &&
            ` The tree keeps the original's shape - nested types per level under a CATALOG-CLOTHING structure, so the rows binding path '/CATALOG/CLOTHING' with parameters { arrayNames: ['CATEGORIES'] } mirrors` &&
            ` {path:'/catalog/clothing', parameters:{arrayNames:['categories']}}. Homogeneous-type caveat: a level-3 leaf article (Jewelry/Necklace class) carries an empty CATEGORIES array (the ABAP row type is` &&
            ` one shape per level) - JSONTreeBinding treats an empty child array as a leaf, so it renders like the original's absent property; and a level-3 CATEGORY row (Dresses) carries initial` &&
            ` AMOUNT/CURRENCY/SIZE fields its original JSON node omits - SIZE '' keeps the Select hidden through the original's own !!SIZE guard, and the Price guard below keeps the cell empty. // NOTE:` &&
            ` Currency.value is the guarded expression binding ``{= ${AMOUNT} > 0 ? ${AMOUNT} : null }`` (a numeric-string 0.00 from the transpiled runtime is truthy, so the guard compares > 0 instead of testing`.
    text1 = text1 && ` truthiness) instead of the original's plain {amount}: a category row's own AMOUNT serializes as 0 (initial packed - a flat ABAP row serializes every field), and an unguarded 0 would render '0.00'` &&
            ` where the original's absent JSON property renders an empty Price cell. The app-220 optional-value-guard idiom (backtick literal so the braces reach the attribute verbatim); leaf amounts are all > 0,` &&
            ` so no real price is masked. // NOTE: unverified in a running system: (a) the nested-structure model root (/CATALOG/CLOTHING via _bind on the catalog-clothing component) feeding the TreeTable rows` &&
            ` binding with arrayNames; (b) Collapse all / Expand first level via roundtrip-free follow_up_action control_by_id (collapseAll whitelisted, expandToLevel int); (c) Collapse/Expand selection 1:1 via` &&
            ` the $event.oSource.getParent().getParent().getSelectedIndices() expression - the resolved index array reaches the unlisted-but-public collapse/expand methods through castArgAuto untouched; (d) the` &&
            ` two-way Select selectedKey writing a changed size back into the tree row. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): (a) and (b) are covered - the tree`.
    text1 = text1 && ` renders its root categories from the nested-structure model, Expand first level reveals the second level (Accessories) and Collapse all hides it again; (c) selection expand/collapse and (d) the` &&
            ` two-way Select remain unexercised. Environment note: the transpiled runtime serializes packed AMOUNT as a numeric string, which is why the Currency guard compares > 0 instead of truthiness.` &&
            ` **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_248.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.TreeTable`                name = `TreeTable.JSONTreeBinding`                     class = `z2ui5_cl_smpc_app_248` path = `src/01/02/z2ui5_cl_smpc_app_248.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `IMPROVISED: The sample's rows come from an OData v2 tree binding over a mock service: the Nodes entity is a FLAT list carrying NodeID / HierarchyLevel / Description / ParentNodeID / DrillState, and` &&
            ` the service METADATA carries the same hierarchy annotations, which the ODataTreeBinding reads on its own. abap2UI5 serves one JSON model, so there is no ODataTreeBinding and no annotation to read:` &&
            ` the same sixteen nodes are modelled NESTED instead (one children table per level) and bound with the arrayNames parameter - the JSON tree binding the framework does support (the app-248 idiom). The` &&
            ` rendered tree is identical, every node keeps all five of its own fields (NodeID, HierarchyLevel, Description, ParentNodeID, DrillState) and the four RENDERED columns are unchanged - the fifth field,` &&
            ` ParentNodeID, is what the nesting replaces, so it is carried but not shown; what is lost is the annotation-driven assembly itself, and with it the countMode: 'Inline' parameter, which is an OData` &&
            ` request option. numberOfExpandedLevels: 1 IS kept as a binding parameter, so the first level opens like in the original. // NOTE: This sample has no controller at all (the view names none); the port`.
    text1 = text1 && ` is correspondingly static - model_init plus view_display on check_on_init, view_display alone on check_on_navigated (the house dispatcher shape), and no on_event branch at all. // NOTE:` &&
            ` localService/mockdata/Nodes.json is inlined in full - all sixteen nodes with the mock's own ids, levels, descriptions, parent ids and drill states; the root nodes' ParentNodeID is null in the JSON` &&
            ` and becomes the empty string in the flat ABAP row type. The mock server itself (localService/mockserver.js over localService/metadata.xml) has no counterpart in an abap2UI5 app, whose backend IS the` &&
            ` service. // NOTE: Unverified in a running system: whether the nested model plus arrayNames renders the same expandable tree the OData tree binding produces. **e2e-verified 2026-08-21** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_366.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.table`       control = `sap.ui.table.TreeTable`                name = `TreeTable.ODataAnnotationsTreeBinding`         class = `z2ui5_cl_smpc_app_366` path = `src/01/02/z2ui5_cl_smpc_app_366.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: sap.ui.unified.DateTypeRange.ariaHasPopup is @since 1.152.0 - newer than the 1.71 floor AND newer than the corpus's pinned 1.151.0 control metadata, but kept for the 1:1 port because it is` &&
            ` the entire subject of the sample. The app needs a UI5 release >= 1.152; below it the attribute is an unknown setting. // NOTE: onInit adds the three DateTypeRanges in JavaScript (day 5 Type01, days` &&
            ` 10-12 Type02, day 20 None - all three with ariaHasPopup Dialog, all on the CURRENT month). specialDates IS a bindable aggregation, so the port binds the same three rows and computes the current` &&
            ` month's prefix from sy-datum, which is how app 240 keeps its special dates on today's month. startDate and endDate are ABAP DATS strings read through Formatter.DateAbapDateToDateObject, which builds` &&
            ` a LOCAL-midnight Date - the same fix apps 017/220/240 carry, without which every marked day lands one day early west of Greenwich. // IMPROVISED: onDateSelect anchors the popover on the day CELL: it` &&
            ` builds a yyyymmdd string, queries the calendar's DOM for [data-sap-day="..."] and calls openBy on that node. A thin frontend has no DOM query, so the popover is anchored on the calendar itself`.
    text1 = text1 && ` (popover_display by_id). The popover is also declared in the view's dependents upstream and built once; here it is served per selection, which is what lets the backend fill its two texts. Everything` &&
            ` else survives: the selected day travels as epoch milliseconds, the backend finds the range that contains it among those carrying an ariaHasPopup, and composes the same two lines - the date label (a` &&
            ` range shows 'start – end') and 'Day type: <type>'. // NOTE: the date label the popover shows is formatted by sap.ui.core.format.DateFormat with style 'long' ('November 5, 2026'). A backend has no` &&
            ` access to the browser's locale, so the port composes the label from the same two dates in the ABAP model's own yyyymmdd form; the range case keeps the sample's en dash separator. // NOTE: not yet` &&
            ` verified in a running system: that selecting day 5, one of days 10-12 or day 20 opens the popover with the matching type, and that a plain day opens nothing. **e2e-verified 2026-08-25** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_611.mjs). // NOTE: The epoch milliseconds arriving from getTime( ) are converted through decfloat34, not i. ABAP's i tops out at 2,147,483,647 and a`.
    text1 = text1 && ` 2026 timestamp is ~1.8e12, so CONV i( ) raises CX_SY_CONVERSION_OVERFLOW and the whole round-trip dumps on a real stack. CI never saw it because the transpiled backend represents i as a JS number. //` &&
            ` NOTE: The sample's own stylesheet is injected through an added core:HTML style leaf (no counterpart in the original view). This sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified` &&
            ` samples SHARE one folder up, archived at ui5/sap.ui.unified/style.css - and the port carried the viewPadding class the original carries with no rule behind it, so it rendered flush against the page` &&
            ` edge where the sample renders padded. Injected are the rules that reach this view: the two viewPadding rules and .sap-phone .sapUiCal{position:relative}, which is written by CalendarRenderer on the` &&
            ` Calendar's own root rather than by the author. The sheet's remaining rules (.labelMarginLeft, .fullHeight - classes this view does not carry - and .sapUiCancel, which no OpenUI5 renderer writes) stay` &&
            ` out. Same treatment as apps 139/177/220/240/246/305/306/307/308; this port was ported after that sweep and was missed by it. Found by scripts/probes/orphan-style-class-probe.mjs.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarAriaHasPopup`                          class = `z2ui5_cl_smpc_app_611` path = `src/02/02/z2ui5_cl_smpc_app_611.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.DateTypeRange.ariaHasPopup is @since 1.152.0 - newer than the 1.71 floor AND newer than the corpus's pinned 1.151.0 control metadata, but kept for the 1:1 port because it is the entire` &&
                 ` subject of the sample. The app needs a UI5 release >= 1.152; below it the attribute is an unknown setting.` ) ).

    text1 = `NOTE: Calendar with primaryCalendarType Islamic / secondaryCalendarType Gregorian. The picked day IS transportable after all - measured 2026-08-05 with` &&
            ` ``scripts/probes/event-arg-expression-probe.mjs`` against real OpenUI5: an event arg is a full UI5 expression, and indexed access into an array-valued getter plus chained calls resolve there` &&
            ` (``$event.oSource.getSelectedDates()[0].getStartDate()``). The earlier rationale - 'select carries no date parameter and the selected DateRange is control state' - was wrong. The wire carries the` &&
            ` three LOCAL date parts as three expression args (year, month+1, day) rather than ``toISOString( )``, which would shift the day for any user east of Greenwich, and each arg is guarded by` &&
            ` ``getSelectedDates().length > 0`` so a re-click that clears the selection arrives as year 0. App 151 now writes the SELECTED day (Gregorian yyyy-MM-dd, matching its label) instead of the server date.` &&
            ` Residual: 'Focus Today' - the original only calls focusDate(today) and changes no text; the port keeps writing today's date and does not move the calendar focus, since focusDate takes a Date OBJECT` &&
            ` no wire can construct.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarCalendarType`                          class = `z2ui5_cl_smpc_app_151` path = `src/01/02/z2ui5_cl_smpc_app_151.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    text1 = `NOTE: handleCalendarSelect/_updateText read the picked day off the control (oCalendar.getSelectedDates()[0].getStartDate()) and format it yyyy-MM-dd. The port transports it as three UI5 expression` &&
            ` args (year, month+1, day - the LOCAL parts, not toISOString( ), which would shift the day east of Greenwich), each guarded by getSelectedDates().length > 0, and formats server-side; the Text gets a` &&
            ` two-way bound text attribute instead of setText (app 139 idiom, probe-verified). The controller's oLastSelectedJSDate becomes a server-side field, so the second click on the same day is recognised` &&
            ` exactly as in the original. // NOTE: handleCalendarSelect's oCalendar.removeSelectedDate(oSelectedDate) is reproduced since 2026-08-21: the second click on the same day sends a control_by_id` &&
            ` follow-up action calling removeAllSelectedDates on the calendar, so the day loses its highlight the way it does in the original. This carried an IMPROVISED deviation until then, on the claim that` &&
            ` removeSelectedDate takes a DateRange CONTROL instance no wire can address - true of that method, but the calendar is single-selection (the view sets neither singleSelection nor intervalSelection), so`.
    text1 = text1 && ` at most one DateRange exists and removeAllSelectedDates removes exactly it. abap2UI5 #2535 un-denied that method for this reason and the sibling port 307 has been calling it 1:1 since; 305's copy of` &&
            ` the deviation had simply outlived it. // IMPROVISED: onInit does byId('calendar').displayDate(UI5Date.getInstance(2021, 6, 1)) to open the calendar on July 2021, and the port opens on the current` &&
            ` month instead. Two of the three reasons this note used to give are wrong and are corrected here. displayDate( ) itself is indeed unreachable: it takes a JS Date and the frontend-action wire casts` &&
            ` only string/int/bool/controlId/controlIdOrNull/within/object/anchor kinds. But "Calendar exposes no bindable property for the displayed month" is FALSE - sap.ui.unified.Calendar.initialFocusedDate` &&
            ` (@since 1.111) is exactly that property - and "a JS Date abap2UI5 cannot hold" is false too: it is typed object like minDate/maxDate, and app 220 feeds those on this very control from an ABAP DATS` &&
            ` string through the curated Formatter.DateAbapDateToDateObject, which is new Date(...parseYmd(d)) - the same local-parts Date the original builds. MEASURED 2026-08-26 in the e2e browser (a probe`.
    text1 = text1 && ` swapped into this port's interaction module and restored): a Calendar constructed with initialFocusedDate opens on July 2021, and the month survives header navigation and a selection. What blocks the` &&
            ` port is the third fact, which no version or wire question would have surfaced: initialFocusedDate is not a one-shot like displayDate( ) - Calendar.onBeforeRendering re-applies it on EVERY render` &&
            ` (this._oFocusedDate = CalendarDate.fromLocalJSDate(this.getInitialFocusedDate(), ...)), and the probe confirmed a full invalidate( ) snaps the calendar back to July 2021. This app's second-click` &&
            ` branch calls removeAllSelectedDates on the calendar, which invalidates it, so binding the property would trade a wrong start month for a calendar that jumps back to July whenever a day is deselected.` &&
            ` Left unported deliberately, with the obstacle now named correctly. // POST-1.71: Calendar.showCurrentDateButton (@since 1.95) is kept 1:1 from the original view. Newer than UI5 1.71. // NOTE: not yet` &&
            ` run in a system: the CAL_SELECT expression-arg round-trip and the same-day second click clearing the label. **e2e-verified 2026-08-16** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_305.mjs). // NOTE: The sample's own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This` &&
            ` sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified samples SHARE one folder up - and it was never archived, so the viewPadding / labelMarginLeft classes the view carries had no` &&
            ` rule behind them and the port rendered flush against the page edge where the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap) and` &&
            ` only the rules this view actually uses are injected. Found by scripts/probes/orphan-style-class-probe.mjs. Corrected 2026-08-23: the sheet's .sap-phone .sapUiCal{position:relative} rule was dropped` &&
            ` as "not used by the view" and is injected now - sapUiCal is written by CalendarRenderer on the Calendar's own root, not by the author, so it does reach this view on a phone. Same sweep as apps` &&
            ` 139/177/220/240/305/306/307/308. The sheet's remaining rule targets .sapUiCancel, which no OpenUI5 renderer writes.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarDateDeselection`                       class = `z2ui5_cl_smpc_app_305` path = `src/02/02/z2ui5_cl_smpc_app_305.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = `Calendar.showCurrentDateButton (@since 1.95) is kept 1:1 from the original view. Newer than UI5 1.71.` ) ).

    text1 = `NOTE: The object-typed calendar date properties (Calendar.minDate, Calendar.maxDate and the disabledDates DateRange startDate/endDate) are fed from plain ISO strings in the model and converted at the` &&
            ` point of use with Formatter.DateCreateObject from the curated module (core:require='{Formatter: z2ui5/model/formatter}'). A plain string binding would crash view creation (Date must be a JS/UI5Date` &&
            ` object). The original's UI5Date.getInstance(year, month0, day) values are normalized to ISO 1:1 (month is 0-based: minDate 2000-01-01, maxDate 2050-12-31, disabled ranges 2016-01-04..2016-01-10 and` &&
            ` 2016-01-15). Corrected 2026-08-21: the dates are ABAP DATS strings read through Formatter.DateAbapDateToDateObject, not ISO date-only strings read through DateCreateObject. The latter is ``new` &&
            ` Date(s)``, and ECMA-262 parses the date-ONLY form as UTC midnight, while Calendar takes the LOCAL parts at the other end (CalendarDate.fromLocalJSDate) - so every fixed date in this sample shifted a` &&
            ` day earlier west of Greenwich: minDate became 1999-12-31 and the disabled range Jan 3-9 instead of Jan 4-10. DateAbapDateToDateObject builds the Date from the parsed parts, i.e. local midnight, and`.
    text1 = text1 && ` additionally answers null for a non-date - so the ternary guard the empty ``end`` needed in the view is gone with it. // NOTE: The second disabled range is a SINGLE day: the original's row omits end` &&
            ` entirely (undefined is falsy), and one bound DateRange template cannot omit an attribute per row. A plain formatter binding over the empty end field crashed the app - Formatter.DateCreateObject('')` &&
            ` is new Date('') = Invalid Date, DateRange.endDate accepts it (type object) and Month._checkDateEnabled then runs CalendarDate.fromLocalJSDate on every TRUTHY endDate, which throws. Fixed 2026-07-28` &&
            ` by guarding the conversion in the binding itself: endDate="{= ${END} ? Formatter.DateCreateObject(${END}) : null }" - written as a backtick literal so the braces survive to the attribute.` &&
            ` Probe-verified against the real OpenUI5 runtime (scripts/probes/calendar-empty-enddate-probe.mjs, headless Chromium, calendar focused on the affected month): the old binding throws and renders 0` &&
            ` days, the guarded one yields endDate null for the empty row and renders all 42. This also matters for the semantics, not only the crash: Month._checkDateEnabled disables a single day ONLY through the`.
    text1 = text1 && ` no-endDate branch (the range branch compares strictly exclusive, oTimeStamp > start && < end), so seeding end = start would disable nothing. Corrected 2026-08-21: the dates are ABAP DATS strings read` &&
            ` through Formatter.DateAbapDateToDateObject, not ISO date-only strings read through DateCreateObject. The latter is ``new Date(s)``, and ECMA-262 parses the date-ONLY form as UTC midnight, while` &&
            ` Calendar takes the LOCAL parts at the other end (CalendarDate.fromLocalJSDate) - so every fixed date in this sample shifted a day earlier west of Greenwich: minDate became 1999-12-31 and the disabled` &&
            ` range Jan 3-9 instead of Jan 4-10. DateAbapDateToDateObject builds the Date from the parsed parts, i.e. local midnight, and additionally answers null for a non-date - so the ternary guard the empty` &&
            ` ``end`` needed in the view is gone with it. // NOTE: handleCalendarSelect formats the picked day with DateFormat.getInstance({style:'long'}) and writes it into the 'selectedDate' Text. The picked day` &&
            ` IS transportable after all - measured 2026-08-05 with ``scripts/probes/event-arg-expression-probe.mjs`` against real OpenUI5: an event arg is a full UI5 expression, and indexed access into an`.
    text1 = text1 && ` array-valued getter plus chained calls resolve there (``$event.oSource.getSelectedDates()[0].getStartDate()``). The earlier rationale - 'select carries no date parameter and the selected DateRange is` &&
            ` control state' - was wrong. The wire carries the three LOCAL date parts as three expression args (year, month+1, day) rather than ``toISOString( )``, which would shift the day for any user east of` &&
            ` Greenwich, and each arg is guarded by ``getSelectedDates().length > 0`` so a re-click that clears the selection arrives as year 0. The port reproduces it: the select wire was added (the Text is bound` &&
            ` instead of carrying the literal 'No Date Selected'), and the English long form ('March 17, 2026') is composed in ABAP from the transported parts - the thin-frontend answer to a locale formatter, as` &&
            ` in app 024. // NOTE: The original Switch (state='true') toggles Calendar week numbers via an imperative change handler (setShowWeekNumbers). This is folded into a two-way binding shared by the Switch`.
    text1 = text1 && ` state and a Calendar showWeekNumbers property (both bound to show_week_numbers, seeded true), so the toggle runs on the client with no round-trip - the thin-frontend move. The Switch change attribute` &&
            ` is therefore dropped and a showWeekNumbers attribute (absent from the original view) is added. // POST-1.71: Formatter.DateAbapDateToDateObject is referenced via core:require, which needs UI5 >=` &&
            ` 1.74. sap.ui.unified.Calendar itself and its minDate/maxDate/disabledDates/showWeekNumbers members are all <= 1.71 (in scope). Corrected 2026-08-21: the dates are ABAP DATS strings read through` &&
            ` Formatter.DateAbapDateToDateObject, not ISO date-only strings read through DateCreateObject. The latter is ``new Date(s)``, and ECMA-262 parses the date-ONLY form as UTC midnight, while Calendar` &&
            ` takes the LOCAL parts at the other end (CalendarDate.fromLocalJSDate) - so every fixed date in this sample shifted a day earlier west of Greenwich: minDate became 1999-12-31 and the disabled range` &&
            ` Jan 3-9 instead of Jan 4-10. DateAbapDateToDateObject builds the Date from the parsed parts, i.e. local midnight, and additionally answers null for a non-date - so the ternary guard the empty ``end```.
    text1 = text1 && ` needed in the view is gone with it. // NOTE: The sample's own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This sample's` &&
            ` manifest lists ``../style.css`` - the sheet the sap.ui.unified samples SHARE one folder up - and it was never archived, so the viewPadding class the view carries had no rule behind them and the port` &&
            ` rendered flush against the page edge where the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap) and only the rules this view` &&
            ` actually uses are injected. Found by scripts/probes/orphan-style-class-probe.mjs. Corrected 2026-08-23: the sheet's .sap-phone .sapUiCal{position:relative} rule was dropped as "not used by the view"` &&
            ` and is injected now - sapUiCal is written by CalendarRenderer on the Calendar's own root, not by the author, so it does reach this view on a phone. Same sweep as apps 139/177/220/240/305/306/307/308.` &&
            ` The sheet's remaining rule targets .sapUiCancel, which no OpenUI5 renderer writes.`.
    text2 = `Formatter.DateAbapDateToDateObject is referenced via core:require, which needs UI5 >= 1.74. sap.ui.unified.Calendar itself and its minDate/maxDate/disabledDates/showWeekNumbers members are all <= 1.71` &&
            ` (in scope). Corrected 2026-08-21: the dates are ABAP DATS strings read through Formatter.DateAbapDateToDateObject, not ISO date-only strings read through DateCreateObject. The latter is ``new` &&
            ` Date(s)``, and ECMA-262 parses the date-ONLY form as UTC midnight, while Calendar takes the LOCAL parts at the other end (CalendarDate.fromLocalJSDate) - so every fixed date in this sample shifted a` &&
            ` day earlier west of Greenwich: minDate became 1999-12-31 and the disabled range Jan 3-9 instead of Jan 4-10. DateAbapDateToDateObject builds the Date from the parsed parts, i.e. local midnight, and` &&
            ` additionally answers null for a non-date - so the ternary guard the empty ``end`` needed in the view is gone with it.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarMinMax`                                class = `z2ui5_cl_smpc_app_220` path = `src/02/02/z2ui5_cl_smpc_app_220.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: handleCalendarSelect walks oCalendar.getSelectedDates() and rebuilds the JSON model with one yyyy-MM-dd string per selected day. The port transports the WHOLE aggregation in ONE event arg -` &&
            ` ``$event.oSource.getSelectedDates()`` - and the frontend marshals each DateRange into its public properties (Lib.normalizeEventArgs), which is the loop the client expression grammar does not have;` &&
            ` on_event unmarshals the array and takes the first ten characters of each startDate. **Rewritten 2026-08-28.** Until then this wire was a FIXED set of 31 index-guarded expression args (one per` &&
            ` selectable slot), each formatting getSelectedDates()[i].getStartDate() to yyyy-MM-dd from its LOCAL parts on the client and yielding an empty string past the end of the aggregation, and the` &&
            ` IMPROVISED deviation beside it declared the resulting 31-day cap. The array route was rejected for one measured reason: a DateRange projects to {ID, startDate, endDate}, and startDate - a Date-typed` &&
            ` property - was serialized by JSON.stringify through toISOString(), so LOCAL midnight of 2018-07-09 arrived as "2018-07-08T22:00:00.000Z" (measured 2026-08-23 with`.
    text1 = text1 && ` scripts/probes/control-valued-event-arg-probe.mjs, candidate dateRange-array, real OpenUI5, browser timezone Europe/Berlin; correct in UTC and west of it, which is why a CI run in UTC reported a` &&
            ` false all-clear). abap2UI5 closed that on 2026-08-24 (backlog item event-arg-date-utc-shift): projectValue serializes a Date as its LOCAL parts, an ISO local timestamp with no Z. The fix reached the` &&
            ` reproducible builds when A2UI5_PIN moved on 2026-08-28, which is what unblocked this rewrite - the cap is gone and no day can be dropped any more. // NOTE: e2e-verified 2026-08-10` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): a day is selected the keyboard way (the day cell carries no clickable layout box headless), the round-trip fills the List, and` &&
            ` "Remove All Selected Dates" clears BOTH sides - the List returns to its "No Dates Selected" noData text AND .sapUiCalItemSel drops to zero, which is the removeAllSelectedDates follow-up action doing` &&
            ` what no model write could: sap.ui.unified.Calendar writes selectedDates itself. The assertion has real discriminating power - before abap2UI5 #2535 the list emptied and the days stayed highlighted,`.
    text1 = text1 && ` which is exactly the state it now rejects. **Extended 2026-08-28** with the array route: the module now picks a SECOND day and asserts both reach the List, because one day proves nothing about the` &&
            ` marshalled aggregation - the 31-slot per-index wire carried the first day just as well. // NOTE: The sample's own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no` &&
            ` counterpart in the original view). This sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified samples SHARE one folder up - and it was never archived, so the viewPadding /` &&
            ` labelMarginLeft classes the view carries had no rule behind them and the port rendered flush against the page edge where the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css` &&
            ` (closing the AGENTS section 4 archive gap) and only the rules this view actually uses are injected. Found by scripts/probes/orphan-style-class-probe.mjs. Corrected 2026-08-23: the sheet's .sap-phone` &&
            ` .sapUiCal{position:relative} rule was dropped as "not used by the view" and is injected now - sapUiCal is written by CalendarRenderer on the Calendar's own root, not by the author, so it does reach`.
    text1 = text1 && ` this view on a phone. Same sweep as apps 139/177/220/240/305/306/307/308. The sheet's remaining rule targets .sapUiCancel, which no OpenUI5 renderer writes. // NOTE: The payload is read by hand - the` &&
            ` frontend marshals each control into an object of its ID plus all its public properties, and this port walks that object for the fields it models. abap2UI5 releases no JSON reader and the vendored` &&
            ` ajson copy is framework-internal (the linter's non-released-api rule reports it, correctly), so a targeted reader is the intended shape here; abap2UI5/samples Z2UI5_CL_SMP_APP_197 and _327 are the` &&
            ` same pattern. It reads what the FRAMEWORK wrote, which is flat, and does not resolve escapes - a payload composed from free user input would need that.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarMultipleDaySelection`                  class = `z2ui5_cl_smpc_app_307` path = `src/01/02/z2ui5_cl_smpc_app_307.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    text1 = `NOTE: handleCalendarSelect/_updateText read the picked day off the control (oCalendar.getSelectedDates()[0].getStartDate()) and format it yyyy-MM-dd. The port transports it as three UI5 expression` &&
            ` args (year, month+1, day - the LOCAL parts, not toISOString( ), which would shift the day east of Greenwich), each guarded by getSelectedDates().length > 0, and formats server-side; the Text gets a` &&
            ` two-way bound text attribute instead of setText (app 139 idiom, probe-verified). // NOTE: handleSelectToday's removeAllSelectedDates( ) + addSelectedDate( DateRange( today ) ) is reproduced since` &&
            ` 2026-08-21, so the calendar's own highlight really moves to today. This carried a residual until then - 'only the calendar's own highlight is not moved, addSelectedDate takes a DateRange CONTROL` &&
            ` which no wire can construct' - and the claim is true of the METHOD while missing the point: selectedDates is a bindable AGGREGATION of sap.ui.unified.DateRange, the same type and shape app 220 binds` &&
            ` for disabledDates, so the aggregation never needed the method. The port binds it to a one-row table and both handlers re-state that row: replacing it IS removeAll + add. Every click round-trips`.
    text1 = text1 && ` through CAL_SELECT, so the model stays the source of truth and the control's own click-selection is re-stated rather than fought. Same stale-rationale shape as apps 305 and 306, found the same day.` &&
            ` // NOTE: not yet run in a system: the CAL_SELECT expression-arg round-trip across the two displayed months and the SELECT_TODAY text update. **e2e-verified 2026-08-16** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_304.mjs). // POST-1.71: core:require (UI5 >= 1.74) on the view root loads z2ui5/model/formatter for the selectedDates startDate binding through` &&
            ` Formatter.DateAbapDateToDateObject - the app needs a UI5 release >= 1.74 to render it. Without it the XMLView parser rejects the binding outright ("formatter function ... not found"), which is what` &&
            ` the render gate caught. Same shape as app 017.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarMultipleMonth`                         class = `z2ui5_cl_smpc_app_304` path = `src/02/02/z2ui5_cl_smpc_app_304.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = `core:require (UI5 >= 1.74) on the view root loads z2ui5/model/formatter for the selectedDates startDate binding through Formatter.DateAbapDateToDateObject - the app needs a UI5 release >= 1.74 to` &&
                 ` render it. Without it the XMLView parser rejects the binding outright ("formatter function ... not found"), which is what the render gate caught. Same shape as app 017.` ) ).

    text1 = `NOTE: handleSelectToday's addSelectedDate( DateRange( today ) ) is reproduced since 2026-08-21, so the calendar's own highlight really moves to today. This carried a residual until then - 'the` &&
            ` calendar's own highlight is not moved, because addSelectedDate takes a DateRange CONTROL that no wire can construct' - and the claim is true of the METHOD while missing the point: selectedDates is a` &&
            ` bindable AGGREGATION of sap.ui.unified.DateRange, the same type and shape app 220 binds for disabledDates, so the aggregation never needed the method. The port binds it to a one-row table and both` &&
            ` handlers re-state that row. Every click round-trips through CAL_SELECT, so the model stays the source of truth and the control's own click-selection is re-stated rather than fought. Same` &&
            ` stale-rationale shape as apps 305 and 306. // NOTE: The sample's own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This sample's` &&
            ` manifest lists ``../style.css`` - the sheet the sap.ui.unified samples SHARE one folder up - and it was never archived, so the viewPadding / labelMarginLeft classes the view carries had no rule`.
    text1 = text1 && ` behind them and the port rendered flush against the page edge where the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap). Injected` &&
            ` are the rules that reach this view: the two viewPadding rules, labelMarginLeft, and - since 2026-08-23 - .sap-phone .sapUiCal{position:relative}, which the first pass dropped as "not used by the` &&
            ` view". It is: sapUiCal is written by CalendarRenderer on the Calendar's own root, not by the author, so on a phone the original positions its calendar relative and the port did not. The sheet's` &&
            ` remaining rule, .sap-phone .sapUiCal > .sapUiCancel, targets a class no sap.ui.unified or sap.m renderer writes and is dead upstream. Found by scripts/probes/orphan-style-class-probe.mjs. //` &&
            ` POST-1.71: core:require (UI5 >= 1.74) on the view root loads z2ui5/model/formatter for the selectedDates startDate binding through Formatter.DateAbapDateToDateObject - the app needs a UI5 release >=` &&
            ` 1.74 to render it. Without it the XMLView parser rejects the binding outright ("formatter function ... not found"), which is what the render gate caught. Same shape as app 017.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarSingleDaySelection`                    class = `z2ui5_cl_smpc_app_139` path = `src/02/02/z2ui5_cl_smpc_app_139.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = `core:require (UI5 >= 1.74) on the view root loads z2ui5/model/formatter for the selectedDates startDate binding through Formatter.DateAbapDateToDateObject - the app needs a UI5 release >= 1.74 to` &&
                 ` render it. Without it the XMLView parser rejects the binding outright ("formatter function ... not found"), which is what the render gate caught. Same shape as app 017.` ) ).

    text1 = `NOTE: handleCalendarSelect/_updateText read the picked interval off the control (getSelectedDates()[0].getStartDate()/getEndDate()) and format both ends yyyy-MM-dd. The port transports six UI5` &&
            ` expression args (the LOCAL year/month+1/day of each end, not toISOString( ), which would shift the day east of Greenwich), each guarded so a missing end arrives as 0 and renders the original's 'No` &&
            ` Date Selected'; the two Texts get two-way bound text attributes instead of setText (app 139 idiom, probe-verified). // NOTE: handleWeekNumberSelect reads the weekNumber and the weekDays DateRange` &&
            ` parameters; both travel as expression args (${$parameters>/weekNumber} and the local date parts of ${$parameters>/weekDays}.getStartDate()/getEndDate()), and the every-fifth-week refusal toast is` &&
            ` composed server-side, exactly as the original composes it. // NOTE: handleWeekNumberSelect refuses every fifth calendar week with BOTH a toast and oEvent.preventDefault( ), so the forbidden week is` &&
            ` not selected - and both halves are reproduced since 2026-08-21. This carried an IMPROVISED deviation until then, saying the refusal could only be a toast because abap2UI5 bakes`.
    text1 = text1 && ` s_ctrl-check_prevent_default in at RENDER time and cannot express a condition evaluated per event. That is true of the BOOLEAN form only: prevent_default_expr evaluates per firing, and the condition` &&
            ` here is a plain expression over an event parameter, so the wire carries ``${$parameters>/weekNumber} % 5 === 0`` directly. Worked precedents: app 247's columnResize and app 354's column filter. The` &&
            ` improvised-cluster harvest had this filed as the corpus' last remaining framework GAP (pr/conditional-prevent-default); it was closed upstream and the deviation had outlived it, the same way app` &&
            ` 305's removeAllSelectedDates entry had. // NOTE: not yet run in a system: the CAL_SELECT interval round-trip and the WEEK_SELECT branch (toast on every fifth week, labels otherwise) - still` &&
            ` unexercised, since the interaction module clicks day cells only. **e2e-verified 2026-08-16** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_306.mjs). // NOTE: The sample's own` &&
            ` stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified`.
    text1 = text1 && ` samples SHARE one folder up - and it was never archived, so the viewPadding / labelMarginLeft classes the view carries had no rule behind them and the port rendered flush against the page edge where` &&
            ` the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap) and only the rules this view actually uses are injected. Found by` &&
            ` scripts/probes/orphan-style-class-probe.mjs. Corrected 2026-08-23: the sheet's .sap-phone .sapUiCal{position:relative} rule was dropped as "not used by the view" and is injected now - sapUiCal is` &&
            ` written by CalendarRenderer on the Calendar's own root, not by the author, so it does reach this view on a phone. Same sweep as apps 139/177/220/240/305/306/307/308. The sheet's remaining rule` &&
            ` targets .sapUiCancel, which no OpenUI5 renderer writes.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarSingleIntervalSelection`               class = `z2ui5_cl_smpc_app_306` path = `src/01/02/z2ui5_cl_smpc_app_306.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        notes = text1 ) ).

    text1 = `NOTE: handleShowSpecialDays adds the special dates and the legend items imperatively (oCal.addSpecialDate(new DateTypeRange({...})), oLeg.addItem(new CalendarLegendItem({...}))) and destroys them` &&
            ` again when the ToggleButton is released. The port binds the two Calendars' specialDates and the two CalendarLegends' items aggregations to four ABAP tables and fills/clears them on the` &&
            ` SHOW_SPECIAL_DAYS round-trip, so the u:DateTypeRange and u:CalendarLegendItem templates plus the specialDates/items aggregation nodes are extra vs the archived view.xml (which declares none of them).` &&
            ` The ToggleButton also gains a two-way bound pressed attribute so its state survives the round-trip. Every date is day n of the CURRENT month, exactly like the original's setDate(n) on a fresh` &&
            ` reference date. // NOTE: DateTypeRange.startDate/endDate are typed 'object' and demand a real JS Date; the model carries ISO strings and Formatter.DateAbapDateToDateObject (the curated formatter` &&
            ` pack) converts them at the point of use via core:require. endDate is set on exactly one range, so its conversion needs no guard - an unguarded new Date('') is an Invalid Date, which is truthy and`.
    text1 = text1 && ` takes the whole view down. Rows without color/secondaryType/tooltip bind through omit_initial_paths so the UI5 default applies instead of an empty string. (Corrected 2026-08-23:` &&
            ` DateAbapDateToDateObject returns null for anything that is not an 8-digit ABAP date, so the plain { path, formatter } binding handles the one range that has no endDate - the ternary guard was removed` &&
            ` when the formatter changed and this sentence was not carried along.) // POST-1.71: DateTypeRange.color (@since 1.76) and DateTypeRange.secondaryType (@since 1.81) are kept 1:1 from the original` &&
            ` controller, and the Formatter.DateCreateObject core:require path needs UI5 >= 1.74. All newer than UI5 1.71. // NOTE: not yet run in a system: the ToggleButton round-trip filling and clearing both` &&
            ` calendars' special dates and both legends. **e2e-verified 2026-08-17** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_308.mjs). // POST-1.71: The sap.ui.unified.CalendarDayType ENUM` &&
            ` VALUE NonWorking is @since 1.121 and is kept 1:1 from the original controller. The attribute it rides on is base-version, so the gate sees nothing - the enum-value residual limit of AGENTS section 5.`.
    text1 = text1 && ` Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); the app needs a UI5 release >= 1.121 for those days to render as non-working. // NOTE: The sample's own stylesheet is` &&
            ` injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified samples SHARE one` &&
            ` folder up - and it was never archived, so the viewPadding / labelMarginLeft classes the view carries had no rule behind them and the port rendered flush against the page edge where the sample renders` &&
            ` padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap) and only the rules this view actually uses are injected. Found by` &&
            ` scripts/probes/orphan-style-class-probe.mjs. Corrected 2026-08-23: the sheet's .sap-phone .sapUiCal{position:relative} rule was dropped as "not used by the view" and is injected now - sapUiCal is` &&
            ` written by CalendarRenderer on the Calendar's own root, not by the author, so it does reach this view on a phone. Same sweep as apps 139/177/220/240/305/306/307/308. The sheet's remaining rule`.
    text1 = text1 && ` targets .sapUiCancel, which no OpenUI5 renderer writes. // NOTE: The special dates are ABAP DATS strings read through Formatter.DateAbapDateToDateObject, which builds the Date from the parsed parts -` &&
            ` local midnight, which is what sap.ui.unified reads back (CalendarDate.fromLocalJSDate). They were ISO date-only strings through Formatter.DateCreateObject until 2026-08-21, and that is ``new` &&
            ` Date(s)``, which ECMA-262 parses as UTC midnight: west of Greenwich every marked day landed one day early. Same defect and same fix as apps 220 and 017, found by sweeping the corpus for the pattern` &&
            ` after 220. Note the ports that pass a date-TIME without an offset (018, 109) are unaffected - that form is parsed as local.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Calendar`               name = `CalendarSpecialDaysLegend`                     class = `z2ui5_cl_smpc_app_308` path = `src/02/02/z2ui5_cl_smpc_app_308.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = `DateTypeRange.color (@since 1.76) and DateTypeRange.secondaryType (@since 1.81) are kept 1:1 from the original controller, and the Formatter.DateCreateObject core:require path needs UI5 >= 1.74. All` &&
                 ` newer than UI5 1.71. // The sap.ui.unified.CalendarDayType ENUM VALUE NonWorking is @since 1.121 and is kept 1:1 from the original controller. The attribute it rides on is base-version, so the gate` &&
                 ` sees nothing - the enum-value residual limit of AGENTS section 5. Declared by policy 2026-08-21 (scripts/probes/post171-blindspot-probe.mjs); the app needs a UI5 release >= 1.121 for those days to` &&
                 ` render as non-working.` ) ).

    text1 = `NOTE: The picked day IS transportable after all - measured 2026-08-05 with ``scripts/probes/event-arg-expression-probe.mjs`` against real OpenUI5: an event arg is a full UI5 expression, and indexed` &&
            ` access into an array-valued getter plus chained calls resolve there (``$event.oSource.getSelectedDates()[0].getStartDate()``). The earlier rationale - 'select carries no date parameter and the` &&
            ` selected DateRange is control state' - was wrong. The wire carries the three LOCAL date parts as three expression args (year, month+1, day) rather than ``toISOString( )``, which would shift the day` &&
            ` for any user east of Greenwich, and each arg is guarded by ``getSelectedDates().length > 0`` so a re-click that clears the selection arrives as year 0. App 177 now formats the SELECTED day as` &&
            ` yyyy-MM-dd, and the original's else-branch is reproduced exactly: re-clicking the same day removes it from the selection, the guard reports year 0 and the Text falls back to 'No Date Selected'.` &&
            ` Residual: 'Select Today' writes the text (server date = today) without moving the calendar's highlight. **Reworked 2026-08-23.** Two behaviours of handleCalendarSelect / handleSelectToday were`.
    text1 = text1 && ` missing and the sidecar claimed the opposite. (a) The original keeps the last picked day and REMOVES the DateRange when the same day is clicked again; single-selection mode never deselects by itself` &&
            ` (Month._selectDay only ever re-points or adds a range - the removal in the multi-selection branch is unreachable here), so the guard that was said to 'reproduce the else-branch exactly' could never` &&
            ` fire and the re-click just re-wrote the same date. The port now compares the picked day against the one it holds and clears both the text and the selection. (b) handleSelectToday is` &&
            ` removeAllSelectedDates + addSelectedDate(today); the port wrote the text only, so the calendar kept highlighting the previously clicked day while the label said today. Both are model writes now:` &&
            ` selectedDates is bound as a DateRange aggregation over an ABAP table, the shape app 139 uses for the same control family, with the startDate going through Formatter.DateAbapDateToDateObject (local` &&
            ` midnight, not UTC). // NOTE: The CAL_SELECT / SELECT_TODAY round-trips updating the bound selectedDate Text are unverified in a running system. **e2e-verified 2026-07-30** (transpiled-framework`.
    text1 = text1 && ` interaction, scripts/e2e-smoke.mjs): 'Select Today' round-trips and the bound #selectedDate Text shows the yyyy-MM-dd value; CAL_SELECT is NOT exercised by that module - it clicks the button only, so` &&
            ` the three $event.oSource.getSelectedDates() expression args, the risky part of this port, are unasserted (and since the 2026-08-23 rework the deselect branch and the bound aggregation are unasserted` &&
            ` too). // NOTE: The sample's own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This sample's manifest lists ``../style.css`` -` &&
            ` the sheet the sap.ui.unified samples SHARE one folder up - and it was never archived, so the viewPadding / labelMarginLeft classes the view carries had no rule behind them and the port rendered flush` &&
            ` against the page edge where the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap) Injected are the rules that reach this view: the` &&
            ` two viewPadding rules, labelMarginLeft, and - since 2026-08-23 - .sap-phone .sapUiCal{position:relative}, which the first pass dropped as not used by the view. It is: sapUiCal is written by`.
    text1 = text1 && ` CalendarRenderer on the control's own root, not by the author. The sheet's remaining rule targets .sapUiCancel, which no renderer writes. Found by scripts/probes/orphan-style-class-probe.mjs.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.CalendarDateInterval`   name = `CalendarDateIntervalBasic`                     class = `z2ui5_cl_smpc_app_177` path = `src/01/02/z2ui5_cl_smpc_app_177.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30.0`
        notes = text1 ) ).

    text1 = `POST-1.71: DateTypeRange.startDate is a date-object (type object) property, so it is bound through core:require="{Formatter: 'z2ui5/model/formatter'}" + formatter 'Formatter.DateAbapDateToDateObject'` &&
            ` (the curated module), which needs UI5 >= 1.74 - a plain ISO-string binding would crash view creation (CAPABILITIES 'Date-object properties'; same idiom as apps 108/109). The xmlns:core + core:require` &&
            ` were added for it; the original view had neither. // NOTE: The original builds the CalendarLegend items and the Calendar specialDates imperatively in onInit (addItem/addSpecialDate). Reproduced the` &&
            ` faithful abap2UI5 way as declared, model-bound aggregations: CalendarLegend.items = {/T_LEGEND} with a CalendarLegendItem template and Calendar.specialDates = {/T_SPECIAL} with a DateTypeRange` &&
            ` template (both templates are extra controls the original view.xml did not carry, as it added them in the controller) - CAPABILITIES marks controller-filled onInit aggregations as expressible this` &&
            ` way. The 10 legend items (Type01..Type10 / 'Placeholder 1..10') and 20 special dates (per type: day i and day i+12 of the current month) are computed server-side in model_init from sy-datum, so the`.
    text1 = text1 && ` special dates track the current month exactly as the original UI5Date.getInstance() logic did. // NOTE: The sample's own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf` &&
            ` (no counterpart in the original view). This sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified samples SHARE one folder up - and it was never archived, so the viewPadding /` &&
            ` labelMarginLeft classes the view carries had no rule behind them and the port rendered flush against the page edge where the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css` &&
            ` (closing the AGENTS section 4 archive gap) and only the rules this view actually uses are injected. Found by scripts/probes/orphan-style-class-probe.mjs. Corrected 2026-08-23: the sheet's .sap-phone` &&
            ` .sapUiCal{position:relative} rule was dropped as "not used by the view" and is injected now - sapUiCal is written by CalendarRenderer on the Calendar's own root, not by the author, so it does reach` &&
            ` this view on a phone. Same sweep as apps 139/177/220/240/305/306/307/308. The sheet's remaining rule targets .sapUiCancel, which no OpenUI5 renderer writes. // NOTE: The special dates are ABAP DATS`.
    text1 = text1 && ` strings read through Formatter.DateAbapDateToDateObject, which builds the Date from the parsed parts - local midnight, which is what sap.ui.unified reads back (CalendarDate.fromLocalJSDate). They` &&
            ` were ISO date-only strings through Formatter.DateCreateObject until 2026-08-21, and that is ``new Date(s)``, which ECMA-262 parses as UTC midnight: west of Greenwich every marked day landed one day` &&
            ` early. Same defect and same fix as apps 220 and 017, found by sweeping the corpus for the pattern after 220. Note the ports that pass a date-TIME without an offset (018, 109) are unaffected - that` &&
            ` form is parsed as local.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.CalendarLegend`         name = `CalendarLegendNavigation`                      class = `z2ui5_cl_smpc_app_240` path = `src/02/02/z2ui5_cl_smpc_app_240.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.22.0`
        is_post171 = abap_true
        notes = text1
        post171 = `DateTypeRange.startDate is a date-object (type object) property, so it is bound through core:require="{Formatter: 'z2ui5/model/formatter'}" + formatter 'Formatter.DateAbapDateToDateObject' (the` &&
                 ` curated module), which needs UI5 >= 1.74 - a plain ISO-string binding would crash view creation (CAPABILITIES 'Date-object properties'; same idiom as apps 108/109). The xmlns:core + core:require were` &&
                 ` added for it; the original view had neither.` ) ).

    text1 = `NOTE: openPopover builds a sap.m.ResponsivePopover with a nested sap.ui.unified.ColorPicker imperatively and calls openBy(oEvent.getSource()). The port expresses it as a core:FragmentDefinition shown` &&
            ` with client->popover_display( xml, by_id = $event.oSource.sId ) - the CAPABILITIES.md 1:1 path for a controller-built popover. Its ResponsivePopover, ColorPicker and the two Buttons are extra vs the` &&
            ` archived View.view.xml, which declares only the inline ColorPicker and the trigger Button. // NOTE: the controller's Device.system.phone branch (phone: keep the header and add Submit/Cancel buttons;` &&
            ` otherwise setShowHeader(false)) stays a branch: abap2UI5 serves the device> named model on every view slot, so ResponsivePopover.showHeader and the two buttons' visible bind {=` &&
            ` ${device>/system/phone}} instead of being resolved to one value at render time (apps 277/279 precedent). Both buttons close the popover client-side via follow_up_action( popover_close ), matching the` &&
            ` original's oRP.close(). // NOTE: not yet run in a system: the anchored popover round-trip and the phone-only Submit/Cancel buttons - NOT covered by the interaction module (scope corrected`.
    text1 = text1 && ` 2026-08-23). **e2e-verified 2026-08-16** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_309.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.ColorPicker`            name = `ColorPicker`                                   class = `z2ui5_cl_smpc_app_309` path = `src/01/02/z2ui5_cl_smpc_app_309.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.48.0`
        notes = text1 ) ).

    text1 = `NOTE: openPopover builds a sap.m.ResponsivePopover with a nested sap.ui.unified.ColorPicker imperatively and calls openBy(oEvent.getSource()). The port expresses it as a core:FragmentDefinition shown` &&
            ` with client->popover_display( xml, by_id = $event.oSource.sId ) - the CAPABILITIES.md 1:1 path for a controller-built popover. Its ResponsivePopover, ColorPicker and the two Buttons are extra vs the` &&
            ` archived View.view.xml, which declares only the inline ColorPicker and the trigger Button. The Large displayMode (@since 1.58) is kept 1:1 on both pickers. // NOTE: the controller's` &&
            ` Device.system.phone branch (phone: keep the header and add Submit/Cancel buttons; otherwise setShowHeader(false)) stays a branch: abap2UI5 serves the device> named model on every view slot, so` &&
            ` ResponsivePopover.showHeader and the two buttons' visible bind {= ${device>/system/phone}} instead of being resolved to one value at render time (apps 277/279 precedent). Both buttons close the` &&
            ` popover client-side via follow_up_action( popover_close ), matching the original's oRP.close(). // NOTE: not yet run in a system: the anchored popover round-trip and the phone-only Submit/Cancel`.
    text1 = text1 && ` buttons - NOT covered by the interaction module (scope corrected 2026-08-23). **e2e-verified 2026-08-16** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_310.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.ColorPicker`            name = `ColorPickerLarge`                              class = `z2ui5_cl_smpc_app_310` path = `src/01/02/z2ui5_cl_smpc_app_310.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.48.0`
        notes = text1 ) ).

    text1 = `NOTE: Breadth-probe (cross-library capability test). Inline sap.ui.unified ColorPicker (HSL / Simplified); the view itself is a 1:1 rebuild. Since 2026-07-30 the button's controller-built` &&
            ` ResponsivePopover-with-ColorPicker is reproduced 1:1 via popover_display( xml = fragment by_id = $event.oSource.sId ): a core:FragmentDefinition with ResponsivePopover (title 'Color Picker'), a` &&
            ` second u:ColorPicker (HSL/Simplified) in its content and Submit/Cancel Button pair - all extra controls vs the original view.xml, which declared none of them (controller-built). The original's` &&
            ` Device.system.phone branch (phone: begin/end Submit/Cancel buttons that close; desktop: setShowHeader(false)) is expressed declaratively: showHeader='{device>/system/phone}' and` &&
            ` visible='{device>/system/phone}' on both buttons, so the buttons themselves appear only on a phone. Corrected 2026-08-23: this used to claim phone AND desktop render as the original, and desktop does` &&
            ` not. Declaring the two aggregations at all runs ResponsivePopover._setButton, which on desktop - where the inner control is a Popover - CREATES a footer Toolbar with a ToolbarSpacer and attaches it,`.
    text1 = text1 && ` before any binding is evaluated, and tears it down only when the button object is falsy rather than invisible. So the port's desktop popover shows an empty footer bar under the ColorPicker where the` &&
            ` original, which never calls setBeginButton/setEndButton on desktop, has no footer at all. The headless e2e runs on desktop and asserts only that the popover opened, so it cannot see this; the buttons` &&
            ` close via the popover_close frontend action (oRP.close()). // NOTE: the anchored ResponsivePopover open (popover_display by_id) with the embedded ColorPicker and the device-bound header/button` &&
            ` variants are unverified in a running system. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the button press opens the ResponsivePopover anchored with the` &&
            ` embedded ColorPicker attached (the picker's inner sliders render zero-size headless, so only the container is asserted). **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_112.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.ColorPicker`            name = `ColorPickerSimplified`                         class = `z2ui5_cl_smpc_app_112` path = `src/01/02/z2ui5_cl_smpc_app_112.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.48.0`
        notes = text1 ) ).

    text1 = `NOTE: The four u:ColorPickerPopover controls are EXTRA in the port (original 0 vs port 4): the controller creates them lazily in JS (new ColorPickerPopover('oColorPickerPopover', { colorString:` &&
            ` 'blue', mode: 'HSL', change: ... }) and the Large/Simplified/liveChange variants) and opens each with openBy(oEvent.getSource()). abap2UI5 declares them up front in the Table's dependents aggregation` &&
            ` with the same ids, colorStrings (blue/green/pink/orange), displayModes and mode='HSL', and each Input's valueHelpRequest opens its own popover roundtrip-free via follow_up_action( control_by_id,` &&
            ` <id>/openBy/$event.oSource.sId ) - the anchored-open idiom of apps 016/060. Same controls, same configuration, declared instead of constructed; the lazy instantiation and the onExit destroy() are` &&
            ` gone with the JS. // IMPROVISED: The Inputs' change handler is dropped: handleInputChange validates the typed text with sap.ui.core.CSSColor.isValid and sets the Input's valueState to Error for an` &&
            ` invalid color. Colour-string validation is frontend logic with no ABAP equivalent that runs on focus-out or Enter, and abap2UI5's thin-frontend rule forbids reimplementing it in a formatter or`.
    text1 = text1 && ` expression binding; a round-trip per change would be a different behaviour. The Input.value stays two-way bound so the typed text reaches the backend, but an invalid entry is not flagged.` &&
            ` handleChange's own valueState reset (ValueState.None after picking a colour) is therefore not needed either. (Corrected 2026-08-23: handleInputChange is wired to Input.change, and sap.m.InputBase` &&
            ` fires change only on focus-out or Enter - liveChange is the per-keystroke event. A change round-trip would have had the original's exact cadence, so the drop is a choice, not a cadence problem.) //` &&
            ` NOTE: Unverified in a running system: (b) that the popover's change round-trip writes the chosen colorString into the right Input and toasts 'Chosen color string: <color>' like handleChange - the` &&
            ` original tracks the source Input in this.inputId, while here every popover has its own event so the target is known statically; (c) that the liveChange round-trip keeps the Text under the last Input` &&
            ` in sync while the user drags in the picker. Leg (a) is closed: **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): clicking the first row's value-help`.
    text1 = text1 && ` icon opens THAT Input's own ColorPickerPopover anchored at the input (control_by_id openBy + domRef), with the ColorPicker inside it. Picking a colour needs the picker's own drag/slider controls,` &&
            ` which carry zero-size boxes headless, so (b)/(c) stay a human check. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_268.mjs). // POST-1.71: Found on` &&
            ` 2026-08-21, when view-gates stopped letting ANY deviation excuse a version finding: a NOTE that merely contained the member's name had been satisfying the gate, and only POST_171 / DROPPED_171 carry` &&
            ` that claim. sap.ui.unified.ColorPickerPopover.liveChange is @since 1.85 and is kept 1:1 from the original's lazily-created popover. The excusing NOTE was the unverified-in-a-running-system entry,` &&
            ` which says 'the liveChange round-trip keeps the Text ...' - a sentence about what the wire DOES. The class moves from src/01/02 to src/02/02 with this; the app needs UI5 >= 1.85.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.ColorPickerPopover`     name = `ColorPickerPopover`                            class = `z2ui5_cl_smpc_app_268` path = `src/02/02/z2ui5_cl_smpc_app_268.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.48.0`
        is_post171 = abap_true
        notes = text1
        post171 = `Found on 2026-08-21, when view-gates stopped letting ANY deviation excuse a version finding: a NOTE that merely contained the member's name had been satisfying the gate, and only POST_171 /` &&
                 ` DROPPED_171 carry that claim. sap.ui.unified.ColorPickerPopover.liveChange is @since 1.85 and is kept 1:1 from the original's lazily-created popover. The excusing NOTE was the` &&
                 ` unverified-in-a-running-system entry, which says 'the liveChange round-trip keeps the Text ...' - a sentence about what the wire DOES. The class moves from src/01/02 to src/02/02 with this; the app` &&
                 ` needs UI5 >= 1.85.` ) ).

    text1 = `NOTE: The controller's Formatting.setCustomCurrencies({BGN4:{digits:4}, WWWW:{digits:5}}) is carried as a follow_up_action( cs_event-control_global, FORMATTING/setCustomCurrencies ) with the same JSON` &&
            ` payload. **Corrected 2026-08-23 - the digit counts are NOT reproduced, and were never going to be.** Two separate reasons, both measured. (1) The framework asked for the module as` &&
            ` sap/ui/core/Formatting, which no UI5 release ships (the module is sap/base/i18n/Formatting), so sap.ui.require returned undefined and the action logged 'FORMATTING.setCustomCurrencies not available'` &&
            ` and did nothing. Fixed upstream on that date - this port is where it was found. (2) Even with the right module the call cannot reach THIS control: the original registers the currencies in onInit,` &&
            ` before the view exists, while a client action necessarily runs after the response is rendered, and sap.ui.unified.Currency caches its NumberFormat in init( ) (Currency.js: this._oFormat =` &&
            ` NumberFormat.getCurrencyInstance) and implements no _handleLocalizationChange - so the cached formatter keeps the CLDR digits. A BOUND sap.ui.model.type.Currency does implement that hook and does`.
    text1 = text1 && ` re-format, which is what the capability is good for. So list five renders BGN4 and WWWW with the default 2 decimals where the original shows 4 and 5. The residual in the deviation beside this one was` &&
            ` right all along; this text was not. // NOTE: The various/nonDecimal arrays back the u:Currency value property (float) as ABAP packed fields, matching the sample's JSON numbers; the bigNumber array` &&
            ` (JSON strings) and the customCurrency array (JSON numbers bound to the string property stringValue, so UI5 coerces them) are ABAP string fields keeping the exact literals (123.45676 has 5 decimals).` &&
            ` listSix binds the numeric variousNumberDataModel/price to stringValue (number coerced to string, as in the original). **e2e-verified 2026-08-01** (scripts/e2e-smoke.mjs interaction, transpiled` &&
            ` backend + real browser): the serialized model reaches the controls: the lists render, the u:Currency controls come up and their currencies (EUR, JPY) are on screen. Residual: the exact digit` &&
            ` formatting per list (maxPrecision, the custom BGN4/WWWW digits), which is the browser's locale formatting rather than the port's data. // POST-1.71: FORMATTING.setCustomCurrencies is a control_global`.
    text1 = text1 && ` frontend action since UI5 1.120 - newer than the 1.71 floor but kept for the 1:1 port: registering BGN4 and WWWW is what the sample is about, and there is no pre-1.120 spelling of it. Below 1.120 the` &&
            ` lazy require returns undefined and the dispatch hits its "not available" guard, so the two custom currencies are simply never registered and their amounts format with the stock digits - the app needs` &&
            ` a UI5 release >= 1.120.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Currency`               name = `Currency`                                      class = `z2ui5_cl_smpc_app_196` path = `src/02/02/z2ui5_cl_smpc_app_196.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.21.1`
        is_post171 = abap_true
        notes = text1
        post171 = `FORMATTING.setCustomCurrencies is a control_global frontend action since UI5 1.120 - newer than the 1.71 floor but kept for the 1:1 port: registering BGN4 and WWWW is what the sample is about, and` &&
                 ` there is no pre-1.120 spelling of it. Below 1.120 the lazy require returns undefined and the dispatch hits its "not available" guard, so the two custom currencies are simply never registered and` &&
                 ` their amounts format with the stock digits - the app needs a UI5 release >= 1.120.` ) ).

    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Currency`               name = `CurrencyInTable`                               class = `z2ui5_cl_smpc_app_171` path = `src/01/02/z2ui5_cl_smpc_app_171.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted, reviewed). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.21.1`
        checked = `CHECKED (2026-07-27): verified in a running system 2026-07-27 - nested transactionAmount/size + currency bindings resolve at runtime (u:Currency renders values) **e2e-verified 2026-09-12**` &&
                 ` (interaction module meta/interactions/z2ui5_cl_smpc_app_171.mjs: the nested transactionAmount/size + currency paths resolve into a u:Currency per row (560.67 EUR, USD) and the Float-typed exchange` &&
                 ` rate renders 0.85654)`
        notes = `NOTE: live-verified 2026-07-27: u:Currency binds a nested object path (value={transactionAmount/size}, currency={transactionAmount/currency}). Ported as a nested ABAP structure TRANSACTIONAMOUNT` &&
                 ` (fields SIZE/CURRENCY), bound {TRANSACTIONAMOUNT/SIZE} / {TRANSACTIONAMOUNT/CURRENCY}. Corrected 2026-08-23: this used to end "CAPABILITIES.md documents nested TABLES/trees but not a nested single` &&
                 ` (non-array) structure within a row, so the nested-object serialization + relative sub-path binding needs a live render check". Both halves were stale. CAPABILITIES.md carries a dedicated row for the` &&
                 ` nested single (non-array) structure bound {OBJ/FIELD}, marked available, and it cites THIS port as its evidence; and the live check it asks for is the one this same deviation opens with and` &&
                 ` checked.note records.` ) ).

    text1 = `NOTE: The full upload cycle is backend/endpoint dependent, so it is reduced to client-side MessageToasts: 'Upload File' press shows an upload-started toast (original handleUploadPress ran` &&
            ` checkFileReadable().then(upload)), and FileUploader.uploadComplete shows the hardcoded success message the original built - which until 2026-08-23 could never fire: nothing called upload( ) and` &&
            ` uploadOnChange defaults to false, so the event's only firing paths were unreachable, and the press raised an invented 'Uploading file to the local server ...' toast the original does not have. The` &&
            ` press now calls upload( ) through control_by_id, which is what handleUploadPress does and what makes the uploadComplete toast live. Its checkFileReadable( ) guard and the error toast it feeds stay` &&
            ` dropped (handleUploadComplete parsed a hardcoded 'Status: 200' response and toasted '(Upload Success)'). The uploadUrl='upload/' is kept 1:1. **e2e-verified 2026-08-04** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_126.mjs). // NOTE: The sample's own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This`.
    text1 = text1 && ` sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified samples SHARE one folder up - and it was never archived, so the viewPadding class the view carries had no rule behind it` &&
            ` (corrected 2026-08-23 - labelMarginLeft was named here too and this sample's view does not carry it; it is a rule of the shared sap.ui.unified sheet that other samples use) and the port rendered` &&
            ` flush against the page edge where the sample renders padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap) and only the rules this view actually uses` &&
            ` are injected. Found by scripts/probes/orphan-style-class-probe.mjs.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.FileUploader`           name = `FileUploaderBasic`                             class = `z2ui5_cl_smpc_app_126` path = `src/01/02/z2ui5_cl_smpc_app_126.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `NOTE: handleUploadPress reproduced since 2026-07-30: the Upload File press fires a backend UPLOAD round-trip; FileUploader.value is two-way bound (an added value attribute, declared) so the backend` &&
            ` runs the original's empty check - no file chosen toasts 'Choose a file first' (1:1), else follow_up_action control_by_id fileUploader upload + clear reproduce oFileUploader.upload() then clear()` &&
            ` (both public non-denied methods via the generalized allowlist). The earlier tooltip-derived 'Uploading file to the local server ...' toast is gone. NOT reproduced: checkFileReadable() - a client-side` &&
            ` File API probe whose rejection branch ('The file cannot be read. It may have changed.') has no declarative equivalent; the port uploads without the readability pre-check. // NOTE:` &&
            ` handleUploadComplete: the original parses a hardcoded 'File upload complete. Status: 200' response and toasts '<response> (Upload Success)'. Reproduced as a client-composed MessageToast with the` &&
            ` resolved literal 'File upload complete. Status: 200 (Upload Success)' (endpoint-independent, same as app 126). // NOTE: change and typeMissmatch handlers are reproduced 1:1 as roundtrip-free`.
    text1 = text1 && ` client-composed MessageToasts (control_global MESSAGE_TOAST.show): change -> "Press 'Upload File' to upload file '{0}'" filled by ${$parameters>/newValue}; typeMissmatch -> 'The file type *.{0} is` &&
            ` not supported. Choose one of the following types: txt, jpg' filled by ${$parameters>/fileType}. The allowed-types list is folded to the literal 'txt, jpg' (the static fileType property the original` &&
            ` builds it from); the original's aFileTypes.map(...) result is unused there too, so the message text matches. // NOTE: The client-composed toasts (change/typeMissmatch/uploadComplete/press via` &&
            ` control_global MESSAGE_TOAST) and the ${$parameters>/newValue} / ${$parameters>/fileType} event-arg resolution are view-create verified (render-smoke) but not yet live-verified in a running system.` &&
            ` **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): pressing 'Upload File' with no chosen file round-trips and toasts 'Choose a file first'; the` &&
            ` change/typeMissmatch toasts need a real file dialog and remain unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_246.mjs). // NOTE: The sample's`.
    text1 = text1 && ` own stylesheet is injected since 2026-08-21 through an added core:HTML style leaf (no counterpart in the original view). This sample's manifest lists ``../style.css`` - the sheet the sap.ui.unified` &&
            ` samples SHARE one folder up - and it was never archived, so the viewPadding class the view carries had no rule behind them and the port rendered flush against the page edge where the sample renders` &&
            ` padded. The sheet now sits at ui5/sap.ui.unified/style.css (closing the AGENTS section 4 archive gap) and only the rules this view actually uses are injected. Found by` &&
            ` scripts/probes/orphan-style-class-probe.mjs. // NOTE: The upload branch clears the FileUploader through control_by_id clear( ) only, and deliberately does NOT clear the bound value field. It did` &&
            ` until 2026-08-23, and that was a wire that could not work: a changed model is pushed to the client BEFORE the queued follow-up actions run (Slots.action('updateModel') sits ahead of` &&
            ` _runPendingCustomJs), so FileUploader.setValue('') reset the form and the upload( ) that followed posted an empty one. clear( ) is also all the original does after upload( ).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.FileUploader`           name = `FileUploaderComplex`                           class = `z2ui5_cl_smpc_app_246` path = `src/01/02/z2ui5_cl_smpc_app_246.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it. // NOTE: anchored open works since the framework` &&
            ` fallback of 2026-07-27 (pr/unified-menu-open-anchored, implemented): the openBy dispatch detects that sap.ui.unified.Menu has no openBy and calls open(false, anchor, 'begin top', 'begin bottom',` &&
            ` anchor) with the button as opener and dock reference - the previously declared no-op wire is now functional, unchanged in the port. Still dropped either way: the keyboard flag (controller` &&
            ` attachBrowserEvent tab/keyup tracking) and the explicit Popup.Dock.BeginTop/BeginBottom constants (default docking is visually equivalent). // NOTE: the fragment Menu (added in the controller via` &&
            ` getView().addDependent(this._menu)) is declared 1:1 inside the Button's ``dependents`` aggregation (the addDependent equivalent, same as app 060) - structurally identical, no loss; structural-diff` &&
            ` ignores the aggregation name. // NOTE: the selected item text/value is read with ${$parameters>/item}.getText() (and .getValue() for the MenuTextFieldItem), a method call on the resolved MenuItemBase`.
    text1 = text1 && ` control, NOT ${$parameters>/item/text}: the $parameters model exposes 'item' as the control object and UI5 keeps properties in the control's internal store, so the path .../item/text reads an` &&
            ` undefined field and the toast arrives empty (same finding as app 060). // NOTE: unverified in a running system: (a) the button press opens the Menu via the 2026-07-27 anchored-open fallback - verify` &&
            ` the open in a running system; (b) each MenuItem select toasts "'<text>' pressed" via ${$parameters>/item}.getText() and the MenuTextFieldItem select toasts "'<value>' entered" via` &&
            ` ${$parameters>/item}.getValue(). **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): the button press opens the sap.ui.unified.Menu anchored via the openBy->open()` &&
            ` fallback ('My 1st Item' visible); the item-select toasts remain unexercised. **e2e-verified 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_227.mjs). (Scope corrected` &&
            ` 2026-08-23: the module clicks the button and asserts 'My 1st Item' is visible; it never selects an item, so the item-select toasts named just above stay unexercised.)`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Menu`                   name = `MenuItemEventing`                              class = `z2ui5_cl_smpc_app_227` path = `src/02/02/z2ui5_cl_smpc_app_227.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.21.0`
        is_post171 = abap_true
        notes = text1
        post171 = `Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it.` ) ).

    text1 = `POST-1.71: Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it. // NOTE: anchored open works since the framework` &&
            ` fallback of 2026-07-27 (pr/unified-menu-open-anchored, implemented): the openBy dispatch detects that sap.ui.unified.Menu has no openBy and calls open(false, anchor, 'begin top', 'begin bottom',` &&
            ` anchor) with the button as opener and dock reference - the previously declared no-op wire is now functional, unchanged in the port. Still dropped either way: the keyboard flag (controller` &&
            ` attachBrowserEvent tab/keyup tracking) and the explicit Popup.Dock.BeginTop/BeginBottom constants (default docking is visually equivalent). // NOTE: the sample's handleMenuItemPress branches on the` &&
            ` runtime item: a parent that only opens its submenu is skipped, a sap.ui.unified.MenuTextFieldItem reports getValue() + ' entered', every other item getText() + ' pressed'. **Reproduced 1:1 since` &&
            ` 2026-08-05**: measured with scripts/probes/event-arg-expression-probe.mjs, a class-name ternary resolves inside an event arg, so the whole branch travels as ONE client expression and the`.
    text1 = text1 && ` client-composed toast (a {0} template) prints exactly what the original composes - with ONE change made 2026-08-23. The earlier claim that the class cannot be inspected was about the BACKEND; the` &&
            ` expression runs on the client, where the sample's own code runs too. Distinct from app 060's parent-chain breadcrumb, which stays impossible: the expression grammar has no loop. The original does not` &&
            ` compose an empty message for a submenu parent - it RETURNS before MessageToast.show, and Menu.selectItem fires itemSelect for a submenu parent too, so the port's client-composed '' produced a real,` &&
            ` empty toast (neither formatTemplate nor showToast nor MessageToast.show has an early return for it). The skip is a decision, so it travels: the wire carries a SUB/ITEM flag beside the composed` &&
            ` message and on_event toasts only for ITEM. The message itself is still composed on the client. // NOTE: the fragment Menu (added in the controller via getView().addDependent(this._menu)) is declared` &&
            ` 1:1 inside the Button's ``dependents`` aggregation (the addDependent equivalent, same as app 060) - structurally identical, no loss; structural-diff ignores the aggregation name. // NOTE: the`.
    text1 = text1 && ` selected item text is read with ${$parameters>/item}.getText() (a method call on the resolved MenuItemBase control), NOT ${$parameters>/item/text}: the $parameters model exposes 'item' as the control` &&
            ` object and UI5 keeps properties in the control's internal store, so the path .../item/text reads an undefined field and the toast arrives empty (same finding as app 060). // NOTE: unverified in a` &&
            ` running system: (a) the button press opens the Menu via the 2026-07-27 anchored-open fallback - verify the open in a running system; (b) selecting a menu item toasts "'<text>' pressed" via the` &&
            ` Menu-level itemSelect + ${$parameters>/item}.getText(). **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the button press opens the` &&
            ` sap.ui.unified.Menu through the 2026-07-27 openBy fallback (open(false, anchor, ...) for a control without its own openBy) and selecting an item raises the client-composed "'My 1st Item' pressed"` &&
            ` toast.`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Menu`                   name = `MenuMenuEventing`                              class = `z2ui5_cl_smpc_app_228` path = `src/02/02/z2ui5_cl_smpc_app_228.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.21.0`
        is_post171 = abap_true
        notes = text1
        post171 = `Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.84 to render it.` ) ).

    text1 = `NOTE: handlePressOpenMenu lazily loads Menu.fragment.xml and calls menu.open(bKeyboard, oButton, Popup.Dock.BeginTop, Popup.Dock.BeginBottom, oButton). The port inlines the fragment into the Button's` &&
            ` dependents aggregation (the declarative addDependent equivalent, app 060/227) and opens it client-side through control_by_id/openBy with $event.oSource.sId as the anchor: the openBy dispatch detects` &&
            ` that sap.ui.unified.Menu has no own openBy and falls back to open(false, anchor, 'begin top', 'begin bottom', anchor) - the same call the original makes. Dropped either way: the keyboard flag the` &&
            ` controller collects from a 'tab keyup' browser event (open's first argument), and the explicit Dock constants, which the fallback hardcodes to the same values. // NOTE: namespace representation: the` &&
            ` archived Menu.fragment.xml declares xmlns="sap.ui.unified", so its Menu, MenuItem and MenuItemGroup controls are unprefixed there, while Page.view.xml declares xmlns="sap.m". One port view can carry`.
    text1 = text1 && ` only one default namespace, so it keeps sap.m (matching the view) and writes the two Menu controls, the fourteen MenuItem controls and the three MenuItemGroup controls with the u: prefix. Same tree,` &&
            ` different prefix - no functional difference. // POST-1.71: sap.ui.unified.MenuItemGroup (control @since 1.127) with itemSelectionMode SingleSelect/MultiSelect and MenuItem.selected (@since 1.127) are` &&
            ` the whole point of this sample and are kept 1:1; sap.m.Button.ariaHasPopup (@since 1.84) is kept 1:1 on the trigger button. All newer than UI5 1.71. // NOTE: not yet run in a system: the anchored` &&
            ` open of the unified Menu and the selectable item groups (single/multi select state) - NOT covered by the interaction module (scope corrected 2026-08-23). **e2e-verified 2026-08-16** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_311.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.ui.unified`     control = `sap.ui.unified.Menu`                   name = `MenuSelectable`                                class = `z2ui5_cl_smpc_app_311` path = `src/02/02/z2ui5_cl_smpc_app_311.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.21.0`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.ui.unified.MenuItemGroup (control @since 1.127) with itemSelectionMode SingleSelect/MultiSelect and MenuItem.selected (@since 1.127) are the whole point of this sample and are kept 1:1;` &&
                 ` sap.m.Button.ariaHasPopup (@since 1.84) is kept 1:1 on the trigger button. All newer than UI5 1.71.` ) ).

    text1 = `IMPROVISED: Block->content inlining, two levels deep (app 161/178/261 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks aggregation holds the custom` &&
            ` BlockBase blockinblock:Block (id='block', mode='Collapsed'), whose view content is an html:div (height:24em, background #A9EAFF) that itself instantiates a second BlockBase sample:InnerBlock` &&
            ` (id='vendorName') - the block-in-block point of the sample - whose view content is another html:div (height:16em, blue, margin 1em). Both block views are pure HTML with no controls, so the whole` &&
            ` two-level structure is rendered as ONE core:HTML leaf carrying the nested divs verbatim in its content attribute (the 161/178 form; a core:HTML cannot wrap another control, but here the inner level` &&
            ` is markup too, so nothing is flattened away). Absent as a result: the two BlockBase controls blockinblock:Block and sample:InnerBlock with their ids and the block-mode config mode='Collapsed' (both` &&
            ` block JS classes declare no additional views, so the mode selects nothing and Collapsed has no visual effect), plus the block views' own mvc:View roots (they carry only xmlns declarations - no`.
    text1 = text1 && ` width/height to lose). Present in their place: one core:HTML. // IMPROVISED: The two uxap ModelMapping config controls are dropped: the main view's ModelMapping (externalModelName='jsonModel'` &&
            ` externalPath='/Vendor' internalModelName='FirstBlock') inside blockinblock:Block, and Block.view.xml's uxap:ModelMapping (externalModelName='FirstBlock' externalPath='VendorName'` &&
            ` internalModelName='NameDisplay') inside sample:InnerBlock. abap2UI5 serves one default model and the inlining removes the BlockBase internal-model machinery the mappings configure (app 230 precedent` &&
            ` - dropping a declared config control is a loss even when it renders identically). In this sample the mapped chain carries no data at all in the original either: no jsonModel is ever instantiated (the` &&
            ` manifest declares no models and there is no controller), and InnerBlock's view binds nothing against NameDisplay - the mappings are pure demonstration config, so nothing renders differently and there` &&
            ` is no data to fold statically; the port consequently has no model_init. // NOTE: Static sample: no model, no controller, no event handlers - the port is init-only with neither model_init nor`.
    text1 = text1 && ` on_event, which is behaviour-identical. All members used (ObjectPageLayout.id/upperCaseAnchorBar, ObjectPageHeader.objectTitle, ObjectPageSection.titleUppercase/title,` &&
            ` ObjectPageSubSection.title/titleUppercase, core:HTML content) are <= 1.71 per ui5/properties.json - no POST_171 needed, the port stays in src/01/03. // NOTE: Rendering not verified in a running` &&
            ` system: the core:HTML nested-div block content inside the ObjectPageSubSection blocks aggregation (the light-blue 24em outer div containing the blue 16em inner div with 1em margin) is render-gate` &&
            ` green but the visual result was not checked live. There is no round-trip to verify - the app is static. **e2e-verified 2026-08-21** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_409.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.BlockBase`                    name = `BlockBaseBlockInBlock`                         class = `z2ui5_cl_smpc_app_409` path = `src/01/03/z2ui5_cl_smpc_app_409.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block-content inlining with a folded block event (app 187/239/261 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks aggregation holds the` &&
            ` sample's own BlockBase control sample:EventingBlock (xmlns:sample='sap.uxap.sample.BlockBaseEventing.controller.blocks', id='block', dummy='onDummy'), a lazy-loading wrapper around` &&
            ` EventingBlock.view.xml whose JS declares a custom 'dummy' event. The block controller's onInnerDummy delegates the inner Button press to the parent block` &&
            ` (this.oParentBlock.fireDummy(oEvent.getParameters())) so the outside world sees the event as fired by the block itself, and the main controller's onDummy shows MessageToast 'dummy event fired by` &&
            ` control ' + oEvent.getSource().getId() - the point of the sample. abap2UI5 has no custom-control layer, so the block is inlined and the two-hop eventing indirection folds to one thin-frontend wire:` &&
            ` the Button press round-trips as event DUMMY carrying $event.oSource.getParent().getParent().sId - the runtime id of the outer m:VBox, which stands in for the block and carries its id 'block', the`.
    text1 = text1 && ` exact analogue of oParentBlock - and on_event composes the same toast text server-side via message_toast_display. Absent as a result: the sample:EventingBlock control with its id and dummy` &&
            ` attributes. // IMPROVISED: The EventingBlock view's content is two nested html:div wrappers (class='dummyContainer1' and class='dummyContainer2') around the Button. A core:HTML leaf carries markup in` &&
            ` a string attribute only and cannot wrap a UI5 control (app 239 precedent), so each html:div becomes an m:VBox carrying the same CSS class - the injected stylesheet's .dummyContainer2 rule` &&
            ` (display:inline-block) then overrides the FlexBox display exactly as it styles the original div, so the inner container shrink-wraps the Button. The two m:VBox controls are extra vs the original` &&
            ` views (the html:div elements themselves are lowercase, aggregation-level in the diff). // NOTE: The EventingBlock controller's onInit injects the sample's styles.css as a link element into` &&
            ` document.head; here the two rules (.dummyContainer1 padding 1em, height 4em, background-color #A9EAFF; .dummyContainer2 display:inline-block) are injected as a style tag in a core:HTML content`.
    text1 = text1 && ` attribute (apps 026/028 idiom, literal braces escaped as backslash-brace; the EXTRA core:HTML control vs the original views), placed as first child of the view root before the ObjectPageLayout so it` &&
            ` occupies no blocks grid cell. // NOTE: Namespace representation and block-root fold (app 239 precedent): EventingBlock.view.xml declares sap.m as its default xmlns, so its Button is bare there; the` &&
            ` single port view keeps the main view's default xmlns sap.uxap, so the inlined button is m:Button - control counts are prefix-sensitive in structural-diff, the bare Button (with its text and press` &&
            ` attributes) reads as missing with m:Button in its place. The block's own root mvc:View wrapper is dropped with the content-only inlining, so its width='100%' attribute (attr View.width) is absent;` &&
            ` the main view root keeps height='100%'. // NOTE: Unverified in a running system: (a) the DUMMY press round-trip - the $event.oSource.getParent().getParent().sId parent-chain arg resolving to the` &&
            ` stand-in VBox's runtime id and the server-composed message_toast_display text; (b) the injected .dummyContainer2 display:inline-block rule winning over the m:VBox flex display so the inner container`.
    text1 = text1 && ` shrink-wraps the button like the original html:div. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_410.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.BlockBase`                    name = `BlockBaseEventing`                             class = `z2ui5_cl_smpc_app_410` path = `src/01/03/z2ui5_cl_smpc_app_410.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 161/178 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks aggregations hold SharedBlocks BlockBase controls -` &&
            ` blockcolor:BlockBlue (7 times) and blockcolor:BlockBlueWithInfo (once), xmlns:blockcolor='sap.uxap.sample.SharedBlocks'. A BlockBase is only a lazy-loading wrapper around a static view: BlockBlue's` &&
            ` view is a single coloured div (html:div style='height:4em; background-color: #A9EAFF ;'), BlockBlueWithInfo's view is a coloured div wrapping an html:a 'ShowSubsectionMore = true', an html:br and an` &&
            ` empty m:Text id='moreContentText'. Since ObjectPageSubSection.blocks accepts any sap.ui.core.Control, each block is inlined as a core:HTML leaf carrying the decoded div markup in its content` &&
            ` attribute, plus one m:Text leaf for the WithInfo block. Absent as a result: the seven blockcolor:BlockBlue controls with their columnLayout attributes ('auto'/'1'/'1'/'2'/'3'/'3'/'3'), the` &&
            ` blockcolor:BlockBlueWithInfo control with its showSubSectionMore='true' and columnLayout='2' attributes, and the block views' own mvc:View roots (they carry only xmlns declarations and their`.
    text1 = text1 && ` controllerName - no width/height attributes). Present in their place: eight core:HTML controls (content attribute) and one m:Text (id attribute). Typed IMPROVISED since 2026-08-23. CAPABILITIES' own` &&
            ` row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss` &&
            ` regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined` &&
            ` form falls back to automatic column assignment, and the block ids and view roots go with it. // IMPROVISED: The BlockBase-specific behaviours are lost with the dropped block controls: (a)` &&
            ` columnLayout ('auto'/'1'/'2'/'3') is a BlockBase property telling the ObjectPageSubSection grid how many columns a block spans - core:HTML has no such property, so the three subsections render every` &&
            ` inlined block at the default column span instead of the 1/2/3-column arrangements their titles describe; (b) showSubSectionMore='true' on blockcolor:BlockBlueWithInfo (the subsection's 'See more'`.
    text1 = text1 && ` toggle switching the block into its Expanded view) is dropped, and with it the BlockBase view-mode machinery: the block controller's onAfterRendering fills m:Text id='moreContentText' with '...More` &&
            ` Content' only in Expanded mode, so the port's m:Text stays permanently empty (the original's initial Collapsed state) and sits as a sibling after its core:HTML div instead of inside it - a control` &&
            ` cannot be wrapped by core:HTML markup, which carries markup in a string attribute only (app 239 precedent). // NOTE: Named-model prefix-drop (app 230/261 precedent): the controller's ConfigModel` &&
            ` (JSONModel {subSectionLayout: 'TitleOnTop'}) is folded onto the one default model - subSectionLayout='{ConfigModel>/subSectionLayout}' becomes the two-way bound root field ({/SUBSECTIONLAYOUT}),` &&
            ` seeded 'TitleOnTop' exactly as onInit does. Same leaf name, same data, renders identically. The controller's toggleTitle handler becomes the TOGGLE_TITLE event branch flipping the field between` &&
            ` TitleOnTop and TitleOnLeft; the press='.toggleTitle' wire on the ObjectPageHeaderActionButton is client->_event('TOGGLE_TITLE'). // NOTE: Round-trip behaviour not verified in a running system: the`.
    text1 = text1 && ` 'toggle title' ObjectPageHeaderActionButton fires TOGGLE_TITLE, the backend flips subsectionlayout between TitleOnTop and TitleOnLeft and pushes it back on the round-trip - whether the bound` &&
            ` subSectionLayout property re-layouts the subsection titles live, and the overall ObjectPage rendering of the eight inlined core:HTML blocks, remain to be checked. The module asserts` &&
            ` getSubSectionLayout( ) - the PROPERTY value - after each of two presses, which does cover the round-trip wire (a dead TOGGLE_TITLE or a one-legged COND fails it). The re-layout half is not asserted` &&
            ` at all; the behaviour itself is real (ObjectPageSubSection re-reads getSubSectionLayout( ) on layout), only the claim is wider than the module. **e2e-verified 2026-08-21** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_408.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.BlockBase`                    name = `ObjectPageBlockBase`                           class = `z2ui5_cl_smpc_app_408` path = `src/01/03/z2ui5_cl_smpc_app_408.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.HeaderFacetPattern`           name = `ObjectPageSectionShowTitle`                    class = `z2ui5_cl_smpc_app_200` path = `src/02/03/z2ui5_cl_smpc_app_200.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        ui5_only = abap_true
        is_post171 = abap_true
        notes = `POST-1.71: sap.uxap.ObjectPageSubSection.showTitle (showTitle='false' on the 'Order Details' and 'Products' subsections) exists only since UI5 1.77 - kept 1:1 (fidelity wins), so the app needs a UI5` &&
                 ` release >= 1.77 to render it. Also kept 1:1: the sap.m.Avatar control (since 1.73) with its displayShape (Square) and displaySize (L) properties, used in the snapped heading and header content. //` &&
                 ` NOTE: The two robot.png avatar assets use the sample's relative './test-resources/sap/uxap/images/robot.png' path; rewritten to the absolute sdk.openui5.org host` &&
                 ` ('https://sdk.openui5.org/test-resources/sap/uxap/images/robot.png') per the offline asset-URL rule. Same asset, literal src value only - structural-diff compares literal attribute names, not values.`
        post171 = `sap.uxap.ObjectPageSubSection.showTitle (showTitle='false' on the 'Order Details' and 'Products' subsections) exists only since UI5 1.77 - kept 1:1 (fidelity wins), so the app needs a UI5 release >=` &&
                 ` 1.77 to render it. Also kept 1:1: the sap.m.Avatar control (since 1.73) with its displayShape (Square) and displaySize (L) properties, used in the snapped heading and header content.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/217/261/402 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold SharedBlocks` &&
            ` BlockBase controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 /` &&
            ` BlockJobInfoPart2 / BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3. Each BlockBase is only a lazy-loading wrapper around a static view (a forms:SimpleForm with` &&
            ` core:Title / m:Label / m:Text, or a layout:HorizontalLayout / layout:VerticalLayout tree), so every block view is inlined 1:1 into its aggregation. Absent as a result: the 13 BlockBase controls with` &&
            ` their ids and their columnLayout / class='sapUxAPObjectPageSubSectionAlignContent' properties, plus the block views' own mvc:View roots with their width='100%'. Present in their place:` &&
            ` forms:SimpleForm, core:Title and the additional m:Label / m:Text / layout:VerticalLayout / layout:HorizontalLayout the block views contain. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row`.
    text1 = text1 && ` says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss` &&
            ` regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined` &&
            ` form falls back to automatic column assignment, and the block ids and view roots go with it. // NOTE: The QuickView fragment is rebuilt 1:1 and shown via client->popover_display( xml = ..., by_id =` &&
            ` ... ) anchored at the pressed link: handleTitleSelectorPress on the 'Created By:' (Julie Armstrong) and 'Changed By:' (John Miller) links round-trips TITLE_SELECTOR transporting $event.oSource.sId,` &&
            ` and the backend opens the fragment at that control - the original's Fragment.load + oQuickView.openBy(oButton) (app 100 precedent). Dropped with that rewiring: the navigate='.onNavigate' attribute on` &&
            ` the QuickView - the sample's controller defines no onNavigate handler at all, so the wire is dead code upstream and a dead event wire is not ported. The pages aggregation is bound via client->_bind`.
    text1 = text1 && ` (t_pages); the binding-info templateShareable: true is kept 1:1 on the relative groups / elements bindings and dropped on pages - each popover_display builds a fresh fragment, so no template outlives` &&
            ` its binding. // NOTE: CompanyData.json is seeded verbatim (both /pages rows, all groups and elements). Elements without an elementType in the source JSON (the page-2 Address and Slogan rows) are` &&
            ` filled with the QuickViewGroupElementType default 'text', and every element row seeds target '_blank' (the QuickViewGroupElement.target default) - absent JSON properties must not serialize as empty` &&
            ` strings, which would override the UI5 defaults (app 099/100 precedent). The page-2 titleUrl stays the empty string, which is the QuickViewPage.titleUrl default. // NOTE: The controller's two` &&
            ` constant-text MessageToast handlers are wired as round-trip-free client toasts (follow_up_action cs_event-control_global MESSAGE_TOAST show, app 005/401 idiom): onExternalApplicationLinkPress` &&
            ` ('Navigate to external application.') on the 'Robotech (234242343)' and 'Average User Rating' links, and onAnotherPageLinkPress ('Navigate to another page in the same application (List of delivery`.
    text1 = text1 && ` items)') on the 'Status' link. // NOTE: onOrderDetailsPress does oObjectPageLayout.setSelectedSection(byId('orderDetailsSection')). sap.uxap.ObjectPageLayout.selectedSection is an ASSOCIATION, so it` &&
            ` cannot be data-bound (app 263 precedent); the 'Order Details' header link is wired round-trip-free via follow_up_action( control_by_id, ObjectPageLayout / setSelectedSection / orderDetailsSection ) -` &&
            ` the controlIdOrNull argument kind resolves the section id to the control, exactly like the original's byId lookup. Re-verified 2026-08-23: still true. An association cannot be data-bound at all,` &&
            ` which is a UI5 fact rather than an abap2UI5 limit, and the frontend action is the reproduction rather than a workaround. // NOTE: The two avatar image paths are absolutized to the sdk.openui5.org` &&
            ` host per the offline asset-URL rule (corrected 2026-08-24). They were kept in the original's './test-resources/...' form, justified as "matching app 261" - which is false in two ways: app 261 does` &&
            ` not use that asset at all - both its avatars are sap-icon://picture, and it carries zero occurrences of imageID_275314.png - and the two icons it DOES share with the family (linkedin.png,`.
    text1 = text1 && ` Twitter.png) were rewritten to the absolute form on 2026-08-23 for exactly this reason, its own sidecar recording that they had rendered broken until then. A relative path resolves against the demo` &&
            ` kit's document root; an abap2UI5 app is served from the ABAP ICF node and has no such root, so the request 404s and the control falls back to its placeholder. No gate sees it - data-fidelity only` &&
            ` rejects a non-OpenUI5 HOST, and a relative path has none. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample is in scope via its entity): the snappedHeading avatar` &&
            ` and the headerContent avatar (displaySize='L'), plus the QuickView page avatar in the fragment. Needs a UI5 runtime >= 1.73. // POST-1.71: the avatar aggregation of sap.m.QuickViewPage (since UI5` &&
            ` 1.92) is newer than 1.71 but kept for the 1:1 port - the fragment fills it with an Avatar bound to {ICON} / {DISPLAYSHAPE} exactly like the original. The app needs UI5 >= 1.92 to show the page icons.`.
    text1 = text1 && ` // POST-1.71: showTitle on sap.uxap.ObjectPageSubSection (since UI5 1.77) is newer than 1.71 but kept for the 1:1 port - the Order Details subsection hides its duplicate title with showTitle='false'.` &&
            ` Needs UI5 >= 1.77 for the title to be hidden. // POST-1.71: the content aggregation of sap.m.Title (since UI5 1.87) is newer than 1.71 but kept for the 1:1 port - the 'Order Details', 'Status' and` &&
            ` 'Average User Rating' header titles each hold an m:Link child, exactly like the original (invisible to the property gate: a default aggregation never appears as an XML attribute). Needs UI5 >= 1.87.` &&
            ` // NOTE: Round-trip behaviour not verified in a running system: the TITLE_SELECTOR event with its $event.oSource.sId anchor and the popover_display of the QuickView fragment at the pressed link` &&
            ` (including the pageLink navigation to companyEmployeePageId inside the popover), the follow_up_action setSelectedSection frontend action on the 'Order Details' link, and the two client toasts. One` &&
            ` leg of that is weaker than it reads: the Order Details check asserts the ObjectPageLayout CONTAINS '589946637'. enableLazyLoading defaults to false, so all four sections are in the DOM from load and`.
    text1 = text1 && ` toContainText reads textContent, not visibility - a dead setSelectedSection follow-up passes it unchanged. The real check is the one app 595's module uses: resolve getSelectedSection( ) and assert` &&
            ` its title. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_412.mjs).`.
    text2 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample is in scope via its entity): the snappedHeading avatar and the headerContent avatar (displaySize='L'), plus the QuickView page` &&
            ` avatar in the fragment. Needs a UI5 runtime >= 1.73. // the avatar aggregation of sap.m.QuickViewPage (since UI5 1.92) is newer than 1.71 but kept for the 1:1 port - the fragment fills it with an` &&
            ` Avatar bound to {ICON} / {DISPLAYSHAPE} exactly like the original. The app needs UI5 >= 1.92 to show the page icons. // showTitle on sap.uxap.ObjectPageSubSection (since UI5 1.77) is newer than 1.71` &&
            ` but kept for the 1:1 port - the Order Details subsection hides its duplicate title with showTitle='false'. Needs UI5 >= 1.77 for the title to be hidden. // the content aggregation of sap.m.Title` &&
            ` (since UI5 1.87) is newer than 1.71 but kept for the 1:1 port - the 'Order Details', 'Status' and 'Average User Rating' header titles each hold an m:Link child, exactly like the original (invisible` &&
            ` to the property gate: a default aggregation never appears as an XML attribute). Needs UI5 >= 1.87.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.HeaderFacetPattern`           name = `ObjectPageWithLinksAndObjectStatus`            class = `z2ui5_cl_smpc_app_412` path = `src/02/03/z2ui5_cl_smpc_app_412.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        ui5_only = abap_true
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `NOTE: Named-model fold: the sample's whole point is the uxap:ModelMapping element inside a custom BlockBase (sample:ModelMappingBlock), which maps the external named model jsonModel (at` &&
            ` {jsonModel>/externalPath}, resolving to /Employee) onto the block's internal named model Contact, so {Contact>firstName}/{Contact>lastName} render John/Miller. abap2UI5 serves a single default model` &&
            ` - a second, independent ABAP-fed named model was DECLINED 2026-07-19 (pr/named-json-models, too complex: one serialized JSONModel per slot). So the two named models are folded into the one default` &&
            ` model: firstName/lastName are seeded at the default-model root and bound {/FIRSTNAME}/{/LASTNAME} (last-segment match with the original {Contact>firstName}/{Contact>lastName}). Consequently the` &&
            ` ModelMapping control (the sample entity, a pure model-mapping config with no visual output) is absent, the externalPath indirection is resolved statically to /Employee, and the ModelMappingBlock` &&
            ` BlockBase wrapper is inlined to its own view content: its forms:SimpleForm (core:Title + two Text) is rendered directly, since ObjectPageSubSection/block content is standard controls (CAPABILITIES`.
    text1 = text1 && ` BlockBase row, apps 161/178). ModelMappingBlock and ModelMapping are therefore both absent and a forms:SimpleForm/core:Title/Text are present in their place.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ModelMapping`                 name = `BoundModelMapping`                             class = `z2ui5_cl_smpc_app_230` path = `src/01/03/z2ui5_cl_smpc_app_230.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Named-model fold + BlockBase inline: the sample's whole point is the uxap:ModelMapping element inside a custom BlockBase (sample:ModelMappingBlock), which maps the external named model` &&
            ` jsonModel (externalModelName='jsonModel') at the static externalPath='/Employee' onto the block's internal named model Contact (internalModelName='Contact'), so {Contact>firstName}/{Contact>lastName}` &&
            ` render John/Miller. abap2UI5 serves a single default model - a second, independent ABAP-fed named model was DECLINED 2026-07-19 (pr/named-json-models) - so the two named models are folded into the` &&
            ` one default model: firstName/lastName are seeded at the default-model root from the controller's jsonModel /Employee record and bound absolutely via client->_bind as {/FIRSTNAME}/{/LASTNAME}` &&
            ` (last-segment match with the original {Contact>firstName}/{Contact>lastName}). Lost, and why this is IMPROVISED rather than a pure prefix-drop NOTE (port-a-sample deviation refinement, app 230` &&
            ` precedent): the ModelMapping config control (the sample entity, zero visual output but the declared artifact the sample demonstrates) and the ModelMappingBlock BlockBase wrapper with its id='block'`.
    text1 = text1 && ` are both absent, and the external-to-internal mapping indirection is resolved statically at port time. The ModelMappingBlock is inlined to its own view content (CAPABILITIES BlockBase row, apps` &&
            ` 161/188/261): its forms:SimpleForm (core:Title 'My name' + two Text) renders directly in the port view; the block view's own mvc:View root is dropped with it (that root carries only xmlns` &&
            ` declarations - no width/height or other attributes - and the main view's mvc:View root carries only xmlns declarations plus controllerName). ModelMappingBlock and ModelMapping are therefore both` &&
            ` absent and a forms:SimpleForm/core:Title/Text are present in their place; the render is pixel-identical to the original.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ModelMapping`                 name = `MPModelMapping`                                class = `z2ui5_cl_smpc_app_411` path = `src/01/03/z2ui5_cl_smpc_app_411.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 161/187 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the five blocks aggregations each hold the sample's own BlockBase` &&
            ` control sample:mySimpleBlock (xmlns:sample='sap.uxap.sample.AnchorBar.controller.blocks', ids bbt1-bbt5). A BlockBase is only a lazy-loading wrapper around a view; mySimpleBlock's view is an html:div` &&
            ` (style 'font-size: 0.875rem') wrapping a forms:SimpleForm (maxContainerCols=2, layout=ResponsiveGridLayout, editable=false) with one Label 'Content' / Text 'some content goes here...' pair. Since` &&
            ` ObjectPageSubSection.blocks accepts any sap.ui.core.Control, that SimpleForm is inlined 1:1 in each blocks aggregation - no custom JS control, thin frontend preserved. Lost with the wrapper: the five` &&
            ` mySimpleBlock controls with their ids, the html:div font-size:0.875rem styling (a core:HTML leaf cannot wrap controls, so the div is not reproducible around the form) and the block view root's own` &&
            ` width='100%' attribute, which goes with the dropped mvc:View wrapper. // NOTE: ObjectPageDynamicHeaderTitle backgroundDesign='Solid' plus ObjectPageLayout backgroundDesignAnchorBar='Translucent'`.
    text1 = text1 && ` (@since 1.58) are pure rendering properties - the port passes them through 1:1, but the rendered title / anchor-bar background was not verified in a running system. **e2e-verified 2026-07-31**` &&
            ` (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the anchor bar renders with the sapUxAPObjectPageNavigationTranslucent class, the dynamic header title shows 'Denise Smith' and` &&
            ` an inlined block form renders. Residual (nothing e2e can assert): the title's backgroundDesign='Solid' is a pure paint property with no own DOM marker. (Corrected 2026-08-23: the module asserts` &&
            ` .sapUiForm.first( ) - one of the five.)`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageDynamicHeaderTitle` name = `ObjectPageHeaderBackgroundDesign`              class = `z2ui5_cl_smpc_app_258` path = `src/01/03/z2ui5_cl_smpc_app_258.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/217 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold SharedBlocks BlockBase` &&
            ` controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 / BlockJobInfoPart2 /` &&
            ` BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3. A BlockBase is only a lazy-loading wrapper around a view; each of these views is static (a forms:SimpleForm with` &&
            ` core:Title / Label / Text children, BlockJobInfoPart3 a layout:HorizontalLayout / layout:VerticalLayout tree), so every block view is inlined 1:1 into its aggregation and the block's own` &&
            ` class='sapUxAPObjectPageSubSectionAlignContent' is carried onto the inlined root control. Absent from the port as a result: the 13 BlockBase controls with their ids (goalsblock, phone, social,` &&
            ` adresses, mailing, part1, part2, jobinfopart1-3, empdetailpart1-3) and their BlockBase columnLayout='1' properties; the block views' own mvc:View roots are gone too. The block views' default`.
    text1 = text1 && ` namespace is sap.m, so their bare Label / Text / SimpleForm counts move to the port's m: / forms: prefixed controls. Corrected 2026-08-21: the parenthesis used to read "(with their width='100%')",` &&
            ` which is not true of this sample - none of the 25 SharedBlocks views carries a width on its mvc:View root; the width='100%' belongs to the forms:SimpleForm and the port keeps it. The phrase is` &&
            ` accurate for app 258's mySimpleBlock and was carried here where it does not hold. // POST-1.71: sap.m.Avatar is a control @since 1.73 (scope-of: OUT_OF_SCOPE for a sample entity, kept here because` &&
            ` the sample's entity sap.uxap.ObjectPageDynamicHeaderTitle is in scope and 1:1 fidelity wins for members/controls used inside): two Avatars (snappedHeading, headerContent) with displayShape='Square',` &&
            ` initials='HF', displaySize='L'. Needs a UI5 runtime >= 1.73. // POST-1.71: sap.m.Title aggregation 'content' is @since 1.87 - the original nests an m:Link inside two m:Title controls (Profile,` &&
            ` Product Description) and the port keeps that nesting 1:1. Needs a UI5 runtime >= 1.87; below it the whole VIEW fails to load. (Corrected 2026-08-23: content is sap.m.Title's only aggregation and its`.
    text1 = text1 && ` defaultAggregation, so on a pre-1.87 runtime Title has no default aggregation and XMLTemplateProcessor THROWS "Cannot add direct child without default aggregation defined" - a blank app, not an` &&
            ` ObjectPage missing two links.) // NOTE: DEVIATION, not a copy: the original writes percentValue="42%" on sap.m.ProgressIndicator, and the port writes ``42``. percentValue is a FLOAT property, so` &&
            ` "42%" is not a valid value for it - UI5 parses the leading number leniently at runtime and the original renders, but the abap2UI5 linter rejects it outright (invalid-property-value, an error in the` &&
            ` chain-format gate), so the port cannot carry the string verbatim. The rendered result is identical, and displayValue="42%" - which IS a string property - is copied verbatim, so the '42%' the user` &&
            ` reads comes from the original either way. Corrected 2026-08-21: this entry used to claim the string was 'copied verbatim from the original', which the class had never done; restoring the literal to` &&
            ` test that claim is what surfaced the linter error, and the honest deviation is the answer rather than either the false claim or a red gate.`.
    text2 = `sap.m.Avatar is a control @since 1.73 (scope-of: OUT_OF_SCOPE for a sample entity, kept here because the sample's entity sap.uxap.ObjectPageDynamicHeaderTitle is in scope and 1:1 fidelity wins for` &&
            ` members/controls used inside): two Avatars (snappedHeading, headerContent) with displayShape='Square', initials='HF', displaySize='L'. Needs a UI5 runtime >= 1.73. // sap.m.Title aggregation` &&
            ` 'content' is @since 1.87 - the original nests an m:Link inside two m:Title controls (Profile, Product Description) and the port keeps that nesting 1:1. Needs a UI5 runtime >= 1.87; below it the whole` &&
            ` VIEW fails to load. (Corrected 2026-08-23: content is sap.m.Title's only aggregation and its defaultAggregation, so on a pre-1.87 runtime Title has no default aggregation and XMLTemplateProcessor` &&
            ` THROWS "Cannot add direct child without default aggregation defined" - a blank app, not an ObjectPage missing two links.)`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageDynamicHeaderTitle` name = `ObjectPageProgressRatingIndicators`            class = `z2ui5_cl_smpc_app_259` path = `src/02/03/z2ui5_cl_smpc_app_259.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 217/188/178/161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'). The original blocks/moreBlocks aggregations each hold a` &&
            ` custom BlockBase control from the sample's SharedBlocks JS: goals:GoalsBlock (id goalsblock), personal:BlockPhoneNumber (id phone), personal:BlockSocial (id social), personal:BlockAdresses (id` &&
            ` adresses), personal:BlockMailing (id mailing, columnLayout=1), personal:PersonalBlockPart1 (id part1, columnLayout=1) and personal:PersonalBlockPart2 (id part2, columnLayout=1). A BlockBase is only a` &&
            ` lazy-loading wrapper around a view, and ObjectPageSubSection.blocks/moreBlocks accept any sap.ui.core.Control, so each block's rendered content (a sap.ui.layout.form.SimpleForm) is inlined directly` &&
            ` here as a form:SimpleForm holding its core:Title, m:Label and m:Text children. Consequently all seven block controls (GoalsBlock, BlockPhoneNumber, BlockSocial, BlockAdresses, BlockMailing,` &&
            ` PersonalBlockPart1, PersonalBlockPart2) are absent from the port and seven form:SimpleForm controls with their core:Title (6), m:Label and m:Text content are present in their place (the m:Label count`.
    text1 = text1 && ` rises from 2 to 14, the m:Text count from 1 to 13); the goals/personal xmlns prefixes drop with the blocks. The blocks carry no controller behaviour to port; the ObjectPageModel (SharedJSONData` &&
            ` employee.json) the controller loads is never bound by the view or the inlined blocks, so no default model is seeded. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the` &&
            ` block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a` &&
            ` pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to` &&
            ` automatic column assignment, and the block ids and view roots go with it. // NOTE: Asset URLs: the sample's host-relative image paths - ObjectPageHeader.objectImageURI` &&
            ` 'test-resources/sap/uxap/images/imageID_273624.png' and the two m:Image src values './test-resources/sap/uxap/images/twitterIcon.png' / './test-resources/sap/uxap/images/linkedInIcon.png' - are`.
    text1 = text1 && ` rewritten to the absolute OpenUI5 host ('https://sdk.openui5.org/test-resources/...') per the offline asset-URL rule (app 200 precedent). Same assets, literal src values only - structural-diff` &&
            ` compares attribute names, not literal values. // NOTE: handlePress (the 'Public Profile' ObjectPageHeaderActionButton press): the original controller calls` &&
            ` setShowHeaderContent(!getShowHeaderContent()) on the ObjectPageLayout. showHeaderContent is a bindable property (default true, no @since tag = base version), so the port two-way binds it` &&
            ` (showHeaderContent={/SHOW_HEADER_CONTENT}, seeded abap_true) and the TOGGLE_HEADER_CONTENT event flips the flag server-side - the prefer-a-bindable-property rule; the attribute is added versus the` &&
            ` original view (extra attributes are not flagged by structural-diff). // NOTE: Popover anchoring: handleMarkChangesPress opens the PopoverUnsavedChanges fragment via` &&
            ` oPopoverChanges.openBy(oEvent.getParameter('domRef')) - the markChangesPress event's domRef parameter, the DOM element of the pressed unsaved-changes marker icon. The port opens the same`.
    text1 = text1 && ` core:FragmentDefinition/ResponsivePopover via client->popover_display( xml = ... by_id = ... ) anchored at the event source transported as $event.oSource.sId, i.e. the ObjectPageHeader control rather` &&
            ` than the marker icon's DOM node - the popover placement (Bottom) is preserved, only the exact anchor element differs. The controller's oPopoverChanges.setModel(oEvent.getSource().getModel()) folds` &&
            ` away: abap2UI5 serves one default model and the fragment binds nothing. // NOTE: Round-trip behaviour unverified in a running system: the MARK_CHANGES_PRESS wire (markChangesPress -> popover_display` &&
            ` of the ResponsivePopover anchored by $event.oSource.sId) and the TOGGLE_HEADER_CONTENT wire (press -> xsdbool flip of the two-way bound showHeaderContent) have not been live-tested. **e2e-verified` &&
            ` 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_414.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeader`             name = `AlternativeProfileObjectPageHeader`            class = `z2ui5_cl_smpc_app_414` path = `src/01/03/z2ui5_cl_smpc_app_414.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 217/401/402 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'). The original blocks/moreBlocks aggregations each hold a custom` &&
            ` BlockBase control from the sample's SharedBlocks JS: goals:GoalsBlock (id goalsblock), personal:BlockPhoneNumber (id phone), personal:BlockSocial (id social), personal:BlockAdresses (id adresses),` &&
            ` personal:BlockMailing (id mailing, columnLayout=1), personal:PersonalBlockPart1 (id part1, columnLayout=1) and personal:PersonalBlockPart2 (id part2, columnLayout=1). A BlockBase is only a` &&
            ` lazy-loading wrapper around a view, and ObjectPageSubSection.blocks/moreBlocks accept any sap.ui.core.Control, so each block's rendered content (a sap.ui.layout.form.SimpleForm) is inlined directly` &&
            ` here as a form:SimpleForm holding its core:Title, m:Label and m:Text children. Consequently all seven block controls (GoalsBlock, BlockPhoneNumber, BlockSocial, BlockAdresses, BlockMailing,` &&
            ` PersonalBlockPart1, PersonalBlockPart2) are absent from the port and seven form:SimpleForm controls with their core:Title, m:Label and m:Text content are present in their place. The blocks carry no`.
    text1 = text1 && ` controller behaviour to port; the ObjectPageModel (employee.json) and the default products.json model the controller loads are never bound by the view, so neither is seeded. Typed IMPROVISED since` &&
            ` 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a` &&
            ` zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real` &&
            ` BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it. // NOTE: The controller's named 'buttons' JSONModel ({ text: 'working` &&
            ` binding', icon: 'sap-icon://chain-link' }) is folded onto the one default model root (same-data prefix-drop): the fifth ObjectPageHeaderActionButton's text='{buttons>/text}' becomes the bound root` &&
            ` field {/TEXT} seeded 'working binding', the sixth button's icon='{buttons>/icon}' becomes {/ICON} seeded 'sap-icon://chain-link' - same values, same leaf names, identical render. The sixth button's`.
    text1 = text1 && ` composite text binding {path:'/text', model:'buttons', formatter:'.onFormat'} runs the controller formatter .onFormat, which ignores its input and returns the constant 'formatted link'; per the` &&
            ` thin-frontend rule that result is computed in model_init and bound as {/FORMATTED_TEXT} (apps 009/010/022/092 formatter precedent). // NOTE: The ObjectPageHeader objectImageURI points at the` &&
            ` sdk.openui5.org host (https://sdk.openui5.org/test-resources/sap/uxap/images/imageID_275314.png) per the offline asset-URL rule; the original uses the relative ./test-resources path (apps 244/262/263` &&
            ` carry the same absolutization for the same image). Literal attribute values are not compared by structural-diff. // NOTE: Unverified in a running system: the breadcrumb m:Link press round-trip (event` &&
            ` LINK2_PRESS) - the controller's handleLink2Press constant-text MessageToast is served via client->message_toast_display( 'Page 2 long link clicked' ) from on_event. **e2e-verified 2026-08-21**` &&
            ` (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_416.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeader`             name = `ChildObjectPage`                               class = `z2ui5_cl_smpc_app_416` path = `src/01/03/z2ui5_cl_smpc_app_416.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/178/161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'). The original blocks/moreBlocks aggregations each hold a custom` &&
            ` BlockBase control from the sample's SharedBlocks JS: goals:GoalsBlock (id goalsblock), personal:BlockPhoneNumber (id phone), personal:BlockSocial (id social), personal:BlockAdresses (id adresses),` &&
            ` personal:BlockMailing (id mailing, columnLayout=1), personal:PersonalBlockPart1 (id part1, columnLayout=1) and personal:PersonalBlockPart2 (id part2, columnLayout=1). A BlockBase is only a` &&
            ` lazy-loading wrapper around a view, and ObjectPageSubSection.blocks/moreBlocks accept any sap.ui.core.Control, so each block's rendered content (a sap.ui.layout.form.SimpleForm) is inlined directly` &&
            ` here as a form:SimpleForm holding its core:Title, m:Label and m:Text children. Consequently all seven block controls (GoalsBlock, BlockPhoneNumber, BlockSocial, BlockAdresses, BlockMailing,` &&
            ` PersonalBlockPart1, PersonalBlockPart2) are absent from the port and seven form:SimpleForm controls with their core:Title, m:Label and m:Text content are present in their place (the m:Label count`.
    text1 = text1 && ` rises from 3 to 15). The blocks carry no controller behaviour to port; the ObjectPageModel (employee.json) the controller loads is never bound by the view, so no default model is seeded. Typed` &&
            ` IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares -` &&
            ` even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a` &&
            ` real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeader`             name = `KPIObjectPageHeader`                           class = `z2ui5_cl_smpc_app_217` path = `src/01/03/z2ui5_cl_smpc_app_217.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `NOTE: handleSCBtnPress branches on the breakpoint - DynamicSideContent.toggle( ) on S, setShowSideContent( true ) everywhere else - and the port branches with it since 2026-08-21, driving toggle( )` &&
            ` through control_by_id on S and flipping the bound showSideContent otherwise. Until then this entry claimed the two legs fold into one bound flip because 'toggle only flips side-content visibility on` &&
            ` S'. They do not fold: setShowSideContent re-derives the control's private visibility pair through _setResizeData( S ), which puts _SCVisible back to (sideContentVisibility === AlwaysShow) - false,` &&
            ` since the default is ShowAboveS - and _MCVisible to true, so _changeGridState keeps the MAIN content on screen and the side content never appears. toggle( ) is the branch that swaps the pair. On S` &&
            ` the port therefore showed nothing while hiding the open button in the same round-trip, with SET_FOCUS pointing at a Close button inside the invisible side content - a dead end until the viewport was` &&
            ` resized. Same defect as apps 344 and 138. // NOTE: style.css (.sapUiTheme-sap_bluecrystal .sapUiDSC gets background-color rgb(242, 248, 252) - a blue-crystal-theme-only rule) is injected through a`.
    text1 = text1 && ` core:HTML <style> leaf, so the port adds one core:HTML control the original view does not have - abap2UI5 ships no separate stylesheet. The CSS braces are escaped \{ \} in a backtick literal so the` &&
            ` XMLView parser does not read them as bindings (app 344 precedent). // NOTE: The controller's JSONModel (SharedJSONData/employee.json, set as the named 'ObjectPageModel') is never bound by any control` &&
            ` in this view - every text is a literal - so the port seeds no model data and has no model_init (app 217/260 precedent); the two bound flags are control state, seeded inline in main. // NOTE: The` &&
            ` three image assets point at the sdk.openui5.org host per the offline asset-URL rule - https://sdk.openui5.org/test-resources/sap/uxap/images/imageID_273624.png (objectImageURI), twitterIcon.png and` &&
            ` linkedInIcon.png (the two m:Image src values); the original uses relative test-resources paths (app 263 precedent). // NOTE: The original's empty blocks and moreBlocks aggregation tags on the` &&
            ` paymentSubSection ObjectPageSubSection are not written - they hold no children, and aggregation elements are optional in XML (ignored by the structural diff). The side content's long Lorem Text keeps`.
    text1 = text1 && ` the original wording verbatim with the XML attribute's line breaks and tab indentation normalized to single spaces, as an XML attribute-value parser would (app 344 precedent). // NOTE: Unverified in` &&
            ` a running system: the breakpointChanged round-trip transporting currentBreakpoint and recomputing the open button's bound visible flag, the showSideContent flip on the two press round-trips, and the` &&
            ` SET_FOCUS follow-up on the openSideContentBtn / closeSideContentBtn ids. The breakpointChanged transport and the showSideContent flip use the same wiring that is e2e-verified on app 267 and live on` &&
            ` apps 138/344. **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_417.mjs). // NOTE: Both handleSCBtnPress and handleSideContentHide branch on the CURRENT` &&
            ` breakpoint, and the port reproduced neither branch reliably until 2026-08-24. (a) breakpoint was written only in the BP_CHANGED round-trip, and that event provably never fires on the first` &&
            ` measurement - _setBreakpointFromWidth guards the fire with ``if (sCurrentBreakpoint !== undefined)`` and _currentBreakpoint is undefined until that very call. An app OPENED at a small viewport (a`.
    text1 = text1 && ` phone, where nothing ever resizes) therefore kept breakpoint initial forever and took the non-S leg every time, which is exactly the dead end the S branch exists to avoid. at_breakpoint_s( ) now` &&
            ` falls back to the server-side device mirror, client->get( )-s_device-resize-width <= 720, which is the control's own S_M_BREAKPOINT; containerQuery is true, so the control measures its CONTAINER,` &&
            ` which can never be wider than the window - so window <= 720 implies S, and the converse is what breakpointChanged still reports. (b) handleSideContentHide's own S branch (toggle( ) rather than` &&
            ` setShowSideContent( abap_false )) was dropped entirely. It matters past the close: toggle( ) reaches its swap through setShowSideContent( true, true ), leaving the PROPERTY true, while` &&
            ` setShowSideContent( abap_false ) leaves it false - so after a close at S and a resize up to M/L the original's isSideContentVisible( ) still reports true and the side content returns, where the port` &&
            ` left it hidden next to a re-shown open button.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeader`             name = `ObjectPageDynamicSideContentBtn`               class = `z2ui5_cl_smpc_app_417` path = `src/01/03/z2ui5_cl_smpc_app_417.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 217/188/161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'). The original blocks/moreBlocks aggregations each hold a custom` &&
            ` BlockBase control from the sample's SharedBlocks JS: goals:GoalsBlock (id goalsblock), personal:BlockPhoneNumber (id phone), personal:BlockSocial (id social), personal:BlockAdresses (id adresses),` &&
            ` personal:BlockMailing (id mailing, columnLayout=1), personal:PersonalBlockPart1 (id part1, columnLayout=1) and personal:PersonalBlockPart2 (id part2, columnLayout=1). A BlockBase is only a` &&
            ` lazy-loading wrapper around a view, and ObjectPageSubSection.blocks/moreBlocks accept any sap.ui.core.Control, so each block's rendered content (a sap.ui.layout.form.SimpleForm) is inlined directly` &&
            ` here as a form:SimpleForm holding its core:Title, m:Label and m:Text children. Consequently all seven block controls (GoalsBlock, BlockPhoneNumber, BlockSocial, BlockAdresses, BlockMailing,` &&
            ` PersonalBlockPart1, PersonalBlockPart2) and their columnLayout attribute are absent from the port and seven form:SimpleForm controls with their core:Title, m:Label and m:Text content are present in`.
    text1 = text1 && ` their place (the m:Label count rises from 4 to 16, the m:Text count from 1 to 13). The ObjectPageModel (SharedJSONData/employee.json) the controller loads is never bound by the view or the inlined` &&
            ` blocks, so it is not reproduced. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that` &&
            ` removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable` &&
            ` loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it. // NOTE: Named-model` &&
            ` fold, pure prefix-drop rendering identically: the controller's inline 'buttons' JSONModel ({text: 'working binding', icon: 'sap-icon://chain-link'}) is flattened into the one default model - the` &&
            ` fifth ObjectPageHeaderActionButton's text='{buttons>/text}' becomes the root binding {/TEXT} (value 'working binding') and the sixth one's icon='{buttons>/icon}' becomes {/ICON} (value`.
    text1 = text1 && ` 'sap-icon://chain-link'). The sixth button's text binding-info {path:'/text', model:'buttons', formatter:'.onFormat'} goes through the controller formatter .onFormat, which returns the constant` &&
            ` 'formatted link' regardless of input; per the thin-frontend rule that result is computed in model_init and bound as {/FORMATTED_TEXT}. The controller's third model (SharedJSONData/products.json as` &&
            ` the OneWay default model) is seeded verbatim as the productcollection table for the Popover fragment's List. // NOTE: Popover anchoring: the original controller opens each ResponsivePopover fragment` &&
            ` with oPopover.openBy(oEvent.getParameter('domRef')) - the DOM element of the pressed title-arrow (titleSelectorPress) resp. lock marker (markLockedPress) inside the ObjectPageHeader. abap2UI5's` &&
            ` popover_display anchors by control id, so both wires transport the event source's control id via $event.oSource.sId and the popovers open by the ObjectPageHeader control (id headerForTest) instead of` &&
            ` the exact icon DOM element - same placement='Bottom', slightly coarser anchor. The fragments themselves (Popover.fragment.xml, PopoverLock.fragment.xml) are rebuilt 1:1 as core:FragmentDefinition`.
    text1 = text1 && ` chains shown via popover_display. // NOTE: Round-trip behaviour unverified in a running system: the two anchored ResponsivePopover opens (TITLE_SELECTOR via titleSelectorPress, MARK_LOCKED via` &&
            ` markLockedPress, both anchored at $event.oSource.sId), the ITEM_SELECT selectionChange round-trip that closes the popover via follow_up_action popover_close (the INTENT of handleItemSelect (selecting` &&
            ` an item closes the popover). Not literally 1:1: the original's handler reads this._oPopover, which its controller never assigns - it only ever sets _oPopoverPromise and _oPopoverLock - so upstream` &&
            ` the handler throws a TypeError and the popover stays open. The port's behaviour is the intended one; the word '1:1' was wrong and is corrected 2026-08-21), and the LINK1/LINK2 breadcrumb Link press` &&
            ` toasts ('Page 1 a very long link clicked' / 'Page 2 long link clicked'). **e2e-verified 2026-08-21** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_415.mjs). // NOTE: The header's` &&
            ` objectImageURI is re-hosted: the view carries the demo-kit-relative './test-resources/sap/uxap/images/imageID_275314.png', which an abap2UI5 app has no document root to resolve against, so the port`.
    text1 = text1 && ` serves it from https://sdk.openui5.org/test-resources/sap/uxap/images/. The sibling ports 413 and 417 declare exactly this and 415 did not - only an inline comment recorded it, and data-fidelity` &&
            ` tolerates the absolutization, so nothing else would have said so. Added 2026-08-21.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeader`             name = `ObjectPageHeaderWithAllControls`               class = `z2ui5_cl_smpc_app_415` path = `src/01/03/z2ui5_cl_smpc_app_415.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 217/188/161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'). The original blocks/moreBlocks aggregations each hold a custom` &&
            ` BlockBase control from the sample's SharedBlocks JS: goals:GoalsBlock (id goalsblock), personal:BlockPhoneNumber (id phone), personal:BlockSocial (id social), personal:BlockAdresses (id adresses),` &&
            ` personal:BlockMailing (id mailing, columnLayout=1), personal:PersonalBlockPart1 (id part1, columnLayout=1) and personal:PersonalBlockPart2 (id part2, columnLayout=1). A BlockBase is only a` &&
            ` lazy-loading wrapper around a view, and ObjectPageSubSection.blocks/moreBlocks accept any sap.ui.core.Control, so each block's rendered content (a sap.ui.layout.form.SimpleForm) is inlined directly` &&
            ` here as a form:SimpleForm holding its core:Title, m:Label and m:Text children. Consequently all seven block controls (GoalsBlock, BlockPhoneNumber, BlockSocial, BlockAdresses, BlockMailing,` &&
            ` PersonalBlockPart1, PersonalBlockPart2) are absent from the port and seven form:SimpleForm controls with their core:Title, m:Label and m:Text content are present in their place. The blocks carry no`.
    text1 = text1 && ` controller behaviour to port; the named ObjectPageModel (SharedJSONData/employee.json) the controller loads is never bound by the view or the blocks, so no default model is seeded and the port is` &&
            ` init-only (no model_init, no on_event). Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit` &&
            ` that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a` &&
            ` measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it. // NOTE: The` &&
            ` three social-icon image assets (linkedInIcon.png, facebookIcon.png, twitterIcon.png) use the sample's relative './test-resources/sap/uxap/images/...' src paths; rewritten to the absolute` &&
            ` sdk.openui5.org host ('https://sdk.openui5.org/test-resources/sap/uxap/images/linkedInIcon.png' etc.) per the offline asset-URL rule (app 200/262 precedent). Same assets, literal src values only -`.
    text1 = text1 && ` structural-diff compares literal attribute names, not values. // NOTE: Rendering not yet verified in a running system: the port is init-only and fully static (no bindings, no events, no round-trip),` &&
            ` so the open behaviours are render-time only - the ObjectPageHeader navigationBar Bar, the showPlaceholder=true Circle placeholder avatar (no objectImageURI is set), showTitleInHeaderContent=true and` &&
            ` the always/snapped visibility flags (isObjectTitleAlwaysVisible/isObjectSubtitleAlwaysVisible=false, isActionAreaAlwaysVisible=true) are unverified live. The module covers the boot-and-render half` &&
            ` only - it asserts the layout is visible, the header title contains Rowan Atkinson, and the layout contains Bangalore, India and 2014 Goals Plan. None of the five header flags listed above is` &&
            ` asserted, and it never touches the navigationBar Bar, the placeholder avatar or the two action buttons. **e2e-verified 2026-08-16** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_413.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeader`             name = `ProfileObjectPageHeader`                       class = `z2ui5_cl_smpc_app_413` path = `src/01/03/z2ui5_cl_smpc_app_413.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 161/178 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): each of the 15 subsection blocks aggregations holds a custom` &&
            ` BlockBase control sample:mySimpleBlock (from the AnchorBar SharedBlocks JS, xmlns:sample='sap.uxap.sample.AnchorBar.controller.blocks', ids bbt1/bbt2/bbt3/bbt4/bbt5/b1..b10). A BlockBase is only a` &&
            ` lazy-loading wrapper around a view; mySimpleBlock's content is an html:div (font-size: 0.875rem) wrapping a forms:SimpleForm with a Label 'Content' + Text 'some content goes here...'. Since` &&
            ` ObjectPageSubSection.blocks accepts any sap.ui.core.Control, each mySimpleBlock is inlined directly as that SimpleForm. Consequently all 15 sample:mySimpleBlock controls (and their id attributes) are` &&
            ` absent from the port; the block's root mvc:View wrapper (its width='100%' attribute) is dropped together with its content-only inlining, and the html:div font-size wrapper is dropped - a control` &&
            ` cannot be wrapped by another control via core:HTML (core:HTML carries markup in a string attribute only), and the WRAPPING is what cannot be reproduced. The empty mySimpleBlock controller carries no`.
    text1 = text1 && ` behaviour to port. **Corrected 2026-08-23**: the font size itself is not lost with the div. CAPABILITIES documents the core:HTML <style> leaf for exactly this and names "CSS dropped although` &&
            ` core:HTML can carry it" as one of the two most common generation mistakes - app 240 already does it. Each of the 15 inlined SimpleForms carries class="smpcBlockFont" (an added attribute, declared)` &&
            ` and one injected rule sets font-size:0.875rem, so the blocks render at the size the sample renders them. What stays lost is the div element itself and its id. // NOTE: Namespace-representation` &&
            ` difference: mySimpleBlock.view.xml declares its default xmlns as sap.m, so its Label and Text controls are bare there; the single port view has a single default namespace (sap.uxap, matching the main` &&
            ` sample view), so the inlined block's Label and Text carry the m: prefix (m:Label / m:Text) and the SimpleForm keeps its forms: prefix. Control counts are prefix-sensitive in structural-diff, so the` &&
            ` block's bare 'Label' and 'Text' read as missing while the port supplies m:Label / m:Text - an unavoidable namespace-representation artifact of folding a sap.m-default fragment into a sap.uxap-default`.
    text1 = text1 && ` view (cheat-sheet 'multi-fragment differing-default-xmlns'). No output difference. // NOTE: Static sample: no controller behaviour, no model, no events - only view_display. All members used` &&
            ` (ObjectPageHeaderActionButton.text/type/icon/tooltip/hideText/hideIcon @since 1.26, ObjectPageDynamicHeaderTitle @since 1.52, ObjectPageLayout.showTitleInHeaderContent/upperCaseAnchorBar,` &&
            ` SimpleForm.maxContainerCols/layout/editable) are <= 1.71 - no POST_171 needed.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeaderActionButton` name = `ObjectPageHeaderActionButtons`                 class = `z2ui5_cl_smpc_app_239` path = `src/01/03/z2ui5_cl_smpc_app_239.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeaderContent`      name = `HeaderContent`                                 class = `z2ui5_cl_smpc_app_216` path = `src/01/03/z2ui5_cl_smpc_app_216.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        notes = `NOTE: The view is a 1:1 static rebuild - every ObjectStatus/ObjectNumber/Label/Text/ProgressIndicator/ObjectAttribute/Button/Tokenizer value is a literal, exactly as in the original` &&
                 ` ObjectPageHeaderContent view.xml. The sample's controller loads sap/uxap/sample/SharedJSONData/employee.json into a named 'ObjectPageModel', but the view never binds a single field from it (all` &&
                 ` header content is hard-coded in the XML), so the port carries no model_init - omitting a dead model changes nothing in the rendered view.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 178/161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the original blocks aggregations each hold a custom BlockBase` &&
            ` control goals:GoalsBlock (from the sample's SharedBlocks JS, xmlns:goals='sap.uxap.sample.SharedBlocks.goals'), used 4 times in the four ObjectPageSubSection blocks aggregations. A BlockBase is only` &&
            ` a lazy-loading wrapper around a view; GoalsBlock's rendered content is a sap.ui.layout.form.SimpleForm (editable='false' layout='ColumnLayout') holding three Label/Text goal pairs. Since` &&
            ` ObjectPageSubSection.blocks accepts any sap.ui.core.Control, each goals:GoalsBlock is inlined here as that SimpleForm (form:SimpleForm) with its m:Label/m:Text content - the whole ObjectPage renders` &&
            ` with the thin generic frontend, no custom JS control. Consequently all four goals:GoalsBlock controls are absent from the port and four form:SimpleForm controls (with their Label/Text children) are` &&
            ` present in their place. GoalsBlock has no controller behaviour to port. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the`.
    text1 = text1 && ` recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row` &&
            ` carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots` &&
            ` go with it. // POST-1.71: the sap.m.Avatar control (since UI5 1.73) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.73 to render it. @since verified in` &&
            ` sap/m/Avatar.js:99 (control-level, which the member-level property gate never saw).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageHeaderContent`      name = `ObjectPageHeaderContentPriorities`             class = `z2ui5_cl_smpc_app_188` path = `src/02/03/z2ui5_cl_smpc_app_188.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.30`
        is_post171 = abap_true
        notes = text1
        post171 = `the sap.m.Avatar control (since UI5 1.73) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.73 to render it. @since verified in sap/m/Avatar.js:99 (control-level, which` &&
                 ` the member-level property gate never saw).` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 217/401/402/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'). All fifteen blocks aggregations hold one` &&
            ` sample:mySimpleBlock each - a BlockBase wrapper whose only job is to lazy-load the static view/blocks/mySimpleBlock.view.xml. That view is a forms:SimpleForm (maxContainerCols 2,` &&
            ` ResponsiveGridLayout, editable false) with a Label 'Content' and a Text 'some content goes here...', and it is inlined into each of the fifteen. That is the whole missing/extra pairing:` &&
            ` forms:SimpleForm 2 vs 15, Label / Text 2 vs 0 against m:Label / m:Text 0 vs 15 - the block view defaults to xmlns="sap.m" while the port's one view defaults to sap.uxap, so the same controls carry a` &&
            ` prefix, and the archived-once template is emitted fifteen times. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's` &&
            ` refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried`.
    text1 = text1 && ` NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with` &&
            ` it. // NOTE: view/blocks/myBlock.view.xml is DEAD in this sample: AnchorBar.view.xml references only sample:mySimpleBlock, never sample:myBlock. Its core:Title 'my Block' is the whole core:Title 1 vs` &&
            ` 0 line, and its own Label / Text are one of the two counted pairs. Nothing was dropped from the rendered page. // NOTE: mySimpleBlock.view.xml wraps its SimpleForm in an <html:div style="font-size:` &&
            ` 0.875rem;"> from the XHTML namespace. abap2UI5 serves sap.* controls, not raw XHTML, so the wrapper and its inline font-size go; the form itself renders identically. Each block view also carries` &&
            ` width="100%" on its own mvc:View root (the View.width line) - the port has one view, and its root carries the sample's height="100%" instead. // NOTE: the sample's subject is what the anchor bar does`.
    text1 = text1 && ` with FIFTEEN sections: section 2 has no subsection at all (an anchor without a target), sections 1 has two, and nine of the fifteen give their single subsection the title " " - a single space, which` &&
            ` is what keeps the subsection title row empty while the anchor still reads 'Section n'. All of that is carried literally, trailing blanks included ('Subsection 1.1 ', 'Subsection 4.1 '), because it is` &&
            ` the point. Only the first seven sections and their subsections carry ids in the original; so do they here. // NOTE: the fifteen BlockBase ids (bbt1..bbt5, b1..b10) have no counterpart: they identify` &&
            ` the wrapper control the port inlines away, and nothing in the sample addresses them.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `AnchorBar`                                     class = `z2ui5_cl_smpc_app_586` path = `src/01/03/z2ui5_cl_smpc_app_586.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 178/161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the original blocks aggregations each hold a custom BlockBase` &&
            ` control blockcolor:BlockBlue (from the sample's SharedBlocks JS, xmlns:blockcolor='sap.uxap.sample.SharedBlocks'), used 4 times with the id attributes bbt1/bbt2/bbt3/bbt4. A BlockBase is only a` &&
            ` lazy-loading wrapper around a view; BlockBlue's rendered content is a single coloured div (<html:div style='height:4em; background-color: #A9EAFF ;'/>). Since ObjectPageSubSection.blocks accepts any` &&
            ` sap.ui.core.Control, each blockcolor:BlockBlue is inlined as a core:HTML leaf carrying that div in its content attribute - the whole ObjectPage renders with the thin generic frontend, no custom JS` &&
            ` control. Consequently all four blockcolor:BlockBlue controls (and their id attributes bbt1/bbt2/bbt3/bbt4) are absent from the port and four core:HTML controls are present in their place. The empty` &&
            ` BlockBlueCtrl controller (a no-op onParentBlockModeChange stub) carries no behaviour to port. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution`.
    text1 = text1 && ` (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31` &&
            ` sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the` &&
            ` block ids and view roots go with it.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `AnchorBarNoPopover`                            class = `z2ui5_cl_smpc_app_187` path = `src/01/03/z2ui5_cl_smpc_app_187.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold the SAME fourteen` &&
            ` SharedBlocks BlockBase controls app 401 carries - goals:GoalsBlock, personal:BlockAdresses / BlockPhoneNumber / BlockSocial / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2,` &&
            ` employment:BlockJobInfoPart1 / BlockJobInfoPart2 / BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob. Each is only a lazy-loading wrapper around` &&
            ` a static view (a forms:SimpleForm with core:Title / m:Label / m:Text, or a layout:Grid / layout:VerticalLayout / layout:HorizontalLayout tree), so every block view is inlined 1:1 into its` &&
            ` aggregation. Absent as a result: the fourteen BlockBase controls with their ids and their columnLayout / showSubSectionMore properties, plus the block views' own mvc:View roots. Present in their` &&
            ` place: forms:SimpleForm 12, core:Title 7, layout:Grid 3, layout:GridData 3 and the additional m:Label / m:Text / layout:VerticalLayout / layout:HorizontalLayout those views contain. The block content`.
    text1 = text1 && ` itself is byte-for-byte what app 401 inlines - the two samples share the SharedBlocks folder. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution` &&
            ` (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31` &&
            ` sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the` &&
            ` block ids and view roots go with it. // IMPROVISED: EmploymentBlockJob is a two-view BlockBase (Collapsed / Expanded, selected by the block mode, with showSubSectionMore='true' offering the toggle)` &&
            ` whose views bind six internal named models emp1>..emp6> that six uxap:ModelMapping elements map onto ObjectPageModel>/Employee/0..5. abap2UI5 serves one default model and has no BlockBase mode` &&
            ` toggle, so the Collapsed view (the block's initial state, two employees) is inlined and emp1>/emp2> are folded onto ONE table (t_employees, the row type name/job) whose rows the view addresses`.
    text1 = text1 && ` individually - client->_bind( val = t_employees[ N ]-field tab = t_employees tab_index = N ), rendering {/T_EMPLOYEES/(N-1)/FIELD} (app 230/261/401/263 precedent). So the ModelMapping indirection is` &&
            ` what goes, not the /Employee array shape the elements address. Lost: the six ModelMapping config controls (the ModelMapping 6 vs 0 line), the Expanded view with employees 3-6, the more/less toggle` &&
            ` behind showSubSectionMore, and the mapping indirection - the two records resolve statically to HRData.json /Employee/0 (Michael Adams, Scrum Master) and /Employee/1 (John Miller, Product Owner). //` &&
            ` POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar` &&
            ` (displaySize='L'), both on sap-icon://picture rather than an image file here. Needs a UI5 runtime >= 1.73. // NOTE: what this sample adds over app 401 (same blocks, same data) is the anchor-bar` &&
            ` NUMBERS: two of the three section titles end in a parenthesised count - 'Personal (2)' and 'Employment (3)' - which is how the sample shows that the anchor bar simply renders the title it is given,`.
    text1 = text1 && ` subsection count included, rather than computing anything. Both are carried literally. Its ObjectPageLayout also sets subSectionLayout='TitleOnLeft' where app 401 leaves the default, and neither` &&
            ` section carries the heading MessageStrip or the importance levels app 401's sample writes. // NOTE: in the Connect subsection the four personal blocks are DIRECT children of the ObjectPageSubSection` &&
            ` rather than nested in a <blocks> element - blocks is that control's default aggregation, so the two forms are identical. The port writes the aggregation explicitly, and keeps the sample's own order` &&
            ` (Adresses, PhoneNumber, Social, Mailing), which differs from app 401's. // NOTE: The two asset paths (linkedin.png and Twitter.png) are absolutized to the sdk.openui5.org host per the offline` &&
            ` asset-URL rule (corrected 2026-08-24). They were kept in the original's './test-resources/...' form, justified as "matching app 261" - which is false: 261 was rewritten to the absolute form on` &&
            ` 2026-08-23 for exactly this reason, and its own sidecar records that both icons had rendered broken. A relative path resolves against the demo kit's document root; an abap2UI5 app is served from the`.
    text1 = text1 && ` ABAP ICF node and has no such root, so the request 404s and the control falls back to its placeholder. No gate sees it - data-fidelity only rejects a non-OpenUI5 HOST, and a relative path has none.` &&
            ` // NOTE: not yet verified in a running system: that the anchor bar lists the three section titles with their numbers and scrolls to each section. **e2e-verified 2026-08-25** (nightly e2e interaction,` &&
            ` meta/interactions/z2ui5_cl_smpc_app_587.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `AnchorBarWithNumbers`                          class = `z2ui5_cl_smpc_app_587` path = `src/02/03/z2ui5_cl_smpc_app_587.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'), both` &&
                 ` on sap-icon://picture rather than an image file here. Needs a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 263/401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold fifteen SharedBlocks` &&
            ` BlockBase controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 /` &&
            ` BlockJobInfoPart2 / BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob and connections:ConnectionsBlock. Each is only a lazy-loading wrapper` &&
            ` around a static view, so every block view is inlined 1:1 into its aggregation (forms:SimpleForm with core:Title / m:Label / m:Text, the layout:VerticalLayout / layout:HorizontalLayout trees of the` &&
            ` personal blocks, the layout:Grid / layout:GridData tree of EmploymentBlockJobCollapsed, and the six m:Panel / m:VBox / m:Image of ConnectionsBlock). Absent as a result: the fifteen BlockBase controls` &&
            ` with their ids and their columnLayout / showSubSectionMore properties, plus the block views' own mvc:View roots. This sample's section tree is IDENTICAL to app 263's - same fifteen blocks, same`.
    text1 = text1 && ` section and subsection ids, same titles - so the two ports carry the same inlined content. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution` &&
            ` (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31` &&
            ` sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the` &&
            ` block ids and view roots go with it. // IMPROVISED: Named-model fold (app 230/263 precedent): EmploymentBlockJob and ConnectionsBlock each carry six uxap:ModelMapping elements mapping` &&
            ` ObjectPageModel>/Employee/0..5 onto the internal models emp1>..emp6>. abap2UI5 serves one default model, so the twelve ModelMapping config controls are gone (the ModelMapping 12 vs 0 line) and the` &&
            ` six employee records are seeded as ONE table (t_employees, the row type name/job/picture) and each block binds the cell it needs - client->_bind( val = t_employees[ N ]-field tab = t_employees`.
    text1 = text1 && ` tab_index = N ), which renders as {/T_EMPLOYEES/(N-1)/FIELD}. So what is lost is the ModelMapping indirection, not the /Employee ARRAY the elements address: the model keeps that shape and only the` &&
            ` named-model names are gone. The cell is matched by data reference, which an abaplint downport older than 2.120.51 lost for a component-level table expression (it outlined into a work area); that` &&
            ` lowering is ASSIGNING upstream since abaplint/abaplint#4276, so the port writes the natural t_employees[ N ]-field and needs no field symbols. EmploymentBlockJob is additionally a two-view BlockBase` &&
            ` (Collapsed / Expanded with showSubSectionMore='true'); only the Collapsed view (employees 1-2) is inlined - the Expanded view with employees 3-6 and the more/less toggle are lost. // POST-1.71:` &&
            ` sap.uxap.ObjectPageLayout.beforeNavigate is @since 1.118 - newer than the 1.71 floor but kept for the 1:1 port, since the event IS this sample. The app needs a UI5 release >= 1.118 for the guard to` &&
            ` fire at all; below it the anchor bar simply navigates. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the`.
    text1 = text1 && ` snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs a UI5 runtime >= 1.73. // NOTE: onBeforeNavigate calls oEvent.preventDefault() whenever the page is in edit mode. That veto` &&
            ` is expressible: the wire carries s_ctrl-check_prevent_default (app 241 idiom), which is baked per WIRE at RENDER time - and here that is exactly right, because the flag it needs is bEditMode, which` &&
            ` the backend owns. onEdit therefore redraws the view, so every fresh beforeNavigate wire carries the veto that matches the mode the toast just announced. The event is still sent when vetoed, so the` &&
            ` backend stays in charge of what happens instead, which is the sample's whole point. // NOTE: the controller keeps oSelectedSection / oPreviousSelectedSection as CONTROL references and compares them` &&
            ` by identity; the port keeps the two section IDs instead and compares those - ${$parameters>/section}.getId() is what travels with the event. The OK button's oOPL.setSelectedSection(oSelectedSection)` &&
            ` becomes follow_up_action( control_by_id, ObjectPageLayout / setSelectedSection / <section id> ), the controlIdOrNull argument kind resolving the id back to the control exactly like the original's`.
    text1 = text1 && ` reference (app 263 precedent). Cancel reverts the remembered id, as the original reverts the reference. // NOTE: the confirm Dialog is built imperatively by the controller (new Dialog({title,` &&
            ` content, beginButton, endButton}), added as a view dependent and reused on every later veto). The port expresses it as a core:FragmentDefinition shown through client->popup_display (app 300 idiom)` &&
            ` and destroyed on either button. Its attachAfterClose handler - oSelectedSection.getDomRef().focus() - has no counterpart: it reaches into the section's DOM node, which a thin frontend never holds. //` &&
            ` NOTE: the four asset paths (the imageID_275314.png avatar twice, linkedin.png, Twitter.png) and HRData.json's person.png point at the sdk.openui5.org host per the offline asset-URL rule, matching app` &&
            ` 263 which carries the same view; the original writes them relative as './test-resources/sap/uxap/images/...'. // NOTE: not yet verified in a running system: that Edit toasts the mode and re-bakes the` &&
            ` veto, that an icon-tab press in edit mode is cancelled and raises the dialog, and that OK then navigates through setSelectedSection while Cancel leaves the page where it was. **e2e-verified`.
    text1 = text1 && ` 2026-08-25**, before the 2026-08-27 render-time re-issue - that half needs a fresh e2e build before the nightly can confirm it; (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_588.mjs).` &&
            ` // NOTE: The Connections subsection carries title="Connections" as the original does. The port's ObjectPageLayout subtree was built from app 263, whose own sample has no title on that subsection, so` &&
            ` the attribute was lost in the copy. structural-diff cannot see it: attribute presence is a union per control TYPE, and other ObjectPageSubSection instances do carry title. // NOTE: The six` &&
            ` ConnectionsBlock Panels are built in a loop (the app 520 idiom): one DO 6 TIMES over a sub-handle of the Connections blocks aggregation, each Panel binding its t_employees row with client->_bind( tab` &&
            ` / tab_index ) on the row index sy-index gives. structural-diff therefore reads the port as dynamic - control counts built in a loop cannot match statically. The rendered view is identical to the` &&
            ` unrolled form it replaced (both expanded and diffed, 2026-09-12): a rewrite of the source, not of the behaviour, so the status is untouched.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageBeforeNavigate`                      class = `z2ui5_cl_smpc_app_588` path = `src/02/03/z2ui5_cl_smpc_app_588.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.uxap.ObjectPageLayout.beforeNavigate is @since 1.118 - newer than the 1.71 floor but kept for the 1:1 port, since the event IS this sample. The app needs a UI5 release >= 1.118 for the guard to` &&
                 ` fire at all; below it the anchor bar simply navigates. // sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading` &&
                 ` avatar and the headerContent avatar (displaySize='L'). Needs a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: the sample's subject is that ONE BlockBase can be authored as four different view types, and it shows the same goals block four times: goals:GoalsBlockJS (a TYPED view, GoalsBlockView.js,` &&
            ` which builds its SimpleForm in createContent), goals:GoalsBlockJSON (GoalsBlock.view.json, a sap.ui.core.mvc.JSONView), goals:GoalsBlockHTML (GoalsBlock.view.html, an HTMLView with data-sap-ui-type` &&
            ` divs) and goals:GoalsBlock (GoalsBlock.view.xml, a plain XMLView). abap2UI5 emits ONE XML view and has no notion of a view type at all, so the four TYPES collapse to one: what each block RENDERS is` &&
            ` inlined into its section, and the four section titles ('Typed View', 'JSON View', 'HTML View', 'XML View') are all that is left of the distinction. That is the whole missing/extra pairing - four` &&
            ` goals:* blocks out, two forms:SimpleForm plus the JSON view's Image / Button, the HTML view's Panel / Button and the typed view's Button in. // IMPROVISED: GoalsBlock.view.html is not a control tree` &&
            ` at all: it is an HTML <template> whose plain <div> carries the sentence 'This is the content of an HTML view', with a second <div data-sap-ui-type="sap.m.Panel"> holding a`.
    text1 = text1 && ` data-sap-ui-type="sap.m.Button". The two sap.m controls port 1:1 (m:Panel id='myPanel' with m:Button id='Button1'); the bare <div> has no counterpart, so its sentence is carried as an m:Text - the` &&
            ` text the browser shows, without the raw element. // NOTE: the typed view and the XML view render nearly the same three goals, and the difference between them is carried literally: GoalsBlockView.js` &&
            ` sets width='100%' on its SimpleForm, adds a Button ('Hello from a typed view') and two spacer m:Texts of a single blank, and spells the first goal 'Evangelize the UI framework accross the company' -` &&
            ` with the sample's own typo, where GoalsBlock.view.xml spells it 'across'. Both are kept as written. // NOTE: all four blocks share GoalsBlockController, whose onBtnPress toasts 'Button was presed'` &&
            ` (the sample's spelling). The three buttons that use it, and the two breadcrumb links, are constant-text toasts, so they are wired round-trip-free as follow_up_action( control_global, MESSAGE_TOAST` &&
            ` show ) (app 005/401 idiom). // NOTE: GoalsBlock.view.json's Image points at http://www.sap.com/global/ui/images/global/sap-logo.png - a marketing asset on a non-UI5 host, over plain http, which the`.
    text1 = text1 && ` repo's asset rule does not allow a port to carry. It is replaced by ``logo_ui5.png`` on the sanctioned sdk.openui5.org host (/resources/sap/ui/documentation/sdk/images/logo_ui5.png), which is the` &&
            ` only asset value in this port that the sample's own files do not carry; the sample's point (that a JSON view can declare an Image at all) is unaffected. The remaining three assets` &&
            ` (imageID_275314.png, linkedin.png, Twitter.png) point at sdk.openui5.org per the offline asset-URL rule; the original writes them relative. // POST-1.71:` &&
            ` sap.uxap.ObjectPageAccessibleLandmarkInfo.headerContentLabel is @since 1.127.0 - newer than the 1.71 floor but kept for the 1:1 port, since the sample sets every one of the eleven landmark properties` &&
            ` precisely to show what the aggregation does. The app needs a UI5 release >= 1.127 for that one label to be applied; the other ten work from 1.61. // NOTE: toggleFooter does` &&
            ` oObjectPageLayout.setShowFooter(!getShowFooter()) on the third ObjectPageHeaderActionButton. showFooter IS a bindable property, so the port binds it two-way and flips the ABAP flag in on_event rather`.
    text1 = text1 && ` than calling the setter through a frontend action - the bindable-property-beats-frontend-action rule (app 401 precedent). The showFooter attribute is therefore present where the original view does` &&
            ` not write it, and the footer starts hidden as it does in the original. // NOTE: showEditHeaderButton='true' is set but editHeaderButtonPress is NOT wired in this sample's view, even though the` &&
            ` controller defines handleEditBtnPress - upstream the edit-header button therefore does nothing. The port carries the same: the property is set, no handler is attached. // NOTE: not yet verified in a` &&
            ` running system: the four sections in the anchor bar, the three block buttons and the footer toggle. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_589.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageBlockViewTypes`                      class = `z2ui5_cl_smpc_app_589` path = `src/02/03/z2ui5_cl_smpc_app_589.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.uxap.ObjectPageAccessibleLandmarkInfo.headerContentLabel is @since 1.127.0 - newer than the 1.71 floor but kept for the 1:1 port, since the sample sets every one of the eleven landmark properties` &&
                 ` precisely to show what the aggregation does. The app needs a UI5 release >= 1.127 for that one label to be applied; the other ten work from 1.61.` ) ).

    text1 = `NOTE: Block->content inlining (app 401/416 precedent): the six 'Personal n' sections each hold one blockcolor:BlockBlue, a BlockBase around blocks/BlockBlue.view.xml whose entire body is <html:div` &&
            ` style="height:4em; background-color: #A9EAFF ; padding: 1rem">Arbitrary block content...</html:div>. abap2UI5 serves sap.* controls, not raw XHTML, so the div and its inline style go and the sentence` &&
            ` it shows is carried as an m:Text - the m:Text 2 vs 8 line, six sections' worth. The six blue boxes are only filler here; what the sample is about is the form in the seventh section. // IMPROVISED:` &&
            ` handleFocusBtnPress is pure DOM: it resolves the CURRENTLY SELECTED section (Element.getElementById(oObjectPageLayout.getSelectedSection())), hands its getDomRef() to sap/ui/dom/getFirstEditableInput` &&
            ` and focuses whatever comes back. A thin frontend has neither the selected section nor a DOM walk. What survives is the outcome the sample demonstrates: 'focus' IS whitelisted in CONTROL_METHODS, so` &&
            ` the button is a round-trip-free follow_up_action( control_by_id, nameInput / focus ) onto the Name field - the first editable input of the Employment section, and the only first-editable-input the`.
    text1 = text1 && ` original can ever produce, because the six Personal sections contain no input at all. Lost: pressing Focus while a Personal section is selected does nothing upstream, but jumps to the Employment` &&
            ` field here. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent` &&
            ` avatar (displaySize='L'). Needs a UI5 runtime >= 1.73. // NOTE: in the six Personal sections the ObjectPageSubSection is a DIRECT child of the section and the block a direct child of the subsection -` &&
            ` subSections and blocks are the two default aggregations, so the two forms are identical. The port writes both aggregations explicitly, as the sample's own seventh section does. // NOTE: the` &&
            ` Employment form is carried 1:1, ColumnLayout with columnsM=2 / columnsL=3 / columnsXL=4, the two f:ColumnElementData layoutData (cellsSmall 2/cellsLarge 1 and cellsSmall 3/cellsLarge 2) that make the` &&
            ` Street/No. and ZIP Code/City rows split, the three-item Country Select and the Url / Email / Tel input types. None of its Inputs is bound in the sample either - it is a layout demo, so the fields`.
    text1 = text1 && ` stay empty here too. // NOTE: the four asset paths (the imageID_275314.png avatar twice, linkedin.png and Twitter.png) point at the sdk.openui5.org host per the offline asset-URL rule; the original` &&
            ` writes them relative as './test-resources/sap/uxap/images/...'. // NOTE: not yet verified in a running system: that the Focus button moves the caret into the Employment form's Name field.` &&
            ` **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_590.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageFormFocusableInput`                  class = `z2ui5_cl_smpc_app_590` path = `src/02/03/z2ui5_cl_smpc_app_590.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs` &&
                 ` a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the two subsections hold ten BlockBase controls between them -` &&
            ` personal:PersonalFormBlock (columnLayout='3'), employment:BlockJobInfo (columnLayout='3') and eight blockcolor:BlockBlue fillers. Each is only a lazy-loading wrapper around a static view, so every` &&
            ` block view is inlined 1:1: PersonalFormBlock's forms:Form with its ColumnLayout, two FormContainers and seven FormElements, BlockJobInfo's forms:SimpleForm with core:Title 'General' and three Label /` &&
            ` Input pairs. Absent as a result: the ten BlockBase controls with their ids and columnLayout properties, plus the block views' own mvc:View roots. The Label / Input / Text 3 / 3 / 7 vs 0 lines against` &&
            ` m:Label / m:Input / m:Text are the same controls: the block views default to xmlns="sap.m" while the port's one view defaults to sap.uxap, so each carries the m: prefix here. Typed IMPROVISED since` &&
            ` 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a`.
    text1 = text1 && ` zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real` &&
            ` BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it. // NOTE: the eight blockcolor:BlockBlue blocks are one view whose entire` &&
            ` body is <html:div style="height:4em; background-color: #A9EAFF ; padding: 1rem">Arbitrary block content...</html:div>. abap2UI5 serves sap.* controls, not raw XHTML, so the div and its inline style` &&
            ` go and the sentence it shows is carried as an m:Text, eight times (app 590 carries the identical block six times). They are filler that makes the form share its subsection with something - what the` &&
            ` sample is about is the two form layouts. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading` &&
            ` avatar and the headerContent avatar (displaySize='L'). Needs a UI5 runtime >= 1.73. // NOTE: the sample declares three namespace prefixes - personal:, employment: and blockcolor: - that all resolve`.
    text1 = text1 && ` to the SAME package, sap.uxap.sample.ObjectPageFormLayout.controller.blocks. They are naming, not structure; the port names each inlined block in a comment instead. // NOTE: the two breadcrumb Links` &&
            ` call handleLink1Press / handleLink2Press, both constant-text MessageToasts, so they are wired round-trip-free as follow_up_action( control_global, MESSAGE_TOAST show ) (app 005/401 idiom). The four` &&
            ` header action buttons (Edit, Delete, Copy, Share) carry no handler in the sample either. // NOTE: the four asset paths (the imageID_275314.png avatar twice, linkedin.png and Twitter.png) point at the` &&
            ` sdk.openui5.org host per the offline asset-URL rule; the original writes them relative as './test-resources/sap/uxap/images/...'.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageFormLayout`                          class = `z2ui5_cl_smpc_app_591` path = `src/02/03/z2ui5_cl_smpc_app_591.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs` &&
                 ` a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/217 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold the SharedBlocks BlockBase` &&
            ` controls goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2. A BlockBase is only a lazy-loading wrapper around a view` &&
            ` and each of those views is a static forms:SimpleForm with core:Title / Label / Text children, so each block view is inlined 1:1 into its aggregation. Absent as a result: the seven BlockBase controls` &&
            ` with their ids (goalsblock, phone, social, adresses, mailing, part1, part2), the BlockBase columnLayout='1' of BlockMailing, PersonalBlockPart1 AND PersonalBlockPart2, and the block views' own` &&
            ` mvc:View roots. Corrected 2026-08-21: the list named only BlockMailing's columnLayout while the original sets it on three blocks, and an enumeration that reads as exhaustive is worse than none - app` &&
            ` 217, the precedent this cites, declares all three. Corrected 2026-08-21: the phrase used to add "with their width='100%'", which no SharedBlocks view supports - none carries a width on its mvc:View`.
    text1 = text1 && ` root; that width belongs to the inner SimpleForm and the port reproduces it. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and` &&
            ` the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that` &&
            ` row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view` &&
            ` roots go with it. // NOTE: The controller's three JSONModels (SharedJSONData/employee.json as 'ObjectPageModel', an inline 'buttons' model with text/icon, SharedJSONData/products.json as the default` &&
            ` OneWay model) are not reproduced: no control in this sample's view or in any of its inlined block views binds against them - every text is a literal. The port therefore has no model at all (no` &&
            ` model_init), which renders identically. // NOTE: preserveHeaderStateOnScroll='true' (@since 1.52) is the whole point of the sample - the header content staying expanded while scrolling on desktop.`.
    text1 = text1 && ` The property is passed through 1:1 but the scroll behaviour was not verified in a running system. **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser):` &&
            ` after scrolling the ObjectPage wrapper to 1500px the header content ('Cost Center') is still visible - that IS preserveHeaderStateOnScroll; without it the header snaps away.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageHeaderExpanded`                      class = `z2ui5_cl_smpc_app_260` path = `src/01/03/z2ui5_cl_smpc_app_260.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `NOTE: twenty-one sections, all identical: one ObjectPageSubSection in Expanded mode holding one ObjectPageLazyLoader with stashed='true', around a VBox with the same Address SimpleForm` &&
            ` (ResponsiveGridLayout, maxContainerCols 2, labelSpan 3, emptySpan 4, one column, the sapUxAPObjectPageSubSectionAlignContent class). All twenty-one are written out rather than looped, so the` &&
            ` twenty-one stashed loaders the anchor bar and the lazy loading act on are really there. The sample's subject is that lazy loading needs NO custom BlockBase - the stashed loader alone defers the` &&
            ` content - so nothing here is inlined away, which is what makes this port unusual among the uxap ones. // NOTE: the controller does bindElement('/SupplierCollection/0') on the view, so every binding` &&
            ` in the form is RELATIVE to that one supplier row. The port folds it to root fields (app 229 idiom): SUPPLIERNAME, STREET, HOUSENUMBER, ZIPCODE, CITY and COUNTRY, seeded from` &&
            ` sap/ui/demo/mock/supplier.json row 0 (Red Point Stores, Main St 1618, 31415 Maintown, Germany). The two composite bindings the sample writes - '{Street} {HouseNumber}' and '{ZIPCode} {City}' - stay`.
    text1 = text1 && ` composite, over the folded names. // NOTE: the mock carries eleven more fields on that row (Url, Twitter, Tel, Sms, Mobile, Pager, Fax, Email, Rating, Prime, Disposable) that this sample's form never` &&
            ` binds, so they are not modelled. // NOTE: The view is built in a loop (the app 520 idiom): the twenty-one identical ObjectPageSection blocks are one DO 21 TIMES over a sub-handle of the sections` &&
            ` aggregation, the title 'Section N' and the ids SectionN / SectionNstashed string templates over sy-index. structural-diff therefore reads the port as dynamic - control counts built in a loop cannot` &&
            ` match statically. The rendered view is identical to the unrolled form it replaced (both expanded and diffed, 2026-09-12): a rewrite of the source, not of the behaviour.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageLazyLoadingWithoutBlocks`            class = `z2ui5_cl_smpc_app_592` path = `src/01/03/z2ui5_cl_smpc_app_592.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 0 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/217/261 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold SharedBlocks BlockBase` &&
            ` controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 / BlockJobInfoPart2 /` &&
            ` BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob. Each BlockBase is only a lazy-loading wrapper around a static view (a forms:SimpleForm with` &&
            ` core:Title / m:Label / m:Text, or a layout:HorizontalLayout / layout:VerticalLayout tree), so every block view is inlined 1:1 into its aggregation. Absent as a result: the 14 BlockBase controls with` &&
            ` their ids and their columnLayout / showSubSectionMore properties, plus the block views' own mvc:View roots with their width='100%'. Present in their place: forms:SimpleForm, core:Title and the` &&
            ` additional m:Label / m:Text / layout:VerticalLayout / layout:HorizontalLayout the block views contain. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content`.
    text1 = text1 && ` substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render.` &&
            ` 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column` &&
            ` assignment, and the block ids and view roots go with it. // IMPROVISED: EmploymentBlockJob is a two-view BlockBase (Collapsed / Expanded, selected by the block mode, with showSubSectionMore='true'` &&
            ` offering the toggle) whose views bind six internal named models emp1>..emp6> that six uxap:ModelMapping elements map onto ObjectPageModel>/Employee/0..5. abap2UI5 serves one default model and has no` &&
            ` BlockBase mode toggle, so: the Collapsed view (the block's initial state, two employees - a layout:Grid / layout:VerticalLayout tree with layout:GridData layoutData) is inlined, and emp1>/emp2> are` &&
            ` folded onto ONE table (t_employees, the row type name/job) whose rows the view addresses individually - client->_bind( val = t_employees[ N ]-field tab = t_employees tab_index = N ), rendering`.
    text1 = text1 && ` {/T_EMPLOYEES/(N-1)/FIELD} (app 230/261/263 precedent). So the ModelMapping indirection is what goes, not the /Employee array shape the elements address. Lost: the six ModelMapping config controls,` &&
            ` the Expanded view with employees 3-6, the more/less toggle behind showSubSectionMore, and the mapping indirection - the two records are resolved statically to HRData.json /Employee/0 (Michael Adams,` &&
            ` Scrum Master) and /Employee/1 (John Miller, Product Owner). // POST-1.71: headerContentPinned on sap.uxap.ObjectPageLayout (since UI5 1.93) is newer than 1.71 but kept for the 1:1 port - the sample` &&
            ` opens with a pinned header content. The app needs a UI5 release >= 1.93 for it to take effect. // POST-1.71: the heading aggregation of sap.uxap.ObjectPageSection (since UI5 1.106) is newer than 1.71` &&
            ` but kept for the 1:1 port - the Goals and Personal sections each carry an m:MessageStrip in it. The app needs a UI5 release >= 1.106 to show those two strips. // POST-1.71: sap.m.Avatar is a control` &&
            ` @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'), both on the sample's own`.
    text1 = text1 && ` imageID_275314.png. Needs a UI5 runtime >= 1.73. // NOTE: The controller's three constant-text MessageToast handlers (handleLink1Press, handleLink2Press, handleEditBtnPress) are wired as` &&
            ` round-trip-free client toasts (follow_up_action cs_event-control_global MESSAGE_TOAST.show, app 005 idiom), so the two breadcrumb Links and the edit-header button behave exactly as in the original` &&
            ` without a backend round-trip. // NOTE: toggleFooter does oObjectPageLayout.setShowFooter(!getShowFooter()). showFooter IS a bindable property, so the port binds it two-way (client->_bind on the` &&
            ` abap_bool flag; the first draft wrote a static as_bool literal the round-trip could never reach, caught by the e2e interaction before its first run) and flips the ABAP flag in on_event instead of` &&
            ` calling the setter through a frontend action - the bindable-property-beats-frontend-action rule. The showFooter attribute is therefore present in the port where the original view does not write it,` &&
            ` and the footer starts hidden as it does in the original. The round-trip toggle itself is not verified in a running system yet. **e2e-verified 2026-08-21** (nightly e2e interaction,`.
    text1 = text1 && ` meta/interactions/z2ui5_cl_smpc_app_401.mjs). // NOTE: The four asset paths (the imageID_275314.png avatar twice, linkedin.png and Twitter.png) are absolutized to the sdk.openui5.org host per the` &&
            ` offline asset-URL rule (corrected 2026-08-24). They were kept in the original's './test-resources/...' form, justified as "matching app 261" - which is false: 261 was rewritten to the absolute form` &&
            ` on 2026-08-23 for exactly this reason, and its own sidecar records that both icons had rendered broken. A relative path resolves against the demo kit's document root; an abap2UI5 app is served from` &&
            ` the ABAP ICF node and has no such root, so the request 404s and the control falls back to its placeholder. No gate sees it - data-fidelity only rejects a non-OpenUI5 HOST, and a relative path has` &&
            ` none.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageOnJSON`                              class = `z2ui5_cl_smpc_app_401` path = `src/02/03/z2ui5_cl_smpc_app_401.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `headerContentPinned on sap.uxap.ObjectPageLayout (since UI5 1.93) is newer than 1.71 but kept for the 1:1 port - the sample opens with a pinned header content. The app needs a UI5 release >= 1.93 for` &&
                 ` it to take effect. // the heading aggregation of sap.uxap.ObjectPageSection (since UI5 1.106) is newer than 1.71 but kept for the 1:1 port - the Goals and Personal sections each carry an` &&
                 ` m:MessageStrip in it. The app needs a UI5 release >= 1.106 to show those two strips. // sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in` &&
                 ` scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'), both on the sample's own imageID_275314.png. Needs a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): all eleven sections hold the SAME employment:EmploymentBlockJob, a` &&
            ` lazy-loading BlockBase wrapper around a static view, so that view is inlined into each of the eleven (the layout:Grid / layout:VerticalLayout / layout:HorizontalLayout tree with its layout:GridData` &&
            ` layoutData and the four m:Label the two employee cards show). Absent as a result: the eleven BlockBase controls with their ids, and the block view's own mvc:View root. Eleven copies of one heavy` &&
            ` block is the sample: a page long enough that enableLazyLoading is worth watching. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)",` &&
            ` and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing` &&
            ` that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and`.
    text1 = text1 && ` view roots go with it. // IMPROVISED: Named-model fold (app 230/401 precedent): each of the eleven blocks carries six uxap:ModelMapping elements mapping ObjectPageModel>/Employee/0..5 onto the` &&
            ` internal models emp1>..emp6> - sixty-six config controls in all, none of which has a counterpart, because abap2UI5 serves one default model. EmploymentBlockJob is also a two-view BlockBase (Collapsed` &&
            ` / Expanded): the Collapsed view is the initial one and the only one inlined, so employees 1 and 2 are seeded as default-model root fields bound via client->_bind and employees 3-6, together with the` &&
            ` more/less toggle, are lost. // POST-1.71: sap.uxap.ObjectPageLayout.headerContentPinned is @since 1.93 - newer than the 1.71 floor but kept for the 1:1 port; the sample opens with a pinned header` &&
            ` content. The app needs a UI5 release >= 1.93 for it to take effect. // NOTE: the ids the sample gives its eleven blocks run employmentblockjob1, 2, 4, 5, 6, 7, 8, 9, 10, 11 and then 3 - the numbering` &&
            ` skips 3 in place and picks it up at the end, an upstream slip with no effect. The port inlines the blocks away, so no id travels. // NOTE: The view is built in a loop (the app 520 idiom): the eleven`.
    text1 = text1 && ` identical ObjectPageSection blocks, each with the same inlined EmploymentBlockJob, are one DO 11 TIMES over a sub-handle of the sections aggregation, the title 'Section N' a string template over` &&
            ` sy-index. structural-diff therefore reads the port as dynamic - control counts built in a loop cannot match statically. The rendered view is identical to the unrolled form it replaced (both expanded` &&
            ` and diffed, 2026-09-12): a rewrite of the source, not of the behaviour.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageOnJSONWithLazyLoading`               class = `z2ui5_cl_smpc_app_593` path = `src/02/03/z2ui5_cl_smpc_app_593.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.uxap.ObjectPageLayout.headerContentPinned is @since 1.93 - newer than the 1.71 floor but kept for the 1:1 port; the sample opens with a pinned header content. The app needs a UI5 release >= 1.93` &&
                 ` for it to take effect.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/217 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold SharedBlocks BlockBase` &&
            ` controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 / BlockJobInfoPart2 /` &&
            ` BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob, connections:ConnectionsBlock. Each is only a lazy-loading wrapper around a view, so every` &&
            ` block view is inlined 1:1 into its aggregation (forms:SimpleForm with core:Title / Label / Text, a layout:HorizontalLayout / layout:VerticalLayout tree, the layout:Grid / layout:GridData tree of` &&
            ` EmploymentBlockJobCollapsed, and the six m:Panel / m:VBox / m:Image of ConnectionsBlock). Absent as a result: the 15 BlockBase controls with their ids and their BlockBase columnLayout='1' properties,` &&
            ` and the block views' own mvc:View roots. Corrected 2026-08-21: the count said 14 while the list and the original both carry 15 - the same reuse-without-re-deriving that got 261 wrong. Typed`.
    text1 = text1 && ` IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares -` &&
            ` even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a` &&
            ` real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it. // IMPROVISED: Named-model fold (app 230 precedent):` &&
            ` EmploymentBlockJob and ConnectionsBlock each carry six uxap:ModelMapping elements mapping an external model onto the internal models emp1>..emp6> (/Employee/0..5). abap2UI5 serves one default model,` &&
            ` so the twelve ModelMapping config controls are gone and the six employee records are seeded as ONE table (t_employees, the row type name/job/picture) and each block binds the cell it needs -` &&
            ` client->_bind( val = t_employees[ N ]-field tab = t_employees tab_index = N ), which renders as {/T_EMPLOYEES/(N-1)/FIELD}. So what is lost is the ModelMapping indirection, not the /Employee ARRAY`.
    text1 = text1 && ` the elements address: the model keeps that shape and only the named-model names are gone. The cell is matched by data reference, which an abaplint downport older than 2.120.51 lost for a` &&
            ` component-level table expression (it outlined into a work area); that lowering is ASSIGNING upstream since abaplint/abaplint#4276, so the port writes the natural t_employees[ N ]-field and needs no` &&
            ` field symbols. EmploymentBlockJob is additionally a two-view BlockBase (Collapsed / Expanded with showSubSectionMore='true'); only the Collapsed view (employees 1-2) is inlined - the Expanded view` &&
            ` with employees 3-6 and the more/less toggle are lost. // NOTE: The ModelMapping elements in this sample name externalModelName='data' while the controller registers the JSONModel as 'ObjectPageModel'` &&
            ` - upstream those block labels therefore resolve to nothing. The port seeds the evidently intended SharedJSONData/HRData.json /Employee rows (Michael Adams / Scrum Master, John Miller / Product Owner,` &&
            ` Richard Wilson / Ux Designer, Julie Armstrong / Quality Engineer, Denise Smith / Team Member, Richard Adams / Team Member) and the person.png picture, i.e. it renders the data the sample means rather`.
    text1 = text1 && ` than the empty labels the name mismatch produces. // NOTE: onNavigate's setSelectedSection(null): sap.uxap.ObjectPageLayout.selectedSection is an ASSOCIATION, so it cannot be data-bound and the reset` &&
            ` must go through the frontend action. Since 2026-08-05 an EMPTY argument reaches the setter as null (the controlIdOrNull argument kind, pr/control-method-null-arg; the framework pads a trailing empty` &&
            ` argument for that kind, because the wire drops trailing empties), so the NAVIGATE round-trip - the NavContainer's navigate event, transporting ${$parameters>/toId} - issues follow_up_action(` &&
            ` control_by_id, ObjectPageLayout / setSelectedSection / ```` ) and clears the association exactly like the original. The earlier substitute (naming the first section 'goals', which is what UI5's` &&
            ` _adjustSelectedSectionByUXRules falls back to) is gone. The checkbox that guards the reset is two-way bound ({/RESET_CHECK}, seeded true like the original selected='true'), so the backend can read it` &&
            ` without touching the DOM. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the`.
    text1 = text1 && ` headerContent avatar (displaySize='L'). Needs a UI5 runtime >= 1.73. // NOTE: The page navigation is roundtrip-free follow_up_action( control_by_id, navigationContainer / to / page1|page2 ) - the` &&
            ` client-side equivalent of the controller's _navTo (oNavContainer.to(oPage)); 'to' is whitelisted in CONTROL_METHODS. Neither the navigation, the NAVIGATE round-trip nor the setSelectedSection` &&
            ` follow-up was verified in a running system. **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the 'To ObjectPage' list item navigates to page 2` &&
            ` (NavContainer.to via follow_up_action control_by_id, ObjectPage with 'Denise Smith' renders) and the nav-back button returns to page 1. Residual: the setSelectedSection reset itself (NAVIGATE` &&
            ` round-trip with the checkbox ticked) is not asserted - the page has no icon tab bar, so the selected section has no stable DOM marker. // NOTE: The Avatar / Image src values point at the` &&
            ` sdk.openui5.org host (imageID_275314.png, linkedin.png, Twitter.png, person.png) per the offline asset-URL rule; the original and HRData.json use the relative ./test-resources path. // NOTE: The six`.
    text1 = text1 && ` ConnectionsBlock Panels are built in a loop (the app 520 idiom): one DO 6 TIMES over a sub-handle of the Connections blocks aggregation, each Panel binding its t_employees row with client->_bind( tab` &&
            ` / tab_index ) on the row index sy-index gives. structural-diff therefore reads the port as dynamic - control counts built in a loop cannot match statically. The rendered view is identical to the` &&
            ` unrolled form it replaced (both expanded and diffed, 2026-09-12): a rewrite of the source, not of the behaviour, so the reviewed status is untouched.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageResetSelectedSection`                class = `z2ui5_cl_smpc_app_263` path = `src/02/03/z2ui5_cl_smpc_app_263.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close` &&
                 ` look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs` &&
                 ` a UI5 runtime >= 1.73.` ) ).

    text1 = `POST-1.71: sap.uxap.ObjectPageLayout.breakpointChange (@since 1.147, incl. its currentRange / currentWidth parameters) is the whole point of this sample and is wired 1:1 as a view attribute (an added` &&
            ` attr - the original attaches it imperatively in onInit via attachBreakpointChange): BREAKPOINT_CHANGE transports ${$parameters>/currentRange} and ${$parameters>/currentWidth}, the backend maps` &&
            ` Phone->M / Tablet->L / Desktop and DesktopExtraLarge->XL into the two-way bound Avatar displaySize ({/AVATAR_SIZE}, an added attribute on both Avatars, seeded S, the control's own default) and toasts` &&
            ` 'Media Range: <range> (<width>px) / Avatar Size: <size>' exactly like onBreakpointChange. Same wiring as app 244 (the sap.f.DynamicPage twin of this sample). Requires a UI5 release >= 1.147; on older` &&
            ` releases the event never fires and both Avatars keep the seeded L. **Corrected 2026-08-23**: the port seeded L, quoting the controller's default branch - but that branch lives inside` &&
            ` _getAvatarSizeForRange, which onBreakpointChange calls; it is never the initial size. sap.m.Avatar.displaySize defaults to S and the original view sets nothing, so the first paint showed L where the`.
    text1 = text1 && ` sample shows S - and on a runtime below 1.147, where breakpointChange never fires, it stayed L forever. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity` &&
            ` sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar (id snappedAvatar) and the headerContent avatar (id headerAvatar). Needs a UI5 runtime >= 1.73. // NOTE: showFooter is two-way bound` &&
            ` ({/SHOW_FOOTER}, seeded false = the original's ObjectPageLayout default, since the sample sets no showFooter attribute) and the Toggle Footer button flips it on a round-trip - the faithful abap2UI5` &&
            ` form of the controller's setShowFooter(!getShowFooter()); a scalar literal -> binding is not a structural diff. The two breadcrumb Link presses and the editHeaderButtonPress raise client-composed` &&
            ` MessageToasts via follow_up_action control_global MESSAGE_TOAST (roundtrip-free, apps 005/060/244). Footer round-trip, toast wiring and the editHeaderButtonPress attribute are unverified in a running` &&
            ` system. **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the Toggle Footer press flips the two-way bound showFooter over a real round-trip (the`.
    text1 = text1 && ` footer with Accept/Reject appears), and the breadcrumb entry raises its client-composed toast 'Page 1 a very long link clicked' (at this viewport the Breadcrumbs collapse into a Select, so the entry` &&
            ` is picked from its list). Residual: editHeaderButtonPress - there is no pencil button to press, so its toast stays a human check. **Corrected 2026-08-23**: showEditHeaderButton is honoured only for a` &&
            ` classic sap.uxap.ObjectPageHeader - ObjectPageLayout says so in its own JSDoc, and only ObjectPageHeaderContentRenderer draws the button. This sample uses ObjectPageDynamicHeaderTitle, whose content` &&
            ` class renders no edit button at all, so editHeaderButtonPress can never fire on either side. The port copies both attributes 1:1 and is faithful; the residual parked here was unclosable by any human` &&
            ` check. // NOTE: Both Avatar src values point at the sdk.openui5.org host (https://sdk.openui5.org/test-resources/sap/uxap/images/imageID_275314.png) per the offline asset-URL rule; the original uses` &&
            ` the relative ./test-resources path. Literal src values are not compared by structural-diff.`.
    text2 = `sap.uxap.ObjectPageLayout.breakpointChange (@since 1.147, incl. its currentRange / currentWidth parameters) is the whole point of this sample and is wired 1:1 as a view attribute (an added attr - the` &&
            ` original attaches it imperatively in onInit via attachBreakpointChange): BREAKPOINT_CHANGE transports ${$parameters>/currentRange} and ${$parameters>/currentWidth}, the backend maps Phone->M /` &&
            ` Tablet->L / Desktop and DesktopExtraLarge->XL into the two-way bound Avatar displaySize ({/AVATAR_SIZE}, an added attribute on both Avatars, seeded S, the control's own default) and toasts 'Media` &&
            ` Range: <range> (<width>px) / Avatar Size: <size>' exactly like onBreakpointChange. Same wiring as app 244 (the sap.f.DynamicPage twin of this sample). Requires a UI5 release >= 1.147; on older` &&
            ` releases the event never fires and both Avatars keep the seeded L. **Corrected 2026-08-23**: the port seeded L, quoting the controller's default branch - but that branch lives inside` &&
            ` _getAvatarSizeForRange, which onBreakpointChange calls; it is never the initial size. sap.m.Avatar.displaySize defaults to S and the original view sets nothing, so the first paint showed L where the`.
    text2 = text2 && ` sample shows S - and on a runtime below 1.147, where breakpointChange never fires, it stayed L forever. // sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity` &&
            ` sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar (id snappedAvatar) and the headerContent avatar (id headerAvatar). Needs a UI5 runtime >= 1.73.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageResponsiveAvatar`                    class = `z2ui5_cl_smpc_app_262` path = `src/02/03/z2ui5_cl_smpc_app_262.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted, live-test). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 263/401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold fifteen SharedBlocks` &&
            ` BlockBase controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 /` &&
            ` BlockJobInfoPart2 / BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob and connections:ConnectionsBlock. Each is only a lazy-loading wrapper` &&
            ` around a static view, so every block view is inlined 1:1 into its aggregation (forms:SimpleForm with core:Title / m:Label / m:Text, the layout:VerticalLayout / layout:HorizontalLayout trees of the` &&
            ` personal blocks, the layout:Grid / layout:GridData tree of EmploymentBlockJobCollapsed, and the six m:Panel / m:VBox / m:Image of ConnectionsBlock). Absent as a result: the fifteen BlockBase controls` &&
            ` with their ids and their columnLayout / showSubSectionMore properties, plus the block views' own mvc:View roots. This sample's section tree is IDENTICAL to apps 263 and 588 - same fifteen blocks,`.
    text1 = text1 && ` same section and subsection ids, same titles - so the three ports carry the same inlined content. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution` &&
            ` (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31` &&
            ` sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the` &&
            ` block ids and view roots go with it. // IMPROVISED: Named-model fold (app 230/263 precedent): EmploymentBlockJob and ConnectionsBlock each carry six uxap:ModelMapping elements mapping` &&
            ` ObjectPageModel>/Employee/0..5 onto the internal models emp1>..emp6>. abap2UI5 serves one default model, so the twelve ModelMapping config controls are gone (the ModelMapping 12 vs 0 line) and the` &&
            ` six employee records are seeded as ONE table (t_employees, the row type name/job/picture) and each block binds the cell it needs - client->_bind( val = t_employees[ N ]-field tab = t_employees`.
    text1 = text1 && ` tab_index = N ), which renders as {/T_EMPLOYEES/(N-1)/FIELD}. So what is lost is the ModelMapping indirection, not the /Employee ARRAY the elements address: the model keeps that shape and only the` &&
            ` named-model names are gone. The cell is matched by data reference, which an abaplint downport older than 2.120.51 lost for a component-level table expression (it outlined into a work area); that` &&
            ` lowering is ASSIGNING upstream since abaplint/abaplint#4276, so the port writes the natural t_employees[ N ]-field and needs no field symbols. EmploymentBlockJob is additionally a two-view BlockBase` &&
            ` (Collapsed / Expanded with showSubSectionMore='true'); only the Collapsed view (employees 1-2) is inlined - the Expanded view with employees 3-6 and the more/less toggle are lost. // POST-1.71:` &&
            ` sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs` &&
            ` a UI5 runtime >= 1.73. // NOTE: what this sample adds over apps 263 and 588, which carry the identical section tree, is one attribute: selectedSection='personal'. With useIconTabBar='true' the page`.
    text1 = text1 && ` therefore opens on the SECOND tab rather than the first. selectedSection is an ASSOCIATION, so it cannot be data-bound - but it CAN be set statically by id in the view, which is exactly what the` &&
            ` sample does and what the port carries. (App 263 needed the frontend action only because it RESETS the association at runtime.) // NOTE: the four asset paths (the imageID_275314.png avatar twice,` &&
            ` linkedin.png, Twitter.png) and HRData.json's person.png point at the sdk.openui5.org host per the offline asset-URL rule, matching apps 263 and 588 which carry the same view; the original writes them` &&
            ` relative. // NOTE: The six ConnectionsBlock Panels are built in a loop (the app 520 idiom): one DO 6 TIMES over a sub-handle of the Connections blocks aggregation, each Panel binding its t_employees` &&
            ` row with client->_bind( tab / tab_index ) on the row index sy-index gives. structural-diff therefore reads the port as dynamic - control counts built in a loop cannot match statically. The rendered` &&
            ` view is identical to the unrolled form it replaced (both expanded and diffed, 2026-09-12): a rewrite of the source, not of the behaviour, so the status is untouched.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageSelectedSection`                     class = `z2ui5_cl_smpc_app_594` path = `src/02/03/z2ui5_cl_smpc_app_594.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs` &&
                 ` a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 263/401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the first three sections hold fourteen SharedBlocks BlockBase` &&
            ` controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2 and employment:BlockJobInfoPart1 / BlockJobInfoPart2 /` &&
            ` BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob. Each is only a lazy-loading wrapper around a static view, so every block view is inlined 1:1` &&
            ` into its aggregation (forms:SimpleForm with core:Title / m:Label / m:Text, the layout:VerticalLayout / layout:HorizontalLayout trees of the personal blocks and the layout:Grid / layout:GridData tree` &&
            ` of EmploymentBlockJobCollapsed). Absent as a result: the fourteen BlockBase controls with their ids and their columnLayout / showSubSectionMore properties, plus the block views' own mvc:View roots.` &&
            ` Apps 263, 588 and 594 carry the same blocks. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is`.
    text1 = text1 && ` explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which` &&
            ` hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it. //` &&
            ` IMPROVISED: Named-model fold (app 230/263 precedent): EmploymentBlockJob and ConnectionsBlock each carry six uxap:ModelMapping elements mapping ObjectPageModel>/Employee/0..5 onto the internal models` &&
            ` emp1>..emp6>. abap2UI5 serves one default model, so the twelve ModelMapping config controls are gone (the ModelMapping 12 vs 0 line) and the six employee records are seeded as ONE table (t_employees,` &&
            ` the row type name/job/picture) and each block binds the cell it needs - client->_bind( val = t_employees[ N ]-field tab = t_employees tab_index = N ), which renders as {/T_EMPLOYEES/(N-1)/FIELD}. So`.
    text1 = text1 && ` what is lost is the ModelMapping indirection, not the /Employee ARRAY the elements address: the model keeps that shape and only the named-model names are gone. The cell is matched by data reference,` &&
            ` which an abaplint downport older than 2.120.51 lost for a component-level table expression (it outlined into a work area); that lowering is ASSIGNING upstream since abaplint/abaplint#4276, so the` &&
            ` port writes the natural t_employees[ N ]-field and needs no field symbols. EmploymentBlockJob is additionally a two-view BlockBase (Collapsed / Expanded with showSubSectionMore='true'); only the` &&
            ` Collapsed view (employees 1-2) is inlined - the Expanded view with employees 3-6 and the more/less toggle are lost. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the` &&
            ` sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs a UI5 runtime >= 1.73. // POST-1.71: the rowMode aggregation of` &&
            ` sap.ui.table.Table and the control sap.ui.table.rowmodes.Auto in it are both @since 1.119 - newer than the 1.71 floor but kept for the 1:1 port, since the sample writes exactly <ui:rowMode><uirm:Auto`.
    text1 = text1 && ` minRowCount="2"/></ui:rowMode>. The app needs a UI5 release >= 1.119: below it the lowercase rowMode tag resolves as a control class and the 404 takes the whole view down, which is why this is an` &&
            ` error rather than a warning. // IMPROVISED: onSectionVisibilityChanged is attached in JS (attachEvent('subSectionVisibilityChange')) rather than in the view, and it reads the event's` &&
            ` visibleSubSections MAP: when exactly one subsection is visible it adds sapUxAPObjectPageSubSectionFitContainer to it, otherwise it removes the class from all of them. Neither half travels - the` &&
            ` parameter is a map of live control instances keyed by id, and the decision is per render, not per round trip. addStyleClass IS whitelisted in CONTROL_METHODS, but there is nothing to key it on` &&
            ` without the map, so the wire is dropped: the page renders identically, only the fit-container class never toggles as the user scrolls. // NOTE: selectedSection='paymentSubSection' names a SUBSECTION` &&
            ` id, not a section id, and is carried as written - the association resolves it to the owning section, which is how the sample opens on Personal with the Payment information subsection expanded`.
    text1 = text1 && ` (mode='Expanded' on that subsection is the second half of it). This is the port's whole subject and needs no round trip: the association is set statically in the view. // NOTE: the ui:Table's rows` &&
            ` binding carries a sorter on 'Name'. A thin frontend sorts the data it sends (app 298 idiom), so the 123 rows of sap/ui/demo/mock/products.json are seeded verbatim in the file's own order, SORTed BY` &&
            ` name in model_init and the sorter itself is gone. Name is carried as a fifth field for exactly that reason, although no column shows it; the four the sample does show are ProductId, SupplierName,` &&
            ` Category and Price. The TableModel> prefix folds onto the default model (same-data prefix-drop). // NOTE: this sample has no Connections section, where apps 263, 588 and 594 do; its fourth section is` &&
            ` the ui:Table one instead. Its sections and subsections carry no ids either, apart from the paymentSubSection the selectedSection association needs. // NOTE: the four asset paths (the` &&
            ` imageID_275314.png avatar twice, linkedin.png, Twitter.png) and HRData.json's person.png point at the sdk.openui5.org host per the offline asset-URL rule; the original writes them relative.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageState`                               class = `z2ui5_cl_smpc_app_595` path = `src/02/03/z2ui5_cl_smpc_app_595.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs` &&
                 ` a UI5 runtime >= 1.73. // the rowMode aggregation of sap.ui.table.Table and the control sap.ui.table.rowmodes.Auto in it are both @since 1.119 - newer than the 1.71 floor but kept for the 1:1 port,` &&
                 ` since the sample writes exactly <ui:rowMode><uirm:Auto minRowCount="2"/></ui:rowMode>. The app needs a UI5 release >= 1.119: below it the lowercase rowMode tag resolves as a control class and the 404` &&
                 ` takes the whole view down, which is why this is an error rather than a warning.` ) ).

    text1 = `IMPROVISED: **Rebuilt on 2026-08-05** - it was a breadth probe whose blocks were replaced by a Text and whose third subsection plus both moreBlocks aggregations were dropped. All three subsections and` &&
            ` all TEN sample:MultiViewBlock positions are there now, each inlined with its block view's CONTENT (CAPABILITIES 'Custom BlockBase blocks', apps 161/178/188): a BlockBase is only a lazy-loading` &&
            ` wrapper around a view, and ObjectPageSubSection.blocks/moreBlocks accept any control. MultiViewBlock ships TWO views and picks by the block MODE - Collapsed (Country/Subsidiary) and Expanded (+` &&
            ` Building/Room). abap2UI5 has no BlockBase mode, so each block carries the variant its subsection asks for (mode='Expanded' the expanded form, the default-mode subsection the collapsed one), which is` &&
            ` exactly what the sample renders on load; what is lost is the per-block runtime switching. Structurally that means: the 10 sample:MultiViewBlock controls are absent, 10 forms:SimpleForm are present in` &&
            ` their place (the original side counts the two block VIEWS once, not their ten instances), and their Label/Text children move to the m: prefix - 6 vs 32 each, the same shape every BlockBase port in`.
    text1 = text1 && ` this corpus declares. The view is written out per block rather than through a helper method, so the structural diff can reconstruct it statically. Retyped NOTE -> IMPROVISED 2026-08-23: the entry` &&
            ` states a lost behaviour (the per-block runtime switching) and ten absent controls, and both the porting recipe and CAPABILITIES put exactly this block-to-content substitution at IMPROVISED - app 261` &&
            ` declares the same two-view BlockBase case that way.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageSubSection`                          class = `z2ui5_cl_smpc_app_116` path = `src/01/03/z2ui5_cl_smpc_app_116.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 263/401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold fifteen SharedBlocks` &&
            ` BlockBase controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 /` &&
            ` BlockJobInfoPart2 / BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob and connections:ConnectionsBlock. Each is only a lazy-loading wrapper` &&
            ` around a static view, so every block view is inlined 1:1 into its aggregation (forms:SimpleForm with core:Title / m:Label / m:Text, the layout:VerticalLayout / layout:HorizontalLayout trees of the` &&
            ` personal blocks, the layout:Grid / layout:GridData tree of EmploymentBlockJobCollapsed, and the six m:Panel / m:VBox / m:Image of ConnectionsBlock). Absent as a result: the fifteen BlockBase controls` &&
            ` with their ids and their columnLayout / showSubSectionMore properties, plus the block views' own mvc:View roots. This sample's section tree is IDENTICAL to apps 263, 588 and 594 - same fifteen`.
    text1 = text1 && ` blocks, same section and subsection ids, same titles - so the four ports carry the same inlined content. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content` &&
            ` substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render.` &&
            ` 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column` &&
            ` assignment, and the block ids and view roots go with it. // IMPROVISED: Named-model fold (app 230/263 precedent): EmploymentBlockJob and ConnectionsBlock each carry six uxap:ModelMapping elements` &&
            ` mapping ObjectPageModel>/Employee/0..5 onto the internal models emp1>..emp6>. abap2UI5 serves one default model, so the twelve ModelMapping config controls are gone (the ModelMapping 12 vs 0 line)` &&
            ` and the six employee records are seeded as ONE table (t_employees, the row type name/job/picture) and each block binds the cell it needs - client->_bind( val = t_employees[ N ]-field tab =`.
    text1 = text1 && ` t_employees tab_index = N ), which renders as {/T_EMPLOYEES/(N-1)/FIELD}. So what is lost is the ModelMapping indirection, not the /Employee ARRAY the elements address: the model keeps that shape and` &&
            ` only the named-model names are gone. The cell is matched by data reference, which an abaplint downport older than 2.120.51 lost for a component-level table expression (it outlined into a work area);` &&
            ` that lowering is ASSIGNING upstream since abaplint/abaplint#4276, so the port writes the natural t_employees[ N ]-field and needs no field symbols. EmploymentBlockJob is additionally a two-view` &&
            ` BlockBase (Collapsed / Expanded with showSubSectionMore='true'); only the Collapsed view (employees 1-2) is inlined - the Expanded view with employees 3-6 and the more/less toggle are lost. //` &&
            ` POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar` &&
            ` (displaySize='L'). Needs a UI5 runtime >= 1.73. // NOTE: this sample and app 594 share one view: same fifteen blocks, same section and subsection ids, same titles, same header. The single difference`.
    text1 = text1 && ` is that app 594's ObjectPageLayout also sets selectedSection='personal' and this one does not - so this page opens on the FIRST tab and shows what useIconTabBar='true' alone does, which is the point` &&
            ` of 'tab navigation mode'. Apps 263 and 588 carry the same section tree again. // NOTE: the four asset paths (the imageID_275314.png avatar twice, linkedin.png, Twitter.png) and HRData.json's` &&
            ` person.png point at the sdk.openui5.org host per the offline asset-URL rule, matching apps 263 and 588 which carry the same view; the original writes them relative. // NOTE: The six ConnectionsBlock` &&
            ` Panels are built in a loop (the app 520 idiom): one DO 6 TIMES over a sub-handle of the Connections blocks aggregation, each Panel binding its t_employees row with client->_bind( tab / tab_index ) on` &&
            ` the row index sy-index gives. structural-diff therefore reads the port as dynamic - control counts built in a loop cannot match statically. The rendered view is identical to the unrolled form it` &&
            ` replaced (both expanded and diffed, 2026-09-12): a rewrite of the source, not of the behaviour, so the status is untouched.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageTabNavigationMode`                   class = `z2ui5_cl_smpc_app_596` path = `src/02/03/z2ui5_cl_smpc_app_596.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the headerContent avatar (displaySize='L'). Needs` &&
                 ` a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/217 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold SharedBlocks BlockBase` &&
            ` controls - goals:GoalsBlock, personal:BlockAdresses / BlockPhoneNumber / BlockSocial / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 / BlockJobInfoPart2 /` &&
            ` BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3 / EmploymentBlockJob. Each BlockBase is only a lazy-loading wrapper around a static view (forms:SimpleForm with` &&
            ` core:Title / Label / Text, or a layout:HorizontalLayout / layout:VerticalLayout tree), so every block view is inlined 1:1 into its aggregation. Absent as a result: the 14 BlockBase controls with` &&
            ` their ids, and the BlockBase columnLayout='1' of BlockEmpDetailPart1, BlockEmpDetailPart2 and BlockEmpDetailPart3, and the block views' own mvc:View roots. Corrected 2026-08-21: this text was reused` &&
            ` from app 259 and never re-derived for this sample. It said 13 while listing 14 (259 really has 13), and the columnLayout clause 259 carries had been dropped - columnLayout='1' forces those blocks`.
    text1 = text1 && ` into a single subsection column, so the loss is visible. Corrected 2026-08-21: the phrase used to add "with their width='100%'", which no SharedBlocks view supports - none carries a width on its` &&
            ` mvc:View root; that width belongs to the inner SimpleForm and the port reproduces it. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution` &&
            ` (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31` &&
            ` sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the` &&
            ` block ids and view roots go with it. // IMPROVISED: EmploymentBlockJob is a two-view BlockBase (Collapsed / Expanded, selected by the block mode, with showSubSectionMore='true' offering the toggle)` &&
            ` whose views bind six internal named models emp1>..emp6> that six uxap:ModelMapping elements map onto ObjectPageModel>/Employee/0..5. abap2UI5 serves one default model and has no BlockBase mode`.
    text1 = text1 && ` toggle, so: the Collapsed view (the block's initial state, two employees - a layout:Grid / layout:VerticalLayout tree with layout:GridData layoutData) is inlined, and emp1>/emp2> are folded onto ONE` &&
            ` table (t_employees, the row type name/job) whose rows the view addresses individually - client->_bind( val = t_employees[ N ]-field tab = t_employees tab_index = N ), rendering` &&
            ` {/T_EMPLOYEES/(N-1)/FIELD} (app 230/263 precedent). So the ModelMapping indirection is what goes, not the /Employee array shape the elements address. Lost: the six ModelMapping config controls, the` &&
            ` Expanded view with employees 3-6, the more/less toggle behind showSubSectionMore, and the mapping indirection - the two records are resolved statically to HRData.json /Employee/0 (Michael Adams,` &&
            ` Scrum Master) and /Employee/1 (John Miller, Product Owner). // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope):` &&
            ` src='sap-icon://picture' in the snappedHeading and in the headerContent (displaySize='L'). Needs a UI5 runtime >= 1.73. // NOTE: The controller's three MessageToast handlers (handleLink1Press,`.
    text1 = text1 && ` handleLink2Press, handleEditBtnPress) are dead code in this sample - the view wires no press event and sets no showEditHeaderButton, so nothing can reach them. The port therefore has no on_event` &&
            ` method, which is behaviour-identical. // NOTE: subSectionLayout='TitleOnLeft' is the point of the sample (subsection titles rendered in a left column instead of above the content). The property is` &&
            ` passed through 1:1 but the resulting layout was not verified in a running system. **e2e-verified 2026-07-31** (scripts/e2e-smoke.mjs interaction, transpiled backend + real browser): the ObjectPage` &&
            ` renders and the folded emp1>/emp2> ModelMapping records show ('Michael Adams'), and the Job Relationship subsection title is there. The TitleOnLeft layout itself stays a human check: measured in the` &&
            ` source, subSectionLayout only changes the subsection's grid column math (ObjectPageSubSection._calculateLayoutConfiguration) - it emits no own CSS class an assertion could hook onto. **e2e-verified` &&
            ` 2026-08-04** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_261.mjs). // NOTE: The two social images (linkedin.png, Twitter.png) are host-absolutized to https://sdk.openui5.org/...`.
    text1 = text1 && ` since 2026-08-23. The demo kit serves them as ./test-resources/..., which an abap2UI5 app has no document root to resolve against, so both icons rendered broken. The sibling ports 262/263 rewrite the` &&
            ` same two files and declare it; this port kept the relative form and declared nothing.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageTitleOnLeft`                         class = `z2ui5_cl_smpc_app_261` path = `src/02/03/z2ui5_cl_smpc_app_261.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): src='sap-icon://picture' in the snappedHeading and in the headerContent` &&
                 ` (displaySize='L'). Needs a UI5 runtime >= 1.73.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 188/217/261 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the blocks / moreBlocks aggregations hold SharedBlocks BlockBase` &&
            ` controls - goals:GoalsBlock, personal:BlockPhoneNumber / BlockSocial / BlockAdresses / BlockMailing / PersonalBlockPart1 / PersonalBlockPart2, employment:BlockJobInfoPart1 / BlockJobInfoPart2 /` &&
            ` BlockJobInfoPart3 / BlockEmpDetailPart1 / BlockEmpDetailPart2 / BlockEmpDetailPart3. Each BlockBase is only a lazy-loading wrapper around a static view (a forms:SimpleForm with core:Title / m:Label /` &&
            ` m:Text, or a layout:HorizontalLayout / layout:VerticalLayout tree), so every block view is inlined 1:1 into its aggregation. Absent as a result: the 13 BlockBase controls with their ids and their` &&
            ` columnLayout / class='sapUxAPObjectPageSubSectionAlignContent' properties, plus the block views' own mvc:View roots with their width='100%'. Present in their place: forms:SimpleForm, core:Title and` &&
            ` the additional m:Label / m:Text / layout:VerticalLayout / layout:HorizontalLayout the block views contain. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content`.
    text1 = text1 && ` substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render.` &&
            ` 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column` &&
            ` assignment, and the block ids and view roots go with it. // POST-1.71: sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the` &&
            ` snappedHeading avatar and the HeaderContainer avatar (displaySize='L'), both on the sample's own imageID_275314.png. Needs a UI5 runtime >= 1.73. // NOTE: The sample has no controller at all - no` &&
            ` model, no event handlers - so the port is init-only with neither model_init nor on_event, which is behaviour-identical. The two avatar/image paths are absolutized to the sdk.openui5.org host per the` &&
            ` offline asset-URL rule (corrected 2026-08-24). They were kept in the original's './test-resources/...' form, justified as "matching app 261" - which is false in two ways: app 261 does not use that`.
    text1 = text1 && ` asset at all - both its avatars are sap-icon://picture, and it carries zero occurrences of imageID_275314.png - and the two icons it DOES share with the family (linkedin.png, Twitter.png) were` &&
            ` rewritten to the absolute form on 2026-08-23 for exactly this reason, its own sidecar recording that they had rendered broken until then. A relative path resolves against the demo kit's document` &&
            ` root; an abap2UI5 app is served from the ABAP ICF node and has no such root, so the request 404s and the control falls back to its placeholder. No gate sees it - data-fidelity only rejects a` &&
            ` non-OpenUI5 HOST, and a relative path has none. // POST-1.71: the content aggregation of sap.m.Title (since UI5 1.87) is newer than 1.71 but kept for the 1:1 port - the 'Order Details', 'Status' and` &&
            ` 'Average User Rating' header titles each hold an m:Link child, exactly like the original (invisible to the property gate: a default aggregation never appears as an XML attribute). Needs UI5 >= 1.87.` &&
            ` Added 2026-08-21: the sibling ports 079, 259 and 412 - 412 for these very three header titles - all declared it and this one did not, so its stated runtime floor read 1.73 (the Avatar) when it is` &&
            ` really 1.87.`.
    text2 = `sap.m.Avatar is a control @since 1.73 (kept for 1:1 fidelity, the sample entity sap.uxap.ObjectPageLayout is in scope): the snappedHeading avatar and the HeaderContainer avatar (displaySize='L'), both` &&
            ` on the sample's own imageID_275314.png. Needs a UI5 runtime >= 1.73. // the content aggregation of sap.m.Title (since UI5 1.87) is newer than 1.71 but kept for the 1:1 port - the 'Order Details',` &&
            ` 'Status' and 'Average User Rating' header titles each hold an m:Link child, exactly like the original (invisible to the property gate: a default aggregation never appears as an XML attribute). Needs` &&
            ` UI5 >= 1.87. Added 2026-08-21: the sibling ports 079, 259 and 412 - 412 for these very three header titles - all declared it and this one did not, so its stated runtime floor read 1.73 (the Avatar)` &&
            ` when it is really 1.87.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageWithHeaderContainer`                 class = `z2ui5_cl_smpc_app_402` path = `src/02/03/z2ui5_cl_smpc_app_402.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = text1
        post171 = text2 ) ).

    text1 = `IMPROVISED: four of this sample's five block types are NOT in the demo kit archive and their content therefore cannot be read: bl:GeneralInfo comes from sap.uxap.sample.Headers.block (a Headers sample` &&
            ` the demo kit does not publish) and edit:SimpleEdit / mb:MixedBlock / sample:MultiViewBlock from sap.uxap.testblocks.objectpageblock / .mixedblock / .multiview - UI5 TEST resources, referenced by this` &&
            ` one view and by sap/uxap/BlockBase.js's documentation, and shipped nowhere else. The port therefore improvises three of them, each labelled in the view and in a core:Title: GeneralInfo becomes a` &&
            ` read-only Name / Job / Location form over the MyEmployee record its own ModelMapping names, SimpleEdit an editable Name / Job form, MixedBlock a form that mixes a read-only Text with an Input (which` &&
            ` is what its name says). The fourth, sample:MultiViewBlock, is NOT invented: sap.uxap.sample.ObjectPageSubSection ships a block of the same name in its own folder` &&
            ` (view/blocks/MultiViewBlockCollapsed.view.xml - app 116 ports it), so its Collapsed view, a Location form with Country France and Subsidiary SAP France, is inlined for all four occurrences. That is`.
    text1 = text1 && ` the missing/extra pairing: eight blocks out, eight forms:SimpleForm in, with the core:Title / m:Text / m:Input / m:Label they contain. // NOTE: what this sample is actually about is not the blocks:` &&
            ` it is an object page whose section TITLES and subsection MODES come from a bound state model rather than from literals ({ObjectPageState>/sections/0/title},` &&
            ` {ObjectPageState>/sections/0/subsections/0/mode} and so on). That is carried 1:1 through a named-model fold onto ten root fields - two section titles, four subsection titles and the four modes the` &&
            ` controller seeds (three Collapsed, one Expanded) - so the same six titles and four modes drive the page from the model here as there. // 1.71: the view sets icon='sap-icon://employee' on the first` &&
            ` ObjectPageSection and icon='sap-icon://employee-pane' on its first ObjectPageSubSection. NEITHER control has ever had an icon property (it is not in the 1.71 metadata and not in 1.151 either) -` &&
            ` upstream those two attributes are silently ignored, and XMLView would log them as unknown settings. They are dropped rather than reproduced. // IMPROVISED: showCurrentSection toasts 'you are`.
    text1 = text1 && ` currently scrolling ' + oObjectPage.getScrollingSectionId(). The sample's own comment calls scrollingSectionId 'the only property that is not really bindable', and it is read-only and computed from` &&
            ` the scroll position, so a thin frontend never sees it. The button is kept and its toast reports the first section instead of the one under the scroll - the shape of the message survives, the live` &&
            ` value does not. showObjectPageState, by contrast, ports honestly: it dumps the state model as pretty JSON, and the backend OWNS that model, so the toast is composed from the same ten fields; only the` &&
            ` scrollingSectionId line inside it is the empty string the model is seeded with. // NOTE: the two subsection action Buttons both call onActionPress, a constant-text MessageToast ('action pressed !'),` &&
            ` so they are wired round-trip-free as follow_up_action( control_global, MESSAGE_TOAST show ) (app 005/401 idiom). They are tooltip-less in the sample, as are the two header buttons; all four get a` &&
            ` tooltip here so they are reachable with a screen reader - the accessibility additions in this port. // NOTE: the controller loads sap/uxap/sample/ObjectPageOnJSON/HRData.json and the two GeneralInfo`.
    text1 = text1 && ` blocks map ObjectPageModel>/MyEmployee onto their internal model 'Employee'. That file has no MyEmployee node (only Employees, counts and Employee), so upstream both blocks bind to nothing - the same` &&
            ` shape of upstream slip app 263 records for its externalModelName='data'. The port seeds the first /Employee row the file does carry (Michael Adams, Scrum Master) so the improvised form shows the data` &&
            ` the sample means. // NOTE: the objectImageURI points at the sdk.openui5.org host per the offline asset-URL rule; the original writes it relative as` &&
            ` './test-resources/sap/uxap/images/imageID_275314.png'. // NOTE: not yet verified in a running system: the two header buttons and their toasts, and that the four bound subsection modes open the page` &&
            ` with three collapsed subsections and one expanded. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_597.mjs).`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `ObjectPageXML`                                 class = `z2ui5_cl_smpc_app_597` path = `src/01/03/z2ui5_cl_smpc_app_597.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 3 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageLayout`             name = `SingleView`                                    class = `z2ui5_cl_smpc_app_161` path = `src/01/03/z2ui5_cl_smpc_app_161.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: 1 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = `NOTE: Wall-break for sap.uxap: the original blocks aggregation holds a custom BlockBase control (blockcolor:BlockBlue from the sample's SharedBlocks JS). A BlockBase is only a lazy-loading wrapper` &&
                 ` around a view; its content (a single coloured div) is inlined here as core:HTML, since ObjectPageSubSection.blocks accepts any sap.ui.core.Control. This removes the need for a custom JS control - the` &&
                 ` whole uxap ObjectPage renders with the thin generic frontend. The blockcolor:BlockBlue control is therefore absent and a core:HTML is present in its place.` ) ).

    text1 = `IMPROVISED: Block->content inlining (app 178/161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the original blocks aggregations each hold a custom BlockBase` &&
            ` control (blockcolor:BlockBlueT1, blockcolor:BlockBlueT2, blockcolor:BlockBlueT3, blockcolor:BlockBlueT4, blockcolor:BlockBlueT5, from the sample's SharedBlocks JS,` &&
            ` xmlns:blockcolor='sap.uxap.sample.SharedBlocks'), used once each with ids bbt1/bbt2/bbt3/bbt4/bbt5. A BlockBase is only a lazy-loading wrapper around a view; each BlockBlueTn's rendered content is a` &&
            ` single coloured div (<html:div style='height:auto;min-height:4em; background-color: #A9EAFF ;line-height: 4em;'>...explanatory text...</html:div>). Since ObjectPageSubSection.blocks accepts any` &&
            ` sap.ui.core.Control, each blockcolor:BlockBlueTn is inlined as a core:HTML leaf carrying that div in its content attribute - the whole ObjectPage renders with the thin generic frontend, no custom JS` &&
            ` control. Consequently all five blockcolor:BlockBlueT1..T5 controls (and their id attributes) are absent from the port and five core:HTML controls are present in their place; the`.
    text1 = text1 && ` xmlns:blockcolor='sap.uxap.sample.SharedBlocks' declaration is dropped and xmlns:core='sap.ui.core' added. The block sources are not archived into ui5/ (matching the scaffolder), read from the` &&
            ` checkout's SharedBlocks/BlockBlueTn.view.xml. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is` &&
            ` explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which` &&
            ` hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with it.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageSection`            name = `ObjectPageSection`                             class = `z2ui5_cl_smpc_app_184` path = `src/01/03/z2ui5_cl_smpc_app_184.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageSubSection`         name = `ObjectPageSubSectionBackground`                class = `z2ui5_cl_smpc_app_293` path = `src/02/03/z2ui5_cl_smpc_app_293.clas.abap`
        score = 2
        score_tip = `Rating 2 of 5 - how much attention this port deserves (complexity + rework + review + test-priority). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        is_post171 = abap_true
        notes = `POST-1.71: sapUxAPObjectPageSubSectionTransparentBackground is @since 1.122 - ObjectPageSubSection's own JSDoc says "As of version 1.122, applications can set transparent background to subsections by` &&
                 ` adding the … class", and the rule behind it lives in themes/base/ObjectPageSubSection.less. The class IS the subject of this sample, kept 1:1 from the view. Below 1.122 it resolves to no rule at all:` &&
                 ` both subsections render identically and the sample demonstrates nothing - silently, since a CSS class is invisible to every gate. Undeclared until 2026-08-23; declaring it moves the port from` &&
                 ` src/01/03 to src/02/03.`
        post171 = `sapUxAPObjectPageSubSectionTransparentBackground is @since 1.122 - ObjectPageSubSection's own JSDoc says "As of version 1.122, applications can set transparent background to subsections by adding the` &&
                 ` … class", and the rule behind it lives in themes/base/ObjectPageSubSection.less. The class IS the subject of this sample, kept 1:1 from the view. Below 1.122 it resolves to no rule at all: both` &&
                 ` subsections render identically and the sample demonstrates nothing - silently, since a CSS class is invisible to every gate. Undeclared until 2026-08-23; declaring it moves the port from src/01/03 to` &&
                 ` src/02/03.` ) ).

    text1 = `NOTE: The ObjectNumber state binding differs: the original binds state="{path:'WeightMeasure', formatter:'.weightState'}" to a controller formatter (parseFloat + Success/Warning/Error thresholds).` &&
            ` That is business logic, so - abap2UI5 being a thin frontend - the weightState is computed in ABAP model_init into a WEIGHTSTATE field and bound state="{WEIGHTSTATE}", not via a frontend formatter.` &&
            ` Visually 1:1 with the original (apps 009/010/022/092 precedent). No core:require is dropped - the original registers the formatter as a controller method, not on the view root. // NOTE: The default` &&
            ` JSONModel (products.json) is folded into the one default model: the Table items path {/ProductCollection} and the row bindings ({SupplierName}, Currency parts:[{path:'Price'},...]) keep the same leaf` &&
            ` names, so structural-diff matches 0 diffs. Full 123-row ProductCollection inlined verbatim; only the columns the view binds are kept as fields. The Price Currency composite type binding is kept 1:1` &&
            ` (price TYPE p) - standard client-side type, no version issue.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageSubSection`         name = `ObjectPageSubSectionHiddenTitle`               class = `z2ui5_cl_smpc_app_245` path = `src/01/03/z2ui5_cl_smpc_app_245.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 noted). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): all twenty-one blocks are the SAME BlockBase, sample:BlockEmpty,` &&
            ` whose Collapsed and Expanded views are both view/blocks/BlockEmpty.view.xml - a forms:SimpleForm with maxContainerCols='2', title=' ' (one blank), editable='false', ResponsiveGridLayout and NO` &&
            ` content at all. It is inlined into each of the twenty-one aggregations, which is the whole missing/extra pairing: sample:BlockEmpty 21 vs 0 against forms:SimpleForm 1 vs 21 (the block view is` &&
            ` archived once, the port emits it twenty-one times). Absent as a result: the twenty-one BlockBase controls with their ids, and the block view's own mvc:View root. Typed IMPROVISED since 2026-08-23.` &&
            ` CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one` &&
            ` - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an`.
    text1 = text1 && ` inlined form falls back to automatic column assignment, and the block ids and view roots go with it. // NOTE: the six subsections hold 1, 2, 3, 4, 5 and 6 empty blocks - which is the sample: it shows` &&
            ` how the ObjectPageSubSection lays blocks out by itself when none of them declares a size. Its own subsection titles say so ('1 block', '2 blocks' ... '6 blocks'), and the section that holds them sets` &&
            ` showTitle='false' so only the subsection titles show. All carried 1:1. The sample's own id numbering skips a step (multiview11, 21/22, 41..43, 51..54, 61..65, 71..76 - there is no multiview3x), an` &&
            ` upstream slip with no effect; the port inlines the blocks away, so no id travels. // IMPROVISED: the controller's only method is onAfterRendering, which does` &&
            ` $('.sapUiSimpleForm').css('backgroundColor', 'green') - a jQuery reach into the rendered DOM, marked '//demokit specific' in the sample itself. It paints the empty forms green so their layout is` &&
            ` visible in the demo kit. This deviation used to read "There is no control behind it (no SimpleForm property carries a background colour), so it is dropped", and it was typed DROPPED_171; both halves`.
    text1 = text1 && ` needed correcting. Only the parenthetical survives, and it survives exactly: SimpleForm DOES have a backgroundDesign property (@since 1.36, so nothing about this is a 1.71 question - hence IMPROVISED` &&
            ` now), but its type sap.ui.layout.BackgroundDesign offers Solid / Transparent / Translucent - theme-dependent designs, not a colour - so it cannot paint anything green. What the sentence missed is` &&
            ` that the framework HAS a wire for precisely this case: CONTROL_METHODS carries css: ["string","string"] with "background-color" among CSS_PROPERTIES, the abap2UI5 form of a sample writing one` &&
            ` declaration onto a control's own DOM node. The line is dropped for a different reason: the original tints EVERY rendered .sapUiSimpleForm at once, the css action addresses ONE control by id, and this` &&
            ` port inlines the block twenty-one times (see the inlining deviation) so not one of the twenty-one forms carries an id to address - and the line is demo-kit scaffolding, not part of what the sample` &&
            ` demonstrates. The port's forms are laid out identically and simply not tinted.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageSubSection`         name = `ObjectPageSubSectionMultiView`                 class = `z2ui5_cl_smpc_app_598` path = `src/01/03/z2ui5_cl_smpc_app_598.clas.abap`
        score = 4
        score_tip = `Rating 4 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 401/416 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): all 359 blocks are the SAME BlockBase, sample:InfoButton, around` &&
            ` view/blocks/InfoButton.view.xml - a single m:Button (width='100%', text='infoButton', type='Emphasized'). It is inlined into each of the 359 aggregations, which is the missing/extra pairing:` &&
            ` sample:InfoButton 359 vs 0 and Button 1 vs 0 against m:Button 0 vs 359 (the block view defaults to xmlns="sap.m" and is archived once; the port's one view defaults to sap.uxap, so each carries the m:` &&
            ` prefix, 359 times). The block view's own mvc:View root with its width='100%' is the View.width line. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content` &&
            ` substitution (IMPROVISED)", and the recipe's refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render.` &&
            ` 26 of the 31 sidecars citing that row carried NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column`.
    text1 = text1 && ` assignment, and the block ids and view roots go with it. // IMPROVISED: this sample is ABOUT sap.uxap.BlockBase.columnLayout: the ten sections walk every arrangement of 'auto' and 1..4 across 2, 3,` &&
            ` 4, 5 and 6 blocks, and the last five sections put one fixed width on all of them. columnLayout is a property of the BlockBase WRAPPER, read by ObjectPageSubSection when it lays its blocks out - and` &&
            ` the wrapper is exactly what the inlining removes, so the property has nowhere to go: an m:Button in a blocks aggregation is laid out as 'auto'. What survives is the structure the sample walks - the` &&
            ` ten sections, the 86 subsections, their titles ('a-1', '2 -a', '1-a-a-a-1' ... which spell out the very layouts) and the right number of buttons in each - so the page reads as the same matrix, with` &&
            ` every row laid out automatically instead of as its title says. The port's section table carries each subsection's title and block count, and the title spells the columnLayout each block had ('a-1' =` &&
            ` auto, 1; '2-a-a' = 2, auto, auto; the 'All N' sections put N on every block), so the matrix stays readable in the source. // NOTE: the ConfigModel the controller seeds ({subSectionLayout:`.
    text1 = text1 && ` 'TitleOnTop', useTwoColumnsForLargeScreen: false}) folds onto two root fields (same-data prefix-drop). Both ObjectPageLayout properties ARE bindable and the sample already binds them, so the two` &&
            ` ToggleButtons keep working exactly as upstream: toggleTitle flips subSectionLayout between TitleOnTop and TitleOnLeft, toggleUseTwoColumns flips useTwoColumnsForLargeScreen, and the port flips the` &&
            ` same fields in on_event - a round trip where the original writes the model directly, but the same two properties and the same two values. // NOTE: the ToggleButtons' pressed state is not bound in the` &&
            ` sample either (the controller keeps isTitleOnTop / bUseTwoColumns of its own), so the port does not bind it: the button's own toggle and the backend flag start in step and stay in step, one press` &&
            ` each. // NOTE: not yet verified in a running system: that the two toggles switch the subsection layout to TitleOnLeft and the page into two-column mode. **e2e-verified 2026-08-25** (nightly e2e` &&
            ` interaction, meta/interactions/z2ui5_cl_smpc_app_599.mjs). // NOTE: The view is built in a loop (the app 520 idiom): the ten ObjectPageSection / 86 ObjectPageSubSection / 359 m:Button tree is one`.
    text1 = text1 && ` local table of sections with their subsection titles and block counts, walked with LOOP AT and DO on a sub-handle of the sections aggregation; the ids infoButton1..infoButton359 are string templates` &&
            ` over a running counter. structural-diff therefore reads the port as dynamic - control counts built in a loop cannot match statically - and the render gate reconstructs one pass of each loop. The` &&
            ` rendered view is identical to the unrolled form it replaced (both expanded and diffed, 2026-09-12): a rewrite of the source, not of the behaviour, so the status is untouched.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageSubSection`         name = `ObjectPageSubSectionSized`                     class = `z2ui5_cl_smpc_app_599` path = `src/01/03/z2ui5_cl_smpc_app_599.clas.abap`
        score = 5
        score_tip = `Rating 5 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 2 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    text1 = `IMPROVISED: Block->content inlining (app 161 precedent, CAPABILITIES 'Custom BlockBase blocks in a sap.uxap.ObjectPageLayout'): the original blocks aggregations each hold a custom BlockBase control` &&
            ` blockcolor:BlockBlue (from the sample's SharedBlocks JS, xmlns:blockcolor='sap.uxap.sample.SharedBlocks'), used 3 times (ids b1/b2/b3). A BlockBase is only a lazy-loading wrapper around a view;` &&
            ` BlockBlue's rendered content is a single coloured div (<html:div style='height:4em; background-color: #A9EAFF ;'/>). Since ObjectPageSubSection.blocks accepts any sap.ui.core.Control, each` &&
            ` blockcolor:BlockBlue is inlined as a core:HTML leaf carrying that div in its content attribute - the whole ObjectPage renders with the thin generic frontend, no custom JS control. Consequently all` &&
            ` three blockcolor:BlockBlue controls (and their id attributes) are absent from the port and three core:HTML controls are present in their place. The empty BlockBlueCtrl controller (a no-op` &&
            ` onParentBlockModeChange stub) carries no behaviour to port. Typed IMPROVISED since 2026-08-23. CAPABILITIES' own row says "Declare the block→content substitution (IMPROVISED)", and the recipe's`.
    text1 = text1 && ` refinement is explicit that removing a control the original declares - even a zero-visual-output one - is a loss regardless of a pixel-identical render. 26 of the 31 sidecars citing that row carried` &&
            ` NOTE, which hid a measurable loss: columnLayout is honoured only for a real BlockBase instance, so an inlined form falls back to automatic column assignment, and the block ids and view roots go with` &&
            ` it.`.
    result = VALUE #( BASE result
      ( module = `sap.uxap`           control = `sap.uxap.ObjectPageSubSection`         name = `ObjectPageSubSectionWithActions`               class = `z2ui5_cl_smpc_app_178` path = `src/01/03/z2ui5_cl_smpc_app_178.clas.abap`
        score = 3
        score_tip = `Rating 3 of 5 - how much attention this port deserves (complexity + rework + review + test-priority: complex, 1 reworked). 1 = simple faithful 1:1, 5 = complex / reworked / worth a close look.`
        since = `1.26`
        notes = text1 ) ).

    " abap2ui5lint-enable

  ENDMETHOD.


  METHOD render_header.

    " ONLY INLINE CONTROLS BELONG INTO A sap.m.Bar. Its content containers
    " became flex boxes only after 1.71: on the oldest release abap2UI5
    " supports, .sapMBarLeft/.sapMBarRight are plain absolutely positioned
    " blocks that lay their children out in normal flow, so a block-level
    " child - a ToolbarSpacer or a ToolbarSeparator, both of which render a
    " <div> - starts a new line, and everything from that line on is cut away
    " by the overflow:hidden the container carries at the bar's height of
    " 3rem. This row used to put a ToolbarSeparator between its two groups and
    " lost the documentation and GitHub icons on 1.71 because of it; the gap
    " now rides on the first icon of the second group (group_start).
    DATA(bar) = page->ele( `customHeader`
        )->ele( `Bar` ).

    " left: what the stock page header would render on its own
    DATA(left) = bar->ele( `contentLeft` ).

    left->tag( `Button`
        )->a( n = `icon`    v = `sap-icon://nav-back`
        )->a( n = `type`    v = `Transparent`
        )->a( n = `tooltip` v = `Back`
        )->a( n = `visible` b = client->check_app_prev_stack( )
        )->a( n = `press`   v = client->_event_nav_app_leave( ) ).

    left->tag( `Title`
        )->a( n = `text`  t = title
        )->a( n = `level` v = `H2` ).

    " right: the sample repositories of the abap2UI5 family, one icon each ...
    DATA(right) = bar->ele( `contentRight` ).

    header_button( toolbar   = right
                   icon      = `sap-icon://lightbulb`
                   name      = `Samples`
                   descr     = `binding, events, popups, tables and much more`
                   class     = cs_overview-samples
                   class_old = cs_overview-samples_old
                   href      = cs_url-samples ).

    header_button( toolbar = right
                   icon    = `sap-icon://palette`
                   name    = `Control Samples`
                   descr   = `the UI5 Demo Kit, rebuilt with abap2UI5`
                   class   = cs_overview-controls
                   href    = cs_url-controls
                   here    = abap_true ).

    header_button( toolbar   = right
                   icon      = `sap-icon://database`
                   name      = `Stack Samples`
                   descr     = `OData, RAP, WebSockets and the Fiori Launchpad`
                   class     = cs_overview-stack
                   class_old = cs_overview-stack_old
                   href      = cs_url-stack ).

    " ... and then, set apart by a wider gap, the two entries that leave the
    " system: the three icons above open an app, these open a site
    header_button( toolbar     = right
                   icon        = `sap-icon://learning-assistant`
                   name        = `Documentation`
                   descr       = `guides, tutorials and the API reference`
                   href        = cs_url-docs
                   group_start = abap_true ).

    " not source-code: in the shared header that icon is reserved for the
    " per-sample source links the overviews render in their lists
    header_button( toolbar = right
                   icon    = `sap-icon://globe`
                   name    = `GitHub`
                   descr   = `the source code of this repository`
                   href    = cs_url-controls ).

  ENDMETHOD.


  METHOD install_display.

    DATA(info) = z2ui5_cl_ui5_view_builder=>factory( ).

    info->ele( n = `FragmentDefinition` ns = `core`
        )->a( n = `xmlns`      v = `sap.m`
        )->a( n = `xmlns:core` v = `sap.ui.core`

        )->ele( `Popover`
            )->a( n = `title`        t = |{ name } - not installed|
            )->a( n = `placement`    v = `Bottom`
            )->a( n = `contentWidth` v = `26rem`

            )->ele( `VBox`
                )->a( n = `class` v = `sapUiContentPadding`

                )->tag( `Text`
                    )->a( n = `text` t = |This system does not have { name } installed, so there is no app to | &&
                                          |jump to. Install the repository with abapGit, then this icon opens it right here.|
                )->tag( `Link`
                    )->a( n = `text`   v = href
                    )->a( n = `href`   v = href
                    )->a( n = `target` v = `_blank`
                    )->a( n = `class`  v = `sapUiSmallMarginTop` ).

    client->popover_display( xml = info->stringify( ) by_id = anchor ).

  ENDMETHOD.


  METHOD header_button.

    DATA target TYPE string.
    DATA hint   TYPE string.
    DATA color  TYPE string.
    DATA press  TYPE string.

    DATA(tooltip) = |{ name } - { descr }|.

    IF here = abap_true.

      " where you are: the entry stays, so every overview shows the same row,
      " but there is nowhere to go - and no press
      hint  = |{ tooltip } - you are here|.
      color = cs_color-inactive.

    ELSE.

      color = cs_color-active.

      IF class IS NOT INITIAL AND class_installed( class ) = abap_true.
        target = class.
      ELSEIF class_old IS NOT INITIAL AND class_installed( class_old ) = abap_true.
        target = class_old.
      ENDIF.

      IF target IS NOT INITIAL.
        " installed on this system: jump right into it, the back button returns
        hint  = tooltip.
        press = client->_event( val = `NAV_APP` arg = target ).

      ELSEIF class IS INITIAL.
        " no CLASS to look for: the documentation and GitHub entries are no
        " destination inside the system to begin with, they open their site -
        " link_press is the same URLHELPER REDIRECT wire the link popover uses
        hint  = tooltip.
        press = link_press( href ).

      ELSE.
        " a repository that is not on this system is a normal, active entry -
        " the press says what is missing and where to get it (install_display),
        " instead of dropping the user on GitHub without a word
        hint  = |{ tooltip } - not installed on this system|.
        press = client->_event( val   = `INSTALL`
                                t_arg = VALUE #( ( class )
                                                 ( href )
                                                 ( name ) ) ).
      ENDIF.

    ENDIF.

    " a core:Icon, not a Button: on 1.71 a Button cannot carry a colour - the
    " coloured sap.m.ButtonType values (Critical, Neutral, ...) are 1.73+ - and
    " the colour is what separates the active entries from the ONE inactive
    " one, the overview you are already in. Everything else is active, whether
    " its repository is on this system or not. The class name doubles as the
    " icon id, so install_display( ) can anchor its popover to the icon pressed
    " the wider begin margin is what sets a new group of the row apart - a
    " margin rather than a separator control, see render_header( )
    DATA(css_class) = COND string( WHEN group_start = abap_true
                                   THEN `sapUiMediumMarginBegin sapUiTinyMarginEnd`
                                   ELSE `sapUiTinyMarginBeginEnd` ).

    toolbar->tag( n = `Icon` ns = `core`
        )->a( n = `src`     t = icon
        )->a( n = `size`    v = `1.125rem`
        )->a( n = `class`   t = css_class
        )->a( n = `tooltip` t = hint ).

    " a( ) writes on the element just added, and an EMPTY attribute would be
    " rendered as one - color="" is not a valid IconColor and press="" is not a
    " valid handler, so the three optional ones are added only when they carry
    " something
    IF class IS NOT INITIAL.
      toolbar->a( n = `id` t = class ).
    ENDIF.

    IF color IS NOT INITIAL.
      toolbar->a( n = `color` t = color ).
    ENDIF.

    " press is decided per branch above and written here once: the linter's
    " reconstructor follows neither a handle held in a variable (it drops the
    " attribute - client-handle-capture) nor an attribute written in several
    " IF branches (it reads them as one control set thrice - duplicate-
    " property), and of the two the dropped wire is the one that only costs
    " this generated class a finding, which its own config accepts
    IF press IS NOT INITIAL.
      toolbar->a( n = `press` v = press ).
    ENDIF.

  ENDMETHOD.


  METHOD class_installed.

    " Is the class ON this system - the same question the framework's start
    " page asks (z2ui5_cl_ui5_util_context=>rtti_check_class_exists), and
    " deliberately NOT "can it be instantiated". CREATE OBJECT was the check
    " here, and it answers a far bigger question than the header has: it loads
    " the whole class pool of the OTHER repository's overview app together with
    " everything that pool statically references, and runs its constructor.
    " Every failure in there - a helper class of that repository the release
    " cannot activate, a repository that landed on the system only in part -
    " came back as "not installed on this system", so the icon offered the
    " abapGit link for a repository that is sitting right there and refused to
    " navigate into it.
    " Existence is what this row has to decide. Whether the app then starts is
    " the navigation's question, and since the silent catch there is gone, a
    " jump that cannot happen says why instead of doing nothing.
    " The name has to be upper case - the repository stores it that way, and
    " the class constants above follow the repository's lower-case spelling rule.
    DATA(name) = to_upper( val ).

    TRY.
        cl_abap_classdescr=>describe_by_name( EXPORTING  p_name         = name
                                              EXCEPTIONS type_not_found = 1 ).
        IF sy-subrc = 0.
          result = abap_true.
        ENDIF.

      CATCH cx_root ##CATCH_ALL.
        result = abap_false.
    ENDTRY.

  ENDMETHOD.


  METHOD link_press.

    " the press wire of the popover's four link buttons: open an EXTERNAL url in
    " a new tab, entirely on the client. cs_event-open_new_tab is same-origin
    " only (isValidRedirectURL) and three of the four targets live on
    " sdk.openui5.org / github.com, so the redirect goes through the URLHELPER
    " frontend action, whose REDIRECT takes a URL/NEW_WINDOW object-literal
    " t_arg - NEW_WINDOW true is what target="_blank" did on the former Links.
    result = client->follow_up_action( val   = client->cs_event-urlhelper
                                       t_arg = VALUE #( ( `REDIRECT` ) ( |\{ URL: '{ url }', NEW_WINDOW: true \}| ) ) ).

  ENDMETHOD.

ENDCLASS.
