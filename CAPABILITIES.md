# CAPABILITIES.md — what abap2UI5 can express

The capability map for porting UI5 demo kit samples. **Read this before deciding
that a sample feature cannot be ported** — the most common generation mistake so
far was not wrong code but a wrong assumption about what abap2UI5 can do (a
Dialog declared "not expressible" although another port in this repo builds one;
CSS dropped although `core:HTML` can carry it). Every entry names the port that
proves it, so claims stay checkable.

Status legend: ✅ direct 1:1 · 🔶 expressible with a documented workaround ·
🧪 plausible, needs a LIVE-TEST port to confirm · ❌ not expressible today.

Verification levels in the Evidence column: **live-verified** = seen in a
running system; **source-verified** = traced in the abap2UI5 framework source
(2026-07-16 pass; clone the repos per TRAINING.md "Reference repositories") — strong enough to generate against, a
live check remains the final confirmation for rendering.

Maintenance rule (same discipline as AGENTS.md §10): whenever a port proves a
new technique, or disproves a ❌, update this table **in the same change** and
reference the port. Never declare an `IMPROVISED`/`DROPPED_171` deviation for a
feature this table marks ✅/🔶.

A `pr/<id>` in the Evidence column is an **upstream request id**. What each one
asked for and what came of it is in
[docs/upstream-requests.md](docs/upstream-requests.md) (`pr/README.md` until
2026-08-18); requests still open live in
[`backlog/`](https://github.com/abap2UI5/abap2UI5/tree/main/backlog) in
abap2UI5.

## Views & controls

| UI5 feature | Status | How in abap2UI5 | Evidence |
|---|---|---|---|
| Any XML view structure (containers, aggregations, namespaces) | ✅ | `z2ui5_cl_ui5_view_builder` ele/tag/end + `a()` | all ports |
| Aggregations filled by the controller in `onInit` (e.g. pre-set `tokens` on MultiInput) | ✅ | declare the aggregation in the view: `ele( 'tokens' )` → `tag( 'Token' )` per entry | missed in app 040 — the tokens aggregation carries no @since in the sources at all, so it is base-version (the figure 1.16 stood here until 2026-08-23 and nothing supports it; sap.m.Tokenizer, which renders the aggregation, is @since 1.22); do not skip these |
| MultiInput `addValidator` (free text + Enter → a new Token) | 🔶 | the bundled custom control `z2ui5.cc.MultiInputExt` (invisible companion referencing the MultiInput by id) installs exactly this validator — `addValidator(({text}) => new Token({key:text, text}))` — and mirrors added/removed tokens back via `addedTokens`/`removedTokens` + a `change` event. Declare `xmlns:z2ui5="z2ui5.cc"` and one `z2ui5:MultiInputExt` leaf with `MultiInputId` per input | source-verified in `app/webapp/cc/MultiInputExt.js`; app 040 wires it on both token inputs (first cc-control usage in these ports, converted 2026-07-18, live-verified 2026-07-20) |
| Custom CSS (`style.css`, `html:style` blocks) | 🔶 | `core:HTML` leaf with the `content` **attribute** carrying `<style>…</style>` — the builder escapes attribute values, no CDATA node needed. **Literal CSS braces must be escaped `\{ … \}`**: the XMLView binding parser reads unescaped `{…}` in any attribute value as a binding and crashes (render-smoke proof 2026-07-18, app 028; pattern-lint gates it). Write the escape in a **backtick** literal (`` `…\{…\}…` ``) so the backslash survives to the serialized attribute — a `\|…\|` template collapses `\{`→`{` and re-crashes (the reverse of the typed-binding string, which wants real braces). Write **decoded** markup in `content` (`<div>`, not the original XML's `&lt;div&gt;`); the builder re-escapes on stringify, so copied entities double-escape | apps 026/028 inject their sample `style.css` exactly this way (escaped 2026-07-18, render-smoke green, live-verified 2026-07-19); app 026 also uses `content` for `<h2>…</h2>` markup; app 169 (`sap.ui.layout.GridData`) injects the style + 21 `core:HTML` cell divs, render-smoke green; apps 122 (`sap.ui.core.Icon`, the five `size1..size5` classes + a `@media` query) and 124 (`sap.ui.layout.CSSGrid`, the tile style) added 2026-07-28 — both had been carrying the sample's class names with **no rules behind them**, so the icons rendered at the default size and the grid tiles as unstyled text. A sample whose stylesheet is not in `ui5/` is an archive gap to close, not a reason to drop the CSS: `manifest.json` `sap.ui5/resources/css` names the file |
| Composite/array properties (RangeSlider `range="[lo,hi]"`) | 🔶 | split into the scalar sibling properties the control keeps in sync (`value`/`value2`) | app 045 |
| Literal line breaks inside attribute values (`&#xA;` in the original) | ✅ | write the break as `\n` in a `\|...\|` template — `xml_escape` emits it as `&#xA;`/`&#xD;`/`&#x9;` so it survives XML attribute-value normalization | app 035 (`noDataText`); builder fix 2026-07-16 |
| Raw text / CDATA child nodes | ❌ | `z2ui5_cl_ui5_view_builder` has element+attribute nodes only; for markup/CSS use the `core:HTML` `content` attribute (row above) | app 026 NOTES |
| A CSS value on a control that has **no matching property** (the sample writes it onto the DOM node) | 🔶 | `follow_up_action( control_by_id, <id> / css / <property> / <value> )` writes one whitelisted declaration (width, height, color, background-color, font-size, opacity, …) onto the control's own DOM ref — the abap2UI5 form of the samples' `byId(x).$().width(v + '%')`. **Prefer a bound property wherever the control has one** (apps 213/214); this is only for the residue, e.g. `sap.m.Page`, which has no `width`. The value may be a client expression (`${$parameters>/value} + '%'`), so a Slider drag stays roundtrip-free | apps 138/267/269 (`DynamicSideContent` width sliders); upstream `pr/control-inline-style` (2026-08-05) |

## Popups & messages

| UI5 feature | Status | How in abap2UI5 | Evidence |
|---|---|---|---|
| Controller-built `sap.m.Dialog` (`new Dialog({...}).open()`) | ✅ | build a `core:FragmentDefinition` → `Dialog` with the same builder, show with `client->popup_display( )`, close via `client->follow_up_action( client->cs_event-popup_close )` | app 042 builds its error Dialog this way (its earlier toast substitution was a wrong improvisation); app 044 used the pattern as a workaround until 2026-07-18, now 1:1 via the popup-mode PDFViewer (row below) |
| Controller-created popup-mode control opened imperatively (`new PDFViewer(...)` + `.open()` on a view dependent) | ✅ | declare the control in the view's `mvc:dependents` aggregation (the `addDependent` equivalent), bind its inputs, open it with `client->follow_up_action( val = cs_event-control_by_id t_arg = id/'open' )` after the round-trip (the view defaults to `cs_view-main`) - `open`/`close` are whitelisted since 2026-07-18 (pr/control-call-whitelist) | app 044 (live-verified 2026-07-20); source-verified in `app/webapp/core/FrontendAction.js` `CONTROL_METHODS` |
| `sap.m.MessageBox` | ✅ | `client->message_box_display` (actions, initialFocus, styleClass, and now `dependentOn` + `contentWidth` supported). **The chosen action comes BACK to the backend**: the optional `onclose` parameter names an event, and `core/actions/ControlCall.js` wires `onClose: (sAction) => …` so the pressed action (`YES`/`NO`/…) arrives as `get_event_arg( )` — a `MessageBox.confirm/warning` with a result handler ports 1:1, never rebuild it as a fragment Dialog (probe-#2 app 624 did exactly that on the false claim no return path exists — the app-042 lesson class). **Regression window closed 2026-07-30**: since #2441 the action had been passed INSIDE the event array (`eB([ONCLOSE, sAction])`), which `Server.roundtrip` shifts away whole — the action silently never reached `T_EVENT_ARG` and every onclose read returned initial. Found by the 093 e2e interaction; fixed upstream (action rides as the first positional arg). A port reading `get_event_arg( )` after an onclose event needs a framework ≥ the fix | app 036; app 278 ports the whole type matrix (`type` is the sample's own method name — `core/actions/ControlCall.js` resolves `MessageBox[TYPE]`, so `confirm`/`alert`/`error`/`information`/`warning`/`success` map 1:1 and each method keeps its own action defaults) plus the onclose→toast return path; onclose source-verified in `z2ui5_if_client` + `app/webapp/core/actions/ControlCall.js` 2026-07-26 |
| `sap.m.MessageToast` (incl. the options object: `my`/`at`/`of`/`offset`/`collision` docking, `duration`, `width`, `class`) | ✅ | `client->message_toast_display( text = … my = 'center center' at = 'center center' … )` — the client method exposes the full MessageToast options object; dock strings ("center center", "begin top", …) are resolved in `core/actions/ControlCall.js`. The plain one-text case is the positional call `client->message_toast_display( \`…\` )` (`text` is the first parameter — omit its name, §8 simplest notation; five probe-#2 agents flagged the signature as nowhere shown) | apps 037, 108; docking proven by app 032 (source-verified in `app/webapp/core/actions/ControlCall.js`) |
| `sap.m.MessagePopover.setAsyncURLHandler` (async per-link URL validation gating navigation) | ✅ | the callback is a live JS promise no wire can carry, so the backend names a **built-in policy** instead: `follow_up_action( val = cs_event-control_by_id t_arg = VALUE #( ( popoverId ) ( \`setAsyncURLHandler\` ) ( \`RELATIVE_ONLY\` ) ) )` — policies `ALLOW_ALL` / `RELATIVE_ONLY` (relative links allowed, absolute schemes + `//host` denied — the demo's http-gate case) / `DENY_ALL`; the popover's `urlValidated` event is wireable for the original's validation toast (implemented upstream 2026-07-29, #2482) | app 067; source-verified `FrontendAction.js` `URL_POLICIES` (pr/messagepopover-async-url implemented) |

## Models & binding

| UI5 feature | Status | How in abap2UI5 | Evidence |
|---|---|---|---|
| A **`sap.ui.integration` Card manifest** | ✅ | either shape works. `Card.createManifest` branches on the JS type: a STRING manifest is read as `manifestUrl` and fetched, an inline manifest has to arrive as a JS **object**. Bind the manifest **URL** where the sample keeps one manifest per file; for a bundled manifest bind the JSON itself with `client->_bind( val = manifest json = abap_true )` - the serializer then splices the string's content into the model as a JSON node instead of quoting it (a manifest's keys `sap.app`/`sap.card` are not valid ABAP field names, so it can be neither a structure nor a plain string). Outbound only, and an unparseable string raises | source-verified `Card.js` `createManifest`; apps 342/350 bind URLs, apps 118/341 bind the inline manifests with `json = abap_true` (`pr/card-manifest-object`, implemented upstream 2026-08-10) |
| JSON model data | ✅ | ABAP `VALUE #( … )` + `client->_bind` (two-way; never the obsolete `_bind_edit`) — keep the data verbatim, full row set | AGENTS §5; silent row subsets bit apps 030/033/034 |
| A **repeating model array the view addresses row by row** (six statically written blocks over `/Employee/0..5`, a `ModelMapping` per row) | ✅ | the CELL binding: `client->_bind( val = t_employees[ N ]-field tab = t_employees tab_index = N )` renders `{/T_EMPLOYEES/(N-1)/FIELD}`, so the model keeps the ARRAY the original addresses while the view goes on writing each control out (which an aggregation binding would collapse into one template — and the structural gate would then report the missing controls). `_bind` matches the cell by data REFERENCE, which an abaplint downport older than 2.120.51 lost for a component-level table expression (it outlined into a work area) — fixed upstream in abaplint/abaplint#4276, so the port writes the expression as it stands and needs no field symbols. An app downported by an OLDER abaplint has to assign the row first (`ASSIGN t_employees[ N ] TO <empN>`, then `val = <empN>-field`), which the same rule already lowered with ASSIGNING. Before this the corpus wrote a series of flat attributes (`emp1_name`, `emp2_name`, …), which renders identically and loses the array | apps 261/263/401/587/588/594/595/596, the UxAP ObjectPage family — the six `/Employee` rows as one seeded table; framework doc + tests on `z2ui5_if_client~_bind`, linter support in `@abap2ui5/linter` `bindingOf` (both spellings), e2e-verified on the transpiled backend |
| A model field that must stay **ABSENT** (the control keeps its UI5 default) | ✅ | `client->_bind( val = t_rows omit_initial = abap_true )` — an ABAP field is never absent, it is initial, so by default every field is serialized as an explicit `""`/`0`, which overrides the control's default and is rejected outright by an enum-typed property. With the flag an initial field stays out of the model. Boolean columns are the exception - an explicit `false` IS initial - so scope the omission instead: `omit_initial_paths = VALUE #( ( `MIN` ) ( `MAX` ) … )` lists the columns that may vanish and leaves the booleans alone | app 049 (`sap.m.sample.StepInput`) — one bound template whose 14 rows fill different property subsets; upstream `pr/model-empty-vs-default` (2026-08-05) |
| Two-way property binding incl. live client-side updates | ✅ | shared `_bind` path on both controls — no event round-trip needed | app 048 (Switch↔Select), app 003 (Breadcrumbs separatorStyle↔Select) |
| Expression bindings (`{= … }`) over bound paths | ✅ | inline `client->_bind( var )` calls composed into the expression string template — never captured in a variable | app 007 (the AGENTS §5 worked example) |
| Imperative controller logic that only derives view state (setWidth/setExpanded from a control value) | 🔶 | prefer a pure expression binding over an event round-trip (`{= ${slider} + '%' }`) | app 007 is the pattern (live-verified 2026-07-15); the avoidable round-trips in 486/530 (pre-renumber ids) were removed 2026-07-16 (those two are pre-renumber ids and carry no live check under a current number) |
| **Recursive / arbitrary-depth** tree binding (`sap.ui.table.TreeTable` `rows="{path:'/x', parameters:{arrayNames:['categories']}}"`, self-referential nodes) | ❌ | ABAP cannot type a table of its own type, so an arbitrary-depth self-referential model has no static ABAP shape; and `TreeTable.expand/collapse(getSelectedIndices())` are not in the `CONTROL_METHODS` whitelist. A **fixed, known depth** is expressible (nest one concrete table type per level, row above). Don't port a TreeTable sample whose tree is arbitrary-depth — declare the boundary | app 174 NOTES (`sap.ui.table.RowHighlights` was chosen over TreeTable for exactly this) |
| Nested tables / tree binding (`items="{path: '/'}"` over nested `nodes`) | ✅ | nested ABAP table types serialize into nested JSON arrays; two-way delta write-back is nested-aware; the framework even ships a `z2ui5.cc.Tree` companion preserving expand state across roundtrips — note it **never actually worked until 2026-07-18**: `setTreeState` only stores an initial state consumed at node creation, so applying it after rendering was a no-op; fixed upstream (slot-wide resolution + `refresh(true)` after `setTreeState`, headless-verified). A "source-verified ✅" can still be broken — the first live/probe test is what counts | source-verified: ajson `convert_table` recursion + `delta_apply_nodes` (z2ui5_cl_ui5_srv_model); app 054 renders it; beta samples 460-462 (abap2UI5/samples 00/08) cover plain tree, DnD node move (context paths carry the `/XX/` prefix!) and popup tree with preserved expand state |
| Nested **single (non-array) structure** on the default model, bound `{OBJ/FIELD}` (a control whose property points at a sub-object, e.g. `sap.ui.unified.Currency` `value="{transactionAmount/size}"`) | ✅ | give the row type a **nested structure component** (`transaction_amount TYPE ty_s_transaction_amount` with `size`/`currency`) — it serializes to a nested JSON object, and the relative sub-path resolves `{TRANSACTION_AMOUNT/SIZE}`. Do **not** flatten to `{OBJ_FIELD}`; keep the nesting so the original `{obj/field}` maps 1:1 (last segment matches structural-diff). Distinct from the named-model row (one default model, no prefix) and the nested-**array** row above | app 171 (`sap.ui.unified.CurrencyInTable`), render-smoke green; **live-verified 2026-07-27** — the nested-object serialization + relative sub-path resolution works at runtime (u:Currency renders the bound values) |
| Named JSON models fed from ABAP (`img>`, `ui>`, `config>`, `range>` …) | 🔶 | **Not needed for the demo-kit cases — fold into the one default model.** A *second, independent* ABAP-fed model was declined 2026-07-19 as too complex (one serialized JSONModel per slot); a frontend model-alias (register the default model under each `{name>}` prefix) was tried 2026-07-23 and **reverted the same day as redundant**: since it aliases the *same* instance, the prefix carries no information the default model doesn't already have. So the faithful port just **drops the prefix and binds the field on the default model** — `{ui>/rowMode}` → `client->_bind( rowmode )` → `{/ROWMODE}`. Structural-diff compares binding paths by **last segment only**, so `{ui>/rowMode}` (original) and `{/ROWMODE}` (port) match with **0 diffs** and no framework change. Genuinely *independent* named data with a colliding leaf name (two models, same leaf, different data) is the real residual gap — and an alias would not have solved it either (same instance collides). Vanishingly rare in the demo kit; use default-model nesting if it ever appears | apps 162 (`img>`, sap.ui.layout), 163 (`range>`, sap.m), 164 (`ui>`, sap.ui.table RowModes — `{ui>/rowMode}` bound in two places, the Table `rowMode` aggregation and the footer SegmentedButton `selectedKey`, both folded to one flat field). Still available beside this: `device>` (row below), named OData via `cs_event-set_odata_model`, OData default with ABAP re-attached as `http>` (`switch_default_model_path`), `template>` preprocessor model |
| Custom `BlockBase` blocks in a `sap.uxap.ObjectPageLayout` (the sample's `SharedBlocks` JS controls) | ✅ | a `BlockBase` subclass is only a lazy-loading wrapper around a view; its content is standard controls. `ObjectPageSubSection.blocks` accepts any `sap.ui.core.Control`, so **inline the block view's content directly** into `blocks` instead of referencing the custom JS control — the whole ObjectPage renders with the thin generic frontend, no custom control needed. Declare the block→content substitution (IMPROVISED) — one deviation naming the block + its replacement (`blockcolor:BlockBlue` → `core:HTML`) covers all instances (structural-diff shows `control missing`/`control extra` lines, both matched by that `what`). Read the block's view from the checkout (`.../sample/<Name>/SharedBlocks/[<area>/]<Block>.view.xml` — some blocks sit directly in `SharedBlocks/`, others under an `<area>/` subdir); the block sources are **not** archived into `ui5/` (matching the scaffolder/161), so such a port is not offline-re-verifiable from `ui5/` alone. This unblocks sap.uxap, whose demo samples all wrap their content in per-sample block controls | apps 161 (`SingleView`) + 178 (`ObjectPageSubSectionWithActions`) inline `BlockBlue`'s coloured div as `core:HTML`; render-smoke green. Batch b05 (apps 258-263) inlines the **real** block views (a `forms:SimpleForm` with `core:Title`/`Label`/`Text`, the `layout:Grid` tree of `EmploymentBlockJobCollapsed`, the six `m:Panel` of `ConnectionsBlock`) rather than a placeholder - that is the 188/217 practice and the one to follow; a two-view BlockBase (`EmploymentBlockJob` Collapsed/Expanded behind `showSubSectionMore`) inlines its **Collapsed** view and declares the lost toggle. Do NOT archive `ui5/sap.uxap/SharedBlocks/` to make the inlining diff-verifiable: the uxap manifests over-list block files the view never instantiates, so structural-diff then demands phantom controls (AGENTS §4, measured 2026-07-31) |
| **i18n texts** (`{i18n>KEY}` resource-model bindings, `i18n.properties`) | 🔶 | **No frontend i18n model — by design, not as a gap** (user decision 2026-07-26): abap2UI5 is a thin frontend, and a frontend translation store duplicates what ABAP does natively — text elements / OTR translated per logon language (`sy-langu`), served as ordinary bound model fields. A translatable app computes the final strings server-side and binds them; the frontend renders. For 1:1 demo-kit ports the `{i18n>KEY}` binding folds to the literal text + IMPROVISED (the runtime language-*switch* behaviour of the sample is lost — rebinding after a server-side language change is the abap2UI5 equivalent). Do not raise a `pr/` for a frontend resource model | app 233; AGENTS §5 cheat-sheet "i18n" row |
| `sap.ui.Device` / device model bindings | ✅ (static: 🔶) | the `device>` named model (JSONModel over sap.ui.Device) is set on **every** view slot — main, popup, popover and nested views — so `{device>/system/phone}` bindings work everywhere; the same data is also mirrored server-side in `client->get( )-s_device`. **Live since 2026-08-05, measured both ways** (`scripts/probes/device-model-live-probe.mjs`, real OpenUI5, viewport 1400px->420px): `JSONModel(Device)` wraps the LIVE Device object but a JSONModel only notifies bindings when told, so `{device>/resize/width}` used to stay `1400` and `{device>/orientation/landscape}` `landscape` after a resize/rotation. The framework now refreshes the model from Device's own resize/orientation/media handlers (pr/live-device-model, implemented upstream), so those bindings follow the viewport with no wire and no round-trip - and publishes the current media range as **`{device>/media/range}`** (`Phone`/`Tablet`/`Desktop`, the Std set), the path a live breakpoint class swap needs, because `Device.media` exposes methods only and has no bindable property of its own (app 168's layout-change class swap runs on it). One thing NOT to misread: `{device>/system/phone}` is correctly STATIC - `Device.system` is UA/screen based, so a narrowing desktop window is not a phone - and every device-BRANCH port is right as it is | source-verified: view1_js binds the one shared model in all slot factories (displayFragment/displayPopover/displayNestedView) plus the main view; apps 030/046 use it in main views (both live-verified 2026-07-20), 277 and 279 use it to keep a controller's `Device.system.phone` branch a branch instead of resolving it to one value at init |
| Date-**object** properties (`type: "object"`: `CalendarAppointment.startDate/endDate`, `PlanningCalendar.startDate/minDate/maxDate`, `SinglePlanningCalendar.startDate`, `DatePicker.dateValue`, `sap.ui.unified.DateTypeRange.startDate` in `Calendar.specialDates`) | 🔶 | keep ISO strings in the ABAP model and convert **at the point of use**: `\|\{ path: 'START_AT', formatter: 'Formatter.DateCreateObject' \}\|` with `core:require="\{Formatter: 'z2ui5/model/formatter'\}"` (POST_171). A plain string binding is ❌ — view creation crashes (`Date must be a JavaScript or UI5Date date object`). A **standard date/time/datetime binding type with no `source`** (`type:'sap.ui.model.type.Time'`, model = a JS Date) is ❌ the same way — but adding a **`source` formatOption** (`formatOptions:{ source:{ pattern:'HH:mm:ss' } }`) makes the model a **parseable string** and the type revives it → 🔶 ✅ (the abap2UI5 equivalent of the Date-object model; app 182 `TypeTimeAsTime`, render-smoke green). A type that already carries a `source` (app 181 `TypeDateAsString`, `source:{pattern:'yyyy-MM-dd'}`) needs no change. Model-level auto-revival of `utclong` fields was **deliberately rejected**: it would silently retype every timestamp field (a plain `\{CREATED_AT\}` Text would render JS `toString()`), so conversion stays per-binding. String date properties (`DatePicker.value` + `valueFormat`) take model strings directly and need none of this. **An OPTIONAL date in a bound row needs a guard**: one template cannot omit an attribute per row, and a formatter binding over an empty field yields `new Date('')` = Invalid Date — which is *truthy*, so consumers that branch on the property (`Month._checkDateEnabled` → `CalendarDate.fromLocalJSDate`) throw and the whole view dies. Write the conversion as an expression binding instead — `` `{= ${END} ? Formatter.DateCreateObject(${END}) : null }` `` in a **backtick** literal so the braces reach the attribute — which keeps the property genuinely `null` for the empty row | headless probe `scripts/probes/date-object-probe.mjs` (2026-07-18): string ❌ crash / Date ✅ / formatter ✅ render identically; beta sample `z2ui5_cl_smp_app_456` (abap2UI5/samples); demokit apps 108 (PlanningCalendar, **live-verified 2026-07-27**) + 109 (SinglePlanningCalendar) render this way, render-smoke green (the `xmlns:core` declaration must accompany `core:require`); the optional-date guard is app 220 (`sap.ui.unified.CalendarMinMax`), probe-verified 2026-07-28 in `scripts/probes/calendar-empty-enddate-probe.mjs` — unguarded renders **0** calendar days and throws, guarded renders all 42 with `endDate` `null` |
| Binding `sorter` (no grouping) | ✅ | keep the original binding-info string: `\|\{ path: '{ client->_bind( val = t path = abap_true ) }', sorter: \{ path: 'COL' \} \}\|` — no ABAP SORT, no deviation needed | apps 011/033/048 converted 2026-07-17; same pass-through as the group-sorter row below |
| Binding `sorter` with `group: true` + default group headers | ✅ | keep a raw binding-info string `{path: '…', sorter: {path: '…', group: true}}` — get the bare model path via `client->_bind( val = t_products path = abap_true )` (human-taught 2026-07-16); only a *custom* `groupHeaderFactory` is out | source-verified on BOTH sides: abap2UI5 passes attribute values to `XMLView.create` unmangled, and UI5's default group header IS `new SeparatorItem({text: group.text \|\| group.key})` (openui5 `ComboBoxBase.addItemGroup`); app 039 converted 2026-07-16, live-verified 2026-07-20 |
| Standard composite binding **types** (`sap.ui.model.type.Currency`, Date/Number types) | ✅ | keep the original binding-info string 1:1 — `\|\{ parts:[\{path:'PRICE'\},\{path:'CURRENCY_CODE'\}], type:'sap.ui.model.type.Currency', formatOptions:\{showMeasure:false\} \}\|` — over a numeric ABAP field (`price TYPE p`); the type is a client-side standard resolved on the frontend, and abap2UI5 passes the attribute value to `XMLView.create` unmangled (the same pass-through as the sorter rows) | apps 033/022 converted 2026-07-17, live-verified 2026-07-19 (033) and 2026-07-20 (022). App 041 keeps a static single-record resolution for an unrelated reason ({/ProductCollection/0}), not because the type is inexpressible |
| Controller-applied binding filter/sort (`oList.getBinding('items').filter(new Filter(...))`, `.sort(...)`) | ✅ | `client->follow_up_action( val = cs_event-binding_call t_arg = id/aggregation/'filter'-or-'sort'/path/operator/value… )` after a backend event, or roundtrip-free by WIRING the same call in the view (`v = client->follow_up_action( val = cs_event-binding_call t_arg = id/aggregation/method/params… )`) with a `${$parameters>/…}` value resolved client-side. Whitelisted methods + filter operators, built from data (no code strings); empty filter value clears the filter. The model stays untouched — selection on filtered-out rows survives, unlike ABAP-side data filtering. **Compound filters since 2026-07-20** (pr/binding-call-compound-filters): a single `filter` param that is a JSON array of groups — each group a list of `["PATH","OP","value1","value2"?]` rows — builds OR inside each group and AND across the groups (`new Filter([...], bAnd)`, the FacetFilter/ViewSettingsDialog multi-facet shape); a model path can never start with `[`, so the positional single-filter form is unchanged; an empty groups array clears | implemented 2026-07-18 (pr/binding-call), compound groups 2026-07-20; source-verified in `app/webapp/core/FrontendAction.js` `evBindingCall`/`buildFilterGroups`; beta samples `z2ui5_cl_smp_app_454`/`_455` (abap2UI5/samples, LIVE-TEST pending); app 022 uses the compound form (live-verified 2026-07-20 - its checked note names the compound binding_call filter as one of the things the live run exercised). **A DECLARATIVE bound filter set needs no action at all** (apps 264/265, 2026-07-31): the `boundFilters` binding-info parameter (@since 1.146) takes filter values that are binding expressions, so the aggregation re-filters itself whenever a bound value changes — write it into the raw `rows`/`items` binding-info string with `client->_bind( … )` as `value1` (absolute) or a relative row field (`'{REGION}'`, per-row filtering). When the controller SWAPS the whole filter set (`oListBinding.filter(aOther, FilterType.ApplicationBound)`), redraw from the backend with the other `boundFilters` list baked in (app 264, the 241 re-bake idiom) instead of reaching for `binding_call` |
| Custom JS formatter **functions** (`Formatter.js`, a `formatter: '.fn'` controller method) | 🔶 | the framework's **curated formatter module** (standard app layout `model/formatter.js`, a served script, CSP-clean) is a **marshalling layer, not a formatting toolbox**, and it exports exactly four functions: `DateCreateObject`, `DateAbapDateToDateObject`, `DateAbapDateTimeToDateObject` (a real JS `Date` for an object-typed property - JSON has no date type) and `expandInlineIcons` (an icon-font glyph of the loaded theme). For those, wire it like the original wires its formatter - `core:require="\{Formatter: 'z2ui5/model/formatter'\}"` on the view root (UI5 ≥ 1.74, declare POST_171) with `formatter: 'Formatter.<fn>'`; pre-1.74 use the published global `z2ui5.Formatter.<fn>`. **For anything else the answer is ABAP**: compute it in `model_init` and bind the finished field (`state="{WEIGHT_STATE}"`). An expression binding is the fallback only for genuinely presentational composition, never for a threshold or a classification. **Do not ask for a new curated function** - the module's admission criteria (one value only; frontend-only for a technical reason; no domain vocabulary) are machine-enforced by abap2UI5's `check_formatter_scope` gate, and the set has already been cut twice: `weightState`/`weightStateByValue` (parseFloat + KG conversion + thresholds) went 2026-07-22, and `round2DP`, `dimensions`, `stockStatusState`, `stockStatusIcon`, `deliveryStatusState` went 2026-08-04 - rounding, joining and status→visual lookups are all work ABAP finishes. A binding naming a non-curated function fails `pattern-lint` (`uncurated-formatter`); UI5 would otherwise resolve it to nothing and render the cell blank with no error. **Never via runtime-compiled JS strings** - an eval-based `register_formatter` was rejected 2026-07-18 for security (needs unsafe-eval, invites building code from data). Factories returning controls (`groupHeaderFactory`, item factories) are ❌ either way | apps 009/010/022/092 compute the ObjectNumber weight state in ABAP (`WEIGHT_STATE`, thin-frontend principle) instead of the original's frontend `Formatter.weightState`; app 062 uses `Formatter.expandInlineIcons` for the inline-icon MessageStrip; source-verified in abap2UI5 `app/webapp/model/formatter.js` + `.github/scripts/formatter-scope-gate.mjs`; samples `z2ui5_cl_smp_app_450` (abap2UI5/samples; the date-string helpers) and `453` (the same table computed in ABAP instead) |
| `sap.m.MessageView` / `MessageItem` / `MessagePopover` | ✅ | bind the messages as a plain ABAP table on the `items` aggregation with a `MessageItem` template (`client->_bind( t_messages )`) - `type`/`title`/`subtitle`/`description`/`groupName` map to columns; grouping, Dialog and Popover hosting all work | demo-kit `MessagePopover` family ported 2026-07-20, which proves the full set incl. grouping + MessagePopover: app 066 (base — plain `/T_MESSAGES` table, `MessageItem` `link` aggregation, precomputed highest-severity button icon/type/count, `toggleBy` open/close), app 067 (async variant — same + `markupDescription` HTML messages; the JS `setAsyncURLHandler` URL gate is dropped/IMPROVISED) |
| MessageManager auto-collection (the global `message>` model gathering **control validation** messages client-side) | ✅ | since 2026-07-18 (pr/message-model, beta) every view slot carries the central message model as `message>` and is registered with `handleValidation`: bind `items="\{message>/\}"` 1:1 (MessagePopover family), and binding **type**/constraint errors on `_bind` paths collect/clear automatically incl. the control's `valueState` — no app code, no roundtrip. For static message sets, driving a plain ABAP table stays fine (app 038) | probe-verified headless (invalid Integer input → message + valueState, valid input clears) |
| **App-authored** messages (the controller's `MessageManager.addMessages`/`removeMessages` with custom text + target) | ✅ | since 2026-07-20 (the `z2ui5.cc.MessageManager` companion control): declare `xmlns:z2ui5="z2ui5.cc"` + `<z2ui5:MessageManager items="\{/T_MESSAGES\}"/>` and drive a two-way bound ABAP message table (`message`/`type`/`target`/`additionaltext`/`description`); the cc reconciles it into the message manager — new rows added as `sap.ui.core.message.Message` with the view's model as processor (sets the bound field's `valueState`), the control's own rows removed when they drop out, auto-collected validation left untouched. A MessagePopover on `{message>/}` shows both. Read-back rides the same two-way binding | app 065 (`MessagePopoverMessageHandling`); source-verified in abap2UI5 `app/webapp/cc/MessageManager.js` (companion pattern like `MultiInputExt`), unit-tested; **live-verified 2026-07-27** (app 065) |
| **Smart controls** (`sap.ui.comp`: SmartForm/SmartField, SmartFilterBar+SmartTable, SmartVariantManagement, SmartChart) driven by **OData V2 metadata** | ✅ | **not portable in this repo** - `sap.ui.comp` ships with SAPUI5 only, so it is outside the OpenUI5-only scope (AGENTS §3). The technique itself works: build the view like any other builder chain (the `smartForm`/`smartField`/`smartFilterBar`/`smartTable`/`smartVariantManagement`/`smartChart`/`navpopover` namespace prefixes) and point the **default model at the service** instead of feeding ABAP data: `client->view_display( val = view->stringify( ) switch_default_model_path = c_odata_service )`. Name a **real** service - the ports use the Gateway demo service `/sap/opu/odata/IWBEP/GWSAMPLE_BASIC/` (`ProductSet`, activate once in `/IWFND/MAINT_SERVICE`) - never an invented `Z…_SRV` path. Columns, filter fields, value helps, chart dimensions and personalization all come from the service metadata, so there is no `model_init`. A controller `bindElement("/Products('4711')")` becomes a declarative `binding` attribute; that OData entity path is the one absolute path not derived from `_bind` (pattern-lint exempts it). **Variant management needs one extra line** ✅ live-verified: a page variant never gets `setPersControler( )` (`addPersonalizableControl( )` returns early for `isPageVariant( )`), so a controller-less app has neither the anchor nor the control promise `initialise( )` requires - saving dies inside `sap.ui.fl` and stored views never load. Since 2026-07-28 the framework does the handshake: `follow_up_action( cs_event-smart_variant_init, t_arg = SmartVariantManagement id + personalizable control id )`. A **SmartTable on a service without a `UI.LineItem` annotation renders zero columns** - name `initiallyVisibleFields` | the five ports moved to abap2UI5/samples on 2026-07-29 (`z2ui5_cl_smps_app_475`-`_479` (abap2UI5/samples-stack `src/02`), the SAPUI5 Smart Controls tutorial); `_478` saves views and shows them again after a restart; `_479` (SmartChart) needs an **analytical** service (`sap:aggregation-role` + `UI.Chart`) and carries an explicit placeholder. Framework: `z2ui5_if_client~view_display` `switch_default_model_path`; the older curated samples `z2ui5_cl_smps_app_313`/`_319` (abap2UI5/samples-stack) show the same switch with SmartTable and SmartMultiInput |

## Events

| UI5 feature | Status | How in abap2UI5 | Evidence |
|---|---|---|---|
| Controller event handlers | ✅ | `client->_event( val = 'NAME' … )` + `check_on_event( )` CASE branches | all interactive ports |
| Passing event/source values to the backend | ✅ | `$`-prefixed `t_arg` forms: `${COL}`, `$event.oSource.sId`, `${$parameters>/value}` — never a bare `{COL}` | source-verified on both sides: abap2UI5 passes `$`/`{`-prefixed args raw (z2ui5_cl_ui5_srv_event `get_t_arg`), and UI5's `EventHandlerResolver` parses `${…}` via `BindingParser.parseExpression` — a bare `{COL}` is no binding there; apps 005, 053 |
| Boolean event parameters | 🔶 | arrive as `abap_bool` (`X`/space) — map back to `true`/`false` when echoing to the UI | source-verified (z2ui5_cl_ui5_handler `request_parse_event_args`); app 007 (correct), app 008 fixed |
| **A CONTROL-valued event parameter, or a whole ARRAY of them** (`ViewSettingsDialog.confirm` → `filterItems`/`sortItem`, `SelectDialog.confirm` → `selectedItems`, `SinglePlanningCalendar.selectedDatesChange` → a `DateRange` list) | ✅ (Date properties: ❌) | pass it like any other arg — `` `${$parameters>/selectedItems}` `` — and the frontend marshals each control into an object carrying its `ID` plus its public **properties** (`Lib.normalizeEventArgs`); `get_event_arg( )` returns the JSON, read with `z2ui5_cl_ajson`. **This is the loop the expression grammar does not have**: the array travels whole, so no per-entry map is needed. Three rules. **(a) Map corresponding fields only** — the payload carries *every* public property, so `parse( )->to_abap_corresponding_only( )->to_abap( )`, or a plain `to_abap( )` aborts on the first field your structure lacks. **(b) Never a display string** — not `filterString`, which is localized and no contract. **(c) A `Date`-typed property does NOT survive**: `JSON.stringify` writes it through `toISOString()`, so a control holding LOCAL midnight arrives as the PREVIOUS day everywhere east of Greenwich (measured, see the probe) — for those keep per-index expression args that format the local parts on the client. Note a binding **Context** is not a control and is not projected: `selectedContexts` would be handed to `JSON.stringify` untouched and its model graph is circular, which takes the whole round-trip body down — use the `selectedItems` twin | apps 298 (`filterItems`/`sortItem`) and 103 (`selectedItems` → the `You have chosen …` toast, 2026-08-23); the Date boundary holds apps 307 and 109 on their per-index wires — measured in `scripts/probes/control-valued-event-arg-probe.mjs` (candidate `dateRange-array`: local 2018-07-09 → `2018-07-08T22:00:00.000Z` at Europe/Berlin, correct in UTC and west of it), filed as `event-arg-date-utc-shift` in abap2UI5's `backlog/` |
| Reading control-internal state via `$parameters>/…/mProperties/…` | ❌ | private UI5 internals, fragile across patches — restructure to a two-way binding or a public parameter instead | apps 022/047/003 were all migrated off it (2026-07-16); pattern-lint blocks any new use |
| **Keyboard shortcuts / `sap.ui.core.CommandExecution`** (declarative `commands` manifest block, `<core:CommandExecution command="Save" execute=".onSave"/>`, `press="cmd:Save"`, `{$cmd>Save/enabled}`) | ✅ | `client->follow_up_action( val = cs_event-keyboard_shortcut t_arg = VALUE #( ( `Ctrl+S` ) ( `SAVE` ) ) )` registers a combo → named-backend-event binding (UI5 spelling, `ctrl/shift/alt/meta` any order, aliases `cmd/command/option/control`; suppresses the browser default; re-register rebinds, empty event unregisters; registrations die with the app switch — implemented upstream 2026-07-29, #2482). Rewire every `press="cmd:X"` button to the same backend event so shortcut + button share one command path, gate the command server-side on its enabled/visible flags (a disabled `CommandExecution` swallows the command), and model `$cmd>…/enabled\|visible` as two-way bound booleans. The `core:CommandExecution` elements themselves still render nothing (declare the dropped controls). **Scoped since 2026-08-06** (pr/keyboard-shortcut-scope): an optional THIRD `t_arg` is the view slot the registration belongs to (`client->cs_view-popover`), and dispatch takes the innermost OPEN scope - so a popover-local command shadows the page-level one for the same combination while the popover is up, the way a `CommandExecution` in a Popover's `dependents` does, and the unscoped one applies again once it closes. Register the combination twice, once per command. The unscoped form is unchanged, unregistering is per scope | app 232 (`Commands`); source-verified `FrontendAction.js` `evKeyboardShortcut` (pr/core-commandexecution-keyboard-shortcuts implemented) |
| **Conditional `oEvent.preventDefault()`** (a handler cancels the event's built-in client default at runtime — `sap.tnt` `NavigationListItem.press` gating selection/href on app state) | ✅ | `client->_event( val = 'X' s_ctrl = VALUE #( check_prevent_default = flag ) )` — the wire binds to `eBP`, which calls `oEvent.preventDefault()` synchronously and then round-trips like `eB`; the event + args still reach the backend. The flag is baked into the handler at render time, so a condition that flips between renders needs a redraw (wire the deciding control's change/select to an event that calls `view_display( )`) — declare the added wire. **For a veto that differs PER FIRING of the same wire** — one column of a table blocked while the others resize — use `s_ctrl-prevent_default_expr` instead (since 2026-08-06, pr/conditional-prevent-default): a client expression evaluated when the event fires (`` `${$parameters>/column}.getId().indexOf('COL_DATE') >= 0` ``), resolved like every other `$`-prefixed event argument. It wins over the flag when both are set, and the event is sent either way — the backend still decides what happens instead | app 241 (`SideNavigationPressEvent`); source-verified `View1.controller.js` `eBP` / `z2ui5_cl_ui5_srv_event` (pr/event-prevent-default implemented upstream) |
| **Event args derived from runtime layout/overflow measurement** (`AvatarGroup` `overflowButtonPressed`/`avatarsDisplayed`, `OverflowToolbar` overflow set — "how many fit / overflowed at render time") | ❌ | the backend never sees DOM geometry, so overflow-conditional controller logic (Individual→Group redirect, avatar-subset slicing) is inexpressible — drop it, declare IMPROVISED. Hard boundary like the arbitrary-depth TreeTable row; no server-side fix feasible | surfaced by `sap.f.AvatarGroup` (out of scope @1.73, not ported); applies to any in-scope overflow control |
| Controller-read list selection (`getSelectedItems`, FacetFilter/List multi-select) | ✅ | bind `selected="{FLAG}"` / `selectedKey` two-way — the incoming model is applied to the app object BEFORE `on_event` runs, so handlers read post-interaction state; clearing server-side resets the client (the push is automatic; `view_model_update` has been an empty method since 1.143.0) | source-verified: `factory_by_frontend` applies `model_json_parse` before recording the event (z2ui5_cl_ui5_action:55-77); apps 022/047 (both live-verified 2026-07-20; 022's checked note names two-way facet selection as one of the things the live run exercised) |
| Opening external URLs (`URLHelper.redirect`) / tel / sms / mailto | ✅ | `client->follow_up_action( client->cs_event-urlhelper, t_arg = ( `REDIRECT` ) ( \|\{ URL: '…', NEW_WINDOW: true \}\| ) )` — sub-action `REDIRECT`/`TRIGGER_EMAIL` take a `{ … }` object-literal arg, `TRIGGER_TEL`/`TRIGGER_SMS` a `\|\{ TEL: '…' \}\|` object literal too - **the key is `TEL` for both**, and a plain string does NOT work: the handler reads `params.TEL` off `args[2]`, so a primitive yields `undefined` and `formatTel` returns `''` (corrected 2026-08-23; app 084 had been wired the wrong way and its live check could not have seen a number). **Not `open_new_tab`** — that is same-origin-only (`isValidRedirectURL`), so it cannot open external sites or `tel:`/`mailto:` | apps 084, 041, 073; **live-verified 2026-07-27 for email and redirect only** — app 084's tel and sms wires were the broken plain-string form until 2026-08-23, so that run cannot have seen a number on either; both need a fresh check |
| Client-side-only behaviour with no backend effect | ✅ | `client->follow_up_action( … )` frontend actions | overview app popup close |
| Post-render actions (focus, scroll, …) | ✅ | `client->follow_up_action( val = client->cs_event-… )` — queued to run AFTER the response is rendered, so the DOM exists | source-verified (z2ui5_cl_ui5_client:43-55) |
| Imperative one-shot control methods with no binding equivalent (`open [pageKey]`, `close`, `setExpanded`, `scrollToIndex`, `scrollTo`, `focus`, NavContainer `to [transitionName]`/`back`, SplitApp/SplitContainer `toDetail`/`toMaster [pageId]`/`backDetail`/`backMaster`/`setMode [SplitAppMode]`, QuickView/QuickViewCard `navigateBack`, Wizard `goToStep`/`discardProgress`/`setNextStep`, `openBy [anchorId]`, `toggleBy [anchorId]`, Carousel `setActivePage`, Tree `expandToLevel`/`collapseAll`, `sap.m.Table` `setHiddenInPopin [JSON Priority array]`, `sap.ui.core.Control` `addStyleClass`/`removeStyleClass`/`toggleStyleClass [className]`) | ✅ | `client->follow_up_action( val = cs_event-control_by_id t_arg = id/method/params… )` (view is the separate `view` parameter, default `cs_view-main`) — whitelisted per method with arg-kind casting (`bool` takes the ABAP `X`/space, `domRef` resolves a control id to its DOM element with control fallback, `object` `JSON.parse`s the arg), runs client-side after render; `open`/`close`/`setExpanded` added 2026-07-18 (pr/control-call-whitelist), optional args for `to`/`open` + `goToStep` 2026-07-19 (pr/control-method-args), `openBy`/`setActivePage` 2026-07-20 (pr/control-methods-openby-setactivepage), `toggleBy` (open-if-closed / close-if-open, no server-side open state) 2026-07-20 (pr/menu-toggle-openby), `setHiddenInPopin` 2026-07-22 (pr/table-hidden-in-popin — app 092, hide columns by importance), `addStyleClass`/`removeStyleClass`/`toggleStyleClass` 2026-07-22 (pr/style-class-toggle — the client-side equivalent of the controller's `oControl.toggleStyleClass('x')`; motivated by app 013), `toDetail`/`toMaster`/`backDetail`/`backMaster`/`setMode` + `navigateBack` 2026-07-22 (pr/split-container-nav — apps 096/097 SplitContainer/SplitApp master-detail, apps 099/100 QuickView(Card) page back). **Arguments beyond the method's declared kinds are still silently dropped** — check `CONTROL_METHODS` BEFORE wiring a parametrized call; a needed-but-unlisted arg is a declared deviation + pr/ request, never a LIVE_TEST hope (probe apps 609/624, 2026-07-19). Aggregation-template **clones** (bound pages/items) are reachable **since 2026-08-06** (pr/aggregation-item-address): wherever an argument takes a control id it also takes `` `<id>/<aggregation>/<index>` `` (`carousel/pages/2`, 0-based), the equivalent of `oCarousel.setActivePage( oCarousel.getPages()[ i ] )`. Their ids are deterministic (`<templateId>-<parentId>-<index>`, measured stable across a model refresh AND a reorder) but embed the view prefix the framework assigns at runtime, so the BACKEND cannot spell one — resolution happens on the client. An out-of-range index or a single-valued aggregation is logged rather than silently passed as `undefined` | apps 044 (`open`), 016 (`openBy` on the hidden DatePicker), 060 (`toggleBy` — Menu press-to-toggle, roundtrip-free, wired in the view); SplitContainer `toDetail`/`toMaster`/`backDetail`/`backMaster`/`setMode` **live-verified 2026-07-27** (app 096); apps 013 and 044 live-verified 2026-07-20; rest LIVE-TEST pending; source-verified in `app/webapp/core/FrontendAction.js` `CONTROL_METHODS` |
| Drag & drop reorder (`dnd:DragDropInfo`, controller reorders the model on `drop`) | ✅ | declarative dnd config in the view; the drop event ships dragged/drop index and position via client-side resolved `$`-args (`${$parameters>/draggedControl/oParent}.indexOfItem(${$parameters>/draggedControl})`, `${$parameters>/dropPosition}`), ABAP reorders the table and the changed model travels back with the response | shipped samples in abap2UI5/samples: 307 (GridList, 01/08/02) and 459 (sap.m.Table incl. `dropPosition=Between`, 01/02); view structure probe-verified |
| Timers / polling | ✅ | `cs_event-start_timer` (callbackEvent, delayMs) — a tick that falls into a running round-trip needs no flag: the timer holds its own single slot and re-fires once the response has rendered (`afterRoundtrip`), so the poll chain stays alive without aborting the request in flight | source-verified (`app/webapp/core/actions/ViewOps.js`, `evStartTimer`) |
| Rich event payloads beyond strings | ✅ | `_event( r_data = ref )` snapshots ABAP data at render time; read back via `client->get( )-r_event_data` | source-verified (core_client:404-414) |
| Element-bind a popup/popover slot to a table row (`oPopover.bindElement(ctx.getPath())`, so the fragment's relative bindings resolve against the pressed row) | ✅ | `client->follow_up_action( val = cs_event-bind_element view = cs_view-popup/-popover t_arg = VALUE #( ( idx ) ( client->_bind( tab ) ) ) )` — `BIND_ELEMENT` strips the braces off the `_bind` path and element-binds the slot view to `<path>/<idx>`; the row index comes from `$event.oSource.getBindingContext().getPath().split('/').pop()`. Binding the table via `_bind` (not a literal path) keeps it rename-safe. Added 2026-07-22 (pr/popover-bind-element) | app 094 (LIVE-TEST pending); source-verified in `app/webapp/core/FrontendAction.js` `evBindElement` + 3 node tests |
| Clearing an **association** (`setSelectedSection(null)`, `setSelectedItem(null)`) | ✅ | an association cannot be data-bound, so it travels as a control-method call: `follow_up_action( control_by_id, ( id ) ( setSelectedSection ) ( `` ) )` — an EMPTY argument arrives as `null` (the `controlIdOrNull` kind; the framework pads it even though the wire drops a trailing empty) | app 263; upstream `pr/control-method-null-arg` (2026-08-05) |
| **Screen-reader announcement** of a backend-driven content change | ✅ | `control_global INVISIBLE_MESSAGE / announce / <text> / Polite|Assertive` — `sap.ui.core.InvisibleMessage` is a singleton with no control id, so this global target is the only route to an ARIA live announcement. Lazy-required (@since 1.78) | app 289; upstream `pr/invisible-message-announce` (2026-08-05) |
| **Custom currency codes / digit counts** for a BOUND `sap.ui.model.type.Currency` | ✅ | `control_global FORMATTING / setCustomCurrencies / <JSON>` (or `addCustomCurrencies / <JSON>`) — a global config, not a control property, and out of reach of a per-binding formatter. Lazy-required (@since 1.120). **Not** for `sap.ui.unified.Currency`: that control caches its `NumberFormat` in `init( )` and has no localization-change hook, so a call arriving after the render cannot reach it — the model type does implement one and re-formats. Until 2026-08-23 the framework asked for `sap/ui/core/Formatting`, a module UI5 does not have, so nothing happened at all | upstream `pr/custom-currency-formatting` (2026-08-05), module id corrected 2026-08-23; app 196 is the sample that does NOT show it working |

## Frontend-action catalog (source-verified 2026-07-16)

`client->follow_up_action( client->cs_event-… )` supports far
more than the ports use so far — relevant when a sample's controller does
browser things: `popup_close`, `popover_close`, `open_new_tab`,
`location_reload`, `set_focus`, `scroll_to`,
`scroll_into_view`, `set_title(_launchpad)`, `clipboard_copy`,
`download_b64_file`, `urlhelper` (redirect / email / sms / tel),
`store_data` (session/local storage), `play_audio`, `start_timer`,
`set_size_limit`, `set_odata_model`, nav-container
`*_nav_container_to` per view slot, `z2ui5` (call registered custom JS).
Gone from `cs_event` and not to be looked for: `history_back` (1.143.0 -
`history.back()` through `follow_up_action` or `nav_app_leave( )`),
`display_message_box` / `display_message_toast` (the MessageBox/MessageToast
options object rides the `control_global` `MESSAGE_BOX` / `MESSAGE_TOAST`
call below, 1:1) and `wizard_set_next_step` (two `control_by_id` calls,
`discardProgress` then `setNextStep`).
Newest (branch, pending release): the generic **`cs_event-control_global`** /
**`cs_event-control_by_id`** — call a *whitelisted* method on a global object
(MessageToast, MessageBox, BusyIndicator, Theming, and since 2026-08-02
**`POPUP.setWithinArea`** — confine every popup to the control whose id is
passed instead of to the window, an empty argument releases it again; needs
UI5 >= 1.89, pr/popup-within-area implemented, app 285. Note the ordering: a
follow-up action runs AFTER the popup of the same round-trip has opened, so
the area has to be set on an earlier round-trip) or on a control resolved
by id (`to [transitionName]`, `back`, `focus`, `scrollToIndex`, `scrollTo`,
since 2026-07-18 also `open [pageKey]`, `close`, `setExpanded` —
pr/control-call-whitelist —, since 2026-07-19 `goToStep` plus the
optional `to`/`open` args — pr/control-method-args — and since
2026-07-20 `openBy [anchorId→domRef]` + `setActivePage` —
pr/control-methods-openby-setactivepage; since 2026-07-27 the `openBy`
dispatch **falls back to `open(false, anchor, 'begin top', 'begin bottom',
anchor)`** when the control has no own `openBy` — `sap.ui.unified.Menu`'s
`open(kbd, opener, my, at, of)` signature — so the same `openBy` wire covers
that family too (pr/unified-menu-open-anchored implemented, apps 227/228;
keyboard flag + explicit dock constants stay dropped),
client-side after render, without adding a dedicated action constant per case.
Since 2026-07-22 a `control_global` single-string method (MessageToast.show,
MessageBox.*) also **composes its text from a template + client-resolved args**
(pr/message-toast-format): the first `t_arg` after the method is a template and
its `{0}`,`{1}`,… placeholders are filled by the following values (each a
`$event.*` / `${$parameters>/…}` expression resolved on the frontend), so a
dynamic "Action triggered on item: X" toast is roundtrip-free — 1:1 with the
demo-kit controllers that do `MessageToast.show("…" + evt.getParameter(…))`
(apps 005, 060). A lone string is unchanged.
**An arg is a FULL UI5 expression, not just a path** (proven 2026-07-31 by the
`pr/menu-item-selected-path` investigation): `EventHandlerResolver` hands the
*entire* handler string to `BindingParser.parseExpression`
(`sap/ui/core/mvc/EventHandlerResolver.js`), so an arg may mix embedded bindings
with the whole expression-binding grammar — method calls, `isA('…')`, string
concatenation, ternaries. Use it for anything the client can compute from the
event without a round-trip.
**Measured 2026-08-05 — what the arg grammar actually supports**
(`scripts/probes/event-arg-expression-probe.mjs`, real OpenUI5, one candidate
per shape): indexed access into an array-valued getter
(`$event.oSource.getSelectedDates()[0].getStartDate()`), indexed access into an
array *parameter* (`${$parameters>/tokens}[0].getKey()`), chained calls,
arithmetic (`getMonth() + 1`), `.join( ',' )` over an array parameter and a
class-name ternary all resolve. Transport LOCAL date parts rather than
`toISOString( )` — the latter shifts the day for any user east of Greenwich —
and guard an indexed access (`…getSelectedDates().length > 0 ? … : 0`), which
doubles as the original's "nothing selected" branch. **Boundary: no DOUBLE
quote** — the handler rides in a double-quoted XML attribute, so an expression
containing `"` breaks the view; a message that wraps a value in quotes puts them
in the client-composed TEMPLATE (or composes in ABAP), not in the expression.
Proving ports: 139/151/177/220 (the clicked day), 228 (a class branch), 186
(array joins), 108 (a whole composed message incl. `\n`).
**Boundary — a menu-item ancestor breadcrumb is NOT transportable** (measured
in a browser on OpenUI5 1.152, and against the transpiled backend): the
expression grammar has **no loop**, and `sap.m.Menu` re-parents its items
through an internal `sap.m.MenuWrapper` whose depth changes with the runtime
state — the parent `MenuItem` is **two** hops up while the submenu is closed
(`MenuItem → MenuWrapper → MenuItem`) and **four** once the submenu popover
exists (`MenuItem → MenuWrapper → Popover → ResponsivePopover → MenuItem`). A
fixed-hop walk therefore works only in one of the two states (it did toast
`Create New Site > Official Store` when the event was fired programmatically,
and the leaf text when a user clicked through). The same wrapper breaks the demo
kit sample's own `while (oItem instanceof MenuItem) … getParent()` loop, so
**upstream now toasts the leaf text too** — apps 060/061 keep
`${$parameters>/item}.getText()`, which is behaviour-identical with the live
sample, and the 060 e2e interaction asserts exactly that.

The sibling **`cs_event-binding_call`** (pr/binding-call) applies a
declarative filter/sorter to an aggregation binding. All three are scheduled
via `follow_up_action` with positional `t_arg` (`control_by_id`:
id/method/params — the view is the separate `view` parameter, default
`cs_view-main`, pass `cs_view-popup`/… for a popup/popover control;
`control_global`: object/method/params, `binding_call`:
id/aggregation/method/params), or roundtrip-free by wiring the same call
where its result is consumed (the interim `control_call*`/`binding_call_by_id`
wrapper methods were consolidated into `follow_up_action`, 2026-07-19; the
`_event_client` alias for the view-wired form is obsolete since 2026-08-12).
Also available: nested view slots (`nest_view_display`), `popover_display(
xml = … by_id = … )` anchored to any control — note the XML parameter is
named **`xml`**, unlike `popup_display( val )`; guessing `val` by analogy
was the single biggest first-try error of the 2026-07-19 hold-out probe
(3 of 25 apps, does not compile; pattern-lint gates it now), app-stack navigation with typed results
(`nav_app_call/leave` + `get_app_prev`), and bundled custom controls
(`z2ui5.cc`: Timer, Storage, Focus, Geolocation, History, Tree,
FileUploader, CameraPicture, MultiInputExt / SmartMultiInputExt,
UploadSetExt, …). Details: the abap2UI5/abap2UI5 sources
`z2ui5_if_client` + `z2ui5_cl_ui5f_frontact_js`.
