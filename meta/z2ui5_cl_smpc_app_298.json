{
  "class": "z2ui5_cl_smpc_app_298",
  "sample": "sap.m.sample.TableViewSettingsDialog",
  "entity": "sap.m.Table",
  "file": "src/02/01/z2ui5_cl_smpc_app_298.clas.abap",
  "batch": "b24",
  "audit": {
    "frontend_action": true,
    "event_t_arg": true
  },
  "status": "reviewed",
  "checked": null,
  "deviations": [
    {
      "type": "POST_171",
      "what": "The whole column-header menu is newer than the 1.71 floor: sap.m.table.columnmenu.Menu, QuickSort, QuickSortItem and ActionItem are @since 1.110, QuickResize is @since 1.137, and the sap.m.Column headerMenu association that points at it is @since 1.98.0. The sample's ColumnMenu fragment is nothing but those controls, so a faithful port cannot avoid them - the app needs a UI5 release >= 1.137 for the menu, the rest of the table renders on 1.71."
    },
    {
      "type": "NOTE",
      "what": "All four controller-loaded fragments (ColumnMenu, SortDialog, GroupDialog, FilterDialog) are declared in the view's mvc:dependents aggregation and opened 1:1 via follow_up_action( cs_event-control_by_id, <id> open ). The three ViewSettingsDialogs are anonymous in the original (the controller keeps them in a map keyed by fragment name); the port gives each the id it is opened by - sortDialog, groupDialog, filterDialog."
    },
    {
      "type": "NOTE",
      "what": "The confirm handlers read the selected ViewSettingsItem CONTROLS (${$parameters>/sortItem}, ${$parameters>/groupItem}, ${$parameters>/filterItems}) rather than a display string: the frontend marshals each control into its id plus its public properties, so the backend gets the real keys. That is the same information the original's mParams.sortItem.getKey() / oItem.getKey() reads."
    },
    {
      "type": "NOTE",
      "what": "Thin frontend: sorting, filtering and the filter-bar text are computed in ABAP over the model table instead of through sap.ui.model.Sorter/Filter on the items binding. The filter item keys keep the original's own encoding (WeightMeasure___LE___1000___X - field, operator, value1, value2 separated by three underscores), which the port splits exactly like the controller's handleFilterDialogConfirm does. The infoToolbar visible flag and the filter label are bound instead of set imperatively on vsdFilterBar / vsdFilterLabel."
    },
    {
      "type": "NOTE",
      "what": "weightState is computed in ABAP rather than in a frontend formatter (thin frontend). **Corrected 2026-08-23**: this sample's own Formatter.js takes ONE argument and compares the RAW WeightMeasure - < 0 None, < 1000 Success, < 2000 Warning, else Error - and the view binds a single part, so the unit never reaches it. On 2026-08-21 the KG rule of a DIFFERENT sample was imported here (sap.m.Table / app 009, whose formatter takes measure AND unit, with boundaries 1 and 5 KG). Every one of the 123 mock rows is below 1000, so the original renders 123x Success while the port rendered 57 Success / 45 Warning / 21 Error - 66 rows wrong, and the note that justified it had the truth inverted. App 377 keeps the KG rule and is right to: its sample's own model/formatter.js IS the two-argument KG one."
    },
    {
      "type": "NOTE",
      "what": "The QuickSort change wire reads ${$parameters>/item} and asks it for getKey( ) and getSortOrder( ), which is what the sample's own onSortChange does. The event's METADATA declares key and sortOrder instead, and QuickSort.onChange fires neither - it does fireChange({ item: oItem }) and nothing else (QuickSort.js:85). An earlier draft used the declared names because the view gate's event-parameter check rejects `item`; that check is wrong here, both arguments then arrived EMPTY on every firing, and the handler's fallback sorted Name ascending whichever column was clicked. The gate finding is carried as a deliberate unknown-event-parameter budget entry with that reasoning - satisfying the rule would mean breaking the port. sortOrder None is handled the way oBinding.sort( ) with no argument behaves: the rows go back to model order."
    },
    {
      "type": "IMPROVISED",
      "what": "Grouping loses its group headers. The original's mGroupFunctions build a sap.ui.model.Sorter group key per row - a plain supplier name, and for Price a bucketed '100 EUR or less' / 'Between 100 and 1000 EUR' / 'More than 1000 EUR' label. abap2UI5 has no client-side grouping function, so the port sorts the table by the chosen field instead: the rows end up in the same order, but without the grey group header rows. The reset button restores model order, which is what the original's resetGroupDialog plus oBinding.sort( ) amounts to; until 2026-08-21 it only cleared a field that GROUP_CONFIRM reassigns before reading, so it changed nothing observable."
    },
    {
      "type": "IMPROVISED",
      "what": "The context-menu ToggleButton reports its state as a toast instead of attaching a sap.m.Menu as the table's contextMenu aggregation. The original's onToggleContextMenu builds the Menu imperatively in the controller and swaps it in and out; the aggregation itself is not driven by any bindable property, and the sample's menu carries no behaviour beyond opening."
    },
    {
      "type": "NOTE",
      "what": "Device.system.desktop adds sapUiSizeCompact to each dialog in the original. The port drops that: abap2UI5 exposes the device data as {device>/system/desktop}, but a style class is not a bindable property, and the density is a global app decision rather than a per-dialog one here."
    },
    {
      "type": "NOTE",
      "what": "The payload is read by hand - the frontend marshals each control into an object of its ID plus all its public properties, and this port walks that object for the fields it models. abap2UI5 releases no JSON reader and the vendored ajson copy is framework-internal (the linter's non-released-api rule reports it, correctly), so a targeted reader is the intended shape here; abap2UI5/samples Z2UI5_CL_SMP_APP_197 and _327 are the same pattern. It reads what the FRAMEWORK wrote, which is flat, and does not resolve escapes - a payload composed from free user input would need that."
    },
    {
      "type": "NOTE",
      "what": "onResize's oColumn.setWidth( width + 'px' ) is reproduced through a bound Column.width: the QuickResize change event carries ${$parameters>/width} and the backend writes the CSS size. An unset value is a valid CSSSize (the type's 0* branch matches the empty string), so the column keeps its automatic width until the first drag, as in the original. Until 2026-08-21 this wire round-tripped only to show a 'Column resized' toast the original does not have, behind a comment claiming onResize merely logs."
    },
    {
      "type": "IMPROVISED",
      "what": "onBeforeColumnMenuOpen is dropped, and the Menu.beforeOpen attribute that wired it with it. It does oQuickResize.setWidth( parseInt( getComputedStyle( oColumn.getDomRef( ) ).width ) ) - it reads the column's RENDERED width out of the DOM so the resize control opens showing the current size. A rendered pixel width has no server-side equivalent, and the round-trip that used to be wired here did nothing at all with it, so the QuickResize opens at its own default instead. The port previously kept the wire as an empty RETURN branch, beside a comment saying the original 'only inspects the opener'."
    },
    {
      "type": "NOTE",
      "what": "**e2e-caught 2026-08-22 on app 571 (this port has no interaction module of its own; the generic boot-and-render gate is all that runs for it)**: the sort ran but the table came back in its original order. The cause is the transpiled backend: `SORT <itab> BY (field)` - the DYNAMIC component form - loses its BY clause entirely (the emitted JS is `abap.statements.sort(t, {})`), so every sort was a no-op. The component is named statically per field now, in a CASE. Apps 298, 362 and 571 all carried the dynamic form and all three are fixed."
    }
  ]
}
