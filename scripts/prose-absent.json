{
  "z2ui5_cl_app": "The optional abstract app base class PROPOSED in docs/upstream-requests.md and declined by the maintainer on 2026-08-11 (row app-lifecycle-base-class). It was never written: an abap2UI5 app implements z2ui5_if_app and nothing else. The row exists so the decision is not re-proposed, and it has to name the thing it declined. Deliberately absent: the class does not exist and must not.",
  "z2ui5_cl_demo_app_453": "A beta sample that demonstrated the curated formatter module (row formatter-demokit-pack, 2026-07-18) while the feature was being built upstream. Named in a dated, past-tense 'Implemented' row, which records what proved the capability at the time. abap2UI5 ships no z2ui5_cl_demo_app_* class today, so this is history, not drift - the same reason docs/history.md is excluded wholesale.",
  "z2ui5_cl_demo_app_454": "A beta sample that demonstrated declarative binding_call filter/sort (row binding-call, 2026-07-18) while the feature was being built upstream. Named in a dated, past-tense 'Implemented' row, which records what proved the capability at the time. abap2UI5 ships no z2ui5_cl_demo_app_* class today, so this is history, not drift - the same reason docs/history.md is excluded wholesale.",
  "z2ui5_cl_demo_app_458": "A beta sample that demonstrated the central UI5 message model as message> (row message-model, 2026-07-18) while the feature was being built upstream. Named in a dated, past-tense 'Implemented' row, which records what proved the capability at the time. abap2UI5 ships no z2ui5_cl_demo_app_* class today, so this is history, not drift - the same reason docs/history.md is excluded wholesale.",
  "z2ui5_cl_smpc_app_overview": "The overview app's name before the 2026-08 rename to z2ui5_cl_smpc_app_000. It is named in the PAST TENSE twice: three times in docs/history.md, in journal entries written while it was still called that (a journal records what a class was called when the entry was written, which is history and not drift), and once in STATUS.md's open finding about check-prose-names.mjs, which cites those three occurrences as the MEASUREMENT that docs/history.md must stay outside the gate when docs/ is brought into it. Deliberately absent: the class exists nowhere and must not.",
  "z2ui5_cl_smpc_mock": "The shared mock-data provider built and reverted on 2026-09-12. It held the demo kit's ProductCollection once and 58 ports projected it instead of inlining their own literal - which cost every one of them the property AGENTS §3 exists for: a sample is ONE snippet a reader copies into a system in one piece. AGENTS §3 and docs/history.md name it in the past tense so the deduplication is not proposed a second time; pattern-lint's standalone-class rule now fails any class that names another class of this repository. Deliberately absent: the class does not exist and must not."
}
